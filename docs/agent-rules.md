# pyFP3D Agent Rules

Current phase: **P13 ◐ (G13.1 ✓ + G13.2 conforming ✓ + **G13.3 subsonic ✓** 2026-07-13; G13.2 level-set clause now scoped as **Track B B8** (row-blend tip taper, NEW 2026-07-13, user-approved) + G13.3 transonic RAN → NEGATIVE (round FINE never reaches M0.84 — ramp dies at M=0.75; site = the sharp tip TE, which the round cap AMPLIFIES not creates; P9-band verdict NOT earned either geometry)) + Track M **M5 ✓ (rounded tip cap)** + **M2 ◐ (WING-BODY MESH ✓ 2026-07-13)** + M1b ✓ (mesh ladder) + P9 CLOSED 2026-07-11 + Track B — B1 ✓ B2 ✓ B3 ✓ B4 ✓ B5 ✓ **B7 ✓** (renumber 2026-07-13: new B8 = LS tip-taper, old B8 multi-wake → B9, old B9 curved wake → B10) + **B8 ◐ RAN → NEGATIVE: the row-blend taper is IMPLEMENTED (bit-identical by default, 13 tests, B-suite 59/0) and behaves exactly as its model says (q=4.73 ≫ criterion 1, perfectly local) BUT DOES NOT bound the LS tip edge (p +1.34 → +1.37); the LS tip singularity is the SHEET TERMINATION / spanwise clip (peak = a `beyond_tip` element at z/b 1.012, normal volume, same cell tapered or not), NOT circulation ⇒ finding (8)'s premise is wrong, B8 needs a RE-SPEC (user arbitration)** + **B8 RE-SPEC ROUND 2026-07-14 RAN (diagnosis-first) → TWO findings: (1) ★ the committed LS p=+1.34 was a ×5 METRIC ARTIFACT (`element_mach2` reads mixed-side plain/beyond-tip cells from the aux-substituted side field; their assembly is MAIN-dof — elem 93977 side 1.532 vs main 0.309); the HONEST exponent is +0.62/+0.37-no-sliver = the SAME OBJECT as conforming +0.52 (G13.1's LS-magnitude comparison needs an erratum; fix shipped OPT-IN `element_mach2(mixed_plain="main")`, default bit-identical since B6/B7 M_max locks read through it). (2) the honest residual object = the termination ring's FINITE jump |δ|≈0.026 (h- and TE-taper-INDEPENDENT — decoupled from Γ_last, which is why the TE blend measured nothing). The SPAN BLEND of the wake-LS rows (`MultivaluedOperator(span_blend=…)`, default None bit-identical, 11 tests, B-suite 116/9) WELDS the ring (0.026→0.0003) but is NEGATIVE ON LOCALITY: ~20% GLOBAL lift loss, uniform in z, r_blend-insensitive, h-GROWING (its flat p at rb0.08 is confounded, corrected ~+0.15); isolation: straddler weld alone −13.3%, inboard blend alone −10.8% ⇒ ★ the implicit Kutta has NO per-station target — ANY sheet-side δ-pin re-levels the global Γ mode ~10× (the conforming secant holds the same F(z) at −1.6%). Both constraint-side routes now measured dead; any further cure must change the FUNCTION SPACE at the termination. Demos `run_b8_termination_diagnosis.py` + `run_b8_span_blend.py` (8/8); roadmap B8 re-spec block. ★ ARBITRATED 2026-07-14 (user): **B8 CLOSED as CHARACTERIZED-NOT-CURED** — the honest exponent is the same object/magnitude as the conforming +0.52 every closed gate lives with ⇒ **B9 UNBLOCKED**; backlog (recorded, not scheduled) = mixed_plain default flip (+B6/B7 M_max re-reads + G13.1 LS-exponent erratum) + the `element_densities` mixed-plain junk-weight fix; function-space termination re-spec only if a future gate needs the LS tip edge bounded**; next = **B9 (wing-body LS solve, M∞0.5)** or the M0.84 round-ladder sub-problem (G13.3 transonic).

★ **WING-BODY EARLY-VALIDATION PUSH (user-directed 2026-07-13).** Goal: a simplified wing-body model + mesh (Track M) validated on the **level-set** path. It maps onto three ALREADY-SCOPED gates — **M2** (wing-body mesh), **B8** (LS tip taper), **B9** (wing-body/multi-wake solve) — not a new track. **User-arbitrated scope: SUBSONIC M∞0.5 ONLY** (M0.84 excluded: on the *wing alone* the round-cap M0.84 fine is still a non-converged limit-cycle, and the round cap makes transonic *worse* at fine — a wing-body transonic run would compound an unsolved singularity with new geometry). **User-directed order: M2 (done) ‖ B8 → then B9.** Current: **M2 mesh ✓** (see Track M M2), **B8 ✓ CLOSED 2026-07-14 (characterized-not-cured, user-arbitrated)** — the honest LS tip exponent (+0.62/+0.37) is the SAME OBJECT as the conforming one (+0.52), which the conforming path lives with at every closed gate ⇒ **B9 = NEXT**.

Next = **B9 (wing-body LS solve, M∞0.5)**. Also open, independent: the round-tip TRANSONIC tip-suction sub-problem (G13.3 transonic; fine over-cap cells pinned by `run_g133_roundtip_transonic_locate.py`).

★ **TRACK M / M2 — THE WING-BODY MESH IS DELIVERED (2026-07-13).** `meshgen/fuselage.py` + `meshgen/wingbody.py`; **`wing3d.py` is BYTE-UNTOUCHED** (the loft, the M5 round cap and `_collect_3d` are imported and reused ⇒ every M1/M4/M5 family stays bit-identical — verified). The family is **WAKE-FREE** (nothing fragmented or embedded, no `wake` group), which is exactly what M2's own 2026-07-10 note prescribed ("schedule with Track B's embedded wake — the wake–fuselage intersection is what the conforming sheet handles worst"); the solver builds the wake from the analytic TE polyline via `WakeLevelSet`. **(1) ★ The fuselage is ONE splined body of revolution, NOT four fused primitives** — fusing nose+cylinder+cone+tail leaves a C0 seam at each junction, and a surface crease is a spurious edge feature (the flat-tip-cap problem M5 removed). **Gate metric = the FUSELAGE CREASE ANGLE** (the M5 argument applied to the body — the solver only ever sees the TRIANGULATION): p99 **20.6° → 11.2°, q ≈ −0.88 ⇒ O(h)**, median **3.60° → 0.80°** ⇒ **DECAYS** ⇒ faceting of a smooth surface, no seam in the limit (vs M5's flat cap, a real edge: 91.9° → 92.1°, q = −0.00, *does not move*). **(2) ★ `junction_z = r_f` is EXACT** (the M6 root chord lies wholly inside the constant-radius section, and the TE is at y=0 with zero TE thickness), and **BOTH ends of the LS TE polyline are exact wall nodes**: tip TE corner **0.0** (the M5 cap radius vanishes at the TE ⇒ the fuselage fuse moved nothing) and the junction end **1.5e-9** (an OCC vertex), identically at coarse and medium. **(3) ★ The LS path ingests it and the wake terminates at the junction FOR FREE** — `CutElementMap`: 1,415 cut elements, **76 TE nodes spanning z ∈ [0.1500, 1.1963]** = junction → tip, **ZERO inboard of the junction**: B1's spanwise clip (`0 ≤ q ≤ span_length`) excludes the fuselage region with **no new code**; TE nodes are identical whether `wall_nodes` is wing-only or wing+fuselage ⇒ the fuselage does not contaminate the TE set. **(4)** Self-similar ladder (no `h_far` clamp — the M1b defect): coarse **59,211** / medium **502,457** tets (ratio 8.49 ≈ 2³); quality inside the M1 bounds (dihedral 5.65°/6.34°, aspect 11.6/6.4). Evidence: `cases/meshes/onera_m6_wingbody/` (stats CSVs + inspection PNG committed; `.msh` gitignored), `tests/test_m2_wingbody.py` **13 passed**. **OPEN for B9:** the B4 TE control volume is *wall-adjacent* (`multivalued.py::_build_te_control_volumes`), so the innermost TE node's fan touches fuselage wall faces — verify the upper/lower CVs there take only wing-side elements.

★ **P13/G13.3 SUBSONIC ✓ — ROUNDING THE TIP CAP (M5) RESTORES 3D GRID CONVERGENCE (2026-07-13).** The flat tip cap was the third singularity (box p=+0.321, on the WALL). On the round ladder (demo `run_g133_roundtip.py`, **9/9**, M∞0.5, A/B vs flat, tip taper both, 0 lim/flr all 6 levels): the **cap-surface exponent falls +0.327 (flat) → +0.091 (round, bounded)**, tip minus design-sharp LE/TE is **converged (p=−0.006)**, and ★ **the three-point Richardson G9.1 could NEVER run is EARNED** — cl_KJ 0.2159→0.2073→0.2055, increments shrink (−3.95%, −0.88%), **observed order p=2.31, cl_KJ(h→0)=0.2050** (flat ladder non-monotone 0.2015→0.2005→0.2121 ⇒ no Richardson). ★ **METRIC TRAP (G13.1 finding 6):** the *broad* G13.1 box still "diverges" (p=+0.38) but its max MIGRATED to the **design-sharp tip TE** (fine peak chord-frac 0.999, both families, carries Kutta) — integrable, doesn't spoil integrated lift. ★★ **G13.3 TRANSONIC RAN → NEGATIVE (2026-07-13/14, demo `run_g133_roundtip_transonic.py`, 5/5 + `..._locate.py`).** M0.84/α3.06 on the ROUND ladder: coarse+medium **REACH M0.84** and are clean (M_max 1.51/2.00, 0 over cap; cl_KJ flatnorm 0.2769→0.2763), **but the FINE mesh NEVER REACHES M0.84 — its Mach ramp DIES AT M=0.75** (last converged 0.7375; dm-halves below dm_min, 1 cell floored) ⇒ **no M0.84 fine state exists**, only 2 of 3 points, **no Richardson, P9 band NOT EARNED.** ★★ **SITE = the SHARP TIP TE; the round cap does NOT create it, it HEATS it.** The committed FLAT M0.84 (39e1ded, same taper) *completes* the ramp at the same level and converges (M_max 2.818, 0 over cap, 0.2866) — which seems to exonerate the shared TE, but does not: the locate rerun puts **20/20 fastest cells ON the sharp tip TE** (z/b 0.97–0.99, chord-frac ≈0.998) and **0 on the cap surface** (z/b>1). The rounded tip lets flow **wrap around and accelerate**, raising velocity at that same pre-existing TE at EVERY level (M_max 1.51/2.00 vs flat 1.39/1.73); at fine it crosses the limiter. **⇒ the cap AMPLIFIED the tip-TE singularity, it did not add one.** ⚠ **METHOD ERRATUM (self-caught, important):** `solve_newton_transonic` returns the **FAILED level's state** (at THAT level's Mach, `converged=False`). An earlier version of this demo censused it at `M_INF=0.84` — **the WRONG freestream Mach** — and produced a spurious "M_max 3.97 / 5 over M_cap / cl_KJ 0.2415 @ M0.84". **RETRACTED.** `solve()` now records `target_reached`/`m_final`/`m_last_converged` and **refuses to census a state whose ramp did not reach the target** (census columns = `n/a`). **P9 band NOT EARNED on EITHER geometry:** flat converges but 0.2593→0.2652→0.2866 RISES (non-asymptotic, clamped ladder ⇒ single point, not Richardson); round has no third point. ⇒ **item (9)'s "0.019 gap = resolution ⇒ P11 refuted" has no clean asymptotic basis on either geometry.**

★ **THE TIP CAP IS ROUNDED — Track M / M5 CLOSED 2026-07-13.** `meshgen/wing3d.py::onera_m6_wing_mesh(tip_cap="round")` (default `"flat"` ⇒ **all existing families bit-identical**, every P5/P8-G8.2/B7/M1 lock untouched) closes the wing with the **half body of revolution swept by the tip section about its own chord line** — an OCC revolve of the tip section's upper half-face about an edge OF that face, fused onto the loft. ★ **The cap radius vanishes at the LE and TE**, so the cap degenerates to a POINT at each ⇒ the **TE line, the wake sheet, the tip TE corner, the Kutta stations and B_SEMI are all UNCHANGED** — only the tip WALL moves, which is what makes the A/B against M1 controlled and why **no solver-side plumbing was needed**. New family `cases/meshes/onera_m6_roundtip/` (self-similar ladder from the start, no h_far clamp; **`h_tip = 0.25·h_wall`** — the cap radius is only 22 mm, so at `h_wall` the coarse cap would be ~one element wide and would **discretize back into a flat one**). ★ **GATE METRIC = the SEAM CREASE ANGLE** (`post/surface.py::wall_crease_angles`; the solver only ever sees the TRIANGULATION, so "the CAD is round" is not the claim that matters). Three-level ladder, p99: **flat 91.9° → 92.1° → 92.1°, q = −0.00** (*does not move* = a real edge; its MEDIAN crease is 0.00° ⇒ the metric reads the edge and nothing else) vs **round 46.8° → 24.5° → 13.7°, q = −0.92** ⇒ **O(h)** faceting of a smooth surface ⇒ no edge in the limit. (Round MAX decays slower — 46.8→25.0→18.1, q=−0.68 — one worst facet at the thin end of the seam window where facet size is set by h_edge, not h_tip; both DECAY, which is the claim.) Cost ×1.29/×1.28/×1.30 tets (59,359 / 448,197 / 3,257,273), level-independent. Tests `tests/test_m5_round_tip.py` (19); demo `cases/demo/m5_round_tip/` (**9/9**). Roadmap Track M M5.

✓ **THE M0.84 FLAT-CAP CLAIM IS NOW EVIDENCED (was prose only; re-run + committed 2026-07-13, commit 39e1ded).** The audit found item (9)'s M0.84 numbers existed only as text; they were re-run from scratch and **reproduce to 4 digits** — demo `cases/demo/p13_tip_edge_singularity/run_g132_transonic.py` (5/5 PASS), artifact `results/g132_transonic.csv`: cl_KJ **0.2593 / 0.2652 / 0.2866**, cl_p 0.2534 / 0.2608 / 0.2835, M_max **1.394 / 1.725 / 2.818**, all 0 over M_cap / 0 lim / 0 flr / converged (census = G9.1's own, strict A/B, only `tip_taper` changed). ⇒ item (9)'s "genuine discrete solution" and "cl_KJ reaches 0.2866" are real. **BUT the P9-band VERDICT is not closed by this**, and the parallel M5 work above is right about why: this is the **FLAT-cap** sequence, which is **not asymptotic** (the flat tip cap still diverges — G13.3), so 0.2866 is a REPORTED single-point value, not a Richardson extrapolation. The definitive P9 transonic verdict wants the **M0.84 Richardson on the ROUND ladder** (in flight). ★ **The rerun also caught the recipe trap that lost the original evidence:** the fine mesh (~450k dofs) must use `precond="amg"` + tight EW forcing (η=1e-8) + `m_start=0.30, n_picard_seed=12`; the medium recipe's `precond="direct"` is P9's 4h39m/26GB splu trap (see [[m6-fine-needs-amg-not-direct]]).

★ **P13/G13.2 — THE TIP SINGULARITY IS FIXED (conforming path, 2026-07-13).** Fix = a **spanwise loading taper** `Γ_eff(z) = F(z)·Γ_Kutta(z)` on the per-station Kutta target (`constraints/wake.py::tip_taper_factors`; `solve_newton_lifting(tip_taper=…)`, default `None` = **bit-identical**; reaches `solve_newton_transonic` via `newton_kw` — no plumbing change). **Shipped model (user-arbitrated): `vanish_smooth` (smoothstep, COMPACT support), r_c = 0.05·b_semi.** Tests `tests/test_p13_tip_taper.py` (15); demos `run_taper_probe.py` + `run_taper_physics.py`. **(1) ★ THE MECHANISM IS DISCRETE — NOT roll-up, NOT a vortex core, NOT the continuum edge exponent** (both earlier framings in design.md §4.1 were wrong and are now superseded): the solver never sees the continuum edge, it sees the **outermost TE station**, which sheds its retained `Γ_last` as a CONCENTRATED VORTEX over the last cell (free-edge nodes are single-valued ⇒ the jump falls to 0 in ONE element), inducing `~Γ_last/h`. With `Γ_last ~ h^q` ⇒ **p ≈ 1 − q**, criterion **q ≥ 1**. Predicts the baseline exactly (measured q=0.44 since Γ~√u, u_last~h ⇒ Γ_last~√h; p_pred 0.56 vs p_meas 0.52). **(2) ★ The taper is AMPLIFIED, not applied:** Γ is a fixed point of `Γ=F·Γ_Kutta(Γ)` and the Kutta map slope is **b≈0.93** (P2) ⇒ `Γ/Γ* = F(1−b)/(1−Fb)` — F=0.8 gives **0.21×**, not 0.8× (test-locked). Explains why measured q≈3.3 ≫ naive; and why r_c must stay small. **(3) MEASURED:** edge peak (strict OFF-BODY box) **0.712→0.981→1.510 (p=+0.592)** untapered vs **0.567→0.565→0.570 (p=+0.009)** tapered; **★ the M6 FINE mesh is now a GENUINE DISCRETE SOLUTION (converged, 0 lim / 0 flr / 0 NaN)** — exactly what G9.1 could NOT achieve. Transonic **M0.84**: coarse/medium converge 0-limited, medium **M_max 2.13 → 1.725** (P5's "bounded tip-TE-corner overshoot" was the same object). **(4) COST:** cl_KJ **−1.1…−1.6%** (scales with r_c: −0.70/−1.58/−3.27% at 0.03/0.05/0.08 b), and it is **LOCAL** — Γ(η)/cl(η)/Cp UNCHANGED inboard of η≈0.95 (inboard circulation −0.51%), TE closure at η=0.90 stays at baseline (0.232 vs 0.218). The taper also makes cl **more** mesh-convergent than the untapered baseline (+0.2% vs +0.7%). **(5) ★ The tanh form (F(tip)=½) DOES regularize (q≈1.00, exactly the marginal case) but is DISQUALIFIED for UNBOUNDED SUPPORT** — tanh never reaches 1, so it unloads to η=0.77 (57/83 stations): −7.4% lift, −4.9% inboard circulation, and it **BREAKS TE pressure closure at η=0.90** (gap 0.972 vs baseline 0.218) where there is no singularity to fix. A tip model must be LOCAL. **(6) ★ METRIC TRAP (cost a wrong conclusion once):** G13.1's tip box (z/b>0.95, dx≥−0.05) admits WING cells — once the edge is regularized its max MIGRATES onto the wing suction peak at z/b≈0.95, making a WORKING fix look like it made p worse. Measure the edge OFF-BODY (dx>0, z/b>0.98). **(7) Pre-existing artifacts CONFIRMED taper-independent** (user-flagged): the Cp **sawtooth** = the P6/G6.1 **wall-gradient RECOVERY** artifact (`smooth_passes`, default 0; reversals 40→2; raw count IDENTICAL across all tip models); the **Γ(z) jitter** = conforming **Kutta-probe placement** on the unstructured swept TE (P5 item; RMS d² 0.042 vs P5's 0.097; the LS path is 11–12× smoother, B7); the **baseline TE Cp gap 0.14–0.22** = the conforming **potential-jump** Kutta (design.md 4.4) being only an approximation of true pressure equality + the sharp-TE P1 floor (this is why B4 needed the explicit nonlinear |q_u|²=|q_l|² Kutta). **(8) OPEN — level-set clause: NOT a mechanical port.** The LS path has **no Γ DOF** and its TE row (`te_kutta_coo`) is **HOMOGENEOUS** (`s·(q_u−q_l)=0`) ⇒ scaling it by F is a **NO-OP**. Clean analogue = BLEND the pressure-equality row with B2's continuity weld: `F·K̂ + (1−F)·Ŵ = 0`. That is a *different model* from `Γ=F·Γ_Kutta` ⇒ needs its own r_c calibration, and the two-path A/B becomes a PHYSICS comparison, not a model-identity check. **SCOPED AS Track B B8 (NEW 2026-07-13) — IMPLEMENTED, AND THE ANSWER IS *NO*: ★★ THE ROW BLEND DOES NOT FIX THE LS TIP EDGE, BECAUSE FINDING (8)'s PREMISE IS WRONG.** Demo `cases/demo/b8_tip_taper_ls/` (**12/12**, M6 coarse+medium, M0.5, `upwind_c=0`); tests `tests/test_b8_tip_taper_ls.py` (13). The blend (`te_kutta_coo(weights=F)` + new `te_weld_coo`; `assemble_matrix(tip_taper=…)`; threaded through `solve_multivalued_lifting`/`_transonic`/`solve_multivalued_newton`; default None ⇒ **bit-identical**, B-suite 59/0) behaves EXACTLY as its model predicts — converges 0 lim/flr at every r_c, **unloads the tip circulation far past the criterion (Γ_last ~ h^q, q = 4.73 vs criterion q ≥ 1)**, and is **perfectly LOCAL** (inboard Γ +0.01%, cl_KJ +0.03%) — **and the tip-edge peak STILL DIVERGES**: p = **+1.341** untapered → **+1.37/+1.41/+1.58/+1.37** tapered (bigger r_c is WORSE). **(a) G13.2's discrete `p ≈ 1−q` does NOT transfer** (q=4.73 yet p=+1.37, not −3.73) ⇒ **killing Γ_last does not kill the peak**. **(b) The lift cost is ~0 (+0.03% vs conforming's −1.74%) because THERE IS NOTHING TO UNLOAD** — the LS **implicit Kutta already drives Γ(tip)→0 emergently** (B7: ±3e-4); the conforming path needs the taper only because its free-edge rule leaves Γ_last ~ √h (q=0.44). **The LS path never had that disease.** **(c) ★ MECHANISM:** the peak cell is **OUTBOARD of the geometric tip** (z/b=**1.0118**, dx=+0.061), a **`beyond_tip` element — one the SPANWISE CLIP refuses to cut** (`cut_elements.py`: crossings need `q ≤ span_length`) — the **SAME element tapered or not** (elem 93977) — and **NOT a small-cut sliver** (V **0.71×** median, not even a cut element ⇒ CutFEM small-cut instability RULED OUT). ⇒ **the LS tip singularity lives in how the embedded sheet TERMINATES, not in the circulation it sheds.** (Untapered p=+1.341 reproduces G13.1's LS exponent 1.34 exactly ⇒ the metric is right.) ⇒ **THE TWO PATHS' TIP SINGULARITIES ARE DIFFERENT OBJECTS.** The shipped machinery is correct/tested/bit-identical-by-default but is **not the cure**; **B8 needs a RE-SPEC aimed at the sheet TERMINATION (spanwise clip / beyond-tip zone) — USER ARBITRATION REQUIRED.** ★ **COST BOUNDARY (recorded):** the LS path has **NO `precond` option** — `solve_multivalued_lifting` is hardcoded to sparse-direct `spsolve` (deliberate B2 decision; GMRES+AMG is the deferred B3+ scaling path). M6 **medium = 484 s/solve** at 67,426 extended dofs (~1.2 GB RSS); **M6 fine (~450k dofs) would hit the same splu wall P9 hit, with NO AMG escape hatch** ⇒ B8's demo is coarse+medium **by necessity**, and any fine-mesh LS work needs the deferred GMRES+AMG path first (see [[m6-fine-needs-amg-not-direct]] — same disease, different path). **(9) ★★ TRANSONIC M0.84 — G9.1's EXACT SCENARIO, CLEARED, AND THE 0.019 GAP IS RESOLUTION.** Where G9.1's fine mesh was a limit-cycle ARTIFACT (unlimited M_max **7.93**, 9 cells over M_cap=3, permanently limited, its cl_KJ 0.2393 declared "not a lift"), the tapered fine solve is a **GENUINE DISCRETE SOLUTION: M_max 2.818, 0 cells over M_cap, 0 lim, 0 flr, converged** (44.6 min; **RE-RUN + COMMITTED 2026-07-13, commit 39e1ded / `run_g132_transonic.py` 5/5 / `g132_transonic.csv` — reproduces to 4 digits**). cl_KJ **0.2593 → 0.2652 → 0.2866** (coarse/medium/fine) vs Tranair/KRATOS **0.288** — and the taper *removes* ~1.5%, so the untapered equivalent is ≈**0.291**. ⇒ fires P9's PRE-REGISTERED **≥0.283 ⇒ resolution-dominated** branch ⇒ **the 0.019 gap is RESOLUTION, not a geometry floor, and P11's LIFT case is REFUTED WITH A REAL SOLUTION (not by inference).** P11 is now down to **G1.6 alone**. **(10) ★ MESH-FAMILY DEFECT (invalidates EVERY past M6 3-pt Richardson, incl. G9.1's, independent of any physics):** `generate_onera_m6.py` derives all sizes from h_wall but **CLAMPS the far field — `h_far = min(2.5, 120·h_wall)`** — so coarse gets 2.5 instead of the self-similar 3.6: coarse→medium refines the far field only **1.39×** vs the wall's **2×** (medium→fine is a clean 2×). **`coarse` was NOT on the refinement ray.** This also explains the coarse→medium spanwise "slosh" (−6.3% root vs +5.4% mid, near-cancelling in total) — it was comparing two different mesh families, not measuring convergence. **★ FIXED 2026-07-13 (Track M M1b):** `_level_params(clamp_h_far=True)` default keeps coarse/medium/fine **bit-identical** (all locks untouched) + a new **`coarse_ss`** level recut without the clamp (h_far 2.5→3.6, 46,067 tets) ⇒ **`RICHARDSON_LADDER = (coarse_ss, medium, fine)` refines by exactly 2.000 in every length scale**, locked by 3 tests in `tests/test_m1_onera_m6.py` (16). **(11) ★ The growth is REAL FIELD RESOLUTION — the Kutta-probe hypothesis was raised and REFUTED.** Decisive test: probe-based `cl_KJ` vs **probe-FREE pressure-integrated `cl_p`** across the 3 meshes — **BOTH rise with growing increments** (cl_KJ 0.2001→0.2005→0.2121 = +0.19%/+5.76%; cl_p 0.1875→0.1939→0.2091 = +3.41%/+7.87%), and probe geometry is healthy (`probe_d/h` constant 0.48–0.49 ⇒ probes scale correctly with h). ⇒ the conforming Kutta extraction is **EXONERATED**, and **the level-set port is NOT the route to G13.3** (it stays a parity/robustness item; B7's smoother Γ(z) is NOT load-bearing for 3D accuracy). **(12) ★ BONUS — the P5-era V6 item behaves exactly as re-specced:** the V6 gap (cl_p below cl_KJ) converges at ~**O(h)** — **6.30% → 3.29% → 1.41%** (ratios 1.9, 2.3) — a real O(h) floor that REFINES AWAY, now **1.41% on fine**, closing on the <1% target deferred since P5/G5.2. **(13) ★★ THE MESH LADDER IS FIXED AND THE SEQUENCE IS STILL NOT ASYMPTOTIC — BECAUSE THERE IS A THIRD SINGULARITY, AND IT IS ON THE WALL.** On the self-similar ladder the increments still GROW (M0.5: cl_KJ 0.2015 → 0.2005 → 0.2121; cl_p 0.1888 → 0.1939 → 0.2091, whose increments grow ~3× while h halves) — the signature of a singularity still being resolved. A three-region box study (demo `run_g133_ladder.py`, **5/5**, artifacts `g133_boxstudy.csv`/`g133_ladder.csv`/`g133_ladder.png`) localizes it: **tip-cap edge (WALL) 0.662 → 0.824 → 1.015, p = +0.321 ⇒ DIVERGES**, while **wake free edge** (the G13.2 fix) 0.536 → 0.565 → 0.570, p = +0.045 ⇒ **bounded** and **wing p99 (control)** 0.642 → 0.628 → 0.629, p = −0.014 ⇒ **converged**. **ROOT CAUSE = a documented DELIBERATE GEOMETRY SIMPLIFICATION:** `meshgen/wing3d.py` builds a **FLAT tip cap** where the real ONERA M6 has a **ROUNDED** one; a flat cap meets the upper/lower surfaces at a sharp convex edge = a potential-flow edge singularity of exactly the kind P13 exists to remove, **only on the BODY rather than the wake**. **This is NOT a P11 (curved wall ELEMENT) problem** — isoparametric elements cannot regularize a genuinely sharp geometric EDGE; the GEOMETRY is wrong. ⇒ **NEXT = round the tip cap** (Track M), regenerate the ladder, then redo the Richardson. **HONEST: still no Richardson** — the sequence is not asymptotic, so the *direction* is established but the *extrapolated number* is not earned (P9/G9.3 discipline: report `n/a`, never fabricate). **So three distinct defects blocked 3D grid convergence: (1) wake free tip edge (p=0.59) → FIXED by G13.2; (2) the h_far mesh-ladder clamp → FIXED by Track M M1b; (3) the flat tip-cap wall edge (p=+0.32) → OPEN.**

★ **P13/G13.1 CLOSED 2026-07-13 (tip / wake-edge singularity — characterization)** — NEW Track-P phase (user-approved; direct descendant of P9/G9.1; appended, no renumber). Demo `cases/demo/p13_tip_edge_singularity/` **10/10 PASS** (probed SUBSONICALLY, M∞0.5, no transonic machinery). roadmap.md Track P P13 + design.md §4.1. **(1) The singularity is 1/√r (flat-plate-edge), NOT the committed docs' "1/r":** conforming three-point (coarse/medium/fine 55.5k/350.7k/2.51M tets) log-log exponent of tip-box peak Mach vs 1/h = **p 0.59 ∈ [0.4,0.65]** (peak 0.712→0.981→1.510; a 1/r line vortex ⇒ p=1). **(2) Driver = trailing vorticity dΓ/dz, NOT bound Γ:** Γ→0 at the tip (necessary-not-sufficient), but |dΓ/dz| is ~10× larger at the tip than mid-span (B7's smooth Γ(z)) — the *unloading rate* is what a terminating flat sheet cannot regularize. **(3) MODEL not representation:** the edge diverges on BOTH conforming (×1.38) and level-set (×2.28) paths on the SAME mesh (LS exponent 1.34 ≥ conforming 0.52); tip-box p95/mean stay FLAT (0.573→0.562→0.525 / ~0.49) while max diverges (localized-edge signature); peak AFT of the TE in the chord plane ⇒ not a wall feature (P11 cannot fix it). **(4) ★ The conforming FINE M0.5 solve does NOT converge** (limited/floored, ~1.4k NaN) — the tip singularity trips the limiter even subsonically = the M0.5 analogue of G9.1. **G13.2 (future) = the wake-MODEL fix** (roll-up / explicit tip vortex ⇒ bounded tip peak p→0 + M6 fine a real discrete solution), implementation handed to **Track B as a rescope of the shelved B9**; **G13.3 (future)** = redo the M6 3D Richardson under the P9 bands. This corrects roadmap :1036 / demo_report :1333 "1/r"→"1/√r" and adds the dΓ/dz mechanism. No solver/numerics change; fine meshes gitignored; the ~34 min conforming fine M0.5 AMG solve is one-shot cached.

★ **B7 CLOSED 2026-07-12 (ONERA M6 3D gate, dual-mesh, first try)** — roadmap B7 + design_track_b.md §11; demo `cases/demo/b7_onera_m6/` **35/35 PASS**; `tests/test_b7_onera_m6.py` (6 fast + 5 gated). M∞0.84/α3.06 coarse, `farfield="neumann"`, ramp 0.60→0.84 @ dm 0.04. **M1 embedded** cl_KJ 0.2765 / shocks 0.635/0.588/0.449 / Γ 0.1076→−0.0003 / M_max 1.453 / **0 lim,flr** (22.7 min); **M4 wake-free** cl_KJ 0.2710 / 0.634/0.584/0.454 / 0.1055→+0.0003 / 1.368 / **0 lim,flr** (18.4 min); V6 1.77%/1.97%; dual-mesh A/B 2.0%. (1) ★ **The B6 lift INVERSION reproduces in 3D:** vs the conforming **NEWTON** truth (cl_KJ 0.2692 — the B6-arbitrated baseline) the LS Picard is **+2.7% (M1) / +0.7% (M4)** while the conforming **Picard** (P5, 0.24788) is **−8.6%** — no early-stoppable Γ outer exists on the LS path (implicit Kutta ⇒ Γ is a solution mode), so gating on P5 would penalise the B path for being CLOSER to the truth; **the wake-free workflow mesh M4 is the more accurate of the two.** (2) ★ **3D far field = `neumann`, and the P5 Γ(z) taper is structurally UNNECESSARY on the B path** (not merely unimplemented): the B-path vortex is a SPAN-UNIFORM 2D vortex whose branch cut is the ray y=0,x>0 at EVERY z, which misfires two independent ways, both measured as a spurious near-sonic spot at the OUTLET where the sheet exits (M∞0.5 outlet M_max) — (a) the α-aimed sheet is not coplanar with that cut (B3's rule in 3D; **0.958** vs neumann **0.513**), (b) even re-aimed coplanar one scalar Γ cannot match Γ(z)→0 and there is no cut beyond the tip (P5's branch-ray artifact; **0.825**) — and neumann carries NO vortex ⇒ neither can exist. (3) ★ **Γ(z) is spanwise-SMOOTH with NO smoothing applied** (unplanned): normalised RMS 2nd difference 0.0079/0.0091 vs conforming P5's 0.0970 = **11–12× smoother** — the conforming path runs a SEPARATE SECANT PER TE STATION (the st133 machinery; `INVESTIGATION_gamma_smoothing.md` failed to smooth its jitter away), while the implicit Kutta has no per-station loop (Γ is ONE solution mode) ⇒ the P5 spanwise-Γ problem is **structurally impossible** here, not merely fixed. (4) ★ **The 3D-only machinery needed NO new solver code** — B1's oblique (v,d̂,n̂) frame + spanwise clip held; Γ(tip)→~3e-4 **discretely** (the LS analogue of the conforming free-edge rule). The only gap was post-processing: `post/surface_ls.py` gained `section_cp_curve_levelset` (D11 per-side plane cut; **the UPPER surface is bit-identical to the `main`-based curve**, so all gate shock metrics are unaffected — the lower surface is where D11 bites) and `cl_pressure_3d_levelset` (planform-area normalisation, pairs with `cl_kj_3d` for V6). Cost far below the risk estimate: ~0.6 s/outer at ~12k 3D DOFs ⇒ ~20 min/solve, not hours. **Caveats (recorded, NOT chased):** top Mach levels park on the P4/B6 Picard residual tail (|R| 4–6e-6, 600-outer cap) — bounded + physical + in-band at every level, so the gate asserts *bounded*, not `converged`; **LS Newton on M6 DEFERRED** (plain splu; P8/N6's true-3D LU fill needs lagged-LU — port `direct_refactor_every` into `newton_ls`); shocks sit 0.02–0.04c aft of P5 (in band) and η=0.90 is 0.087 aft of the P8 Newton shock; coarse only.

Prior: **B6 (transonic) IN PROGRESS since 2026-07-12 — coarse M0.80 gate MET (Newton-baselined); LS Newton (`solve/newton_ls.py`) DELIVERED + FD-verified (1.3e-9) and reaches machine-converged quadratic fold solutions (medium M0.7875 M3 wake-free |R| 1.5e-12); medium quantitative closure has two recorded gaps — M0-embedded needs the N5 frozen-selection (live Newton limit-cycles at 3e-6), and the converged LS fold lift is ~13% below conforming-Newton (discretization difference to apportion).** ★ B6 findings so far (design_track_b.md §10, roadmap B6): per-side artificial density delivered (D10; subcritical exact no-op; the M0.80 instability lives in the pocket ABOVE the airfoil — zero cut/wake cells — so the shock machinery is isomorphic to conforming); (1) the P4 `damping_theta` whole-field stabilizer does NOT transplant — diag damping is a Jacobi smoother and on the B path the implicit Kutta makes Γ a smooth global SOLUTION mode (conforming keeps it outside the damped matrix as a secant unknown) ⇒ damping is LOCALIZED to the ν>0 rows (`damping_scope="supersonic"`, default; "fluid" kept as the reproducible negative control); (2) ★ near the FP fold the live option-a Γ→far-field-vortex loop has gain > 1 (Γ climbs monotonically THROUGH the conforming-Picard stall 0.1819 AND the Newton solution ≈0.2295 at flat residual ~5e-5, then blows up; under-relaxation cannot fix monotone gain>1; the per-level lagged vortex measures an outer map with NO fixed point ⇒ coarse M0.80 with a vortex far field sits PAST the fold — the P8 conforming-medium phenomenon one mesh earlier); (3) **the transonic B-path recipe is `farfield="neumann"`** (López outlet, no Γ feedback): converges at coarse M0.80 to Γ≈0.21 / M_max 1.39 / 0 lim+flr — closer to the Newton truth than the conforming Picard's own stall (B5's subsonic option-a default verdict unaffected); (4) ★ INVERSION — the raw Picard A/B gap (+4.9% M0.70 / +10.5% M0.75 coarse, +7.4% M0.70 medium) is the CONFORMING Picard's own stall bias, not an LS error: same-mesh conforming NEWTON arbitration gives Γ 0.1151/0.1377 (coarse M0.70/0.75) and 0.1190 (medium M0.70), i.e. the conforming Picard under-circulates −4~−8% (P4-erratum quantified at weak shocks) while the **LS Picard sits +0.6%/+1.0%/+0.25% off the Newton truth**, converging toward it under refinement — the implicit Kutta has no early-stoppable Γ outer, so Γ converges with the field; **gate clauses 1 & 3 need USER re-arbitration** (proposals in roadmap B6: read both against the same-mesh Newton locks). Also fixed en route: `own_side_field` mislabeled te_lower elements as upper (junk M_max 2.56 vs true 0.85 diagnostic). New: `solve_multivalued_transonic` (Mach ramp, NO Γ secant — st133-class failures structurally impossible), `post/surface_ls.py` (D11 wall-Cp/shock on the B path), `tol_residual` convergence route, +9 fast tests & +2 gated (`tests/test_b6_transonic.py`). **Gate baseline CHANGED (user-arbitrated 2026-07-12): B6's reference is the same-mesh conforming NEWTON truth, not the conforming Picard** (which under-circulates 4–8% at these shocks). **coarse M0.80 gate MET** dual-mesh (M0 Γ −7.9% / M3 Γ +0.9% of Newton, shock 0.644/0.678 inside 0.658±0.06, 0 lim/flr; demo `cases/demo/b6_transonic/` 14/14 checks). **medium M0.7875 = FOLD, PARTIAL**: bounded physical Picard state (C=2.0/θ=0.5-local/dm=0.025 — the coarse recipe DIVERGED, M_max 60/1103 limited) but stalls at Γ −18.8% of the same-mesh Newton truth 0.2643 — a Picard method (conforming OR LS) does not reach the isolated fold solution (P4-erratum/P8 record), so **the quantitative medium gate needs the deferred LS Newton** (design_track_b §5.5). ⇒ B6 stays IN PROGRESS (coarse ✓; medium bounded-Picard, fold under-convergence recorded). Also fixed en route: `own_side_field` mislabeled te_lower elements as upper (junk M_max 2.56 vs true 0.85). New: `solve_multivalued_transonic` (Mach ramp, NO Γ secant — st133-class failures structurally impossible), `post/surface_ls.py` (D11 wall-Cp/shock on the B path). (Track-B renumber 2026-07-12, TWO same-day renumbers: (1) a new B4 was INSERTED; (2) the half-integer IDs were then regularized away. NET mapping from the pre-2026-07-12 scheme: far-field A/B (once B3.5, then B4.5) → **B5**; old B4 transonic → **B6**; old B4.5 M6 3D → **B7**; old B5 multi-wake → **B8**; old B6 curved wake → **B9**. Docs before that date use the old IDs; the demo dir `cases/demo/b4p5_farfield/` and `tests/test_b45_farfield.py` keep their old names on purpose — the committed paths stay stable.) **★ B5 verdict (far-field A/B, design_track_b.md §5.4): option a (Dirichlet+vortex) STAYS the default** — `solve_multivalued_lifting(farfield="vortex"|"neumann"|"freestream")`, default `"vortex"`. López-style domain-size re-calibration (NACA dual-mesh, R∈{15,30,60,120}c) shows option a is domain-robust (Γ within 0.45%/1.09% over 15→120c, 0.25% of conforming at 15c); the López Neumann outlet (option b, no vortex) truncates **O(Γ/R)** — −4.07% at 15c, halving per R-doubling ⇒ meets the B3 ±2% band only at R≥~30c (<1% at ≥60c = 2–4× larger domain); freestream is crudest and diverges on compact 15c M0. Both mesh families agree bit-for-bit; the O(Γ/R) truncation is geometry-universal so this decides the M6 far-field too. **M6 leg folded into B7** (the 3D B-path solve is B7 machinery; the span-uniform vortex without the P5 Γ(z) taper also recreates the branch-ray artifact on M6). Tests: `tests/test_b45_farfield.py` (10); demo `cases/demo/b4p5_farfield/`. **★ The level-set path now produces LIFT with an implicit Kutta** — no Γ secant, no master–slave Γ: Γ matches the conforming solver **within 1% on the SAME mesh** at M=0 and M=0.5 (0.1177/0.1191/0.1197 vs 0.1175/0.1200/0.1202 on coarse/medium/fine), cl_KJ 0.2828 (medium) lands inside the committed [PG 0.2788, KT 0.2919] bracket, and the **wake-free M3 mesh** (no `wake` tag at all, generic cuts — the workflow target) reproduces the embedded-mesh Γ to 0.3%. **★ B4 finding (design_track_b.md §9) — the wake LS CANNOT pin Γ:** its residual is identically zero for any spatially-constant jump because Σ_c ∇N_c = ∇(1) = 0 (partition of unity), measured 1.9e-16 ⇒ design_track_b.md §2.3/D2's "g₂ IS the discrete Kutta" is **FALSE and retired** (the López dissertation has no explicit Kutta at all). Γ needs its own condition. **B4 supplies it: the NONLINEAR TE pressure-equality (Bernoulli) Kutta** |q_u|² = |q_l|², factorized EXACTLY as (q_u+q_l)·(q_u−q_l)=0 and linearized by freezing the mean s̄ each Picard outer (no new outer loop). It replaces the TE **aux** row (whose old lower-side mass-conservation entries are re-routed onto the TE main row = total balance, so mass is conserved and no side is arbitrarily robbed). **Non-degenerate where g₂ is not** because q_u and q_l come from DIFFERENT element sets, so q_u−q_l is not a jump gradient. **★ The control volumes must be WALL-ADJACENT** (elements with a wall face = the upper/lower body surface at the TE), NOT the whole element fan: full-fan recovery gives Γ +11–15%, wall-adjacent gives <1%. Symmetrizing the TE control volume is NOT an available route — the mesh is naturally asymmetric there (user-arbitrated). The **D2 penalty-Kutta fallback is no longer needed** (no penalty weight, no tuning parameter). **Load-bearing B3 fixes (do not regress):** far-field **aux DOFs must stay FREE** (pinning them to the vortex lower branch drains the circulation); the wake must be **coplanar with the vortex branch cut** (chord plane y=0) or the outlet carries an unsupported Dirichlet jump and the density blows up; the per-side cut-strip density **limit-cycles** and must be under-relaxed (`omega_rho`=0.5); **D11 wall-Cp side mapping is mandatory** (else cl_pressure = −3.35 junk); **compressibility is carried by the BULK density, NOT the far-field vortex** (PG-scaling β leaves Γ unchanged). Also: the ε shift was manufacturing **spurious cuts in the below-TE fan** (López p.57) — fixed. Tests: `tests/test_b3_lifting.py` (6) + `tests/test_b4_te_control_volume.py` (8).

**★ P9 RESULT (2026-07-11; Track-B attribution CORRECTED 2026-07-12) — it REDIRECTS P11 and localizes the 3D blocker to the WAKE MODEL (not, as first written, to Track B specifically — see the correction below).** Demo `cases/demo/p9_grid_discrimination/` (11 PASS + 3 documented XFAIL); roadmap P9 gates + ledger. The pre-registered bands could NOT fire, and *why* is the finding. **G9.2 PASS (clean):** 2D sharp-TE subsonic lift error vs the PG–KT midpoint **2.71% → 0.33% → 0.03%** (all three converged) ⇒ **a sharp TE imposes NO lift floor** — the 2D leg of the "sharp-TE/LE P1 wall" attribution is GONE. **G9.1 INVALID:** the M6 **fine mesh is not a discrete solution** — unlimited max local Mach **1.40 → 2.13 → 7.93** (coarse/medium/fine) with **0 / 0 / 9** cells crossing M_cap=3; permanently-limited cells block the N5 freeze machinery (it requires 0 limited), so fine limit-cycles at |R|~1e-5 for its whole budget and its cl_KJ 0.2393 is an ARTIFACT, not a lift (coarse 0.2621 / medium 0.2692 are the only discrete solutions) ⇒ no three-point Richardson. **★ The divergent singularity sits ON THE WAKE SHEET at its free tip edge** — all 9 capped cells at z/b 0.998–1.000, x−x_TE +0.002…+0.017 (AFT of the TE, so NOT on the wing), |y| < 0.003 (chord plane) — i.e. exactly where Γ(tip)=0 is enforced (M1: tip free edges stay single-valued). This is the classic **vortex-sheet-edge singularity of a RIGID PLANAR wake** (the real flow rolls up into a tip vortex) — **driven by the trailing vorticity dΓ/dz (largest at the tip), not the bound Γ (which →0 there), and 1/√r not "1/r"; quantified at P13/G13.1 2026-07-13, see the P13 entry above.** P5's "bounded tip-TE-corner P1 overshoot" (M_max 1.995 medium, recorded as the only surviving singularity trace) is the SAME object, merely under-resolved. **⇒ WAKE-MODEL defect (a rigid-planar-sheet property), NOT a wall-element defect (curved elements do not remove a sheet edge).** ★ **CORRECTION 2026-07-12 (was over-claimed as "precisely what Track B exists to fix"):** Track B changes the wake **REPRESENTATION, not the MODEL** — B7 keeps the same rigid planar sheet ending at the tip with Γ(tip)→0 (softly/emergently: the tip TE corner is a TE node with a free aux DOF, Γ(tip) comes out ±3e-4, it is NOT topologically pinned to 0 as the conforming free-edge rule does). The tip-edge demo (`cases/demo/p13_tip_edge_singularity/`, **10/10 as of P13/G13.1**, subsonic M0.5, NO limiter/shock — the clean geometric probe) measures the tip-edge **peak local Mach DIVERGING under refinement on BOTH paths** (same mesh coarse→medium: conforming ×1.38, level-set ×2.28; the LS peak sits in the +2.9% straddler cells just beyond the geometric tip, so it is if anything **less** blunted; conforming three-point exponent **p≈0.59 ⇒ 1/√r**), while the tip-box p95/mean stay flat (localized-edge-singularity signature, now confirmed with zero transonic machinery). **⇒ Track B does NOT fix G9.1's cause** (its representation change leaves the singularity in place). The model-level fix is wake **roll-up / an explicit tip vortex**, which **NO current Track B phase does** — B10 (free wake) is SHELVED and, even unshelved, is about O(θ²) wake-*deflection*, not roll-up. What Track B DOES structurally eliminate is the **secant/Kutta-closure class** of M6 defects (P5 st133 single-station stall, the swept-TE probe-sharing degeneracy, the Γ(z) spanwise jitter — 11–12× smoother, B7 finding 3 — and the far-field branch-ray artifact), because the implicit Kutta has no per-station secant loop; that is a different family from the tip singularity. **G9.3 verdict:** the 0.019 gap is **UNSPLITTABLE as posed** (resolution/floor shares recorded `n/a`, not fabricated); **P11 (curved walls) is NOT supported by P9 as the 3D-lift fix** — its remaining valid case is G1.6 (sphere-Cp on a SMOOTH curved wall, a different mechanism). Any 3D grid-convergence claim must wait for the tip/wake treatment. **Solver-path findings (recorded, feed P10; NO default changed):** (a) `precond="direct"` does NOT scale — a single splu at ~450k dofs ran **4h39m without returning** (RSS 26 GB, killed) vs 18.6 s on medium; (b) **`precond="amg"` + TIGHT Eisenstat–Walker forcing (η=1e-8) is valid AND faster at every size** (M6 medium 66 s vs 141 s direct — same solution to 4 digits, 0 GMRES stalls; coarse 8 s vs 42 s): N5's "Krylov steps stall on the shock-position soft mode" is a property of the LOOSE default forcing, NOT of AMG; (c) the fine COLD Picard-5 seed overshoots the LE into the density floor (M0.70: 4036 limited / 1847 floored ⇒ level-0 stalls and cannot dm-halve ⇒ the whole solve breaks) — fixed at continuation-PATH level only (m_start 0.30 + n_picard_seed 12 ⇒ clean 0/0 start; 13 levels, 5294 s, inside the 2 h budget). Prior (same day): **P10 G10.2 + G10.3 CLOSED** — see the roadmap P10 ledger.
 B2 evidence: `tests/test_b2_multivalued.py` **17 passed** (dual-mesh V0<1e-12 / V1 MMS 1.94 / Laplace α=0 cl≈0); multivalued assembly = same P1 element matrix assembled twice via B1's `dofs_upper`/`dofs_lower`, expressed as a sparse main→aux column redirection of the single-valued matrix (`kernels/cut_assembly.py`), aux rows = continuity weld (aux_k=main_j) so the extended system reduces EXACTLY to single-valued (fold recovers stiffness to 1e-13); nonsymmetric ⇒ spsolve, GMRES+AMG deferred to B3+. `wake/multivalued.py::MultivaluedOperator` + `solve/picard_ls.py::solve_multivalued_laplace`; conforming paths byte-untouched; suite 229+8+2. B1 evidence: `tests/test_b1_cut_elements.py` **34 passed** across the FULL dual-mesh matrix — 2.5D M0/M3 coarse+medium AND 3D M1/M4 ONERA M6 (M0 embedded: census == `cut_wake` minus-star exactly, all sheet nodes ε-shifted "+"; M3 wake-free: gap-free corridor at α=0 and re-aimed α=4° on the same mesh; **M6**: census a strict superset of the conforming minus-star, 0 missing / +2.9% tip-edge straddlers — expected, the sheet's tip EDGE need not conform in an embedded method — and the spanwise clip verified). ★ **Two 3D-only mechanisms found and fixed at B1** (they are invisible on quasi-2D meshes): (1) the swept TE span axis is NOT perpendicular to the wake direction ⇒ q must come from the **oblique (v, d̂, n̂) frame**; an orthogonal projection leaks the downstream distance into the spanwise coordinate and wrongly clipped ~60% of the true M6 cut set (measured, fixed, regression-pinned); (2) the **spanwise clip** (crossings need 0 ≤ q ≤ span_length) is mandatory — without it the level set cuts the wake-plane extension beyond the tip, i.e. P5's far-field branch-ray artifact re-created (the conforming path gets the same semantics from its free-edge rule, Γ(tip)=0). Mesh families: M3 = `cases/meshes/naca0012_wakefree_2.5d/` (`planar.py embed_wake=False` + ±6° size-field-only corridor fan; coarse committed, medium/fine gitignored ~40 s regen; corridor_median_edge 0.0595 ≈ h_wake 0.06; ~1.8–2.4× M0 element counts, accepted); M4 = `cases/meshes/onera_m6_wakefree/` (`wing3d.py embed_wake=False`; coarse 50,605 / medium 329,645 tets = **within 6–9% of M1 at equal h_wall**, the controlled-A/B property for B7; all `.msh` gitignored, stats CSVs committed; **no α-wedge corridor in 3D** — the wedge volume scales with span, ±3° would ~4× the tet count, so 3D α re-aiming stays in the near-nominal band and the wide-envelope sweep demo lives on the cheap 2.5D M3). Suite baseline now **218+8+2** (was 184+8+2; +34 B1 dual-mesh tests, some skip without the local wake-free meshes; full suite measured 409 s at the 8-thread cap alongside the running P9 demo — the 302 s G8.3 number remains the 16-thread reference). Track B numerics spec = [docs/design_track_b.md](design_track_b.md) (2026-07-11, López-dissertation cross-check, supersedes DN1: 3D wake BC = g₁+g₂ two-component LS, implicit Kutta via TE duplication + wake-LS, ε side-shift for on-wake nodes, full-element cut integration, per-side upwind states, nonsymmetric system → GMRES); purpose user-arbitrated = mesh/geometry workflow capability, NOT solver speed; gates re-specced + B5 (far-field a/b) + B7 (M6 3D) added; **dual-mesh rule**: every B1–B6 gate runs on both the wake-embedded meshes (M0/M1 — ε stress + strict A/B vs conforming) and the wake-free families (M3 quasi-2D + M4 ONERA M6 — generic cuts, the workflow target). Track B code = parallel new modules (`wake/`, `embed_wake` flags in meshgen) — conforming-path solver numerics stay byte-untouched while P9 runs. **Thread discipline while the P9 fine-mesh demo is in flight: check load first, cap any new test/solve at a reduced budget (≤8 threads incl. BLAS/OMP)** — the 16C/32T box must not be oversubscribed (the G8.2 A/B measured ~33% penalty). — P9 status: **grid-convergence & accuracy-gap discrimination (evidence phase, NEW 2026-07-11; user-directed Track-P renumbers, NET same-day mapping: curved walls → P11, backlog → P12; pre-renumber docs use the old IDs). User unpaused code work 2026-07-11; current user-directed order: G10.2 (✓) → G10.3 (✓) → **P9 (now running)**. **G10.3 CLOSED 2026-07-11 — verdict KEEP the Mach ramp** (`run_g103_noramp.py`, 2 seedings × 4 cases): far from the fold no-ramp reaches the SAME solution under both seedings (class A — branch selection not binding there), but Picard-5 transits clamped states (peak 45 lim+flr, final 0/0 + locks pass) at +44% (141.4→79.0 s) failing the pre-registered clamp-free clause, and clamp-free Picard-40 gains only +6.6%/−37% failing ≥20% ⇒ no promotion; fold-zone medium class C under BOTH seedings (s1 stalls at cl 0.449≠0.523; s2 un-continued Picard seed DIVERGES into clamps, 9934 limited — deep seeds harmful without a ramp, the P4/P5 record); NACA coarse s1 class-A in 4 s = single-case exception. The +44%-but-clamped path is recorded for USER arbitration of the clamp-free clause. `clamp_history` (additive key) added to solve_newton_lifting. G10.2 CLOSED 2026-07-11 — SPLIT A/B verdict** (demo `cases/demo/p10_newton_usability/`, roadmap P10 ledger): `solve_newton_transonic(intermediate_tol=…)` opt-in, default None bit-identical (`solve_newton_lifting` grew `tol_residual_loose`/`tol_residual_rel`/`accept_on_stall` + `accept_reason` reporting; per-level `accept_reason` in `level_results`); shipped rule = the pre-registered candidate + two A/B-measured hardenings (loose acceptance requires ≥1 Newton step at the level — warm-started levels ENTER below any absolute threshold, zero-step acceptance degenerates the ramp into a level skip; dm-halving retry levels run STRICT — the halving cascade is the robustness fallback). **(a) M6 medium: all G8.2 locks intact, final level identical (12 steps, 7.8e-15), solve 239.5→140.3 s (+41.4%) ⇒ intermediate_tol=1e-5 PROMOTED into NEWTON_M6_RECIPE (gated G8.2 now ~145 s). (b) NACA medium M0.7875: NEGATIVE result — the "warm-start only from CONVERGED levels" trap measured in G10.2 form: the fold-zone loose ramp (1–4 steps/level, dcl/dM ~6–10) leaves an untracked Γ seed; the final level AND strict halving-retry levels stall 60 steps each at the ~5e-6 live-churn floor (cl 0.369 vs lock 0.523, unconverged) ⇒ NEWTON_TRANSONIC_RECIPE unchanged; loose intermediates CONTRAINDICATED near folds.** Suite +2 (`tests/test_p10_continuation.py`: default-path accept_reason lock + subsonic-ramp adaptive path), baseline **184+8+2**. G10.1 (non-lifting Newton entry) remains open, NOT started, no ordering constraint. P9 scope (roadmap P9; decision bands PRE-REGISTERED): G9.1 M6 coarse/medium/fine Newton + Richardson extrapolation of cl_KJ/cl_p (bands: cl_KJ∞ ≥ 0.283 ⇒ resolution-dominated; ≤ 0.278 ⇒ floor confirmed; else inconclusive); G9.2 NACA 2.5D coarse/medium/fine subsonic lift oracle (monotone |err| decrease + fine within ±1% of the PG–KT midpoint); G9.3 attribution verdict + P11 (curved walls) go/no-go (user arbitrates). Motivation (P8 capability demo `cases/demo/p8_capability/`, 36/36 PASS, commit 2b9cfc8): the 0.019 M6 lift-gap attribution to the flat-facet wall is inference-grade — 2D sharp-TE lift converges cleanly (−2.7%→−0.3%), M6 cl_KJ still rising under refinement (0.2621→0.2692), unlike the oracle-grade G1.6 sphere evidence. Also recorded by that demo: ★ **fold-zone grid sensitivity** — NACA M0.78 same-condition coarse/medium NOT grid-comparable at α1.25 (cl 0.3399 vs 0.4339) NOR at the fallback α1.0 (0.2626 vs 0.3238), all four true Newton solutions; dcl/dM ≈ 6–10 makes O(h) act like O(0.01) Mach shifts ⇒ per-mesh regression locks, never grid-convergence claims near the fold. New instrumentation (additive keys only, suite 182+8+2 unchanged): `solve_newton_lifting` returns `gamma_history`; `solve_newton_transonic` returns per-level `level_results` (histories/wall_s/timings — the top-level `timings` covers only the final level). P9 rules: no solver/numerics changes; fine meshes stay gitignored; heavy runs are one-shot demo scripts (G8.3 CI budget untouched). Prior: **P8 CLOSED 2026-07-11 (G8.1 + G8.2 + G8.3 all closed).** **N6 + G8.2/G8.3 delivery (2026-07-11):** ONERA M6 Newton closes P8. (1) **lagged-LU direct mode** in `solve/newton.py::solve_newton_lifting` — new `direct_refactor_every` (default 1 = N5 behavior bit-identical, suite-locked) + `direct_reuse_rtol` (1e-8): refactor the LU every k-th step, in between GMRES on the FRESH coupled operator preconditioned by the stale LU; GMRES failure → refactor + exact Woodbury in the same iteration (robustness never below every-step-direct). Why: **true-3D LU fill makes splu ~100× the 2.5D cost at equal dofs** (measured 18.6 s vs ~0.2 s at ~6e4 dofs; the every-step M6 medium run was 1606 s, 97% splu — NOTE the returned `timings` dict is the FINAL level only, which hid this on first read; cProfile settled it). (2) **`NEWTON_M6_RECIPE`** (tests/test_p8_newton.py): dm 0.05 (P5 schedule — M6 is far from the NACA-medium fold, zero dm-halving), dm_min 0.01, freeze_tol 1e-6, refresh 8, precond direct, refactor_every 1000 (once per level + on-demand), farfield_spanwise_gamma=True. (3) **G8.2 closed: M6 medium 249.2 s end to end** (mesh 7.3/solve 239.8/post 2.1; 63k nodes; coarse 42 s) — **timing protocol: the 16-thread cap must include BLAS/OMP** (`NUMBA_NUM_THREADS=16 OMP_NUM_THREADS=16 OPENBLAS_NUM_THREADS=16`; A/B measured: without the BLAS/OMP caps the identical gated run is ~333 s and FAILS the 300 s assert — oversubscription costs ~33% on the 16C/32T box); **G8.3 closed: suite 301.66 s**, baseline now **182 passed + 8 skipped + 2 xfailed** (+1 gated skip = `test_g82_m6_medium_newton_end_to_end`). (4) **M0.84/α3.06 reachable on BOTH M6 meshes** — the fold contingency never triggered; terminal quadratic in frozen phases (medium …2.6e-7→2.1e-10→7.0e-15), 0 lim/flr, Kutta ~2e-16. (5) **P5-caveat MEASURED** (closes the recorded item): Newton residual at committed P5 Picard states 8.6e-6 coarse / 7.6e-6 medium (|F| ~5.5e-4) — not solutions (P4-erratum in kind, milder); true solutions **cl_p 0.2560/+5.8% coarse, 0.2646/+7.9% medium**, shocks essentially unchanged (0.596/0.541/0.362 vs 0.594/0.526/0.345), M_max 2.13; **under-convergence is circulation/lift, not shock position**; cl_KJ 0.2692 narrows the P9 inviscid-vs-Tranair(0.288) gap 0.043→0.019. P5 ledger annotated (gates stand as Picard-quality; reference_data untouched, hard rule 6). (6) Solution identity: coarse continuation-path independent to 1e-15; medium dm-0.025 vs dm-0.05 paths differ |dφ| 2.6e-4 / cl equal to 5 digits — the N5 assignment-floor semantics (live floor ~5e-5, stale 84 vs 92), lock bands (cl ±0.005, shock ±0.02, M_max ±0.05) cover it. Evidence: gated test + demo `cases/demo/p8_newton/` part 3 (V8.1c convergence, V8.2 M6 runtime breakdown, `g82_m6_medium.csv` incl. `newton_residual_at_p5_state`). Prior: **N5 + G8.1 delivery (2026-07-11):** transonic Newton robustness chain in `solve/newton.py` + `kernels/upwind.py`: (1) `precond="direct"` exact steps (splu + Woodbury on the rank-n_st Γ-coupling) — **the shock-position soft mode stiffens under refinement and η-accurate Krylov steps STALL** (measured: frozen-system residual flat at 3.7e-6 with GMRES converging to η=1e-2 on medium; direct steps → crisp 1e-13); (2) **stall-adaptive freeze** of the upwind assignment (`freeze_upwind_state`/`rho_tilde_frozen_sweep`/`rho_tilde_frozen_sensitivities_sweep` — classification forced, bitwise-identical to the live sweep at the freeze state; NO floor on frozen branches) + **active-set refresh** with two-cycle acceptance and honest `residual_unfrozen`/`n_assignment_stale` reporting — the 2.5D prism-split family parks ~1e3 elements in the max(ν_e,ν_u) NEAR-TIE band (P7 kink trap in Newton form): live Newton limit-cycles (measured floors 3e-9@M0.7375…1.4e-5@M0.80 medium; branch flips 300-800/step, u-flips 40-120/step); within a frozen assignment the system is smooth and terminal-quadratic; (3) safety nets: freeze-revert on frozen-path divergence (10× or any lim/floor — frozen sweeps are floor-free by design), level fail-fast (100× above level best), best-of-tried line search (unconditional λ-floor acceptance allowed sustained growth). Recipe = `tests/test_p8_newton.py::NEWTON_TRANSONIC_RECIPE` (dm 0.025/dm_min 0.003/freeze_tol 1e-6/refresh 8/direct/budget 60). **★ Baseline findings (user-arbitrated 2026-07-11):** (a) **P4 Picard states are NOT discrete solutions** (roadmap P4 erratum): Newton residual at the committed coarse M0.80 state = 2.2e-4; Newton from it converges (6 steps) to the TRUE solution shock 0.658/cl_p 0.459/M_max 1.408 (dissipation-robust 2.0→1.5 path-independent to the last bit; C=1.2 diverges — don't go below ~1.5 on coarse); the "bounded residual tail" WAS the stall; G4.3's damping-dependent cl 0.389→0.458 retro-explained. (b) **medium steepens into the FP non-uniqueness fold**: Newton-converged M0.775 shock 0.570/cl 0.396 (1.8e-13), M0.7875 shock 0.674/cl 0.523 (7.9e-11), NO reachable isolated solution at M0.80 (M_max≈1.45 > isentropic validity; conservative FP over-lifts vs Euler at strong shocks — Holst). ⇒ **G8.1 re-specced (user-approved) to coarse M0.80 + medium M0.7875** with regression-lock bands around the measured Newton solutions; `cases/reference_data/naca0012_m080/` untouched (hard rule 6); P4 gates stand as Picard-quality gates (warm-start engine). G8.1 evidence: gated `test_g81_terminal_quadratic_*` ×2 + gated Newton-field FD pocket (rel ~1e-10) + demo `cases/demo/p8_newton/` (V8.1a/b, V8.2-lite; medium run ~100 s vs Picard 16m39s-to-a-non-solution). Kutta |F| ~1e-16 (coupled) vs secant 1e-4. **P5 caveat recorded:** M6 Picard results carry the same convergence-quality caveat in kind — N6's M6 Newton run will measure it. Protocol notes for future FD/convergence tests: quadratic-drop pairs must be SEARCHED over the level history (freeze-refresh honesty re-evaluations interleave live jumps with the quadratic frozen phases). Prior: **P8 N2–N4 delivery (2026-07-10):** exact Newton Jacobian (6.3) at frozen selection in `kernels/jacobian.py::assemble_newton_jacobian` — Terms 1+2 fused on the SHARED Picard CSR pattern/coloring (Term 2 added only when s_e≠0 ⇒ masked/limited elements reduce to the Picard A **bitwise**; a fused `w1*(..)+w2*(..)` FMA-contracts differently under fastmath — measured, hence the split), Term 3 = **active-set COO** (16 entries per s_u≠0 element, rows nodes(e) → cols nodes(u(e)) at graph-distance ≤4, rebuilt every Newton step ⇒ selection churn cannot corrupt a reused pattern; **NO recolor/wider-CSR machinery** — COO is ms-scale at pocket sizes; `newton_nnz`/`n_term3_active` recorded = the N2 measurement), speed-limiter consistency by masking s_e/s_u with `lim = (q2l == q2n)` (flat clamp ⇒ derivative 0; bitwise no-op at 0 limited). `wake.py::reduce_operator(A) → (TᵀAT, TᵀAG)` pure extraction (`update_matrix` delegates bit-identically); called on the Newton J, the H column IS the exact wake-jump ∂R_red/∂Γ. `solve/linear.py::solve_gmres` (auto-retry 2× restart) + `build_ilu_preconditioner` — the exact Jacobian is NONSYMMETRIC in supersonic zones (asserted in tests), CG inapplicable. `solve/newton.py`: `NewtonWorkspace` with ONE shared `eval_residual` state-reconstruction path (residual/Jacobian consistency = the quadratic-convergence prerequisite); affine far-field basis `vals_red(Γ)=vals0_red+V_red·Γ` extracted by unit-Γ probing of `farfield_dirichlet` (linearity guarded at machine precision); sparse Kutta row K (±1/n_j; shared probes appear in both rows = correct derivative). `solve_newton_lifting` does the **exact δΓ elimination** ((2,2) block = −I): `(J_ff + B·K) δφ = −R_free − B·F`, `δΓ = K δφ + F`, with `B = J_red[free,dir]@V_red + H_J[free,:]` — **the easy-to-miss far-field vortex column is IN and FD-guarded** (`test_gamma_column_fd`, the trap detector); GMRES on the low-rank LinearOperator, AMG built on the SPD Term-1 block (rebuilt every 2 steps), Eisenstat–Walker choice-2 forcing (η₀=1e-2 — note it makes a LINEAR m_inf=0 problem take ~3 inexact steps unless tightened), safety-only backtracking line search, **no damping_theta anywhere in Newton** (Picard stabilizer — kills quadratic convergence), optional consistent `ptc_dtau`; convergence refused while limited/floored > 0. `solve_newton_transonic` = upward-only Mach-continuation SKELETON (warm start from the last CONVERGED level, dm halving to dm_min, `upwind_c_post` for López's post-ramp μ_c 2.0→1.6; interfaces final, **untuned — N5's job with gated G4.1 runs**). **Measured:** NACA coarse M0.5/α2 — seeded Newton 2 steps 4.2e-7→3.8e-9→**4.7e-13**; cold start 3 steps, observed orders **[2.57, 1.79]**, terminal step gains >3 digits; Γ/φ/cl match the P3 Picard/secant to **1.9e-8 / 2.0e-8 / ~1e-7**; m_inf=0 → ONE step ≡ P2 Laplace (with η tightened). **Jacobian JVP vs frozen-selection residual FD: rel ~1.3–1.5e-10** (tol 1e-6) in all regimes incl. Term 3 (938 active on the constructed cube pocket). Tests: `test_p8_jacobian.py` (7 + 1 gated converged-pocket FD = the G8.1 FD clause, runs at N5) + `test_p8_newton.py` (8); green under JIT and PYFP3D_NOJIT; suite **180 passed + 5 skipped + 2 xfailed** (baseline 165+4+2, zero regressions; forward Picard paths byte-untouched, structural bit-guard test). FD-kink protocol inherited from P7: generic noise-broken fields, element-level ε-guard lifted to residual ROWS (nodes of kink elements), assert the kink ELEMENT fraction (row fraction is inflated on small cubes). Prior: **P7 CLOSED 2026-07-10 (frozen-selection ∂ρ̃/∂φ of the walk flux); P8 = fully-coupled (φ_red, Γ) Newton.** **P7 delivery (2026-07-10):** exact branch-wise sensitivities `(s_e, s_u) = (∂ρ̃_e/∂q²_e, ∂ρ̃_e/∂q²_u)` at frozen u(e) in `kernels/upwind.py::rho_tilde_sensitivities_sweep` + `UpwindOperator.rho_tilde_sensitivities` (walk mode only; floored/self-upstream flat branches → 0; new scalar `isentropic.py::mach_squared_derivative_wrt_q_sq`); G7.3 FD-verified vs the SHIPPED `rho_tilde_sweep` at frozen selection: **3–5e-10** cube (8 tests, `test_p7_diff_flux.py`, green under PYFP3D_NOJIT), **3.5e-9** constructed NACA-coarse multi-regime, **5.7e-9 on the converged G4.1 M0.80 field** (pocket 1189 accel + 977 shock-point). `max_ε` NOT implemented (scope decision) — forward flux byte-untouched, G7.1/G7.2 held by construction; P4/P5/P6 bit-identical; suite 165+4+2. **Sign fix:** design.md §6.3 `dμ/dM² = +M_c²/M⁴` (FD-arbitrated; "−" was a typo). **Kink trap for P8:** FD probes straddling the max(ν_e,ν_u) tie / switch threshold read branch AVERAGES (~1e-5, not a bug; measure-zero locus, active set freezes near the solution); separable fields on structured/prism-split meshes park whole slabs ON the tie — use generic/noise-broken fields in any future FD check. P8 consumes `(s_e, s_u)` × `2∇φ·∇N_k` as Term-2/Term-3; stencil +1 upstream element/row at graph-distance ≤4. Prior: **P6 CLOSED 2026-07-08 (surface-Cp recovery / sawtooth); P7 was re-scoped 2026-07-08: Newton prereq = the shipped **frozen-walk** ∂ρ̃/∂φ, sparse ~+1 element/row; the streamline-Gaussian kernel is demoted to an **optional** Picard-speed path, a P8 flux candidate only if its denser Jacobian measures favourable. Track P renumbered: P6 recovery / P7 flux / P8 Newton / P9 curved walls / P10 backlog.** **★ N1 correction (2026-07-08): the surface-Cp sawtooth is a per-triangle wall-gradient RECOVERY artifact, NOT the artificial-density flux** — proven decisively: nodal/edge-neighbour smoothing of the *same* walk solution's wall gradient drops the G6.1 metric ~330× (0.0758→0.00023) while a smoother flux (the streamline kernel) does not reduce it at all. So G6.1's fix is a **normal-gated wall-gradient recovery smoothing** in post-processing (`post/surface.py::smooth_wall_tangential_gradients`, `smooth_passes` on `wall_cp_curve`/`section_cp_curve`/`wall_force_coefficients`, default 0 = bit-identical, preserves the sharp TE; design.md §9.1). This **overturns** the earlier ρ̃-selection-flip attribution (demo_report §P4). The differentiable **flux** kernel (streamline-Gaussian, `UpwindOperator(weighted=True, mode="kernel")`, opt-in, ~10× faster/iter, stable, C^∞ in ∇φ) is an **optional** Picard-speed path, **NOT** the P7 Newton prerequisite (re-scoped 2026-07-08: the prereq is the frozen-walk ∂ρ̃/∂φ, sparse; the kernel's Newton Jacobian is denser — a P8 candidate only if measured favourable) — it does not fix the sawtooth. P4/P5 solutions unchanged (post-processing only; shock/M_max/cl_KJ identical); only reported cl_p (−0.3 % coarse)/cd_p (~15 %, untrusted FP drag) shift under smoothing. Tests: `test_p6_recovery.py`, `test_p6_cp_metric.py` (metric re-specced shock-robust: sign-alternating so the monotone shock isn't counted). Demo `cases/demo/p6_surface_recovery/`. **Track-P renumber (2026-07-08): P6 = surface recovery (this, done); P7 = differentiable flux at frozen selection (Newton prereq = frozen-walk ∂ρ̃/∂φ; kernel optional); P8 = fully-coupled Newton; P9 = curved wall elements (G1.6 + residual V6<1%); P10 = backlog.** **Original P6 phase framing (design pass 2026-07-08):** next: the consistent/differentiable artificial-density flux (roadmap P6; it also owns the deferred P5 V6<1% target). **P6/P7 design pass (2026-07-08, design.md §3.1–3.2 / §6.3 / §8.1 / §13; roadmap P6/P7 sub-phases + ledger):** the López dissertation was cross-checked directly against the PDF (`docs/references/Dissertation_Inigo_Lopez.pdf`), confirming the fully-coupled-Newton / full-Jacobian / nonzero-upstream-coupling (Eq. B.4) / frozen-selection architecture, and correcting 3 claims in discussion-note 4 — (1) López used a **sharp** TE (Eq. 4.2 sharpens the NACA0012), not blunt, and still converged quadratically → a sharp 2D TE is **not** a Newton obstacle; (2) within a Mach ramp M_crit/μ_c are held **fixed**, with μ_c *decreased* 2.0→1.6 only *after* reaching target M∞ on ONERA M6 (Table 4.13), not swept per step; (3) Newton differentiability needs a flux differentiable **at frozen upstream selection**, not a differentiable selection. P6 accordingly has two separable defects — the surface-Cp **sawtooth** (a selection-flip *spatial* artifact) and Newton **non-differentiability** — both addressed by the **streamline-projected weighted upwind density** (design.md Eq. 3.4, `ρ_up=Σ_f w_f ρ_{nb(f)}/Σ_f w_f`, `w_f=max(0,−V_e·n̂_f)^p`); the smooth density clamp (note-4 "N0") is **optional** (converged states have 0 floored/limited, so hard clamps never enter the converged Jacobian). P7 = fully-coupled (φ_red, Γ) Newton eliminating the P5-fragile Γ-secant; the Γ-Jacobian blocks are nearly in-code (`wake.py::self._h`, `kutta_targets`) but the far-field vortex column `−A_coupling·∂vals_red/∂Γ` must be added. **P5 closed (2026-07-08)** — ONERA M6 M0.84/α3.06, demo 16/16 PASS, but only after the medium `physical` root cause was re-diagnosed a SECOND time and the first attribution OVERTURNED: the failure (M_max 5.204, 8 floored/4 limited; 26 M>2 cells = 18 outboard-TE z/b 0.80–0.81 + 8 far-field beyond the tip) was **NOT a TE discretization singularity** as first recorded — four targeted experiments (T1–T4, `cases/demo/p5_onera_m6/INVESTIGATION_kutta_closure.md`) pinned it on a **single-station Kutta-closure failure**: the per-station secant does not converge at the top Mach level on the 3D mesh (pushing 10→16 evals DIVERGES, M_max≈29 — the 10-eval budget was early-stopping regularization) and left st133 (z/b=0.801, steepest |dΓ/dz|) **32% under-circulated** (|F|/Γ=37% vs ≤5% at all 165 other stations); the Γ deficit drives a real TE overspeed that the ρ̃-floor/limiter freeze into a spurious M≈3 state. Decisive test T3: setting ONLY st133's Γ to its own Kutta target collapses the cluster 18→0 cells on the same mesh (a 1/h singularity cannot depend on Γ; the coarse-1.47→medium-5.20 'refinement sharpening' was the finer mesh resolving the same Γ-error's overspeed). T1: 0 tets straddle master+slave (cut clean). T2: better density convergence at the same Γ leaves the defect bit-identical (not under-convergence). The far-field 8 cells (incl. the pre-fix M_max=5.20) were the independent span-uniform-2D-vortex **branch-ray artifact beyond the tip** (no cut there to carry the prescribed ~Γ_mean jump). **Fix (recipe-level, defaults off, all non-3D paths bit-identical):** (1) `farfield_spanwise_gamma=True` — Γ(z)-tapered vortex far field (per-station interpolant, 0 at/beyond the sheet tip; `constraints/dirichlet.py::farfield_dirichlet`); (2) `n_kutta_polish=4` — fixed-Γ Kutta-closure polish after the Mach continuation (`solve/continuation.py`): apply the measured target, re-solve with `omega_rho_polish=0.5`, repeat — secant-free, contractive (|F| halves per step to 5.8e-4). NOTE omega_rho<1 *inside* the active-secant continuation is NOT the fix (measured to diverge). **Closed gate numbers (from-scratch):** coarse M_max 1.398 0/0, shocks x/c 0.596/0.570/0.425, Γ 0.097→0.0206, V6 2.40%, CL 0.2419; medium M_max 1.995 0/0 (the bounded tip-TE-corner P1 overshoot is the only surviving singularity trace), shocks 0.594/0.526/0.345 (~1 cell), Γ 0.097→0.0151, V6 1.82%, CL 0.2453, Kutta |F| 5.8e-4 (28× tighter than pre-fix). **G5.2 V6 re-spec (user-approved):** V6 is a systematic O(h) CL_p-below-CL_KJ discretization floor (sharp-TE/LE P1 wall gradient + P4 Cp sawtooth — both P6 targets; removing the M>2 clusters left V6 unchanged), reported against a 3% floor bound; **<1% is deferred to post-P6, re-measure after G6.1**. **Dead routes stay dead:** spanwise-Γ *smoothing* (A–E, `INVESTIGATION_gamma_smoothing.md` — moves Γ AWAY from the self-consistent value; the polish drives the stuck station TOWARD its own target) and TE-element treatments *for this gate*. **Known-robustness item (recorded, not fixed):** the swept-TE Kutta probe assignment shares probe nodes between adjacent stations (st133/134 share their upper probe; 35 upper/41 lower of 166 — `diagnose_medium.py` audit) — the latent reason st133 specifically stalled. Infrastructure delivered en route (unchanged): `post/surface.py::planform_area`+`cl_kj_3d`, `post/section_cut.py::section_cp_curve` (bit-identical refactor), `solve/continuation.py` `rtol` forwarding (**rtol=1e-7 ~5.5× faster, M_max identical to 5 digits**; default 1e-10 keeps P4 bit-identical), viscous AGARD overlay reference; recipe `seed40/eval300/gamma10/rtol1e-7/polish4/taper`; **all runs cap `NUMBA_NUM_THREADS=16`** (16C/32T shared box). Tests: `tests/test_p5_onera_m6.py` 4 fast + 2 gated (gated updated to the polish recipe + 3% V6 floor bound); the medium demo cache now stores residual/drho histories. Full default suite green: **140 passed + 4 skipped + 2 xfailed**. Prior: **P4 closed (2026-07-07)** — the same-day G4.1 medium divergence was re-diagnosed and fixed: the global mass-lumped pseudo-time damping diag(m_lumped/Δτ) is replaced by **local** θ·diag(A_free) (θ=0.2 — `solve/picard.py::solve_subsonic_lifting`'s `damping_theta` param, mutually exclusive with the retired `pseudo_dt`; the `TRANSONIC_DEFAULTS` default), recomputed every outer iteration so the damping ratio is mesh- and shock-strength-independent by construction. Medium G4.1 PASSES (shock 0.633, |F| 1.23e-4, M_max 1.366, 0/0, 16m39s); G4.2 bitwise no-op + G4.3 sweep re-verified (M0.82/α1.25 cl 0.389→0.458 recorded as a different converged/physical state, not a regression). `WakeConstraint.update_matrix`'s per-station h_j loop batched into one sparse `T^T@(A@G)` product (bit-identical, removes ~166 matvecs/density-iteration on M6 medium). **Transonic convergence semantics unchanged**: engineering-converged (physical M_max, 0 limited/floored, Kutta |F| below eval noise, cl drift <1e-3/hundreds iters); the sharp-shock residual tail is bounded and slowly decaying, NOT 1e-10 — P7 Newton remains the designed cure (Newton renumbered P6→P7 on 2026-07-07 when the differentiable-flux P6 was inserted ahead — roadmap P6/P7). Heavy gates (G4.1 medium, G4.3 sweep, G5.1/G5.2) run only under `PYFP3D_TRANSONIC_GATES=1`. M1 recap (closed 2026-07-07, mesh delivery): `meshgen/wing3d.py` (OCC ruled-loft half wing; `occ.fragment` stitches the TE edge but **`mesh.embed` must follow** or the tets don't conform; axis convention chord x / lift y / span z), `cases/meshes/onera_m6/` (one h_wall parameter, 55.5k/350.7k/2513k tets; **.msh gitignored** — regenerate coarse+medium in ~30 s), `mesh/wake_cut.py` M1 extensions (tip FREE edges stay single-valued ⇒ Γ(tip)=0 discretely; Kutta-probe off-plane fallback; both inert on 2.5D meshes). Also: P1 still open on one item — G1.6 sphere-Cp (strict xfail) awaits its **Option C acceptance re-spec**; curved/isoparametric wall elements separately scoped (design.md §5.1.2); do not propose h-refinement, recovery tweaks, Nitsche, or boundary-data corrections for G1.6 — ruled out with evidence — per [docs/roadmap.md](roadmap.md).

Authoritative docs (the ONLY documents development plans against):
- [docs/design.md](design.md): theory, discretization, architecture, and validation formulas.
- [docs/roadmap.md](roadmap.md): phases, gates, and progress ledger.

NOT authoritative: `docs/discussion_notes/` (DN1–DN6, PLAN.md) is discussion and
reference material only — exploratory analyses, candidate designs, integration
views. Never implement from a discussion note: its content becomes actionable
only after being merged into roadmap.md/design.md, and where a note conflicts
with roadmap/design, roadmap/design win.

Hard rules:
1. After any kernel or assembly change, run `pytest tests/test_v0_freestream.py` first.
2. A phase closes only when its medium-mesh gate and the full coarse regression suite are green.
3. Numba kernels use SoA arrays only, no Python objects inside `@njit`, colored assembly for `prange`, zero allocation in hot loops, `cache=True`, and the `PYFP3D_NOJIT` switch.
4. All sparse linear algebra stays in SciPy/PyAMG.
5. Physics scalars live only in `physics/isentropic.py`.
6. Never edit files under `cases/reference_data/`.
7. Wake-cut changes must rerun topology asserts on all meshes in `cases/meshes/`.
8. Mesh generation stays vanilla Gmsh; node duplication happens only in the solver preprocessor.
9. Validate against full-potential references before Euler.
10. Keep commits phase-scoped and update the v2 progress ledger when a gate closes.
11. Code only against roadmap.md gates + design.md numerics; `docs/discussion_notes/` is discussion/reference material, never a coding spec.
