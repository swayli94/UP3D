# pyFP3D Agent Rules

Current phase: **P5 in progress (2026-07-08)** — ONERA M6 M0.84/α3.06. Post-processing + validation infrastructure delivered and **coarse-mesh validated; the medium gate is NOT closed, G5.1/G5.2 remain unchecked.** New code: `post/surface.py::planform_area` + `cl_kj_3d` (3D Kutta–Joukowski spanwise integral CL_KJ=2∫Γdz/(U·S)); `post/section_cut.py::section_cp_curve` (arbitrary-η wall-plane cut; `wall_cp_curve` core refactored to `_wall_section_points`, **bit-identical**); `solve/continuation.py` now forwards `rtol`/`maxiter` — the inner CG was over-solved to 1e-10 while the outer density fixed point needs only ~1e-6, so **`rtol=1e-7` runs ~5.5× faster with M_max identical to 5 digits** (default 1e-10 keeps the P4 2.5D path bit-identical; v0+P4 re-verified). Calibrated bounded recipe `seed40/eval300/gamma10/rtol1e-7`; **all runs cap at `NUMBA_NUM_THREADS=16`** (user's 16C/32T shared box). The synthesized `reference_data/onera_m6_m084/` was removed for the user-committed **viscous** AGARD `reference_data/onera_m6_experiment/` (qualitative overlay only). **Coarse (55.5k): 8/8 demo self-checks pass** — physical (M_max 1.47, 0 floored/limited), **tip does not diverge** (tip-band max Mach 1.321, the lowest across inboard/outboard/tip 1.396/1.470/1.321; Γ root0.100→tip0.021), shock present/monotone/**forward-migrating** (x/c 0.595→0.569→0.424 for η 0.44/0.65/0.90), V6 consistency 2.4% (coarse discretization floor), CL 0.248. **Medium (350.7k) FAILS the physical + V6 gates** (M_max 5.20, 8 floored/4 limited, V6 1.73%). Cached-solution diagnostic: only **26/350718 cells (0.007%) have M>2, all in the wake-sheet/far-field region downstream near the tip** (x≈10 near the R=15 MAC sphere, z/b≳1.0) + a few outboard-TE cells — **NOT on the wing surface** (section-Cp shocks are physical & forward-migrating). Consistent with the flagged 2D-vortex far-field / wake-tip concern (design.md §5), **not** a global wing-flow under-convergence. **Open: investigate the far-field/wake treatment near the tip before re-running the medium gate** (a heavier iteration budget alone may not heal a boundary artifact). Full default suite green: **140 passed + 4 skipped + 2 xfailed** (+4 fast P5 tests, +2 gated P5 tests skipped). Prior: **P4 closed (2026-07-07)** — the same-day G4.1 medium divergence was re-diagnosed and fixed: the global mass-lumped pseudo-time damping diag(m_lumped/Δτ) is replaced by **local** θ·diag(A_free) (θ=0.2 — `solve/picard.py::solve_subsonic_lifting`'s new `damping_theta` param, mutually exclusive with the retired `pseudo_dt`; wired as `solve/continuation.py::TRANSONIC_DEFAULTS`'s new default), recomputed every outer iteration from that iteration's own operator so the damping ratio is mesh- and shock-strength-independent by construction instead of weakening ~4× coarse→medium at a fixed Δτ. Medium gate now **PASSES**: upper shock x/c 0.633 vs 0.62±0.03, Kutta |F| 1.23e-4 < 2e-4 tol, M_max 1.366, zero limited/floored cells, cl_pressure/cl_KJ sign-consistent 0.349/0.354 — **16m39s wall vs the prior divergent attempt's 2h43m** (better-conditioned CG: ~0.18 s/outer-iter vs ~0.51 s/outer-iter). G4.2 bitwise no-op and the G4.3 10-case sweep both re-verified green under the new default the same session (note: the M0.82/α=1.25° sweep corner's cl shifted 0.389→0.458 under the new damping — recorded as evidence of a different but still-converged/physical state, not a regression, since G4.3 gates on convergence+physicality, not an exact cl value). Full default suite unaffected: **136 passed + 2 skipped + 2 xfailed, ~5 min**. Same session, the P5 blocker flagged during the G4.1 diagnosis is also closed: `constraints/wake.py::WakeConstraint.update_matrix`'s per-station h_j loop is now one batched sparse `A @ G` product instead of one matvec per station (verified bit-identical on the real ONERA M6 coarse mesh, 83 stations) — inert on 2.5D single-station meshes, removes ~166 extra matvecs/density-iteration on the M6 medium mesh. **Transonic convergence semantics unchanged** (documented in picard/continuation docstrings): engineering-converged — physical M_max, zero limited/floored cells, Kutta |F| below eval noise, cl drift < 1e-3/hundreds of iterations; the sharp-shock residual is a bounded slowly-decaying tail, NOT 1e-10 — P7 Newton remains the designed cure for the tail regardless of the stability fix (Newton was renumbered P6→P7 on 2026-07-07 when a new differentiable-flux phase P6 was inserted ahead of it to remove the non-physical supersonic-pocket Cp sawtooth — see roadmap.md P6/P7). Heavy gates (`G4.1 medium`, `G4.3` sweep) still run only under `PYFP3D_TRANSONIC_GATES=1`, excluded from the default suite. **Next: P5 (ONERA M6) is open to start** — M1 mesh family ready and cut_wake-ingested, and both the transonic stability gap and the wake per-station cost that would have hit P5 are now closed. M1 recap (closed 2026-07-07, mesh delivery): `meshgen/wing3d.py` (OCC ruled-loft half wing; `occ.fragment` stitches the TE edge but **`mesh.embed` must follow** or the tets don't conform; axis convention chord x / lift y / span z), `cases/meshes/onera_m6/` (one h_wall parameter, 55.5k/350.7k/2513k tets; **.msh gitignored** — regenerate coarse+medium in ~30 s), `mesh/wake_cut.py` M1 extensions (tip FREE edges stay single-valued ⇒ Γ(tip)=0 discretely; Kutta-probe off-plane fallback; both inert on 2.5D meshes). Also: P1 still open on one item — G1.6 sphere-Cp (strict xfail) awaits its **Option C acceptance re-spec**; curved/isoparametric wall elements separately scoped (design.md §5.1.2); do not propose h-refinement, recovery tweaks, Nitsche, or boundary-data corrections for G1.6 — ruled out with evidence — per [docs/roadmap.md](roadmap.md).

Authoritative docs:
- [docs/design.md](design.md): theory, discretization, architecture, and validation formulas.
- [docs/roadmap.md](roadmap.md): phases, gates, and progress ledger.

Hard rules:
1. After any kernel or assembly change, run `pytest tests/test_v0_freestream.py` first.
2. A phase closes only when its medium-mesh gate and the full coarse regression suite are green.
3. Numba kernels use SoA arrays only, no Python objects inside `@njit`, colored assembly for `prange`, zero allocation in hot loops, `cache=True`, and the `PYFP3D_NOJIT` switch.
4. All sparse linear algebra stays in SciPy/PyAMG.
5. Physics scalars live only in `physics/isentropic.py`.
6. Never edit files under `cases/reference_data/`.
7. Wake-cut changes must rerun topology asserts on all meshes in `cases/meshes/`.
8. Mesh generation stays vanilla Gmsh; node duplication happens only in the solver preprocessor.
9. Validate against full-potential references before Euler.
10. Keep commits phase-scoped and update the v2 progress ledger when a gate closes.
