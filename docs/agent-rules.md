# pyFP3D Agent Rules

Current phase: **P6 open to start (P5 closed 2026-07-08)** — next: the consistent/differentiable artificial-density flux (roadmap P6; it also owns the deferred P5 V6<1% target). **P5 closed (2026-07-08)** — ONERA M6 M0.84/α3.06, demo 16/16 PASS, but only after the medium `physical` root cause was re-diagnosed a SECOND time and the first attribution OVERTURNED: the failure (M_max 5.204, 8 floored/4 limited; 26 M>2 cells = 18 outboard-TE z/b 0.80–0.81 + 8 far-field beyond the tip) was **NOT a TE discretization singularity** as first recorded — four targeted experiments (T1–T4, `cases/demo/p5_onera_m6/INVESTIGATION_kutta_closure.md`) pinned it on a **single-station Kutta-closure failure**: the per-station secant does not converge at the top Mach level on the 3D mesh (pushing 10→16 evals DIVERGES, M_max≈29 — the 10-eval budget was early-stopping regularization) and left st133 (z/b=0.801, steepest |dΓ/dz|) **32% under-circulated** (|F|/Γ=37% vs ≤5% at all 165 other stations); the Γ deficit drives a real TE overspeed that the ρ̃-floor/limiter freeze into a spurious M≈3 state. Decisive test T3: setting ONLY st133's Γ to its own Kutta target collapses the cluster 18→0 cells on the same mesh (a 1/h singularity cannot depend on Γ; the coarse-1.47→medium-5.20 'refinement sharpening' was the finer mesh resolving the same Γ-error's overspeed). T1: 0 tets straddle master+slave (cut clean). T2: better density convergence at the same Γ leaves the defect bit-identical (not under-convergence). The far-field 8 cells (incl. the pre-fix M_max=5.20) were the independent span-uniform-2D-vortex **branch-ray artifact beyond the tip** (no cut there to carry the prescribed ~Γ_mean jump). **Fix (recipe-level, defaults off, all non-3D paths bit-identical):** (1) `farfield_spanwise_gamma=True` — Γ(z)-tapered vortex far field (per-station interpolant, 0 at/beyond the sheet tip; `constraints/dirichlet.py::farfield_dirichlet`); (2) `n_kutta_polish=4` — fixed-Γ Kutta-closure polish after the Mach continuation (`solve/continuation.py`): apply the measured target, re-solve with `omega_rho_polish=0.5`, repeat — secant-free, contractive (|F| halves per step to 5.8e-4). NOTE omega_rho<1 *inside* the active-secant continuation is NOT the fix (measured to diverge). **Closed gate numbers (from-scratch):** coarse M_max 1.398 0/0, shocks x/c 0.596/0.570/0.425, Γ 0.097→0.0206, V6 2.40%, CL 0.2419; medium M_max 1.995 0/0 (the bounded tip-TE-corner P1 overshoot is the only surviving singularity trace), shocks 0.594/0.526/0.345 (~1 cell), Γ 0.097→0.0151, V6 1.82%, CL 0.2453, Kutta |F| 5.8e-4 (28× tighter than pre-fix). **G5.2 V6 re-spec (user-approved):** V6 is a systematic O(h) CL_p-below-CL_KJ discretization floor (sharp-TE/LE P1 wall gradient + P4 Cp sawtooth — both P6 targets; removing the M>2 clusters left V6 unchanged), reported against a 3% floor bound; **<1% is deferred to post-P6, re-measure after G6.1**. **Dead routes stay dead:** spanwise-Γ *smoothing* (A–E, `INVESTIGATION_gamma_smoothing.md` — moves Γ AWAY from the self-consistent value; the polish drives the stuck station TOWARD its own target) and TE-element treatments *for this gate*. **Known-robustness item (recorded, not fixed):** the swept-TE Kutta probe assignment shares probe nodes between adjacent stations (st133/134 share their upper probe; 35 upper/41 lower of 166 — `diagnose_medium.py` audit) — the latent reason st133 specifically stalled. Infrastructure delivered en route (unchanged): `post/surface.py::planform_area`+`cl_kj_3d`, `post/section_cut.py::section_cp_curve` (bit-identical refactor), `solve/continuation.py` `rtol` forwarding (**rtol=1e-7 ~5.5× faster, M_max identical to 5 digits**; default 1e-10 keeps P4 bit-identical), viscous AGARD overlay reference; recipe `seed40/eval300/gamma10/rtol1e-7/polish4/taper`; **all runs cap `NUMBA_NUM_THREADS=16`** (16C/32T shared box). Tests: `tests/test_p5_onera_m6.py` 4 fast + 2 gated (gated updated to the polish recipe + 3% V6 floor bound); the medium demo cache now stores residual/drho histories. Full default suite green: **140 passed + 4 skipped + 2 xfailed**. Prior: **P4 closed (2026-07-07)** — the same-day G4.1 medium divergence was re-diagnosed and fixed: the global mass-lumped pseudo-time damping diag(m_lumped/Δτ) is replaced by **local** θ·diag(A_free) (θ=0.2 — `solve/picard.py::solve_subsonic_lifting`'s `damping_theta` param, mutually exclusive with the retired `pseudo_dt`; the `TRANSONIC_DEFAULTS` default), recomputed every outer iteration so the damping ratio is mesh- and shock-strength-independent by construction. Medium G4.1 PASSES (shock 0.633, |F| 1.23e-4, M_max 1.366, 0/0, 16m39s); G4.2 bitwise no-op + G4.3 sweep re-verified (M0.82/α1.25 cl 0.389→0.458 recorded as a different converged/physical state, not a regression). `WakeConstraint.update_matrix`'s per-station h_j loop batched into one sparse `T^T@(A@G)` product (bit-identical, removes ~166 matvecs/density-iteration on M6 medium). **Transonic convergence semantics unchanged**: engineering-converged (physical M_max, 0 limited/floored, Kutta |F| below eval noise, cl drift <1e-3/hundreds iters); the sharp-shock residual tail is bounded and slowly decaying, NOT 1e-10 — P7 Newton remains the designed cure (Newton renumbered P6→P7 on 2026-07-07 when the differentiable-flux P6 was inserted ahead — roadmap P6/P7). Heavy gates (G4.1 medium, G4.3 sweep, G5.1/G5.2) run only under `PYFP3D_TRANSONIC_GATES=1`. M1 recap (closed 2026-07-07, mesh delivery): `meshgen/wing3d.py` (OCC ruled-loft half wing; `occ.fragment` stitches the TE edge but **`mesh.embed` must follow** or the tets don't conform; axis convention chord x / lift y / span z), `cases/meshes/onera_m6/` (one h_wall parameter, 55.5k/350.7k/2513k tets; **.msh gitignored** — regenerate coarse+medium in ~30 s), `mesh/wake_cut.py` M1 extensions (tip FREE edges stay single-valued ⇒ Γ(tip)=0 discretely; Kutta-probe off-plane fallback; both inert on 2.5D meshes). Also: P1 still open on one item — G1.6 sphere-Cp (strict xfail) awaits its **Option C acceptance re-spec**; curved/isoparametric wall elements separately scoped (design.md §5.1.2); do not propose h-refinement, recovery tweaks, Nitsche, or boundary-data corrections for G1.6 — ruled out with evidence — per [docs/roadmap.md](roadmap.md).

Authoritative docs:
- [docs/design.md](design.md): theory, discretization, architecture, and validation formulas.
- [docs/roadmap.md](roadmap.md): phases, gates, and progress ledger.

Hard rules:
1. After any kernel or assembly change, run `pytest tests/test_v0_freestream.py` first.
2. A phase closes only when its medium-mesh gate and the full coarse regression suite are green.
3. Numba kernels use SoA arrays only, no Python objects inside `@njit`, colored assembly for `prange`, zero allocation in hot loops, `cache=True`, and the `PYFP3D_NOJIT` switch.
4. All sparse linear algebra stays in SciPy/PyAMG.
5. Physics scalars live only in `physics/isentropic.py`.
6. Never edit files under `cases/reference_data/`.
7. Wake-cut changes must rerun topology asserts on all meshes in `cases/meshes/`.
8. Mesh generation stays vanilla Gmsh; node duplication happens only in the solver preprocessor.
9. Validate against full-potential references before Euler.
10. Keep commits phase-scoped and update the v2 progress ledger when a gate closes.
