# pyFP3D Agent Rules

Current phase: **P9 OPEN — grid-convergence & accuracy-gap discrimination (evidence phase, NEW 2026-07-11; user-directed Track-P renumbers, NET same-day mapping: curved walls → P11, backlog → P12; pre-renumber docs use the old IDs). P10 (defined 2026-07-11, NOT started, user-proposed): Newton generality & continuation efficiency — G10.1 non-lifting Newton entry (wc=None, raw mesh, sphere-vs-solve_subsonic acceptance), G10.2 level-adaptive intermediate continuation tolerance (opt-in, default bit-identical, A/B-gated ≥15% on the G8.2 recipe with locks intact; framing correction: the coupled Newton has NO Kutta outer loop — Γ updates every step, ‖F‖ machine-zero; the capability-demo two-level structure is the MACH RAMP, so loose-then-tight applies to continuation levels; P8 trap to test: warm-start only from CONVERGED levels). P9/P10 code work NOT started — user-paused, docs first.** P9 scope (roadmap P9; decision bands PRE-REGISTERED): G9.1 M6 coarse/medium/fine Newton + Richardson extrapolation of cl_KJ/cl_p (bands: cl_KJ∞ ≥ 0.283 ⇒ resolution-dominated; ≤ 0.278 ⇒ floor confirmed; else inconclusive); G9.2 NACA 2.5D coarse/medium/fine subsonic lift oracle (monotone |err| decrease + fine within ±1% of the PG–KT midpoint); G9.3 attribution verdict + P11 (curved walls) go/no-go (user arbitrates). Motivation (P8 capability demo `cases/demo/p8_capability/`, 36/36 PASS, commit 2b9cfc8): the 0.019 M6 lift-gap attribution to the flat-facet wall is inference-grade — 2D sharp-TE lift converges cleanly (−2.7%→−0.3%), M6 cl_KJ still rising under refinement (0.2621→0.2692), unlike the oracle-grade G1.6 sphere evidence. Also recorded by that demo: ★ **fold-zone grid sensitivity** — NACA M0.78 same-condition coarse/medium NOT grid-comparable at α1.25 (cl 0.3399 vs 0.4339) NOR at the fallback α1.0 (0.2626 vs 0.3238), all four true Newton solutions; dcl/dM ≈ 6–10 makes O(h) act like O(0.01) Mach shifts ⇒ per-mesh regression locks, never grid-convergence claims near the fold. New instrumentation (additive keys only, suite 182+8+2 unchanged): `solve_newton_lifting` returns `gamma_history`; `solve_newton_transonic` returns per-level `level_results` (histories/wall_s/timings — the top-level `timings` covers only the final level). P9 rules: no solver/numerics changes; fine meshes stay gitignored; heavy runs are one-shot demo scripts (G8.3 CI budget untouched). Prior: **P8 CLOSED 2026-07-11 (G8.1 + G8.2 + G8.3 all closed).** **N6 + G8.2/G8.3 delivery (2026-07-11):** ONERA M6 Newton closes P8. (1) **lagged-LU direct mode** in `solve/newton.py::solve_newton_lifting` — new `direct_refactor_every` (default 1 = N5 behavior bit-identical, suite-locked) + `direct_reuse_rtol` (1e-8): refactor the LU every k-th step, in between GMRES on the FRESH coupled operator preconditioned by the stale LU; GMRES failure → refactor + exact Woodbury in the same iteration (robustness never below every-step-direct). Why: **true-3D LU fill makes splu ~100× the 2.5D cost at equal dofs** (measured 18.6 s vs ~0.2 s at ~6e4 dofs; the every-step M6 medium run was 1606 s, 97% splu — NOTE the returned `timings` dict is the FINAL level only, which hid this on first read; cProfile settled it). (2) **`NEWTON_M6_RECIPE`** (tests/test_p8_newton.py): dm 0.05 (P5 schedule — M6 is far from the NACA-medium fold, zero dm-halving), dm_min 0.01, freeze_tol 1e-6, refresh 8, precond direct, refactor_every 1000 (once per level + on-demand), farfield_spanwise_gamma=True. (3) **G8.2 closed: M6 medium 249.2 s end to end** (mesh 7.3/solve 239.8/post 2.1; 63k nodes; coarse 42 s) — **timing protocol: the 16-thread cap must include BLAS/OMP** (`NUMBA_NUM_THREADS=16 OMP_NUM_THREADS=16 OPENBLAS_NUM_THREADS=16`; A/B measured: without the BLAS/OMP caps the identical gated run is ~333 s and FAILS the 300 s assert — oversubscription costs ~33% on the 16C/32T box); **G8.3 closed: suite 301.66 s**, baseline now **182 passed + 8 skipped + 2 xfailed** (+1 gated skip = `test_g82_m6_medium_newton_end_to_end`). (4) **M0.84/α3.06 reachable on BOTH M6 meshes** — the fold contingency never triggered; terminal quadratic in frozen phases (medium …2.6e-7→2.1e-10→7.0e-15), 0 lim/flr, Kutta ~2e-16. (5) **P5-caveat MEASURED** (closes the recorded item): Newton residual at committed P5 Picard states 8.6e-6 coarse / 7.6e-6 medium (|F| ~5.5e-4) — not solutions (P4-erratum in kind, milder); true solutions **cl_p 0.2560/+5.8% coarse, 0.2646/+7.9% medium**, shocks essentially unchanged (0.596/0.541/0.362 vs 0.594/0.526/0.345), M_max 2.13; **under-convergence is circulation/lift, not shock position**; cl_KJ 0.2692 narrows the P9 inviscid-vs-Tranair(0.288) gap 0.043→0.019. P5 ledger annotated (gates stand as Picard-quality; reference_data untouched, hard rule 6). (6) Solution identity: coarse continuation-path independent to 1e-15; medium dm-0.025 vs dm-0.05 paths differ |dφ| 2.6e-4 / cl equal to 5 digits — the N5 assignment-floor semantics (live floor ~5e-5, stale 84 vs 92), lock bands (cl ±0.005, shock ±0.02, M_max ±0.05) cover it. Evidence: gated test + demo `cases/demo/p8_newton/` part 3 (V8.1c convergence, V8.2 M6 runtime breakdown, `g82_m6_medium.csv` incl. `newton_residual_at_p5_state`). Prior: **N5 + G8.1 delivery (2026-07-11):** transonic Newton robustness chain in `solve/newton.py` + `kernels/upwind.py`: (1) `precond="direct"` exact steps (splu + Woodbury on the rank-n_st Γ-coupling) — **the shock-position soft mode stiffens under refinement and η-accurate Krylov steps STALL** (measured: frozen-system residual flat at 3.7e-6 with GMRES converging to η=1e-2 on medium; direct steps → crisp 1e-13); (2) **stall-adaptive freeze** of the upwind assignment (`freeze_upwind_state`/`rho_tilde_frozen_sweep`/`rho_tilde_frozen_sensitivities_sweep` — classification forced, bitwise-identical to the live sweep at the freeze state; NO floor on frozen branches) + **active-set refresh** with two-cycle acceptance and honest `residual_unfrozen`/`n_assignment_stale` reporting — the 2.5D prism-split family parks ~1e3 elements in the max(ν_e,ν_u) NEAR-TIE band (P7 kink trap in Newton form): live Newton limit-cycles (measured floors 3e-9@M0.7375…1.4e-5@M0.80 medium; branch flips 300-800/step, u-flips 40-120/step); within a frozen assignment the system is smooth and terminal-quadratic; (3) safety nets: freeze-revert on frozen-path divergence (10× or any lim/floor — frozen sweeps are floor-free by design), level fail-fast (100× above level best), best-of-tried line search (unconditional λ-floor acceptance allowed sustained growth). Recipe = `tests/test_p8_newton.py::NEWTON_TRANSONIC_RECIPE` (dm 0.025/dm_min 0.003/freeze_tol 1e-6/refresh 8/direct/budget 60). **★ Baseline findings (user-arbitrated 2026-07-11):** (a) **P4 Picard states are NOT discrete solutions** (roadmap P4 erratum): Newton residual at the committed coarse M0.80 state = 2.2e-4; Newton from it converges (6 steps) to the TRUE solution shock 0.658/cl_p 0.459/M_max 1.408 (dissipation-robust 2.0→1.5 path-independent to the last bit; C=1.2 diverges — don't go below ~1.5 on coarse); the "bounded residual tail" WAS the stall; G4.3's damping-dependent cl 0.389→0.458 retro-explained. (b) **medium steepens into the FP non-uniqueness fold**: Newton-converged M0.775 shock 0.570/cl 0.396 (1.8e-13), M0.7875 shock 0.674/cl 0.523 (7.9e-11), NO reachable isolated solution at M0.80 (M_max≈1.45 > isentropic validity; conservative FP over-lifts vs Euler at strong shocks — Holst). ⇒ **G8.1 re-specced (user-approved) to coarse M0.80 + medium M0.7875** with regression-lock bands around the measured Newton solutions; `cases/reference_data/naca0012_m080/` untouched (hard rule 6); P4 gates stand as Picard-quality gates (warm-start engine). G8.1 evidence: gated `test_g81_terminal_quadratic_*` ×2 + gated Newton-field FD pocket (rel ~1e-10) + demo `cases/demo/p8_newton/` (V8.1a/b, V8.2-lite; medium run ~100 s vs Picard 16m39s-to-a-non-solution). Kutta |F| ~1e-16 (coupled) vs secant 1e-4. **P5 caveat recorded:** M6 Picard results carry the same convergence-quality caveat in kind — N6's M6 Newton run will measure it. Protocol notes for future FD/convergence tests: quadratic-drop pairs must be SEARCHED over the level history (freeze-refresh honesty re-evaluations interleave live jumps with the quadratic frozen phases). Prior: **P8 N2–N4 delivery (2026-07-10):** exact Newton Jacobian (6.3) at frozen selection in `kernels/jacobian.py::assemble_newton_jacobian` — Terms 1+2 fused on the SHARED Picard CSR pattern/coloring (Term 2 added only when s_e≠0 ⇒ masked/limited elements reduce to the Picard A **bitwise**; a fused `w1*(..)+w2*(..)` FMA-contracts differently under fastmath — measured, hence the split), Term 3 = **active-set COO** (16 entries per s_u≠0 element, rows nodes(e) → cols nodes(u(e)) at graph-distance ≤4, rebuilt every Newton step ⇒ selection churn cannot corrupt a reused pattern; **NO recolor/wider-CSR machinery** — COO is ms-scale at pocket sizes; `newton_nnz`/`n_term3_active` recorded = the N2 measurement), speed-limiter consistency by masking s_e/s_u with `lim = (q2l == q2n)` (flat clamp ⇒ derivative 0; bitwise no-op at 0 limited). `wake.py::reduce_operator(A) → (TᵀAT, TᵀAG)` pure extraction (`update_matrix` delegates bit-identically); called on the Newton J, the H column IS the exact wake-jump ∂R_red/∂Γ. `solve/linear.py::solve_gmres` (auto-retry 2× restart) + `build_ilu_preconditioner` — the exact Jacobian is NONSYMMETRIC in supersonic zones (asserted in tests), CG inapplicable. `solve/newton.py`: `NewtonWorkspace` with ONE shared `eval_residual` state-reconstruction path (residual/Jacobian consistency = the quadratic-convergence prerequisite); affine far-field basis `vals_red(Γ)=vals0_red+V_red·Γ` extracted by unit-Γ probing of `farfield_dirichlet` (linearity guarded at machine precision); sparse Kutta row K (±1/n_j; shared probes appear in both rows = correct derivative). `solve_newton_lifting` does the **exact δΓ elimination** ((2,2) block = −I): `(J_ff + B·K) δφ = −R_free − B·F`, `δΓ = K δφ + F`, with `B = J_red[free,dir]@V_red + H_J[free,:]` — **the easy-to-miss far-field vortex column is IN and FD-guarded** (`test_gamma_column_fd`, the trap detector); GMRES on the low-rank LinearOperator, AMG built on the SPD Term-1 block (rebuilt every 2 steps), Eisenstat–Walker choice-2 forcing (η₀=1e-2 — note it makes a LINEAR m_inf=0 problem take ~3 inexact steps unless tightened), safety-only backtracking line search, **no damping_theta anywhere in Newton** (Picard stabilizer — kills quadratic convergence), optional consistent `ptc_dtau`; convergence refused while limited/floored > 0. `solve_newton_transonic` = upward-only Mach-continuation SKELETON (warm start from the last CONVERGED level, dm halving to dm_min, `upwind_c_post` for López's post-ramp μ_c 2.0→1.6; interfaces final, **untuned — N5's job with gated G4.1 runs**). **Measured:** NACA coarse M0.5/α2 — seeded Newton 2 steps 4.2e-7→3.8e-9→**4.7e-13**; cold start 3 steps, observed orders **[2.57, 1.79]**, terminal step gains >3 digits; Γ/φ/cl match the P3 Picard/secant to **1.9e-8 / 2.0e-8 / ~1e-7**; m_inf=0 → ONE step ≡ P2 Laplace (with η tightened). **Jacobian JVP vs frozen-selection residual FD: rel ~1.3–1.5e-10** (tol 1e-6) in all regimes incl. Term 3 (938 active on the constructed cube pocket). Tests: `test_p8_jacobian.py` (7 + 1 gated converged-pocket FD = the G8.1 FD clause, runs at N5) + `test_p8_newton.py` (8); green under JIT and PYFP3D_NOJIT; suite **180 passed + 5 skipped + 2 xfailed** (baseline 165+4+2, zero regressions; forward Picard paths byte-untouched, structural bit-guard test). FD-kink protocol inherited from P7: generic noise-broken fields, element-level ε-guard lifted to residual ROWS (nodes of kink elements), assert the kink ELEMENT fraction (row fraction is inflated on small cubes). Prior: **P7 CLOSED 2026-07-10 (frozen-selection ∂ρ̃/∂φ of the walk flux); P8 = fully-coupled (φ_red, Γ) Newton.** **P7 delivery (2026-07-10):** exact branch-wise sensitivities `(s_e, s_u) = (∂ρ̃_e/∂q²_e, ∂ρ̃_e/∂q²_u)` at frozen u(e) in `kernels/upwind.py::rho_tilde_sensitivities_sweep` + `UpwindOperator.rho_tilde_sensitivities` (walk mode only; floored/self-upstream flat branches → 0; new scalar `isentropic.py::mach_squared_derivative_wrt_q_sq`); G7.3 FD-verified vs the SHIPPED `rho_tilde_sweep` at frozen selection: **3–5e-10** cube (8 tests, `test_p7_diff_flux.py`, green under PYFP3D_NOJIT), **3.5e-9** constructed NACA-coarse multi-regime, **5.7e-9 on the converged G4.1 M0.80 field** (pocket 1189 accel + 977 shock-point). `max_ε` NOT implemented (scope decision) — forward flux byte-untouched, G7.1/G7.2 held by construction; P4/P5/P6 bit-identical; suite 165+4+2. **Sign fix:** design.md §6.3 `dμ/dM² = +M_c²/M⁴` (FD-arbitrated; "−" was a typo). **Kink trap for P8:** FD probes straddling the max(ν_e,ν_u) tie / switch threshold read branch AVERAGES (~1e-5, not a bug; measure-zero locus, active set freezes near the solution); separable fields on structured/prism-split meshes park whole slabs ON the tie — use generic/noise-broken fields in any future FD check. P8 consumes `(s_e, s_u)` × `2∇φ·∇N_k` as Term-2/Term-3; stencil +1 upstream element/row at graph-distance ≤4. Prior: **P6 CLOSED 2026-07-08 (surface-Cp recovery / sawtooth); P7 was re-scoped 2026-07-08: Newton prereq = the shipped **frozen-walk** ∂ρ̃/∂φ, sparse ~+1 element/row; the streamline-Gaussian kernel is demoted to an **optional** Picard-speed path, a P8 flux candidate only if its denser Jacobian measures favourable. Track P renumbered: P6 recovery / P7 flux / P8 Newton / P9 curved walls / P10 backlog.** **★ N1 correction (2026-07-08): the surface-Cp sawtooth is a per-triangle wall-gradient RECOVERY artifact, NOT the artificial-density flux** — proven decisively: nodal/edge-neighbour smoothing of the *same* walk solution's wall gradient drops the G6.1 metric ~330× (0.0758→0.00023) while a smoother flux (the streamline kernel) does not reduce it at all. So G6.1's fix is a **normal-gated wall-gradient recovery smoothing** in post-processing (`post/surface.py::smooth_wall_tangential_gradients`, `smooth_passes` on `wall_cp_curve`/`section_cp_curve`/`wall_force_coefficients`, default 0 = bit-identical, preserves the sharp TE; design.md §9.1). This **overturns** the earlier ρ̃-selection-flip attribution (demo_report §P4). The differentiable **flux** kernel (streamline-Gaussian, `UpwindOperator(weighted=True, mode="kernel")`, opt-in, ~10× faster/iter, stable, C^∞ in ∇φ) is an **optional** Picard-speed path, **NOT** the P7 Newton prerequisite (re-scoped 2026-07-08: the prereq is the frozen-walk ∂ρ̃/∂φ, sparse; the kernel's Newton Jacobian is denser — a P8 candidate only if measured favourable) — it does not fix the sawtooth. P4/P5 solutions unchanged (post-processing only; shock/M_max/cl_KJ identical); only reported cl_p (−0.3 % coarse)/cd_p (~15 %, untrusted FP drag) shift under smoothing. Tests: `test_p6_recovery.py`, `test_p6_cp_metric.py` (metric re-specced shock-robust: sign-alternating so the monotone shock isn't counted). Demo `cases/demo/p6_surface_recovery/`. **Track-P renumber (2026-07-08): P6 = surface recovery (this, done); P7 = differentiable flux at frozen selection (Newton prereq = frozen-walk ∂ρ̃/∂φ; kernel optional); P8 = fully-coupled Newton; P9 = curved wall elements (G1.6 + residual V6<1%); P10 = backlog.** **Original P6 phase framing (design pass 2026-07-08):** next: the consistent/differentiable artificial-density flux (roadmap P6; it also owns the deferred P5 V6<1% target). **P6/P7 design pass (2026-07-08, design.md §3.1–3.2 / §6.3 / §8.1 / §13; roadmap P6/P7 sub-phases + ledger):** the López dissertation was cross-checked directly against the PDF (`docs/references/Dissertation_Inigo_Lopez.pdf`), confirming the fully-coupled-Newton / full-Jacobian / nonzero-upstream-coupling (Eq. B.4) / frozen-selection architecture, and correcting 3 claims in discussion-note 4 — (1) López used a **sharp** TE (Eq. 4.2 sharpens the NACA0012), not blunt, and still converged quadratically → a sharp 2D TE is **not** a Newton obstacle; (2) within a Mach ramp M_crit/μ_c are held **fixed**, with μ_c *decreased* 2.0→1.6 only *after* reaching target M∞ on ONERA M6 (Table 4.13), not swept per step; (3) Newton differentiability needs a flux differentiable **at frozen upstream selection**, not a differentiable selection. P6 accordingly has two separable defects — the surface-Cp **sawtooth** (a selection-flip *spatial* artifact) and Newton **non-differentiability** — both addressed by the **streamline-projected weighted upwind density** (design.md Eq. 3.4, `ρ_up=Σ_f w_f ρ_{nb(f)}/Σ_f w_f`, `w_f=max(0,−V_e·n̂_f)^p`); the smooth density clamp (note-4 "N0") is **optional** (converged states have 0 floored/limited, so hard clamps never enter the converged Jacobian). P7 = fully-coupled (φ_red, Γ) Newton eliminating the P5-fragile Γ-secant; the Γ-Jacobian blocks are nearly in-code (`wake.py::self._h`, `kutta_targets`) but the far-field vortex column `−A_coupling·∂vals_red/∂Γ` must be added. **P5 closed (2026-07-08)** — ONERA M6 M0.84/α3.06, demo 16/16 PASS, but only after the medium `physical` root cause was re-diagnosed a SECOND time and the first attribution OVERTURNED: the failure (M_max 5.204, 8 floored/4 limited; 26 M>2 cells = 18 outboard-TE z/b 0.80–0.81 + 8 far-field beyond the tip) was **NOT a TE discretization singularity** as first recorded — four targeted experiments (T1–T4, `cases/demo/p5_onera_m6/INVESTIGATION_kutta_closure.md`) pinned it on a **single-station Kutta-closure failure**: the per-station secant does not converge at the top Mach level on the 3D mesh (pushing 10→16 evals DIVERGES, M_max≈29 — the 10-eval budget was early-stopping regularization) and left st133 (z/b=0.801, steepest |dΓ/dz|) **32% under-circulated** (|F|/Γ=37% vs ≤5% at all 165 other stations); the Γ deficit drives a real TE overspeed that the ρ̃-floor/limiter freeze into a spurious M≈3 state. Decisive test T3: setting ONLY st133's Γ to its own Kutta target collapses the cluster 18→0 cells on the same mesh (a 1/h singularity cannot depend on Γ; the coarse-1.47→medium-5.20 'refinement sharpening' was the finer mesh resolving the same Γ-error's overspeed). T1: 0 tets straddle master+slave (cut clean). T2: better density convergence at the same Γ leaves the defect bit-identical (not under-convergence). The far-field 8 cells (incl. the pre-fix M_max=5.20) were the independent span-uniform-2D-vortex **branch-ray artifact beyond the tip** (no cut there to carry the prescribed ~Γ_mean jump). **Fix (recipe-level, defaults off, all non-3D paths bit-identical):** (1) `farfield_spanwise_gamma=True` — Γ(z)-tapered vortex far field (per-station interpolant, 0 at/beyond the sheet tip; `constraints/dirichlet.py::farfield_dirichlet`); (2) `n_kutta_polish=4` — fixed-Γ Kutta-closure polish after the Mach continuation (`solve/continuation.py`): apply the measured target, re-solve with `omega_rho_polish=0.5`, repeat — secant-free, contractive (|F| halves per step to 5.8e-4). NOTE omega_rho<1 *inside* the active-secant continuation is NOT the fix (measured to diverge). **Closed gate numbers (from-scratch):** coarse M_max 1.398 0/0, shocks x/c 0.596/0.570/0.425, Γ 0.097→0.0206, V6 2.40%, CL 0.2419; medium M_max 1.995 0/0 (the bounded tip-TE-corner P1 overshoot is the only surviving singularity trace), shocks 0.594/0.526/0.345 (~1 cell), Γ 0.097→0.0151, V6 1.82%, CL 0.2453, Kutta |F| 5.8e-4 (28× tighter than pre-fix). **G5.2 V6 re-spec (user-approved):** V6 is a systematic O(h) CL_p-below-CL_KJ discretization floor (sharp-TE/LE P1 wall gradient + P4 Cp sawtooth — both P6 targets; removing the M>2 clusters left V6 unchanged), reported against a 3% floor bound; **<1% is deferred to post-P6, re-measure after G6.1**. **Dead routes stay dead:** spanwise-Γ *smoothing* (A–E, `INVESTIGATION_gamma_smoothing.md` — moves Γ AWAY from the self-consistent value; the polish drives the stuck station TOWARD its own target) and TE-element treatments *for this gate*. **Known-robustness item (recorded, not fixed):** the swept-TE Kutta probe assignment shares probe nodes between adjacent stations (st133/134 share their upper probe; 35 upper/41 lower of 166 — `diagnose_medium.py` audit) — the latent reason st133 specifically stalled. Infrastructure delivered en route (unchanged): `post/surface.py::planform_area`+`cl_kj_3d`, `post/section_cut.py::section_cp_curve` (bit-identical refactor), `solve/continuation.py` `rtol` forwarding (**rtol=1e-7 ~5.5× faster, M_max identical to 5 digits**; default 1e-10 keeps P4 bit-identical), viscous AGARD overlay reference; recipe `seed40/eval300/gamma10/rtol1e-7/polish4/taper`; **all runs cap `NUMBA_NUM_THREADS=16`** (16C/32T shared box). Tests: `tests/test_p5_onera_m6.py` 4 fast + 2 gated (gated updated to the polish recipe + 3% V6 floor bound); the medium demo cache now stores residual/drho histories. Full default suite green: **140 passed + 4 skipped + 2 xfailed**. Prior: **P4 closed (2026-07-07)** — the same-day G4.1 medium divergence was re-diagnosed and fixed: the global mass-lumped pseudo-time damping diag(m_lumped/Δτ) is replaced by **local** θ·diag(A_free) (θ=0.2 — `solve/picard.py::solve_subsonic_lifting`'s `damping_theta` param, mutually exclusive with the retired `pseudo_dt`; the `TRANSONIC_DEFAULTS` default), recomputed every outer iteration so the damping ratio is mesh- and shock-strength-independent by construction. Medium G4.1 PASSES (shock 0.633, |F| 1.23e-4, M_max 1.366, 0/0, 16m39s); G4.2 bitwise no-op + G4.3 sweep re-verified (M0.82/α1.25 cl 0.389→0.458 recorded as a different converged/physical state, not a regression). `WakeConstraint.update_matrix`'s per-station h_j loop batched into one sparse `T^T@(A@G)` product (bit-identical, removes ~166 matvecs/density-iteration on M6 medium). **Transonic convergence semantics unchanged**: engineering-converged (physical M_max, 0 limited/floored, Kutta |F| below eval noise, cl drift <1e-3/hundreds iters); the sharp-shock residual tail is bounded and slowly decaying, NOT 1e-10 — P7 Newton remains the designed cure (Newton renumbered P6→P7 on 2026-07-07 when the differentiable-flux P6 was inserted ahead — roadmap P6/P7). Heavy gates (G4.1 medium, G4.3 sweep, G5.1/G5.2) run only under `PYFP3D_TRANSONIC_GATES=1`. M1 recap (closed 2026-07-07, mesh delivery): `meshgen/wing3d.py` (OCC ruled-loft half wing; `occ.fragment` stitches the TE edge but **`mesh.embed` must follow** or the tets don't conform; axis convention chord x / lift y / span z), `cases/meshes/onera_m6/` (one h_wall parameter, 55.5k/350.7k/2513k tets; **.msh gitignored** — regenerate coarse+medium in ~30 s), `mesh/wake_cut.py` M1 extensions (tip FREE edges stay single-valued ⇒ Γ(tip)=0 discretely; Kutta-probe off-plane fallback; both inert on 2.5D meshes). Also: P1 still open on one item — G1.6 sphere-Cp (strict xfail) awaits its **Option C acceptance re-spec**; curved/isoparametric wall elements separately scoped (design.md §5.1.2); do not propose h-refinement, recovery tweaks, Nitsche, or boundary-data corrections for G1.6 — ruled out with evidence — per [docs/roadmap.md](roadmap.md).

Authoritative docs (the ONLY documents development plans against):
- [docs/design.md](design.md): theory, discretization, architecture, and validation formulas.
- [docs/roadmap.md](roadmap.md): phases, gates, and progress ledger.

NOT authoritative: `docs/discussion_notes/` (DN1–DN6, PLAN.md) is discussion and
reference material only — exploratory analyses, candidate designs, integration
views. Never implement from a discussion note: its content becomes actionable
only after being merged into roadmap.md/design.md, and where a note conflicts
with roadmap/design, roadmap/design win.

Hard rules:
1. After any kernel or assembly change, run `pytest tests/test_v0_freestream.py` first.
2. A phase closes only when its medium-mesh gate and the full coarse regression suite are green.
3. Numba kernels use SoA arrays only, no Python objects inside `@njit`, colored assembly for `prange`, zero allocation in hot loops, `cache=True`, and the `PYFP3D_NOJIT` switch.
4. All sparse linear algebra stays in SciPy/PyAMG.
5. Physics scalars live only in `physics/isentropic.py`.
6. Never edit files under `cases/reference_data/`.
7. Wake-cut changes must rerun topology asserts on all meshes in `cases/meshes/`.
8. Mesh generation stays vanilla Gmsh; node duplication happens only in the solver preprocessor.
9. Validate against full-potential references before Euler.
10. Keep commits phase-scoped and update the v2 progress ledger when a gate closes.
11. Code only against roadmap.md gates + design.md numerics; `docs/discussion_notes/` is discussion/reference material, never a coding spec.
