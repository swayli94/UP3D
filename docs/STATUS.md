# pyFP3D 开发现状（快照）

> **快照日期：2026-07-14。** 本文件是给人读的高层概览，**不是**权威进度源：
> 阶段/gate 状态以 [roadmap.md](roadmap.md)（含进度台账）为准，当前阶段细节以
> [agent-rules.md](agent-rules.md) 的 "Current phase" 行为准，证据在
> [demo_report.md](demo_report.md)。若本文件与它们冲突，以它们为准。
> 每次 gate 关闭后按 CLAUDE.md 工作流更新 roadmap/agent-rules 时，顺手刷新本文件。

## 一句话状态

求解器主线（Track P）P0–P9 全部关闭（P10 的 G10.2/G10.3 亦关）。**当前工作重心 =
P13（翼尖/尾迹边缘奇点）**，它是 P9 判别结论的直接后继。P9 判定 3D 升力缺口不是壁面
单元的问题，而是刚性平面尾迹在翼尖自由边上的涡片边缘奇点；**P13 把这条线走完了，并且
沿途推翻了两次自己的框架**：奇点的机理**既不是卷起也不是涡核，而是离散的**（最外侧 TE
站把残留 Γ_last 在一个单元内当集中涡脱落 ⇒ p≈1−q，判据 q≥1），所以**修复不需要改尾迹
模型、也不经 Track B**——只需在 Kutta 目标上做**展向载荷锥化**（G13.2 ✓）。此后又发现
并修掉了 **M6 网格族的 h_far 钳位**（Track M M1b ✓，`coarse` 原本根本不在加密射线上，
使过去一切 M6 三点 Richardson 无效，含 G9.1）。**★ 但 3D 网格收敛仍未拿到：还有第三个
奇点，在壁面上——`wing3d.py` 的平顶翼尖盖**（真实 M6 是圆顶）**，其锐凸棱随加密发散
（p=+0.321）**，而尾迹边与机翼内部都已有界/收敛（demo `run_g133_ladder.py` 5/5）。
**⇒ 顶盖已倒圆（Track M M5 ✓ 2026-07-13），且 G13.3 亚声速 Richardson 已挣得**：
圆帽阶梯 M∞0.5 上 p=2.31、cl_KJ(h→0)=0.2050——全项目第一个合法的 3D 三点外推。
**但 G13.3 跨声速为阴性（2026-07-13/14）**：圆帽 fine 的 Mach 续接死在 M=0.75、从未
到达 M0.84（site = 设计尖锐的 tip TE，圆帽**放大**而非制造它；平帽序列则非渐近）⇒
**M0.84 的 P9 判决带在两种几何上都未挣得**。P11 曲面壁元
作为"3D 升力修复"已撤销（仅剩 G1.6 一条理由；且它也修不了几何锐棱）。
Track B 侧：
B1–B5 + **B7** 已关（level-set 尾迹 + 隐式 Kutta 已产生升力，无 Γ secant）；
**★ B7（ONERA M6 三维 gate）2026-07-12 一次通过**——双网格达标，且 B6 的**升力反转在
三维复现**：以 conforming **Newton 真解**（cl_KJ 0.2692）为基准，LS Picard 落在
**+2.7%(M1) / +0.7%(M4 无尾迹工作流网格)**，而 conforming Picard（P5, 0.24788）低了
**−8.6%**；三维专属机械（斜交标架、展向裁剪 ⇒ Γ(tip)→3e-4 离散归零）**无需新求解器
代码**，唯一缺口是后处理（`section_cp_curve_levelset` / `cl_pressure_3d_levelset`）；
**三维远场 = `neumann`，P5 的 Γ(z) taper 在 B 路径上结构性地不需要**（B 路径的涡是
展向均匀、支割线恒为 y=0 的二维涡，两个独立错法都已实测；neumann 不带涡故二者皆
不可能）。诚实缺口：顶部 Mach 级停在 Picard 残差尾（|R| 4–6e-6，有界物理、指标在带内，
gate 断言"有界"而非 `converged`）；**LS Newton 在 M6 上推迟**（朴素 splu，需 lagged-LU）。
**B8（LS 翼尖去奇异）已于 2026-07-14 以 characterized-not-cured 关闭（用户裁决）**：
委员会认定的 LS 翼尖指数 p=+1.34 是 `element_mach2` 混合侧读法的 **×5 度量伪影**
（诚实值 +0.62/+0.37，与 conforming 的 +0.52 是**同一对象、同一量级**——每个已关
gate 都带着它活着）；两条约束侧修法（TE 行混合 taper、span-blend）实测皆死
（后者焊掉终止环但全局升力掉 ~20%——隐式 Kutta 无逐站目标，片上任何 δ-pin 被
~10× 放大）；真正的治愈需改片终止处的**函数空间**（记录在案，未排期）。
**下一步 = B9（翼身 LS 求解，M∞0.5；M2 网格已交付）**。
（★ Track-B 重编号 2026-07-13:新插入 B8 = LS 翼尖去奇异,旧 B8 多尾迹 → **B9**,
旧 B9 曲线尾迹（搁置）→ **B10**。）**B6（跨声速）仍进行中**——
coarse M0.80 gate 已达成（基线改为同网格 Newton 真解）、**LS Newton 已交付并 FD 验证、
在折叠区取得二次收敛真解**（工作流网格 M3 medium |R| 1.5e-12），medium 定量收尾余两项
（用户决定暂不追）。

## 阶段一览

| 阶段 | 状态 | 要点 |
|------|------|------|
| P0–P3 | ✓ | 基础设施、Laplace、尾迹割缝/Kutta、亚声速压缩（P1 仅 G1.6 未关，见下） |
| P4 | ✓（带勘误） | 人工密度跨声速 Picard；★2026-07-11 勘误：Picard 态**不是**离散方程的解（Newton 残差 2.2e-4），P4 gate 降级为 Picard 质量/鲁棒性 gate（warm-start 引擎） |
| P5 | ✓（带注记） | ONERA M6 M0.84/α3.06；同类欠收敛注记（真解 cl 高 +5.8%/+7.9%，激波位置不变） |
| P6 | ✓ | 表面 Cp 锯齿 = 壁面梯度**恢复**伪迹（非通量），法向门控平滑修复 |
| P7 | ✓ | 冻结选择下的 ∂ρ̃/∂φ 精确灵敏度，FD 验证 3e-10~6e-9 |
| P8 | ✓ | 全耦合 (φ_red, Γ) Newton：精确 Jacobian + 直接步 + 停滞自适应冻结；G8.2 M6 medium 249 s（G10.2 后 ~145 s）、G8.3 全套件 302 s |
| P9 | ✓（2026-07-11） | 判别阶段。**G9.2 干净 PASS：2D 尖 TE 升力误差 2.71%→0.33%→0.03% ⇒ 尖 TE 不构成升力地板**（"尖 TE/LE 平壁"归因的 2D 支腿没了）。**G9.1 INVALID：M6 fine 不是离散解**（无限幅 M_max 1.40→2.13→**7.93**，9 个单元触 M_cap=3 ⇒ 无三点 Richardson）。★**发散奇点位于尾迹片的自由翼尖边**（9 个封顶单元全在 z/b 0.998–1.000、TE 之后、弦平面内）= 刚性平面尾迹的经典涡片边缘奇点 ⇒ **尾迹模型缺陷（修复=卷起/翼尖涡，非 Track B——见 p13_tip_edge_singularity demo），不是壁元缺陷**。G9.3：0.019 缺口按原提法**不可拆分**（份额如实记 n/a）。顺带测得：`precond="amg"` + 紧 EW forcing（η=1e-8）在每个规模都更快且有效 |
| P10 | ◐ | G10.2 ✓（分裂裁决：M6 +41% 提速已晋升；折叠区禁用松容差）；G10.3 ✓（裁决：**保留 Mach ramp**）；G10.1（非升力 Newton 入口）未动、无顺序约束 |
| P11 | 未开（条件性） | 曲面/等参壁元。**"3D 升力修复"这一立论已撤销**：G9.3 先判其不被 P9 支持（曲面壁元去不掉尾迹片边缘奇点），P13/G13.2 进一步用真实 fine 解**强烈指示其无立足点（★2026-07-14 用户裁决措辞：strongly indicated moot、NOT earned，非"证伪"）**——该 M0.84 跑**已重跑并提交证据**（`g132_transonic.csv`，cl_KJ 0.2866 逐位复现），但为平顶盖单点、非渐近，且圆顶盖 M0.84 fine 从未到达目标 Mach（G13.3 跨声速阴性）⇒ P9 判决带两种几何皆未触发。**仅剩 G1.6**（光滑曲壁上的球面 Cp，另一机理）为有效理由 — 待用户仲裁。注意：**第三个奇点（平顶盖锐棱）也不是 P11 能修的**——等参单元修不了真正锐的几何棱，那是 Track M 的几何问题 |
| P12 | 未开 | Backlog：离散伴随、VII transpiration、混合单元/BO 标定 |
| P13 | ◐（G13.1 ✓ + **G13.2 conforming ✓** 2026-07-13；G13.2 level-set 条款 + G13.3 未闭） | ★ **G13.2：翼尖奇点已修复（conforming 路径）**。修复 = **展向载荷锥化** `Γ_eff(z)=F(z)·Γ_Kutta(z)`（`tip_taper_factors`；`solve_newton_lifting(tip_taper=…)`，默认 None **逐位不变**；经 `newton_kw` 直达 transonic driver）。**发货模型（用户裁决）：`vanish_smooth`（smoothstep，紧支撑），r_c = 0.05·b_semi**。★**机理是离散的——不是卷起、不是涡核、也不是连续极限的边缘指数**（design §4.1 先前两版说法皆被推翻）：求解器看不到连续边缘，它看到的是**最外侧 TE 站点**，该站把残留环量 `Γ_last` 在**最后一层单元内**当作**集中涡**脱落（自由边节点单值 ⇒ 跳跃在一个单元内落到 0），诱导 `~Γ_last/h`。`Γ_last ~ h^q` ⇒ **p ≈ 1 − q**，判据 **q ≥ 1**；对基线预测精确（实测 q=0.44 ⇒ p_pred 0.56 vs p_meas 0.52）。★**锥化被放大而非直接施加**：Γ 是 `Γ=F·Γ_Kutta(Γ)` 的不动点、Kutta 映射斜率 b≈0.93（P2）⇒ `Γ/Γ* = F(1−b)/(1−Fb)`，F=0.8 给 **0.21×**（已锁测试）。**实测**：边缘峰值（严格离体盒）**0.712→0.981→1.510（p=+0.592）** vs 锥化后 **0.567→0.565→0.570（p=+0.009）**；★**M6 fine 现在是真离散解（收敛，0 限幅/0 落地/0 NaN）**——正是 G9.1 做不到的事；跨声速 M0.84 coarse/medium 0 限幅、medium **M_max 2.13→1.725**。**代价**：cl_KJ **−1.1~−1.6%**（随 r_c 缩放），且**局部**（η<0.95 内侧 Γ/cl/Cp 不变，内侧环量 −0.51%；η=0.90 的 TE 闭合与基线一致）。★**tanh 形式确实能正则化（q≈1.00，恰是边缘情形）但因"无紧支撑"被否**：tanh 永不到 1，内侧卸载到 η=0.77（57/83 站），−7.4% 升力、−4.9% 内侧环量，且在 η=0.90 **破坏 TE 压力闭合**（gap 0.972 vs 基线 0.218）。★**度量陷阱**：G13.1 的 tip 盒含机翼单元，边缘一旦正则化其 max 会**迁移到机翼吸力峰**，使**有效**的修复看起来更糟——必须用离体盒（dx>0, z/b>0.98）。**用户提出的两个伪迹已证实与锥化无关**：Cp 锯齿 = P6 壁面梯度**恢复**伪迹（`smooth_passes`，默认 0；反转 40→2；三个模型原始计数**完全相同**）；Γ(z) 抖动 = conforming **Kutta 探针布置**（P5 已记录；RMS d² 0.042 vs P5 的 0.097；LS 路径结构上光滑 11–12×）；基线 TE Cp 间隙 0.14–0.22 = conforming **势跳形式**的 Kutta 只是压力相等的近似 + 尖 TE P1 地板（这正是 B4 需要显式非线性 |q_u|²=|q_l|² 的原因）。**未闭：level-set 条款**（LS 无 Γ DOF、TE 行**齐次** ⇒ 乘 F 是**空操作**；正解 = 与 B2 continuity weld 混合 `F·K̂+(1−F)·Ŵ=0`，属**不同模型**、需自己标定 ⇒ 已设计未实现）。★★ **跨声速 M0.84 = G9.1 的原始场景，已攻克，且 0.019 缺口是"分辨率"**：G9.1 的 fine 曾是极限环**伪解**（无限幅 M_max **7.93**、9 个单元越 M_cap=3、永久限幅，其 cl_KJ 0.2393 被判定"不是升力"）；加锥化后 fine 是**真离散解**（**M_max 2.818、0 个越 M_cap、0 限幅、收敛**，44.6 min；★**已重跑并提交 2026-07-13，commit 39e1ded / `run_g132_transonic.py` 5/5 / `g132_transonic.csv`，逐位复现**）。cl_KJ **0.2593→0.2652→0.2866** vs Tranair/KRATOS **0.288**（锥化本身扣 ~1.5% ⇒ 无锥化等价 ≈**0.291**）⇒ 落在 P9 预注册 ≥0.283 阈值的分辨率一侧——★**措辞已降级（2026-07-14 用户裁决）：分辨率主导是"强烈指示、未挣得"**——预注册带以 **Richardson 外推值**触发而非 fine 单点值，而两种几何都不存在合法外推（平帽序列非渐近；圆帽无 M0.84 fine 态）⇒ **P11 升力立论 = strongly indicated moot，非 refuted**，实践上 P11 只剩 **G1.6** 一条理由。★ **网格族缺陷（使过去一切 M6 三点 Richardson 皆无效，含 G9.1，与物理无关）**：`h_far = min(2.5, 120·h_wall)` 在 coarse **被钳位**（2.5 而非自相似的 3.6）⇒ coarse→medium 远场只加密 **1.39×**、壁面却 **2×**（medium→fine 才是干净的 2×）⇒ **coarse 根本不在加密射线上**；这也解释了 coarse→medium 的展向"晃动"（根部 −6.3% vs 中展 +5.4%、总量几乎抵消）——那是在比较两个不同的网格族，不是在测收敛。★ **已修（Track M M1b，2026-07-13）**：`clamp_h_far` 默认 True 保持 coarse/medium/fine **逐位不变**（所有 lock 不动），新增 **`coarse_ss`**（唯一被钳位影响的一级，h_far 2.5→3.6，46,067 tets）⇒ **`RICHARDSON_LADDER = (coarse_ss, medium, fine)` 每个尺度精确 2.000**，由 `test_m1_onera_m6.py` 的 3 条新测试锁定。★ **增长是真实的场分辨率——"Kutta 探针"假说已提出并被证伪**：判决性测试 = 探针的 `cl_KJ` vs **无探针的压力积分 `cl_p`**，**两者都在涨且增量都在增大**（cl_KJ 0.2001→0.2005→0.2121 = +0.19%/+5.76%；cl_p 0.1875→0.1939→0.2091 = +3.41%/+7.87%），且探针几何健康（`probe_d/h` 三套网格恒为 0.48–0.49 ⇒ 随 h 正确缩放）⇒ conforming Kutta 提取**被洗清**，且 **level-set 移植不是 G13.3 的路径**（只是对等性/稳健性条款；B7 的 Γ(z) 更光滑对三维精度**不承重**）。★ **附带：P5 时代的 V6 完全按其 re-spec 表现**——V6 间隙（cl_p 低于 cl_KJ）以 ~**O(h)** 收敛：**6.30% → 3.29% → 1.41%**（比 1.9、2.3），是会**随加密消失**的真 O(h) 地板，fine 上已 **1.41%**，逼近自 P5/G5.2 起就推迟的 <1% 目标。★★ **网格阶梯修完后增量仍在增大——因为还有第三个奇点，而且在壁面上。** 自相似阶梯上 cl_KJ 0.2015 → 0.2005 → 0.2121、cl_p 0.1888 → 0.1939 → 0.2091（h 减半而增量涨 ~3×）。盒子研究（demo `run_g133_ladder.py`，**5/5**）定位：**翼尖平顶盖锐边（壁面）0.662 → 0.824 → 1.015，p = +0.321 ⇒ 发散**；而**尾迹自由边**（G13.2 修好的）0.536 → 0.565 → 0.570，p = +0.045 ⇒ **有界**；**机翼 p99（对照）** 0.642 → 0.628 → 0.629，p = −0.014 ⇒ **已收敛**。**根因 = 已记录的刻意几何简化**：`wing3d.py` 建的是**平顶盖**，真实 ONERA M6 是**圆顶**；平顶与上下表面相交成锐凸棱，正是 P13 要消除的那类势流边缘奇点，**只不过长在机体上而非尾迹上**。**这不是 P11 问题**——等参**单元**修不了一条真正锐的几何**棱**。**⇒ 下一步 = 把顶盖倒圆（Track M `wing3d.py`），重生成阶梯，再重做 Richardson；不需要 P11、也不需要 level-set。****⇒ 阻挡 3D 网格收敛的是三个不同的缺陷：(1) 尾迹自由翼尖边（p=0.59）→ G13.2 已修；(2) h_far 网格阶梯钳位 → Track M M1b 已修；(3) 平顶盖壁面锐边（p=+0.32）→ 未修。****诚实：仍未做 Richardson**（序列非渐近——网格族现已自相似，但第三个奇点还在）——方向已确立，外推数字尚未挣得。✓ **上面 M0.84 那一段已补齐证据（2026-07-13）**：曾是纯散文（无脚本/CSV/npz），现**重跑逐位复现**并提交（`run_g132_transonic.py` 5/5，`g132_transonic.csv`：cl_KJ 0.2593/0.2652/0.2866，三级皆 0 越 cap/0 限幅/收敛，census 用 G9.1 原法故是严格 A/B）。⚠ 但这是**平顶盖**单点、序列非渐近（平顶盖仍发散），只是 REPORTED、非 Richardson；P9 判决带原指望**圆顶盖**的 M0.84 Richardson——**M5 已完成且亚声速 Richardson 已挣得（p=2.31），但圆帽 M0.84 fine 从未到达目标 Mach（ramp 死于 M=0.75，site=尖 tip TE，圆帽放大之）⇒ G13.3 跨声速 = 阴性开放，P9 判决带两种几何皆未挣得**。★ 重跑还暴露丢证据的坑：fine（~450k dof）必须 `precond=amg`+紧 forcing(η=1e-8)+`m_start=0.30/n_picard_seed=12`；medium 配方的 `precond=direct` 是 P9 的 4h39m/26GB splu 陷阱（见 [[m6-fine-needs-amg-not-direct]]）。 原 G13.1 记录：**CLOSED**（demo `p13_tip_edge_singularity` 10/10，亚声速 M0.5 无跨声速机械）：★奇点是 **1/√r 平板边缘型，非"1/r"**（conforming 三点 coarse/medium/fine 峰值 Mach 对数斜率 **p=0.59∈[0.4,0.65]**，峰值 0.712→0.981→1.510；1/r 线涡应 p=1）；★驱动量 = **脱落涡强 dΓ/dz（翼尖最大 ~10× 中展），非束缚 Γ**（Γ 翼尖→0 是必要非充分）；★**模型非表示**（conforming ×1.38 与 level-set ×2.28 同网格皆发散，LS 斜率 1.34≥conforming 0.52；同盒 p95/mean 平；峰值在 TE 之后弦平面 ⇒ 非壁元、P11 修不了）；★**conforming fine M0.5 不收敛**（限幅/落地 ~1.4k NaN）= G9.1 的亚声速对照。（当时设想的修复 = 尾迹模型改（卷起/显式翼尖涡，交 Track B 重定 B9）——**已被 G13.2 推翻**：机理是离散的，Kutta 目标锥化即可，不改尾迹模型、不经 Track B。）已纠正 roadmap:1036/demo_report:1333 "1/r"→"1/√r" |
| M0/M1 | ✓ | 准 2D 挤出网格族 + M6 后掠尾迹网格族（.msh gitignored，脚本再生） |
| M3 / M4 | ✓ | 无尾迹面("O 型")网格族（2026-07-11,Track B 双网格规则用）：M3 = NACA 准 2D（走廊扇形覆盖 α 扫掠;coarse 已提交）、M4 = ONERA M6（复用 M1 走廊尺寸场,单元数与 M1 差 6–9% ⇒ B7 受控 A/B;.msh 全部 gitignored） |
| Track B | ◐ | **B1–B5 ✓ + B7 ✓（B1 2026-07-11;B2/B3/B4/B5/B7 2026-07-12）;B6 跨声速进行中（2026-07-12）;下一步 = B8**。level-set 尾迹产生升力,隐式 Kutta,无 Γ secant。**B4：尾迹 LS 对常数跳跃恒零（单位分解,1.9e-16）⇒ "g₂ 即 Kutta" 错、退休;真 Kutta = 非线性 TE 压力相等 |q_u|²=|q_l|²,q 在壁面邻接控制体恢复**（全扇形 +11~15%、壁面邻接 <1%）。Γ 与 conforming 同网格 <1%,无尾迹 M3 差 0.3%。**B5：远场 A/B — 选项 a（Dirichlet+涡）保持默认（亚声速）**;选项 b（López Neumann 出口、无涡）截断 O(Γ/R),15c −4%。**B6 已测得三个机理结论（design_track_b.md §10）**：逐侧人工密度落地（亚临界严格无操作,激波区与 conforming 同构）;P4 全场阻尼**不可平移**（Jacobi 光滑子掐死作为解模态的 Γ）⇒ 阻尼局部化到 ν>0 行;★折叠区 option a 实时涡反馈增益 >1（Γ 单调穿过真解后爆掉,lagged 外层映射无不动点）⇒ **跨声速配方 = Neumann 出口**,coarse M0.80 收敛到距 Newton 真解 −6%（比 conforming Picard 自己的 stall 态近得多）;★A/B 反转——原始 Picard-vs-Picard 差（M0.75 +10.5%）经同网格 **Newton 仲裁**证明是 **conforming Picard 的欠环量偏置**（−4~−8%,P4 勘误在弱激波区的定量化）,**LS Picard 距 Newton 真解仅 +0.25%~+1.0%** 且随加密收敛（隐式 Kutta 无可早停的 Γ 外层）。**gate 基线已改（用户裁决 2026-07-12）为同网格 Newton 真解**;**coarse M0.80 gate 达成**（M0 Γ −7.9% / M3 +0.9%,激波 0.644/0.678,demo 14/14 PASS）;**medium M0.7875 = 折叠区**——Picard 只到有界 stall（Γ −18.8%）。**★ LS Newton（`solve/newton_ls.py`，§5.5/§10.6）已交付+FD 验证 1.3e-9**：精确 Jacobian = Picard 矩阵 + 逐侧 Term 2/3 + TE Kutta 精确二次导数,尾迹 LS 行线性,无 Γ DOF/Woodbury。**在折叠区拿到机器精度、二次收敛的真离散解（0 lim/flr）**——medium M0.7875 **M3 无尾迹（工作流网格）|R| 1.5e-12** Γ 0.2292、coarse M0.80 双网格皆收敛。**两个诚实缺口**：M0 嵌入 medium 活选择 Newton 极限环于 3e-6（P8/N5 近平局 churn 的 LS 形态,需接入冻结选择）;收敛的 LS 折叠解比 conforming Newton 低 ~13%（真离散差异,待厘清 neumann 截断/切层 O(h)/人工密度网格依赖）。设计 [design_track_b.md](design_track_b.md)。B6:coarse ✓、LS Newton 交付+折叠区工作流网格达真解、medium 定量收尾余两项。**★ B7（ONERA M6 三维 gate）2026-07-12 一次通过**（design_track_b.md §11;demo 35/35 PASS）:M∞0.84/α3.06 coarse、`farfield="neumann"`、ramp 0.60→0.84@dm0.04;**M1 嵌入** cl_KJ 0.2765／激波 0.635/0.588/0.449／Γ 0.1076→−0.0003／M_max 1.453（★2026-07-14 重读:side 读法伪值,诚实 main 读数 **1.392**,见 `mmax_reread.csv`）／**0 lim,flr**（22.7 min）,**M4 无尾迹** 0.2710／0.634/0.584/0.454／0.1055→+0.0003／1.368／**0 lim,flr**（18.4 min）;V6 1.77%/1.97%;双网格 A/B 2.0%。**★ B6 的升力反转在三维复现**:以 conforming **Newton 真解** 0.2692 为基准,LS Picard **+2.7%(M1)／+0.7%(M4)**,而 conforming Picard（P5, 0.24788）**−8.6%** ⇒ 按 P5 设 gate 等于因"更接近真解"而扣分,故升力锚定 Newton;**无尾迹工作流网格 M4 是两者中更准的**。**★ 三维远场 = neumann,P5 的 Γ(z) taper 结构性不需要**（B 路径的涡展向均匀、支割线恒为 y=0:(a) α 对准的尾迹面与之非共面 ⇒ 出口承载无切割支撑的跳跃,出口 M 0.958 vs neumann 0.513;(b) 即使共面,标量 Γ 也匹配不了 Γ(z)→0 且翼尖外无切割,出口 M 0.825 = P5 branch-ray 伪迹;neumann 不带涡故二者皆不可能）。**★ Γ(z) 本身展向光滑、无需任何平滑**（计划外结论）:归一化二阶差分 0.0079/0.0091 vs conforming P5 的 0.0970 = **光滑 11–12 倍**——conforming 每个 TE 站点解一个独立 secant（st133 那套机械;`INVESTIGATION_gamma_smoothing.md` 曾试图平滑其抖动而失败）,隐式 Kutta 没有逐站循环（Γ 是**一个解模态**）⇒ P5 的展向 Γ 问题在此**结构上不可能发生**,而非被修好。**★ 三维专属机械无需新求解器代码**（B1 的斜交标架+展向裁剪已生效,Γ(tip)→~3e-4 离散归零）,唯一缺口=后处理 `post/surface_ls.py::section_cp_curve_levelset`（D11 逐侧;**上表面与 main 场逐位相同** ⇒ gate 激波指标不受影响）+ `cl_pressure_3d_levelset`。成本远低于风险估计(~0.6 s/outer @ ~12k 三维 DOF ⇒ ~20 min/解)。**诚实缺口**:顶部 Mach 级停在 Picard 残差尾（|R| 4–6e-6,600-outer 上限;但每级有界物理 0 lim/flr、指标在带内 ⇒ gate 断言"有界"而非 `converged`）;**LS Newton 在 M6 推迟**（朴素 splu,需 lagged-LU);激波比 P5 靠后 0.02–0.04c（在带内）;仅 coarse。**B8 ✓ 2026-07-14 characterized-not-cured**（度量伪影勘误 + 两条约束侧修法皆负,详见 agent-rules / roadmap B8 re-spec 块）;**下一步 = B9（翼身 LS 求解,M∞0.5）** |
| M5 | ✓ | 圆顶翼尖盖（2026-07-13）:`tip_cap="round"` opt-in（默认 flat,全部旧网格族逐位不变）;缝棱角 p99 q=−0.92 = O(h) 衰减 vs 平帽 q=0 不动;成本 ×1.3 tets |
| M2 | ◐ | **翼身组合体网格 ✓ 2026-07-13**（机身=单样条旋转体,crease p99 20.6°→11.2° O(h) 衰减;wake-free 族;TE 折线两端为精确壁点）;求解腿 = B9。⚠ 提交信息里的 LS 摄入 census（1,415 切元/76 TE 节点）2026-07-14 前无 committed 工件,正在补测试+CSV |
| Track V | 未开 | VII 粘性耦合（Drela IBL3 六方程 + transpiration BC）,设计完整、**零实现** |

## 长期挂起项（勿反复重提）

- **G1.6 球面 Cp <2%**（strict xfail，11.6%）：已定根因 = 平坦面片壁上的自然边界条件
  （变分罪）；h 加密、恢复调参、Nitsche、边界数据修正**均已用证据排除**。
  唯一在案路线 = Option C 重定义 + P11 曲面壁元。
- **V6 <1%**（CL_p 对 CL_KJ 的 O(h) 离散地板）：**归因已明确、与 P11 无关** —— P13/G13.2
  实测它按 ~O(h) 干净收敛（6.30% → 3.29% → **1.41%**，比值 1.9/2.3），是会随加密消失的
  真 O(h) 地板，fine 上已逼近 <1% 目标（工件在 `cases/demo/p13_tip_edge_singularity/results/`）。
- ~~M0.84 跨声速无证据工件~~ **已修复 2026-07-13**（commit 39e1ded：`run_g132_transonic.py`
  5/5 + `g132_transonic.csv`，cl_KJ 0.2593/0.2652/0.2866 逐位复现）。但注意：这是**平顶盖**
  非渐近序列的 REPORTED 单点值，**不是** Richardson 外推（见 G13.3 跨声速阴性）。
- **G13.1/G9.1 原文中的 LS 指数（1.34 / ×2.28 / "LS ≥ conforming"）已被 B8 判为
  ×5 度量伪影**（诚实 +0.62），勘误注记 2026-07-14 落入 roadmap/demo_report；引用旧文时以勘误为准。
- 后掠 TE Kutta 探针跨站共享（P5 记录的鲁棒性隐患，未修）。

## 当前最大困难（按层级）

1. **模型极限（不是 bug）：FP 非唯一性折叠。** NACA medium 在 M≈0.79 附近进入
   全势方程的折叠区（dcl/dM ≈ 6–10）：同工况不同网格不可比、warm-start 有陷阱
   （G10.2b 负结果）、M0.80 无可达孤立解、无 ramp 直接求解 class C（G10.3）。
   对策已固化为纪律：折叠区只做单网格回归锁、Mach ramp 保留、松容差禁用。
2. **3D 网格收敛：三个不同的缺陷，两个已修，第三个（翼尖平顶盖）未修。**
   0.019 的 M6 升力缺口曾归因于尖 TE/LE 的 P1 壁面梯度；P9 推翻了它（2D 尖 TE 干净
   收敛 0.03%，而 3D fine 根本不是离散解）。此后逐个定位并修掉：
   **(1) 尾迹片自由翼尖边**（P13/G13.1 定量：驱动量是 dΓ/dz 非束缚 Γ，p≈0.59；
   ★G13.2 又推翻了"连续边缘/卷起"框架——**机理是离散的**：最外侧 TE 站把残留 Γ_last
   在一个单元内当集中涡脱落 ⇒ p≈1−q，判据 q≥1）⇒ **已修：展向载荷锥化**（`tip_taper`，
   `vanish_smooth`, r_c=0.05·b；**不是卷起、不经 Track B**）。
   **(2) M6 网格族 h_far 钳位**（`coarse` 不在加密射线上 ⇒ 过去一切 M6 三点 Richardson
   无效，含 G9.1）⇒ **已修：Track M M1b**（`clamp_h_far` + `coarse_ss` +
   `RICHARDSON_LADDER`，逐位不动老三级）。
   **(3) 翼尖平顶盖的锐壁边 ⇒ 已修：Track M M5 圆顶盖（2026-07-13）**——盒子研究
   （`run_g133_ladder.py` 5/5）定位其发散（p=+0.321），圆帽使 cap 面指数落到 +0.091
   （有界），**M∞0.5 的三点 Richardson 随之成立（p=2.31，cl→0.2050）**。
   **★ 第四项（跨声速专属，未修）：设计尖锐的 tip TE 在 M0.84 下的翼尖吸力。**
   圆帽让绕流包裹加速，每一级都抬高该 TE 的峰值 Mach（M_max 1.51/2.00 vs 平帽
   1.39/1.73），fine 级越限、Mach 续接死于 M=0.75 ⇒ **M0.84 无 fine 态、无 Richardson
   （两种几何皆然；平帽 fine 收敛但序列非渐近）**。结论：3D **亚声速**网格收敛断言
   现在成立；**跨声速**仍不成立（`run_g133_roundtip_transonic_locate.py` 已把 20/20
   最快单元钉在尖 tip TE 上，z/b 0.97–0.99、chord-frac ≈0.998）。
3. **Track B 的下一个未知数：跨声速（B6）。** 切单元有**两个速度状态**（上/下），
   人工密度必须逐侧求值，且上游 walk 的邻接图要**限制在同侧**（尾迹是滑移线，密度
   信息不穿过切向间断）——design_track_b.md §5.2/D10（DN1 曾错判为"upwind 不用改"）。

## 运行/成本纪律（易踩的坑）

- 16 线程封顶且**必须同时盖住 BLAS/OMP**（`NUMBA_NUM_THREADS=16 OMP_NUM_THREADS=16
  OPENBLAS_NUM_THREADS=16`；漏了 BLAS 会慢 ~33% 并挂掉 G8.2 断言）。
- 贵重工件不随手重算（P4 heavy demo ~40 min、G4.1 medium ~17 min、P5 medium
  45–75 min、M6 fine 网格数分钟）；已提交的 CSV/PNG 是权威。
- fine 网格与解算 npz 一律 gitignored，本地缓存、demo 缺则重算。
- 回归基线：**358 passed + 17 skipped + 2 xfailed**（2026-07-14 起，实测
  882.98 s @16 线程）。演进：B7 的 276+17+2（719.29 s）→ P13/G13.2 +15 →
  M1b +3（=294）→ M5 +19 / B8 +13+11 / M2 +13（=350，B8 关闭）→ B8-backlog
  执行 +7（M2 LS 摄入 census 锁）+1（`element_mach2` side opt-in 锁）。G8.3 的 302 s 仍是 CI 参考；含 B1–B7 的 Track B 测试，其中若干条
  在无尾迹网格未本地生成时跳过；重 gate 走 `PYFP3D_TRANSONIC_GATES=1`。
  内核/装配改动后先跑 `tests/test_v0_freestream.py`。
- P9 demo 等重活在跑时,新测试/求解一律降到 8 线程(NUMBA/OMP/OPENBLAS 同盖)。
