# 第六阶段开发历史（每轮一行）

规则同 phase two / three / four / five：每轮一行，叙事写在轮次文件里，判定只有
**PASS / FAIL / RECORDED / UNDEFINED**（收敛腿 < 2 报 UNDEFINED，不报方向）。
轮次文件格式见 [../dev_phase_two/_TEMPLATE.md](../../phases/p2/docs/dev_phase_two/_TEMPLATE.md)。

★★★ **本阶段的入口文件 = [20260824-0000-initiation.md](20260824-0000-initiation.md)**
—— 开工前先读它的 **§1（中心风险：写出一条不会失败的门，以及三条常设要求
G-TEETH / G-DOMAIN / G-CADENCE）**、**§5（六件不许静默改掉的事）**、
**§6（本阶段成功的定义 + kill criterion）**。

★ **本阶段新造 `GS6.x`**（已核未被占用）—— 理由在入口文件 §0：
一个梳理阶段如果不留下带编号的门，它的结论就只是一份读过的清单。

## ★★ 阶段进度概览

| | |
|---|---|
| **开工** | 2026-08-24（phase 5 收口、PR #30 合并之后） |
| **靶子** | 代码 / 文档 / 测试 / 算例的全面梳理 + **为测试算例建立合理的门** |
| **轮次** | 0（立项） |
| **交接基线** | 未门控 **595 / 12 / 2**；门控 **604 / 1 / 4 / 0 XPASS**；快层 **5/5** |

## 产品指标追踪

★ 本阶段**不以移动产品指标为目标**，列表随立项文书 §7 承接；任何指标发生移动都要在本表登记。

| 指标 | 承接状态 | 本阶段变化 |
|---|---|---|
| M1a | ✓ 达标 | — |
| M1b | ✓ PASS（−16.33 / −16.15 %） | — |
| M1c | ◐ PASS(seed 5) / FAIL-覆盖(seed 0) | — |
| M1(a) | ⊘ 已删除（2026-08-24 裁决） | — |
| M2 / M4 | ✗ 未动 | — |
| M3a | ✗ **无粘不可达**（归因已定） | — |
| M3b | ◐ medium 达标（49.9 s）· ★ **无锁** | — |
| M5 | ✗（一条子句已达成） | — |

---

## 轮次

| # | 日期 | 轮次文件 | 内容 | 一句话结论 | 判定 | 测试状态 |
|---|---|---|---|---|---|---|
| 21 | 2026-08-27 | [归类 note §2 勘误](20260824-0400-gate-taxonomy-analysis.md) | **计划第 3 项前半「2D 圆柱补求解腿」—— 查证后划销：它的前提是错的** | ★★★ **没有东西要建，而错在哪比这个结论重要。** 归类 note §2 写着「2D 圆柱 ✗（那两个文件 0 次求解）」，实测**两个文件都跑生产求解器**：`tests/C/test_C02_cylinder_wall_correction.py` 与 `tests/A/test_A14_meshgen_cylinder.py` 都经 `tests/mesh_utils.py::run_cylinder_case` 调 `pyfp3d.solve.picard.solve_laplace`，C02 **三级各求解两次**（uncorrected / oracle-corrected，5.22 s，8 passed + 1 skipped + 1 xfailed）。而且 **C02 的判据本来就已经是裁决③的 C 类形状**：阶 `0.85 < slope < 1.25`（实测 **1.02**）**加**逐级绝对界 0.12 / 0.06 / 0.03，外带一条带记录理由的 strict xfail（G1.3 的负面结论）与已提交证据目录 `cases/gates/C02_cylinder_wall_correction/G1.3/`。★★★ **错误的来源**：写 note 时问的是「**这个文件里有没有 `solve_*` 这个名字**」，而该问的是「**这道门有没有跑求解器**」—— 求解在 `tests/` 内的一个 helper 里，字面 grep 看不见。★★ **而我在 2026-08-27 照着这条 note 又复述了一次，并据此向使用者做了推荐**，直到真的去读 `run_cylinder_case` 才发现 ⇒ **一个没有被测量支撑的判断，写进活文档之后会被它自己的作者当成事实再用一次。**★ 这是本阶段同一族仪器缺陷的第 N 次（`tip_cap` 在 docstring 里而不在签名、F07 第一次跑红在自己的举例上、PLACEHOLDER 探测器把「检查 PLACEHOLDER 的文件」算成占位、basename 映射遇上 44 个同名 `run.py`）——**「文件里出现某个名字」不是「代码做了某件事」**。 | **第 3 项前半划销**（第 3 项后半 = 喷管复活，已由第 18 轮的 C05 完成）；归类 note §2 与 C 类候选表**两处就地勘误**，写明错误来源与传递性判据的正确做法 | 无代码改动；C02 实测 **8 passed + 1 skipped + 1 xfailed @5.22 s**；全套基线不动（**639 / 32 / 2**） |
| 20 | 2026-08-27 | （C08 门文件自带完整判定记录；载体 `tests/_ringleb_case.py`；证据 `cases/gates/C08_ringleb/`） | **计划第 5 步：C08 Ringleb —— 唯一 2-D 跨声速精确解，人工耗散第一次对着真值定价** | ★★★ **与 C05 的分工是它存在的全部理由**：喷管里**有激波** ⇒ 数值解**本该**与等熵精确解在激波处不同，那里只能判**激波位置**；Ringleb 的超声速口袋是**无激波、光滑**的 ⇒ **人工密度加的每一分耗散都是纯误差**。★★★ **实测（h=0.08，469/2730 超声速单元，精确 M_max 1.2464）**：生产 `upwind_c=1.5, m_crit=0.95` 在超声速区造成 **10.01 % 的速度 RMS**；C 降到 0.25 → **3.76 %**（收缩 **2.66×**）；而 **C = 0 直接不收敛**（60 步撞顶，\|R\| 2.5e-07）⇒ **那份耗散买的是稳健性，代价现在有价格**。`m_crit` 0.95→1.20 把误差降到 **2.93 %**（3.4×）且仍收敛，代价 Newton 步数 11→37 —— **RECORDED 而非建议改默认**（只测了一个无激波算例）。生产**低估**峰值 Mach（1.2182，−2.26 %），C=0 **高估**（1.3102，+5.1 %），方向相反 = 耗散抹峰的直接读数。★★★ **判据的形状必须说清楚，否则会被误读成缺陷**：裁决③要「阶 ∧ 绝对值」，而**解的误差不随 h 收敛**（RMS 6.4e-2 → 5.9e-2 → 7.3e-2）—— 那**不是缺陷，是模型误差**。⇒ **阶判在 `R(exact)`**（把精确解代进离散残差，实测 **2.32 / 2.28 阶**，且**不需要求解**因此便宜），绝对值与归因判在解上，而**证明那 10 % 是人工密度的不是论证、是剂量-响应**。★★★ **两个真实符号错误是 oracle 抓的，第二个的教训更值钱**：① 速度角写成 `+asin(q/k)`，**PDE oracle**（∇×u、∇·(ρu)）当场以 `O(1)` 判错，实测应为 `−asin(q/k)`；② **下支流向反了 —— 而 PDE oracle 对它是瞎的**：`∇×u = 0` 与 `∇·(ρu) = 0` 都是**齐次条件、对整体变号不变**，两个符号都"通过"。判别条件必须是**鼻子处的界面连续性**（现约定跳 1.9~2.1，取负后 2.8e-05）。⇒ **一个 oracle 通过，不等于它有能力分辨你正在选的那件事。** ★★ 七个 oracle 全部实测：两套密度约定一致 3.7e-16 · Mach 一致 · PDE 本身（两支机器精度）· 鼻子速度连续 2.8e-05 · φ 路径无关 8e-09 · 逆映射往返 9.1e-15 · **∇φ = u 6.9e-07**（把求积点向鼻子端聚束前是 4.07e-05 —— 那是 Dirichlet 数据的精度地板）。★ 载体复用 C05 的 `DuctSystem`（进出口 Dirichlet、壁面自然），**因此同样护住 `pyfp3d/kernels/` 而护不到 `solve/newton.py` 的全局化层**，这句写进了门 | **C08 占位 → 实门，8 条 + 1 导图腿**；★★ **牙验了两条，第一条直接重放本轮真实抓到的 bug**：把下支速度符号改回错的 → **5 failed**；把精确速度整体放大 5 % → **2 failed**；撤除后复原 8 绿。★★★ **本轮踩到 CLAUDE.md 记过的 aliasing 坑**：`assemble_residual` 返回**共享缓冲区的视图**，`R0` 被 Newton 覆写 ⇒ `R(exact)` 读到 1.2e-14（其实是最终残差），据此算出的「阶 2.1/2.8」**是假数**；加 `.copy()` 后真值是 2.32/2.28。门里在 `.copy()` 处写了这段。★ 另记一次自伤：重写 `phi_exact` 用 `s[:i]+new+s[j:]`，两个锚点之间**装着另外两个函数**被一并删除 —— **锚点没有界定住它看起来界定的范围**，而那是 scratch 文件、无 git 备份 | **639 passed + 32 skipped + 2 xfailed, 0 failed @689.35 s @8 线程 load 14.1**；★ 与 631/32/2 逐项对上：C08 占位（1 skip）→ 8 passed + 1 skipped ⇒ **631+8 = 639**，skipped 净变 0，xfailed 不动。★★ **裁决②「C 类全都建立」至此完成**：C 类占位 **0** |
| 19 | 2026-08-26 | （C07 门文件自带完整判定记录；载体 `tests/_kt_case.py`；证据 `cases/gates/C07_karman_trefftz/`） | **计划第 4 步：C07 Kármán–Trefftz —— 唯一能把 Kutta 条件对精确 Γ 检验的门** | ★★★ **与 C06 的分工是本轮的设计核心**：圆柱没有尖尾缘 ⇒ Kutta 选不出环量，C06 里 Γ 是**规定**的，检验的是「给进 Γ、量出 cl」；KT 有**有限尾缘角** ⇒ Kutta **必须自己选出** Γ（`n_kutta = 2`，门里有一条断言专门确认它真的跑过），而精确值有解析式 ⇒ 本门检验的是**选择本身**。medium 实测 **Γ 2.803 % / cl 3.045 %**，两条独立路线（Kutta 行 vs 表面压力积分）**相差 8 %**。★★★ **本轮最值钱的读数：积分量收敛、前缘点值不收敛，而且在同一个解上。** Γ 阶 **2.109 / 1.493**、cl 阶 **1.957 / 1.468**、Cp **MID** 带 **2.893 / 1.761**；而 **LE 带阶 0.085**、TE 带 0.53 / 0.65。★★ 两个慢带**都有已定位的成因，都是能力陈述而非缺陷**：**LE = 本项目 G1.6 那一族**（球上 LE 带阶 0.37/0.87、「medium 的 11.6 % ≈ P1 场在 h=0.08 的固有 max-norm 能力」），**首次在一个有精确解的二维翼型上复现**；**TE = 有限楔角本身**（精确解 \|q\| 以 (ζ−b)^(τ/π) 趋零，τ=10° 时指数只有 0.0556，θ=1e-5 处精确 \|q\| 还有 0.49 ⇒ **精确解自己就没有干净的驻点**）。★★ **归因前排除了两个我自己造的混淆项**：折线几何点数 ×20（100→2000）只让 LE 阶 0.241→0.287；oracle 采样加密 5×（匹配距 3.5e-05→6e-06）只让 LE RMS 0.1594→0.1574 ⇒ 都不是原因。★★★ **LE 带只判「显著慢于 MID」（实测 20.6×）而不判它自己的阶，理由是硬的**：xcoarse 上 x<0.05 内**一个壁面节点都没有**（`n_LE = 0`，RMS 为 nan）—— **自变量改变了样本是否存在**，四条判据缺陷里的第 2 条；门里有一条断言把这个事实钉住，免得将来有人顺手给 LE 补一个三级的阶。★★ **精确解是用四个与它无关的 oracle 验过的**：① 物理围线环流（相对差 4.1e-07）② 纯几何量的尾缘夹角（10.207° vs 参数 10°）③ 复速度在 ζ = b 处解析为零 ④ **最强的一条** —— θ→0 时 \|q\| 每十倍衰减比 **0.879**，等于映射指数给的 `0.1^(τ/π) = 0.8797`，且 **τ=0（Joukowski 尖点）时 \|q\| → 0.9056 常数非零**，两个情形干净分开 | **C07 占位 → 实门，10 条 + 1 导图腿**；★★ **牙验了两条**：把解析 Γ 改错 5 % → **5 failed**；改传 `gamma_fixed`（Kutta 不再自己选）→ 门的前提 **3 failed**。★★ **写门时自己踩了两次同族的坑，都记下**：① oracle ③ 第一版断言「\|dw/dζ\| 的**全局 argmin** 落在尾缘」，而翼型有**两个**驻点（前驻点 θ≈190°、后驻点 θ≈360°），离散 argmin 取哪个纯看半格偏移离谁近 ⇒ **把「集合里的某个元素」写成了「那个元素」**，改为在 ζ = b 上**解析求值**；② docstring 第一版引的是**我探针的数**而不是**门自己 CSV 的数**（探针用 `poly(max(80,int(2/h)))`、门用固定 `polyline(400)`）⇒ 按「引用你的管线实际跑出来的那个数」全表校准。★ 最紧的一条判据是 cl 3.045 % 对 3.5 %，**余量只有 15 %**，如实写在判据旁 —— 解是确定性 Laplace 解，无线程/随机来源 | **631 passed + 32 skipped + 2 xfailed, 0 failed @573.51 s @8 线程 load 14.8**；★ 与 621/32/2 逐项对上：C07 占位（1 skip）→ 10 passed + 1 skipped ⇒ **621+10 = 631**，skipped 净变 0，xfailed 不动 |
| 18 | 2026-08-26 | （C05 门文件自带完整判定记录；载体 `tests/_nozzle_case.py`；证据 `cases/gates/C05_nozzle_quasi1d/`） | **计划第 3 步后半：C05 准一维 Laval 喷管 —— 项目里唯一带激波的精确解** | ★★★ **这一轮的产出是两半，第二半比第一半值钱。** 第一半是常规 C 类：激波位置以 **0.927 / 0.834** 阶收敛，捕捉宽度**始终亚单元**（`err_cells` −0.651 / −0.685 / −0.768，且**恒在精确位置上游**）。★★★ 第二半：**从 x_s = 8 出发（边界数据仍是 x_s = 12 的真值），nx = 200 收敛到 `reason=tol`、\|R\| = 1.30e-15（机器零），而激波停在 x = 7.98 —— 错 40.17 个单元 / 喷管全长的 20 %**；同一条腿在 nx = 100 上更极端，收敛到 \|R\| ~ 1e-12 而**激波根本不存在**（整场亚声速）。而 `verify_uniqueness` 证明**连续解唯一**（Δφ 19.3186 > 17.6307，余量 1.688）⇒ 那是**离散算子的伪根，连续唯一性挡不住**。**于是「`converged` ∧ 残差机器零」在一个有真值的案例上被证伪** —— `bench/usability.py` 在翼型上只能拿「共识」当锚点，这里锚点是解析解。两条失败**形状不同**（错位置 / 没有激波），所以各自一条判据：只看位置误差的判据**看不见后者**，因为 nan 不参与比较。★★ **把一条禁令变成带自检的条件许可**：phase 5 的侦察留下「不得引用 h=0.05 的喷管腿 —— 全部 `reason=cap`」。那是针对**收敛性**主张的。本门实测迭代上限 20 → 40 → 80：残差只在 ~2e-8 的**地板**上晃（4.45e-08 → 1.89e-08），而 **x_shock 只动 1.3e-06 = 2.6e-05 个单元** ⇒ 对**位置**可用、对**残差**不可用，**并把这条不变性写成门里的一条断言**，而不是照抄或照违禁令。★★ **复活是 AST 精确抽取，不是手抄**：`tests/_nozzle_case.py` = `phases/p2/bench/s1_duct/nozzle.py` 全体 + `duct.py` 的 `DuctSystem`/`element_u`（抽取时验过这两块**不再引用 duct.py 的任何其它名字**）；Case A（等截面管）**没有带回来** —— phase 2 自己否掉了它（等截面管里每个激波位置都是精确解，且存在满足同样两个 Dirichlet 条件的**光滑解**）。★★★ **门护住什么必须说准**：`DuctSystem` 用的是**生产核**（`PicardOperator.assemble_residual` / `assemble_newton_jacobian`、`UpwindOperator.rho_tilde_sensitivities`、`EntropyOperator`）⇒ 护住 `pyfp3d/kernels/` 的人工密度/迎风/熵修正；**护不到** `solve/newton.py` 的全局化那一层（线搜索/EW/freeze/Kutta），因为喷管是进出口 Dirichlet。**把 C05 的绿读成「求解器能正确算激波」是错的读法**，这句写进了门的 docstring。★ 成本：实测发现**门的时间不在求解而在 `exact_solution`**（`n_quad = 40001` 的 Python 循环求根，每条腿调两次，一次 ~8 s）⇒ 加记忆化，**75 s → 32 s，数字一位不变**；缓存放在测试文件而非载体，好让载体保持与归档逐字一致的抽取结果 | **C05 占位 → 实门，7 条 + 1 导图腿**；★★ **牙验了两条，第一条还顺带是一次独立复算**：`UPWIND_C` 1.5 → 3.0 时门报出 **−1.038 个单元**，与 phase 5 归档扫掠 CSV 在 h=0.1/C=3.0 处的 **1.038** 四位有效数字吻合 ⇒ **复活忠实**；把反例初值改回 x_s = 12（反例消失）→ 红。★ 反例那条断言的是**缺陷仍在**，若将来变红是好消息但**必须走纪律 11 勘误**（usability/R23 的论证前提会变），这句写在断言消息里 | **621 passed + 32 skipped + 2 xfailed, 0 failed @450.16 s @8 线程 load 4.1**；★ 与 614/32/2 逐项对上：C05 占位（1 skip）→ 7 passed + 1 skipped ⇒ **614+7 = 621**，skipped 净变 0，xfailed 不动。★ 顺手修一处 **F07 抓出来的真问题**：C05 的 docstring 在 markdown 表格里写了 `\|`，而它不是 raw 字符串 ⇒ Python 非法转义告警；改 raw 后全仓（tests/ + pyfp3d/ + bench/）复查**再无同类** |
| 17 | 2026-08-26 | （C06 门文件自带完整判定记录；证据 `cases/gates/C06_lifting_cylinder/`） | **计划第 4 步的台阶：C06 带环量圆柱 —— 第一道对精确解检验 Kutta 的门** | ★★★ **它能做别处做不到的一件事**：圆柱**没有尖尾缘**，Kutta 选不出环量 ⇒ Γ 被**规定**（`gamma_fixed`，实测 `n_kutta_updates == 0`），于是**给进去的是 Γ，量出来的是表面压力积分的升力**，两条路中间**没有尾缘奇点**。medium 实测 cl_p **1.186373** vs 精确 `2Γ/(Uc)` = **1.2**（1.136 %）。★★ **零新增网格代码**：`naca0012_wake_2d` 只是 `airfoil_wake_2d(任意闭合折线)` 的薄包装，而它取 `pt[0]` 为尾缘、`argmin(x)` 为前缘、远场圆心**硬编码在半弦 (0.5,0)** ⇒ 把圆柱摆成半径 0.5、圆心 (0.5,0) 就全部对齐。★ 几何误差**实测**而非假设：样条后壁面节点到精确圆偏差 **1.74e-09（相对 3.5e-09）**，比要测的网格误差低**九个量级**。★ Γ 的符号约定也是**量出来的**：反号时 max\|ΔCp\| 从 0.10 跳到 **1.60**（15.9×），门里专门有一条把这个差距钉住。★★★ **「先把答案量出来再注册判据」当场救了这道门**：第一版阶梯沿用 NACA 的 `h_far = min(3.0, 150·h)`，三级**全被钳在 3.0** ⇒ 只加密壁面，实测 Cp 阶塌到 **0.325**、cl 误差**完全不动**（1.124 % → 1.136 %）—— 与 P11 在球上测到的**固定体网格污染地板**同一形状。改成 `h_far = 150·h` 全尺度加密后：Cp 阶 **1.417 / 1.686**、cl 阶 **1.614**。若照第一版写，这道门会给出一个「阶 0.3 但绝对误差看起来还行」的错误结论。★ 又加了第四级 h = 0.01（25652 节点、30 s）**只为定渐近率、不入门**（`fine` 按项目约定不在阶梯里）：Cp 阶 1.329 / cl 阶 1.600 ⇒ 确认 c→m 的 1.686/1.614 不是偶然。★★ **一处 RECORDED 没有藏**：xcoarse → coarse 的 cl 误差是**平的**（3.393 % → 3.477 %，阶 −0.035），那一级只有 598 节点、`h_far` 12 而域半径 15，仍在**前渐近区** ⇒ 收缩判据**只从 coarse → medium 判**（实测 3.06×），并且图里照实画出来。把一个已知处于前渐近区的点塞进收敛判据，正是本项目记过的「判据覆盖不了被比较量的定义域」。★ 顺手修一处规则与实现不一致：`gate_evidence_dir` fixture 会 `mkdir` 一个 `figures/` 子目录，而**八个门一个都没往里写**（PNG 全在门目录根下），git 又不跟踪空目录 ⇒ 改 fixture 跟实现走，并同步它的文档串 | **C06 占位 → 实门，9 条 + 1 导图腿**；**牙在绿基线上验了两条**：翻转 Γ 符号 → **4 failed**；把 `h_far` 钳回固定值 → **2 failed**（且门报出的塌陷阶 0.752 与独立测量一致）；撤除后复原 9 绿。图 + `summary.csv` 已提交，`INDEX.md` 加条目 | **614 passed + 32 skipped + 2 xfailed, 0 failed @664.75 s @8 线程 load 15.1**；★ 与 605/32/2 逐项对上：C06 占位（1 skip）→ 9 passed + 1 skipped ⇒ **605+9 = 614**，skipped 净变 0（占位 −1、导图 +1），xfailed 不动 |
| 16 | 2026-08-26 | （随第 15 行的收口；读数在本行） | **计划第 9 项：删掉那道永远不可能有意义的门 + 清掉代码内嵌的失效路径**（使用者裁决 2026-08-26） | ★★★ **`test_ab_bit_identity_gate_free_library` 的缺陷是「判据覆盖不了被比较量的性质」**，与那四次判据缺陷同族：它要求 HEAD 的 flag-OFF loose-loop φ 与钉住的 pre-V6 commit `13916b5` **逐位一致**，而 loose loop **本身不是 run-to-run 可复现的** —— 同 commit、同机器、同线程数跑两次，6104/6106 个节点上最大相对差 **1.024**，**正好就是它判为失败的量级** ⇒ 一切正常时它也红。自 2026-07-29 起是 plain `skip`。★★ **删之前先量了代价，而代价不在当初写「唯一确定该删」时的视野里**：`_git`/`_ab_leg`/`_overlay_working_tree_delta` **只被它一个用**，而**纪律 #12 拿它当机制引用**。⇒ 按使用者裁决**先改纪律 #12 的指向**（改为倚靠已提交的 `bench/studies/v6_1_wake_sheet/results/ab_cache_mode_isolation.csv`，并记下删除的代价与仍成立的测量），**再删** —— 否则删完那句话指向空气。共删 **152 行**（测试 44 + 三个辅助 68 + 死导入/死常量 40），文件 8 passed+1 skipped → **7 passed**。★★★ **一个看似可行、实则不行的选项也记下来**：无粘腿上的逐位 A/B 在**两个轴上都有测量支持**（纪律 #12 的「k=0 exact」+ 归档脚本实测「inviscid path IS deterministic, 10/10」），但 **flag-OFF 在 k=0 根本不碰 wake sheet** ⇒ 那道门在 wake sheet 完全坏掉时照样绿。**well-posed 而 vacuous 不是门。** 仍在守 V6 惰性的是活着的 `(a)(i)` 腿；丢掉的只有**跨 commit** 这一维。★★★ **第二项：代码内嵌失效路径 48 处**（第 15 行只扫了 `*.md`，**这一半当时漏了** —— 同一族的仪器覆盖不足）。其中两处不是「文档过期」那么轻：`CLAUDE.md` 硬规则 #2 指向的 `pytest tests/test_v0_freestream.py` 已不存在（**规则跑不动**，第 15 行已修），以及 **`pyfp3d/solve/newton.py` 引用 `bench/run_capability_matrix.py` —— 那是本期改名造成的，库文件指空**。★★ **basename 映射在这里彻底失效**：`run.py`/`PRE_REGISTRATION.md`/`VERDICT.md`/`checks.csv` 各有 19–44 份同名候选 ⇒ 改用**整段路径后缀**匹配（归档就是加了 `phases/pN/` 前缀），**28 个唯一命中、0 个歧义**。★★★ **修法本身踩了同族的第三次**：朴素子串替换把**已经正确**的那些也换了（`docs/roadmap.md` 是 `phases/p1/docs/roadmap.md` 的子串），造出 37 处 `phases/p1/phases/p1/...`；不能用 `git checkout` 回滚（会毁掉本轮未提交的删除），改为向前折叠重复前缀。★★★ **而这个坑还把我自己的报数灌了水，一并记下**：修完我在会话里报「重指 56 处 / 45 个文件」，**净效果实测只有 15 个 `.py` 文件、20 条失效路径** —— 差额全是「先被子串替换改坏、又被折叠还原」的**净零文件**。⇒ **一次改动的规模要按 `git diff` 的净结果报，不能按替换计数器报**，否则中间态的副作用会被当成工作量写进记录。★ 上一轮的 docs 扫描查过，**0 处双前缀 —— 是运气不是设计**（那批映射的目标不含源串）。★ 9 处不匹配全部核实：2 处是 `bitcheck.py` 自己写出的产物、4 处在 gitignored 的 `build/`、1 处是散文省略号、1 处 `run_entropy_knee.py` **归档里也没有（是被删的）** ⇒ 就地写明「已删除，此处引文即幸存记录」、1 处即上面的改名。★★ **库文件动了 12 个，验的是「剥掉文档串后 AST 逐位相同」** ⇒ 代码零改动 | **第 9 项执行完毕**；新增 **F07 第 6 条**门（活代码内嵌路径必须存在），**在绿基线上验牙**：注入一个失效引用 → 红，撤除 → 6 绿。★ 该门第一次跑**红在自己的 docstring 上** —— 举例用的失效路径被当成真引用，「提到 ≠ 使用」**今天第四次**，且又发生在为抓这类问题而写的门里面 ⇒ 加显式哨兵 `PATH-EXAMPLE`，让讲述缺陷的散文能举例而不被判为犯了它 | **605 passed + 32 skipped + 2 xfailed, 0 failed @400.94 s @8 线程 load 13.7**；★ 与上一读数 604/33/2 逐项对上：删掉那道被 skip 的门 **−1s**、F07 新增第 6 条 **+1p** ⇒ **604+1 = 605、33−1 = 32、xfailed 不动 = 2** |
| 15 | 2026-08-26 | （第 10–14 轮的五面收口；读数在本行） | **收口仪式：把重编号与门体系写进那四个「会静默腐烂」的面** | ★★★ **查出来的不是过期，是一条跑不动的硬规则**：`CLAUDE.md` 硬规则 #2「kernel 改动后先跑 `pytest tests/test_v0_freestream.py`」指向的路径**重编号后已不存在**（`AGENTS.md:31` 同）—— 过期文档只是误导，**跑不动的规则是坏的**。★★★ 全仓活文档共 **158 处**引用扁平旧测试路径，涉及 60 个文件名。**映射表不是我猜的，是从 git 的重命名检测建的**（`git log --diff-filter=R -M fbd8d08`，69 条），补上归档里的同名文件后：**128 处 git 可映射 + 11 个已归档进 `phases/*/tests/`**，共重指 **155 处 / 9 个文件**。★★ **判据不是「改完了」，是「替换后活文档里每个被提到的测试路径都必须真实存在」** —— 一条可机械复验的后置条件，正是上一次归档轮只能写「改了 121 处但未验证能跑」时缺的那样东西。剩 9 处全部合理：6 处是登记表在**门号分配之前**写的待定名 `A0x/B0x/C0x`（已落实为 A53/B05/C05–C08，六个目标路径均存在）、2 处是历史叙述里已删的 `test_conftest_artifacts.py`、1 处是散文里的通用示例 `tests/test_x.py`。★★ `PROJECT_STRUCTURE.md` 的 `tests/` 段是 **121 行 phase-one 时代清单**，里面还列着**已被删除**的 `artifacts_dir` fixture 与 `test_conftest_artifacts.py` ⇒ 换成 A–F 结构（**分层由目录承载**）+ 新增 `cases/gates/` 段；连带删掉整块过期的 `artifacts/` 树。★★ **规则与实现原本不一致**：`CLAUDE.md` 写「C/D 类图写到 TRACKED `results/`」，而实际实现写的是 `cases/gates/<门号>/`（`gate_evidence_dir`，按测试文件名 stem 取键）—— 已对齐，并补上一句被编辑截断的断句。★ 另修两处 A53 让其现形的过期陈述：`tip_cap` 的默认值文档写着「default flat」（实为 round，正是 A53 锁的那条），以及 2026-07-06 那次审计对 `artifacts_dir` 的记述（**那次修的是「teardown 不再删」，不是「成为证据」**）| **四个面全部更新**：`CLAUDE.md` 基线行 + 硬规则 #2 + 图证据规则、`AGENTS.md`、`PROJECT_STRUCTURE.md`（tests 树 + `cases/gates/` + 页脚基线 + 两处勘误）、本台账；**判据（被提到的路径均存在）通过** | 文档改动，不触求解；`tests/F` **5 passed**（F07 含活代码 `tests.*` import 的实际解析）；全套读数见第 14 行 |
| 14 | 2026-08-26 | [M3a 平滑勘误](20260826-0100-m3a-smoothing-erratum.md) | **纪律 11：截面平滑落地后，哪些已提交数字动了** | ★★★ **事前预测被证伪，而这正是它的用处**：我预注册过「MID 带只动 −0.90 % ⇒ pooled 移动很小，若移动大就停下来查」。实测 R1 报 **−6.12 % DRIFT** ⇒ 停。查下来**数对了一半、前提整个错**：MID 确实几乎不动（medium **−0.44 %**），但我写的「误差集中在 MID/TE」是错的 —— **LE 上表面独占 SS 的 69.6 %**，而那张分带表**当时就躺在已提交的 CSV 里**。★★★ **第一个读数还是假的**：逐列 diff 得到一片「+0.00 %」，差点据此宣布改动无效 —— 真相是 `run_m3_budget.py:348` 按 `taper` 分流写文件，我的运行写 `m3_budget_head.csv` 而我 diff 的是 `m3_budget.csv`（`git status` 干净、mtime 停在 08-10、**与备份 sha 相同**）⇒ **我拿一个文件和它自己 diff**。这是「我在比的两个数是同一个东西吗」的**第六个成员：跨输出文件**，判别法零成本 —— **一个「重生成」若没改变目标文件的 mtime，它就没有发生**。★★ **正确的 A/B 是一次求解、两次提取**（同一个 φ，平滑是唯一变量）：`pooled_rms_5` **−4.01 %**、`band_LE_upper` **−10.36 %**、`band_MID_upper` −0.44 %；`band_LE_upper` 复现已提交行到 5 位小数（0.242226 vs 0.242221）⇒ 同溯源。★ 重跑求解那条路会把 taper/线程/收敛路径一起卷进来 —— 本轮 `taper=False` 腿**直接不收敛**（\|R\| 9.2e-07），正好演示了那条路的产物只能作 RECORDED。★★ **−6.12 % 只分解出一半**：平滑 −4.01 %（实测），其余 −2.19 % **故意不归因**（参照 0.1288 是另一代码状态下的常数，而唯一 no-taper 腿没收敛，不能替它作证）。★★ **范围按列不按文件**：同一 CSV 里经 `section_cp_curve` 的 **22 列动**、**16 列不动**，而不动的 16 列**恰好是全部升力数字**（`cl_p`/`cl_kj` 直接由 φ 积分）—— 正是「载荷不平滑」裁决的形状。★★ **`xshock_*` 单独量过**（激波位置是 (a) 类判据）：5 个未遮蔽站最大只动 **+0.0028 x/c**，比激波位置带小一个量级 ⇒ **平滑改幅值不改位置**，C/D 类门可以依赖这条。★ 分带**份额** 69.9 % → 63.9 %：「LE 主导」**结构性结论稳健**，**绝对占比 70.35 % 不稳健，引用必须带平滑状态**。★ 另有一次**自查省下的误报**：`PROJECT_STRUCTURE.md`/`bench/README.md` 的「LE 69.6 % / MID 20.7 %」我第一次复算成 66.9/10.9 并差点当文档缺陷 —— **是我漏了点数权重**，按 SS = RMS²·n 加权**精确复现**，且出自**冻结的** `m3_budget.csv`（脚本按构造不覆盖它）⇒ 不动 | **勘误清单出齐；三个受影响 CSV 刻意不重生成**（重生成 = 静默给已关闭轮次再基线，且违反纪律 2），改为在 `run_m3_budget.py` 的 `smooth_passes=1` 处**就地标注**移动量 + 指向判定文件；归档的 phase-3 文档**不改**（改冻结档案本身是本项目记过的危险） | **604 passed + 33 skipped + 2 xfailed, 0 failed @573.78 s @8 线程**；★ 与 A53/B05/B06 之前的 586+35+2 **逐项对上**：A53 占位→实门 +4p/−1s、B05 占位→实门 +6p/−1s、B06 新增 +7p、F07 新增一条 +1p ⇒ **586+18 = 604、35−2 = 33、xfailed 不动** |
| 13 | 2026-08-25 | （随提交 `5353be5`/`b06d506`；读数在第 14 行） | **平滑按裁决落地 + `sectional_cl_from_cp` + B06 截面 cl 交叉核对门** | ★★ 使用者裁决的**边界按量而不按路径**：载荷积分不平滑（曲面积分天然抵消，平滑只引入额外误差），**截面 Cp 提取与截面 cl 都平滑**。落地面只有两处取截面的活代码（`run_m3_budget.py`、`capability_matrix.py`），`smooth_passes=1`。★★★ **使用者的方法论纠正改掉了 B06 的判据**：我原写「截面 cl 是沿围线积分出来的 ⇒ 积分把 ± 配对恢复了」，使用者指出**非结构三角形网格在展向不均匀，截面切上下表面不对称，抵消无保证**，而**两个截面 cl 对不上很可能是求解器内部错误** ⇒ **唯一合理的载体是排除展向不均匀的 2.5-D 翼型网格**。B06 遂**只在 2.5-D 上设判据**（medium 0.150 %，coarse 0.826 % ⇒ **5.5× 收缩**），3-D 上的 ~4 % 差**记为公开问题**而不设门。★★ **G-TEETH 抓到轴向项完全没被覆盖**：把 `ca` 项符号翻转，B06 **全绿** —— 因为 α=2 时轴向项只占 cl 的 0.16 %；补 α=6 腿（1.01 %）后翻转即 1 failed。★ `sectional_cl_from_cp` 在缺 `y_*` 且 α≠0 时**抛错而不是静默丢掉轴向项**；`section_cut.py` 的 `_wall_section_points` 返回元数 3→4 以带出 `ys`，两个调用点同文件内同步改 | **B06 7 条全绿，牙已验**；`sectional_cl_from_cp` 入库 | 见第 14 行 |
| 12 | 2026-08-25 | （随提交 `7d35786`；读数在第 14 行） | **B05 锯齿度量门 + 「平滑边界」裁决入账** | ★★★ **本轮的定义被推翻两次，都是「名字 ≠ 内容」**：① 我提「加一个锯齿度量」——**它早就存在**（`cp_oscillation_metric`，且比我的计划更细：只取**斜率反转点**的二阶差分，从而把锯齿与真实激波分开），我读的是 P6 那批测试的**名字**而不是实现；② 我改提「锁住生产平滑抑制锯齿」——**生产刻意不平滑**，照建会造出一道**与生产相反**的门。★★ **我自己的 RMS 也是错的，而且我先把它归成了「约定差异」**：我把上下两面都插值到**全部**实验点，等于拿上表面实验数据比我们的下表面，把真实的 **−4.10 %** 稀释成 0.46 %。**「解释不能代替定位」** —— 4× 不符当时就该定位而不是解释。★ 实测（M6 coarse）：锯齿中位 **0.1742 → 0.0469**，7 站 pooled RMS **−4.10 %**；**两遍反而 +1.08 %，LE 带 +8.3 %**（G6.3 的「抹掉前缘吸力峰」）⇒ **一遍不是两遍**，阈值是标定不是推导 | **B05 6 条全绿**；使用者裁决：**载荷不平滑、截面平滑**，并注册**触发条件式要求** —— 解决其他本质问题时须把平滑影响一并考虑，或至少在其后把平滑重新引入评估 | 见第 14 行 |
| 11 | 2026-08-24 | （随提交 `c393316`；读数在第 14 行） | **A53：库默认值门 —— 此前没有任何东西检查它们** | ★★ 这些默认值决定每一个报出去的数字，却只活在文档散文里。门**读 `inspect.signature`**（零求解），每条带**来源与代价**，红了就知道自己动了什么、要不要走勘误清单。★★★ **G-TEETH 两次抓到我自己**：① 第一版 `tip_cap` 探针**静默什么也没做** —— 参数带类型标注，我的正则匹配到的是 **docstring 里**那一处而非签名，于是门**在基线上就红**，**根本没法证明有牙**；②「提到 ≠ 使用」这一族错误**出现在一道为防止该错误而写的门里**。改读签名后在绿基线上重跑，牙已验 | **A53 4 条全绿，牙已验** | 见第 14 行 |
| 10 | 2026-08-24 | （随提交 `fbd8d08`；读数在本行） | **测试全量重编号进 `tests/A..F/` + `artifacts/` 真删 + 门证据落到 `cases/gates/`** | ★★★ **分层由目录承载**：`pytest tests/A tests/B`（不设门）· `tests/C tests/E`（快层）· `tests/D`（gated）· `tests/F`（可选，含默认开启子集）——**87 个门号、4 次拆分、12 个占位**（使用者裁决④「暂时没有数据的先占位」）。★★★ **收集全绿 ≠ 全套全绿**：`--collect-only` 报 621 条全绿，全套却 **3 failed + 28 errors** —— 目录深了一级，`__file__` 相对路径全部少一级，而**这类错误 import 期不报**。**收集不是这一类的牙。**★★★ 这一族在一轮内**咬了 13 次**，每次我只补当次那种写法（`parents[N]` / `.parent.parent` / 拼 `..` / `dirname(dirname())` / 逐段拼给 `spec_from_file_location`）⇒ 遂改为**不匹配任何写法**的穷举检查（F07）：把含 `__file__` 的模块级赋值 **exec 出来看落点**，并按**作用域**判名字可见性 —— 上一版问「有没有在任何地方 import」，被一个**函数内**的 import 骗过，而另一函数的使用点不在那个作用域。★★ `artifacts/` 的处置不是删目录而是**删那个 fixture**：它在 `.gitignore` 里 ⇒ 永不进 HEAD ⇒ 按纪律 3 **不是证据**，却被 11 处活文档当证据引用。改为 `gate_evidence_dir` → **`cases/gates/<gate_id>/`（已追踪）** + `PYFP3D_GATE_FIGURES` 开关；已产出 **7 个门目录 / 14 张图 / 26 文件 / 1.4 MB + `INDEX.md`** | **重编号完成，账在三个数上对上**；`cases/gates/` 成为门的图证据落点 | **586 passed + 35 skipped + 2 xfailed @403 s @8 线程**（A53/B05/B06 之前） |
| 9 | 2026-08-24 | [bench 普查 §7](20260824-0600-bench-audit.md) | **追问：`artifacts/` 还有用吗** | ★★★ 答案不是「有用/没用」：**它已经在 git 之外**（`.gitignore:45`，**跟踪文件 0**，本地 1.8 MB / 22 目录，跑测试再生）⇒ **没有东西可归档也没有东西可删**；★★★ **但它作为规格是自相矛盾的，而且矛盾已经写进了文档**：`CLAUDE.md:569-570` 与 `README.md:109-110` 把 `artifacts/<gate_id>/*.png` 定为视觉门的**证据形式**（「never GUI-only checks」），而 README 那句的**下一句**正是「A number that exists only in prose is not evidence」—— 可 `.gitignore` 保证它**永不进 HEAD** ⇒ 按纪律 3 **它们不是证据**。★★ **两条规则互相否证，而且已有 11 处活文档把它当证据引用**（`docs/design.md:673` → `artifacts/G1.3/`；`PROJECT_STRUCTURE.md:1295` → `artifacts/G2.{1..5}/` 连读数）——**那些路径在任何新 clone 里都不存在**；与上一轮查出的 `docs/temp/` 引用**同族，但更系统，因为它被写进了 workflow 规则**。★★★ 最能说明问题的是 `tests/test_conftest_artifacts.py` **自己的 docstring**：「artifacts land in the persistent **(gitignored)** `<repo>/artifacts/`」—— **那次修的是「teardown 不再删」，不是「成为证据」**。★★ 而它的**功能是真的**（强制无头输出，使视觉门不可能退化成 GUI 目视），**位置必须换** —— 因为裁决要求「C/D 画图**让我能够肉眼检查**」，**图若是 gitignored 的，只有跑过测试的人能看**。★ 可行模式已在跑：`cases/demo/*/results/` **55** + `bench/studies/*/results/` **238**，全库 **292 个 PNG 已被跟踪**。⇒ 建议：**C/D 的图写进跟踪目录**（约 19 组，体量很小）· **A/B/E/F 附带图保持 gitignored scratch**（不是证据，每跑必提交 PNG 是纯二进制 churn）· **`artifacts/` 降级为纯 scratch 并从 CLAUDE.md 规则 1 与 README 里删掉「它是证据」那句**（矛盾的源头）· 修那 11 处引用 · `test_conftest_artifacts` 保留但归 **F 的默认开启子集** | RECORDED（分析；**未删除、未移动任何文件**）| `pyfp3d/`、`tests/` 零改动 ⇒ 承 **595 / 12 / 2** |
| 8 | 2026-08-24 | [bench 角色普查](20260824-0600-bench-audit.md) + 导入可重定位 | **`bench/` 的代码与产物哪些还有用；命名全是阶段戳** | ★★★ **真实结构是「角色」，而没有一个角色是从名字看出来的**：共享库 4（`run_le14_common_root.py` ★★★ **被 12 个脚本 import**，里面是 CLAUDE.md 明文的 `classify_failure`——**一个载重共享模块，名字却是 phase-2 的轮次号「LE-14」**；`run_capability_matrix.py` 被 5）· 产品指标门 3（★★ `run_m3_budget.py` **既是门又被 17 个脚本当库用** —— 一个文件两个角色，与测试那边「一个文件两类门」同型）· runner 1（快层入口，★ **不被任何东西 import**，方向相反 ⇒ 单靠「被读就留」会**误删快层**）· 工具 2 · ★ **一次性研究脚本 89 / 99**。★★ **保留规则要三条**：① tests/ 的传递闭包（**4 个**）② 有 cadence 的 runner/工具 ③ 产品指标门。★★★ **而我第一版判据是松的，报了 53 个种子** —— 按「目录名出现在文本里」算，**把 docstring 里的提及算成使用**；这与 `phases/README.md` 自己记的「第一版闭包太松（把注释里的提及也算了）」**是同一个错误的又一次出现，而这次我是读着那份记录犯的**。收紧（去 docstring + 只认路径样字面量与真实 import）后种子 **53 → 2**、闭包 **4**；★ 并按 README 的要求**先用已知会断的链验证判据**（`test_meshgen_rae2822 → v5_2_rae2822 → v3_loose_coupling`，**实测抓到 ✓**）才使用。★★ **产物剔掉生产者自引用**：`gate_results` 71 个，含自引用 58「**这个数没有意义**」，剔后 **23**，其中真正被**文档**引用的只有 `capability/` 与 `g82_anchor_check.csv`；余下多为脚本间**共享缓存态**（`gs40*_states`、`le14_cache`）应随生产者一起归档。`studies/*/results/` 的 168 **未剔自引用，标注不得引用**。★ **处置建议**：留 **10 个脚本并全部改角色名**（`run_le14_common_root.py` → **`failure_modes.py`**；`run_capability_matrix.py` 去掉 `run_`；★★ `run_m3_budget.py` **拆成门 + 库两半**；新增 **`bench/recipes.py`** = 裁决 (a) 的 `NEWTON_M6_RECIPE` 新家）；归档 **89 个**按阶段前缀（task3/hex/gs40 → p3 · gs41 → p4 · r*/m1*/n* → p5 · v*/a*/c1/b26/p14 → p2）。★★★ **而使用者这个问题把顺序改对了**：原计划直接做测试重编号，实测应当**先 bench 后 tests** —— ① 先归档能让「要改的 `NEWTON_M6_RECIPE` importer」从 **13 降到留下的那几个**（有一部分 importer 本身要被归档）② `run_capability_locks.py` 的节点表两轮都要动，先 bench 后 tests **只改一次**。**顺序有真实的省力与降险效应。**★ 同轮完成**导入可重定位**（重编号的前置）：24 处行首 + ★ **26 处函数内**相对导入改绝对；第二批是我第一版正则**锚在行首**漏的 —— **同一个仪器太窄的第二次实例**；收集 609 条不变 | RECORDED（普查 + 顺序修正；**未移动任何文件、未改任何名字**）| ★★ **全套 595 passed + 12 skipped + 2 xfailed @720.50 s @8 线程 load 12.1**（导入转换后实测，与基线逐项相同）；`pyfp3d/` 零改动 |
| 7 | 2026-08-24 | [登记表第 1 步](20260824-0500-gate-registry-step1.md) | **A–F 新体系下逐条读完 556 条断言（AST，多行安全），产出重编号的输入** | ★★★ **E 类的真实规模是 5 条，不是 167** —— 我上一轮的上界**偏大 33 倍**。真 E：`p8_newton` 的 G8.1 coarse（cl 0.41006/x_shock 0.6196/M_max 1.3946）、G8.1 medium（0.523/0.674/1.404）、G8.2 M6（0.268691/1.99687/三站激波）· `b32`（`CL_P = 0.2738`，★ **藏在模块常量里，逐行扫不到**）· `s1_m1a_envelope` 的 `LOCK[level]["cl"]`。★★★ **裁决 ① 的直接后果事前说清**：Euler/RANS 到位后 **4 of 5 升格 D，E 会缩到约 1 条**（翼身 M0.84，无参照）⇒ **E 是一个「等参照到位就搬空」的中转站**，而搬不走的那部分正是它存在的理由。★★ 而**我上一轮给 E 类的理由比我说的弱**：「b9 红了 17 天」那条断言（`abs(cl_f) < 0.15·abs(cl_w)`）**已被删除**（`695baa0` B9 re-spec），且它当年红是因为**一条退役判据被重新阈值化塞回去**，不是抓到能力回归；**E 真正 earn 自己的实例是 2026-08-09**（圆尖切换移动 committed 数字 ⇒ 7 条门控红）—— 它抓的是「**一次改动移动了已提交的数**」，可能是回归也可能是有意再基线。★★ **三处归类被 AST 推翻**：`test_seed_fallback`（70.2 s，6 条）**全是机制断言、零物理值 ⇒ 纯 A**；`test_b9`（9 条）**全是网格/拓扑/符号/有限性 ⇒ 纯 A**；`test_p8_newton` **11 条里 8 A / 3 E**。★★★ 而其中一处是**我自己的错**：我说「我知道 b9 断言 `abs(cl_f) < 0.15·abs(cl_w)`」并据此**否掉了正则的结论** —— 那条断言 15 天前就删了，**正则当时是对的，我用过期记忆丢掉了一个有效测量** ⇒ 口径：**否证一个仪器之前，先确认反例本身还存在**。★★ **全套分布（556 条全覆盖）**：结构/拓扑/API 无容差 **258** · FD/阶次/位相同/紧容差 **239** · 含解析闭式 **35** · 真加载参照 **12→7**（剔 5 条误报）· 比到已提交值 **10→5** ⇒ **A ≈ 497/556 = 89 %**，与使用者判断一致。★★★ **四个在 A–F 里没有干净归属的边界，登记待裁决**：① 整机的网格收敛性质但**无解析解**（M1a 三级收敛）② **模块级**解析解算 A 还是 C（V2 圆柱吹气、`CD_ODE_BLASIUS` vs Blasius、V1 自相似）③ **跨路径一致性**算 A 还是 E（Newton vs Picard、3D 退化 2D）④ **基础设施**算 A 还是 F（产物落盘、gmsh 可用性、溯源指纹 21 条）—— ★ 而 F 是**可选开启**，溯源指纹恰恰是**不跑就烂掉**的那类。★★ **第 2 步方案已备**：★ **用目录承载类别**（`tests/A/`…`tests/F/`）⇒ 裁决 ⑤ 的分层变成**一次路径选择**（`pytest tests/A tests/B` = 未门控 · `tests/C tests/E` = 快层 · `tests/D` = 门控全集），**不需要 marker 机制、也不会出现「marker 忘了打」这类失效**，并把「一个文件 = 一类门」**在文件系统层面强制**；占位 8 个，其中 **4 个可立即开工**（喷管、带环量圆柱、库默认值、锯齿度量），4 个等 Euler/RANS。★ 重编号要护住 **74 处**门号引用 + 快层节点表 + conftest + 两份导航文档，顺序同搬迁（**改写前仪器必须报出新断裂，报 0 = 仪器坏了**），且**改名前后套件条数必须逐项相同** | RECORDED（测量 + 登记；**未建立任何门，未改任何文件名**）| `pyfp3d/`、`tests/` 零改动 ⇒ 承 **595 / 12 / 2** |
| 6 | 2026-08-24 | [归类 note §4.7](20260824-0400-gate-taxonomy-analysis.md) | **裁决三：三层参照，按模型层级对齐** —— 「对无粘用 Euler 设门，对有粘耦合用 RANS 设门，对有粘耦合用实验记录偏置」 | ★★ **这个设计比我提的更完整，对的地方是「模型层级对齐」**：我们的两个能力模式**各自拿到一个同层级的参照**，而不是都去比同一个东西 —— **全速势(无粘) ↔ Euler**（设门）· **全速势+IBL ↔ RANS**（设门）· **全速势+IBL ↔ 实验**（记录偏置）。★★★ **而有一处不对称必须事前量清，否则第二道门会继承一个没有边界的不确定度**：两个模型差**不是同一种东西** —— **FP↔Euler** 差在**一个已知机制**（激波处的熵/涡量），位置与方向都清楚，而**熵修正正是为它存在**；**FP+IBL↔RANS** 差在**好几个机制**，其中最要紧的是**湍流模型是一个自由选择，不同模型之间的分歧可能大于我们想门住的那个量**。★★ **本项目已吃过这个教训且记录在案**：GS4.1 第 15 轮把 E-CF **从门降为 RECORDED**，理由逐字是「那两条相关式**自身的适用范围在本仓库没有可引用出处** ⇒ 平板的红**不能归因**于闭合」—— **一个参照的不确定度没有边界，它就不能用来归因**。⇒ **RANS 请求加一条：同一工况至少给两个湍流模型（如 SA 与 SST），两者分歧 = 这道门的分辨底噪**，低于它的判据只能报 UNDEFINED；★ 与已在用的 **A4 输入带 2.5 %** 同一逻辑：**先定底噪，再定容差**；NASA 的 CFL3D 验证算例本就常同时给 SA/SST。★★★ **三层合起来的真正收益是误差可以分解**（本项目一直做不到）：`FP−Euler` = 我们的无粘离散 + FP 模型误差 · `(FP+IBL)−RANS` = **我们的粘性耦合误差** + 积分 BL 模型误差 · `RANS−实验` = RANS 自身模型误差 + 风洞效应（**不归我们**）· `(FP+IBL)−实验` = 以上之和 —— **而这正是现在唯一能看到的量**。⇒ **具体价值**：M3a 的「无粘模型地板 0.0516–0.0707」当初靠**四点全尺度加密序列反推**，正因为**没有处在正确模型层级的参照**；有了 Euler 与 RANS，那个数**直接可归因**。同理 GV5.2/GV5.3 的 honest FAIL（松位移反馈太弱）才能分清**是我们的 IBL 还是 FP 的无粘内核**。★★ **RANS 的额外要求**（通用六项之外）：① 湍流模型写明且**至少两个** ② **转捩处理必须与我们对齐或说明** —— Track V 是**显式指定 x_tr**（RAE2822 0.03、M6 0.05，`naca0012_viscous_xfoil` 有 xtr005/xtr030 两套），RANS 若**全湍流**那是**另一个物理问题**，可用但必须写明且偏置单列 ③ **边界层必须解析**（给 y+ 与首层间距）—— 没解析 BL 的 RANS **不是参照** ④ **Re 对齐实验**（NACA0012 3e6/6e6/6.5e6 · RAE2822 6.5e6 · M6 **11.72e6**，均从已提交文件读出）⑤ **cd 分解**（压差/摩擦）—— ★ **阻力目前完全没有任何参照**，而 FP+IBL 能给出它，**这是一条新的能力轴**。★ **RANS 工况已开 7 条**（R-1…R-7，与实验逐字同工况）；其中 **R-4 NACA0012 M0.352/α12.86 近失速**标注为**最能量出分辨底噪、但最不适合当紧门**。★ α 口径同裁决二：**一律实验 α 不修正**，RANS 与 Euler 都如此 | RECORDED（裁决入账；**仍未建立任何门**）| `pyfp3d/`、`tests/` 零改动 ⇒ 承 **595 / 12 / 2** |
| 5 | 2026-08-24 | [归类 note §4.6](20260824-0400-gate-taxonomy-analysis.md) | **使用者两条裁决入账：判据形状 + 对实验一律用实验攻角** | **裁决一（采纳）**：**(a) 激波位置 · (b) 激波前 Cp · (c) cl 设门；(d) 激波后 Cp 只 RECORDED 不设门** —— 理由是等熵+无旋在**激波处**产生**真实模型差**（熵修正就是为此存在），写成「七站 Cp 逐点之差」会**把已知的模型差当缺陷**。**裁决二（采纳）**：「使用实验攻角还是修正攻角很难说，**目前先使用实验攻角**」。★ 这条有一个实在优点：**修正攻角需要一个要自己辩护的修正量**，而那个量没有可引用的唯一出处；用实验 α **不引入任何自造常数**。★ **操作后果已写进数据请求**：**CFL3D 的算例也必须用实验 α**，README 明写「α = 实验值，未修正」—— 否则参照本身带一个口径差（跨口径那族错误已发生五次）。★★★ **而裁决二把一个偏置引进了「对实验」那一侧，方向是已知的，所以事前写下来** —— 否则第一次读到它的人会把它当缺陷，**那正是裁决一要防的同一件事，只是换了个位置**。机制：实验**有粘**，粘性位移使有效弯度下降并把激波**推向上游** ⇒ 无粘解在**同一 α** 上**升力偏高、激波偏下游**。★★ **本项目自己的数已能给它定量**（全部已提交测量）：粘性耦合打开后 cl **−2.20 %**（`cl_KJ`）/ −2.40 %（`cl_p`），M6 medium（GV5.3）⇒ **无粘同 α 偏高 ~2 %**；**松**粘性耦合的激波仍落在实验**下游 0.06–0.10 c** 且随 M/α 变差（GV5.2 RAE2822）⇒ **无粘的下游偏置 ≥ 0.06 c**（方向确定，**量级下界，未直接测**）；七站 Cp RMS 无粘地板 **0.0516–0.0707**（M3a）。★★★ **两条裁决合起来的直接推论：两侧的门必须分开写** —— 对 **CFL3D Euler** 双方都无粘、同一 α ⇒ **α 无歧义，可设紧门**；对**实验**带上述已知偏置 ⇒ **以 RECORDED 为主**，若设门则带宽必须**容纳**该偏置且**门是单侧的**。★★ 而按本季判据缺陷第 1 条，**单侧门必须写出「相反的结果会落在哪里」**：若无粘 cl **低于**实验、或激波**偏上游**，那**不是「更好」而是与已知机制相反** ⇒ 判**异常并停下来查**，不得当成通过。★ 留给设计的选项（不做决定）：若将来**同时**用实验与 Euler，最干净的形状是**「对 Euler 设门，对实验记录偏置」**—— α 的歧义就**完全不进门，只进记录** | RECORDED（裁决入账；**仍未建立任何门**）| `pyfp3d/`、`tests/` 零改动 ⇒ 承 **595 / 12 / 2** |
| 4 | 2026-08-24 | [归类 note](20260824-0400-gate-taxonomy-analysis.md) | **A–E 五类门的归类实测 + 待补参考数据清单**（使用者给出五类原则，要求分析删减与补充）| ★★★ **先记一处更正，因为它是本项目那族错误的第六次，由我在这份分析里犯**：我量的是「快层五个文件有没有代码真的加载 `reference_data`」（答案 0），随后**让它代表了另一件事** ——「这五个文件不属于 A 类」。使用者反问「**快层不算 A 类吗**」，对的：「没有外部参照」**≠**「不是功能模块验证」，而**一条不需要真值的机制/收敛/健壮性断言正是 A 类**。逐个核实：`test_seed_fallback`（控制流合约）与 `test_s1_m1a_envelope`（无需真值的网格收敛性质）**是 A**；`test_p8_newton`/`test_b9`/`test_b32` 各含 **A 半 + X 半**。⇒ ★★★ 更正后的结论**比原来那句强**：**门的类别是「断言」的属性，不是「文件」的属性** ⇒ **任何按文件归类的做法必然出错**；设计原则随之而来：**一个文件 = 一类门** —— 拆开后 A 半进快层（便宜、无需真值），X 半等 D 类原料升格；现在绑在一起，**便宜的机制检查被 508 s 的算例拖着跑**。★★ **归类实测**（去 docstring 后机械统计，72 文件 / 556 条全部归类无剩余）：**A 42/340（强项）· B 5/28 · C 3/10 · D 3/13 · E ~8/49 · X ≤21/≤167（上界）**。★ 判据必须先去 docstring —— 实测 7 个「读 reference_data」的测试里 **6 个只是 docstring 提了一句**。★★★ **三个结构性空洞**：① **C 类跨声速一条都没有**（Laplace 圆柱/球都是不可压；2D 圆柱那两个文件 **0 次求解**；喷管机器**已归档无活门**）⇒ **「人工耗散的影响」目前没有任何有真值的算例可量**；② **D 类整类只有 3 文件 13 条断言**，而 `reference_data` 的 **7 个数据集里 4 个没有任何测试比它**（`onera_m6_experiment` 的 7 站 Cp、`rae2822`、`naca0012_experiment` —— 后者**从未被任何代码读过**）；③ **CFL3D Euler 完全没有**，现有参照全是**实验**或**别的低阶方法**（面元、XFOIL）。★★ **C 类「还有别的吗」的答案**：**Ringleb 流**（唯一真正 2D 跨声速精确解，含超声速区）· **Kármán–Trefftz**（唯一能把 Kutta 对**精确 Γ** 检验，现在只能对面元法）· 带环量圆柱 · **准一维喷管**（唯一带激波的精确解）· 椭圆/椭球（把壁面几何误差与 TE 奇点**分开**）· Prandtl–Glauert（渐近，非真值）；★★★ 而 **3D 可压不存在闭式解** ⇒ C 类的「3D + 跨声速」**数学上就是空的**，**这正是 D 类不可替代的理由**。★★ 两条事前告警：**Ringleb 要曲面壁** ⇒ 先判内场，否则量到的是 G1.6 同一件事；**喷管门立在「位置」上不是「残差」上**（phase 5 实测：|R|~1e-8 地板，而位置对迭代数不变 ≤8.7e-05 单元）。★★★ **为什么 CFL3D 能把「不可判」变「可判」**：M3a 已判**无粘不可达**（模型地板 0.0516–0.0707 vs 容差 0.05）—— 拿全速势比 Re 11.72e6 **有粘**实验，**模型形式误差本身大于容差**；对 Euler 比只剩「全速势 vs Euler」，**可收紧到有意义的容差**。★★ 隐性收益：快层那 658 s 的 X 半**加参照后直接升格 D，不用写新算例**。★★ **待补清单已开**（note §4）：2D NACA0012 四组（含 **M0.80/α1.25 = M1 靶子本身**、以及**对上已提交实验的两工况**⇒ 能把模型误差与离散误差分开）· 2D RAE2822 两组（★ **必须注明原始 α 还是修正 α**）· 3D M6 两组（**M0.8395/α3.06 逐字同工况**、M0.5 补亚声速缺口）；每份必须附**七站 y/b = 0.20/0.44/0.65/0.80/0.90/0.96/0.99**（从实验文件直接读出）、积分量与参考尺度、**≥2 级网格的收敛证据**（单网格一个数**没有误差棒**）、收敛口径、来流定义与湍流模型明确关闭、列名写在文件里。★★ 并事前定判据形状：**(a) 激波位置 (b) 激波前 Cp (c) cl 设门，(d) 激波后 Cp 只 RECORDED** —— 等熵+无旋在激波处的模型差是**已知且预期存在**的，逐点比会把它当缺陷。★ **两处我做失败的地方如实记**：① 想再分「断言比物理输出对硬编码数」与「求解作载具」，**多行断言让单行正则失效**（实测 `test_b9` 两项都是 0，而它明明断言 `abs(cl_f) < 0.15·abs(cl_w)`）⇒ **那个数未采用**；② **按名字归类会把 A 类误判成该删的** —— `test_v5_above_band_seed`/`near_band_seed`/`te_outflow`/`tight_scaled` 我按名字归成「研究结论锁」，**读完发现全错**（no-mesh/no-solve 的纯映射函数测试）⇒ **删减名单一个都不点名**。★ 唯一确定该删的：`test_ab_bit_identity_gate_free_library`（2026-07-29 因**前提为假**已 skip）。★ 顺序唯一硬依赖：**X 分类必须排在 CFL3D 之后**，否则会把「加参照就升格 D」的重锁一起删掉 | RECORDED（分析 note，无判据；**未建立任何门**、**未动 `xfail_strict`**）| `pyfp3d/`、`tests/` 零改动 ⇒ 承 **595 / 12 / 2** |
| 3 | 2026-08-24 | （搬迁善后，无判定文件；读数在本行） | **GS6.1-c：清掉 2026-08-10 那次整理遗留的死引用 —— 纯善后，不含任何门的设计** | ★★★ **范围先说清**：使用者 2026-08-24 指示 —— **门与测试案例最终如何设置还没有指示，而且要「大改」**⇒ 本轮**只做搬迁善后**，**一条门、一条锁都没建**，`xfail_strict` 也**没动**（那是测试策略，属于要大改的那部分）。★★ 立项文书 §4 的轮次序列已就地标为**「提案，未获采纳」**，并写下理由：现在按它写一批锁把当前默认值/分层/判据形状钉住，大改时那批锁会变成**残骸**，更糟的是会**把旧结构锚死** —— 与「临时旋钮变永久」同一族。承接台账里两处「从它开始立门」的话也一并撤掉。★★ **修掉的**：**216 条**死文件字面量 + **31 处**死目录引用（125 个文件），全部按**唯一 glob 命中**重指到 `phases/p*/`；另手改 2 处真陈旧（`CLAUDE.md` 的 2026-07-17 审计路径、`PROJECT_STRUCTURE.md` 的目录树条目，顺带把「25 rounds」订正为 **27**）。★★★ **故意不修的两类，因为它们不是坏引用**：① `docs/discussion_notes/`（**21 处**）——**2026-07-14 按裁决删除**，CLAUDE.md 已载恢复命令 `git show 8aa4aee:...`；② `docs/temp/`（**7 处**）—— 实测**从未进过 git**（2026-07-17 的一致性审查就记了「empty directory, no git history, gitignored」）⇒ 那些引用指向**未提交的 scratch**，按纪律 3「没有提交产物的说法不算证据」，正确处置是**标注而非重指**；③ `docs/PROJECT_STRUCTURE.md` / `docs/STATUS.md` —— 是**记录该路径已死的散文本身**（`overview.md` 明写「已删除：`docs/STATUS.md`」）。★★★ **本轮最有价值的是一条关于仪器的结论**：**死字面量仪器不可门**，因为它**分不清「一条坏引用」与「一句正确地指出某路径已不存在的散文」** —— 剩下的 39 处 bare-dir 命中里，绝大多数是搬迁表的「原位置」列，**写法是对的**。而**断链仪器可门**：定义干净（跳过代码跨度、排除 `[φ](TE) = Γ`），全库实测 **3 条，全部是 TE 例外**。这条区分是留给那次大改的**输入**。★★ 三层 AST 判据（比上一轮细化）：改动 32 个 `.py` ⇒ **31 个 docstring-only（AST 逐位相同）**、**1 个仅字符串常量不同**（`mark.skip` 的 `reason=`）、**0 个代码/结构真变** | **断链 3 条（全为 TE 例外） PASS · 代码零改动 PASS（三层 AST） · 未建任何门 —— 按裁决保留给设计指示** | ★★ **全套 595 passed + 12 skipped + 2 xfailed, 0 failed @635.19 s @8 线程 load 1.33**（与基线逐项相同）。★ 同一套本日三次读数 **712.79 s（load ~12）/ 635.19 s（load 1.33）**，答案逐位相同 ⇒ 又一次「墙钟是机器的标定，不是结果」 |
| 2 | 2026-08-24 | [承接台账](20260824-0300-carried-forward.md) + [第一波勘误](20260824-0200-gs61a-archive-verdict.md) | **GS6.1-b：使用者裁决 —— phase 2–5 文档全部归档、`docs/inspection/` 删除** | ★★★ 裁决理由:「**我们现在本来就是要进行全面梳理,以前的分析和文档都只是参考**」⇒ **覆盖了第一波那份检查单挡下的 4 个文件**。★★ 而挡它们的两条理由**是可测的事实**(EW forcing 1e-6 仍是库默认;它是唯一的能力声明)⇒ **归档的同时必须有新落点,否则不是归档而是丢失** ⇒ 建 **承接台账**:§1 **实测**的库默认值(`inspect.signature` 读出,**不从被归档的文档抄**):`ew_eta0`=`ew_eta_max`=**1e-06**、`upwind_c` 1.5、`m_crit` 0.95、`m_cap` 3.0、`rho_floor` 0.05、`precond` amg、`entropy_correction` **True**、`kutta_estimator` **probe**、`tip_taper`/`gamma_target` None、`tip_cap` **round**;§2 裁决 D1–D5 —— ★ **只核了 D5(已执行)与 D2(已激活),D1/D3/D4 如实记为未逐条核对**,不写「全部承接」那句无据的话;§3 **能力边界仍只有归档那一份 ⇒ 产出 phase 6 自己的是本阶段交付**;§4 不许重开的封闭负结果。★ `docs/` 从 12 → **7 个文件**(4 份活文档 + `dev_phase_six/` 3)。★ **第一波判定正文一字不改,加勘误块** —— 那次勾选的读数是真的,**裁决可以覆盖一份检查单,两件事不冲突**。★★ 仪器再走一遍同样的顺序:搬迁后**先证明有牙**(报出 **35** 条新死字面量)→ 改写(30+5 字面量、119+18 相对链接)→ 复验 **断裂 markdown 链接 3 条,全部是 `[φ](TE) = Γ` 数学记号**(唯一被明确允许的例外)。★★★ **两个仪器缺陷本轮自查出来**:① 链接正则**不跳过代码跨度**,把我自己写在反引号里的示例当成链接,还**把两处示例"修"坏了**(那些示例正是要逐字展示 CLAUDE.md 里的历史名)⇒ 加 code-span 排除并复原;② **AST 判据只覆盖 docstring** —— 第二波改到 3 个文件的**非-docstring 字符串**(一条 assert 消息、`mark.skip` 的 `reason=`、一条 verdict 文本)⇒ 它们 **AST 不同**,所以「docstring-only,AST 证明」这句话**只对第一波的 8 个 `pyfp3d/` 文件成立**,对这 3 个**不成立**;实测它们**没有一处被当路径使用**(0 次 open/Path)且 8 passed / 1 skipped ⇒ 行为不变,**但话要说准**。★ `pyfp3d/` 复验仍为 **8 个文件、AST 逐位相同、代码零改动** | **链接 3 条(全为 TE 例外) PASS · G-TEETH PASS(0→35) · pyfp3d AST 零代码改动 PASS · 三文件字符串仅消息文本 RECORDED** | ★★ **全套 595 passed + 12 skipped + 2 xfailed, 0 failed @712.79 s @8 线程**(第一波后实测,与基线逐项相同);第二波仅改消息字符串,复跑那 2 个文件 **8 passed / 1 skipped** |
| 1 | 2026-08-24 | [判定](20260824-0200-gs61a-archive-verdict.md) | **GS6.1-a：phase 2–5 文档 + `docs/inspection/` 归档进 `phases/p*/`**（使用者指示，参照 `phases/README.md`） | ★★ **移 226 个文件，测试一个都没移**。六件套按 `phases/p2/docs/dev_phase_two/README.md` **自己 2026-08-10 写下的逐文件检查单**勾选 ⇒ **只有 2 个可移**（`progress.md`、`LEVELSET_DELETION_INVENTORY.md`），**4 个被条件挡住，而两条阻塞是关于当前库的可测事实**：`roadmap.md`（实测**无任何后续计划承接 D1–D5** —— `D5` 只在 `CLAUDE.md` 出现，那是文档地图不是计划）、`DECISION-2026-08-02-precond.md`（实测 `newton.py` 的 `ew_eta0`=`ew_eta_max`=**1e-6 仍是库默认**）、`_TEMPLATE.md`（phase 6 正在沿用并链接）、`PHASE_TWO_CAPABILITY_BOUNDARY.md`（实测 phase 3/4/5 **都没产出自己的能力边界**，它仍是唯一那一份）⇒ ★★★ **按「phase 2 已结束」这个日期理由一次性搬走，会把当前库默认值的决策记录和唯一的能力声明归档掉；那份检查单正是为拦住这件事而存在，它拦住了**。★★ **测试不归档是测量结论**：`phases/README.md` 第 59–61 行「把一条活的收口门指进归档比没有这道门更糟」+ 实测 **72/72 个测试文件在门控全集里都有通过项** ⇒ 每个都在检验活代码；phase 1 的 18 个能归档是因为**它们那条路被删了**，而 **phase 2–5 没删任何路线**。★ 唯一例外是一条**该删不该归档**的（`test_ab_bit_identity_gate_free_library`，2026-07-29 因前提为假 `mark.skip`）⇒ 登记删除。★★★ **本轮最贵的发现是搬迁自己造成的**：**239 条相对 markdown 断链**（基线 9 → 248），最大一块是 **`dev_phase_five/progress.md` 留在原位而它的 60 个轮次文件搬走**，11 条兄弟链接当场断掉 —— **「留一个、搬其余」这个决定本身有链接代价，而它不在任何检查单上**；而这一类**根相对的正则在原理上看不见**，正是 2026-08-10 那次整理「已修三类」里**没有**的第 5 类（第 4 类 = 活脚本里的 `Binding gate text:` 绑定行，实测预存在 **227** 条死字面量 + **139** 处死目录引用，`phases/p1/docs/roadmap/track_v.md` 57 领头）。★★ **方法**：① 移动前取基线（A=12/C=9/D=139）→ ② `git mv` → ③ **移动后、改写前** A 必须 **12 → 152**（**G-TEETH：此刻报 0 的仪器是坏的**）→ ④ 改写（唯一 basename 重指，231 自动 + 12 通名手改）→ ⑤ **按集合差复验**（计数会上升，因为**我为记录亏空而写的新文档本身引用了那些死路径**）⇒ **净新增断裂 0，顺带修掉 70 条旧账**；两条初看新增的核后是**同一发现换了路径**。★★ **`pyfp3d/` 被改 8 个文件而代码零改动 —— 用 AST 证明**（清空 docstring 后与 HEAD 基线逐位相同），不是靠读 diff：我第一版 grep 过滤器就漏了 docstring 的**续行**。★ **第一版仪器是错的且被抽样抓住**：把 `[phases/p1/docs/roadmap.md](phases/p1/...)` 这种 **markdown 显示文本**算成路径引用，报出 277 条假断裂 ⇒ **与 `phases/README.md` 自己记的「第一版闭包太松」是同一个错误的第二次出现**；口径：**报一个数之前先抽样看它是不是你以为的东西**。★ 四条事前预测**全部落空**，一次没改预测 | **移动 PASS · 净新增断裂 0（B/C 集合差）· G-TEETH PASS（12→152）· AST 零代码改动 PASS · 主回归 6 passed** | 见下一行台账（全套复跑）；`pyfp3d/` **代码**零改动 |
| 0 | 2026-08-24 | [立项](20260824-0000-initiation.md) | 阶段立项：四个面的规模与覆盖侦察、`GS6.x` 命名、三条常设要求、拟定六轮序列 | ★★★ **四个面不对称**：测试面的侦察 phase 5 收口时已做完（209 门号的四类划分、22 条只在 `bench/`、三条产品指标无锁、全项目仅一条墙钟断言且它是机器标定）⇒ **本阶段不从测试开始**；**代码面（21 635 行）与文档面（29 388 行）是零覆盖的两个面，那里是工作面**。★★ 立项当天 60 秒内查出**三处 CLAUDE.md 失效**（"73 test files" 实为 **72**；"`cases/demo/` keeps 9 of 33" 实为 **6**；"`cases/analysis/` 10 of 33" —— ★★★ **该目录不存在**，已在 `7edce18` 并入 `bench/studies/`）⇒ 每个 session 最先读、且明写 override 默认行为的那份文件，**在仓库自己的布局上是过期的**，而共同点不是"过期"而是**没有任何东西检查它** —— 与 `bench/studies/README.md` 缺 32 个目录同一族；★★ 而它们**是可检查的陈述** ⇒ 第一轮有了具体对象 | RECORDED（立项，无判据） | 承接 **595 / 12 / 2**；`pyfp3d/` 零改动 |
