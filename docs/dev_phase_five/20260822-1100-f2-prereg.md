# 20260822-1100 — **第五阶段第五轮**预注册（F2）：`C_L = 0.09` 的**出处**

**性质**：预注册。**在任何测量/编辑之前提交**。
**执行前置**：[立项](20260822-0000-initiation.md) §4（F2）+
[第四轮审计 A20](../dev_phase_four/20260819-1100-closures-source-audit-verdict.md)。

---

## 0. 被检查的东西

`closures.py:67-69`：

```python
C_L_DEFAULT = 0.09    # outer dissipation length L = C_L * delta (D-CT-2;
                      # Bradshaw outer-layer value; 2-D-reduction calibration
                      # recorded in the GV1.1 VERDICT)
```

而第四轮审计的 **A20** 记载：论文 **p.9 声称 `L` 按 Clauser G-beta locus 标定**
⇒ **出处声明的标定从未做过**；`0.09` 目前是 **Bradshaw 值 + 项目自己的二维标定**。

★ **干跑已核**：`reference/` 里**两份 PDF 都在**
（`drela-2013-…-general-configurations.pdf`、`drela-giles-2012-…-airfoils.pdf`）⇒ **可读**。

## 1. 两条分支（**立项 §4 已事前写死，本轮逐字执行**）

| 分支 | 条件 | 动作 |
|---|---|---|
| ★ **(A)** | 论文**给出了** G-beta locus（可用的关系或数值） | 做一次标定，**并报它与 0.09 的差** |
| ★ **(B)** | 论文**只给符号不给值** | **改注释承认 `0.09` 是项目的选择** —— 与第四轮对 `κ, B, r` 的处置**一致** |

★★ **裁决相容性（立项 §4 已核，重申）**：G-beta locus 是**论文里的相关式**，
**不是 BL 剖面实验数据** ⇒ **不违反**「不引入 BL 剖面参照」的裁决。

## 2. 判据

| 判定码 | 条件 | ★ 反向结果落在哪 |
|---|---|---|
| ★★★ **C-SOURCE** | **逐字引出**论文里关于 `L` / `C_L` / G-beta 的原句与页码 | 找不到 ⇒ **(B)**，并**记明"找过哪里"** |
| ★★ **C-CAL** | 若 (A)：由 locus 推出的 `C_L`，与 **0.09** 的差 | ★ **不论差多少都只 RECORDED** —— **本轮不改 `C_L` 的值**（改它会移动全部 Track V 数，需自带半径，与 F1/F6 同流程） |
| ★ **C-COMMENT** | 注释订正到**与实际来源一致** | ★ 改注释**不动任何数** ⇒ **不需要半径**（F7 的先例） |
| **C-N** | 若论文给的是**范围**而非单值 ⇒ 报范围，**不挑一个数** | —— |

★★ **本轮的"修"只到注释为止** —— 与 F7 同一处置，理由相同。

## 3. 守卫

**G-FROZEN-LIB**（★ 除注释外 `pyfp3d/` 零改动，用 `git diff` 逐行证明）、
**G-SENTINEL**（`C_L_DEFAULT` 的**值**不变，从模块 import 断言）、
★ **G-QUOTE**（新）：**引用必须带页码与原文**，不得转述 —— 第四轮 A20 就是靠原文定的性。

## 4. Kill criterion

1. ★★ 想在本轮**改 `C_L` 的值** ⇒ 停（那要走 F1/F6 的半径流程）。
2. ★ 论文里找不到就**编一个来源** ⇒ 停，判 (B) 并记明找过哪里。
3. 引用不带页码 ⇒ 停。
4. > 30 min ⇒ 停。

## 5. 事前预测

1. ★★ **落在 (B)** —— 依据：第四轮已发现论文对 `κ, B, r` **只给符号不给值**，
   `C_L` 很可能同样。★ **可以落空**：p.9 若真给了 locus 的数值形式，就是 (A)。
2. ★ 若落在 (A)，**由 locus 推出的 `C_L` 与 0.09 的差 < 50 %** —— ★ 依据薄弱（Bradshaw 的
   `a1 = 0.15` 与 Clauser 的 `G ≈ 6.1–6.8` 是不同的标定族），**这条我没有把握，如实标注**。
3. 零 FP 求解，< 30 min。
