# 20260822-0000 — **下一阶段立项**：`closures.py` 求积/标定的一次性结清（F1 + F3 + F2）+ M1（激波位置）

**性质**：阶段立项文书。使用者裁决 2026-08-21：**收 GS4.1 的口**，
**下一阶段 = F1 + F3 + F2（求积/标定的一次性结清，F1 的成本在勘误清单）+ M1（激波位置）**。
**前置阅读**：[GS4.1 收口 note](../dev_phase_four/20260821-1700-gs41-closeout-note.md) +
[收口处置](../dev_phase_four/20260821-1800-gs41-disposition.md)（**含使用者的参照体系裁决**）。

---

## 0. ★★ 命名：**本阶段不新造门号**（先写，免得按 ID 推内容）

已核占用：**GS4.2 = Veldman 准同时/交互律**、**GS4.3 = 二维粘性标定**、
**GS5.1 = 级集删除（已完成）**、**GS5.2/5.3 = 产品层**。⇒ 造一个 `GS4.x` 会撞号，
而且**本阶段的两半分属不同 S 阶段**（F 项是 S4 的仪器，**M1 是 S1 的无粘标定**）。

⇒ **两半各用它们已有的 ID**：
- **F1 / F3 / F2** —— GS4.1 第四轮 `closures.py` 源码审计的登记项（[判定](../dev_phase_four/20260819-1100-closures-source-audit-verdict.md) §"登记给后续"）；
- **M1** —— 产品指标（[能力边界](../dev_phase_two/PHASE_TWO_CAPABILITY_BOUNDARY.md) §M1）。

★ 本项目已多次记过「**名字不是作用域**」，所以这一节是文书的一部分，不是注解。
★ 轮次记录延续 **`docs/dev_phase_five/progress.md`**（本阶段新建），格式同前。

## 1. ★★★ 两半**不对称**，事前说清

| | F1 + F3 + F2 | M1 |
|---|---|---|
| 性质 | **有界的审计结清** | **产品指标，已记录为在当前离散上不可达** |
| 已知 | F1 已量化（`φ*₁` **4.3e-05**）、F3 未检、F2 出处声明与实现不符 | (a) 激波位置**两级都 PASS**；**(b) −16.2 %**（要 <3 %）、**(c) 9.4 %/17.9 %**（要 <3 %）⇒ 差距 **5.4× / 6.0×** |
| 预期轮数 | **1–2 轮**，多数零求解 | ★★ **它自己就是一个阶段** |
| ★ 第一个问题 | "改完有多少数会动" | ★★★ **"什么能改变离散"** —— **不是"重测 M1"** |

★★★ **为什么 M1 不能以"重测"开场**：能力边界 §M1 已写明
**要 (b)(c) 达标需把激波位置控到 ±0.0055 c（`h_shock ≲ 0.005–0.01 c`）**，
且 **"可达性：在当前离散上不可达"**。⇒ 在同一离散上重测只会**复现 −16.2 %**，
**花机时换一个已知的数**。

## 2. F1：★★★ **先量"爆炸半径"，再写勘误清单**

使用者的措辞是「**F1 的成本在勘误清单**」。★ 那个措辞假设**有数会动**。
而第四轮审计自己写着：那 **4.3e-05 比它上面压着的模型误差（GV1.1 的 2 % 带 / 第一轮的 4.5 % 地板）
小约 1000 倍** ⇒ **可能一个已提交的数都不动**，那么**勘误清单是空的，F1 近乎免费**。

⇒ **第一轮的唯一任务是量它**，判据事前写死：

| 判定码 | 条件 |
|---|---|
| ★★ **Q-RADIUS** | 把 `ETA_LAM` 8 → 11 点，**跑全套件**，报**移动的断言数**与**每一处的移动量** |
| **Q-EMPTY** | 若套件**逐位不动** ⇒ **勘误清单为空**，F1 只剩"改常数 + 加锁" |
| **Q-LIST** | 若 N 条动了 ⇒ **勘误清单 = 那 N 条 + grep 移动值across `docs/`**（纪律 #11） |

★★ **kill 条（事前）**：**绝不为吸收这次移动而放宽任何测试容差** ——
那会把一次订正变成一次掩盖。若某条锁因订正而红，**红本身是覆盖成功**，照实记并**重钉带出处**。
★ 方法：`git stash`（项目认可、不碰 index）+ **pop 后哨兵复查**，与 E-CF 降级同一套。

## 3. F3：湍流侧 24 点求积的同型检查（零求解）

第四轮的清单**封闭、不擅自扩**，所以 F3 当时没做。现在做，**用第四轮修好后的度量**：

★★ **必须带走第四轮的教训** —— 那一轮**第一次执行报了六条假 AUD-DEFECT**，全是度量缺陷：
分母会取零（`f2,f3 ∝ (1−η)²`）、比较**两个截断误差**（Ψ 加权含 `arctan`，不是多项式）、
**两个恒为零的量做相对比较**。⇒ **复用它修好的度量 + `G-TEETH`**（注入 1e-6/1e-9/1e-11 已知扰动，三个必须全被抓到）。

★ 判据：**报被积函数的次数与所需点数**，**不报误差大小**（与第四轮的锁同构 —— 那条锁的是**根因**，修与不修都成立）。

## 4. F2：`C_L = 0.09` 的出处

论文 p.9 声明耗散长度 `L` 按 **Clauser G-beta locus** 标定；库注释写的是 **Bradshaw 值 + 项目自己的二维标定**
⇒ **出处声明的标定从未做过**。

★★ **两条分支，事前都写下**：
- 若 `reference/` 的 Drela 2013 **给出了那条 locus** ⇒ 做一次标定，**并报它与 0.09 的差**；
- 若**没给**（只给符号）⇒ ★ **改注释承认 `0.09` 是项目的选择**，与 A17/A18 的处置一致
  （第四轮已把 `κ, B, r` 归为"项目的选择"）。

★ **裁决相容性检查**：G-beta locus 是**论文里的相关式**，不是 BL 剖面实验数据
⇒ **不违反"不引入 BL 剖面参照"**。这一句写下来，免得下一轮误判范围。

## 5. M1：★★★ 第一个问题是**仪器**，不是指标

GS4.0 第 18 轮（把位置当主判据）实测：**`D` 中位 0.0120 c，而一个单元 `h/c ≈ 0.041`
⇒ `D ≈ 0.29 个单元**；足迹提取是**逐单元**的，**分辨率就是 `h`**，却被要求分辨 `h/4`
（四个态出现并列 = 提取器落到了同一个单元上）。
⇒ 那一轮自己的下一步 **(a)** 就是：**把足迹提取改成亚单元（对 `M=1` 线性插值）**。

★★★ **M1 的入口因此是：先把位置仪器做成亚单元可分辨的** ——
**否则任何修法能不能起作用，我们量不出来**。
★ 而它**便宜**，且**它本身就是登记项 1 的一个小版本**（GS4.0 第 18 轮原话）。

★★ **诚实限定（事前）**：这一步**不修 M1** —— 它只让 M1 的进展**可测**。
**不得**把"仪器变可分辨"写成"M1 有进展"。

## 6. 六件**不许静默改掉**的事

1. ★★ **E-CF 已降为 RECORDED**，且**九条关闭各带重开条件**（[处置](../dev_phase_four/20260821-1800-gs41-disposition.md) §3）；
2. ★★★ **参照体系两级**：边界层量 → XFOIL（答实现）、`Cp` → 实验（答模型）；
   **一条边界层量与 XFOIL 的差异，只有被证明会移动 `Cp` 时才算债务**；
3. ★ **裁决的限制**：与 XFOIL 一致是**上限不是正确性**（第四轮实测我们的层流族对 Falkner–Skan 更准）；
4. ★★ **F1 的订正不得被容差放宽吸收**（§2 kill 条）；
5. ★★ **M1 的 (a) 已经 PASS** —— **不得**在"改进"中把它弄成 FAIL；★ 引用 M0.80 medium **必须带线程数**（8 线程不收敛 / 16 线程收敛）；
6. ★ **`closures.py` / `ibl3.py` 此前 17 轮逐字节未改** —— F1 是**第一次**要动 `closures.py`，
   所以那条 `G-FROZEN` 守卫**必须换成一条会说明"改了什么、动了多少数"的守卫**，不是删掉。

## 7. 阶段级事前预测

1. ★★★ **Q-RADIUS：移动的断言数 ≤ 3**（可能为 0）—— 依据是那 1000× 的量级差。
   ★ **它可以落空**：`ibl3.py` 的三维路径把 `φ*₁` 用在一条控制方程上，放大是未知的。
2. ★ **F3 会找到与 A11 同型的一处**（库注释声称"24 点可解析 δ⁺ 到 O(1e5)"）—— 但**大小可能可忽略**。
3. **F2 落在"改注释"那一支** —— 依据：第四轮已发现论文对 `κ, B, r` **只给符号不给值**。
4. ★★ **M1 的亚单元仪器会让位置度量变得可分辨**（并列消失）—— 而那本身就是
   "亚单元表示确实要紧"的一次直接演示。
5. 第一轮（F1 爆炸半径）**零新求解**，成本 = 一次全套件（~17 min @8 线程）。
