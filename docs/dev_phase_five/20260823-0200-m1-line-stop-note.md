# 20260823-0200 — **停止 M1 位置线**：第八至十一轮建立在**第四阶段第 19 轮已经作废的读数**上

**性质**：复查记录（**零机时、零求解、零代码**）。触发：为 R12 核事实时读
[能力边界 §3.2 / §3.5](../dev_phase_two/PHASE_TWO_CAPABILITY_BOUNDARY.md)。
**不是判定** —— 本文不产生新测量，只把已提交的记录与我这四轮的关系说清楚。

---

## 1. 事实

| | |
|---|---|
| 第四阶段 **第 18 轮**（`20260818-1300/1500`） | 把位置当主判据，13 个态，`D`，判 **POS-MIXED** —— ★ **我这四轮的全部锚点** |
| 第四阶段 **第 19 轮**（`20260818-1700/1900`） | **G-SUB FAIL ⇒ kill 条 1 ⇒ 停**；查出**外侧两站的"足迹" ≈ 0.99 = 后缘**，误差 **0.54–0.56 弦长 = `D` 的 50 倍**；**判"三轮位置读数全部作废"**，其中就包括第 18 轮 |

**第 19 轮在第 18 轮之后四个小时，同一天，同一个目录。我没有读它。**

## 2. 它已经写下的，与我四轮"发现"的对照

| 第 19 轮已记（2026-08-18） | 我的轮次 |
|---|---|
| η 0.96/0.99 足迹 ≈ 0.99 而裁判 0.44，差 **0.54–0.56 弦** | 第十轮"发现"（0.4515 / 0.5292） |
| **50 倍于 `D`** | 第十轮"发现"（50×） |
| **七站中位数把它藏了三轮** | 第十轮"发现"（MAD 忽略尾巴） |
| 根因 = **取极值于 `{M≥1}`**，撞上 roadmap 明文禁止项（P13 翼尖伪影） | ★ 我归因到"站"，勘误后归到"规则饱和" —— **仍未归到 P13**，而那才是原因 |
| 判据缺陷 **#13**：走进一条**自己在审计里复述过**的禁止项 | —— |

★★★ **而 §3.5 把禁止项推广过了**：
> **凡是「超声速区上的极值」这一类的量，都继承 P13 这个阻塞** —— 包括**后来才发明的**。

**`foot_from_field` 正是这一类。** 我的四轮全部在这条禁止项之内。

## 3. R12 的前提也是退役的

我对使用者说："唯一还开着的修法是**登记项 1「激波定位」= 体内梯度重构**"，
出处是能力边界**第 88 行**。而**同一份文件第 263 行（§3.2）写着**：

> ★★★ **退役 2026-08-17（GS4.0b 探针，kill 条 1）**：这句「共同修法」**不再是可用前提** ——
> **G-SMEAR**：线性 patch 重构与纯平滑分不开，**输出由 patch 尺度决定，不由「重构还是平均」决定**。

⇒ **文件自相矛盾（第 88 行未随 §3.2 更新），而我引了过时的那一半。**
⇒ **R12（在 NACA 上排 P0–P3）的前提不成立，不开。**

## 4. 另一处：`0.61 ± 0.02` 不是被提交的参照

被提交且门控的是 `cases/reference_data/naca0012_m080/shock_reference.csv`：
**`upper_shock_x_c = 0.62, tolerance 0.03`**，带完整出处（Euler 锚 0.60–0.63 + Holst 的 FP 后移带）。
`tests/test_p4_transonic.py:206` 用的正是它。
而 **`0.61 ± 0.02` 在 9 份文档里流传**（含 D7 草案与独立审计），**参照文件里没有这个数**。

★ **核过：没有任何判定翻转。** 最接近的一处是 σ OFF coarse 的 **0.6581**，
判"在带外（高侧）"—— 对 `[0.59, 0.65]` **仍在带外**，但**超出量 0.0081 而不是 0.0281**，
**边际是报出来的 1/3.5**。原句不改写，加勘误指针。

## 5. 我这四轮里**确实是新的**部分（已核对第 19 轮无此内容）

| | |
|---|---|
| ★ **饱和率依臂单调** P0 36.3 % → P1 22.2 % → P2 16.7 % → P3 12.5 % | 第 19 轮只给了**一个态的逐站表**，没有依臂率 ⇒ 这是第 18 轮自己登记的 `Q-SMOOTH` 疑问的**具体机制** |
| ★ **站带偏置** +0.0188 c（P0），随带宽近似成倍 | 第 19 轮没有变过站带 |
| ★★ **J1 自身噪声 0.0096 c** vs M1 容差 **±0.0055 c** | 第 19 轮把 J1 当"独立裁判"用，**没有量过它的噪声** ⇒ **即使足迹修好，J1 也证不了 ±0.0055** |

★ 但**这三条都长在第 19 轮建议停掉的那条线上**，且第八至十一轮的**头条读数**要么重复、要么建立在作废的数上。

## 6. 我的过程失败，说准

**不是"没查资料"** —— 我读了能力边界的 §M1（59–96 行）并逐字引用了它。
**是我读了一节，用了它，没有读它自己指向的那一节**（§3.2 就在 88 行那句话的落点上），
**也没有读那条线最后一轮的判定**。
★★ 而项目的 CLAUDE.md 对这件事有现成的一句：
**「在第二次复述一个债务之前，花掉那个零机时去弄清它的指称物在哪」** ——
我复述了"唯一还开着的修法"，**没有去看它的指称物**。

★ 第二处：`G-REPRO` 我写成"必须复现第 18 轮的 4/13"，并因为它复现了而 PASS。
**复现一个被作废的数，仍然是复现一个被作废的数** —— 守卫**没有防住任何东西**，
因为它检的是"我算得对不对"，不是"这个数还成不成立"。
⇒ **常设新增：锚定一个历史数字之前，先确认它没有被后续轮次撤销** ——
`G-REPRO` 这类守卫要多一条**「该锚点当前有效」**的检查。

## 7. 重新规划的起点（不是本文的裁决，是可用的事实）

第 19 轮 §6 给了**若仍要测位置必须先做的三条**（事前登记）：
① 尖端带 η > 0.95 **排除或掩蔽**（沿用生产 taper `r_c = 0.05·b_semi` 语义）；
② **逐站报，不用中位数**；
③ 给"足迹"换一个**不取极值**的定义。
并建议：**这条线在这里停**，把「相干性可刻画，而它是否对应正确位置在 P13 被处理前无法测量」交出去。

★★★ 而我这四轮里唯一**不依赖足迹度量**的观察，可以直接进下一步的规划：
**M1 的 (b)(c) 是差分判据**（两级 cl 差、`upwind_c` 扫掠内 cl 变化），
**它们不需要任何绝对位置裁判** —— 两个量都是我们自己解的 cl 之差。
⇒ **一条不经过位置度量、因而不受 P13 阻塞的路径存在。** 它是否值得走 = 使用者的裁决。

---

# ★★★ 勘误（同日，使用者第二次"再检查一遍"后）：§7 最后那条路**不存在**

§7 写：「M1 的 (b)(c) 是差分判据，不需要绝对位置裁判 ⇒ **一条不受 P13 阻塞的路径存在**」。
**前半句对，结论错。**

## E1. 那些臂**根本碰不到 `cl`**

`pyfp3d/post/surface.py::wall_force_coefficients`（`bench/run_capability_matrix.py:312` 就是用它算 `cl_p`）：

> 每个壁面三角形：**velocity = in-plane tangential gradient**（自然 BC 下**正是**壁面速度）

⇒ 它读的是**壁面三角形的面内切向梯度**，**根本不经过体单元梯度**。
而 P0–P3 是 `element_velocity_q2` → `L.refit` 的**体单元梯度重构**。

★★★ **两者不相交：这些臂改变的是我们怎么读体场（足迹、相干性），不是载荷。**
⇒ **它们既动不了 (b) 也动不了 (c)。** 我说的"路径"没有杠杆。

## E2. 于是 M1 的真实处置，比"这条线该停"更清楚

| 杠杆 | 状态 |
|---|---|
| 后处理体梯度重构（P0–P3） | ★ **碰不到 `cl`**（E1）；且它读位置的那条用途**被 P13 阻塞**（第 19 轮） |
| 把重构**接进求解器** | **BR-LARGE**：动 > 10 % 的场 ⇒ **这是一个不同的离散化，不是一个修正**，按新离散化定价（第 17 轮已定价） |
| 「共同修法 = 体内梯度重构」 | **退役 2026-08-17**（G-SMEAR） |
| `h`（唯一被实测过的杠杆） | 需要 `h_shock ≲ 0.005–0.01 c` —— 网格成本问题 |

⇒ ★★★ **M1 在当前成本下没有开着的路** —— 这不是"线该停"，是**一个可以交出去的处置**：
**剩下的两条都是"新离散化"或"更细的网格"，都不是一轮能做的修正。**

## E3. 我这次错的形式

★★ **"不需要 X" 不等于 "有办法做到"。** 我确认了 (b)(c) 不需要位置裁判（真），
就把它写成了一条路径（假）—— **中间少了一步：谁来动它？**
⇒ **常设新增：宣布一条路径之前，先点名它的杠杆，并验证那个杠杆接在目标量上。**
★ 而这一步**只花了两次 grep**：`cl_p` 从哪来、那些臂动的是什么。
