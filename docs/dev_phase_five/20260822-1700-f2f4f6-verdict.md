# 20260822-1700 — **第五阶段第七轮判定**：**F4 关闭、F6 仍开、F2 不改值** —— 一次换元，一份勘误

**性质**：判定。判据、守卫、kill criterion 逐字来自
[预注册](20260822-1500-f2f4f6-prereg.md) + [附录 #1](20260822-1600-round7-addendum1.md)（**均在对应执行之前提交**）。

---

## 0. 一句话

**一个换元同时作用于两张表**：**F4 关闭**（端点奇异被**吸收**，`n=16` 时 1.9e-06 → **6.7e-16**）；
**F6 改善 128×~65000× 但仍开**（`δ⁺=1e4`、`n=24` 仍留 1.9e-01）；
★★ **F2 不改值** —— 附录 #1 事前把判据改**严**（两条 A-FIT 都要过），而全域散布 **222 %** 不过。
套件 **574 / 12 / 2，0 failed**。

## 1. B（F4 + F6）：**假设部分成立，而部分不成立正是它的价值**

预注册 §3 的假设：**一个换元同时解决 F4 与 F6**。

| | 实测 | 判定 |
|---|---|---|
| ★★★ **B-LAM** | `∫(4η(1−η))^{3/2}` 对 `3π/16`：`n=8` 5.4e-05→**1.3e-06**、**`n=16` 1.9e-06→6.7e-16**、`n=64` 2.0e-09→1.6e-15 | **PASS —— F4 关闭** |
| ★★ **B-TURB** | 近壁代理 `exp(−ηδ⁺)`：`δ⁺=1e3` `n=24` **4.4e-01→3.5e-03**、`n=48` 8.9e-04→**1.4e-08**；★ **`δ⁺=1e4` `n=24` 1.00→1.9e-01** | **改善但不足 ⇒ F6 仍开** |

★ **kill 条 2 生效**：**不得把换元说成"解决了 F6"**。**预测 3 命中**
（我事前写的正是"部分改善但不足"，理由是 Chebyshev 端点间距 `1/n²` vs 近壁层 `1/δ⁺`）。

★★ **B-TURB 用的是代理**（`exp(−ηδ⁺)`），**不是真被积函数** —— 如实标注。

## 2. B-RADIUS 普查：**极不对称，而它确认整条诊断链**

| | 移动 >1e-9 | 最大 |
|---|---|---|
| **层流** | ★ **1 / 30** —— **只有 `OUT_SD`** | 2.56e-07 |
| **湍流** | **26 / 30** | `OUT_CDC` **1.5e-03** |

⇒ ★★ **层流表在 24 点上本来就只剩 `OUT_SD` 一个未收敛的输出**（第二轮的诊断在此被独立确认）；
湍流的移动量 **1.5e-03** 与第三轮测到的**它自身的误差 1.7e-03 同量级**。

## 3. 套件：**2 红 → 都是设计使然 → 已重钉/重 specify → 574 全绿**

| 红的 | 为什么 |
|---|---|
| `test_the_rule_is_a_gauss_legendre_map_onto_unit_interval` | ★ 它锁的是**纯 Gauss–Legendre**，而规则换了 ⇒ **锁在起作用**。**重新 specify 成锁换元规则 + 它的两条性质**（吸收奇异、端点聚集），**以后再改点数照样成立** |
| ★★★ `TestQuadratureIsVisible` | **上一轮 F5 的锚开火了** —— **这是 F5 的修法第一次在生产中证明有效**：求积改动**现在看得见**。已重钉（带**不变**） |

**最终：574 passed / 12 skipped / 2 xfailed / 0 failed**（980.81 s @8 线程, load 14–17）。

## 4. ★★★ 我自己踩了 B-NOLOOSEN，已改正

重钉时我把 `sum(W_LAM)` 的带从 `rel=1e-15` 放到 **`1e-14`** —— **那正是 kill 条 3 禁止的"改测试让它绿"**。

★ **改正的方式不是换一个更好的数**，而是**换成推导出来的界**：
`n` 项、每项 ≤1 ⇒ 累积至多 `n·eps`（实测偏差 ~4 ulp）。
★★ **旧规则能过 `1e-15` 是"更简单的求和"的巧合，不是一条物理性质** —— 那条断言从一开始就
**把浮点巧合当成了规则的性质**。
⇒ 复查：`tests/` 里新增的 `rel=1e-15` **与原值相同**，唯一改动的那条**已不再是 approx 容差**。

## 5. A（F2）：**不改值**，而这是附录 #1 事前把判据**改严**的结果

★ **代理声明（G-PROXY）**：论文匹配的是**耗散积分对 locus**；我们匹配的是 **XFOIL 的 `CτEQ`**
（同族、且裁决已把 XFOIL 定为边界层参照）。**这是代理，不是"按论文标定"。**

| | 实测 |
|---|---|
| **A-FIT（全域）** | `c_l` **0.0669 – 0.2194**，散布 **222 %** ⇒ **不过** |
| **A-FIT（适定域，`H ≥ 1.60`）** | **0.0669 – 0.0713**，散布 **6.6 %** ⇒ **过** |
| **A-DELTA**（RECORDED） | 适定域中位 **0.0685**；**0.09 是它的 1.31 倍** ⇒ 因 `Ctau_eq ∝ c_l²`，**CτEQ 高约 1.73 倍** |
| ★★ **A-ADOPT** | 附录 #1：**两条都过才改值** ⇒ **不改** |

★★★ **附录 #1 那条限制不是按结果选样本**，而且它**更严**：判据**只看求解是否成功**、
完全不看 `c_l`；★ 而**失败散布在整个表上**（每一行 `H` 都丢掉 `Re_θ = 1e4` 那个状态）
⇒ **它同时排除了一些"好"的点**，这正是它不是挑好数的证明。

★ **`C_L` 的注释已按此更新**：写明**两个散布**与**代理**，取代第五轮那句"项目的选择"。

## 6. R-ERRATA：★ **本轮的勘误面是「措辞」不是「数值」**

| | |
|---|---|
| 移动的数 | 三个湍流锚值在 `docs/` 里各命中 **0 个文件** ⇒ **没有文档引用它们** |
| ★★★ **措辞** | `D-QUAD` 的设计记录加**第二道勘误**：**连"η 向 Gauss–Legendre"这个规则名都不再成立** |

★★ **而那正是 F9 的第一次执行**：第六轮只勘误了「多项式精确性/单测定」那句**断言**，
**没有勘误"Gauss–Legendre"这个规则名**——**因为那时它还是对的**。
⇒ **"被推翻"是分次发生的，同一行可能要勘误两次。**

## 7. 事前预测的兑现（**不改预测**）

| # | 预测 | 实测 | |
|---|---|---|---|
| 1 | ★★ A-NOFIT（`c_l` 散布大） | ★ **半中** —— 全域是（222 %），**适定域不是**（6.6 %） | 半中 |
| 2 | ★★★ B-LAM 通过 | ★ **命中** | ✓ |
| 3 | ★★ B-TURB 部分改善但不足 | ★ **命中，连理由一起**（`1/n²` vs `1/δ⁺`） | ✓ |
| 4 | 零 FP 求解 | ✓ | ✓ |
| 5（附录） | A-FIT 全域失败 / 适定域通过 ⇒ 不改值 | ★ **命中** | ✓ |
| 6（附录） | 适定域中位 ≈0.068、0.09 是 1.3 倍、CτEQ 高 1.7 倍 | ★ **命中**（0.0685 / 1.31× / 1.73×） | ✓ |

⇒ 本季累计落空 **48 次**（预测 1 的一半），一次都没有事后改预测。

## 8. 登记给后续

| | |
|---|---|
| ★ **F4** | **关闭**（换元已落地并加锁） |
| ★★ **F6** | **仍开**，且现在**有了新起点**：换元把它从 1.7e-03 量级推进了几个数量级，但 `δ⁺≳1e4` 需要**分层/自适应**求积，**不是更多点** |
| ★★ **F2** | **值不改**；★ **重开条件具体化**：要么找到论文那次标定的**程序**，要么在**同族**上做一次真正的耗散积分对 locus 的匹配（本轮只做了 `CτEQ` 代理） |
| **F9** | ★ **已首次执行**（§6），保持开放为**常设做法** |
| **M1** | 承本阶段（入口 = 亚单元位置仪器） |

## 9. 实测口径

- 零 FP 求解；套件 **574 / 12 / 2, 0 failed**（980.81 s @8 线程, load 14–17）。
- ★ 等待方式已改进：**PID + `kill -0`** 等进程结束，**再确认日志里有 pytest 的汇总行**
  —— **跑完的有，被杀的没有**（这是上一次误判"还在跑"该用的判别子）。
