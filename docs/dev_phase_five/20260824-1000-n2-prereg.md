# 20260824-1000 — **第五阶段第二十七轮预注册（N2）**：喷管 h ≤ 0.05 的 `reason=cap` 是**预算**还是**能力**

**性质**：预注册。**在任何代码之前提交**。`pyfp3d/` 不改（用归档的 `duct`/`nozzle`，已核可导入）。

---

## 0. ★★★ 开工前读实现，问题的形状变了

我此前把 `reason=cap` 当成 `m_cap` 限幅。**读代码后不是**：

| 事实 | 出处 |
|---|---|
| **上限硬编码 `n_max=80`、容差 `tol=1e-11`** | `phases/p2/bench/s1_duct/run_nozzle.py:65` |
| `reason` 只有 `{'cap', 'tol'}`，`cap` 腿的 `n_newton` **全是 80** | 已提交 CSV |
| 那些腿的 `res_final` = **5.9e-9 .. 1.9e-8** ⇒ **差容差 2–3 个数量级** | 同上 |
| `newton()` 返回 **`residual_history`** | `duct.py:214, 229` |

⇒ ★★★ **这正是 CLAUDE.md 明写「不是失败」的 `budget_limited` 形态**
（"still descending, ran out of iterations"；且有先例：LE-1 的 `ls_naca_medium` **放开上限后 6 步就收敛**）。
★★ **但 CLAUDE.md 同时警告**：`budget_limited` **要求单调尾部**，不能只看残差 ——
一个周期 3 的极限环曾给出 `descent10 = 2.0021` 骗过一个手挑的阈值。
⇒ **必须取 `residual_history` 并分类，不能只看 `res_final`。**

## 1. 判据

| 判定码 | 内容 | 两个方向 |
|---|---|---|
| ★★★ **B-BUDGET（binding）** | 把 `n_max` 80 → **600**（其余逐字不变），跑 `nx=400`（h=0.05）× `C ∈ {1.0,1.5,2.0,3.0}`、`regular/exact` | **收敛到 `tol=1e-11` ⇒ 原来是预算，h 趋势变可测**；**仍撞上限 ⇒ 是能力，那才是真障碍** |
| ★★ **B-MODE（binding）** | 对**每条**腿的 `residual_history` 分类：尾部**单调**？`descent10`？有无**重访**？ | **单调下降 ⇒ budget_limited**；**振荡/重访 ⇒ limit_cycle**，即便 \|R\| 很小 |
| ★ **B-TREND（条件）** | 若 B-BUDGET 通过：把 h=0.05 纳入后重算 `err_cells` 的 h 趋势与 `d(err_cells)/dC` | 现有可引用数据只有两级 ⇒ 这会给到**三级** |
| **B-COST（RECORDED）** | 每腿步数与墙钟 | —— |

## 2. 守卫
**G-FROZEN-LIB**（`pyfp3d/` 不改；**归档目录也不改** —— 新脚本放 `bench/studies/n2_nozzle_budget/`）、
★ **G-ONEKNOB**：**只**改 `n_max`，`tol`/`C`/`nx`/`jitter`/`start` 逐字沿用 `run_nozzle.py`；
★★ **G-TRUTH**：解析激波位置由 `nozzle.exact_solution` 给出，**`err_x` 由代码算不由我写**；
★ **G-REPRO**：`n_max=80` 重跑必须复现已提交的 `res_final`（≈6e-9 等）与 `x_shock`，否则先查会话差异；
★ **G-CHECKOFF**。

## 3. Kill criterion
1. 改 `pyfp3d/` 或归档文件 ⇒ 停。2. 改除 `n_max` 外的任何参数 ⇒ 停。
3. **G-REPRO 不过 ⇒ 停**。4. 单腿 > 10 min ⇒ 停。
5. ★★ **不得**把"\|R\| 很小"当成收敛 —— 判定看 `converged`/`reason` 与 B-MODE 的分类。

## 4. 事前预测
1. ★★★ **B-BUDGET 通过** —— 依据：\|R\| 已到 6e-9、离 1e-11 只差 2–3 个数量级，而 80 步是硬上限；
   且 CLAUDE.md 有"放开上限后 6 步收敛"的同类先例。★ **可以落空**，那才是重要结果。
2. ★★ **B-MODE：单调尾部** ⇒ budget_limited。★ 可以落空（极限环），而**落空会解释为什么 80 步不够**。
3. ★ **B-TREND：三级下 `err_cells` 仍随加密增长**（0.651 → 0.684 → ~0.77）⇒ **定律"按单元数近似常数"在收敛数据上也不成立**。
4. 4 腿合计 < 10 min。
★★ **事前声明本轮的意义边界**：即便 B-BUDGET 通过，它**不解决 M1** ——
它只让那条机理定律的 h 趋势第一次有三级**可引用**数据。使用者已说明：**结果不好就结束 phase 5。**
