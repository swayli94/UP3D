# 20260822-0200 — **第五阶段第一轮判定（F1-a）**：半径**不小** —— 而我关于"哪些数会动"的两条预测都错了

**性质**：判定。判据、守卫、kill criterion 逐字来自
[预注册](20260822-0100-f1-radius-prereg.md)（**代码之前提交**）。
**本轮按 kill 条 2 停在"量半径"，不写勘误清单、不改文档。**

---

## 0. 一句话

**9 条测试变红**（7 条真移动 + 2 条**按设计**该红的审计锁），
★★★ **而更重要的是我对"哪些数会动"的判断错了两次**：
**Track V 一条锁都没红,但 Track V 的数确实动了** —— 实测 **30 个闭包输出里有 10 个动，最大相对 8.0e-04**。

## 1. Q-RADIUS：9 条红，分两类

| 类 | 条数 | 内容 |
|---|---|---|
| ★ **按设计该红** | **2** | `test_gs41_closures_audit.py::TestLibraryRuleUnchanged` —— 它锁的是**旧规则**（"层流规则是 8 点"）。★ **第四轮那条锁锁的是根因，不是误差大小**，所以修了规则它**必须**红 ⇒ **这是锁在起作用**，下一轮**重新 specify 成锁新规则 + 21 次这个根因** |
| **真移动** | **7** | GS4.1 的型线族不动点（经 `strip2d.march()` —— 第一轮的设计就是**复用 `closures.py`**） |

**七条的实测移动**：

| 断言 | 旧 | 新 | 相对 |
|---|---|---|---|
| `A`（平板不动点） | 8.02881134 | **8.028406562** | −5.0e-05 |
| `H`（ZPG 不动点） | 2.708292 | **2.707930911** | −1.3e-04 |
| `H/H_Blasius − 1` | 0.04523 | **0.045089310** | −3.1e-03（相对自身） |
| 楔 `m = 0` | 2.708292 | 2.707930911 | −1.3e-04 |
| 楔 `m = 0.0435` | 2.469411 | 2.469240351 | −6.9e-05 |
| 楔 `m = 1` | 2.087933 | 2.087925650 | −3.5e-06 |
| `test_profile_march_is_untouched_by_the_new_route` | —— | —— | 同源（型线族行进） |

★ **全部远小于它们上面压着的模型误差**（型线族对 Blasius 的 **4.5 %** 地板）——
**移动是真的，但它改变不了任何已发表的结论**。

## 2. ★★★ 我差点写下一句假话：**"Track V 零条变红" ≠ "Track V 的数没动"**

`test_v1_*` **一条都没红**。★ 而"没红"只说明**移动落在现有容差里**。
所以我**直接量了**（`closure_node` 的 30 个输出，8 点 vs 11 点，三种层流状态）：

| 状态 | 动了几个 | 最大相对移动 |
|---|---|---|
| `lam_zpg` | **10 / 30** | **1.4e-04** |
| `lam_apg` | **10 / 30** | 4.3e-05 |
| `lam_fpg` | **10 / 30** | **8.0e-04** |

⇒ ★★★ **Track V 的数确实动了，只是没有一条锁的带窄到能看见。**
★ 这是本季记了八次的那一族：**"守卫没响" 不等于 "什么都没发生"**。
★★ 而它同时是**关于锁的发现**：**Track V 的锁带宽于 8e-04 相对** ⇒
**它们看不见这个量级的求积改动** —— 与第八轮"每条湍流锁的窗口都从 `Re_θ≥800` 起、而改动在其下"**同型**。

## 3. Q-LIST：**非空,而且比我预测的大**

| 要做的 | 范围 |
|---|---|
| 重钉 | **7 条**（§1 表） |
| 重新 specify | **2 条**审计锁（锁**新规则** + **21 次**这个根因） |
| ★★ **勘误 grep** | 移动值 across `docs/`：`2.708292` / `2.469411` / `2.087933` / `8.02881134` / `0.04523`；★ **并且**：既然 §2 测出闭包输出动到 **8.0e-04**，**任何引用到 ≥4 位有效数字的已提交 Track V 数都可能动** ⇒ **勘误面比我预测的大** |

★ **本轮不做**（kill 条 2）。

## 4. Q-NOLOOSEN：**零容差被放宽** ✓

`git diff --name-only -- tests/` = **0 个文件**。★ 本轮**一行测试都没碰** ——
"改测试让它绿"与"量半径"是两件事，而混起来做正是把订正变成掩盖。

## 5. 守卫

| ID | 实测 |
|---|---|
| **G-SENTINEL** | 8 → **11** → （stash）**8** → （pop）**11**，每次都**从模块 import 断言**，不看 `git status` |
| **G-ONECHANGE** | `git diff --stat` 只有 `closures.py`，+10/−3 |
| **G-FROZEN-IBL3** | `ibl3.py` 逐字节相同 ✓ |
| **G-SWAP** | `git stash` + pop 后哨兵复查 ✓ |

## 6. 事前预测的兑现（**不改预测**）

| # | 预测 | 实测 | |
|---|---|---|---|
| 1 | ★★★ 移动的断言 **≤ 3**，很可能 **0** | ★ **落空** —— **9 条**（7 真 + 2 按设计） | **落空** |
| 2 | ★ 若有动，动的是 **Track V** 的锁，**不是** GS4.1 的 71 条 | ★★ **落空，而且方向相反** —— 红的**全是 GS4.1**，Track V **零红**（★ 但 §2 测出 Track V 的**数**动了） | **落空** |
| 3 | `n=11` 后到机器精度（本轮不测） | 未测，如实标注 | —— |
| 4 | 零新求解；成本 = 两次全套件 | ✓（**857.37 s @8 线程**，一次；另一次由 §2 的直接探针代替，更便宜） | ✓ |

⇒ 本季累计落空 **42 次**，一次都没有事后改预测。
★★ **预测 2 的错法值得单记**：我推理"`closures_2d.py` 与 `closures.py` **零交叉 import** ⇒ GS4.1 不受影响"，
**那个前提是真的，结论却是假的** —— 因为 **`strip2d.march()`（型线族那条路）本来就调 `closures.py`**，
**那正是第一轮"复用 `closures.py` 不重写"的设计**。
⇒ **我查了模块间的 import，却没查函数级的调用图。**

## 7. 下一轮（F1-b）

1. 重新应用 8 → 11（+ 注释订正）；
2. 重钉 **7 条**、重新 specify **2 条**审计锁；
3. ★★ **勘误 grep 两轮**：先 grep §3 的五个移动值，**再**按 §2 的 8.0e-04 检查 Track V 的高位数字引用；
4. ★ **Q-NOLOOSEN 继续适用**：重钉是**换成实测的新值**，**不是**放宽带。

★ **本轮的工作树已恢复到 8 点** —— 带着 9 条红提交会把"未完成"混进历史。
