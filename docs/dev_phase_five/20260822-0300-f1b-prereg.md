# 20260822-0300 — **第五阶段第二轮**预注册（F1-b）：**先查计划,再执行** —— 四处修正 + 结清

**性质**：预注册。**在任何代码之前提交**。
**触发**：使用者 2026-08-21：「**你之前的预期错了,那就再检查一下自己的计划,然后继续**」。
**前置**：[第一轮判定](20260822-0200-f1-radius-verdict.md)。

---

## 0. ★★★ 查计划查出四处站不住的地方

第一轮我两条预测都落空。**落空本身不改计划**，但**落空暴露的推理习惯要改**。
逐条查下来，**F1-b 的原计划有四处靠假设撑着**：

| # | 原计划里的假设 | ★ 为什么站不住 | 修正 |
|---|---|---|---|
| ★★★ **(a)** | "重钉那 **7 条**红的" | **只钉红的 = 只钉守卫看得见的。** 第一轮**自己测出** Track V 的数动到 **8.0e-04 而一条锁都没红** ⇒ **动了但没红的数会静悄悄变陈旧**（与第八轮 `TestAttraction` 硬编码种子**同型**） | **普查所有移动的已提交数**，不只红的 |
| ★★★ **(b)** | "`n = 11` 由 21 次推出" | **21 次是第四轮对 `φ*₁, φ*₂` 两个被积函数测的**，而**这张表被 30 个输出共用**（本轮已核：`closures.py:515-533` 只有**一个** `n_g` 循环）⇒ **其它被积函数的次数从没查过**；若有 23 次以上，**11 点仍然不够** | ★★ **不用推导定目标** —— **实测每个输出对 `n` 的自收敛**（8/11/14/20/30），**由数据定 `n`** |
| ★ **(c)** | "换点数只改精度" | 需确认没有把点数写死的算法 | ✓ **已核**：`n_g = ETA_LAM.shape[0]` **从数组读**，只有一个循环，**无写死** |
| ★★ **(d)** | 第一轮那三个移动量（1.4e-04 / 4.3e-05 / **8.0e-04**）是干净的 A/B | ★ **纪律 #12（GV6.1 实测）**：**numba 的 cache-load 对 fresh-compile 不逐位忠实,而那处不忠实整个住在 `pyfp3d/viscous/`**，量级 **~1e-5** ⇒ **我那次 A/B 一腿刚改完（fresh）、一腿 stash 回去（可能 cache-load）** ⇒ **4.3e-05 那个数与噪声只差一个量级** | **两腿都 fresh-compile**（腿间清 `pyfp3d/viscous/__pycache__`），重量 |

★ **(d) 不推翻第一轮的头条**：**8.0e-04 比 ~1e-5 的噪声高 80 倍**；★ 但**较小的那两个必须重量**，
而第一轮判定里我**没有**标注这条限制 —— **本轮补测并回填勘误指针，不改写原数**。

## 1. 判据

| 判定码 | 条件 | ★ 反向结果落在哪 |
|---|---|---|
| ★★★ **R-N** | 每个层流输出对 `n ∈ {8, 11, 14, 20, 30}` 的自收敛；**`n*` = 全部输出都收敛到 1e-12 相对的最小 `n`** | `n* > 11` ⇒ **我原来的目标就是错的**，按实测取 `n*`；`n* ≤ 11` ⇒ 目标确认 |
| ★★ **R-CENSUS** | 在 `n=8 → n*` 之间**普查**：`closure_node` 的 30 输出 + **所有 `test_v*` / `test_gs41_*` 里被断言的量**，报**移动 > 1e-9 的全部**，**不只红的** | 若有"动了但没红"的断言 ⇒ **逐条列出**（它们就是静悄悄的陈旧） |
| **R-FRESH** | R-CENSUS 的两腿**都 fresh-compile**（腿间清 `pyfp3d/viscous/__pycache__`），并**打印这一点** | 不做 ⇒ 小移动量**不可判** |
| **R-FIX** | 应用 `n*`、订正注释、**重钉**移动的断言、**重新 specify** 那 2 条审计锁（锁**新规则 + 21 次这个根因**） | —— |
| ★★★ **R-NOLOOSEN** | **零测试容差被放宽** —— 逐字比 `tests/` 的容差数字；**重钉是换成实测新值，不是放宽带** | 放宽任何一条 ⇒ **kill 条 1** |
| ★★ **R-ERRATA** | 勘误 grep **两轮**：① §R-CENSUS 报出的**每一个**移动值 across `docs/`；② 移动 > 1e-4 的量对应的**任何 ≥4 位有效数字**的已提交引用 | 逐条**改正或标注**，不得留 |

## 2. 守卫

**G-SENTINEL**（从模块 import 点数）、**G-SWAP**（`git stash` + pop 后哨兵）、
**G-ONECHANGE**、**G-FROZEN-IBL3**（`ibl3.py` 逐字节不动）、
★ **G-FRESH**（新）：A/B 两腿**都清 `pyfp3d/viscous/__pycache__` 后重编**，并打印"两腿均 fresh"。

## 3. Kill criterion

1. ★★★ 放宽任何测试容差 ⇒ 停。
2. ★★ `n*` 测出来 > 11 却仍按 11 改 ⇒ 停（那是按推导否决实测）。
3. `ibl3.py` 需要改 ⇒ 停并报。
4. ★ R-CENSUS 报出"动了但没红"的断言却不处理 ⇒ 停（那正是第一轮学到的东西）。
5. > 90 min ⇒ 停。

## 4. 事前预测

1. ★★ **`n* = 11`** —— 第四轮实测 `n=11` 给 7.99e-15。★ **可以落空**：那只是**两个**被积函数，
   而表被 **30 个输出**共用，**其余的次数从没查过**（这正是 (b)）。
2. ★★★ **R-CENSUS 会找出"动了但没红"的断言** —— 依据：第一轮已测 Track V 动到 8.0e-04 而零红。
   ★ **数量我不预测**（预测一个我没有依据的数就是编）。
3. ★ **R-FRESH 之后 `lam_apg` 的 4.3e-05 会变**（它与 ~1e-5 的 cache 噪声只差一个量级）；
   **8.0e-04 基本不变**。
4. 零新 FP 求解；成本 = 一次 n-扫 + 一次普查 + 一次全套件。
