# 20260822-1600 — **第七轮 附录 #1**：A-FIT 的**适定域**要事前注册（在下结论之前）

**性质**：预注册附录。**在对 A 部分下任何结论之前提交**。第一次执行的输出**保留不改写**。

---

## 1. 第一次执行看到的结构

| `H` | `c_l`（各 `Re_θ`） |
|---|---|
| 1.35 | **0.1679 / 0.2194**，另有 **2 个状态求解失败** |
| 1.45 | **0.2053**（Re 500）/ 0.0762 / 0.0704，另 1 个失败 |
| 1.60 | 0.0713 / 0.0693 / 0.0685，另 1 个失败 |
| 1.80 | 0.0675 / 0.0677 / 0.0679，另 1 个失败 |
| 2.20 | 0.0669 / 0.0674 / 0.0679 / **0.0681** |

**总散布 222 %**；★ 而 **`H ≥ 1.60` 全部落在 0.0669–0.0713（≈ 6.6 %）**。

## 2. ★★★ 限制适定域**有独立理由**，但**我没有事前注册** —— 现在补，且说明为什么它不是按结果选样本

**独立理由（不看 `c_l` 的值就成立）**：那些离群状态**紧邻求解直接失败的状态** ——
`brentq` 报 "f(a) and f(b) must have different signs"，即**型线族在那个 `(H, Re_θ)` 上根本达不到该 `H`**
（★ 与第一轮已记录的"型线族不动点 `H ≈ 2.708`、可达 `H` 区间受限"**同一现象**）。

⇒ **适定性判据（事前，不含 `c_l`）**：
> **只在"型线族能解出该 `(H, Re_θ)`，且其相邻 `Re_θ` 也能解出"的状态上比较。**

★★ **它为什么不是按结果选样本**：判据**只用求解是否成功**，**完全不看 `c_l` 的值**；
★ 而**证据是**：`H = 1.35 / 1.45` 那两行各有状态**直接失败**，`H ≥ 1.60` 也各有一个 `Re_θ = 1e4` 失败
⇒ **失败散布在整个表上**，不是只在离群处 —— **所以这条判据会同时排除一些"好"的点**，
**这正是它不是挑好数的证明**。

★★★ **两个数都报**：全域散布 **222 %** 与适定域散布 **≈6.6 %**，
**判定同时给出，不得只引用后者。**

## 3. 判据修订

| 判定码 | 修订 |
|---|---|
| **A-FIT** | ★ **两个都判**：**A-FIT(全域)** 与 **A-FIT(适定域)**，各自对 ≤10 % 的线 |
| ★★ **A-ADOPT** | **只有两者都成立才改值** —— ★ 若只有适定域成立，**不改值**，把**两个散布**一起写进注释取代"项目的选择" |

★ **这比原判据更严**：原来只有一条 A-FIT，现在要**两条都过**才允许改值。

## 4. 追加事前预测

5. ★★ **A-FIT(全域) 失败、A-FIT(适定域) 通过** ⇒ 按 §3 **不改值**。
6. ★ 适定域的中位数 `c_l ≈ 0.068`，即 **0.09 是它的 1.3 倍**；
   因 `Ctau_eq ∝ c_l²` ⇒ **CτEQ 高约 1.7 倍**。★ 这条**是读数不是判定**。
