# 20260822-0400 — **第五阶段第二轮判定（F1-b）**：F1 结清 —— 而**目标本身被实测换掉了**

**性质**：判定。判据、守卫、kill criterion 逐字来自
[预注册](20260822-0300-f1b-prereg.md)（**代码之前提交**，含使用者要求的"先查计划"）。

---

## 0. 一句话

**F1 已落地**：`closures.py` 的层流求积 **8 → 24 点**、注释订正、**7 条重钉 + 2 条重新 specify**、
**10 处勘误**、**零容差放宽**，套件 **572 / 12 / 2, 0 failed**。
★★★ **而本轮真正的产出是:目标从 `n=11` 换成 `n=24`,因为"次数 ⇒ 点数"这个框架被实测推翻。**

## 1. R-N：★★★ 被积函数**不是多项式**，所以**没有精确的 n**

两腿**都 fresh-compile**（每腿清 `pyfp3d/viscous/__pycache__`，纪律 #12），对 `n=48` 参照：

| `n` | 8 | 11 | 14 | 20 | **24** | 30 | 40 |
|---|---|---|---|---|---|---|---|
| 最大相对距离 | 7.9e-04 | 1.2e-05 | 3.6e-06 | 6.2e-07 | **2.5e-07** | 7.7e-08 | 1.2e-08 |
| 仍动 >1e-12 | 30/90 | 14/90 | 6/90 | 4/90 | 4/90 | 3/90 | 3/90 |

**多项式会在某个 `n` 精确;这个是代数衰减 ⇒ 它不是多项式。**

★★★ **元凶是单一个输出**，三个状态上完全一致（`n≥11` 起它一直是 binding）：

```python
sd_g = (R * wsig) ** 1.5      # OUT_SD;  wsig = 4 eta (1 - eta) 在两端取零
```

⇒ `w^{3/2}` 在**两个端点**有**平方根型奇异导数** ⇒ **高斯–勒让德只能代数收敛。**

★★ **所以第四轮"21 次 ⇒ n ≥ 11"对 `φ*₁, φ*₂` 是对的,对整张表是错的** ——
**这张表被 30 个输出共用**（本轮已核：`closures.py:515-533` 只有**一个** `n_g` 循环，
`n_g` 从数组读，**无写死**）。★ 而第四轮**自己记过** "Ψ 加权含 `arctan`、不是多项式" ——
**我的原计划无视了它自己的记录。**

## 2. 目标：`n = 24`，★ **明说它是选择不是推导**

| 依据 | |
|---|---|
| ① | 残差 **2.5e-07**，比**任何现有锁加在这张表所供量上的最紧带**（`rel = 1e-6`）低 **4 倍** ⇒ 未来落在那个带里的锁不会被求积扰动 |
| ② | **与 `ETA_TURB` 的 24 一致** ⇒ 消掉两张表"无理由不一致" |
| ③ | 成本可忽略（层流侧每节点一个 24 点循环） |

★★★ **`OUT_SD` 的结构性修法是换元吸收端点 `w^{3/2}`，不是加点数** ——
**只报不做**（与第四轮对它自己发现的缺陷**同一处置**）。**登记为 F4。**

## 3. R-CENSUS：**9/30 输出动 > 1e-9**，而 **Track V 零红**

| 输出 | 最大相对移动（8 → 24） |
|---|---|
| `OUT_PS1`（`φ*₁`） | **7.95e-04** |
| ★ `OUT_THS1`（`θ*₁ = φ*₁ − δ*₁`，**动能控制方程上那个**） | **6.46e-04** |
| `OUT_PS2`（`φ*₂`） | 1.01e-04 |
| `OUT_TC1` / `OUT_SD` / `OUT_TC2` | 1.01e-04 / 5.39e-05 / 4.82e-05 |
| `OUT_DC2` / `OUT_CDC` / `OUT_DQC` | 4.42e-06 / 5.0e-08 / 3.9e-09 |

★★ **`test_v*` 一条都没红** ⇒ **Track V 的锁带宽于 6.5e-04，看不见这个量级** ——
与第八轮"每条湍流锁的窗口都从 `Re_θ ≥ 800` 起、而改动在其下"**同型**。
★ 这条是**关于锁的发现**，不只是关于 F1 的。

## 4. R-FIX / R-NOLOOSEN

| | |
|---|---|
| 重钉 **7 条** | `A` 8.02881134 → **8.028406562**；`H` 2.708292 → **2.707931**；`c_f√Re_x` 0.710235 → **0.710301160**；楔 `m=0.0435` → **2.469240**、`m=1` → **2.087926**；对 Blasius 的差 0.04523 → **0.0450893** |
| 重新 specify **2 条**审计锁 | 从锁**旧规则**（"8 点"）改为锁**新规则 + 根因** —— ★ 新增的那条断言的是 **`(R*wsig)**1.5` 这个结构**，**不是误差大小** ⇒ **以后再改点数它照样成立**（第四轮那条锁的同一设计） |
| ★★★ **R-NOLOOSEN** | **逐字比过 `tests/` 的容差数字**：`rel=1e-6` / `abs=5e-5` / `abs=1e-4` **全部原样** ⇒ **零放宽**。★ 重钉是**换成实测新值**，不是放宽带 |

★★ **一处我自己抓住的错**：重钉时我在注释里写「`cf` 在这个精度上没动」——
**那是从失败清单推的，没量**。实测它动了 **+9.3e-05**；它的断言只是**没被执行到**
（前面 `A` 先失败）—— **b7/b9 那个"第一条失败的断言遮住后面"的陷阱**。
★ 已改正，**并把这句话写进注释**，因为下一个人会踩同一处。

## 5. R-ERRATA：**10 处**

| 面 | 处置 |
|---|---|
| **8 份轮次文件** | 顶部加**勘误指针**（列出六个移动值的新旧），★ **原数一个都不改写** |
| `PHASE_TWO_CAPABILITY_BOUNDARY.md` / `roadmap.md` | ★ **就地标注**（长寿面，不能只靠指针） |

★★ **每一处都写了"结论未变"**：全部移动 **≤ 1.3e-04**，而压在它们上面的是型线族对 Blasius 的
**4.5 %** 地板 ⇒ **"型线族留不住 Blasius"、"175.6×"、"家族性质不是实现缺陷"三条结论原样成立。**

## 6. 事前预测的兑现（**不改预测**）

| # | 预测 | 实测 | |
|---|---|---|---|
| 1 | ★★ `n* = 11` | ★★★ **落空** —— `n=11` 还有 **1.2e-05**、14/90 在动；**没有精确的 `n`** | **落空** |
| 2 | ★★★ R-CENSUS 会找出"动了但没红"的断言（**不预测数量**） | ★ **命中**：**9/30 输出动 > 1e-9、Track V 零红** | ✓ |
| 3 | ★ R-FRESH 之后 `lam_apg` 的 4.3e-05 会变、8.0e-04 基本不变 | ★ **命中**（`lam_fpg` 7.95e-04 稳住；`lam_apg` 那一档确实随两腿 fresh 而重排） | ✓ |
| 4 | 零新 FP 求解 | ✓（n-扫 7 腿 + 两次全套件） | ✓ |

⇒ 本季累计落空 **43 次**，一次都没有事后改预测。
★★ **预测 1 我事前写下了它怎么落空**：「那只是**两个**被积函数，而表被 **30 个输出**共用，
其余的次数从没查过」—— **落空正是照那条路径发生的**。
★★★ 而**它落空得好**：若我按 `n=11` 改完就走，`OUT_SD` 会留在 **1.2e-05**，
**而那正是被积函数根本不是多项式这件事的唯一线索**。

## 7. 登记给后续

| | |
|---|---|
| ★★ **F4（新）** | `OUT_SD` 的 `(4η(1−η))^{3/2}` 端点奇异 ⇒ **换元**，不是加点数。**只报不做** |
| ★★ **F5（新）** | **Track V 的锁带宽于 6.5e-04** ⇒ 它们看不见这一类求积/闭包改动。★ 与第八轮同型，**修法是给关键量加窄带锚，不是收紧所有带** |
| **F3 / F2** | 承下一轮（湍流侧 24 点同型检查 / `C_L` 的 G-beta 标定） |
| **M1** | 承本阶段（★ 入口是**亚单元位置仪器**，见[立项](20260822-0000-initiation.md) §5） |

## 8. 实测口径

- 套件 **572 passed + 12 skipped + 2 xfailed, 0 failed**（**895.80 s @8 线程, load 18.9**
  —— 忙机，**带负载引用不作成本**）；★ **571 + 1 = 572 恰好闭合**（新增的那条锁根因的锁）。
- ★ `ibl3.py` **逐字节未改**（G-FROZEN-IBL3）；`closures.py` 是**十七轮以来第一次**被改动，
  而立项 §6.6 事前要求的正是"把那条 `G-FROZEN` 换成会说明改了什么的守卫"，本轮照此执行。
