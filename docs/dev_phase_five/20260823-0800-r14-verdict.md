# 20260823-0800 — **第五阶段第十四轮判定（R14）**：`C = 1.0` 在 medium 上炸在**后缘/近尾迹**，不在激波 —— 而我的分带没盖住它

**性质**：判定。判据、守卫、kill criterion 逐字来自 [预注册](20260823-0700-r14-prereg.md)（**代码之前提交**）。
**`pyfp3d/` 零改动**；一个 medium 解 **@16 线程，load average 16.6**（带负载引用）。
证据 `bench/studies/r14_medium_coverage/results/`。

---

## 1. 测到的

| 判据 | 实测 | 判定 |
|---|---|---|
| **G-REPRO** | `n_limited` = **1135**，与被提交的完全一致；★ 而我**独立**用 `q2_at_mach` 判出的也是 **1135** | **PASS**（判别方法被交叉验证） |
| ★★★ **V-WHERE** | LE **0.0 %** · MID **0.0 %** · SHOCK **0.0 %** · TE **84.6 %**；峰值 `M²` 在 **x/c 0.9965** | ★ **按注册判 UNDEFINED**（两种模式都不占）—— 而**测到的东西很清楚：后缘** |
| **V-CL** | 该钳制态 `cl_p` **0.85795**、`x_shock` **0.9787** | RECORDED，**不得使用**（收敛态是 0.34134 ⇒ 钳制态的数是收敛值的 **2.5 倍**） |
| **V-READ** | §0 三句全部成立 | PASS（事前已声明"几乎是推论"） |
| G-SCOPE / G-RECIPE / G-THREADS / G-CACHE / G-CLAMP | 全过 | **PASS** |

## 2. ★★ 我的分带**没有覆盖被测量的域** —— 15.4 % 落在所有带之外

四条带止于 `x/c = 1.01`，而**被限幅单元有 175 个（15.4 %）在 1.01 之后** —— 在**尾迹里**。
所以四条带加起来只有 84.6 %，**不是 100 %**。

⇒ **这正是本阶段登记的那一族**：**判据没有覆盖它所比较的那个量的定义域**。
★ 而它是**我为"激波"设计的分带**，于是答案落在我随手放在最远端的那一条上，**外加一截根本没有带**。

## 3. ★★★ 事后描述（**未注册**，如实标注）：那是一团**贴在后缘、伸进尾迹的体结构**

| | |
|---|---|
| `x/c` 分位数（0/5/50/95/100 %） | **0.810 / 0.887 / 0.954 / 1.056 / 1.166** |
| **贴壁的比例** | ★★ **52 / 1135 = 4.6 %** ⇒ **这不是表面现象，是体内结构** |
| \|y\| 分位数（50/95/100 %） | **0.077 / 0.187 / 0.370**（翼厚 0.12 c ⇒ \|y\| ≤ 0.06 才算贴面） |
| `x/c ∈ [0.95, 1.05]` 的比例 | **47.7 %** —— **横跨后缘** |

⇒ **在 `upwind_c = 1.0` 下，人工密度限幅器在 medium 上最先饱和的地方是近尾迹/后缘区**，
而**不是**激波（SHOCK 带 **0.0 %**）。

## 4. 于是 (c) 下端失败的**归因换了**

★★ **不成立**：「`C = 1.0` 上风量不足**以解析激波**」—— SHOCK 带一个单元都没有。
★★★ **成立**：**后缘/近尾迹区是"人工耗散不足"最先失效的地方。**

★ 机理上说得通而**本轮未测**（如实标注为假设）：尾迹割缝是一条 φ 跳 Γ 的缝，
它的**前端点就是后缘**；上风量不足时该处速度先失控。这与项目已记的
**P13 翼尖自由边**（三维）、**Track V 的 TE 带 (B, δ) 地板**是同一族的"自由边/尾缘"问题，
**而本轮是 2.5-D，没有翼尖** ⇒ **翼尖那条解释在这里被构造性排除**。

★ **kill 条 5 生效：本轮不重判、不重新规定 (c)。** 只报事实：
**它的下端在 medium 上失败，而失败位置是后缘/尾迹，不是激波。**

## 5. ★ 两处我自己的过失

1. ★★ **kill 条 3（> 20 min 停）被超过而脚本没有实现它**：解跑了 **1285.0 s = 21.4 min**。
   我**注册了一条时间 kill 条，却没有把它写进代码**。
   ⇒ **与"注册逐站、实现中位数"、"注册对的勘误清单、执行短的那个"同族：注册了 A、实现了 A 的弱版本或没实现。**
   ⇒ **常设：kill criterion 里每一条**可机械检查**的，必须在脚本里有一行对应的检查。**
2. ★ 写脚本时抓到的两处（在花机时之前，已记在脚本注释里）：`m_cap` 封的是**局部马赫 `M`**
   而我草稿拿 `M²` 去比它；键名 `m_cap_value` 不存在。**两者都会产出看似合理的无意义直方图。**
   改成用限幅器**自己的** `q2_at_mach` 判别后，计数与求解器**逐一相符（1135 = 1135）**。

## 6. 事前预测的兑现（**不改预测**）

| # | 预测 | 实测 | |
|---|---|---|---|
| 1 | ★★ SHOCK + MID ≥ 70 %，LE ≈ 0（依据 CLAUDE.md 的先例 81 % MID / 峰值 0.75） | **TE 84.6 %，SHOCK 0.0 %** | **落空** |
| 2 | ★ 峰值落在 x/c 0.6–0.8 | **0.9965** | **落空** |
| 3 | V-READ 三句成立（事前标注"几乎是推论"） | 成立 | ✓ |
| 4 | 一个 medium 解 ≈ 400–600 s | **1285 s** | **落空**（2.1–3.2×） |

⇒ 本季累计落空 **60.5 次**。
★★ **预测 1 的落空方式我事前写下来了**：「那条先例是 `n_picard_seed` 缺省失败（`C` 未变），本轮变的是 `C`」——
**落空正是沿这条路径发生的**，而我仍然把预测押在了那条先例上。
★★★ **它落空得好**：若真落在 SHOCK 带，我会写下"`C=1.0` 不够解析激波"，而那**是错的**。

## 7. 禁止句
① **不得**说「`C=1.0` 上风量不足以解析激波」—— SHOCK 带 0 个单元；
② **不得**引用该钳制态的 `cl_p 0.858` / `x_shock 0.979`（GS1.4）；
③ **不得**把"后缘/尾迹"写成已证的**机理** —— 本轮只测到**位置**，机理是假设；
④ **不得**据本轮重判 M1 的 (c)。
