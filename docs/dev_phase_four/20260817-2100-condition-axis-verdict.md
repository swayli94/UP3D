# 20260817-2100 — 判定：**W-DRIFT** —— `k*=1` **不跨工况**（我的主预测被推翻）

**预注册**：[20260817-1900](20260817-1900-condition-axis-prereg.md)（动代码前提交 `56469cb`）。
**产物**：`bench/gate_results/gs40f_condition_axis.csv` + `.json`。**349.0 s**（闸 1 500 s）。

**判定：W-DRIFT —— 内点极大处处存在，但 `k*` 不全是 1（4/5 在 P1，M0.82 在 P2）。**
**⇒ 上一轮 K-FIXED 的可迁移性，只能声称到「跨级别」，不能声称到「跨工况」。**

---

## 1. 读数

| 腿 | M | α | 墙钟 | sup_frac | M_max | C(P0) | C(P1) | C(P2) | C(P3) | **k\*** |
|---|---|---|---|---|---|---|---|---|---|---|
| M0.78 | 0.78 | 3.06 | 10.2 s | 0.0120 | 1.465 | 0.057 | **0.143** | 0.134 | 0.030 | **P1** |
| **M0.82** | 0.82 | 3.06 | 72.1 s | 0.0475 | 1.591 | 0.114 | 0.281 | **0.291** | 0.154 | ★ **P2** |
| M0.86 | 0.86 | 3.06 | 77.5 s | 0.1090 | 1.738 | 0.243 | **0.457** | 0.301 | 0.222 | **P1** |
| ANCHOR | 0.88 | 3.06 | (缓存) | 0.1540 | 1.864 | 0.221 | **0.479** | 0.396 | 0.291 | **P1** |
| A1.53 | 0.88 | 1.53 | 28.5 s | 0.1381 | 1.339 | 0.208 | **0.419** | 0.309 | 0.247 | **P1** |

**5 条计入腿全部有内点极大；4 条在 P1，M0.82 在 P2 ⇒ W-DRIFT。**

★★ **前提检查通过，而且通过得很稳**：超声速占比跨腿 **0.0120 → 0.1540 = 12.81×**
（判据要 ≥2×）⇒ **工况确实变过**，所以这是一次**真的**跨工况检验，不是白送的。

### 1.1 ★ 那条"漂移"有多大：**3.6 %**

M0.82 上 `C(P1) = 0.281` vs `C(P2) = 0.291` —— 差 **3.6 %**。

★★ **我按注册判 W-DRIFT，不重读。** 我的并列规则是"取最小的 k"，但那是给**并列**用的，
0.281 与 0.291 在 6 位小数上不是并列。**规则怎么写就怎么判。**
★ 但**如实说清楚它有多弱**：这是**五条腿里的一条、一格、3.6 % 的余量**。
把它转述成「`k*` 随工况移动」而不带这个余量，就是**夸大**。
⇒ 正确的措辞是：**`k* ∈ {1, 2}`，压倒性地在 1，在一条中等激波强度的腿上以 3.6 % 翻到 2。**

### 1.2 A4.59 不收敛 —— 而它**不是**能力上限

`accept_reason=None`、**clamps 0/0**、`gmres_stalled=0`、`m_last_converged=0.825`、
`target_reached=False`，残差尾部 **0.00171 → 0.00138 → 0.00103 单调下降**。

★★ 按 CLAUDE.md 的分型表，**单调下降的尾部 = `budget_limited`（还在降，迭代用完了），
"A budget_limited leg is NOT a failure"** —— 把它读成能力上限是**项目已记录过的、
曾经撤回一整个"死区"结论的错误**。
⇒ **本轮不宣称 α=4.59 是上限**；它只是**没跑完**，而本轮**不加预算**（kill 条 3 的同一精神：
看到结果之后加资源就是范围蔓延）。

---

## 2. 事前预测的核对（**不改预测**）

| # | 预测 | 结果 |
|---|---|---|
| 1 | ★ **W-FIXED** | ★★ **被推翻** —— 实测 W-DRIFT |
| 2 | M0.78 可能被 EXCLUDED-SUBCRITICAL | ★ **落空** —— 实测 **1 062** 个跨声速面，远在 100 之上，计入 |
| 3 | α=4.59/M0.88 可能不收敛 | ★ **命中**（但见 §1.2：是 `budget_limited` 不是上限） |
| 4 | 强度前提成立（≥2×） | ★ **命中**（**12.81×**） |
| 5 | < 10 min | **命中**（349 s = 5.8 min） |

★ 本季第五次预测落空，**照实记，不改预测**。

---

## 3. RECORDED（事后读数，**不参与判定**）

### 3.1 ★★★ `C` 本身与激波强度**强烈共变** —— 这是对**度量**的一条警告

| | M0.78 | M0.82 | M0.86 | M0.88 | A1.53 |
|---|---|---|---|---|---|
| sup_frac | 0.0120 | 0.0475 | 0.1090 | 0.1540 | 0.1381 |
| C(P1) | 0.143 | 0.281 | 0.457 | 0.479 | 0.419 |

**C 随超声速占比几乎单调**（五点全对上）。
⇒ ★★ **跨工况直接比 `C` 的绝对值是被混淆的** —— 它同时在量"算子多好"和"激波有多少"。
本轮的判据**没有踩这个坑**（判的是 `C(k)` 的**形状**，形状在每条腿**内部**成立），
但**任何未来引用 C 绝对值跨工况的说法都会踩**。**记下来。**

### 3.2 ★★ 而**相对增益**在 12.8× 的强度跨度上近乎不变

| | M0.78 | M0.82 | M0.86 | M0.88 | A1.53 |
|---|---|---|---|---|---|
| **C(P1)/C(P0)** | 2.51 | 2.46 | 1.88 | 2.17 | 2.01 |
| **\|C_P1−C_S1\|/C_S1** | 1.38 | 1.30 | 2.57 | 1.66 | 1.38 |

⇒ **小 patch 重拟合相对控制臂稳定给出 ~2×，相对纯平滑给出 1.3–2.6×，
横跨 12.8× 的激波强度。** ★ 这是**事后**读数（未事前登记）⇒ **只作 RECORDED**，
但它指出：**下一轮若要跨工况比较，比的应当是比值不是绝对值**（§3.1 的直接推论）。

---

## 4. 结论：把可迁移性的声称**收回到测到的范围**

**已建立**：
1. **每一个测过的工况上都存在内点最优**（5/5），且**强度跨度 12.81×**；
2. **`k*` 压倒性在 1**（4/5），例外是一条 **3.6 % 余量**的腿；
3. **相对增益跨工况稳**（vs 控制 ~2×，vs 纯平滑 1.3–2.6×）。

**不再能声称**（这是本轮相对上一轮的**净变化**）：
- ~~「`k*=1` 是可迁移的拓扑规则」~~ ⇒ **只在跨级别上成立**（20× 单元数，上一轮，半径/h 常数到三位）；
  **跨工况上它是"通常是 1"，不是"总是 1"。**

**仍然不能声称**（四条禁止句照旧）：
必要非充分 · 零求解对求解器内部零发言权 · **相干 ≠ 正确** ·
★ **几何仍是一族**（翼身 conforming coarse）—— **几何维度未测**。

★ **仍不撤销**「共同修法 = 体内梯度重构」的退役。

## 5. 一句话

**我预测 `k*` 跨工况不动，它动了 —— 五条腿里一条、一格、3.6 % 的余量；
前提检查确认工况真的变了 12.8 倍，所以这是一次真的检验而不是白送的通过。
可迁移性的声称因此收回到"跨级别"，而相对增益反倒在同样的跨度上稳住了 ~2×。**
