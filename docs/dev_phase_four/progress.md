# 第四阶段开发历史（每轮一行）

规则同 phase two / three：每轮一行，叙事写在轮次文件里，判定只有
**PASS / FAIL / RECORDED / UNDEFINED**（收敛腿 < 2 报 UNDEFINED，不报方向）。
轮次文件格式见 [../dev_phase_two/_TEMPLATE.md](../dev_phase_two/_TEMPLATE.md)。

★★★ **本阶段的入口文件 = [../dev_phase_three/20260816-1000-gs41-initiation.md](../dev_phase_three/20260816-1000-gs41-initiation.md)**
—— **链接，不复制**（该文件 §0 自己定下这条维护约定：复制出来的入口会和原件分叉）。
开工前先读它的 §0：交接基线、裁决 D7 分派过来的项、六条不许静默改掉的事项、
以及 GS4.1 必须带的两条已测结论。

**计划与裁决（D1–D7）**仍在 [../dev_phase_two/roadmap.md](../dev_phase_two/roadmap.md)。

---

## 使用者定的顺序

**GS4.0（本阶段第一件事）✓ 已收口 → ★ GS4.1 已开工（2026-08-18，使用者裁决）。**

★ GS4.0 的账已全清（审计 §7 六条 + R1–R4），全门控 **509/1/3/1 零失败**，入口文书两个基线已更正 ⇒ **GS4.1 无已知阻碍**。PR: `claude/gs40-audit-fixes` → #28。

GS4.0 = 2026-08-16 独立审计
（[../inspection/20260816-2200-independent-audit-zh.md](../inspection/20260816-2200-independent-audit-zh.md)）
§7 查出的六条**活缺陷**。使用者裁决（2026-08-16）：先解决，再看要不要开 GS4.1。

★ **命名说明**：门禁编号约定是 `GS<阶段>.<序号>`，而 S4 是「粘性重启」。GS4.0 里
**没有一条是粘性工作** —— 它是 S4 的**进入前置**（同 S0「地基与基线」之于 S1 的关系）。
按 §0 的读法：**GS4.0 = 「在开 S4 之前把仪器修好」**，不是 S4 的一个粘性子门。
这条写下来免得将来有人按 ID 推断内容 —— 本项目已记过多次「名字不是作用域」。

| # | 日期 | 轮次文件 | 内容 | 一句话结论 | 判定 | 测试状态 |
|---|---|---|---|---|---|---|
| 1 | 2026-08-16 | [20260816-2300-gs40-prereg.md](20260816-2300-gs40-prereg.md) | **GS4.0 预注册**（动代码前提交） | 六条缺陷的判据 + kill criterion + 明确的范围排除；★ 事前用新提的**第八问**（判据在 n=1 / 全通过 / 全失败 / 空集上给什么）过了一遍自己的判据，**当场抓到两处构造性通过**（空的 `NOT_COVERED`、空的 `level_results`），两处都写进了判据 | RECORDED（预注册） | 未动代码 |
| 2 | 2026-08-17 | [20260817-0100-gs40-verdict.md](20260817-0100-gs40-verdict.md) | **GS4.0 执行**：审计 §7 的六条活缺陷 | ★★ **F1**：`m_last_converged`/`m_final`/`target_reached` **只由已删除的 level-set 驱动器提供**（G-S 源码比对 `d224223:newton_ls.py:1186-1193`），保形侧**从来没有过** ⇒ `run_capability_matrix.py` 的 `MACH_NOT_ATTAINED` 守卫 `abs(m_att - m)` **恒为 0、永远不可能开火**，而它的注释写着它存在正是为了不信任驱动器标志位。补 `newton.py::_ramp_honesty_fields`（纯读判、不回喂求解），三个消费者改为**无默认值地读**；★ 记录一处与原实现的差异（保形爬坡**插入重试级**⇒`levels[i-1]` 失义，改从 `level_results` 派生，等价性有理由且已锁）。★★ **根因是删除路线时的账在 `tests/` 上对平（457→457），而它在原理上看不到 `bench/`** —— 已写进 CLAUDE.md 作为常设一条。★ **F2**：重读两轮判定后**登记删除条件而不删** —— 软集合路线**没被判死**（"机理靶子对、这一版参数化不可采纳"，kill 四条全不成立），与被 K3 判死的 `capture_select` **不同类** ⇒ 原则 4 在这里生效的是前半句，而那句话从来没写过；三条到期条件已写在旗标旁。★★★ **F3**：路径校验**先验证会红**（G-V 抓到 `tests/test_b18_wingbody_transonic.py`）再修；而**我第一版守卫 `checked >= len(LOCKS)` 在空表上是 `0>=0` 的空洞通过，被本轮自己的极端样本测试在上线前抓到** ⇒ 第八问在被提议的同一轮还清了成本。★ **F4** README 重写（不再把冻结的 phase-1 roadmap 称 active；三条承重环境要求就位；9 个链接逐条验证）。★ **F5** 两处过期文字已改，`post/unified.py` 去留**按 kill 条 5 不自行裁决**。★★★ **F6** `mesh/manifest.py` 接进 `write_mesh` ⇒ 10 个生成器免费获得；sidecar **tracked 而 `.msh` 仍 ignored**（实测 `git check-ignore`）；F6-c 对着真实缺陷回放通过（round 68 624 vs flat 55 531 tets、wall 6 890 vs 5 152），**且三个数逐位复现 phase 3 已记录值 = 计划外跨轮核对通过**；20 张网格 2.9 s 全部盖章，★★ **第一次使用就查出 20 个文件只有 17 张网格**（三对同 sha 不同名，`md5sum` 独立复核；9 303/59 359/448 197 正是 GS4.1 成本表的 S0/S2/S4 ⇒ phase 3 的 S 阶梯与 `onera_m6_roundtip` 是同一批网格），★ 两种解释未分离故**不作归因**。★ 事前预测**③ 落空**（我写 N≈10–14，实测 **20**），如实记不改预测 | **F1–F6 全 PASS；G-B/G-N/G-D/G-S/G-V/G-SELF 全过；kill 五条全不成立** | 常开 **499 / 12 / 2, 0 failed**（477.27 s @8 线程；**+20 = `tests/test_gs40_provenance.py`，skipped 与 xfailed 一个没动**；改动前独立复跑 479 基线 472.99 s 逐项吻合）；快层 **5/5 绿 553 s @8t, load 2.3**；总机时 ~25.4 min |

| 2b | 2026-08-17 | 同上 §5 附录 | **R1 结清**（使用者裁决：收尾时一并做掉） | `capability_matrix.csv` 表头 **19 列** vs 脚本 **24 列**，而表头只在文件不存在时写 ⇒ 下次运行会把 24 字段写在 19 字段表头下。★ **拒绝而不自动修复**：改表头 = 就地改写已提交证据（G3 类），静默开新文件 = 把一次测量劈成两个互不指向的产物 ⇒ 报错 + 点名那 5 个新列 + 给操作者两条出路。★ **先在真 CSV 上见红再写锁**（同 F3 的 G-V 纪律），同一测试再验好路径未被误伤。★ 归类：**第 5 问的又一层** —— 不是"两个**数**是不是同一个东西"，是"两个**行**是不是同一个 schema" | **PASS** | 常开 **500 / 12 / 2, 0 failed**（465.24 s @8t；499 + 1；库改动仍为 0，只碰 `bench/`）|
| 3 | 2026-08-17 | [20260817-0300-gradient-probe-prereg.md](20260817-0300-gradient-probe-prereg.md) | **登记项 1 探针预注册**（动代码前提交） | 使用者裁决 2026-08-17:「**我要修根因**」⇒ 走登记项 1。★ **零求解判决性探针**：固定一个已收敛的 φ（`le14_cache/rc0.05.npz`，生产 taper、`conv=True`、0/0 clamps；网格由 GS4.0 的 manifest 钉住），**唯一自变量是恢复算子** ⇒ 本项目在网格侧一直造不出来的那种干净单变量实验，在这里是**构造性**干净的。四条臂 A(现状常梯度) / B1(线性 patch LSQ) / B2(二次 patch LSQ) / ★★ **S(纯平滑，故意的伪解)** —— S 臂是设计核心：阻塞性结论说薄片与激波的**局部** signature 是同一个东西，所以任何一起抹平的算子都会让重尾变小，**若 B 与 S 分不开就是平滑不是重构**。判据按分区相对本轮控制臂（**不用** 0.219 那个**跨算例**的老数 = 第 6 问），分区**在 A 上算一次四臂共用**（公共点集）。★ 第八问事前抓到一处**构造性不可达**（SHOCK 分区为空 ⇒ G-REAL 不可能成立），已写进处置。★★★ **kill 条 1：判 G-NULL/G-SMEAR ⇒ 路线死，能力边界 §3.2 那句「共同修法 = 体内梯度重构」退役** —— 那是一个被当作前提用了三周、从没被检验过的判断。★ **禁止句**：本轮任何结果**不得**写成「体内梯度重构可行」—— 后处理探针只是**必要条件**，不是充分条件 | RECORDED（预注册） | 未动代码 |

| 4 | 2026-08-17 | [附录 #1](20260817-0400-gradient-probe-addendum1.md) + [附录 #2](20260817-0500-gradient-probe-addendum2.md) → [判定](20260817-0700-gradient-probe-verdict.md) | **登记项 1 探针执行**（零求解，60.1 s） | ★★★ **G-SMEAR ⇒ kill 条 1 开火 ⇒「共同修法 = 体内梯度重构」退役为"未建立"**。**B1: R(SLIVER) 0.251×，而与纯平滑 S 的差只有 6.6 %**（注册线 20 %）。★ **三条独立读数同向**：① 判据本身；② 算子直比 —— `mean\|g_B1−g_S\|` 只有 `mean\|g_B1−g_A\|` 的 **17.5 %**，而 `mean\|g_B1−g_B0\|` 是 **61.9 %** ⇒ **换 patch 比换方法多动 3.5 倍**（同时回答了"判别子会不会按构造分不开"= **不会**）；③ 声速集相干性 B1 反而更碎（0.191→0.110）。★★ **SHOCK 分区为空**（18 746 跨声速面 / 6 273 分量 / 中位 **2** / 最大 **47** / 0 个 ≥50）⇒ **G-REAL 从未可测，本轮不能说"激波保住了"**；★ 最大 47 对我事前的 50 **贴得很近，我不改线** —— 而分布本身不贴（81 % 的面在 <10 的碎片里）。★★ 这条本身**独立量化了阻塞性结论的机理**：逐单元常马赫给出的 M=1 集合**根本不是一张面**。★★★ **一条方向相反的 RECORDED，而它指出我挑错了主臂**：**小 patch（面邻居）重拟合 B0 使声速集显著更相干**（≥10 面份额 **0.191→0.428**、最大 **47→118**、出现 **8** 个 ≥50 分量，控制臂 0 个），而它重尾降幅最小（0.476×）—— 两者一致：**平滑得少、锐化得多**。⇒ **判据缺陷 #11（新形状）**：附录 #1 为了让 B2 可解把主臂 patch 从面邻居换成 1-ring，而 **B2 随后没过自己的守卫、根本没跑** ⇒ 主臂落在一个"为了一条已不存在的臂而选的" patch 上 ⇒ **常设新增：附录为 A 改动 B 时，必须写下"若 A 失效，B 是否回滚"**。★ **不因此重判**（B0 是事后未注册读数，扶正它就是事后换主臂）⇒ 它是**重开条件**。★ 事前预测：**① 落空**（我预测 G-REAL）、**② UNMEASURABLE**（附录 #2 已预登记）、★★ **③ 命中 —— 「S 会与 B1 接近，这是最可能的坏消息」正是发生的事**、④ 命中。★ 干跑校验在花机时前抓到 **3 件事**：B0 两个原点（真 bug）、B2 点数≠秩（真 bug）、B2 门槛低于浮点地板（**判据缺陷 #10**：`max(cond)·eps = 1.6e-7` vs 我写的 1e-12 ⇒ **容差必须从条件数推出来，不能挑整数**）⇒ **B2 移出计分臂而不放宽门槛** | **G-SMEAR；kill 条 1 已执行；G-Z/G-MESH/G-A/G-P/G-SELF 全过；G-CONS B1/B0 过、B2 失败且不计分** | 零求解，60.1 s（闸 1 200 s）；库零改动；`gs40b_gradient_probe.csv` + `.json` |

| 5 | 2026-08-17 | [20260817-0900-coherence-ladder-prereg.md](20260817-0900-coherence-ladder-prereg.md) | **重开测量的预注册**（动代码前提交） | 上一轮的**失败模式变成本轮的靶子**：SHOCK 分区为空 ⇒ **本轮的可交付成果是把那个测试重新变得可做**（次判据 D-USABLE = 至少 1 个 ≥50 面分量，★ **沿用上一轮注册的 50，不放宽、不降到 47**）。★ 轴 = **面邻接跳数 k ∈ {0,1,2,3}**，**方法固定为线性重拟合** ⇒ 上一轮测出"换 patch 比换方法多动 3.5 倍"，所以本轮把 patch 尺度做成**唯一自变量**；N1（节点 1-ring）作跨定义对照，**S1（P1 patch 上求平均）逐尺度保留纯平滑对照**。★★ **主判据是 C 在 k 上的形状不是任何绝对值**（C-PEAK 需内点极大 ≥1.3×两端）—— 形状**自归一**，避开本季栽过的**绝对阈值**（#3）与**跨工况定带**（#7）；★ **反向结果说得很清楚**：**C-MONO-DOWN ⇒ 上一轮那条 B0 读数是伪影、本线关闭**，而它是完全可能拿到的结果。★★★ **样本独立性很弱，已写死**：盘上 5 个收敛态**全在同一张网格**，可用的 3 个**只差 taper 半径** ⇒「样本外」= **另一个 taper 半径不是另一个工况**，且 **binding 恰好 2 条 = 常设最小值**，任一条触发第八问即整轮 UNDEFINED；另 2 个态**因 npz 缺 `nlim/nflr` 无法满足钳制断言而登记为不可用**（记理由，不静默丢弃）。★ **G-CONS 门槛改成从条件数推出**（`max(1e-12, 10·κ·eps)`，κ 实测打印）= **判据缺陷 #10 的修法**；新增 **G-REPRO**：P1 必须复现上一轮 B0 的数（13 295 / 118 / 0.428），对不上即停。★ **判据缺陷 #11 的新常设条已应用**：patch 阶梯**只为主问题而选，不依赖任何其他臂**，某条臂死掉也不回滚。★ 三条**禁止句**：必要非充分、零求解对"放进求解器"零发言权、★ **新增「相干 ≠ 正确」**（无声速面外部真值）。★ 事前预测 **③ 是最有价值的可能坏消息**：我预测小 patch 上 S1 与 P1 **分得开**（与上一轮相反）—— 若仍分不开，则"重构"在**任何尺度上都只是平滑** | RECORDED（预注册） | 未动代码 |

| 6 | 2026-08-17 | [判定](20260817-1100-coherence-ladder-verdict.md) | **patch 阶梯执行**（零求解，302.5 s） | ★★★ **C-PEAK（两条 binding 腿一致）+ D-USABLE 达成**。`C(P0..P3)` = **0.203/0.413/0.303/0.201**（rc0.025）与 **0.193/0.431/0.298/0.205**（rc0.045）⇒ **内点极大在 k=1，是两端的 2.03×/2.10×**（判据要 ≥1.30×），三个态同一形状。★★ **D-USABLE**：P1 给出 **11 个 ≥50 面分量、最大 120** ⇒ **上一轮空掉的 SHOCK 分区非空，那条不可测的判据第一次可测**；★ **50 这条线一个数没动**（上一轮实测最大 47，拒绝降线；本轮直接到 120）。★★★ **预测 ③ 落在好的一侧**：`|C_P1−C_S1|/C_S1` = **1.854/1.877** ⇒ 同一 patch 上**重拟合的相干性是求平均的 2.85 倍** —— 与上一轮大 patch 上"分不开"相反，所以"最有价值的可能坏消息"没有发生。★★★ **与上一轮的关系：G-SMEAR 不被推翻，而是被解释了** —— 上一轮的主臂是**节点 1-ring = 本轮的 N1**，而 N1 实测是**整条阶梯最差的臂**（C 0.079–0.092，**比控制臂还差一半**）⇒ **上一轮把主臂放在了阶梯最平滑的一端**，正是当时自查的**判据缺陷 #11**，而**本轮把它的代价量化了：C 从 0.43 掉到 0.09**。★ **退役不撤销**（预注册事前写死）：「共同修法」仍为"未建立"，本轮开的是**一条新的、有具体形状的候选（k=1 小 patch 线性重拟合）**。★ **G-REPRO 精确通过**（13295/13295、118/118、0.4281/0.428）⇒ 上一轮那条读数不是仪器噪声；**G-CONS 门槛由实测 κ=11.9 推出**（缺陷 #10 的修法，不再挑整数）；**G-A 构造性通过**（k=0 的重拟合 = 内核，四节点唯一确定线性函数）。★ **RECORDED 三条**：最大分量是**条带状不是面状**（λ2/λ1 0.12）⇒ **相干 ≠ 正确**，位置无外部真值；预注册说「N1 在 P1 与 P2 之间」**描述错了**（实测 32.4 > P3 的 24.6，在最上端），不影响判据但照改；跨声速面**总数**随 patch 单调下降（18.7k→9.8k）⇒ P1 用**更少**的面拿到**更高**的份额 = 锐化而非保留噪声。★ 预测 **①③④ 命中、② 半中**（"只在 P1"不精确：P3 在两个态上各刮到 1 个 ≥50 分量），如实记不改措辞 | **C-PEAK + D-USABLE；六条守卫全过；kill 六条全不成立** | 零求解，302.5 s（闸 1 500 s）；库零改动；`gs40c_coherence_ladder.csv` + `.json` |

| 7 | 2026-08-17 | `bench/run_gs40d_level_cost_probe.py` + `gs40d_level_cost.csv` | **成本探针**（无判据、无判定） | ★★★ **我把一条选项叫"贵"却从没量过它 —— 实测 16.6 秒。** xcoarse **3.3 s** / coarse **13.3 s**，两级都 `converged`、**0/0 clamps**、`target_reached=True`、9 个爬坡级，φ 已落盘 ⇒ **下一轮真·零求解**。★ 这是 phase 3 第 21 轮记过的同一个错（把一条几乎免费的义务按一个**编造的** 4 011 s 推迟了四轮），也是 GS4.1 §7.6 的常设条「**一笔债在被转述第二次之前先花零机时定下成本**」—— **我刚犯了它，代价是一次错误的排序建议**。★ 顺带：这是 **GS4.0 诚实字段的第一个外部消费者**，读数正确（`m_final` 0.88 / `m_last_converged` 0.88 / `target_reached` True）—— GS4.0 之前这两个键**在这条驱动器上根本不存在**，调用方会静默拿回请求值。★ RECORDED 不追：两个新态都报 `frozen_in_transient`（phase 3 已测 τ=1e-3 上触发率 100 % ⇒ 是预期签名不是新闻），但**下一轮的每个态都带着它** | RECORDED（成本，无判据） | 库零改动；16.6 s |
| 8 | 2026-08-17 | [20260817-1500-level-ladder-prereg.md](20260817-1500-level-ladder-prereg.md) | **跨级别阶梯的预注册**（动代码前提交） | ★★ 问一个比"多加样本"**更锋利**的问题：**`k*` 是跟着网格走的长度尺度，还是钉在 k=1 的拓扑性质？** 三级跨 **33 459 → 90 099 → 679 391 tets = 20×**，同工况同几何同配方，**只变 h**。★ **K-FIXED**（三级都 k\*=1）⇒ 最优是**拓扑**的、physical 半径随 h 缩 ⇒ **可迁移**；**K-DRIFT** ⇒ 它是个长度尺度，"k=1 最优"只是这张网格的巧合；★★★ **K-VANISH（任一 binding 腿没有内点极大）⇒ 上一轮的发现是网格特有的、候选死**（kill 条 1）—— **这是本轮完全可能拿到的结果**。★ **阈值一个没新挑**：1.30× 与 1.15 **逐字沿用上一轮已注册的数**（第 6 问：定带的参考数必须同源）。★ **binding = xcoarse + coarse**（新级别），**medium 只作复现锚点不进 binding**（我已经看过它）。★ 新增 **G-RECIPE 源码比对**（两个新态的配方须与 `run_le14_common_root` 逐字一致，不靠记忆）+ **G-REPRO**（medium 必须复现 C = 0.191/0.428/0.284/0.214 到 ±0.001）。★ **第八问事前点名 xcoarse 最可能撞 UNDEFINED**（33 k tets 上跨声速面可能 <100），并事前写死：**若 binding 只剩一条，整轮 UNDEFINED，我不会把 medium 拉进 binding**。★ `k*` 并列时**取最小的 k**，事前定死，避免事后挑对自己有利的。★ **第四条禁止句新增**：「k\*=1 在三级成立」**不等于**「在别的工况成立」—— 三级同一工况，**工况维度仍未测** | RECORDED（预注册） | 未动代码 |

| 9 | 2026-08-17 | [判定](20260817-1700-level-ladder-verdict.md) | **跨级别阶梯执行**（真·零求解，217.2 s） | ★★★ **K-FIXED：跨 20× 单元数、3× 的 h，内点极大一次都没动，永远在 k=1**（xcoarse 0.168/**0.467**/0.380/0.250、coarse 0.221/**0.479**/0.396/0.291、medium 0.191/**0.428**/0.284/0.214；峰/端 1.87×/1.64×/2.00×）。★★★ **事前登记的判别量落得毫不含糊**：`k*` 处的 **physical 半径/h = 0.723 / 0.723 / 0.724** —— **三位数字上是常数** ⇒ 预注册写的"若不是常数，拓扑读法要弱化"**不必弱化，它被直接量到了** ⇒ **最优 patch 是拓扑规则（单元+面邻居），不是待标定的长度**。★★ 这一条值得单独说：项目反复记过「**任何固定阈值都是标定不是保证**」（EW forcing / taper r_c / descent10 / 墙钟 / eps 甜点），而 **`k*=1` 是很久以来第一个测出来「不是标定」的量** —— 换网格不用重新校。★ 守卫全过：**G-RECIPE 源码比对 9 个配方片段**、G-MESH ×3（两个新态的 npz `mesh_sha` 也对上）、G-A ×3、**G-CONS 门槛由实测 κ 推出** ×3、**G-REPRO medium 复现已发表的 C 到 ±0.001**；★ 算子**从 gs40c import 不重打**，两轮不可能漂移。★ 预测 **①③④ 命中、② 落空在好的方向**（我预告 xcoarse 可能因跨声速面 <100 而 UNDEFINED，实测 **2 106** 个）。★★ **三条 RECORDED 限定**：**C 本身不随 h 单调**（P1 0.467/0.479/0.428，控制臂自己也在抖）⇒ **可以说「k\* 稳」，不能说「效应量稳」**；**D-USABLE 依赖分辨率**（≥50 分量 **1/6/8**）⇒ 上一轮"把不可测判据变可测"在 xcoarse 上**只勉强成立**，引用必须带级别；**四条禁止句全部有效**，尤其 **k\*=1 跨三级 ≠ 跨工况**（三级全在 M0.88/α3.06，**工况维度仍未测**）。★ **仍不撤销**「共同修法」的退役 —— 本轮加固的是**上一轮开出来的那条新候选**（k=1 小 patch 线性重拟合） | **K-FIXED；七条守卫全过；kill 六条全不成立** | 真·零求解，217.2 s（闸 1 200 s）；库零改动；`gs40e_level_ladder.csv` + `.json` |

| 10 | 2026-08-17 | [20260817-1900-condition-axis-prereg.md](20260817-1900-condition-axis-prereg.md) | **工况维度预注册**（动代码前提交） | 上一轮判定自己写下的第 4 条禁止句点名的维度。**几何/级别/配方全冻结，只动工况**；十字设计 M∈{0.78,0.82,0.86,0.88}@α3.06 + α∈{1.53,3.06,4.59}@M0.88，**5 条新求解 ≈70 s**（按成本探针 13.3 s/腿）。★★★ **第八问抓到本轮最大的构造性陷阱**：**亚临界腿没有声速集 ⇒ C 无定义**，若算成"没有反例"判据就被**空洞满足** ⇒ 事前定死**只有控制臂跨声速面 ≥100 的腿计入**，其余标 EXCLUDED-SUBCRITICAL 并打印计数。★★ **第二个更隐蔽的空洞也事前封掉**：「k\* 跨工况不变」若工况根本没怎么变就毫无价值 ⇒ **强度前提检查**（最强/最弱超声速占比须 ≥2×，否则 W-FIXED **降级为 RECORDED**），并写成 **kill 条 4** 而不是留给事后判断。★ 腿在知道谁收敛之前定死，不收敛的**按 CLAUDE.md 分型表分型后如实报**，不换成"能收敛的工况"（roadmap 明令禁止）。★ 阈值全部逐字沿用前两轮 | RECORDED（预注册） | 未动代码 |
| 11 | 2026-08-17 | [判定](20260817-2100-condition-axis-verdict.md) | **工况维度执行**（349 s） | ★★★ **W-DRIFT —— 我的主预测被推翻**。5 条计入腿**全部有内点极大**，但 **4 条 k\*=P1、M0.82 在 P2** ⇒ **上一轮 K-FIXED 的可迁移性只能声称到「跨级别」，不能到「跨工况」**。★★ **前提检查通过得很稳**：超声速占比跨腿 **0.0120→0.1540 = 12.81×**（要 ≥2×）⇒ **这是一次真的检验，不是白送的通过**。★ **那条漂移只有 3.6 %**（M0.82 上 C(P1) 0.281 vs C(P2) 0.291）—— **我按注册判 W-DRIFT 不重读**（并列规则是给并列用的，这不是并列），但**如实说清它有多弱**：五条腿里一条、一格、3.6 % 余量 ⇒ 正确措辞是「**k\*∈{1,2}，压倒性在 1**」，转述时不带余量就是夸大。★★ **A4.59 不收敛，而它不是能力上限**：clamps **0/0**、无 GMRES 停滞、残差尾部 **0.00171→0.00138→0.00103 单调下降** ⇒ 按分型表是 **`budget_limited`（还在降）**，CLAUDE.md 原文「**budget_limited 不是失败**」，把它读成上限正是项目撤回过一整个"死区"结论的那个错 ⇒ **不宣称 α 上限、不加预算**（看到结果后加资源 = 范围蔓延）。★★★ **RECORDED 两条，一警告一支持**：① **C 本身与激波强度强烈共变**（sup_frac 0.012→0.154 对 C(P1) 0.143→0.479，五点全对上）⇒ **跨工况直接比 C 的绝对值是被混淆的**；本轮判据判的是**形状**（每条腿内部成立）**没踩这个坑**，但**未来任何跨工况引用 C 绝对值的说法都会踩**；② 而**相对增益在 12.8× 强度跨度上近乎不变** —— C(P1)/C(P0) = **2.51/2.46/1.88/2.17/2.01**，vs 纯平滑 **1.30–2.57×** ⇒ **下一轮跨工况要比的是比值不是绝对值**。★ 预测 **① 被推翻、② 落空**（M0.78 实测 1 062 个跨声速面，计入）、**③④⑤ 命中**（强度 12.81×、5.8 min）。★ 本季第五次预测落空，照实记不改预测 | **W-DRIFT；G-ANCHOR/G-A/G-CONS/G-RECIPE/G-MESH 全过；强度前提通过 12.81×；kill 六条全不成立** | 5 条新解 ~189 s + 阶梯，总 349 s（闸 1 500 s）；库零改动；`gs40f_condition_axis.csv` + `.json` |

| 12 | 2026-08-17 | [20260817-2300-geometry-axis-prereg.md](20260817-2300-geometry-axis-prereg.md) | **几何维度预注册**（动代码前提交） | 翼身 → **M6 机翼**，**工况与已缓存的腿逐一匹配** ⇒ 几何是单变量。★★ **躲不掉的混淆事前登记**：换族必然换生产配方（机翼 amg/1e-6/m_start 0.70，翼身 direct/1e-5/0.50）⇒ 加一条 **CTRL 臂**（机翼跑**翼身配方**）去界定它，并事前写明「**若 CTRL 与 W0.88 不同 k\*，几何结论全部被配方混淆吞掉，必须如实说**」。★★ **判据比"k\*=1"更强**：问的是「换几何后 `k*` 还等于**同工况翼身腿**的那个值吗」—— 自动继承上一轮 M0.82 那条 3.6 % 的例外，而不是假装它不存在。★ 沿用上一轮教训：**绝对 C 不得跨几何比**，登记的跨几何量是**比值**。★ N-2.5D 登记为 **RECORDED-only**（一次变四件事，不投票） | RECORDED（预注册） | 未动代码 |
| 13 | 2026-08-18 | [判定](20260818-0100-geometry-axis-verdict.md) | **几何维度执行**（115.8 s） | ★★ **X-SHIFT（一条腿）**：M0.86/M0.88 匹配（都 P1），**M0.82 上机翼 P1 而翼身 P2**。★★★ **CTRL 通过 ⇒ 配方混淆被界定住** —— 机翼跑翼身配方仍是 P1 ⇒ 差异**不是** `precond`/`freeze_tol`/`m_start` 造成的（事前最担心的那条被排除，预测 ③ 命中）。★★★ **而本轮抓到自己的设计缺陷 #12**：**匹配 M ≠ 匹配激波强度** —— 同一个"M0.82"，翼身 sup_frac **0.0475** vs 机翼 **0.0873 = 1.84×**（去掉机身、同 M，超声速区大得多；事后毫不意外，**我事前没想到**）⇒ **老家族「我在比的两个东西是同一个吗」的新面孔：「工况」这个词在两个几何上指了两件事** ⇒ **常设新增：跨几何匹配工况必须匹配物理量（激波强度）不是名义参数（M）**。★ **X-SHIFT 仍按注册成立，我不重读**，但**归因被削弱**：M0.82 的差异是几何还是那 1.84× 强度差，**本轮分不开**。★★ **RECORDED 汇总视角**：**8 条计入腿（2 几何 × 3 级别 × 6 工况）里 7 条 k\*=P1**，唯一例外是翼身 M0.82（余量 3.6 %），**换几何同 M 下不复现**，且它**也不是最弱激波**（更弱的 M0.78 给 P1）⇒ 看不出结构、更像噪声，★ **但这是事后读数，不重判**。★ **仪器跨几何一致**：radius/h **0.726** vs 翼身 0.723–0.724。★ **相对增益跨几何稳**：C(P1)/C(P0) **2.94/2.29/2.33**（翼身 1.88–2.51）、vs 纯平滑 **1.94–3.01**。★★ **一处必须自报的遗漏**：预注册登记的 **N-2.5D RECORDED 臂只做了 G-MESH、没真跑** ⇒ **记为遗漏不记为空结果**（"没测"和"测了没东西"是两回事，本项目为混淆这两者付过代价）⇒ **2.5-D 那一步未做**。★ 预测 **①③④⑤ 命中、② 落空**（机翼 M0.88 **6.3 s 收敛 0/0**） | **X-SHIFT；G-ANCHOR ×3 / G-RECIPE-M6 / G-MESH / G-A / G-CONS 全过；CTRL 界定住配方混淆；kill 六条全不成立** | 4 条新解 ~28 s，总 115.8 s（闸 1 500 s）；库零改动；`gs40g_geometry_axis.csv` + `.json` |

| 14 | 2026-08-18 | [20260818-0300-strength-and-2p5d-prereg.md](20260818-0300-strength-and-2p5d-prereg.md) | **两笔债的预注册**（动代码前提交） | ★★★ **换主量并事先说明为什么这不是事后放宽**：五轮的主量是**离散 argmax `k*`**，而全部争议在**一条腿 3.6 % 的余量**上 —— 一个 argmax 把 3.6 % 的抖动放大成了判定码。新主量 = **连续余量 `R21 = C(P2)/C(P1)`**。三条理由事前写下：**问的是新问题**（不是"最优在哪"而是"那条例外是强度还是几何"）、**旧量照报且前五轮一个不重判**、★ **方向对我不利也照用**（R21 同样会暴露"P1 赢得很勉强"的腿）。★ Part B：**把强度轴在两族上填到重叠**（新增机翼 M∈{0.76,0.78,0.80}，**M 值事前定死，不按跑出来的 sup_frac 回头调** —— 按目标输出搜自变量就是调参）；判据**自归一**（两族拟合的偏移 vs 合并残差散布），不引入绝对阈值。★ Part A：**补跑 N-2.5D，仍 RECORDED-only**，并事先写下两条限定（M0.80/α1.25 是**已测的多解工况**、2.5-D 是**一层棱柱切四面体**故 k 的含义不完全可比）+ 新守卫 **G-2P5D** 打印面邻接分布，**让那条限定带数字而不是一句话** | RECORDED（预注册） | 未动代码 |
| 15 | 2026-08-18 | [判定](20260818-0500-strength-and-2p5d-verdict.md) | **两笔债执行**（219 s） | ★★★ **S-STRENGTH**：两族拟合在重叠区偏移 **0.0398 = 合并残差 0.1355 的 0.29×**（判据 ≤1×）⇒ **几何在强度之外加不上信息** ⇒ **判据缺陷 #12 的归因问题结清，而且结清在"不是几何"这一侧**；更直接的证据：**机翼在 sup_frac 0.0529 上也给 P2**，与翼身 0.0475 的 P2 几乎同处 ⇒ **同强度下两几何行为一致**。★ 上一轮的 **X-SHIFT 判定保留**（判据写的是同 M 匹配，不重判），但**归因现在定了**。★★★ **我必须撤回一句话**：上一轮写那条例外「**看不出结构、更像噪声**」—— 本轮实测 **`R21>1` 有三条腿**（WB M0.82、W M0.76、W M0.80），**全部在弱激波端**（sup_frac 0.0108/0.0475/0.0529），**事前预测 ② 被推翻**。★★ **两族斜率都是负的**（−0.202/−0.314）⇒ **`R21` 随激波强度单调下降，在 sup_frac ≈ 0.02–0.05 穿过 1** ⇒ **弱激波偏好更大 patch，强激波偏好 k=1**（与"大 patch = 平滑"一致；物理解释非测量）。★★★★ **整个系列的适用范围必须重述**：**`k*=1` 只在激波足够强时成立**（sup_frac ≳ 0.09 上 **8/8**），**sup_frac ≲ 0.05 时不可靠**（4 条腿 3 条给 P2 或贴 1）；★ **为什么前面没看见** —— **K-FIXED 那轮三级全在 M0.88**（强激波），**W-DRIFT 那轮只有一条弱腿** ⇒ **强度维度被采样偏差藏住了**，本季**第七次**"在一个配置上测了就推广"，只不过这次**是四轮之后我自己发现的**。★★ **常设新增：当争议集中在一个 argmax 的边界上，先把离散量换成它的连续余量再下结论** —— 同一批 8 条旧腿，用 `k*` 看是"7:1 的孤例"，用 `R21` 看是"一条清楚的斜率"。★★ **Part A（RECORDED-only）**：2.5-D coarse **收敛 0/0** 但 **C ≡ 0.000 四臂全零** —— 跨声速面 100 个却**没有任何分量达到 10 面** ⇒ **相干性度量在一层棱柱切成的 2.5-D 上退化到 0**；**G-2P5D 给数字支撑**（面邻居 **3.25 vs 三维 3.7**）⇒ **比预注册那句"不完全可比"更强：是退化**；medium **不收敛（4388/563 clamps）**，与能力边界 §3.6 记录一致。★ 预测 **①④ 命中、② 被推翻、③ 前半落空后半命中** ⇒ 本季第 7、8 次落空 | **S-STRENGTH；G-ANCHOR 8/8 / G-RECIPE-M6 / G-RECIPE-M1 / G-MESH / G-A / G-CONS 全过；kill 五条全不成立** | 3 条新解 ~8 s + 2 条 NACA，总 219 s（闸 1 500 s）；库零改动；`gs40h_strength_2p5d.csv` + `.json` |

| 16 | 2026-08-18 | [阶段小结](20260818-0700-recovery-line-summary.md) + [20260818-0900 预注册](20260818-0900-position-and-blastradius-prereg.md) | **恢复算子线小结** + 两个剩余问题的预注册 | 小结把六轮放一页：**成立的 A1–A7 各带范围**、**从未证明的 N1–N5**、代价（**≈25 min 机时、10 条解、`pyfp3d/` 零改动**）、以及这条线自己的过程账（**判据缺陷 #9–#12 + 一次遗漏 + 一次误判 + 一次误价**）与换来的**五条常设做法**。★★★ 预注册**先把话说死**：**Part B 不是 N2 的答案，是它的定价** —— 完整答案要改 Jacobian（CSR 加宽 + 精确 FD），本轮不做；**新增第五条禁止句：爆炸半径量的是扰动大小不是解会怎么变，小的不能读成"接进去安全"，大的也不能读成"接进去会坏"**。★★ Part A **终于非循环**：**J1 = 壁面 Cp 激波位置**走**壁面三角切向梯度、零读体梯度** ⇒ **对每条臂逐位相同**，是固定参照；**J2 = ONERA M6 实验 TEST 2308**（仓库唯一外部裁判）**只作 RECORDED 不作门**（有粘实验 vs 无粘求解器，当门会把模型误差算到算子头上）。★ **POS-WORSE 登记为致命**并明写完全可能 | RECORDED（小结 + 预注册） | 未动代码 |
| 17 | 2026-08-18 | [判定](20260818-1100-position-and-blastradius-verdict.md) | **位置正确性 + 爆炸半径执行**（19.1 s） | ★★ **判定 A：POS-SAME，比值 1.278，离判死线 1.30 只差 0.022**（我按注册判，不重读）。★★★ **而结构比判定要紧：相干最优的 P1 在位置上最差（0.0169），相干较差的 P2/P3 最好（0.0055 = 0.42×）** ⇒ **六轮优化的代理量，在第一次被独立裁判检验时不跟踪目标、可能反向**。★ **三条限定一起说**：**n=1**（一态一网格，RECORDED 非结论）、**绝对值全 ≤0.017 c**（说"每条臂位置都不算错"同样公道）、**判定没有因此改成 POS-WORSE**。★ **G-INDEP PASS**（J1 对每臂逐位相同）、**G-STRENGTH PASS**（sup_frac 0.1230 落在 A2 强激波区）。★ **J2 RECORDED**：我们的激波一律在实验**下游 0.037–0.143 c 且向翼尖增大**，与已记录的无粘-有粘差距同向（GV5.2 的 0.06–0.10 c）。★ **干跑校验又抓到一个真 bug**：实验读取**丢了 `Z/L` 列**、用 `Cp<0` 当"上表面"（**吸力筛选不是表面筛选**），读出 x_shock 0.02–0.04 = 前缘吸力恢复 ⇒ 改按 `Z/L>0` 并**改用项目自己的 `shock_metrics` 而非我自造的规则**，读数随即物理正确（0.639→0.214，外翼前移 = λ 激波形态）。★★★ **判定 B：BR-MED → BR-LARGE** —— B1 耗散 2.7–3.5 %、B2 供体 6.1–6.4 %、**B3 σ 集合 3.7→8.7→12.5 %（随马赫增大）**，**排序 B3 > B2 > B1（预测 ②③ 都命中**：供体是离散选择比连续耗散斜坡敏感，σ 是**硬开关**最敏感）⇒ **生产马赫上 BR-LARGE ⇒ 按预注册的读法：接进求解器不是局部修正，是一个不同的离散化，成本要按"新离散化"估**。★ 预测 **①判定命中但方向落空**（是 POS-SAME 却**偏 WORSE**）、**②③④⑤ 命中**。★★ **对 A1–A7 的后果如实写**：它们**在各自范围内仍成立**（都是关于相干性的陈述），**变的是"相干性值不值得优化"这个前提** ⇒ **不撤销，但此后引用必须带上这一条** | **A: POS-SAME（擦线）；B: BR-LARGE；G-INDEP/G-STRENGTH/G-EXP/G-MESH/G-A/G-CONS 全过；kill 五条全不成立** | 1 条新解 4.2 s，总 19.1 s（闸 1 500 s）；库零改动；`gs40i_position_blastradius.csv` + `.json` |

| 18 | 2026-08-18 | [20260818-1300 预注册](20260818-1300-position-primary-prereg.md) → [判定](20260818-1500-position-primary-verdict.md) | **把位置当主判据**（13 态，零求解，196 s） | ★★ **POS-MIXED —— `k_pos*` 在 13 个态上无多数**（MIXED 4 / P0 1 / P1 2 / P2 3 / P3 3），与 `k_coh*` **只 4/13 一致**；对照：`k_coh*` 前几轮是 **7/8 稳在 P1** ⇒ **一个量稳，另一个不稳**。★★★ **而最可能的原因是我的尺子不是求解器**：**D 中位数 0.0120 c，而一个单元 h/c ≈ 0.041 ⇒ D ≈ 0.29 × 一个单元**；足迹提取是**逐单元**的（"壁面带里 M≥1 的最大质心"），**分辨率就是 h**，却被要求分辨 h/4 的差异 —— **四个态出现并列（多臂 D 完全相同）是同一个证据：提取器落到了同一个单元上**。⇒ **POS-MIXED 是关于仪器的结论，不是关于算子的结论，不得读成"各臂位置一样好"**。★★★ **讽刺必须写下**：**我造的位置度量，恰好害在登记项 1 本身要解决的那个毛病上 —— 亚单元激波定位。**★★ **上一轮那条 n=1 观察没有活下来**：13 态上没有"P1 系统性最差"这回事 ⇒ **「相干与位置反向」撤回**，本线**第二次自我撤回**（第一次是"那条例外更像噪声"）—— ★ **两次都是被一轮事前登记的确认推翻的**，正是"RECORDED 不是结论"这条规矩的用处。★ **守卫改进（不是放宽）**：继承的 G-Z 断言"没有求解器模块被 import"**在本轮开火**（我 import 前两轮的模块是为**复用 helper 不重打**）—— 而**模块是否被 import 只是"有没有求解"的代理，两个方向都不对** ⇒ **改成把 6 个求解入口替换为抛异常的桩：求解不是"会被发现"而是"不可能发生"**，**严格更强**。★ **本轮是我最差的一轮**：预测 **①③④ 被推翻、② 不可测、⑤ 命中**（D 实测上界 0.0655 = 我预测的 3.3 倍；弱激波态 J1 竟 7/7 全有效）⇒ 本季累计落空 **12 次**，**一次都没事后改预测**。★ **下一步 (a)：把足迹提取改成亚单元（对 M=1 线性插值）** —— 便宜，**且它本身就是登记项 1 的一个小版本**：若亚单元插值让位置度量变得可分辨，**那就是"亚单元表示确实要紧"的直接演示** | **POS-MIXED；Q 不可测；G-ANCHOR/G-STATE/G-MESH/G-A/G-CONS/G-SMOOTH 全过；G-Z 已强化** | 零求解，196.1 s（闸 1 500 s）；库零改动；`gs40j_position_primary.csv` + `.json` |

| 19 | 2026-08-18 | [20260818-1700 预注册](20260818-1700-subcell-extractor-prereg.md) → [判定](20260818-1900-subcell-verdict.md) | **亚单元足迹提取**（零求解） | ★★ **G-SUB 失败（5/13）⇒ kill 条 1 开火 ⇒ 停**（不调提取器、不重跑）。★★★ **而停下来查出的东西比本轮原问题重要得多**：外侧两站（η 0.96/0.99）的"足迹"是 **≈0.99（后缘）**而 J1 是 **0.44** ⇒ **误差 0.54–0.56 弦长 = 我一直在报的 D（~0.01）的 50 倍** —— 我的取法是「壁面带里 `M≥1` 的**最大 x/c**」，而**最大值被 P13 翼尖自由边奇点的超声速斑块吃掉了**。★★ **而七站的中位数把这两个离群站藏了整整三轮**。★★★ **判据缺陷 #13：我走进了一条项目写明的禁止项** —— roadmap §3 原文「**不得用 `M_max` 作为能力指标，直到 P13 被处理 —— 现在它读的是局部伪影**」，而我的足迹就是一个**超声速区的极值**；★ 这不是新坑，是一条**我自己在审计里复述过**的禁止项。★★★ **后果：位置三轮的读数全部作废** —— 第 9 轮 POS-SAME、第 10 轮 POS-MIXED **均作废**；**N1 退回到「从未被可靠测量过」，比开测之前更差**（那时以为没测，现在知道测了三轮且测错）；连带作废两条判断：「相干与位置反向」的**证据基础**、以及「POS-MIXED 是分辨率造成的」（**部分对但不是主因**，主因是外侧两站量错了对象）⇒ **本线第三次自我撤回**。★ **A1–A7 不受影响**（关于相干性 `C`，不使用足迹提取器）；「共同修法」退役不撤销。★★ **值得单记的过程观察**：**抓到它的是 G-SUB —— 一条我为「新提取器」写的守卫，而它暴露的是「旧提取器」的缺陷** ⇒ **常设新增：换仪器时给「新的不该比旧的差」写一条守卫，它检验的其实是两者共同的前提，而那正是最不容易被单独检查的东西**。★ 预测 **③ 被推翻**（D 中位数不降反升，正是 G-SUB 抓到的）、**①② 因 kill 先开火而不可判**、④ 命中 ⇒ 本季累计落空 **13 次**。★ **建议这条线在此停**：交出「**相干性可被刻画，而它是否对应正确位置，在 P13 被处理之前无法测量**」；若仍要测位置，**必须先**排除/掩蔽尖端带、**逐站报不用中位数**、并给足迹换一个**不取极值**的定义（三条都要事前登记） | **G-SUB FAIL ⇒ kill 条 1 执行；位置三轮作废** | 零求解；库零改动；`gs40k_subcell.csv` |

| 20 | 2026-08-18 | 阶段核查（无预注册 —— 仪式动作） | **GS4.0 后的全门控集** + R2/R3/R4 结清 + 入口基线更正 | ★★★ **全门控 509 passed + 1 skipped + 3 xfailed + 1 XPASSED，0 failed，1:12:38 @16 线程**。★★ **四个数全部对平**：未门控 500+12+2 ⇒ 门控解锁 **11** 个 skip ⇒ **9→passed（500+9=509）、1→xfailed（2→3）、1→XPASSED**，9+1+1=11。★★★ **且 488 + 21 = 509 恰好** —— 与 phase 3 交接读数的差**正是 GS4.0 的 21 条仪器锁，别的一个没动**；XPASS 仍是 mark 自己预言的那条（线程依赖）⇒ **无回归**。★ **溯源干净**：第一次门控跑到一半时我改了工作树，**按启动时记的 PID 杀掉重跑**，不引用来源不明的结果（代价 ~12 min）。★ **R2 已删** `post/unified.py`（删前验证逐位相同：cl 0.2775363765023681；顺带删掉一个**够不到所以从没被抓到**的 `dict(mvop=,phi_ext=)` 坏分支）；★ **R3 已归因**：同 sha 是**生成器等价**不是目录复制（零机时，从两个生成器自己的注释定死）⇒ **phase 3 的 S 阶梯从来没有跨网格族风险**；★ **R4 使用者裁决保留**。★★ **我更正了自己一句话**：说 `onera_m6_roundtip/` "冗余" **太重** —— 只有 `.msh` 字节重复，目录本身是 **M5 门（19 个活测试）+ 已提交证据**，删它是删门。★★ **网格命名问题以零风险方式解掉**：不改名（`coarse_ss` 在 28 个 py/md + 已提交 CSV 里），改为**把两条轴盖进 manifest 的 `extra`** —— `tip_cap` + `h_far/h_wall`（120 = 未被钳制 = 在加密射线上），**取自生成器自己的 LEVELS 表而非我按命名规则再推导一遍**；G-IDENTITY 断言 sha256 未动。★ **入口文书两个基线已更正** 479→500 / 488→509（旧值划掉保留）。★★ **本轮自己一处报告层缺陷**：批量更新脚本在第 2 步断言失败，而我**已经把只含第 1 步的改动提交了**，提交信息因此**超出了它实际包含的内容** ⇒ 已在下一提交里补齐并注明 —— 与本项目记过的"报告层要像求解层一样被守卫"同族 | **全门控 0 failed；四数对平；无回归** | 门控 1:12:38 @16t；未门控 500/12/2 |

| 21 | 2026-08-18 | [20260818-2100-gs41-strip-core-prereg.md](20260818-2100-gs41-strip-core-prereg.md) | ★★★ **GS4.1 开工：条带核的预注册**（动代码前提交） | 立项文书 §3 把门拆成四件并**明写不建议同时做耦合** ⇒ **本轮只做 ①核 + ②平板/Blasius/自相似验证**；★★ **③ 成本口径（S2/S4）与 ④ 横流处置不是被忘掉是被排期**，本预注册点名并要求**下一轮的预注册以它们开头**。★★★ **R1 在开工时就承认**：门写 `< 1 s` 而生产级三维 IBL 实测 **918 s = ~900×**；**本轮根本不测整机翼**故 **不判这条**，但**每个算例的墙钟都报**，为下一轮外推留可引数据。★ 交付 `pyfp3d/viscous/strip2d.py`（沿流向**行进**、不组装全局系统 —— 这正是它比 `ibl3.py` 便宜的机理），**闭包复用 `closures.py` 不重写**，`ibl3.py` 一行不改（roadmap 冻结令，G-FROZEN 用 `git diff --exit-code` 断言）。★ 判据：**V-BLASIUS**（`c_f√Re_x` 与 `H` 同时 ±5 %，≥3 个 Re_x 十倍程）+ **V-ORDER**（单调且阶 ≥0.8）；**±5 % 与 0.8 事前声明是本轮自己的标定**，将来引用必须连这句一起引（第 6 问）。★★ **湍流侧只 RECORDED 不设门** —— 门的原文只要求层流算例，给没有解析真值的东西设门就是拿标定当保证（本季四次绝对阈值缺陷）。★ 守卫：**G-ANALYTIC 要求 Blasius/Falkner–Skan 参照值在脚本里独立算出并打印**，`0.664`/`2.59` 只作**自检打印**不作硬编码判据；**G-NOSOLVE 沿用 GS4.0k 强化过的抛异常桩**（求解"不可能发生"而非"会被发现"）。★★★ **事前预测最要紧的一条是对我自己不利的**：我预测 **`c_f` 过而 `H` 不过** —— 依据是 phase 1 **GV1.1 已记录的 (a) FAIL：闭包家族不动点 `H*` = 2.7083 ≠ Blasius 2.59（+4.6 %）** ⇒ 若命中则判 **V-FAIL 且原因已提前定位到闭包**，**kill 条 1 开火本轮停**。★★ 而那样的话**本轮的价值不是"做出了核"，是把一条 phase 1 的已知 FAIL 从「三维 IBL 的属性」重新定位成「闭包家族的属性」** —— 因为条带核与 `ibl3.py` **共享闭包但不共享离散化**；这是本轮设计里**唯一真正新的信息** | RECORDED（预注册） | 未动代码 |

| 22 | 2026-08-18 | [附录 #1](20260818-2100-gs41-strip-core-prereg-addendum-1.md) + [#2](20260818-2100-gs41-strip-core-prereg-addendum-2.md) + [#3](20260818-2100-gs41-strip-core-prereg-addendum-3.md) → [判定](20260818-2300-gs41-strip-core-verdict.md) | **GS4.1 条带核执行**（①核 + ②层流验证，4.58 s） | ★★★ **V-FAIL，而事前预测的方向是反的，反过来的那一半才是新信息**。预注册预测「`c_f` 过而 `H` 不过」，实测**镜像**：`H` **+4.52 % 带内**、`c_f√Re_x` **+6.94 % 带外** ⇒ V-BLASIUS 要求二者同时在带 ⇒ **V-FAIL，原因定位到闭包 ⇒ kill 条 1 开火，本轮停，不换闭包**。★★ **缺陷在哪**：`+4.6 %` **落在它自己引用的 ±5 % 带之内** —— 被引来支持"会失败"的那个数从没拿去和它支持的那条带比过（第 6 问再次命中）；★ 而这条**在执行前就已记下**（附录 #1，零机时纯代数，不是事后补的说法）。★★★ **`c_f` 那个数一直在盘上，只是没有任何判据去比它**：phase 1 自己提交的 `gv1_1a_march.csv` 的 `cf_march` 列（**层流**）从 **+15.77 %** 单调收敛到 **+7.75 %**（末站），**窗内每站都在 ±5 % 外**，收敛方向正是本轮的不动点 0.710235 ⇒ **两轮相互印证（零额外机时的跨轮核对）**；phase 1 没报它是因为**没有门覆盖它**（门 (a) 比 `H`，门 (b) 比的是**湍流** `c_f`）。★★ **而我第一版把这条写错了，自纠**：我曾把门 (b) 的 `0.07 % PASS` 写成"两臂同时离解析真值 ~6.9 %" —— **门 (b) 是湍流腿、Blasius 是层流真值，那是跨流态比较**（第 5 问，我在批评它的同一段里犯了它）；门 (b) 的**结构性**问题仍成立（两臂共用同一闭包 = 共模，看不见共享模型误差），**但它的 0.07 % 不得与任何层流数字并列**。★ 且 phase 1 唯一设门的 `H`（4.5 %）**是两者中较好的那个**。★★ **归属不是离散化，三条独立证据**：① 自收敛阶 **4.039/3.859 = RK4 设计阶**，n=2000 上离散误差 **1.3e-13**，比那 4.5 % 低 **11 个数量级**，而对 Blasius 的误差**九位有效数字不动**（预注册 §4 明令分开的两种解释，分开的结果是 11 个数量级不是措辞）；② **代数不动点不含任何离散化**（自相似代入两方程消去 `dδ/dξ`），行进落在它上面（m=1 差 **1.06e-15**）；③ 该代数值 `H = 2.708292` 与 GV1.1 用**三种独立构造**得的 **2.7083** 吻合 **5 位** ⇒ 第四种构造。★ **重定位按附录 #1 的分寸写**：把 GV1.1 (a) 从「三维 IBL 属性」移到「闭包家族属性」**这一半 GV1.1 自己已做过**（其 VERDICT 原文：FE 与闭包自己的二维行进吻合 2.2e-4），**本轮新增的是**代数层、`c_f` 侧、以及不共享离散化的第二实现。★★ **F-SIMILAR PASS 5.789 %（2/3 楔角）**，而 §2.2 事前给宽带的理由（"对压力梯度拟合更松"）**被数据反驳一半**：有压力梯度的两个楔角反而更准，**最松的是零梯度那个** ⇒ 下一轮引用 ±8 % 必须连这句一起引；★ 被排除的 m=0 读数 **+4.485 % 同样在带内** ⇒ **排除它不改判定**（不是挑样本）。★ **墙钟口径教训**：预测「单条带 < 0.1 s」**没写子步数** ⇒ 250 子步 **0.0149 s 命中**、2000 子步 **0.1225 s 落空 22 %** ⇒ **同一预测两个答案 = 本来不良定**（与"门控计数必须带线程数"同族：**墙钟必须连分辨率一起引**）。★ **干跑校验在花机时前抓到两个真 bug**（`_rhs` 返回序被反着解包；`similar_seed` 内部用 Blasius 相关式推 `theta`，把平板厚度强加给楔流、m=1 发散到 `A~3e11`）；★★ **而第一次执行仍挂死 6.5 min，挂的是参照臂** —— 我给被测对象做了干跑，却把**预言机**当成"标准库不会错"直接下场 ⇒ **常设新增：参照臂也要干跑**。★ 预测 **① 半中、② 被推翻（方向相反）、③ 分辨率相关半中、④ 命中** ⇒ 本季累计落空 **14 次**，一次都没事后改预测。★★ **同日自纠一条判定文字**：初稿把岔路 (a) 写成"受**裁决 D2** 约束"—— **错的**，D2 管的是**壁面精度后备路线（等参 P2）= 无粘求解器的单元阶数/壁面几何**，与 BL 闭包家族无关（「**名字不是作用域**」，而我是在一份专门讨论"两个数是不是同一个东西"的判定里犯的）。实际约束 = **冻结令 + kill 条 5 + Track V 证据基础**，且**只打 (a1) 改 `closures.py`，不打 (a2) 另配二方程相关式闭包**（后者 `closures.py` 一行不动 ⇒ **满足**冻结令，且门的原文本就允许「Drela 二方程」）。★ 同时实测钉死**"改闭包"不可能是重标定**：`C_L_DEFAULT`/`A1_BRADSHAW`/`KAPPA`/`B_SPALDING`/`CTAU_LAM` 五个常数全改，`H` **逐位不动** ⇒ 层流侧完全由型线族 `_lam_UW` 决定（`c_f`、`c_D`、`θ`、`θ*` 都是同一条型线的泛函，一个形状参数命中不了两个目标） | **V-BLASIUS V-FAIL / V-ORDER V-FAIL（模型地板，与离散误差分开 11 个数量级）/ F-SIMILAR PASS / 湍流 RECORDED；G-FROZEN·G-REUSE·G-NOSOLVE·G-ANALYTIC·G-WALL 全过；kill 条 1 开火** | 未门控 **521 / 12 / 2, 0 failed**（**472.19 s @8 线程**；**500 + 21 = `tests/test_gs41_strip2d.py`**，skipped 与 xfailed 一个没动）；快层 **5/5 绿 557 s @8t, load 3.4**；测量机时 4.58 s + 挂死 6.5 min ≈ **6.6 min**（闸 20 min）；`pyfp3d/` 仅**新增** `strip2d.py`，`ibl3.py`/`closures.py` 收口时再验逐位未改 |

| 23 | 2026-08-19 | [预注册](20260819-0100-gs41-cost-and-crossflow-prereg.md) → [判定](20260819-0300-gs41-cost-and-crossflow-verdict.md) | **GS4.1 第二轮：③ 成本口径 + ④ 横流处置**（零求解，31.71 s） | ★ 使用者裁决 2026-08-18 走 **(b)**：接受层流偏置、先做 ③，**(a2) 登记不开**。★★★ **③ C-PARTIAL：条带核把生产级 BL 从无粘成本的 607 % 降到 1.45 %** —— S4 `n_sub=2` **2.188 s** vs committed 三维 IBL **914.62 s = 418×**，`P1_strip` **0.01453** vs `P1_IBL3` **6.073** ⇒ **BL 不再是成本大头**，立项 §2.2 那条"P1 加密穿带 ⇒ 动机失效"的担忧**不再是瓶颈**。★★ **但门的 `< 1 s` 在任何注册分辨率上都没到**（最便宜的 `n_sub=1` 也要 **1.224 s**）⇒ 按 R1 原话记**部分达成**，**门的数字一个字没动**。★★★ **profile 决定了 (a2) 的成本论据：92.7 % 的时间在闭包求积**（`closure_scalar` 7.82 µs ×4 vs `2×2 solve` 2.48 µs ×1）⇒ **(a2) 的"更便宜"成立**；★ 附一条**明标为外推**的估算：相关式闭包若 ~0.3 µs/次 ⇒ 约 9× ⇒ S4 ~0.24 s **进门** —— **不得当实测引用**。★★ **④ 已记录并多给了一层"后果"**：S4 丢掉的横流 `max|B|/max|A| = 0.018562`、逐点 max **0.031499**、集中在 x/c **0.9712–1.0000** ⇒ `|u_e|` 偏低 **0.0496 %**、流向偏角 **1.80°**；★ **分母口径必须写明**（`(真−二维)/二维` vs `/真`：S4 两者都是 0.0496 %，**S2 是 87.35 % vs 46.62 %**，S2 引用必带口径），且 S2 的 1.58 本就是 committed 证据判定的**粗网格高估**（`max\|B\|` 41.2→3.73）。★★★ **拒绝下"横流可忽略"的结论**（J = 0.600 vs 零假设 0.060 = 10× ⇒ 真实后缘结构，禁改项 (iv)；1.80° 驱动二次流与转捩，幅度小 ≠ 影响小）。★★ **我自己抓到一条限定**：第一轮的精度读数在 **19 站/2000 子步**上，本轮的成本读数在 **13 764 站/`n_sub` 1–4** 上 ⇒ **「2.188 s」不等于「已验证精度下的 2.188 s」**；本轮只验了行进态保持物理（`1<H<10` 全过），**未在生产站点数上验收敛阶** ⇒ 登记为便宜跟进项 —— ★ 这是第 5 问用在**我自己的两轮之间**。★★ **G-JIT 是本轮最该有的守卫**：冷 0.1026 s vs 热 0.0073 s ⇒ **编译占冷启动 92.9 %**，不热身就会把编译器当求解器量；★ 它与"求积占一步 92.7 %"是**两个不相干的量**，数值接近纯属巧合，**别读串**。★ **G-REF 把 phase-3 判定正文的 `133.5 s / 9.96×` 与其自己 CSV 的 `136.75 / 10.205` 一起打印**（差 2.4 %）—— 参照取 CSV，两者不得混用；`21 342 = 6×3 557`、`82 584 = 6×13 764` 断言通过。★ 预测 **③④⑤ 命中（求积 92.7 % > 80 %、G-INDEP 1.93 %、31.71 s）、② 半中（0.0145 vs 预测 0.011）、① 码中量偏且子预测被推翻**（我写"`n_sub=1` 可能进带"，实测 1.224 s **没进**）⇒ 本季累计落空 **15 次**，一次都没事后改预测 | **C-PARTIAL（R1 部分达成，门未改）+ ④ RECORDED；G-FROZEN·G-NOSOLVE·G-REF·G-JIT·G-INDEP·G-RES 全过；kill 五条全不成立** | 零求解 31.71 s @8t（闸 20 min）；`pyfp3d/` **零改动** ⇒ 套件不重测（**521/12/2** 承第一轮实测）；快层 **5/5 绿 547 s @8t, load 1.3** |

| 24 | 2026-08-19 | [预注册](20260819-0500-gs41-a2-correlation-closure-prereg.md) + [附录 #1](20260819-0500-gs41-a2-prereg-addendum-1.md) → [判定](20260819-0700-gs41-a2-correlation-closure-verdict.md) | **GS4.1 第三轮：(a2) 相关式闭包**（零求解，9.12 s） | ★★★ **门的两条文字都达到了** —— **D-COST = C-SUB1 0.5040 s**（S4/13 764 站/`n_sub=2`），★★ 且**整条分辨率阶梯 0.2813/0.5040/0.9512 s 全在 1 s 以下**（对照第二轮：**每个**分辨率都在 1 s 以上）⇒ 不是擦线；`P1 = 0.003347` ⇒ 生产级 BL 从无粘的 **607 % → 0.33 %**，**1815×** 三维 IBL。★★★ **但"验证通过"那条含金量远低于表面** —— 预注册 §0 **事前**写死：三条相关式**本身就是拟合到 Falkner–Skan 的**，拿它过 Blasius 是**用拟合对自己的训练数据** ⇒ **D-BLASIUS/D-FS 的职能是「转录检查」不是模型验证**，判定**禁止**写成"(a2) 精度得到验证"。★ 转录检查确实执行了且有效：**G-ORACLE** 把三条相关式对**独立积分的 FS ODE** 扫 9 个楔角，Blasius 处 `H*` +0.041 % / `c_f` −0.082 % / `c_D` −0.007 %；★ 全扫最差 `c_f` **5.146 %**（m=−0.08 强逆压）= **拟合自身的误差**，落在本轮楔角**不覆盖**的一端 ⇒ 禁止句"不得声称在强逆压/分离附近可用"生效。★★★ **结构上真正变了的一件事**（附录 #1 的诊断，**同一种代数构造施加到两族、各自对同一外部真值**）：ZPG 不动点 **相关式 2.590433（−0.0258 %）vs 型线族 2.708292（+4.5229 %）= 175.6×** ⇒ **第一轮那个 FAIL 的机理被直接消掉**（型线族留不住 Blasius，相关式按构造留得住）；代数值与行进渐近值**逐位相同**。★★★ **D-ORDER 又 FAIL，而这次暴露的是判据本身**：对 Blasius 的误差不动（阶 0.000）⇒ 字面 D-FAIL；但地板是 **2.58e-4 = 0.026 %**（拟合残差，第一轮是 **4.5 %**），而**离散误差 3.3e-14 = 机器精度**，在 n=250 上就已收敛 ⇒ **判据缺陷 #9：D-ORDER 量的是"对真值的误差随加密下降"，而加密只动离散误差 —— 只要模型有任何偏置它就按构造失败，于是对 4.5 % 与 0.026 % 给出完全相同的判定码，一个 175 倍的改善对它完全不可见** ⇒ **本轮不重 specify（事后放宽被禁），登记为下一轮使用前必修**；★ 附带：`H` 的自收敛"阶 0.024"是**在机器精度的数上拟合的** ⇒ **舍入噪声上拟合出的阶不是阶**（第一轮 §4 想到了"撞模型地板"，没想到"已收敛到机器精度"）。★★ **D-TURB 复现了，而报"未复现"的是我的守卫**：逐站最大差 **1.277e-09**，恰是第一轮 CSV 以 `%.8e`（9 位有效数字）落盘的**写入精度**（≈1.5e-9），而我把阈值挑成 1e-9 ⇒ **比数据自己的存储精度还紧，这条检查原理上不可能通过** —— 与缺陷 #10（容差必须推导不能挑）同族，新面孔是"从存储精度推"；★ 与附录 #1 那条（**把有量纲的速率对无量纲阈值断言**，量纲错配）合起来，**本轮我自己犯了两次同族缺陷，两次都被自己的红灯抓到**。★ **G-LEGACY**：型线族不动点 `8.02881134/2.708292/0.710235` 逐位复现、第一轮 21 条锁全过 ⇒ **加一条路线没动任何既有读数**；**G-AUTHORITY** 两族零交叉 import（已下沉为套件锁）。★ 预测 **①④⑥⑦ 命中、② 码中量偏高 26 %、③⑤ 落空**（加速比实测 **4.34×** 而非预测的 8–20× —— 相关式求值确实便宜了但 `2×2` 解与解释器开销没跟着降；★ 第二轮那条 ~0.24 s 外推**偏乐观 2.1×**，而当时标注"外推、不得当实测引用"正是它的用处）⇒ 本季累计落空 **17 次**，一次都没事后改预测。★ **遗留接缝已点名**：湍流仍走型线族 ⇒ 条带核目前**层流相关式/湍流型线族混合**，下一轮必须显式处理 | **D-COST C-SUB1 / D-BLASIUS PASS（近乎循环）/ D-FS PASS 3/3 / D-ORDER D-FAIL（判据缺陷 #9）/ D-TURB RECORDED；七条守卫全过；kill 六条全不成立** | 未门控 **537 / 12 / 2, 0 failed**（**485.74 s @8t**；**521 + 16 = `tests/test_gs41_closures_2d.py`**，skipped 与 xfailed 未动）；快层 **5/5 绿 545 s @8t, load 3.0**；零求解 9.12 s（闸 20 min） |

| 25 | 2026-08-19 | [预注册](20260819-0900-closures-source-audit-prereg.md) + [附录 #1](20260819-0900-closures-audit-addendum-1.md) → [判定](20260819-1100-closures-source-audit-verdict.md) | **`closures.py` 对装订出处的源码审计**（零求解，0.1 s） | ★ 使用者裁决走 **C**：先审计后湍流轮；A 的材料已到位（`reference/` 已 gitignore）。**装订出处此前一直不在工作树里 ⇒ 这份库从来没被对着源头核过。** ★★★ **先说没发生的事**：预注册 §0 事前写明本轮**可能推翻立了三周多的结论** —— 若型线转录有误，第一轮的 `H = 2.708292` 就从**家族性质**变成**实现缺陷**，会连带 GV1.1 (a) 一起倒。**Tier 1 全部 MATCH 到 1e-15**（eq 46 基函数含四条 η-导数、eq 42/43 层流型线、eq 51 Spalding、eq 47/48 湍流型线、eq 52 Coles、eq 53/54 `K,Υ`）⇒ **第一轮的归因在源头层面被确认**；★ 论文 p.11 自己也说 U 型线是"老的**一方程 Pohlhausen**"的修改版，且**从未声称**该多项式族在 Blasius 上准。★★★ **真发现在 Tier 2**：库注释写 `integrands are polynomials of degree <= 13 -> 8 points exact to degree 15`，**这句话只对一半成立** —— eq (60) 的**二次**厚度（`δ*₁,φ₁₁,φ₁₂,φ₂₂,δ_q`）确实 ≤14 次、8 点够；但**动能厚度** `φ*₁,φ*₂` 的被积函数是**三次**（`1−R·U·(U²+W²)`，而 `U,W` 各 7 次）⇒ **21 次，需 n ≥ 11 而库用 8**。实测收敛：`n=8` **4.293e-05** / `n=10` 1.897e-07 / **`n=11` 7.99e-15**；★ **对照双向验证**：`φ₁₁`（14 次）`n=7` 给 2.51e-05、`n=8` 给 2.24e-14 ⇒ degree 论证成立。★★ 后果落在 `θ*₁ = φ*₁ − δ*₁` = **动能厚度**，即论文 eq (28) 那条**动能积分方程**、三维 IBL 两条控制方程之一。★ **分类为 DIVERGENT-UNDOCUMENTED 的理由**：公式**是对的**（Ψ 加权五条在**同一 8 点规则**上比到 1e-17/1e-19 ⇒ eq (60) 定义无误），差的是**交付的数**；项目**有** `D-QUAD` 记录，**但它的理由对这两条厚度是假的 ⇒ 错误的记录不构成记录**。★★ **量级两个方向都不夸**：4.3e-05 比它上面压着的模型误差（GV1.1 的 2 % 带 / 第一轮的 4.5 % 地板）**小约 1000 倍** ⇒ **极不可能改变任何已发表结论**，但它**是真的、可复现、此前无人知道、且落在一条控制方程上**。★ **只报不修**（修会移动全部 Track V 数字，需自带 re-baseline 勘误清单 ⇒ 登记为 F1）。★★★ **而第一次执行报的是 `AUD-DEFECT` 六条，全是我的度量缺陷**：`f2,f3 ∝ (1−η)²` 在 η→1 **取零**而我的相对度量除以 `\|paper\|`（A1–A4）；我声称"8 点精确"**只对多项式那一半成立**、Ψ 加权含 `arctan(W/U)` ⇒ 我在比**两个截断误差**（A11）；层流 `C_D^×` **恒为零**（`S∝dU,T∝dW`）两侧 ~1e-19 我却做相对比较（A12）。**若照单报出去，那句话会是"Track V 的地基与其出处不符"** —— 本轮最大的风险来自**审计仪器**而非被审计对象。★★ **同一家族第三轮连续命中**（第三轮：有量纲速率对无量纲阈值；容差紧于存储精度；本轮：相对度量用在会取零的量上）⇒ **常设一句：阈值必须连同它的分母一起推导 —— 一个量会不会取零、它自己的尺度是多少，是阈值的一部分**。★ 修完加 **G-TEETH** 注入 1e-6/1e-9/1e-11 已知扰动、**三个全 CAUGHT**（干净 4.44e-16）⇒ 度量没被修瞎。★ Tier 4/5 inventory：**A20 = 登记发现**（论文 p.9 说耗散长度 `L` **按 Clauser G-beta locus 标定**，库 `C_L_DEFAULT=0.09` 注释写的是 **Bradshaw 值** + 项目自己的二维标定 ⇒ **出处声明的标定从未做过**）；A17/A18 **NOT-IN-SOURCE**（论文的 `κ,B` 与 `r` 只给符号不给值 ⇒ 库的 0.41/5.5/0.85 是**项目的选择**）；A19/A21 DIVERGENT-DOCUMENTED；★ 另记文档缺陷 `closure_node` docstring 写 "28-output" 而 `N_OUT=30`。★ 预测 **③④⑤⑥⑦ 命中、① 半中、② 落空**（我预告湍流侧至少 1 条 UNCHECKABLE，实测 **0 条**）⇒ 累计落空 **18 次**；★ **①半中最值得记**：方向对（型线族没问题），但我没料到缺陷出现在"**求积够不够**"这一维 —— 我的 A11 问的是"定义对不对"，**它答的是"算得准不准"** | **AUD-DEFECT（Tier 1–3 一条 DIVERGENT-UNDOCUMENTED = A11；一条 UNCHECKABLE < 3 故不触发 AUD-PARTIAL）；G-INDEP·G-FROZEN·G-TEETH 全过；kill 五条全不成立** | 未门控 **542 / 12 / 2, 0 failed**（**537 + 5 = `tests/test_gs41_closures_audit.py`**（一条发现、五条断言）；★ 墙钟 **875.53 s @8t**，同日同一套件早先是 472–486 s ⇒ **1.8× 散布而结果相同**；★ 启动时 load 仅 **1.26**，故**成因未确立** —— 按项目已记的墙钟散布惯例**带旗引用、不作成本**，★ 锁的是**根因**（21 次 ⇒ 需 11 点）**不是误差值**，修与不修都成立）；快层 **5/5 绿 1727 s (28.8 min) @8t, load 17.4** —— ★ 同日早先同一层是 545 s @load 3.4 ⇒ **3.2× 散布、结果相同**，而 **G8.2 那条墙钟断言仍然通过**；★ 这个 17.4 的负载也是套件那 875 s 的**可能**成因（未确立）；`pyfp3d/` **零改动** |

| 26 | 2026-08-19 | [预注册](20260819-1300-turbulent-closure-prereg.md) + [附录 #1](20260819-1300-turbulent-closure-addendum-1.md) → [判定](20260819-1500-turbulent-closure-verdict.md) | **湍流闭包（局部平衡 + 转捩，不含滞后）**（零求解，1.35 s） | ★ 使用者供源后开工：`reference/XFOIL6.99.zip` + `drela-giles-*.pdf`（均 gitignored）。★★ **范围按"本轮的验证能测到什么"划**：**滞后不做** —— ZPG 平板**在原理上测不了它**（平衡态**就是** `C_τ = C_τEQ`，滞后项恒零）⇒ 做了就是交付本轮无法验证的代码；门的"二方程 + lag"由**五、六两轮合起来**满足，**本轮不声称满足**。★★★ **物理结论：闭包 work 了** —— 上次凭记忆实现塌到 `H = 0.60`（物理不可能），本轮有出处地实现后 `H` 从转捩后的 1.80 单调弛豫到 **1.33–1.44**（ZPG 湍流平板该有的值），`c_f` 与**两条独立已建立相关式**多数差 1–5 %；★ **G-DI 精确通过（0.00e+00）** —— 那条为上次失败专门写的断言正是守在这里：**上次唯一的实质错误是把 `DIT` 的 `DI = 2c_D/H*` 当成 `c_D`**（多乘 `H*/2`），结构与常数当时就对到 1 %（`GBCON` 0.75 ✓、`CTCON` 0.01485 vs 我的 0.015）。★ **G-SOURCE**：六个常数**连 file:line 一起打印**，`CTCON` **由 GACON/GBCON 推出不是打进去的**。★ **判定 T-FAIL ×3,但三条要分开读**：**T-CF** 30/35 站在带内 —— 出带的 5 站分两类：**(a) `Re_θ=556` 是转捩调整**（预注册 §3 事前登记的 GV3.1 机制，禁止句 ④ 禁止把它记为相关式的失败）；★★ **但我的量测设计有缺陷** —— 我把 `x_tr` 与判据窗口起点选得**重叠**了，机制我事前想到、**它会伸进窗口我没想到**；**(b) `Re_θ≈5071–6231` 四站真擦边**，超出仅 **0.02–0.50 pp**，且**排除转捩区救不了**（`Re_θ≥800` 仍 28/32）。★★ 顺带揭示**推导带的一个性质**：带 = `max(3 %, 2s)` 而两条参照在 `Re_θ≈5000` **交叉**（`s` 最小）⇒ **带在那里收缩到 3 % 地板** ⇒ **推导带在参照彼此最一致处最难过** —— 这大概是对的行为，但"窄带处 FAIL"与"宽带处 FAIL"**不是同一件事**。**T-H** 仅 1 站失败（1.5071 vs 1.50，就是那个转捩站；`Re_θ≥800` 全在 [1.3252, 1.4434]）。★★★ **T-EQUIL 的失败是我的判据不是闭包**：我注册「漂移 < 2 %/decade」，实测 4.80 % ⇒ 字面 FAIL，**但 ZPG 湍流 `H` 本来就随 `Re_θ` 下降约 5 %/decade，那是物理** —— **我把「平衡（自洽、不塌陷）」与「`H` 恒定」混为一谈**，判据要求了一件**物理上错误**的事。**本轮不重 specify**（事后放宽被禁），登记 **G1：再用之前必修**。★★ **本季第四轮连续判据缺陷**（③ D-ORDER 分不开模型偏置与离散误差；④ 相对度量用在会取零的量上；⑤ 附录 #1 **Kármán–Schoenherr 变量对用错**——它是"长度平均 `C_F` vs `Re_L`"不是"局部 `c_f` vs `Re_θ`"，第 5 问用在**参照**上；⑤ 本条）⇒ 共同点仍是**判据没覆盖它所比较的量的定义域**，这次是**没覆盖"这个量本来就随自变量变化"**。★ 附录 #1 另记一条：**Ludwieg–Tillmann 被考虑并否决**，因为它**依赖 `H`** 而 `H` 正是被测量 ⇒ 参照会**部分吸收 `H` 的误差**（第 7 问，守卫不得量到自己）；两条选中的参照**只依赖 `Re_θ`**。★ `H*` 两臂 RECORDED：**old 臂略优**（31/35、最大超出 1.44 vs 2.74 pp），与第四轮"层流侧 published 比 XFOIL 代码版更准"**同向**，★ **但预注册事前写死不据此改默认** ⇒ 登记 G4。★ **T-CROSS 重叠 True**（单边，只支持不证伪）。★ 预测 **④⑤⑥⑦ 命中、①物理中判据落空、②③ 落空** ⇒ 累计落空 **21 次**；★ 预测 2 我**事前就写了"不保险"的理由**（干跑的 −5..+6 % 是**直接给定 `H=1.4`** 算的，而本轮 `H` 是解出来的）—— 那条限定说中了 | **T-CF/T-H/T-EQUIL 三条 T-FAIL（其中 T-EQUIL 是判据缺陷）；T-CROSS + old 臂 RECORDED；G-FROZEN·G-NOSOLVE·G-SOURCE·G-DI·G-LEGACY 全过；kill 五条全不成立（`H` 全程物理，未重演 0.60）** | 未门控 **552 / 12 / 2, 0 failed**（**542 + 10 = `tests/test_gs41_turbulent_closure.py`** 恰好闭合；★ 墙钟 **1052 s @8t, load 16.74** —— 机器确实忙，这也回过头解释了上一轮那 875 s）；快层 **5/5 绿 942 s (15.7 min) @8t, load 18.8**（同日安静时是 545 s @load 3.4 ⇒ 1.7× 散布、结果相同，**G8.2 的墙钟断言仍通过**）；零求解 1.35 s（含首次崩溃 0.6 s，闸 20 min） |

## ★ GS4.0 收口时**仍然缺失**的东西（写在这里免得被读成"审计的问题都解决了"）

- **产品层整层**：没有 CLI / `entry_points` / 输入文件格式 / 极曲线驱动 / 阻力分解 /
  任何阻力门禁 —— 那是 **GS5.2 / GS5.3 整个阶段**，注册 §0.2 明确排除；
- **五条产品指标一条都没动**（M1 仍 FAIL、M2 未动、M3a 可达性**未知**、M3b 大网格差 66×、
  M4 卡在不存在的 case 入口、M5 未度量）—— **刻意的**：修仪器不等于修能力；
- **续项 R1**：`run_capability_matrix.py` 的已提交 CSV **19 列 vs 脚本 24 列**，
  再跑一次会把 24 字段写在 19 字段表头下（真缺陷，按反蔓延记 RECORDED）。
