# 第四阶段开发历史（每轮一行）

规则同 phase two / three：每轮一行，叙事写在轮次文件里，判定只有
**PASS / FAIL / RECORDED / UNDEFINED**（收敛腿 < 2 报 UNDEFINED，不报方向）。
轮次文件格式见 [../dev_phase_two/_TEMPLATE.md](../dev_phase_two/_TEMPLATE.md)。

★★★ **本阶段的入口文件 = [../dev_phase_three/20260816-1000-gs41-initiation.md](../dev_phase_three/20260816-1000-gs41-initiation.md)**
—— **链接，不复制**（该文件 §0 自己定下这条维护约定：复制出来的入口会和原件分叉）。
开工前先读它的 §0：交接基线、裁决 D7 分派过来的项、六条不许静默改掉的事项、
以及 GS4.1 必须带的两条已测结论。

**计划与裁决（D1–D7）**仍在 [../dev_phase_two/roadmap.md](../dev_phase_two/roadmap.md)。

---

## 使用者定的顺序

**GS4.0（本阶段第一件事）→ 收口后再决定是否进入 GS4.1。**

GS4.0 = 2026-08-16 独立审计
（[../inspection/20260816-2200-independent-audit-zh.md](../inspection/20260816-2200-independent-audit-zh.md)）
§7 查出的六条**活缺陷**。使用者裁决（2026-08-16）：先解决，再看要不要开 GS4.1。

★ **命名说明**：门禁编号约定是 `GS<阶段>.<序号>`，而 S4 是「粘性重启」。GS4.0 里
**没有一条是粘性工作** —— 它是 S4 的**进入前置**（同 S0「地基与基线」之于 S1 的关系）。
按 §0 的读法：**GS4.0 = 「在开 S4 之前把仪器修好」**，不是 S4 的一个粘性子门。
这条写下来免得将来有人按 ID 推断内容 —— 本项目已记过多次「名字不是作用域」。

| # | 日期 | 轮次文件 | 内容 | 一句话结论 | 判定 | 测试状态 |
|---|---|---|---|---|---|---|
| 1 | 2026-08-16 | [20260816-2300-gs40-prereg.md](20260816-2300-gs40-prereg.md) | **GS4.0 预注册**（动代码前提交） | 六条缺陷的判据 + kill criterion + 明确的范围排除；★ 事前用新提的**第八问**（判据在 n=1 / 全通过 / 全失败 / 空集上给什么）过了一遍自己的判据，**当场抓到两处构造性通过**（空的 `NOT_COVERED`、空的 `level_results`），两处都写进了判据 | RECORDED（预注册） | 未动代码 |
| 2 | 2026-08-17 | [20260817-0100-gs40-verdict.md](20260817-0100-gs40-verdict.md) | **GS4.0 执行**：审计 §7 的六条活缺陷 | ★★ **F1**：`m_last_converged`/`m_final`/`target_reached` **只由已删除的 level-set 驱动器提供**（G-S 源码比对 `d224223:newton_ls.py:1186-1193`），保形侧**从来没有过** ⇒ `run_capability_matrix.py` 的 `MACH_NOT_ATTAINED` 守卫 `abs(m_att - m)` **恒为 0、永远不可能开火**，而它的注释写着它存在正是为了不信任驱动器标志位。补 `newton.py::_ramp_honesty_fields`（纯读判、不回喂求解），三个消费者改为**无默认值地读**；★ 记录一处与原实现的差异（保形爬坡**插入重试级**⇒`levels[i-1]` 失义，改从 `level_results` 派生，等价性有理由且已锁）。★★ **根因是删除路线时的账在 `tests/` 上对平（457→457），而它在原理上看不到 `bench/`** —— 已写进 CLAUDE.md 作为常设一条。★ **F2**：重读两轮判定后**登记删除条件而不删** —— 软集合路线**没被判死**（"机理靶子对、这一版参数化不可采纳"，kill 四条全不成立），与被 K3 判死的 `capture_select` **不同类** ⇒ 原则 4 在这里生效的是前半句，而那句话从来没写过；三条到期条件已写在旗标旁。★★★ **F3**：路径校验**先验证会红**（G-V 抓到 `tests/test_b18_wingbody_transonic.py`）再修；而**我第一版守卫 `checked >= len(LOCKS)` 在空表上是 `0>=0` 的空洞通过，被本轮自己的极端样本测试在上线前抓到** ⇒ 第八问在被提议的同一轮还清了成本。★ **F4** README 重写（不再把冻结的 phase-1 roadmap 称 active；三条承重环境要求就位；9 个链接逐条验证）。★ **F5** 两处过期文字已改，`post/unified.py` 去留**按 kill 条 5 不自行裁决**。★★★ **F6** `mesh/manifest.py` 接进 `write_mesh` ⇒ 10 个生成器免费获得；sidecar **tracked 而 `.msh` 仍 ignored**（实测 `git check-ignore`）；F6-c 对着真实缺陷回放通过（round 68 624 vs flat 55 531 tets、wall 6 890 vs 5 152），**且三个数逐位复现 phase 3 已记录值 = 计划外跨轮核对通过**；20 张网格 2.9 s 全部盖章，★★ **第一次使用就查出 20 个文件只有 17 张网格**（三对同 sha 不同名，`md5sum` 独立复核；9 303/59 359/448 197 正是 GS4.1 成本表的 S0/S2/S4 ⇒ phase 3 的 S 阶梯与 `onera_m6_roundtip` 是同一批网格），★ 两种解释未分离故**不作归因**。★ 事前预测**③ 落空**（我写 N≈10–14，实测 **20**），如实记不改预测 | **F1–F6 全 PASS；G-B/G-N/G-D/G-S/G-V/G-SELF 全过；kill 五条全不成立** | 常开 **499 / 12 / 2, 0 failed**（477.27 s @8 线程；**+20 = `tests/test_gs40_provenance.py`，skipped 与 xfailed 一个没动**；改动前独立复跑 479 基线 472.99 s 逐项吻合）；快层 **5/5 绿 553 s @8t, load 2.3**；总机时 ~25.4 min |

| 2b | 2026-08-17 | 同上 §5 附录 | **R1 结清**（使用者裁决：收尾时一并做掉） | `capability_matrix.csv` 表头 **19 列** vs 脚本 **24 列**，而表头只在文件不存在时写 ⇒ 下次运行会把 24 字段写在 19 字段表头下。★ **拒绝而不自动修复**：改表头 = 就地改写已提交证据（G3 类），静默开新文件 = 把一次测量劈成两个互不指向的产物 ⇒ 报错 + 点名那 5 个新列 + 给操作者两条出路。★ **先在真 CSV 上见红再写锁**（同 F3 的 G-V 纪律），同一测试再验好路径未被误伤。★ 归类：**第 5 问的又一层** —— 不是"两个**数**是不是同一个东西"，是"两个**行**是不是同一个 schema" | **PASS** | 常开 **500 / 12 / 2, 0 failed**（465.24 s @8t；499 + 1；库改动仍为 0，只碰 `bench/`）|
| 3 | 2026-08-17 | [20260817-0300-gradient-probe-prereg.md](20260817-0300-gradient-probe-prereg.md) | **登记项 1 探针预注册**（动代码前提交） | 使用者裁决 2026-08-17:「**我要修根因**」⇒ 走登记项 1。★ **零求解判决性探针**：固定一个已收敛的 φ（`le14_cache/rc0.05.npz`，生产 taper、`conv=True`、0/0 clamps；网格由 GS4.0 的 manifest 钉住），**唯一自变量是恢复算子** ⇒ 本项目在网格侧一直造不出来的那种干净单变量实验，在这里是**构造性**干净的。四条臂 A(现状常梯度) / B1(线性 patch LSQ) / B2(二次 patch LSQ) / ★★ **S(纯平滑，故意的伪解)** —— S 臂是设计核心：阻塞性结论说薄片与激波的**局部** signature 是同一个东西，所以任何一起抹平的算子都会让重尾变小，**若 B 与 S 分不开就是平滑不是重构**。判据按分区相对本轮控制臂（**不用** 0.219 那个**跨算例**的老数 = 第 6 问），分区**在 A 上算一次四臂共用**（公共点集）。★ 第八问事前抓到一处**构造性不可达**（SHOCK 分区为空 ⇒ G-REAL 不可能成立），已写进处置。★★★ **kill 条 1：判 G-NULL/G-SMEAR ⇒ 路线死，能力边界 §3.2 那句「共同修法 = 体内梯度重构」退役** —— 那是一个被当作前提用了三周、从没被检验过的判断。★ **禁止句**：本轮任何结果**不得**写成「体内梯度重构可行」—— 后处理探针只是**必要条件**，不是充分条件 | RECORDED（预注册） | 未动代码 |

## ★ GS4.0 收口时**仍然缺失**的东西（写在这里免得被读成"审计的问题都解决了"）

- **产品层整层**：没有 CLI / `entry_points` / 输入文件格式 / 极曲线驱动 / 阻力分解 /
  任何阻力门禁 —— 那是 **GS5.2 / GS5.3 整个阶段**，注册 §0.2 明确排除；
- **五条产品指标一条都没动**（M1 仍 FAIL、M2 未动、M3a 可达性**未知**、M3b 大网格差 66×、
  M4 卡在不存在的 case 入口、M5 未度量）—— **刻意的**：修仪器不等于修能力；
- **续项 R1**：`run_capability_matrix.py` 的已提交 CSV **19 列 vs 脚本 24 列**，
  再跑一次会把 24 字段写在 19 字段表头下（真缺陷，按反蔓延记 RECORDED）。
