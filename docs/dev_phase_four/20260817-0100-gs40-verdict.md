# 20260817-0100 — GS4.0 判定：六条活缺陷全部处置，套件 479 → 499 账目对平

**预注册**：[20260816-2300-gs40-prereg.md](20260816-2300-gs40-prereg.md)（**动代码前已提交**，`c3b78eb`）。
**来源**：[2026-08-16 独立审计](../inspection/20260816-2200-independent-audit-zh.md) §7。

**判定：F1 PASS / F2 PASS（按注册的"登记而非删除"）/ F3 PASS / F4 PASS /
F5 PASS（按注册的缩减范围）/ F6 PASS；六条守卫全过；kill criterion 五条都不成立。**

---

## 1. 逐条结果

### F1 ✓ PASS —— 构造性失效的守卫已修好，而修法是补一次迟到的 backport

**根因确认（G-S，源码比对不靠记忆）**：`git show d224223:pyfp3d/solve/newton_ls.py:1186-1193`
证实 `m_last_converged` / `m_final` / `target_reached` **只由 level-set 驱动器设置**，
随路线于 `9119798` 删除；保形 `newton.py` **从来没有过它们**。

**交付**：`newton.py::_ramp_honesty_fields(level_results, m_inf, converged)` —— 纯读判，
从 `level_results` 派生，不回喂求解 ⇒ **按构造不可能移动任何已提交数字**。

★ **与原实现的一处差异，如实记录（注册 §2.1 事前写下）**：原版用 `levels[i-1]`，
而保形爬坡**会向 `levels` 插入重试级** ⇒ 该下标失义。改从 `level_results` 派生
（CLAUDE.md 本来就是这么建议调用方的）。等价性有理由：**收敛过的级在 m 上单调递增**
（插入的重试级在"上一个收敛级"之上、"失败级"之下），所以"最后一个收敛的"与
"最高的收敛马赫"是同一级；用 `max` 是为了把这个不变量写出来。
**已锁**：`test_inserted_retry_levels_do_not_break_the_derivation`（该腿下位置索引会选错）。

**三个消费者**（审计 §7.1 的表）：

| 位置 | 处置 |
|---|---|
| `run_capability_matrix.py` | `m_att` 改成**两个显式分支**：ramp 驱动器**无默认值地读** `r["m_final"]`（键缺失必须响，这正是本缺陷的全部教训）；单马赫 `solve_newton_lifting` 走 `m_att = m`，因为**那是构造性正确的读法而不是回退**。CLEAN 守卫加上 `m_att is None` 分支（裸 `abs(None - m)` 会在报告层抛异常 = 40 分钟求解丢失那一族） |
| `run_le14_common_root.py` | `conv` 改用 `r["target_reached"]`，不再是恒真的合取项 |
| `run_task3_m6_triage.py` | `.get` **故意保留**（该调用点合法地同时接收两种驱动器），但注明两列过去在**两条臂上都是 None** |

★ **`m_final` 而不是 `m_last_converged` 当列标签**：这一列标注的是**该行自己的**
`cl_p`/`cl_kj`/`m_max`，而它们算自 `r["phi"]` = `m_final` 处的状态。爬坡在 0.80 死掉
（0.75 曾收敛）返回的是**失败的 0.80 状态**。

★★ **一处 ERRATUM 写进 `run_capability_matrix.py` 的原注释**：它的括号补充
"CLEAN rows were never affected: conv already requires `|m_final - m| < 1e-9`"
**即便在当时也是错的** —— 本脚本的 `conv` 就是 `bool(r["converged"])`，从不查 `m_final`；
那是 level-set 驱动器的内部不变量。CLEAN 行**碰巧仍然安全**（保形爬坡死掉时
`converged` 为 False），但那是**另一个理由**，现在守卫显式检查。

### F2 ✓ PASS —— 登记删除条件，**不删**（注册 §2.2 事前定死）

★★★ **重读两轮判定后，删除是错的**：软集合那轮的原文是
「**机理靶子是对的，但这一版参数化不可采纳**」，**kill 四条都不成立 ⇒ 路线没被判死**；
而审计自己的 §6.2 把"把集合判定做软"列为 σ 冻结那条机理**唯一明确的候选靶子**
（内核里的一处不一致：人工密度早就是 `m_crit` 的连续斜坡，只有熵的集合判定是硬的）。
⇒ 与 `capture_select` **不同类**（那条被 K3 判死，旗标随路线走）。
⇒ 原则 4 在这里生效的是**前半句**（"必须写下它的删除条件"），而**那句话从来没写过**。

**已登记**（写在 `entropy.py` 的旗标旁边，与旋钮同生共死）：三条任一成立即删 ——
① 某个 `(eps, q)` 被采纳为默认；② 一轮**事前登记**的判定同时给出 **F5 类 FAIL** 与
**eps 敏感性仍非单调** ⇒ 参数化判死；③ **2026-12-31** 前无任何一轮使用 ⇒ 按不活跃删除。
**每次阶段收口必须复读本条**（`newton.py` 的驱动器 docstring 也指到它）。

★ **这是本轮唯一一条可被使用者一句话推翻的处置**（注册 §5 事前预测，命中）。
若认为该路线其实该判死，F2 就翻回"删除"，代价是 `test_s1b_entropy.py` 的 5 条锁与
两个 bench 脚本的可运行性。

### F3 ✓ PASS —— 而它自己的判据先被第八问抓到一个空洞

**G-V 先验证会红**（修那条烂路径**之前**跑）：

```
★ this tier's node list has rotted -- these paths do not exist:
  tests/test_b18_wingbody_transonic.py
```

⇒ 断言**确实能抓到真缺陷**，不是一条从没红过的锁。修后 `checked = 7 -> PASS`。
条目改为 `phases/p1/tests/test_b18_wingbody_transonic.py  [ARCHIVED]`，并注明
**保形那一半现在就在本层里**（`test_b32_wingbody_conforming_transonic.py`）。
校验在 `main()` 里**先于那 9 分钟**跑。

★★★ **本轮最值得记的一件事**：我的第一版守卫写的是 `assert checked >= len(LOCKS)`，
而 `LOCKS` 为空时那是 `0 >= 0` ⇒ **空洞通过**。**是本轮自己按注册 §1.3 写的
极端样本测试（`test_cannot_pass_by_being_empty`）在守卫上线之前抓到的**，不是事后。
⇒ **第八问（"判据在 n=1 / 全通过 / 全失败 / 空集上给什么？"）在它被提议的同一轮
就还清了自己的成本**，建议按审计 §9.5 升为常设。

### F4 ✓ PASS —— README 重写

- 不再把**已冻结的** phase-1 roadmap 称为 "active implementation roadmap"；
- **三条承重的环境要求全部就位**（`up3d` / `PYTHONNOUSERSITE=1` / `scipy>=1.12`），
  每条都带它已经造成过的**实测后果**（`import pyamg` 失败；81 failures + 50 errors）；
- 新增"从哪读起"表：能力边界 → **phase 4 唯一入口** → roadmap → 本阶段 progress →
  design → inspection；并明写 `phases/p1|p2` 是**冻结的历史不是计划**、
  其中 import `pyfp3d.wake` 的东西**不可运行**；
- 加上线程/墙钟/门控计数的三条引用纪律与当前两个基线；
- ★ **9 个相对链接逐条验证可解析**。

### F5 ✓ PASS（按注册的缩减范围）

已改两处过期文字：`run_capability_matrix.py::save_cp` 的 "for BOTH paths … conforming and
level-set curves"（D5 之后只剩一条路径），以及上面 F1 的 `append_row` 勘误。
★ **`post/unified.py` 本身不删** —— 它需要一个裁决（91 行、2 个消费者、输出与
`post/surface.py` 逐位相同），按 **kill criterion 第 5 条**（"任何一条修复需要一个新的裁决
⇒ 不自行裁决"）记 RECORDED 留给使用者。

### F6 ✓ PASS —— 网格终于可版本化，而它在第一次使用时就还清了成本

**交付**：`pyfp3d/mesh/manifest.py`（`mesh_fingerprint` / `write_manifest` /
`read_manifest` / `describe_difference`），并接在 `write_mesh` 里 ⇒
**10 个走项目写入器的生成器全部免费获得**。sidecar 是 `<name>.msh.manifest.json`，
**不碰 `.msh` 的字节**，且**按构造被 gitignore 放行**（`*.msh` 不匹配它）——
实测 `git check-ignore`：mesh IGNORED / manifest TRACKED。

★ 指纹里**故意不放**墙钟、mtime、主机名、生成器 git SHA —— 那些会让两张**逐位相同**的
网格比较不等，即回答了**另一个问题**。不可复现的溯源放 `extra`，不进 `sha256`（有锁）。
★ **自指检查**（第八问的另一半）：manifest 不是 `.msh` 的一部分，所以**不进自己的 hash**。

**F6-c，对着真实缺陷回放** —— 那对引发整件事的网格：

| | n_nodes | n_tets | wall 三角 | sha256 |
|---|---|---|---|---|
| `coarse.msh`（圆角） | 13 607 | **68 624** | **6 890** | `ee8c58fe6bc4` |
| `coarse_flat.msh`（平截） | 11 033 | **55 531** | **5 152** | `2463bcd32ff0` |
| `medium.msh`（生产 S4） | 80 754 | **448 197** | 27 068 | `1565a279b6ac` |

⇒ `DIFFERENT MESH -- … n_tets 68624 vs 55531; n_tris[wall] 6890 vs 5152`。
★★ **计划外的跨轮仪器核对，通过**：68 624 / 55 531 / 448 197 三个数**逐位复现**
phase 3 已记录的值（round-cap coarse 预算、F-MESH 那轮的 flat 腿、GS4.1 成本表的 S4），
而本模块与产生它们的代码**零共享**。

**20 张 gitignored 网格全部盖上 tracked manifest，2.9 s。**

★★★ **第一次使用就交出一条新事实**：那 20 个文件只有 **17 张不同的网格** ——
三对**逐位相同**却挂在两个不同族名下：

| sha | tets | 两个名字 |
|---|---|---|
| `2fdfaf71b6cd` | 59 359 | `onera_m6/coarse_ss.msh` ≡ `onera_m6_roundtip/coarse.msh` |
| `1565a279b6ac` | 448 197 | `onera_m6/medium.msh` ≡ `onera_m6_roundtip/medium.msh` |
| `4f6828336b83` | 9 303 | `onera_m6/xcoarse_ss.msh` ≡ `onera_m6_roundtip/xcoarse.msh` |

**独立复核**：`md5sum` 给出同样的配对（与本模块零共享代码）。
★ 而 9 303 / 59 359 / 448 197 **正是 GS4.1 立项文书成本表里的 S0 / S2 / S4** ⇒
**phase 3 的 S 阶梯与 `onera_m6_roundtip` 是同一批网格**，此前无法判定。
★★ **限定，不许读过头**：这是**关于本工作树**的测量，不是关于 git 的。两种解释
（两个生成器产出相同 / 某个目录被复制过）**本轮没有分离**，故记 **RECORDED 不作归因**。

---

## 2. 守卫

| 守卫 | 结果 |
|---|---|
| **G-B** 套件对平 | ★★ **479 → 499 passed，skipped 12 与 xfailed 2 一个没动**，0 failed，477.27 s @8 线程。**+20 = `tests/test_gs40_provenance.py`，逐条点名**。★ 改动前**独立重跑过 479 基线**（472.99 s @8t，逐项吻合）⇒ 这是**干净的增量**，不是重定基线 |
| **G-N** 零数值影响 | **PASS** —— `test_v0_freestream` + `test_s1b_entropy` + `test_p8_newton` + `test_mesh_reader_roundtrip` = 66 passed / 3 skipped |
| **G-D** 干跑校验（返回数 + 参数类型） | **PASS**，**在花任何机时之前**：7 种形状（空集 / 首级死 / 中途死 / 插入重试级 / 到达目标 / post 腿失败 / 单级），键集恒为 3、类型恒为 `float\|None` + `bool` |
| **G-S** 语义用源码比对 | **PASS**（`d224223:newton_ls.py:1186-1193` 引在 §1 与代码注释里） |
| **G-V** 断言先验证会红 | **PASS**（见 F3） |
| **G-SELF** 自指 | **PASS** —— 路径校验读 LOCKS/NOT_COVERED 与文件系统、**不扫自己的源码**（`pgrep -f` / G-Z / G-L 三次的那一族）；manifest 不进自己的 hash |
| **收口第 0 步** 快速能力锁层 | ★ **5/5 绿，553 s（9.2 min）@8 线程，load 2.3** —— 在记录的 526–891 s 带内。新的 node 校验行随之打印 |
| **收口第 6 步** grep 判据措辞 | 无判据被退役或重定（GS4.0 不动任何门的措辞） |

**Kill criterion：五条全不成立。** 总机时 ≈ **25.4 min**（基线 473 s + 套件 477 s +
锁层 553 s + 定向 20 s + manifest 3 s），在注册的 30 min 闸内。

★ **事前预测的核对（注册 §5，不改预测）**：
① F1-b 守卫会开火 —— **命中**（G-V 见红）；
② F6-c 会通过 —— **命中**；
③ N ≈ 10–14 —— ★ **落在区间外，实测 20**，如实记，不改预测。多出来的是我事前没算进去的
第八问那三条极端样本测试与 `describe_difference` 的两条；
④ F2 是唯一可能被使用者推翻的处置 —— **命中**（它确实是唯一一条）。

---

## 3. RECORDED（本轮**没做**，各带理由）

| # | 事项 | 为什么不做 |
|---|---|---|
| ~~R1~~ | ~~`run_capability_matrix.py` 的 CSV 模式不一致~~ | ★★ **已于 2026-08-17 结清，见 §5 附录**（使用者裁决：收尾时一并做掉） |
| ~~R2~~ | ~~`post/unified.py` 的去留~~ | ★★ **已删 2026-08-18（使用者裁决）** —— 分派层的存在理由随 D5 走了；**删前先验证** docstring 的「`np.array_equal`」声称（M6 机翼 M0.8395：cl **0.2775363765023681** 两边逐位相同，cf/cp_tri/section 全 `array_equal`）；四个成员里 `wall_cp` 与 `_conforming_wall_state` **零消费者**、`wall_forces` 是重复、`section_cp` 是一行透传。★ 顺带删掉一个**够不到所以从没被抓到**的坏分支：`save_cp` 仍在构造 `dict(mvop=, phi_ext=)` 而那两个关键字在 phase 3 塌缩时已被移除 ⇒ 真跑到会 TypeError，现改为显式 `assert mf is None` |
| ~~R3~~ | ~~三对同 sha 不同名的网格~~ | ★★ **已结清 2026-08-18（使用者裁决「检查并处理」）：是 (a) 生成器等价，不是目录复制** —— 零机时，从源码定死。`onera_m6` **默认钳制** `h_far=min(2.5,120·h_wall)`，为的是让 `coarse/medium/fine` **逐位不变**（它们扛着 P5/G8.2/B7/M1 的回归锁），并另定义 `RICHARDSON_LADDER=(xcoarse_ss, coarse_ss, medium)` **关掉钳制**；而 `onera_m6_roundtip` **从一开始就不钳制**，它自己的注释原文：*「its `coarse` is what the shipped onera_m6 family needs a `coarse_ss` for」*；2026-08-04 起两族的非 `_flat` 级都是 `tip_cap="round"`。⇒ **同一构造、同一参数、同一字节，逐位相同是预期不是巧合**。★★ **后果：phase 3 的 S 阶梯从来没有跨网格族风险** —— S0/S2/S4 = 9 303/59 359/448 197 就是 `RICHARDSON_LADDER`，用哪个目录名都是同一批字节。★ **`onera_m6_roundtip/` 因此是冗余的**（三级都可由 `onera_m6` 的 `_ss` 名复现），但**按使用者裁决网格保留**；事实已**盖进 6 份 manifest 的 `extra`**，在使用现场可查，不必再从判定里翻 |
| ~~R4~~ | 四个**零消费者**的网格族 | ★ **裁决 2026-08-18（使用者）：网格保留，不删。** 记录在案：它们当前无活的消费者（`fuselage_bor` 曾服务 phase 1 的 GV3.3 机身算例）|
| R5 | ★★ **产品层仍然整层缺失**（CLI / 输入文件 / 极曲线 / 阻力分解与阻力门） | 注册 §0.2 **明确排除** —— 那是 GS5.2/5.3 **整个阶段**。**GS4.0 收口时它们仍然缺失**，这句必须留在判定里，免得"审计的问题都解决了"被读成"产品能力好了" |
| R6 | ★★★ **五条产品指标一条都没动** | 注册 §0.2 **刻意** —— 修仪器不等于修能力 |

---

## 5. 附录（2026-08-17）：R1 已结清

使用者裁决"收尾时一并做掉"。**缺陷**：`bench/gate_results/capability_matrix.csv` 的表头是
**19 列**，而脚本的 `keys` 自 2026-08-03 起是 **24 列**，`open(CSV, "a")` **只在文件不存在时
写表头** ⇒ 下一次运行会把 **24 个字段写在 19 字段的表头下**，产出一个列名与内容对不上的文件。
★ 归类：**第 5 问的又一层** —— 前面问的都是"我在比的两个**数**是不是同一个东西"，这里是
"我在写的两个**行**是不是同一个 schema"。

**处置：拒绝，不自动修复。** 两条理由都是项目已经付过学费的：改写表头 =
**就地改写已提交证据**（G3 类危险，`run_m3_budget` 当初改成写自己的文件名正是为了躲它）；
静默开新文件 = 把一次测量**劈成两个互不指向的产物**。⇒ 报错并给出两个**由操作者选**的出路
（把已提交 CSV 按日期挪开 / 本次运行指向新文件名），并明写**不要就地改表头**。

**验证（与 F3 同一条 G-V 纪律：先在真缺陷上见红）**：

```
★ .../capability_matrix.csv has a 19-column header but this script now writes 24 columns;
  appending would put fields under the wrong names.
  columns this file has never seen: ['m_attained','accept_reason','res_unfrozen','f_final','descent10']
```

—— **恰好是 2026-08-03 加的那五列**。同一测试再验**好路径未被误伤**：新文件正常写表头、
两行 24 列往返一致。锁：`test_capability_matrix_refuses_a_stale_schema`。

**套件**：**500 passed + 12 skipped + 2 xfailed，0 failed，465.24 s @8 线程**
（499 + 1 = 500，skipped/xfailed 仍未动）。库改动仍为 **0**（R1 只碰 `bench/`）。

---

## 4. 一句话

**六条活缺陷全部处置，套件账目在三个数上同时对平，而 GS4.0 没有、也不该有推动任何一条
产品指标——它买到的是"下一次测量不会被自己的仪器骗"，代价 25 分钟机时；
下一个决定（进 GS4.1 还是先做 M4 的 case 入口 / 体内梯度重构）仍然是使用者的。**
