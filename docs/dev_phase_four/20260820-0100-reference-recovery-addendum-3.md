# 20260820-0100 GS4.1 第七轮 — **执行附录 #3**：★★★ 我**注册了对的判据，实现了弱的那个**

**性质**：执行附录，**重执行前**提交。★★ **判据一个字没动** —— 恰恰相反：
本附录是**把实现改回注册时写的那条**。

## 1. 发生了什么

预注册 §3 的 **R-STATIONS** 原文：

> 重生成的**表面站点 `x`** 与已提交的**逐一对应**（到该列存储精度）

而我的实现是：

```python
"PASS" if (len(up) == len(ru) and len(lo) == len(rl)) else ...
```

—— **只比了行数，没比 `x`**。于是 140+140 通过，R-REPRO 拿着**错位的站点**往下比，
报出 415/560 外、最差 388 ulp。

**实测 `x` 其实对不上**：LE 附近逐位相同（1e-5, 8e-5, 2.2e-4 …），
向 TE **发散到 `max|Δx| = 1.75e-3`**（例：上表面倒数第三站 **0.99082** vs 已提交 **0.98962**）。

## 2. ★★★ 这个失败模式值得单独命名

前三个 bug 都是"读外部格式读错了"。**这一个不是** ——
**判据本身写对了，是我把它实现成了一个更弱的版本。**

⇒ 常设一句：**预注册里的每一条判据，实现时要逐字回读一遍它自己的措辞** ——
"逐一对应"不是"数量相等"。★ 而它的危险性正在于：**弱实现会通过**，
于是下游判据拿着错的前提继续跑，产出的数字**看起来像结论**（415/560）。

## 3. 后果：R-REPRO 的读数**作废**

`415/560`、`430/560`、最差 388/437 ulp 是在**两组不同站点**之间比出来的
⇒ **不可解释，撤回，不得引用**。

## 4. 改了什么

R-STATIONS 增加它注册时就该有的那一半：**逐站比 `x`**，容差 = `x_c` 列的存储精度
（6 位小数 ⇒ 1e-6），并打印 `max|Δx|`。

★ **不去凑 paneling**。预注册 §3 事前写死了反向结果的落点：

> R-STATIONS 失败 ⇒ 整轮停在 R-STATIONS，报"参照的 paneling 无法从供给源码复现"，**不往下比**。

**照此执行。** 调 PPAR 直到 paneling 对上，就是按目标输出搜自变量。

## 5. 记账

- R-REPRO 的两行读数**撤回**（§3）；R-STATIONS / R-POLAR / R-FIELDS 的读数**保留**；
- 机时：三次执行合计约 10 s。
