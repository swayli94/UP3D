# 20260821-1100 — **GS4.1 第十五轮判定**：E-CF 已降为 RECORDED；**G18 判 UNDEFINED**，而它测出了一条更清楚的

**性质**：判定。判据、守卫、kill criterion 逐字来自
[预注册](20260821-1000-round15-prereg.md)（**代码之前提交**）。**`pyfp3d/` 零改动。**

---

## 0. 一句话

★ **① E-CF 降级完成，`window.csv` 逐字节相同、只有一个 verdict 变**（G-NOREWRITE 已证）。
★★ **② G18 按我自己注册的措辞判 UNDEFINED**（一侧过一侧不过），
**而它顺手测出一条比原问题清楚得多的读数**：**转捩间隔那一站的 `Cτ` 两侧都高 16.2 %，
且两侧一致到 0.05 pp** —— 而同一站的 `H` 第十轮测得只差 0.45 %。

## 1. ① E-CF 降级（E-DEMOTE **PASS**）

| | |
|---|---|
| 改了什么 | **只有判定码**：`E-FAIL` → `RECORDED (demoted 2026-08-21; was E-FAIL at 43/70)` |
| ★ **G-NOREWRITE** | `window.csv` **逐字节相同**；`summary.csv` 的 `measured` 列**逐行相同**；**恰好一个** verdict 变 |
| 方法 | `git stash` 换出降级、同库跑一次、`git stash pop`，**降级前后同一库** ⇒ 不是跨时间比较；★ pop 后**哨兵复查**（`DEMOTED` 计数 0 → 1） |
| 降级理由 | 全部**已在册**（预注册 §①）：更忠实 ⇒ 一致变差；平板那段在同族参照上**无对应**；两条相关式的适用范围**无可引出处** |
| ★★★ 记在脚本里的禁止句 | **不**意味着"平板侧没问题"，而是"**平板的红不能归给闭包,因为它的参照不合格**" |
| ★ 重开条件 | 补进**适用范围可引**的 ZPG 参照，或带 BL 剖面的实验 |

★ 三处 bench 脚本（第六、八、九 A 轮）全部带上降级说明，**指向同一处理由**，不各写一套。

## 2. ② G18：**按注册判 UNDEFINED**

判别子 = 第十轮的干净层流种子（从最后一个纯层流站起步、`x_tr` = **我们自己设的** `x/c 0.05`，**不扫**）。

| 站 `x/c` | side_a `Cτ` 比 | side_b `Cτ` 比 |
|---|---|---|
| **0.0537**（转捩间隔站） | ★ **1.1616 OUT** | ★ **1.1621 OUT** |
| 0.0586（第十四轮 P-SEED 那一站） | **1.0086 IN** | ★ **0.9788 OUT**（第十四轮 0.9618） |
| 0.0639 | 0.9596 OUT | 0.9440 OUT |

**预注册的三分**：进带 ⇒ (2)；不进带 ⇒ (1)；**一侧过一侧不过 ⇒ UNDEFINED，不报方向。**
⇒ **side_a 进、side_b 不进 ⇒ UNDEFINED。**
★★ **我不挑我偏好的那一读** —— 这一条正是为此写的。

★ **但有一个可引用的量**：干净种子把 side_b 的差**从 3.82 % 减到 2.12 %**
⇒ **(2)（区间处理）是一个已量化的实际贡献者，约占一半**，**而不是全部**。

## 3. ★★★ 未被要求、但更清楚的一条：**转捩间隔站的 `Cτ` 两侧都高 16.2 %**

| | side_a | side_b |
|---|---|---|
| 转捩站 `Cτ` 比 | **1.1616** | **1.1621** |
| 该站层流侧种子的 `H_k` | 2.5680 | **2.3960** |
| 同一站的 `H`（第十轮 T-C1'） | 0.45 % | 0.45 % |

★★★ **两侧的 `H_k` 差 0.17，而 `Cτ` 比一致到 0.05 pp。**
**种子公式的误差会随 `H_k` 变**（它是 `exp(−CTRCEX/(H_k−1))`，对 `H_k` 强依赖）——
**这个比不随 `H_k` 变** ⇒ **这是一个判别性的测量,不是推理**，且它**支持 (2) 而不是 (1)**。

★★ 并且同一站 **`H` 只差 0.45 %、`Cτ` 差 16.2 %** ⇒ **积分方程侧对得上，滞后的初值对不上** ——
与「种子在**整站**上取，而 XFOIL 的在**区间分段内部**取」完全一致。

★ **但它不能回填 §2 的判定**：注册的判据问的是**转捩后首站**的比，答案是 UNDEFINED。
**新读数登记为独立的 RECORDED，不追认注册判据。**

## 4. 事前预测的兑现（**不改预测**）

| # | 预测 | 实测 | |
|---|---|---|---|
| 1 | ★★ W-SEED 落在 (2) | ★ **判定落空（UNDEFINED）**；★★ 但 §3 那条**未被要求的读数独立支持 (2)**，且它是**判别性的**（不随 `H_k` 变） | **判定落空 / 方向被支持** |
| 2 | 降级后数一位不变 | ★ **命中**（逐字节） | ✓ |
| 3 | 零求解 < 5 min | ✓ | ✓ |

⇒ 本季累计落空 **38 次**，一次都没有事后改预测。
★ 预测 1 我事前写了「**这正是第十四轮说过「是推理不是测量」的那条推理，本轮把它变成测量，它可以落空**」——
**判定确实落空了**，而**方向反而被一条我没预料到的测量支持** ⇒ **两件事分开写，不合并成"我说对了"。**

## 5. 登记给后续

| | |
|---|---|
| ★★★ **G19（新，取代 G18）** | **转捩间隔站的 `Cτ` 高 16.2 %（两侧一致到 0.05 pp）** —— 比 G18 的 3.8 % 清楚得多、两侧可复现、且**已排除随 `H_k` 变化的种子公式误差**。真答它要么实现 `TRDIF` 的区间分段，要么在种子处对区间内位置做加权 —— **两者都是库改动，本轮不做** |
| **G18** | **关闭为 UNDEFINED**，其内容被 G19 取代且加强 |
| **E-CF** | RECORDED，**重开条件已写在脚本里** |
| 承前 | G4 / G6 / G7 / G9 / G11 / G12 / G15 / G16 / G17 / F1 / F2 / F3 |

## 6. 实测口径

- 零 FP 求解；**`pyfp3d/` 零改动**（G-FROZEN-LIB）⇒ 套件承 **569 / 12 / 2**，快层承 **5/5 绿 593 s @8t**。
- ★ **G-ONECHANGE**：G18 与第十四轮之间**唯一的差别是行进的起点**，并在输出里打印了这一点。
- ★ 降级用 `git stash`（项目认可的方法，不碰 index），**pop 后哨兵复查** —— 本项目为 `git checkout -- ` 丢过工作。
