# pyFP3D 能力边界(phase two 收尾交付 · 2026-08-09)

**这份文件的用途**:phase three 接手时,**先读这一份**。它回答三个问题 ——
**求解器现在能做什么**、**哪些路已经走死(别再走)**、**哪些亏空还没解释(是真正的工作面)**。

**这不是**进度报告(那是 [progress.md](progress.md)),**不是**计划(那是 [roadmap.md](roadmap.md))。
它只陈述**被测量支撑的**边界,并对每个数字标注它**依赖什么**。

**权威口径**:每条都指到提交的产物,一个只活在 .md 里的数字不算证据(roadmap §3 原则 6)。
★ **2026-08-10 的整理改了产物的位置,没改产物本身**:仍被保留代码读取的 CSV 留在
`bench/gate_results/`(9 个),**其余 244 个 phase-2 产物在
[`phases/p2/bench/gate_results/`](../../phases/p2/bench/gate_results/)**(含 `capability/` 的 189 个);
phase-1 的证据链在 [`phases/p1/cases/`](../../phases/p1/cases/)。**全部仍被 git 跟踪** ——
归档是目录重组,不是取消证据。

---

## 0. 先说三句最要紧的话

1. ★★ **有一个指标被测量证明"本模型类不可达",而不是"还没做到"**:M3a(M6 七站 Cp
   RMS < 0.05)。四点全尺度加密序列测出**无粘模型地板 0.0516–0.0707** ——
   我们是无粘求解器,参照是 Re 11.7e6 的**有粘**实验。⇒ 它是**粘性能力**的靶子,
   不是网格或离散的靶子。
2. ★★ **在这个网格范式里做不出一次单变量实验**。四个旋钮全部被实测抓到越界
   (`h_wall`、`h_edge`、`h_far` 近体 +24%、**`r_far` 近体 +67%** —— 而 `r_far`
   根本不属于尺寸场)。⇒ "加密哪里"这类问题**连诊断都很贵**,这是 phase three
   转向六面体贴体网格的**直接实测依据**(§7)。
3. ★★ **最大的未解释亏空是 M6 的 LE 带那 70%**,三个候选**全部被否证**
   (Kutta 形式 ✗、LE 分辨率 ✗、几何 ✗),而已知它由**体网格分级**主导、
   **且加密不朝实验收敛**(位移 0.098–0.143,误差只动 −1.4%…+3.4%)。

---

## 1. 五条产品指标:目标 / 实测 / 为什么 / 可达性

口径:目标数字**一个都没有放松过**(M3 那次只改**归属**,不改数字)。

### M1a — 包线内的网格收敛性 ✓ **达标**

| | |
|---|---|
| **工况** | NACA0012 **M0.72 / α1.25**(M_max ≈ 1.17,即 S1 测定的包线内) |
| **目标(重定后)** | 三级 `(xcoarse, coarse, medium)` 上 cl **单调** + **收缩**(比率 < 0.7)+ 末步变化 < 5% |
| **实测** | cl **0.222742 / 0.242984 / 0.253237**;比率 **≈0.506**;末步 **≈4.2%**;三级 0 lim / 0 flr |
| **锁** | `tests/test_s1_m1a_envelope.py`(**常开**,4 项,8 s) |

⚠ 比率与末步只引到三位:测试文件里同一对量有两处记法(重定表 0.5056 / +4.214%,
阈值注释 0.5065 / 4.22%)—— 是同一测量的不同取整,**不是分歧**;判据是 < 0.7 与 < 5%,
两种记法都过,所以**不去挑其中一个当"准确值"**。

★ **为什么判据换了**:原判据写在 **medium→fine** 上,而 `fine` 已按裁决退出正式阶梯
(成本 345–1561 s/点,对 2 CPU·min 的目标无意义);新阶梯上原判据是 **+4.21%**,
**不成立**。所以按"重定门单独一轮"的规则重定为**趋势判据**(单调 + 收缩 + 末步),
它在新阶梯上被实测支撑。
⚠ **这是漂移锁,不是正确性声明** —— M0.72 没有外部真值锚点,测试注释里写明了这点。

**可达性:已达。** 这是目前唯一一条"求解器在自己的包线内表现得像个收敛的离散方法"的证据。

### M1 — 二维无粘标定 ✗ **FAIL(三条里一条通过)**

| | 目标 | 实测(2026-08-05 重测,熵修正默认 ON) |
|---|---|---|
| (a) 激波位置 | 0.61 ± 0.02 | coarse **0.6073** / medium **0.6006** —— **两级都在带内 ✓** |
| (b) 两级 cl 差 | < 3% | **−16.2%** ✗(等熵是 +101.4% ⇒ 改善 **6.3×**,★ 符号也翻了) |
| (c) `upwind_c ∈ [1,3]` 内 cl 变化 | < 3% | coarse **9.4%** / medium **17.9%** ✗(等熵 36.1% / 根本不收敛) |

★★ **上表是 `n_picard_seed = 5`(与等熵记录同配方,故 like-for-like)。生产默认是 seed = 0,
而在生产默认上这三条的状态更弱,必须分开说**:
- 重测当天(2026-08-05,**种子回退落地之前**)seed = 0 的 medium **不收敛**
  (撞 `m_cap`,7265 lim / 758 flr)⇒ (b)(c) 在 medium 上**无法评估**;
- 随后的 **`clamped` 触发的自动种子回退**把这个案例救回来了(M1a 重定时实测 M0.80 三级
  **全部收敛、0 clamp**),但 ★ **回退后的 NACA M0.80 medium 收敛性是线程依赖的**
  (8 线程失败 / 16 线程通过 —— `tests/test_seed_fallback.py` 只在**被接受的那条分支**上
  断言收敛,无条件断言的是残差改善 ≥ 1000×);
- ⇒ **已于 2026-08-10 复测(§3.6)**:16 线程下生产默认的 **(a) 两级都 PASS**
  (coarse 0.6195 / medium 0.6006),**(b) −16.3%** 与 **(c) 12.6% / 无法评估** 仍 FAIL;
  ★ 那条 medium 的 PASS 是**回退**做的功(与 seed = 5 逐位相同),而 **8 线程上它不收敛** ——
  所以引用 M0.80 medium 必须带线程数。

**为什么**:GS1.1 在有解析解的 Laval 喷管上把机理量清了 —— 激波位置误差 ≈ **0.35·C 个单元**、
**在 h 上近似常数**,所以 h 是唯一杠杆;而翼型的病态是
**亚单元激波位置误差 × dcl/dx_shock ≈ 2/弦长** 的放大。要 (b)(c) 达标,需要把激波位置
控到 **±0.0055 c**,即 `h_shock ≲ 0.005–0.01 c`。
★ 而 GS1.2 的 Q2 **测不出来**:在那个分辨率上驱动器**到不了工况**
(fine 在 α1.25 约 M0.75 就关不掉,且 `n_floored = 0` —— 不是钳制)⇒ **鲁棒性随加密变差**。

**可达性:在当前离散上不可达。** 剩余差距 (b) **5.4×** / (c) **6.0×**。
唯一还开着的修法是**登记项 1「激波定位」= 体内梯度重构**(S2 的噪声图已排除"棱柱层能一起修好":
最噪单元只有 0.6–6.8% 落在第一层)。
⚠ 措辞更正:"跨声速解不可控"**已不准确** —— 等熵时代是 33×,现在是 5–6×。

### M2 — 二维粘性标定(RAE2822 case 9/10) ✗ **未在本阶段动**

第一阶段 GV5.2 实测:**每个激波偏后 0.06–0.10 c**、**1/4 腿收敛**、**无可信 cd**;
且**不随加密改善**。**可达性:未测定** —— 前置是 M1(激波位置)与粘性重启(S4 未开始)。
**不得**在 M1 未达标前评价它。

★★ **前置已测一条(GS4.1 第一轮,2026-08-18,phase 4)** ——
[判定](../dev_phase_four/20260818-2300-gs41-strip-core-verdict.md):
粘性闭包家族(`closures.py`)在**层流平板**上的自相似不动点离 Blasius 有一个**已量化的偏置**:
**`H` +4.52 %、`c_f√Re_x` +6.94 %**。这**不是离散化误差**(条带核自收敛阶 4.04、误差 1.3e-13,
低 11 个数量级;代数不动点根本不含离散化),而是**家族本身的性质** ⇒ 它是 M2/GS4.3/GS4.4
的**输入偏置**,一切下游标定都带着它。

★★★ **而这个数一直在盘上,只是没有任何判据去比它**:
`bench/studies/v1_ibl3_standalone/results/gv1_1a_march.csv` 的 `cf_march` 列(**层流**行进)
从 **+15.77 %**(x=0.30)单调收敛到 **+7.75 %**(x=2.10 末站),**窗内每一站都在 ±5 % 之外**,
并且收敛方向正是本轮量到的不动点 **0.710235(+6.94 %)** ⇒ 两轮相互印证。
**phase 1 没有报它,是因为没有门覆盖它**:GV1.1 的门 (a) 比 `H`(FAIL 于 3.76 %),
门 (b) 比的是 **湍流** `c_f`,**层流 `c_f` 对 Blasius 从来不是一个门**。
★ 门 (b) 的**结构性**问题另算且仍成立(`|c_f_FE − c_f_march|/c_f_march = 0.07 % PASS`,
**两臂共用同一闭包 = 共模比较 ⇒ 看不见共享的模型误差**),★★ **但它是湍流读数,
不得与任何层流数字并列** —— 那是跨流态比较(第 5 问)。

★★★ **第九轮(2026-08-20)把这条边界推进了一步,而结论是分裂的** ——
[A 腿](../dev_phase_four/20260820-1400-five-terms-verdict.md) /
[B 腿](../dev_phase_four/20260820-1800-lag-xfoil-verdict.md):
条带核走的是**另一族**(`closures_2d.py`,Drela–Giles 拟合式),与上面那条型线族的偏置**无关**。
★★ **勘误(2026-08-21,第十轮)**:下面第一条的逐站计数是 **pre-x_tr-fix** 的。`CT` 列转捩前是放大因子(`xbl.f:821-822`),我把转捩点取早了两站;修正并把 `x_tr` 放到间隔内点后,**转捩间隔那一站被预测到 `H` 0.45 %**,而 `TRDIF` **不是缺项**(细分步连续行进已包含它) ——见[第十轮判定](../dev_phase_four/20260821-0000-transition-attribution-verdict.md)。**残余**:无自由度地把 `x_tr` 放在我们设的转捩点上,side_b 进带、side_a `H` **2.39 %**,另有 3–8 处出界在转捩后弛豫段。
- **能力上限(已测)**:给定 XFOIL **自己的** `u_e`,条带核在 NACA0012 `Re 3e6, α 2°` 上
  逐站复现 XFOIL 的 `θ/H/c_f/Cτ` 到 **2 % 以内**,**除了转捩后 `x/c 0.049–0.089` 一小段**
  (`θ` 177/216、`H` 208/216、`c_f` 206/216、`Cτ` 204/216 在带内);
- ★★ **滞后方程已交付并已验证**:滞后臂对 XFOIL 自己的 `Cτ` 比平衡臂近 **2.8×**(弛豫段)/
  **13×**(全湍流段),41/43 站 —— **相对判据,不含任何挑出来的阈值**;
- ★★★ **但这不是模型验证**:XFOIL 是**同一族**相关式 ⇒ 一致只说明**实现**对。
  **真正的模型验证需要一个带边界层剖面的实验**(实测 `Cp` 给 `u_e` **且**实测 `θ/H/c_f`),
  而 `cases/reference_data/` 的三个实验参照(`onera_m6`/`rae2822`/`naca0012`)**全部只有 `Cp`**
  ⇒ **这是一条外部数据请求,不是计算量问题**,并且它**挡在 M2/M3a 的粘性验收前面**。
- ★ ~~转捩后那一段的候选成因是 `TRDIF`~~ **已测掉(第十轮)**:`TRDIF` 是 XFOIL 两点隐式格式
  所需的**离散装置**,我们的细分步连续行进本来就在间隔内任意点切换 ⇒ **非缺项**;
  那道差距**主要是转捩点放错**。真正未答的是 **XFOIL 两点隐式格式本身**(G12)。

★ **一条只作参考、不得当作结论的对照**:GS4.3 的验收带自己写着 **`cf < 15 %`**,
而这个偏置是 **6.9 %** ⇒ *指示性地*看,它在下游门自己的容差之内。
**但这是跨算例比较**(层流平板对 Blasius **vs** NACA0012 M0.5 对 XFOIL)⇒
**只能读作指示,不能读作"GS4.3 已经安全"** —— 否则就是本文件反复记的那个缺陷家族本身。

### M3a — 三维有粘对实验 ✗ ★★ **测量为"无粘不可达"**

| | |
|---|---|
| **目标** | M6 **M0.8395 / α3.06**,七站 Cp RMS **< 0.05**(数字未放松) |
| **实测** | T1 全点 **0.1007** |
| ★ **地板** | 四点全尺度加密序列(h 0.030/0.020/0.015/0.010,**全部严格收敛**)测出 **MID/TE 存在加密消不掉的地板**;重建全点地板 **0.0516–0.0707** |
| **分布** | **LE 带收敛到零**(阶 0.709)、**激波位置收敛到实验**(阶 0.845)、地板在 **MID(79%)/TE** |

⇒ 那 0.05 的差额里有一块是**模型**的。**归属已改为 S4 / 粘性**(2026-08-02 重定,数字不变)。
**可达性:无粘路线上不可达;粘性路线上未测定。**

### M3b — 三维无粘速度 ◐ **medium 达标 / 大网格未达标**

| | |
|---|---|
| **目标** | 同算例 **< 60 s**(数字未放松) |
| **实测** | M6 medium M0.84 爬坡 **49.9 s**(参照 `direct` 125.4 s)⇒ 该网格**达标** |
| **大网格** | T1(1.16 M tets)~~**950 s**~~ ★ **勘误 2026-08-14:实测 3 950.4 s**(`phases/p2/bench/gate_results/tip_allscales.csv` 的 `solve_s`;GS2.1 结果 .md 记 4 011 s 含生成) —— 形状像掉了一个 "3"。**差距是 66×(不是 16×)** ✗ |

⚠ **两处如实标注**:① 49.9 s 是 M0.84/α3.06 的爬坡,与 M3 条件(M0.8395)实质同一算例,
但**未在 M3 的精确条件下单独复跑**;② ★ **任何固定的 EW forcing 是标定不是保证** ——
1e-6 在 350k 上过、在 **1.16 M 上失效**,换根阈值**随网格移动**。
★ 另:**种子占墙钟 28.3%**(14.44 s)—— 曾被我误读为"0.00 s 无可省",因为读的
`timings` 只含最后一层,整条 ramp 在 `timings_total`。GS3.3b 仍开着。

### M4 — 翼身组合体粘性单点(含生成网格) ✗ **未在本阶段动**

审计实测 **3.5 h 且不收敛**,目标 **< 2 CPU·min**。**可达性:未测定**(前置是 M2/M3a + S4)。

### M5 — 包线健壮性 ✗(一条子句已达成)

M5 本身(单配置 20 点)**仍未度量**。相邻的更广测量:能力矩阵 **13 配置 × 阶梯 = 78 点** ——
★ **这是删除前的读数,已不可复现**(2026-08-10):那 78 点里含 **10 个 level-set cell**,而 `bench/run_capability_matrix.py` 的 LS cell 与 dispatch 分支已随 D5 删除(分支改为显式 raise)。committed CSV 作为**历史读数**仍然有效,但该脚本此后只测 conforming ⇒ 引用 78/72/0 这三个数时必须带「pre-deletion」限定。 ——
**收敛 72/78 = 92.3%**、**clamp 0/78 ✓**、**M_max > 1.5 共 4 点**;不收敛的 6 点已分型
(5 个真停滞 + 1 个 σ 输运,后者已定为假失败并修好)。

★★ **但 M5 的第三条子句("无 M_max > 1.5")现在读的是伪影**,这本身是本阶段的发现:
生产的**圆角**翼尖帽在 **M∞ = 0.5** 下给出 **M_max 1.4022**,而同一配方的平截帽是 **0.7726** ——
**亚声速来流下的伪超声速斑块**,即 P13 翼尖自由边奇点被圆角帽"解了出来"。
⇒ 与 roadmap §3 的禁止项一致:**M_max 在 P13 未处理前不能当能力指标**。

---

## 2. 已排除的路线(带实测依据;重开需要新证据)

| 路线 | 结论 | 实测依据 |
|---|---|---|
| "耗散公式与物理尺度解耦" | **REFUTED** | GS1.1:Laval 喷管上位置误差 O(h)、C 敏感性 O(h)、误差 ~1 个单元、对抖动不敏感 |
| 显式处理 fold / 弧长延拓 / bordered / deflation | **前提消失** | GS1b.2 Q4:**没有 fold**,`M∞_max` 是**延拓步长的极限**不是极限点 |
| 去掉马赫爬坡 | **更负** | GS3.3a:无爬坡 **222.8 s 不收敛** vs 爬坡 **70.8 s 收敛** |
| LE 局部加密 / LE 棱柱层 | **负** | GS2.1:LE 误差**不受 LE 分辨率控制**(最好 −8.3% 且**非单调**);几何已排除;噪声图:最噪单元 0.6–6.8% 在第一层 |
| 换网格解决 M3 | ★★★ **证据基础失效 2026-08-15(不是结论被证反)** | 原依据:MID/TE 地板 **0.052–0.071 SUPPORTED**;四点外推说 LE 上 **< 0.08 不可达**。★★ 而 phase 3 的**自收敛(Cauchy)读数**在生产 round-cap 的**干净因子-2 射线**(h_far 显式 = 120·h_wall)上实测:**LE 上表面的相邻两级之差 `d_self` 随加密 *增大*(0.241693 → 0.292833,+21.2 %),且已达 e 自身的 1.22 倍** ⇒ **那个序列没在收敛**,而"< 0.08 不可达"正是**从它外推**出来的 ⇒ **外推不成立**,靶子可达与否**现在未知**。★ 非收敛是**局部的**:LE 下与 MID **正常收敛**(d 与 e 同时缩小); (★ 勘误 2026-08-15:此处"正常收敛"依据的是**「d_self 在两对之间缩小 + e 缩小」**这个判据,**不等于** `R = d_self/e ≤ 1/3` —— 实测 LE 下 R = **1.72–1.76**、MID 上 **0.638**,**整个数据集没有任何 band 的 R ≤ 1/3**。见 [20260815-2000 判定 §4/§6](20260815-2000-nearsingular-partition-verdict.md))而 **TE 两面 d 缩小却 e 单调变差**(TE 上 0.0294→0.0392→**0.0716**)= **收敛到离实验更远处 = 模型误差签名**(n=15/5,RECORDED)。⇒ 下一个问题是「**为什么 LE 上表面不收敛**」,与预算/分配/阶数都不同类。见 [判定](../dev_phase_three/20260815-0000-common-pointset-verdict.md) |
| "LE 亏空是 Kutta 形式误差" | **自设规则否证** | 先写解读规则再看数字,规则否掉了假设 |
| **修远场以减小 M3 误差** | **否掉** | 第三次因子 addendum #4:位移最大的那一腿误差**升 3.4%** |
| **"远场 vs 近体"的二分实验** | ★ **不可测(有界负结论)** | 本轮:`r_far` 15→30 MAC 把**近体 r 1–2 MAC 动 +67.33%** |
| 等参 P2 壁面单元 | **不采用** | 裁决 D2:生产级全速势求解器都不用,复杂度过高 |
| mapped-P1(超参)曲面壁面单元 | **测负** | 第一阶段 P11:球面 11.56% → 11.33%,mapped-P1 在二次几何上**丢线性再现**,O(h) 抵消 O(h) |
| G1.6 的四条老修法(h 加密 / 恢复微调 / Nitsche / 边界数据修正) | **已排除** | 第一阶段 G1.3/G1.4 oracle |
| **level-set 尾迹路线** | ★★ **放弃(裁决 D5)** | 见 §6 |
| LS 侧翼尖治法 | **测负** | 第一阶段 B31 C-class(C1 内侧回流 −19.5% / C3 coarse 发散) |
| 谱展向 Γ 平滑 | **死路** | 第一阶段 |

---

## 3. 仍未解释的亏空(这一节就是 phase three 的工作面)

### 3.1 ★★ M6 的 LE 带那 70% —— 最大的一个

**事实**:M3 的误差预算里 **LE 上表面占 69.6%**(RMS **0.2361**,medium),MID 20.7%。

★★★ **勘误:这个数已过期,待在 HEAD 上重测(2026-08-13,[判定](../dev_phase_three/20260813-0700-m6-triage-verdict.md) §2/§4)** —— 它的唯一出处 `bench/gate_results/m3_budget.csv` 提交于 **2026-07-31 16:09**,而生产 M6 配方此后**改过三次**:**2026-08-02 `12b9e3d`**(GS3.1 翻成 `amg`)、**2026-08-05 `3631f6f`**(G8.2 重锚到**圆角帽 + 生产 taper**)、2026-08-10 `1a41cec`。实测:在 HEAD 上按同一条腿重跑,medium 给 **cl_p 0.2493 而不是 0.2765(−9.8%)且不收敛**,LE 上表面 RMS 给 **0.2953 而不是 0.2361**。⇒ **69.6% 与 0.2361 已退役,不得再当作在用的数引用。** ★★ **替代读数(2026-08-13,HEAD:圆角帽 + 生产 taper,medium,收敛 |R| 7.33e-15)**:**LE 上 70.35 %(RMS 0.2422)、MID 上 21.05 %(0.0983)**、LE 下 5.02 %、TE 上 2.42 %,全点 pooled 0.1323;cl_p 0.272994 / cl_KJ 0.277167(**带 taper 的 −1.3 % 模型偏差**)。见 [判定](../dev_phase_three/20260813-1100-m3-remeasure-verdict.md)。★ 与退役的 69.6 %/20.7 % **不可比**(几何与配方都变了);新数落在同一位置是一条**稳健性观察**,不是「旧数被验证」。 ★ 最可能的主因是 08-05 的圆角帽/taper 重锚(量级相称),但 `.msh` 是 gitignored、无法核实当日的 `medium.msh` 是圆角还是平角帽 ⇒ 标为**最可能**,不作断言。★ 后果:**任何以「LE 占 70%」为前提的工作都要等重测**;而重测本身是独立一轮(它要动 `run_m3_budget` 的重跑与整条勘误链)。
S2 的中间靶就是把它做到 **< 0.08**。

★★★ **phase 3 任务 ③ 的收口陈述(裁决 D7,2026-08-16)—— 这一节的最终状态**:
1. **可达性 = 未知**。旧的"不可达"是从**一个没在收敛的序列**做四点外推;而 HEAD 上的自收敛读数是
   **LE 上表面的 `d_self` 随加密增大**(0.2417 → 0.2928,**R = d_self/e = 1.2245**)⇒ **外推不成立**。
   ⇒ **既不得写"不可达",也不得写"可达"**;
2. **非收敛的位置已定死**:**前缘 x/c ≈ 0.004–0.008**,而**整条 LE 带都在超声速泡内**(泡从 x ≈ 0.005 起);
   其 d² 质量在七站上**普遍**集中于 x < 0.05(87.9–96.6 %),幅度沿展向**在 η0.90 见顶后回落**;
3. **候选已逐条测负**:Kutta 形式、几何、弦向错位、**尖端(剂量响应 + η0.96/0.99 样本外两次)**、
   局部弦长、实验点陡度、**σ(σ ON/OFF 的 d_self 比值 1.000)**、**激波足迹**(声速线 0.371–0.649,
   **不在** LE 带内)、Γ / Γ/c;★ 而"**LE 分辨率已否证**"这一条**降级**(它建在平截帽腿上);
4. ★ **仅解释约一半**:近奇异内区以 **41.7 %** 的点占 **74.9 %** 的 d² 质量、去掉它 d_self 减半,
   **但外区仍不收敛**(R = 0.91)⇒ **另一半没有机理**,而它**偏外侧**;
5. **固定单元预算下,把单元搬进 LE 带是真杠杆但会饱和**:落在族自身误差-单元数曲线**下方 ~17 %**,
   在 `h_edge` ≈ 生产值的 **1/2–2/3** 处见底,代价是 **MID 上 +8…+13 %**;
6. ★★ **重开条件**:须先给出**外区那另一半**的机理候选;而"**提高阶数**"仍受 **裁决 D2** 约束
   ⇒ 走它要**显式推翻 D2**。

★★★ **2026-08-15:这条靶子的"不可达"判定其证据已失效**(见 §2 的"换网格解决 M3"行)——**不是**它变可达了,而是**支撑"不可达"的四点外推建立在一个没在收敛的序列上**。★ 同轮把非收敛**定位到 LE 上表面**(LE 下与 MID 正常收敛),所以 §3.1 这条亏空现在有了一个**更准的名字**:**LE 上表面的解在加密下不收敛**(`d_self` 增大 21.2 %),而不只是"误差大"。

**已否证的三个候选**:Kutta 形式 ✗、~~LE 分辨率 ✗(GS2.1)~~、几何 ✗。

★★★ **2026-08-14 更正:「LE 分辨率 ✗」撤到 RECORDED** —— GS2.1 第一步的腿走 `run_le_response.build`,其 `BASE` 是 **`tip_cap="flat"`**,而 CLAUDE.md 记着 **LE 带收敛阶 0.37 flat / 0.87 round**:**平截帽正是 LE 加密看起来没用的地方**。在**生产 round cap** 上、**固定单元预算**下重测:把 `h_edge` 减半并抬 `h_wall` 补偿(65 653 tets = coarse 预算 −4.3 %)使 **LE 上 RMS 0.372531 → 0.312736**,= **恢复 coarse→medium 级差增益的 46 %**,并落在该族自身误差-单元数曲线**下方 16.9 %**;反向(单元搬向外场)**+11.5 % 落在曲线上方**。★ 限定:两条腿差**帽型和预算补偿两件事**,不能把差异全归帽型 ⇒ 留一条 ~20 s 的 round-cap LE-only 检验。

★★ **2026-08-14 续测(轴的事前确认,[判定](../dev_phase_three/20260814-1600-nearbody-axis-verdict.md))**:在**事前登记**的近体占比轴上以单一自变量 `h_edge` 扫 5 档(固定预算 68 624 tets,G-R 逐位复现控制腿),结果**非单调 = 分配会饱和**:vs 族曲线 **+18.7 / 0.0 / −16.1 / −16.9 / −13.2 %** ⇒ **优势在 `h_edge` ≈ 0.010–0.0075(生产值的 1/2–2/3)处见底,再细退回**;★ 最好的固定预算腿 **0.312736 仍比 medium 高 29.1 %** ⇒ **分配把精度/成本曲线下移 ~17 %,不替代单元数**。★ **代价已定价**:MID 上 **+8…+13 %** 变差(而全带池化仍 **−14.7 %** ⇒ 是精度,不是 band 间搬家)。★ 那条 round-cap LE-only 检验**已做 = 降幅 14.0 %**,是 flat 的 1.7× 但**低于事前 15 % 线一个点** ⇒ **RECORDED,不宣称,不挪线**;它独立地落在族曲线下 **10.2 %**,即分配效应在**第二个预算级别**复现(★ 但与固定预算腿**不是同一个干预**,不可比出"优势随预算衰减")。见 [判定](../dev_phase_three/20260814-1200-fixed-budget-verdict.md) §6。

★★★ **2026-08-14 补(只读轮,[判定](../dev_phase_three/20260814-0700-p2-reassessment-verdict.md) §5)**:候选表还缺一条,而它是唯一被测到**能降低对实验误差**的 —— **按比例的全尺度加密**:`phases/p2/bench/gate_results/tip_allscales.csv` 有**四条**腿(h_wall 0.030/0.020/0.015/0.010,46 k→1.16 M tets,全部收敛 0 lim/0 flr),**LE 上 RMS 0.3899 → 0.2704 → 0.2358 → 0.1758,四级单调,共 −54.9 %**(池化 −49.4 %)⇒ GS2.1 §7.2 那条事前样本外预测(单调 + coarse 明显更差)**被满足**,而它此前被记为"未做"。 ★★★ **勘误 2026-08-14 addendum #1:这四条腿是 `tip_cap="flat"`**(`run_tip_allscales.py:49` 导入的 `run_le_response.build` 里 `BASE` 写死 flat),而生产族是 **round** ⇒ 按 CLAUDE.md 的标准规则"**平截帽上任何基于加密的论断前提为假**"(P13/G13.3:平截帽在加密下发散,LE 带阶 **0.37 flat / 0.87 round**),**此读数降级为 flat-cap 专属,生产行为 UNMEASURED**。见 [addendum #1](../dev_phase_three/20260814-1000-fixed-budget-addendum1.md)。★ 限定三条:**阶乱且低于一阶**(0.902/0.476/0.725)、**解本身在动**(M_max +43.2 %、cl_p +12.1 %)、**不可接到 HEAD**(2026-08-01 的同代码腿)、★★★ **跨网格族(flat vs round)**。★★ 与本节上文**不矛盾但必须分开读**:下文那批**单旋钮**腿主导的是 Cp 的**位移**(81 %),而它们对**实验误差**只动 −1.4 %…+3.4 % ⇒ **起作用的是尺度按比例一起动,不是把体网格单独调细**。★ 代价:−54.9 % 要 **25.2× 单元 / 549× 墙钟(7.2 s → 3 950 s)** ⇒ 对 **2 CPU·min** 不是产品答案。**等参 P2 = 已被裁决 D2 否决(2026-07-28,§2 表)**,而 2026-08-14 的只读评估**与它同向**(穿透 4/4 条内核契约);★ 该评估**曾把它写成「已评估-未开 + 立项条件」**,那是**漏读 D2** 造成的框架错误,已更正 —— **重开 = 推翻 D2 = 使用者裁决**,不是某条判据能自动满足的。★ 它按构造免疫 P11 的失效机理且抬场阶,这一点仍如实记录。

**已知的正面归因(第三次因子,判据 P 达成)**:它由**体网格(含近体分级)**主导 ——
只动"远场"那一腿把 LE 带 Cp 移动 **0.09811** = 全尺度加密 **0.12067** 的 **81%**
(判据要 ≥ 60%),且是其自身 LE 污染上界的 **10.4 倍**;对照条款**未触发**
(MID 0.114 / TE 0.092)⇒ 是 **LE 特有的**,不是全局位移。

★★ **同时被否掉的推论**:同一批腿对**实验**的 LE 误差 **0.28214 → 0.29171(+3.4%,更差)/
0.28017(−0.7%)/ 0.27810(−1.4%)** —— **位移 0.098–0.143,误差只动 −1.4%…+3.4%**。
⇒ **解在大幅移动,却几乎不朝实验去。**

★ **两处必须一起读的更正**(否则会读反):
① `h_far` 不是远场旋钮,它把近体 r 1–2 MAC 动了 **+24.04%** ⇒ "远场加密使误差 +3.4%"
实为"**近体粗化**使误差 +3.4%",而**粗化近体让 LE 吸力峰变差毫不意外**;
② 因此"解在游走"这句要**弱化** —— far-only 那腿并非纯加密。干净的"加密不收敛"证据
仍来自 GS2.1(−8.3% 非单调)与四点外推(< 0.08 不可达),**不来自那一轮**。

**剩下的候选**(P11 已把一条测过):P1 场在吸力峰的**固有能力**;体内梯度重构(登记项 1)。
★ 而"远场 vs 近体"这条探针**已被本轮关掉** —— 测不了。

### 3.2 激波定位(登记项 1)

五族判别子已排除,并有阻塞性结论;共同修法 = **S2 的体内梯度重构**。

★★★ **退役 2026-08-17（GS4.0b 探针，kill 条 1）**：这句「共同修法」**不再是可用前提**。零求解探针（固定收敛 φ、只换恢复算子）判 **G-SMEAR** —— **线性 patch 重构与纯平滑分不开**（B1 与 S 的薄片重尾比值差 **6.6 %**，注册线 20 %；`mean|g_B1−g_S|` 只有 `mean|g_B1−g_A|` 的 **17.5 %**，而换 patch 动 61.9 % ⇒ **输出由 patch 尺度决定，不由「重构还是平均」决定**）。★ **准确措辞**：这**不是**「体内梯度重构没用」—— **二次阶从未被测**（该臂未过 G-CONS），**SHOCK 分区为空**故「激波是否保住」从未可测，而后处理探针**本来只是必要条件**。★★ **同一批数据给出一条反向 RECORDED**：**小 patch（面邻居）重拟合**使声速集**显著更相干**（≥10 面份额 **0.191 → 0.428**、最大分量 47 → **118**、出现 **8** 个 ≥50 分量，而控制臂 0 个）⇒ **重开条件 = 对它做一轮事前登记的测量**。见 [判定](../dev_phase_four/20260817-0700-gradient-probe-verdict.md)。
它与 M1 的 (b)(c) **同根**:亚单元激波位置误差被 dcl/dx_shock ≈ 2/弦长放大。

### 3.3 ★ LE 线的全部结论只在 **M0.75** 上成立

窗口测定说 **M0.75 是可用的最高马赫**(有激波、加密腿仍 0/0 clamps),而 **M3 的条件是 M0.8395**。
**M0.75 → M0.8395 的外推未测。** 这是引用 §3.1 任何数字时必须带的限定。

### 3.4 b7 的不对称(假设已登记,未测)

同一个圆角帽,**尾迹嵌入体网格的 M1 受伤远重于无尾迹的 M4**(M4 亚声速仍 5/5)。
假设:**M1 的尾迹自由边正终止在翼尖**,圆角改了那处几何于是放大 P13。
测它需要给 `onera_m6_wakefree` 生成 `_flat` 变体(**目前不存在**)。

### 3.5 P13 翼尖自由边奇点本身

生产 taper 是**缓解不是解决**(G8.2 重锚:M_max 2.929 → 1.997)。它是
**M_max 不能当能力指标**的原因,也是 §1 M5 第三条子句读伪影的原因。

★★★ **一般化 2026-08-18(GS4.0k,判据缺陷 #13 的代价)——这一条被推广,因为它又咬了一次:**

> **凡是「超声速区上的极值」这一类的量,都继承 P13 这个阻塞。**
> 不只是 `M_max`:**任何**取 `max`/`argmax` 于 `{M ≥ 1}` 的构造 —— 包括后来才发明的 ——
> 在 P13 被处理之前,读到的都是**翼尖自由边伪影**,而不是它名字所指的东西。

**代价已实测**:一个「激波足迹 = 壁面带里 `M ≥ 1` 的最大 x/c」的度量,
在 **η 0.96/0.99 两站给出 ≈0.99(后缘)而独立裁判给 0.44** ——
**误差 0.54–0.56 弦长,是该度量报出的 D(~0.01)的 50 倍**;
而**七站的中位数把它藏了三轮**,直到一条为**换尺子**写的守卫(G-SUB)偶然把它翻出来。
⇒ **三轮位置读数作废**,见 [dev_phase_four/20260818-1900](../dev_phase_four/20260818-1900-subcell-verdict.md)。

★ **这条禁止项此前只写在 roadmap §3 的「不得用 `M_max`」上**,措辞绑在**那一个量的名字**上,
所以一个**新造的、名字不同的**极值量没有被它覆盖 —— 上面的推广就是为了堵这个口子。
★★ **它挡住的是什么,要说准**:它挡住**位置正确性的测量**与 **M5 第三条子句**;
它**不挡 GS4.1** —— GS4.1 的验收在**平板/Blasius/自相似解**(无翼尖),
而其三维应用侧已登记 **R3 的尖端掩蔽**(`r_c = 0.05·b_semi`,`build_wing_case` 已实现)。
**不要把它读成"phase 4 要先解 P13"。**

★ **重开位置测量的三条前置**(任一度量都适用):① **掩蔽或排除尖端带**;
② **逐站报,不用中位数**(中位数刚藏了三轮);③ **足迹/位置的定义不得取极值**。

### 3.6 ✓ **已填(2026-08-10)** —— M1 在生产默认上的读数

原文这里记的是"M1 的三条判据没有在生产默认上复测过"。**已补测**
([20260810-1500](../../phases/p2/docs/dev_phase_two/20260810-1500-m1-production-seed-verdict.md),预注册在先):
生产默认(`n_picard_seed = 0` + `clamped` 自动回退,**16 线程**)——
**(a) 两级都在带内 PASS**(coarse 0.6195 / medium **0.6006**)、
**(b) −16.3% FAIL**、**(c) coarse 12.6% FAIL / medium 只 1 条收敛腿、无法评估 FAIL**。
⇒ M1 仍 FAIL,但**这个 FAIL 现在是生产路径的读数**,且 (b) 第一次是合法比较(两级都真收敛)。

★★ 但那条 medium 的 PASS **是回退做的功**:`fallback_fired = True, fallback_seed = 5`,
落到的状态与 seed = 5 那条腿**逐位相同** ⇒ **不得**写成"seed 0 收敛了"。

★★★ **勘误/加限定(2026-08-11,phase 3 任务 ③ 实测,
[判定](../dev_phase_three/20260811-2100-task3-nonuniqueness-verdict.md))**:上面 (a) 的"两级都在带内"
**是单次抽样,不是稳健读数**。同一网格、同一配方、**只换 `n_picard_seed`(取 {0, 5, 12})**:
cl 撑开 **32.11 %** = (b)(c) 的 **3 %** 判据的 **10.7 倍**,x_shock 撑开 **0.1683 c**
= (a) 的 ±0.02 带宽的 **8.4 倍**;三个解各自都是 `|R| ≤ 3e-11、0/0 clamps` 的**真根**
⇒ 这是**离散多解**。
★★ **限定(2026-08-12 勘误,原文写的是"两族都如此、非结构族更糟",那是错的)**:
上面这些数是**混合族**(phase 3 新生成器)的。**本文件所引的非结构族**上,
跨种子 cl 只差 **0.21 %**(coarse,三种子诊断口径 2.80 %)—— **在 3 % 之内**;
而 **medium 在 8 线程下 0/3 种子收敛**(σ ON 与 σ OFF 都是)⇒ 那里 (b)(c) 无法评估的理由是
**没有解**,**不是**解不唯一(16 线程下收敛,0.6005923629)。
原错因:散布计算漏了 `converged` 过滤,把钳制垃圾态算进了散布。
★ **范围限定,免得读成"本文件的数据已经显示了它"**:M1 门**只采 {0, 5}**,
而这两个种子在已提交的 coarse 上只差 **0.02 / 0.21 / 2.76 %**(cl_p)—— 都未超 3 %,
上面那个大散布来自**门从不采的 seed 12**。已提交数据里真正的警讯是
**coarse C=1.0 的 2.76 %,它用掉了 (b)(c) 全部 3 % 预算的 92 %**,而门**从不跨种子比**、
所以这个数从未被打印出来。★ 顺带独立验证:该腿跨种子 x_shock 差 0.0136 c × `dcl/dx_shock ≈ 2`
= 2.7 % 预测 vs **2.76 % 实测** ⇒ **GS1.1 的敏感度链成立**。
且线程数另贡献 **0.027 c**(medium/C=1.5:16 线程 0.6005923629 **收敛** vs 8 线程 0.5738 **不收敛**)。
⇒ **M1 的 (b)(c) 在 M0.80/α1.25 上不可测量,直到解选择被钉住**;
范围:包线内 M0.72 完全唯一(0.0002 c),α=0 近乎唯一(0.0088 c)。

★★★ **加限定(2026-08-12,phase 3 七轮 σ 调查的收口,
[判定](../dev_phase_three/20260812-2300-capture-selection-verdict.md) §6)** ——
"钉住解选择"这件事**已被系统地试过,并且测出它是一对取舍而不是一个可单独修的缺陷**:

> **熵修正的"幅度"是携带种子敏感性的东西。现有每一种手段(强度刻度 θ、软化宽度 eps、
> 指数 q、捕获选择规则)在压低种子散布的同时都在削弱修正;而唯一一种构造性
> 不削弱修正的手段(在已算出的 σ 场里"选择"而非"混合",输入侧逐位守卫 12/12 PASS)
> 没有压低散布 —— 在 binding 族上反而把散布从 1.21 % 推到 7.47 % 并丢掉一个种子。**

⇒ **就当前离散化而言,"唯一性"与"修正强度"是取舍。** 而它有明确边界:
包线内(M0.72)完全唯一;出问题只在**强激波 + 升力耦合同时在场**时。
★ 一并否掉的一条:"捕获到的 m1_max 散布"与 cl 散布**单调对应**(相关成立),
但**去动它并不能推动 cl 散布** ⇒ 它**不是杠杆**。

★ 而 **8 线程上那条腿不收敛**(`0.573794`),即"两级都在带内"这个头条**在 8 线程上消失** ——
已登记的线程依赖(§4.3)现在**落在一个已发表的数字上**。
⇒ **引用 M0.80 medium 的任何数字必须带线程数。**

★ 顺带量到回退的覆盖面:medium 的 C = 3.0 腿是 **`limit_cycle` + 0/0 clamps**,而回退的触发条件是
`clamped` ⇒ **按构造碰不到它** —— (c) medium "无法评估"正是因为这条腿,所以它是
**结构性**无法评估,不是"变化太大"。

**剩余差距**:(b) **5.4×**、(c) **4.2×**,修法仍是 §3.2 的体内梯度重构。

### 3.7 4.9% 的升力去哪儿了(登记项 5)

Kutta 估计子那一轮留下的:那 4.9% 的升力**加到哪儿去了**未测;合理猜测是**被遮罩的尖端区**。

---

## 4. 真实缺陷(代码里的,已知未修)

| # | 缺陷 | 为什么它危险 | 处置 |
|---|---|---|---|
| 1 | ★★ **软位移** —— 收敛、0 限幅,但**落到另一个解上**(M1a 的 fine 腿:−0.75%,d2 变号)。★★ **量级已在 2026-08-11 重测并大幅上修,2026-08-12 按族更正**:M0.80/α1.25 的**混合族**上**只换种子**即 x_shock **0.1683 c**、cl **32.11 %**,三个种子给三个都是 `\|R\| ≤ 3e-11、0/0 clamps` 的**真根** ⇒ 这不只是"位移",是**离散多解**([判定](../dev_phase_three/20260811-2100-task3-nonuniqueness-verdict.md) + 其顶部勘误);需要 **强激波 + 升力耦合** 同时在场(M0.72 唯一 0.0002 c、α=0 近乎唯一 0.0088 c)。★ **本文件所引的非结构族上要小得多**:coarse 跨种子 cl 仅 **0.21 %**,medium **0/3 收敛(散布无定义)** —— 那里的障碍是**收敛**而非多解 | **它不报任何错**;种子回退**治不了它**(触发条件是 clamped,不会开火);★ 而且**任何"两次独立运行"式的判据在原理上看不到它** —— 固定种子+固定线程的两次运行构造性逐位相同 | 需要**答案锚点**(M1a 那条测试就是),不是回退机制 —— 两件事分开处置。★ **并需要第三个种子 + 一条跨种子/跨线程的一致性诊断**(先只 RECORDED):门现采的 {0, 5} 在 coarse 上吻合到 0.02–2.76 %,看不到这件事 |
| 2 | `m_last_converged` / `m_final` 在**失败的 ramp** 上是 `None` | 调用方 `float(None)` 崩;实际代价 **40 分钟**的求解白跑 | 未修;调用方要自己防 |
| 3 | ★ **`seed_fallback` 的线程依赖**:8 线程失败 / 16 线程通过。★ **2026-08-11 补上量级**:medium/C=1.5 上 16 线程 x_shock **0.6005923629 收敛** vs 8 线程 **0.5738 不收敛** = **0.027 c = 4.9× M1 容差** —— 不只是"通过/失败",两者落在**不同的 x_shock** 上 | 不是回归,但意味着**答案依赖线程数** | 已在测试里写明;未修 |
| 4 | `G4.1` 的 `xfail(strict=False)` 现在 **XPASS** | ★ **非 strict 的 xfail 检测不到回归**;它是为**两个**理由设的,只有种子那个被修了(线程依赖与 0.118 c 配方依赖未动) | 暂不移除,理由已登记 |
| 5 | ★★ **四个网格旋钮全部越界** —— `h_wall`、`h_edge`、`h_far`(近体 +24%)、**`r_far`(近体 +67%)** | **旋钮的名字不是它的作用域**;生成器里**没有干净的单变量旋钮** ⇒ 受控网格实验做不出来 | 解耦尺寸场 = phase three(§7) |
| 6 | **EW forcing 是标定不是保证** | 1e-6 在 350k 过、**1.16 M 失效**;换根阈值随网格移动 | `DECISION-2026-08-02-precond.md` 记录了推翻条件 |
| 7 | `m1_gate_entropy.csv` 的等熵时代行 | 产于膝点修正、σ freeze、m_cap 逃逸修复**之前** ⇒ **不 like-for-like** | 保留为历史记录,引用时必须标注 |
| 8 | **两条尾迹路径的密度关系已分叉** | conforming 带熵修正、**LS 未接线** ⇒ 跨模型比较需重读 | 因 D5 成为死账(§6) |

---

## 5. 证据完整性的边界(读任何数字前必须知道)

1. ★ **生产 taper 带 −1.3% cl 的模型偏差**(medium 实测 −1.514% @M0.50 / −1.31% @M0.84)。
   **任何翼身/机翼升力数字都带着它。** 它不能去掉:去掉后 M0.88 medium 直接失败(6/6 clamps)。
2. **测试有三层,不要把快层当全覆盖**:
   | 层 | 内容 | 实测 |
   |---|---|---|
   | 常开全套 | `pytest tests/` | **700 passed / 28 skipped / 2 xfailed @16 线程 1668.59 s** |
   | ★ 快层 | `bench/run_capability_locks.py`(7 项能力锁) | **644 s = 10.7 min,7/7 green** |
   | 门控全套 | `PYFP3D_TRANSONIC_GATES=1 pytest tests/` | **720 passed / 2 skipped / 8 xfailed @3:04:44,8 线程,0 failed** |
   ★ 快层**每次都打印自己没覆盖什么**及各自成本(b22 medium ~35 min、p4 ~32 min、p5 45–75 min)——
   一个被误当成全覆盖的快层会**重造**它本要防的那个失效。
3. ★★ **四条 strict xfail 是故意的,不是欠账**:b7 三条 + b22 medium。
   在**生产的圆角网格**上那些门**没有可锚的解**(b22 medium:M0.60 就 2/1 clamps,
   M_max 从 3.31 涨到 M0.70 的 **15.26**),而锚一个带 clamp 的状态等于在那条测试里
   **作废 GS1.4「带限幅的状态不是解」契约**(roadmap §3 明令禁止)。
   **strict 是为了将来出现干净解时必须变红逼人重锚。**
4. ★★ **一个流程缺口的教训**:B28 在 demo 里退役了一个前提,**却没同步到测试** ——
   而那条测试是**门控**的,于是**躺了十七天**没人跑。⇒ 收口仪式加了**第 6 步:
   退役或重定一个判据时,要 grep 它在 `tests/` 里的出现**。
5. **墙钟不可比**:phase two 有整段在 **8 线程**约束下执行(以及一次他人负载 4 h 09 的门控跑),
   与 16 线程的记录**不能对比**。引用墙钟必须带线程数。
6. **位相同 A/B 需要同一 numba 缓存模式**(第一阶段纪律 12:缓存加载**不**位保真于新编译,
   差异在 `pyfp3d/viscous/` 里,约 1e-5)。
7. **CLAUDE.md 里的基线曾停在 479**,已更正为 **700**,门控债已记 —— 引用基线请用本节的表。

---

## 6. 裁决 D5 的代价(必须在没有交叉验证的前提下陈述能力)

D5(2026-08-09,使用者):**放弃 level-set,未来只开发 conforming**。
实测依据四条(汇总在 roadmap §7):① 上限**无优势**(B27:LS+clip 到达与 conforming
**同一上限位点**);② 答案**无优势**(B9 medium 两路径差 **0.4%/0.6%**、B27 跨模型 **2.4%**);
③ 成本更高(Track B **17 个阶段**处理这条路自己的病理,B14 还要单造 Schur+AMG 且小规模更慢);
④ ★ **在生产(圆角)配置上严格更差**(LS medium **每一级都带 clamp**、M_max 3.31 → **15.26**,
**无解可锚**;conforming 加 taper **仍到 M0.84**,\|R\| 6.155e-15、0/0;且 LS 驱动器
**没有 `tip_taper` 参数**、B31 的 LS 侧治法**已测负** ⇒ **赎不回来**)。

★★ **代价,如实记**:下面这些结论此后是**历史证据**,不再是**活的交叉验证**
(删掉 LS 腿之后只剩已提交的 CSV/PNG,**不可重跑**):

| 结论 | 数字 |
|---|---|
| P14 两条尾迹模型的跨模型一致 | **0.17% / 0.36%**(medium M0.84) |
| B9 翼身两路径一致 | **0.4% / 0.6%**(medium M0.5) |
| B27 跨模型 | M0.65 **2.4%** / M0.75 2.5% |
| A1 四驱动器成本对比 | 三维两条 Newton 都是**预条件子瓶颈**(~40% 墙钟) |
| **B5 远场域稳健性** | Γ 在 R ∈ {15,…,120}c 内 **0.45% / 1.09%** |

★★ **2026-08-11:这条代价已经生效** —— level-set 已删除(phase 3 第 1 件工作,9 库文件 /
4624 行),上表每一条此后**只剩已提交的 CSV/PNG,不再可重跑**。
⇒ **phase three 的能力边界必须在没有它们的情况下陈述**;此后唯一的外部裁判是
`cases/reference_data/`(**永不编辑**)。
★ 另两件"删除不是纯减法"的事:**`test_v2_newton_rhs_channel.py` 是粘性侧测试却走 LS 的
`b_base` 通道** ⇒ 删前要给 V2 的 transpiration 通道**补一条 conforming 等价测试**;
**`post/unified.py` 是塌缩不是删除**(它是设计好的两路径统一入口)。

---

## 7. 为什么转向六面体贴体网格(phase three 第 2 件工作;实质是激活搁置的 S6)

裁决 D2 当年把半结构贴体网格定为**后备**,启动条件是"**非结构路线被测量证明达不到 M2/M3**"。
下面是**已测的**依据 —— 逐条标明是测量还是推断。

| # | 依据 | 性质 |
|---|---|---|
| 1 | ★★ **做不出一次单变量网格实验**:四个旋钮全部越界(`h_wall`/`h_edge`/`h_far` +24%/**`r_far` +67%**),而 `r_far` **根本不属于尺寸场** | **测量**(本阶段两轮) |
| 2 | **LE 亏空由体网格分级主导**(判据 P,81%),而分级在非结构里**不可独立控制**(依据 1) | **测量** |
| 3 | **壁面精度的天花板**:G1.6 的"medium 上 2% max"被 P11 测为**任何 P1 场在任何网格上不可达**;结构/半结构的**正交壁面层**是 P1 场唯一还没试过的杠杆 | **测量**(P11)+ **推断**(正交层会有帮助) |
| 4 | **翼尖自由边奇点被四面体圆角帽解了出来**(M∞ = 0.5 下 M_max **1.4022** vs 平截 0.7726);贴体六面体在翼尖的拓扑是**已知可控**的 | **测量**(前半)+ **推断**(后半) |
| 5 | 大网格成本:T1 **1.16 M tets** / ~~950 s~~ ★ **3 950.4 s**(勘误 2026-08-14,见 M3b);同等壁面分辨率下六面体 DOF 少得多 | ★ **推断,未测** |
| 6 | 速度标尺 **BLWF 2 CPU·min** 本身就是**结构/嵌套网格**的成绩 | 外部事实 |

★★★ **2026-08-11 的实测更正(裁决 D6):上表依据 3 与"六面体在翼尖拓扑可控"的推断部分已被否。**
phase 3 任务 ② 的路线 (A) 实测:**壁面精度买不到** —— 壁面自由度精确匹配、且两族**壁面刻面几何
逐位相同**时,结构化正交分级只改善 max |ΔCp| **3.4–9.6%**、**收敛阶零改善**(两族 ≈1.0)
⇒ 误差由 **`O(h)` 刻面几何**决定;★ 而**平面刻面的六面体壁面一模一样**,所以这条动机
**对真六面体单元 (B) 同样被否**,只有曲面几何能动它(P11 已测负)。
**供体确定性也买不到,且那个问题本不存在** —— 非结构侧在收敛的包线内状态上已是 0 供体环、0 自指。
⇒ **依据 ①②(做不出单变量网格实验、分级不可独立控制)仍然成立且已被解掉**
(新生成器 G0 逐位单变量);**依据 ③④ 的推断部分作废**;**依据 ⑤ 仍未测**。
判定:[dev_phase_three/20260811-0500](../dev_phase_three/20260811-0500-hex-q2-verdict.md) 与
[20260811-1200](../dev_phase_three/20260811-1200-hex-q1-verdict-v2.md)。

★★ **必须写清的三条限定**:
- 依据 1/2 是**关于这个生成器和这些旋钮**的,**不是**"非结构不行"的全称判断;
- 依据 3/4/5 各含**推断部分**,上面已逐条标注 —— 不得引用成测量;
- roadmap 原则 8 要求 **kill criterion 写在开工那一轮**,**不许事后补** ——
  phase three 开六面体那一轮必须先写下"什么结果算这条路也走不通"。

---

## 8. level-set 的代码面(删除前的清单)

**完整清单**:[LEVELSET_DELETION_INVENTORY.md](LEVELSET_DELETION_INVENTORY.md)(AST 扫真实
`import`,不用名字子串 grep)。摘要:

| | 规模 | 要点 |
|---|---|---|
| **库** | **9 文件 / 4624 行** | ★ **更正 roadmap 的"约 2500 行",差 1.85 倍**;**全库只有 `post/unified.py` 一处 import 进 LS**,且它是设计好的两路径入口 ⇒ **塌缩** |
| **测试** | **18 整删 / 8 摘腿** | 摘腿的那 8 个**承载交叉验证**(§6)⇒ 不是纯减法 |
| **demo** | 14 整删 / 8 摘腿 | |
| **bench** | 5 整删 / 1 摘腿 | |
| **网格** | **只有 2 个** wakefree 族是 LS 专用 | ★ 我曾按名字误判 `onera_m6_roundtip` 为 LS 专用,**核实消费者后确认它是 conforming,留下** |

清单里给了**七步删除顺序**(先 demo/bench → 18 个纯测试 → 摘 8 个混合腿 → **先补 V2 的
conforming 等价测试** → 塌缩 `unified.py` → 库与网格 → 收口)。

---

## 9. phase three 接手清单

**顺序由使用者定(2026-08-09)**:

1. **删除 level-set**(§8 的清单 + 七步顺序);
2. **开发六面体贴体网格能力**(§7 的依据;★ 开工那轮先写 kill criterion);
3. **其余问题** —— 按本文件 §3 的亏空与 §4 的缺陷排序。

**接手时立刻可用的东西**:
- **门控节奏**:收口先跑**快层**(644 s / 7 项),阶段边界跑**门控全套**(3 h 04);
- **五条指标的自动脚本**都在 `bench/`(`run_m1_gate.py`、`run_capability_locks.py`、
  `run_capability_matrix.py`、`run_m3_budget.py` …),产物在 `bench/gate_results/`;
- **收口仪式六步**(CLAUDE.md);第 0 步 = 快层,第 6 步 = **grep 判据在 `tests/` 里的出现**。
