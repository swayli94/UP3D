# 第二阶段开发历史（每轮一行）

规则：每轮开发结束加**一行**。叙事写在轮次文件里，这里只放：日期、文件、
阶段/门、一句话结论、测试状态。判定只有 PASS / FAIL / RECORDED 三种。

计划见 [roadmap.md](roadmap.md)；轮次文件格式见 [_TEMPLATE.md](_TEMPLATE.md)。

| # | 日期 | 轮次文件 | 阶段/门 | 一句话结论 | 判定 | 全套测试 |
|---|---|---|---|---|---|---|
| 0 | 2026-07-28 | （无，见审计报告 [20260728-1330-full-audit-zh.md](../inspection/20260728-1330-full-audit-zh.md)） | 无门（审计） | 无粘亚声速健康；跨声速激波位置由调参和网格决定（同工况同参数加密网格 cl +40%）；速度差在预条件子（direct→amg 即 3.4×，答案不变）；粘性选型过重（IBL 比无粘贵 9 倍）；门禁体系自洽但不对外求真 | RECORDED | 10 failed / 641 passed / 26 skipped / 2 xfailed @8 线程 1026 s |
| 1 | 2026-07-28 | [20260728-1435-phase-two-plan.md](20260728-1435-phase-two-plan.md) | 无门（立项） | 建立第二阶段纲领：五条产品指标 + 八条原则 + S0–S6 阶段划分；第一阶段 roadmap 冻结为历史 | RECORDED | 未改代码，同上 |
| 2 | 2026-07-28 | [20260728-1520-s0-foundation.md](20260728-1520-s0-foundation.md) | S0 / GS0.1–0.3 | wheel 从 1 个 py 文件修到 56 个（9 子包）；网格脚本 11/11 可独立运行；numpy 1.x/2.x 兼容；5 处位相同断言改 1e-12、1 处无锚点历史锁改数量级守卫；`bench/` 基线 + `bitcheck` 工具建立（自检 10/10 逐位一致） | **PASS** | **0 failed / 651 passed / 26 skipped / 2 xfailed @8 线程 892 s** |
| 3 | 2026-07-28 | [20260728-1640-s1-shock-bench.md](20260728-1640-s1-shock-bench.md) | S1 / GS1.1 | 判据台建成（Laval 喷管，解析解 + 唯一性证明）。**S1 原问题陈述被推翻**：出厂 walk 通量算子一致——位置误差 O(h)、C 敏感性 O(h)、~1 单元、对网格抖动与 α=0 生产网格同样成立。新发现：`rho_floor` 会制造机器零残差伪解（4 数量级 A/B）。翼型病态 = 亚单元激波误差 × dcl/dx_shock ≈ 2/弦长的放大；要 M1 需把激波位置控到 ±0.0055c | **PASS**（台子）+ **REFUTED**（原归因） | 未重跑（库代码零改动，`bitcheck` 10/10 逐位一致） |
| 4 | 2026-07-28 | [20260728-1825-s1-resolution.md](20260728-1825-s1-resolution.md) | S1 / GS1.2 | Q1 **PASS**：激波位置误差 ≈ 0.35·C 个单元、在 h 上近似常数 ⇒ h 是唯一杠杆，要 ±0.0055c 需 h_shock ≲ 0.005–0.01c。Q2 **FAIL（测不出来）**：在那个分辨率上驱动器到不了工况——fine 在 α=1.25 约 M0.75、α=0 约 M0.79 就关不掉（`n_floored=0`，不是钳制），**鲁棒性随加密变差**。⇒ 把原 GS3.3（去掉马赫爬坡）提前为 S1 前置 | Q1 PASS / Q2 **FAIL** | 未重跑（库代码零改动，`bitcheck` 10/10） |
| 5 | 2026-07-28 | [20260728-1930-s1-gamma-amplification.md](20260728-1930-s1-gamma-amplification.md) | S1 / GS1.3 | 闭合行放大 **A ∝ h^(−0.56)**（亚声速 M0.5 就已 15→22→33，无激波）；两个独立 Kutta 实现给出同一答案与同一加密敏感性（cl 差 ≤0.3%，+40.5% vs +40.4%）⇒ 换估计器无用。拼起来：升力误差 ∝ h^0.44 ⇒ 只加密要 ~350×（不可行）；**闭合行做成 h-无关 + 激波更锐，两者都做则 ~3.7×（可行）**。★ 我的判据表把'触发条件'与'该做的动作'混为一条，动作前提未检验 | **PASS**（测量）+ 判据表自身有缺陷 | 未重跑（库代码零改动） |
| 6 | 2026-07-28 | [20260728-2030-s1-ptc.md](20260728-2030-s1-ptc.md) | S1 / GS1.2b | PTC **被否证**（我的第一版 boost 安全阀跑飞，已诊断记录；SER 用 R_0 归一化在 churn floor 上无效）⇒ 按原则 4 **删除** `ptc_lambda0` + 死代码 `m_lumped_free` + 从未调用的 `ptc_dtau`，newton.py 净 −11 行、旋钮数减一。真答案是**马赫爬坡可以删掉**：目标马赫直接解复现爬坡答案到 5–6 位、快 2.8×，且 fine 上爬坡到不了的工况它到得了。★ 由此补上 GS1.2 Q2 缺的数：**α=0 激波位置收敛（0.464/0.502/0.513，比值 3.3），α=1.25 升力发散（0.373/0.523/0.715）**⇒ 根因锁定在**升力耦合**，GS1.3b 升为 S1 关键项 | 判据1 PASS / 判据2 **FAIL** / 判据3 PASS（换手段） | `bitcheck` 10/10；p8_newton+p8_jacobian+v0 23 passed |
| 7 | 2026-07-28 | [20260728-2133-s1-closure-stiffness-and-envelope.md](20260728-2133-s1-closure-stiffness-and-envelope.md) | S1 / GS1.3b(1) | 矩阵量证实 `A_probe` 15→32（与 ε 扰动吻合 0.3%），并测出 **`pressure` 闭合行本来就是 h-无关的**（A 1.2–2.2，`D_raw ∝ 1/h` 补偿 `S_code→0`）⇒ 但两种行的加密跳幅完全一致（+40.4% vs +40.5%），**闭合行条件数不是发散原因**，GS1.3b 的实现步骤自我取消。★ 真变量是**激波强度**：三级 cl 收敛比 0.08/0.21/0.36/0.90 对 M_max 0.85/1.17/1.27/1.60 ⇒ **可信包线 M_max ≲ 1.2**（包线内 M0.72 外推 cl 0.2563，与文献量级相符）。**M1 工况落在包线外** | **PASS**（测量）+ 取消自己的第 2 步 | 未重跑（库代码零改动） |
| 8 | 2026-07-28 | [20260728-2215-s1-m1a-lock.md](20260728-2215-s1-m1a-lock.md) | S1 / GS1.5a + GS1.6 立项 | 按裁决 D3 执行：**M1a 能力锁落地**（`tests/test_s1_m1a_envelope.py`，3 常开 24 s + 1 门控 78 s，全过；M0.72/α1.25 三级 cl 0.242797/0.253351/0.255662，Δ₂ +0.91%，收敛比 0.219，外推 0.2563）；只锁**网格收敛性**不锁精度（该工况无外部参考，已登记为未结项）；**M1 原条件带发散数字原样保留**；`bench/` 新增 `m1a/*` 组并重新冻结基线（其余 7 行未动）。**GS1.6 熵修正立项**（含验收 4 条） | **PASS** | +4 测试（3 常开/1 门控）；库代码零改动，未重跑全套 |
| 9 | 2026-07-28 | [20260728-2301-s1-entropy-fix.md](20260728-2301-s1-entropy-fix.md) | S1 / GS1.6 → 作废 | **判据 0（动作前提）不成立，熵项一行未写**：喷管（精确解、无升力）在 M₁ 直到 1.60 都 O(h) 收敛（|err| 比值 0.458/0.490/0.526/0.531，误差 0.45→0.97 单元）⇒ 强等熵激波不是发散机制。★ 固定 Γ 判别：M0.72 冻结环量后 cl 的最后一步变化从 **0.91% 降到 0.04%**（20 倍）⇒ **场求解优秀，h 依赖几乎全在 Γ 的确定过程上**。M0.7875 那一腿因施加不相容环量而无效（设计缺陷已命名）。GS1.6 作废，接替者 **GS1.7 = 扫 Γ，分离场映射与闭合根** | **停止（判据 0）**+ 诊断 RECORDED | 未重跑（库代码零改动） |
| 10 | 2026-07-29 | [20260729-0010-s1-gamma-sweep.md](20260729-0010-s1-gamma-sweep.md) | S1 / GS1.7 | 扫 Γ 分离场与闭合：**场映射收敛**（散布 0.008–0.012c）；闭合方程 `F = T₀+(b−1)Γ` 的 **T₀ 与 (1−b) 同时 ~h^0.6 趋零**，根是两者之比 —— 弱激波同步消失故收敛、强激波抵消失效故发散；两种闭合实现的根一致到 0.13–0.17%（第三次确认无关）。方法自检：扫描根与耦合平衡值吻合 5 位 | **PASS** | 未重跑（库零改动） |
| 11 | 2026-07-29 | [20260729-0130-s1-clamp-not-silent.md](20260729-0130-s1-clamp-not-silent.md) | S1 / GS1.4 | 钳制契约化：缺口只有 `solve_subsonic_lifting`（Picard 种子），已修 + `clamped` 标志 + 4 个契约测试 + 伪解回归（物理支路对 floor 四数量级散布 **0.00e+00**）。★ 判据 4 逼出的副产品：既有失败的根因是**松耦合回路不可复现**（同 commit 两遍 max rel 1.024），该测试退役留证 | **PASS** | 0 failed / 657 passed / 27 skipped / 2 xfailed |
| 12 | 2026-07-29 | [20260729-0230-s1-closeout.md](20260729-0230-s1-closeout.md) | S1 / GS1.5 + 收口 | **M1 如实 FAIL**（脚本退出码 1）：激波 0.6581 带外、两级 cl **+101.4%**、C 扫掠 36.2%、medium 无收敛腿。**S1 收口**：交付一条测量出的能力边界 M_max ≲ 1.2 + M1a 锁 + **七条已排除路线** + 三条待裁决候选（A 显式处理 fold / B 非等熵移动 fold / C 接受包线） | **FAIL（如实）+ S1 收口** | 同上 |
| 13 | 2026-07-29 | [20260729-0330-s1b-fold-measurement.md](20260729-0330-s1b-fold-measurement.md) | S1b / GS1b.1 | 沿 M∞ 延拓测分支：报出 `M∞_max` >0.86/0.7925/0.7700 单调下降 ⇒ 判"fold 被确证"。★★ **结论已于同日被下一轮 Q4 推翻**（那是延拓步长的极限，不是极限点）；仍成立的是"同马赫 dΓ/dM∞ 随加密增大" + §1.1 的"最小奇异值不能当判据"论证 | **PASS → 撤回** | 未改库代码 |
| 14 | 2026-07-29 | [20260729-0430-s1b-entropy-premise.md](20260729-0430-s1b-entropy-premise.md) | S1b / GS1b.2（前提检验） | **Q1 PASS**：M₁=1.30/1.35/1.40 处等熵跳跃比 R–H 强 5.14/6.90/8.89%（实测激波强度 1.26–1.45 正落此段），且 **ρ_s = (p₀₂/p₀₁)·ρ_isen 精确复现 R–H**（0.0000%）= GS1b.3 的 0 维单元测试；★ 同轮纠正我自己的指数错误（密度削减 2.1–4.2%，非 5.1–10.1%）。**Q2 VOID / Q3 测不出来**（代理轴不成立，如实记，不给数）。★★ **Q4 推翻了 GS1b.1**：缩小延拓步长后三处"分支终止"全部走过（0.7925→0.8015 等）⇒ **没有 fold**。★ **Q5**：削弱激波（C 1.5→3.0）**不能**恢复 h 收敛（逐级 +40.5% → +29.4% 再 +28.2%），**且在 fine 上两个 C 几乎重合 ⇒ 人工耗散的削波作用 ∝ h、随加密自己消失** ⇒ 只能靠 h 无关的机制 = B | **Q1 PASS / Q2 VOID / Q3 测不出 / Q4+Q5 RECORDED** | 未改库代码 |
| 15 | 2026-07-29 | [20260729-0700-s1b-entropy-implementation.md](20260729-0700-s1b-entropy-implementation.md) | S1b / GS1b.3 | **熵修正实现并验收：A/B/C/D/F/H PASS，E/G 如实 FAIL，I 记录**。★★ 主判据 **F**：三级 cl 跳幅 **+40.5% → +5.73% / +11.46%**；**H**：1−σ 3.19→3.32→**4.38%**，修正随加密**增强**（人工耗散相反）；**B**：命中预注册的喷管位移 −0.379 UPSTREAM，同口径位置误差 0.2 单元；**C**：FD 2.57e-08，spread 118.5 ≈ 1/ε；**E FAIL**：σ 不动点极限循环（一单元进出激波后集合），封顶后 12 步收到 5.25e-12；**G FAIL（字面）**：激波每级被推上游但随加密仍向下游；**I**：0/0 结构仍在，但 T₀ 与 (1−b) 速率变匹配 = F 的机制。M1 仍 FAIL，**首次有判据通过**（(a) coarse 0.5992 进带；(b) +36.3%；(c) 6.8%）。旗标默认关闭，采纳待裁决 | **6 PASS / 2 FAIL / 1 记录** | 673 passed / 28 skipped / 2 xfailed，0 失败（910 s @8t） |
| 16 | 2026-07-29 | [20260729-1500-s1b-preshock-mach-fix.md](20260729-1500-s1b-preshock-mach-fix.md) | S1b / GS1b.3 修正轮 | **把默认翻成 ON 来量影响范围，一小时内抓出两个 bug**：① segfault（Picard 先读了未填的供体图，default-OFF 测试碰不到）；② ★★ **M₁ 取错、修正强 2 倍** —— 翼型气泡马赫非单调（峰值 1.408 @0.33c，激波 @0.63c 只有 ≈1.15–1.29），**Laval 喷管在结构上照不到**；被唯一红掉的 G4.1 逮住。改成走到**膝点**后：M₁ 1.3897 → 1.2682，激波 0.548 → **0.6186**（Euler 锚点中点 0.615，差 0.004c）；**判据 F 的 +5.73% 作废 → +13.79%**，fine 未到目标（新开放项）；喷管 B 仍 PASS。默认 ON 暂缓，先在正确量值下重测 H/G/E/I | **2 bug 修复 + F 重测（作废旧值）** | `test_s1b_entropy.py` 19 passed（+3：越界锁、无激波空转锁、开关有效锁） |
| 17 | 2026-07-29 | [20260729-1500-s1b-preshock-mach-fix.md](20260729-1500-s1b-preshock-mach-fix.md) §7 | S1b / GS1b.3b 追加 | **fine 的开放项关闭**："走不到目标"是 **8 线程的边际步假象** —— 16 线程下同一协议一路走过 M0.795；重跑后 fine 到达目标（cl_p 0.45760），coarse/medium 对线程数不敏感。**三级表补全（修正量值）：F = +13.79 % / +12.61 % vs 等熵 +40.51 %,逐级一致；H = 1−σ 1.598 → 2.964 → 3.058 %,不衰减且在收敛**；G 仍 FAIL（激波随加密仍向下游）。★ **新真缺陷：M₁ 探测器状态间不稳定** —— 延拓途中相邻马赫之间 M₁ 跳 1.26–1.49、σ 跳 0.932–0.986（收敛目标态上干净：1.271/1.347/1.352），与判据 E 的 churn 是同一对象 ⇒ **GS1b.4 靶子合并** | **F/H PASS,G FAIL,新缺陷登记** | 未改库代码（纯测量） |
| 18 | 2026-07-29 | [20260729-1700-s1b-smooth-sigma.md](20260729-1700-s1b-smooth-sigma.md) | S1b / GS1b.4 | **光滑化 σ = 测量出来的负结果,路线关闭**。把三处硬判断换成光滑过渡后，**S1 主判据 FAIL**：默认 eps_m=0.05 给 **6.590 pp**(比硬版本 4.6 pp **更差**)；唯一通过的 eps_m=0.02 只走出 3 个状态且修正极弱；**S5 强敏感且非单调**(同一状态三个宽度给 σ_min 0.9954/0.9652/0.9792)⇒ 按原文判定为拟合参数,不保留。已回退。★ **重新诊断成功:σ 继承供体图自己的 churn** —— 相邻马赫步换供体的单元 coarse **0.06%** / medium **0.34%**,而 σ 沿链求积,一个翻转改掉整条下游链;churn 随加密增长(σ 摆动 0.13 → 0.99 → 4.6 pp)。宽慰读数:**收敛态上 σ 干净且在收敛**,不稳定只伤延拓边际步 | **S1 FAIL(kill criterion 触发)+ 机制查明** | 19 passed(回退后) |
| 19 | 2026-07-29 | [20260729-1900-s1b-sigma-freeze-binding.md](20260729-1900-s1b-sigma-freeze-binding.md) | S1b / GS1b.5(a) | **把 σ 绑到 freeze 上 = 测量出来的负结果(T2 FAIL),已回退**。两个变体都实现并测量:**v1**(只在 freeze 事件重建)→ σ **陈旧**,medium 停在 0.78 且 M_max 2.3267;**v2**(live 后钉住 + 在 σ 自己量上的循环检测)→ medium 修好(+13.08%)但 **fine 坏**(停 0.775)。★ **(a) 之前的封顶策略是唯一三级全到的** ⇒ (a) 的前提被否证,与 GS1b.4 自洽(churn 在供体图里,这三个策略都没碰它,只是在'陈旧'与'跳'之间换哪一级坏)。回退后把那个 8 从驱动器参数改成**模块常数** `_SIGMA_REFRESH_MAX`(它不是调参旋钮,是止住实测极限循环的算法常数)—— 满足原则 4 本意。T3/T4 PASS(激波 0.6186 在 Euler 锚点内;19/19;Jacobian FD 2.76e-08)。★ 附带发现契约缺口:`usable()` 把 M_max 2.33 判为可用(m_cap=3.0),已登记 | **T2 FAIL(已回退)+ 前提否证** | **676 passed / 28 skipped / 2 xfailed,0 失败**(888.57 s @8t) |
| 20 | 2026-07-30 | [20260730-0000-s1b-onoff-recompute.md](20260730-0000-s1b-onoff-recompute.md) | S1b / GS1b.6 | **第一阶段 ON/OFF 双结论重算,B1+B2 完成**。**R1 PASS:OFF 腿把每个已提交数字复现到 +0.000%**;R2 范围排除已验证(无激波逐位相同 + LS 路径 grep=0)。B1:包线内(M0.72)最大 −0.28%,M1a 三级 +0.91% → **+0.67%** 仍过;M0.80 两级 cl 差 **+72.7% → +28.9%**,coarse 激波 0.6581 → **0.6200**。B2:门控套件 OFF **1F/701P**(那 1 个是既有失败,`main` 上同样红,已归因)、ON **5F/697P** ⇒ **翻默认会红 4 个 committed 锁**(G4.1 + 三个 P8)。★ 自我纠正:我先把 Picard/Newton 差 0.055c 读成"σ 策略依赖",两次测量推翻(σ 自洽度 4%;两驱动器等熵基线本来就差 0.054c,熵增量两条路一致 ≈ −0.04c)。B3 暂缓 | **R1/R2 PASS,影响面=4 锁** | 门控 OFF 1F/701P、ON 5F/697P |
| 21 | 2026-07-30 | [20260730-0200-s1b-driver-disagreement.md](20260730-0200-s1b-driver-disagreement.md) | S1b / GS1b.7 | ★★ **那 0.054c 是收敛差距,不是模型分歧 —— Picard 的"解"不是解**:\|R\| **2.20e-04 vs 5.46e-12**(差 8 个数量级);Newton 用 Picard 的 φ 做种子 **6 步就走掉**回到自己的 0.6581;Newton 残差**在 Picard 态上**就是 2.198e-04。而它报 `kutta_converged=True` —— 因为只收敛 Kutta 外循环(`tol_gamma=2e-4`),**没有场残差闸门**(10464 次迭代后仍 2.2e-04)。后果:**每个 P4/G4.1 数字都建立在未收敛态上**(cl 0.357196 vs Newton 的 0.458974 = **+28.5%**);G4.1 不是物理门;**交付基准应为 Newton 路径**;★ 两个误差在 G4.1 上部分相消(参考带为等熵后移留了余量,而未收敛把激波推向前)⇒ 那种"准"是假的 | **D1 PASS(归因具名)** | 未改库代码 |
| 22 | 2026-07-30 | [20260730-0400-s1b-residual-gate-and-g41.md](20260730-0400-s1b-residual-gate-and-g41.md) | S1b / GS1b.8 | **补场残差闸门 + G4.1 改走 Newton 路径**(使用者裁决)。`solve_transonic_lifting` 的上报拆成 `converged`(含 \|R\| < tol_residual)与 `engineering_converged`(旧语义,显式命名)+ `residual_final` + 原因;跨声速如实报 False(2.198e-04),亚声速不受影响(3.858e-09,余量仅 2.6×)。G4.1 移到 Newton 路径,旧 Picard 断言保留但标注**不是物理门**。★★ 验证时发现我只改了 coarse、**medium 那条门还在 Picard 路径上断言参考带并通过** —— 同样移到 Newton 后:**等熵不收敛(0.8819),熵修正收敛且落进带内(0.6146)** = 这条线最强的单条证据 | **E1–E6 全达成** | 非门控 676/28/3;门控 2F/700P(1 既有 + 1 本轮预期) |
| 23 | 2026-07-31 | [20260730-0600-s1b-sigma-self-consistency.md](20260730-0600-s1b-sigma-self-consistency.md) | S1b / GS1b.9 | **σ 自洽 polish:P1 PASS / P2 FAIL**。配方依赖 0.118 c → **0.0029 c**(改善 40×),但自洽差**不下降**(震荡 0.35–1.22,撞满 epoch 上限)⇒ **不动点不可达**(σ→φ→σ 不是压缩映射);而且它把配方依赖**换成**了 **±0.0123 c 的 epoch 上限依赖**,正压在 P8 锁的 0.012 容差上 ⇒ medium 仍不可锚 | **P1 PASS / P2 FAIL** | 19 passed |
| 24 | 2026-07-31 | [20260731-0000-s1b-chainfree-sigma.md](20260731-0000-s1b-chainfree-sigma.md) | S1b / GS1b.10 | **候选 (b) 链自由 σ = 负结果,已回退;但产出一条阻塞性结论**。交付:FV 上风输运(按 φ 排序一次扫掠即精确、确定性)+ 可加熵产密度(Q1 验证 0.004–0.49%)⇒ **churn 归零、σ 自洽差精确为 0、旋钮 −4、Newton 8 步**。未解决:**过量产熵**(1−σ 6.22% vs 上界 3.57%)。**五族判别子全灭**(活跃度不分族 / 一阶差分之比合成过真实网格败 / 绝对阈值非单调翻号 / Jameson 锐利↔噪声取舍 / 非局部 reach 太慢),★★ 阻塞性结论:**畸形四面体与激波的局部 signature 是同一个东西**;★ 战略连接:**与 G1.6 同根**,S2 的梯度重构是共同修法 | **负结果 + 阻塞性结论** | 25 passed(回退后) |
| 25 | 2026-07-31 | [20260731-0800-s1b-flip-default.md](20260731-0800-s1b-flip-default.md) | S1b / GS1b.11 | ★★ **熵修正成为默认 + 重锚**(A1–A6 全达成)。**M0.80 两级激波 0.6073 / 0.6006,都落进 Euler 锚点带**(等熵 coarse 0.6581 带外、medium 不收敛);G8.1 coarse 重锚 **0.6196/0.41006/1.3946**(0.6196 距带中心 0.0004);M1a 重锚(三级一致性 +0.91% → **+0.63%**)。不可锚的如实 xfail:G8.1 medium(**0.118 c 配方依赖**)、G8.2(**三维 σ_min = 0.0** = 供体环,驱动器按 GS1.4 契约拒绝上报收敛)。★★ **A3 预言检验抓到我自己的错**:medium 的 G4.1 没如我所写变 pass —— 追因到 **GS1b.9 的 polish 把它推出带 0.0885 c**,而那个迭代本就不收敛 ⇒ **polish 已移除**,两条腿随即都进带。A6:6 个与熵无关的 probe 逐位未动。★ 新登记项:**两条尾迹路径的密度关系已分叉**(LS 未接线)⇒ 跨模型比较需重读 | **A1–A6 全 PASS** | 非门控 **677 passed / 28 skipped / 2 xfailed**,0 失败 |

| 26 | 2026-07-31 | [20260731-1200-s2-opening.md](20260731-1200-s2-opening.md) | S2 / 开篇前提检验 | **GS2.2 那个动作早就做过了** —— `tests/test_laplace_sphere.py::run_sphere_case` 一直在调 `wall_tangential_gradient_quadratic`,球面 23.05/11.56% 就是**开着二次恢复**测出来的 ⇒ **门作废**;且 G1.6 的**活动**判据已是 P11 采纳的 Option C(`TestG16Respec`,现在通过),所以球面侧引用**这条已采纳形式**而非字面 2%。★ 前提检验又一次改写了一个门的内容 | RECORDED(门作废) | 未记录(补录) |
| 27 | 2026-07-31 | [20260731-1400-s2-noise-map.md](20260731-1400-s2-noise-map.md) | S2 / 噪声空间分布 | **棱柱层不是「一个修法解两个问题」** —— 双指标一致:近场**无朝壁趋势**(ρ −0.09/−0.02 与 +0.10/+0.10),最噪单元仅 **0.6–6.8% 落在第一层** ⇒ 离散噪声**不是壁面层群体**,棱柱层只买**壁面精度**,登记项「激波定位」需要**自己的体内梯度重构** | RECORDED(负) | 未记录(补录) |
| 28 | 2026-07-31 | [20260731-1800-m3-budget.md](20260731-1800-m3-budget.md) | S2 / M3 误差预算(prereg 1600) | **LE 上表面占 69.6%、MID 上表面(激波带)20.7%、其余 9.7%** ⇒ **只做 LE 最好到 0.0715 > 0.05**,**M3 是两项目标、S2 单独交付不了**;且 MID 那项**不是**熵/激波强度误差(熵修正修好后 ΔRMS 仅 −0.0004)。并**精确定位**了一个 3-D 熵缺陷 | RECORDED | 未记录(补录) |
| 29 | 2026-07-31 | [20260731-2200-entropy-mcap-fix.md](20260731-2200-entropy-mcap-fix.md) | S2 / 熵修正 m_cap 逃逸(prereg 2000) | 默认路径缺陷**修好**(熵修正在 m_cap 限幅单元上逃逸)—— **但激波带仍然不动**,M3 的第二项另有其因;顺路把 **G8.2 的 strict xfail 移除并重锚** | PASS(修复)+ RECORDED(激波带未动) | 未记录(补录) |
| 30 | 2026-08-01 | [20260801-0030-kutta-le-result.md](20260801-0030-kutta-le-result.md) | S2 / LE 的 Kutta 假设(解读规则 [20260731-2330](20260731-2330-kutta-le-rule.md) 先写) | ★ **看到数字之前先写下解读规则**,然后**我自己的规则否掉了我自己的假设**:LE 那 70% **不是** Kutta 形式误差 ⇒ 登记项 (4) 关闭,而 **GS2.1 的前提首次被测出支撑** | REFUTED(自设规则否证) | 未记录(补录) |
| 31 | 2026-08-01 | [20260801-0230-gmsh-broken.md](20260801-0230-gmsh-broken.md) | 环境 / gmsh | **这台机器上的网格生成是坏的** —— `import gmsh` 直接失败(与 2026-08-04 的 base 环境自相矛盾是同一族问题的早期一次) | RECORDED(环境) | 未记录(补录) |
| 32 | 2026-08-01 | [20260801-0600-gs21-result.md](20260801-0600-gs21-result.md) | S2 / GS2.1 第一步(prereg 0200 + [addendum 0400](20260801-0400-gs21-addendum1.md)) | ★★ **重大负结果:LE 误差不受 LE 分辨率控制** —— 只加密 LE 的四腿最好 **−8.3%**(判据要 <10% 才叫不受控)**且非单调**(LE×4 的 0.2214 比 LE×2 的 0.2165 更差),而**分辨率误差不会非单调**;几何被排除(L0p 与 L0 **逐位**相同 ⇒ `h_te` 分裂场本身不改变任何东西)⇒ 唯一剩下的路是**先治尖端**。★ addendum #1 记的是我**把旋钮读错了**(`h_edge` 是 LE **和** TE,那一腿测的不是我登记的量) | **FAIL(重大负结果)** | 未记录(补录) |
| 33 | 2026-08-01 | [20260801-1200-gs21-tip-result.md](20260801-1200-gs21-tip-result.md) | S2 / GS2.1 第二步(prereg 0800) | **G1 按字面 FAIL,而那个闸门是我设计错的** —— 我用「峰值落在 η>0.95 尖端带内」当作尖端自由边奇点的代理(实测 M_max 1.6719、尖端带 8/8,两条都不满足);按预登记处置,**本轮不得把 T1 的 RMS 当 G2 的答案**,照办。★ 真收获:**taper 让全尺度网格可解**(T0/T1 都 0 lim/0 flr、残差到机器精度) | **FAIL(如实;闸门设计缺陷自陈)** | 未记录(补录) |
| 34 | 2026-08-01 | [20260801-1600-gs21-oos-result.md](20260801-1600-gs21-oos-result.md) | S2 / 样本外检验(prereg 1400) | ★ **样本外预测两条都成立**:P1(coarse LE 上 ≥0.26)实测 **0.3899**;P2 单调 **0.3899 → 0.2358 → 0.1758** ⇒ 加密是**真趋势**(阶 **0.725**,三腿全部 0 lim/0 flr、残差机器精度)—— **但外推说目标不可达** | PASS(样本外)+ RECORDED(外推不可达) | 未记录(补录) |
| 35 | 2026-08-01 | [20260801-2000-floor-partial.md](20260801-2000-floor-partial.md) | S2 / 地板判别(部分;prereg 1800,[addendum 2300](20260801-2300-floor-addendum1.md),[旁证 2100](20260801-2100-viscous-band-note.md)) | **D-a 干净地指向模型地板;D-b 两点且不干净 ⇒ 按预登记不下结论**。addendum #1 记的是**两条判别器不一致、而缺口是点数** ⇒ 补一个便宜的第四点。★ 2100 的粘性逐带旁证**显式标注为未登记、非仲裁者** | RECORDED(按预登记拒绝下结论) | 未记录(补录) |
| 36 | 2026-08-02 | [20260802-0000-floor-verdict.md](20260802-0000-floor-verdict.md) | S2 / 地板判定 | ★★ **MID/TE 的地板 SUPPORTED(0.052–0.071),而激波位置却收敛到实验** ⇒ **换网格解决不了 M3**。第四点(h 0.020,145 303 tets,61 s,\|R\| 6.36e-15,0/0)使四点序列**超定**:MID 上 A = **0.0740**(>0.03 ✓)、残差比 **0.039**(<0.5 ✓),且 A 在 3→4 点之间**稳定**;LE 带收敛到零(阶 0.709)、激波位置阶 0.845 | **RECORDED(地板 SUPPORTED)** | 未记录(补录) |
| 37 | 2026-08-02 | [20260802-0100-m3-respec.md](20260802-0100-m3-respec.md) | 指标重定 / M3 → M3a + M3b | **M3 拆成 M3a(精度,归 S4/Track V)+ M3b(速度,归 S3)—— 两个数字一个都没放松**,改的只有归属,而且理由是测量:M3 的参照是 **Re 11.7e6 的有粘实验**而我们是**无粘**求解器,而地板判定说那 0.05 里有一块是**模型**的。按 roadmap §5「门要改必须单独写一轮」执行 | 重定门(按规则单独一轮) | 未记录(补录) |
| 38 | 2026-08-02 | [20260802-0400-gs31-result.md](20260802-0400-gs31-result.md) | S3 / GS3.1(prereg 0200) | **张力只存在于小网格** —— `amg_tight` 在 **1.16 M** 上给 **3.96×** 且**复现 `direct`**(四个积分/RMS 量 ≤2.6e-09);`M_max` 经使用者裁决**不计入**。★ 前提检验第四次命中:**库默认早就是 amg**,债在 **39 处调用点**;实际内容 = **EW forcing 收紧** + 翻生产调用点。决策记录 `DECISION-2026-08-02-precond.md` | **PASS** | 未记录(补录) |
| 39 | 2026-08-02 | [20260802-0800-gs32-premise.md](20260802-0800-gs32-premise.md) | S3 / GS3.2 前提(prereg 0600) | **归因成立,但审计给的理由是错的** —— 同一张二维三角剖分上纯 Laplace 就复现了 AMG 迭代数病态,而原因不是「拉伸各向异性」;并**发现一条便宜得多的替代路线** | RECORDED(归因成立/理由更正) | 未记录(补录) |
| 40 | 2026-08-02 | [20260802-1200-gs32b-result.md](20260802-1200-gs32b-result.md) | S3 / GS3.2 (b)(prereg 1000) | ★ **根因比「各向异性」精确一层:`pyamg.smoothed_aggregation_solver` 的默认 strength-of-connection 就是 theta = 0.0**(`theta0.0` 腿与 `default` 腿数字**完全相同**)。2.5-D 上能拿 **4×** —— 但**没有全局赢家** | RECORDED(根因确定 / 无全局赢家) | 未记录(补录) |
| 41 | 2026-08-02 | [20260802-1600-gs32c-result.md](20260802-1600-gs32c-result.md) | S3 / GS3.2 (c)(prereg 1400) | **三个统计量都把两组分开了,但都不到 3×** ⇒ **按事前判据不实施**(判据在写探测器之前就已提交) | **FAIL(按事前判据不实施)** | 未记录(补录) |
| 42 | 2026-08-02 | [20260802-1800-gs33-premise.md](20260802-1800-gs33-premise.md) | S3 / GS3.3 前提 | **只有一半被证伪**(★ 原标题写成「两半」,文末自带 ERRATUM):**去爬坡 = G10.3 的已关闭负结果,在 AMG 新成本基准上复核后更负**(无爬坡 222.8 s 不收敛 vs 爬坡 70.8 s 收敛;G10.3 当年在 `direct` 下快 44%,**关系翻转**因 AMG 消掉了每层 splu)⇒ **GS3.3a 关闭**。但 **GS3.3b(种子)未被证伪** —— ERRATUM:我先前读成 seed = 0.00 s,是因为读了 `timings`(**只有最后一层**)而整条 ramp 在 `timings_total`,实际 **14.44 s = 墙钟 28.3%** | GS3.3a **✗ 关闭**;GS3.3b **开**(+ 自带 ERRATUM) | 未记录(补录) |
| 43 | 2026-08-02 | [20260802-2300-noseed-global.md](20260802-2300-noseed-global.md) | S3 / 无种子成为全局默认(prereg 2000) | 使用者裁决「无种子更简洁,希望成为所有情况的默认」+ 追问「**这能保证原来正确的场景都正确吗?**」⇒ 我先答那个追问:**一个算例不构成那个保证**,并写明覆盖只到「测试套 + 门控集」。验证:**全套 ungated 683/29/2 与 seed=5 完全一致且更快**(826.75 s vs 887 s);**门控跨声速 13 passed / 2 xfailed**(含 M6 medium M0.84 的 G8.2 与 NACA fold 区族)。★★ **ERRATUM 2026-08-05:那个「不保证」现在有反例了** —— `n_picard_seed` 5→0 让 **NACA0012 M0.80 medium 不收敛**(撞 m_cap)、并把 **M1a 的 fine 腿 d2 变号**;见 [20260805-1200](20260805-1200-m1-remeasure.md) | 采纳(裁决)+ **2026-08-05 反例 ERRATUM** | 全套 ungated 683/29/2;门控 13 passed / 2 xfailed |
| 44 | 2026-08-03 | [20260803-1400-capability-matrix.md](20260803-1400-capability-matrix.md) | 能力矩阵（用户重定向：能力优先于效率） | ★★ **13 格 × 马赫阶梯 = 69 点(90.6 min) + α 轴 9 点**。**答案:两条路径在 2.5D/3D、wing/wing-body、亚/跨声速下都能收敛,但必须分开报两条界限** —— 6 格终止于 **M_max<1.4 判据**(那些是**真解**被判据排除)、5 格终止于**收敛**、2 格**走完整条阶梯**(conf/ls wing coarse 双双到 M0.84)。★★ **我的一个猜测被数据否证**:NACA M0.78 的 12% 跨路径 cl_p 差**不是** vortex 远场(B17 +2.5%)—— 两路径在 **M≤0.70 一致到 ≤0.8%**,差异只在**折叠区**出现 ⇒ 折叠区灵敏度。wing-body 一致 **≤0.65%**(合 B9 的 0.4%)。★ **告示:所有跨路径对比都是跨网格族的**(conforming 需嵌入尾迹 / LS 需 wake-free,6 对节点数全不同)⇒ m6wing 的 2–4% 偏移**不能**单独归因尾迹模型;反过来 naca 亚声速 ≤0.8% 是**更强**的结果。★ **包线随加密单调下降**(naca 0.78→0.75→0.70)= 判据先绷断,非求解器退化 ⇒ **能力必须按网格层报**。★ **α 轴:6 格全恢复**,`ls_wb_medium` 从**零有效点**变成 M0.50/α2.0;**P2 确证**(α=0 时 M_max **0.657** vs 阈值 1.0)⇒ B23 升力耦合归因外推成立,该格杠杆是 α 非 M∞;**P1 否证且判据设计不良**(割线基线不可比、响应强非线性 0.245 vs 0.080)⇒ 撤下"奇点 vs 物理激波"区分,**第三次同类模式,列为模式不作挡箭牌**。★ **失效分型**(新增 `descent10`):4 个 conforming 全是**真停滞**(2 个残差逐步**完全不变**、2 个极限环,均 Kutta 机器零 + σ 正常 + 0 clamp ⇒ Newton 一步未进),仅 1 个 LS 行**预算受限**(降 807×/10 步),重测 `n_newton_max` 80→160 → **CLEAN,只多 6 步**(记为配方偏离,两半都报)。★ 修掉三个自身缺陷:stale import 曾**杀掉整个数小时作业**(后处理移入失败纪律 + 断点续跑)、**CSV 曾让一行声称它没达到的马赫数**(新增 `m_attained` 等 5 字段 + `MACH_NOT_ATTAINED`;损害有界,CLEAN 行本就受 conv 保护)、`descent10` **在 LS 行上一直是死的**(用了斜坡层而非 Newton 步)。★ 我曾给用户一个**不完整的数字**(把 9.05e-11 说成擦边,实际它已过 tol,真因是 `sigma_transport_not_converged`) | **能力已测定,两条界限分列** | 矩阵/α/预算三份 CSV + 13 逐格图 + 2 汇总图 + 72 个截面 Cp |
| 45 | 2026-08-03 | (同上轮记录) | 环境:gmsh 修复 + 基线重测 | **gmsh 已持久修复**(用户授权 `sudo apt install libglu1-mesa`,apt 连带装 `libopengl0`;`libglvnd0`/`libglx0`/`libgl1` 本就有 ⇒ **我先前给的四包命令过宽**)。gmsh **4.15.2** 无垫片可用,`tests/test_env_meshgen.py` 由 skip 转 **pass**。★ **基线重测:684 passed / 28 skipped / 2 xfailed @ 841.74 s @16 线程**(gmsh 坏时为 683/29/2)⇒ **只解禁了 1 个测试**;CLAUDE.md 提的 16 个 M1 测试早就在跑(网格虽 gitignore 但已在盘上)。★★ **两处自我更正**:① 我说 gmsh 坏「导致无法加密马赫阶梯 / 补 M<0.50 档」—— **错**,加密阶梯只需现有网格,gmsh 只闸新网格**层/几何**;② 我猜「全套变慢是网格生成测试解禁了」—— **也错**,它没变慢(841.74 s)。★ 第三处:我用 `pgrep -f "pytest tests/"` 等全套,**模式串匹配到了我自己那条等待命令的 bash 进程**(自匹配),反复误读为「仍在跑」,白等了一段墙钟 | **基线 684/28/2** | 684 passed / 28 skipped / 2 xfailed |

| 46 | 2026-08-03 | [20260803-2300-alpha-discriminator-verdict.md](20260803-2300-alpha-discriminator-verdict.md) | α 判别子 + 收敛性指标重建 | ★★ **结论 = 判别子不成立(仪器分辨率不足),H1/H2 均未裁决**。预注册要问三维不收敛是升力耦合还是壁面几何;答案是**池化 5 站截面 Cp 范数的站位抽样标准误 10.6–18.6%,与待分辨效应(18–24%)同量级**。★★ **撤回**我在画图前报的「level-set 带有随升力增长的 LE 收敛退化(+18%/+24%),已定位」:① 落在抽样散布内(1.4–1.5σ);② 事后配对检验在两个几何上**符号相反**(m6wing +0.105±0.045 / 翼身 −0.108±0.237)⇒ 不可复现。★ **站得住**:m6wing 的 **LE 带显著低于一阶**(R 0.816–0.988 对一阶 0.500,**2.3–4.7σ**,两路径×两 α 全部如此);**该赤字不是 level-set 特有的**(配对差 α=0 不显著且跨几何反号);2.5-D 对照健康(LE R 0.446/0.477);**cd 异常判据 0 异常 / 5 组合 × 全部 α ⇒ 未检出 bug 级缺陷** ⇒ 赤字是**离散化/建模**层面、集中在 **LE 带**、与尾迹模型无关、**M0.50 亚声速就存在**(非跨声速伪影)。★★ **仪器的两个失效源(主要技术收获)**:① **截面切割锯齿** —— d2 曲线在 x/c<0.04 高频振荡、幅度与均值同量级(我此前的平滑敏感性检验 9.2–9.7% **低估了它**);② **站位抽样** —— per-station R 摆动 0.38–1.28(3.3 倍)且**两路径逐站重合** ⇒ 摆动是站位性质。**下一阶段第一件事 = 换仪器**(壁面三角形面积加权分带积分范数,去掉截面切割与站位抽样),换掉前 <15% 的 LE 效应不可读。★ **指标体系经用户三次纠正定型**:p ✗(模型形式不成立,cl_p 给 0.20 而 cl_KJ 给 0.45)→ **R**(不拟合幂律;阈值**阶梯相关**,均匀 2× 为 1.000、翼身 1.47/2.00 为 1.810 —— 我误用通用 1.0 错判翼身"被否证",已撤回);**对有粘实验的误差 ✗**(FP 无粘无涡,距离主要是模型误差);**cd 不能主导 ✗**(一有激波零参照消失,而那是求解器最容易的区域)⇒ **cl & 截面 Cp 主导,cd 降为异常判据**。★ 本轮 8 处自身错误全部提交记录,归为两个共同模式:**在不可比基线上套判据**(3–5 次)与**从不足以支撑结论的数据读出结论**(3 次);全部由独立检验抓住,无一靠事后辩解修正 | **判别子不成立;LE 定为下一阶段靶区** | 无新测试(纯 bench/docs) |

| 47 | 2026-08-04 | [20260804-0100-le-tip-taper-superseded.md](20260804-0100-le-tip-taper-superseded.md) | LE 阶段 / LE-0…LE-4 | ★★ **翼尖 taper 已被熵修正取代,现在只剩系统误差**。用户背景:taper 是为防"翼尖 Γ→0 时翼尖涡表达不出来导致发散"而做的简化模型,有系统误差。**决定性判别子(单旋钮 A/B)**:无 taper 的 M0.84 medium 翼身,`entropy_correction=True` **conv 0/0 \|R\| 2.070e-14 (511 s)** / `=False` **不收敛 \|R\| 7.273e-06 (1389 s)** ⇒ **熵修正(S1b 设为默认)削弱激波、削掉了当初导致翼尖失速的峰值,接管了 taper 的防发散职责**;而 B32 的归因是在**等熵**求解器上测的。**偏差实测(medium)**:生产 `vanish_smooth`/0.05 **−1.514%**、`vanish_linear`/0.05 −1.679%、`vanish_smooth`/0.025 **−0.515%**、`none` 0%;**五个配置在 coarse 与 medium 都干净到 M0.84(0/0 clamps)** ⇒ 按预注册规则(偏差最小且仍收敛)裁决 = **移除 taper**。★★ **库 docstring 一处 erratum**:`vanish_linear`(s=1)被称为"minimal-bias form",实测偏差**比 `vanish_smooth`(s=2)更大** —— 那句讲的是渐近指数 s,不是**积分**偏差(smoothstep 在 Γ~√u 已小的翼尖侧多卸、在 Γ 大的带内侧少卸,积分更省)。★ **r_c 是主导杠杆**,0.05→0.025 砍偏差 2.9–3.5 倍(标度律 (r_c/b)^{3/2} 预测 2.83×)。★★ **LE-0 换仪器**:分带积分升力取代 5 站截面采样,带**机器精度接线守卫**(分带求和 vs 总和 8e-17…4e-16;总和 vs `wall_forces` **逐位 0**),立刻暴露 cl 非单调 = **分带抵消**(LE 涨、MID/TE 跌)。★ **LE-1**:赤字扛过换仪器(m6wing p 0.27–0.37),但**不是"三维"现象** —— 更复杂的翼身健康(p 1.19–1.42);"2.5-D 收敛/3-D 不收敛"改写。★ **LE-2**:赤字在**翼尖**不在根部(我的根部假设方向反了);根部 p 1.4–1.7 健康。★★ **LE-3**:`tip_taper` 对收敛贡献 ~0(五箱各 0.005–0.008 = 微小全局扰动),**真杠杆是 Kutta 估计器**(probe→pressure 让翼尖 R 1.983 无正阶 → 1.270),⇒ `NEWTON_M6_RECIPE` 仍用 P14 已取代的 probe = **承重配方缺口**。★ 自身错误:根部假设反向、"配方缺口=taper"定量否证、两次凭记忆写导入/参数名(`phi_init` 放错层导致 LE-4 首跑 5 条 robustness 腿全 TypeError)| **移除 taper = 裁决;采纳待重基线** | 无新测试(纯 bench/docs) |
| 48 | 2026-08-04 | [20260804-2148-le-round-tip-and-environment.md](20260804-2148-le-round-tip-and-environment.md) | LE-5…LE-15 / round-tip 采纳 / 环境自洽化 | ★★ **用户的方法论纠正是本轮最大产出:失效必须分类,不能报 `conv=False`** —— 五种模式签名不同、处置不同,把它们混成一个标签意味着相关性分析可能在用一个量分开一堆不同的病;这本身解释了我本轮**五个**机理假设全灭(耗散强度 / 熵修正削弱激波 / 超声速连通性 / 根部终止 / taper=配方缺口)。加入分类器后 **撤回两个结论**:① **r_c「死带 [0.030, 0.0375]」是两种病拼的**(0.030 削波、0.0375 只是 `descent10=2.002` 的**预算不足**,仍在下降)⇒「生产值在安全侧」的论证撤回;② **「tip 载荷自由度 vs 高马赫收敛」统一叙事不成立**,三次观察是不同模式,四个候选量全部重叠。★ **taper 移除流程走完 = 不能移除**(M0.88 medium 真失败 6/6 clamps;裁决维持 0.05 + **−1.3% 偏差写进报告口径**)。★ **熵修正机理:用户的耗散假设与我的削弱激波假设都被否证**(`upwind_c` 1.5→3.0 使 nu_max **+55%** 仍不收敛且抹掉激波 n_active 48690→2498;熵 ON 的 M²max **6.426 > OFF 5.607**,峰值更强)⇒ 它是**模型修正,不是稳定性技巧**。★ **LE-11/LE-12 有产出**:残差 **83% 集中翼尖 10% 内**,位置不分成败、**幅度分**(5–15×);配对显示两旋钮**翼尖外逐点相同**(corr 0.998/k 0.968)、**翼尖内不同**(0.429)。★★ **round tip 全面采纳**:`wing3d` 默认 flat→**round**、**标准族名=round / flat 退为显式 `_flat`**(反转我第一版)、**P13/M5 在翻转前查到并显式指向 `_flat`**(否则变 round-vs-round 测「flat 发散」且**不报错**);阶梯 re-spec **(xcoarse, coarse, medium)**,fine 退出。**取舍如实记**:LE 带阶 **0.37→0.87**(与二维 0.85 一致,「三维更差」是平面翼尖造成的)**代价是 LS medium 包线 M0.84→M0.70**;LE-13 判它非标定(四腿全失败、三腿逐位相同)但⚠ **该结论仍在未分类证据上,LE-15 待做**。★★ **环境**:`base` **自相矛盾**(scipy 1.11.4 的 `cg` 无 `rtol` + user-site pyamg 5.3.0 需 ≥1.12 + 残缺 scipy 目录),**`scipy≥1.12` 是代码要求**(`spla.cg(rtol=)` 无版本分支)—— 我第一版 up3d 钉 1.11.4 得到 **81 failed/50 errors 全是该 TypeError**;`up3d` 建立并以全套为采纳判据:**676 passed / 8 failed**,8 个**逐条归因、无一数值漂移**(5 个 round-tip 预期后果、2 个缺 pandas、1 个待查);**隔离必须 `PYTHONNOUSERSITE=1`**(实测不屏蔽时 up3d 内 `import pyamg` 仍失败)。★ 耐久规则已写进 **CLAUDE.md** 四节(环境 / 报告口径 / 失效分类 / 操作陷阱)。★ 我的错误共同模式:**凭记忆写签名 5 次**、**pgrep/pkill 自匹配 2 次**(一次杀掉自己的 shell)、**缓存不全致重复重解 3 次** | **note 补齐;采纳 up3d** | **684 passed / 28 skipped / 2 xfailed @14:20(up3d, round tip)** —— 与切换前的 684 passed **同一通过数**,8 个中间失败已逐条修好(见下轮) |
| 49 | 2026-08-05 | [20260805-0030-le15-le16-classifier-fix.md](20260805-0030-le15-le16-classifier-fix.md) | LE-15 / LE-16 / 分类器修正 | ★ **LE-15:round-tip 包线退步 = 真实几何代价,判定成立** —— 逐马赫台阶分类,转不过去的是 **M0.6750,模式 clamping**(descent10 0.14/0.71/1.16,clamp 数随马赫单调 3→4→5→9)⇒ 圆角翼尖把单元推进 `m_cap`/`rho_floor` 限幅器,**物理限制而非迭代预算**;**这是分类器第一次确认而非撤回一个结论**。★★ **LE-16:死带存在,但我的 `budget_limited` 标签是错的** —— 预算 60→200 后 r_c=0.0375 的 |R| **逐位不动**(4.025e-05),尾部是**周期精确为 3 的极限环**(9.037e-05→8.059e-05→4.026e-05 重现到 4–5 位);补测 0.0325 是 **σ 输运**(**|R|=2.19e-14,流场完全收敛**)、0.035 是 clamping ⇒ **[0.030,0.0375] 连通失败区间真实存在,但四点是三种病**,「生产值 0.05 在安全侧」**恢复成立**(距上界 0.0125 vs 候选 0.025 距下界 0.005),20260804-2148 §4 的「撤回 1」部分收回。★★ **分类器缺陷:一个我手定的阈值决定了一个已发布结论** —— `descent10=2.0021` 只是「周期内高点比低点高 2 倍」的算术产物,恰好越过我随手定的 `>2.0`;重现容差 1e-12 也远紧于极限环在浮点里的重现度(数到 0)。**修法不是换数**,而是**`budget_limited` 现在要求尾部单调**(极限环无论 descent10 多少都过不了)+ 容差 1e-4,并**在两个已知答案案例上验收**(0.0375→limit_cycle ✓;LE-1 的 ls_naca_medium→budget_limited ✓)。★ **最干净的新线索:σ 输运失效第三次出现**(LE-4 conf_wb M0.78、p13 round-tip coarse、LE-16 r_c=0.0325),**三次都是流场与 Kutta 已满足、唯一障碍是 σ 未 settle,且都在翼尖相关算例上** ⇒ 下一个靶子。★ 术语更正(用户指出我造词):`assignment_cycle` 是求解器**内部诊断字符串**、且是**接受理由不是失效理由**(读 `newton_ls.py:745-750`),我此前硬译为「指派环」并当成失效原因 —— 第七次从符号名推含义而不读代码 | **LE-15 确认 / LE-16 修正 + 分类器修好** | 684 passed(未改测试)|
| 50 | 2026-08-05 | [20260805-0200-sigma-transport-root-cause.md](20260805-0200-sigma-transport-root-cause.md) | σ 输运根因 + 修复 | ★★ **`sigma_transport_not_converged` 是一个假失效,根因是供体图里一个长度 2 的无害环** —— 环上 s ≡ 1,却让 pointer-doubling 的 24 轮全部空转,于是终止判据「每个祖先都是真根」永不成立,把 **\|R\| = 8.85e-15、0 clamp、0 激波单元**的完美解判成失败。实测两例各只有 **2 / 68624** 与 **2 / 90099** 个环上单元。★ **图论无环要求只来自熵修正**:上风密度与 Jacobian Term 3 只读 `upstream[e]` **一跳**、对环无所谓 ⇒ 那个环在 S1b 之前**完全无害**,是熵修正把它变成了问题。★ 链长实测中位 35 / p90 82 / 最长 145 跳,而**最深激波只在第 61 跳** ⇒ σ 早在到根之前就已完整。★★ **两个判据被实测判死,而第二个在所有手工算例上全绿**:(E)「乘积不再变化」当场被塌缩锁判红(**我把"平方塌缩到 0"误当成"永不稳定" —— 0 才是最稳定的不动点**);`P[A[e]] == 1`「祖先不再贡献」通过**两条塌缩锁 + 我特意构造的五种环**,却被**随机图 + 逐跳走查 oracle** 判死 **4/5**(它只检查从祖先起 2^k 跳这一段,更上游的激波被静默漏掉)。⇒ **本轮真正的方法论产出:写一个与实现无共享代码的 oracle,比再想一个更聪明的判据值钱**(已写进 CLAUDE.md)。★ 采纳 **(D′)**:串行 O(n) 找出所有环,在 `upstream` 的**局部副本**里**只打断无害环**(环上 s 全为 1 —— 因子皆 ≤ 1,故乘积恰为 1 ⟺ 全为 1,**无阈值**),**带激波的环不动**、照旧塌缩并被拒绝,终止判据**回到原来那条**。★ **判别性先量再采纳**:pre-fix 跑新锁 = **恰好三条无害环腿失败**,五条带激波环腿前后都通过。★ **活算例 A/B(同网格同配方,只换内核)**:三条腿 conv **False → True**,而 **σ_min 与 cl_p 一致到 10 位**、σ 刷新数与 Newton 步数**逐个相同** ⇒ **只改判定,不改数**(差 ~2e-11 = 打断环使少数单元乘法顺序变化的舍入;按 D1 口径记为 1e-11 移动,**不是 bit-identical**)。★ **顺带根因了 20260804-2148 §7 遗留的「a1 待查」**:`test_conforming_picard` 的 `other < 20%·wall` 在 **numba 缓存冷时必失败**(编译时间落进 `other`)—— 删 `kernels/__pycache__` 即按需复现 35.4%、热缓存立刻通过、**用修复前内核冷跑同样失败 32.2%** ⇒ 与任何改动无关,是该断言本身在**度量编译**;**这也解释了新建 up3d 环境为何在此报失败**(新环境缓存按定义全冷)。修法 = 模块级 autouse 先编译一次。★★ **后果已重测**:LE-16 的 r_c = 0.0325 那点(唯一被 σ 拒绝的)现在**收敛**(conv=True / `tol` / 38 步 / 0-0 clamps / \|R\| 2.189e-14 / cl_p 0.304470,与修复前**轨迹逐个相同**) ⇒ **20260805-0030 §2 的「[0.030, 0.0375] 连通失败区间」是错的**,失败集是三个**孤立点** {0.030, 0.035, 0.0375};「0.05 在安全侧」**仍成立但换理由**(到最近失败点距离 0.0125 vs 候选 0.025 的 0.005),用户保持 0.05 的裁决不受影响 —— 这已是同一叙事的**第三次修正**。★ p13 的 `converged` 门**按其自身留下的线索恢复**。★ 我的错误:把"我构造的情形已覆盖"当成"完整性已证";两处**测试**缺陷先被我读成内核 bug(要求浮点乘积**逐位**相同 —— 乘法不结合;给**根**赋 s < 1 —— 违反内核明写前提);又一个未验证推断当解释("两个 σ_min 在不同状态上量的",实测轨迹逐个相同);**第九次凭记忆写签名**(`shock_factor_sweep` 是 9 参,且 `lim=np.zeros(0)` ≠ `lim=None`)| **PASS**(四条预注册验收全过) | **694 passed / 28 skipped / 2 xfailed @16 线程 918 s**(基线 684;+10 = 新增 11 个熵锁 − 被重定规格替掉的 1 个)|

---

| 51 | 2026-08-05 | [20260805-1200-m1-remeasure.md](20260805-1200-m1-remeasure.md) | 任务 1+2 / M1 重测 + 台账补录 | ★ **M1 三条判据首次在库默认密度上一起测**(两处调用点欠债先修:`--entropy` 默认 False、`n_picard_seed` 不传)。seed=5(like-for-like):**(a) 激波 coarse 0.6073 / medium 0.6006 —— 两级都进带,三条判据里首次有一条通过**(等熵 coarse 0.6581 带外、medium 不收敛);(b) **+101.4% → −16.2%**(改善 6.3×,★ **符号翻了**);(c) coarse **36.1% → 9.4%**、medium **首次可测 17.9%**。**M1 仍 FAIL**,差距 5.4×/6.0×,但 S1 kill criterion 里「不可控」的措辞已不准确(33× → 6×)。★★ **抓到三条已提交能力锁的回归,没有一条是我今天的 σ 修复**(先换回旧内核 A/B,逐位相同才下结论):① **NACA0012 M0.80 medium 不收敛** = 08-02「无种子全局默认」(`n_picard_seed` 5→0)—— seed=5 时 \|R\| 2.85e-13/0-0 clamps,seed=0 撞 m_cap(M_max **恰好 3.0**,7265/758,cl 转负);② **M1a 的 fine 腿 d2 变号**(ratio +0.1557 → −0.0300)也是同一原因,且 xcoarse/coarse/medium **与 seed 无关**⇒ 种子只在最难算例上咬合;★ 该测试注释里的「收敛比 6.45」是**把比率写成了倒数**;③ **G8.2 = round-tip 网格重生成**,已**证实**:`medium_flat.msh` 把锚点复现到 5–6 位(cl_p 0.263882 vs 0.263888、M_max 2.10697 vs 2.10709、\|R\| 8.66e-15、0/0),圆角不收敛。★★ **三条为什么都没被发现,是同一个结构缺口**:能力锁在门控集里,而门控集不是每轮都跑、覆盖逐轮不同;其中 NACA 那条被 **`xfail(strict=False)`** 盖住 —— **非严格 xfail 原理上无法探测回归**,而它被设成非严格是为了**另一个**理由。★ 还有一处**作用域承诺静默失效**:GS3.3b 的注释写「the library default stays 5, so no other call site moves」,后一轮把库默认也翻了。**任务 2**:66 个轮次文件里 **36 个没有台账行**(08-01 与 08-02 两整天 = 整个 S2/S3 执行块),补 **18 行**(prereg 按惯例折叠进结果行并点名),现在**非 prereg 文件全部被引用**;并就地更正**五处过期表面**(M3b 指标行仍停在审计的 108 s,实测 **49.9 s**;M1 指标行引用 GS1b.3 era 的数;M1a 指标行;S3 阶段格的「seed = 0.00 s 无可省」已被同轮 erratum 更正为 **14.44 s = 28.3%**;S2 阶段格写着「下一步 = GS2.1」而 GS2.1 早已执行完并给出重大负结果) | **M1 ✗(3 条中 1 条已过)** + 3 条回归**已定因** + 台账补齐 | 全套未重跑(仅 bench/docs 与门控探针);门控集 **2 failed / 15 passed / 2 xfailed** |
| 52 | 2026-08-05 | [20260805-2000-le-factorial-verdict.md](20260805-2000-le-factorial-verdict.md)(prereg [1600](20260805-1600-le-mechanism-prereg.md)) | 任务 4 / LE 亏空机理 2×2 因子 | **预注册的问题没答上,而挡住它的东西更重要**。2×2 做不成:每一条加密 `h_wall` 的腿都撞 `m_cap`(`M_max` **恰好 3.0**,三腿,clamp 4/14、46/25、**883/202**)⇒ 按 G3 它们的 RMS **不是读数**;**G1 也 FAIL** —— 按住 `h_edge` **并没有**按住 LE 分辨率(LE 三角形 **+41.7%**,而 h_t 只动 +0.86%)⇒ **`h_edge` 不是干净的单变量 LE 旋钮**,两个方向的单变量实验都要重设计。判定 **RECORDED,不下结论**。★ 三件没登记但更重要的:① **M0.8395 上每个加密方向都把峰值马赫推向限幅器**(收敛三腿 M_max **1.56 → 2.20 → 2.86**,加密 bulk 直接撞帽)⇒ **该工况不能做加密归因**,这比 GS2.1 的「LE 误差不受分辨率控制」强得多也具体得多,并解释了它的非单调;② ★★★ **支撑整个 LE 故事的「阶 0.725」全尺度序列、以及地板判定的整张四点表,是在平截翼尖上测的**(`run_tip_allscales` 从 `run_le_response` 导入 `build`,后者 `tip_cap="flat"`)—— 而本项目自己记着平截帽在加密下发散(p = +0.321)、故「平截网格上任何基于加密的结论前提失效」⇒ **erratum 已加到地板判定**,「LE 带收敛到零(阶 0.709)」从此须带「在平截网格上」的限定,round tip 上**尚未被测**;③ LE-only 那一臂在 round tip 上**复现**了 GS2.1 的小效应(**−1.55%**)—— 本轮唯一守卫干净的读数,把 GS2.1 的核心观察从平截网格上救了下来。★ 我的错误:预注册里的成本估计错了一个数量级(写 7 s/61 s,实测 187 s/1043 s —— 那两个旧数是别的配方的),**成本闸门救了这次**;以及**没在预注册里要求记 clamp 的位置**,于是量到「撞帽」却答不出「撞在哪」 | **RECORDED(不下结论)** + 1 个新负结果 + 1 条阻塞性结论 + 1 份 erratum | 未动库代码 |

| 53 | 2026-08-05 | [20260805-2200-seed-exposure.md](20260805-2200-seed-exposure.md) | 诊断 / `n_picard_seed=0` 的暴露面与机理 | 用户直接问「哪些会失败、失败原因是什么」。假设**执行前写进脚本**,且来自项目自己的记录(GS3.3b 的注释:「无种子可行是因为**爬坡第一层是亚临界的**;**不要**带到第一层就超临界的配方上」)。★ **预写的 P2 成立:不是「种子」失败,而是「无种子 **且** 直接在超临界目标上冷启动」失败** —— 同一算例加马赫爬坡后 seed 0 **收敛**(\|R\| 4.49e-11、0/0)。★★ **机理三条证据**(限幅位置本轮首次重建,补上前一轮承认的仪器缺口):① **位置**:限幅 **81% 在 MID / 17% 在 TE / 0% 在 LE**,峰值 q² 在 **x = 0.7505**(激波处)⇒ 不是前缘;② **时机**:**第 3 步**就限幅 ⇒ 是**初始场**问题,而两条收敛腿也**短暂**限幅过(第 49/19 步)然后恢复 ⇒ 「限幅过」不是失败标志,「第 3 步就限幅且回不来」才是;③ **爬坡救得回来** ⇒ **上一马赫级的收敛解接替了种子的角色**,爬坡与种子是同一功能的两种实现。**失败条件是三者合取**:(a) 不传种子 + (b) 无爬坡的超临界冷启动 + (c) **网格细到能解出那个超声速区** —— ★ (c) 反直觉:**同工况 coarse 活、medium 死**(coarse M_max 1.394、0 限幅),**加密使它变坏**。★ **两级暴露不可混**:**硬失败**(撞帽拒收敛)与**软位移**(收敛且 0 限幅但落到另一个解 —— M1a 的 fine 腿 cl 0.254830 → 0.252930、d2 变号),后者**不报任何错**,回退机制治不了它,需要的是答案锚点。⇒ 对选项 (ii) 的影响:**触发条件可以写死且可测**(现成的 `clamped` 标志),成功路径零成本,但**只治硬失败**。★ 我的错误:**第十次把签名/结构写错**(`alpha_deg` 是驱动器参数,被我放进 `newton_kw` ⇒ 爬坡腿全 TypeError);以及第一版报了「68 个调用点暴露」这个**解析假影**(`m_inf=None` 只是马赫来自模块常量),已删除并改为如实说「静态数不出来」——**测出来的**暴露面只有 3 处,而未被测试覆盖的调用点是**未知项**,不是已确认安全 | **RECORDED(机理已定,预写预测成立)** | 未动库代码;全套 ungated 694 不受影响 |

## 阶段进度概览

| 阶段 | 内容 | 状态 |
|---|---|---|
| S0 | 地基与基线（环境可复现、测试分层、基线冻结） | **✓ 完成 2026-07-28**（第 2 轮，3/3 门 PASS） |
| S1 | **跨声速能力边界（总闸门）** | **✓ 收口 2026-07-29**（GS1.1–GS1.7 全部执行）。包线 **M_max ≲ 1.2** 已测定并**锁住**（M1a）；M1 保留为包线外的 FAIL（脚本退出码 1）；七条路线已排除。**S1 的最终归因**：同工况 cl 随加密不收敛，且代码里唯一的削波机制 ∝ h、随加密自己消失（S1b/GS1b.2 Q5）—— ★ 收口时提的"fold"解释已被 GS1b.2 的 Q4 **推翻** |
| **S1b** | **扩包线（非等熵 FP）** ★ 裁决 D4 | **✓ 可交付部分落地 2026-07-31（GS1b.11）：熵修正已成为默认**。M0.80 两级激波 **0.6073 / 0.6006 都落进 Euler 锚点带**（等熵 coarse 带外、medium 不收敛）；M1a 与 G8.1 coarse 已重锚，G8.1 medium / G8.2 如实 xfail（0.118 c 配方依赖 / 三维供体环）。**两个登记项**：① **激波定位**（独立问题，五族判别子已排除 + 阻塞性结论，共同修法 = S2 的梯度重构）；② **两条尾迹路径密度关系已分叉**（LS 未接线）。**下一步 = S2** |
| **S2** | **壁面精度** | **◐ 已开工 2026-07-31（分支 `claude/phase-two-s2`，保留待与 S0/S1 一并 PR）。★ 已按测量重定：GS2.2 作废（动作早是现状，球面 23.05/11.56% 就是开着二次恢复测的；G1.6 活动判据 = P11 Option C，已通过）；棱柱层**只**买壁面精度（噪声非壁面层群体，双指标一致）；**M3 误差预算实测 LE 上 69.6% / MID 上 20.7%** ⇒ M3 是两项目标，S2 单独交付不了，S2 自己的验收改为**中间靶 LE 上 RMS 0.2361 → <0.08**。顺路修掉默认路径缺陷（熵修正 m_cap 逃逸），**G8.2 strict xfail 移除并重锚**。★ **GS2.1 已于 2026-08-01 执行完,是重大负结果**:**LE 误差不受 LE 分辨率控制**(最好 −8.3% 且**非单调**),几何被排除,样本外确认加密是真趋势(阶 0.725)但**外推说 <0.08 不可达**;随后地板判定给出 **MID/TE 地板 0.052–0.071 SUPPORTED** ⇒ **M3 换网格解决不了**,并触发 M3 → M3a/M3b 重定。**S2 的中间靶(LE 上 RMS <0.08)= 测量为不可达**;LE 那 70% 的机理**仍未查清**(Kutta 假设已否证、分辨率已否证、几何已否证)= 现在最大的未解释亏空** |
| **S3** | **速度**（换预条件子、真二维路径、去爬坡） | **◐ 已开工 2026-08-02;GS3.1 ✓**。前提检验第四次命中:**库默认早就是 amg**,债在 **39 处调用点**。实际内容 = **EW forcing 1e-2 → 1e-10** + 翻生产配方。**T1(1.16 M)4.00×**、四个积分量一致到 **≤2.6e-09**;`M_max` 经裁决不计入。★ 途中 **1e-6 在 350k 过、在 1.16M 失败** ⇒ **换根阈值随网格移动,任何固定 forcing 是标定不是保证**。决策记录 `DECISION-2026-08-02-precond.md`(含被排除选项与推翻条件)。全套 **683+29+2 不变**,门控 P8 10/1 且 **178 s → 115 s**。**GS3.3a ✗ 关闭**(去爬坡 = G10.3 的已关闭负结果,在 AMG 新成本基准上复核后**更负**:无爬坡 222.8 s 不收敛 vs 爬坡 70.8 s 收敛)。★ **GS3.3b 仍开着** —— 本格此前写的「生产路径 seed = 0.00 s 无可省」是**已被同轮 ERRATUM 更正的读数**(我读了 `timings` = **只有最后一层**,整条 ramp 在 `timings_total`);实际 **种子 14.44 s = 墙钟 28.3%**,是 GS3.4 预算里第二大项。**EW 二次修订 1e-10 → 1e-6**:1e-10 使 M3b 不达标(67.6 s),1e-6 同时满足 1e-8 与 60 s(**49.9 s**);⚠ 阈值随规模收紧,**1.16M 级上 1e-6 失效** |
| S4 | 粘性重启（条带 IBL + Veldman 准同时耦合） | 未开始（S3 后） |
| S5 | 产品化（删 level-set、阻力、CLI、包线门） | 未开始 |
| S6 | 战略决策点（非结构 vs 半结构） | S1/S2 结束后评审 |

## 产品指标追踪

| ID | 指标 | 目标 | 当前 | 达标？ |
|---|---|---|---|---|
| **M1a** | **包线内网格收敛**(M0.72/α1.25) | medium→fine cl 差 < 1% | **重测 2026-08-05**:seed=5(锚点配方)**+0.629%**、比率 **+0.1557**、四层 cl 0.222742/0.242997/0.253237/0.254830,全部 0 lim/0 flr —— **精确复现 GS1b.11 重锚的 +0.63%**;seed=0(现生产默认)**−0.121%**、比率 **−0.0300**(fine 的 cl 0.254830 → 0.252930,**d2 变号**)。⇒ **判据本身(<1%)两种 seed 都成立**,但 seed=0 下三级**非单调**、Richardson 失效。★ 测试注释里的「收敛比 6.45」是**把比率写成了倒数**(1/0.1557 = 6.42);★ 判据定义在 **medium→fine** 上,而 `fine` 已按使用者裁决退出 —— 新阶梯 xcoarse/coarse/medium 上是 **+4.21%**,**该判据在新阶梯上不成立** ⇒ 需要**重定门**([20260805-1200](20260805-1200-m1-remeasure.md)) | **✓**(判据成立;两项登记) |
| M1 | NACA0012 M0.80 激波 / 网格一致性 / 耗散不敏感 | 0.61±0.02;两级 cl 差 <3%;C 扫掠 <3% | ★ **本行 2026-08-05 重测,取代 GS1b.3 era 的读数**(那些数产于膝点修正、σ freeze、m_cap 逃逸修复**之前**)。库默认密度、两条 `n_picard_seed` 都测(`bench/gate_results/m1_gate_default.csv`,12 腿)。**seed=5(与等熵记录同配方,故此列 like-for-like)**:(a) coarse **0.6073** / medium **0.6006** —— **两级都在带内,三条判据里首次有一条通过**(等熵:coarse 0.6581 带外、medium 不收敛);(b) **−16.2%**(等熵 **+101.4%**,改善 6.3×,★ **符号翻了**);(c) coarse **9.4%**(等熵 36.1%,改善 3.8×)、medium **17.9%**(等熵 0 条腿收敛,**首次可测**;且只含 C∈{1.5,3.0} 两点 —— C=1.0 仍撞 m_cap)。**seed=0(现生产默认)**:(a) coarse 0.6195 PASS / medium **FAIL 不收敛**(撞 m_cap,7265 lim/758 flr);(b)(c) medium 无法评估。⇒ **仍 FAIL**,剩余差距 (b) 5.4× / (c) 6.0×;「不可控」这个措辞已不准确(等熵是 33×)。等熵时代记录保留在 `m1_gate.csv` | ✗(3 条中 1 条已过) |
| M2 | RAE2822 case 9/10 | 激波 ±0.02c，cl ±3%，cd ±10 counts，<10 s | 偏后 0.06–0.10c；1/4 收敛；无 cd | ✗ |
| **M3a** | M6 七站 Cp 对**有粘实验** | RMS **<0.05**（数字不变，归属改 **S4**） | T1 全点 **0.1007**；★ 四点加密测出**无粘模型地板 0.052–0.071** ⇒ **无粘不可达**；LE 带收敛到零（阶 0.709）、激波位置收敛到实验（阶 0.845）、地板在 **MID(79%)/TE** | ✗（归属已改） |
| **M3b** | 同算例**无粘速度** | **<60 s**(数字不变) | ★ **本行 2026-08-05 更正(此前停留在审计的 108 s)**:GS3.1 的 EW 二次修订实测 **M6 medium M0.84 爬坡 49.9 s**(参照 `direct` 125.4 s;`DECISION-2026-08-02-precond.md` §9)⇒ **该网格上判据已达标**;**T1(1.16 M tets)仍 950 s**,且那一规模只有 EW 1e-10 成立(1e-6 失效)。⚠ 49.9 s 是 **M0.84/α3.06 的爬坡**,与 M3 条件(M0.8395/α3.06)实质同一算例,但**未在 M3 的精确条件下单独复跑**,如实标注 | **◐ medium 达标 / 大网格未达标** |
| M4 | 翼身组合体粘性单点含网格 | <2 CPU·min | 3.5 h 不收敛 | ✗ |
| M5 | 20 点包线 | 100% 收敛，0 clamp，无 M_max>1.5 | **M5 本身仍未度量**（它规定单配置 20 点包线）。但能力矩阵给出一个**更广的相邻测量**：13 配置 × 阶梯 = **78 点**，**收敛 72/78 = 92.3%**、**clamp 0/78 ✓**、**M_max>1.5 共 4 点**（3 个是 α=3.06 下干净收敛的 LS 翼身/机翼，降 α 后 α 轴 9 点里 0 个越 1.5；1 个是 conf_naca_fine M0.75 的停滞）。⇒ 三条子句里 **0 clamp 完整达成**；不收敛的 6 点已分型（5 个真停滞 + 1 个 σ 输运） | ✗（0 clamp 子句 ✓） |
