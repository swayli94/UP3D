# 20260814-0500 预注册 — **只读重评:等参 P2 壁面层这条路该不该开**

**在任何代码 / 任何求解之前提交**(roadmap 原则 8)。
上游 = [20260814-0300 判定](20260814-0300-le-offset-scatter-verdict.md) §6 第 1 条 (b);
使用者裁决(2026-08-14):**先做 (b) 的只读评估**。

## 0. 这一轮是什么、以及它**不是**什么

**是**:一次**零求解**的评估 —— 读已提交的证据与代码,回答"**等参 P2 值不值得开**"。
**不是**:实现,也不是"评估完顺手试一下"。★ 本轮的产物是一份**建议**加上它的依据,
**开不开是使用者的裁决**。

★★ **为什么这需要预注册**:只读轮最大的危险恰恰是"**先读完再决定判据**" ——
那样任何结论都能被证据支持。所以下面先写死:**我已经知道什么**、**要问什么**、
**什么答案导向什么建议**。

## 1. ★ 我在动手读之前已经掌握的(入口检查,只读标题不读数字)

| 来源 | 已知的一句话 |
|---|---|
| P11(agent-rules 现行块) | **mapped-P1(超参数元)测负**:medium 球 11.56 % → **11.33 %** = G1.4 边界数据神谕的天花板;机理 = **mapped-P1 在二次几何上以 `O(h)` 丢掉线性再现**,与它移除的 `O(h)` 刻面法向误差**同阶** |
| P11 同轮的再归因 | 球上的阶塌陷是**固定体网格的污染底**,不是几何:结构化 icosphere(**同样的平面刻面**)阶 **1.67/1.98**;固定 h_min=0.03 只refine远场使壁面 φ 误差降 **3.17×**、阶回到 **1.89** ⇒ **几何罪份额 ≈ 0.2 pp / 恢复 ≈ 0.2–0.5 pp**,余下 ≈ 11 pp 归**h=0.08 处 P1 场的固有能力** |
| P11 的路线分叉 | (a) Option C 重新定标 **已采纳**;(b) **等参 P2 壁面层** = P11 自己列为"**唯一能达到字面判据**"的路线,**未选**;(c) 接受为永久限制 |
| ★★ 任务 ② Q2(2026-08-11,本阶段) | **圆柱**上、**壁面自由度精确匹配**、**刻面几何逐位相同**时,结构化正交网格只把壁面 max\|ΔCp\| 改善 **3.4–9.6 %**,**阶毫无改善(两族 ≈ 1.0)** ⇒ 该算例的壁面误差**由 `O(h)` 刻面几何决定** |
| 本阶段上一轮 | M6 的 LE 亏空:**上表面 91 % 的预算是散布、检测不到偏置** ⇒ 指向壁面恢复/刻面几何 |

★★ **张力已经出现,而它是本轮的核心**:圆柱说"**几何是瓶颈**(阶 1.0)",
球说"**几何只占 0.2 pp、瓶颈是场阶/污染底**"。**这两条不能同时被当作一般结论用。**

## 2. 事前写死的问题(顺序即执行顺序)

- **A0 禁止项**:phase-2/3 roadmap 是否明文**禁止新增并行技术路线**?
  ★ 任务 ② 的预注册引用过这一条来否掉"真六面体单元"。**若它同样管住 P2 元,那结论在做任何技术论证之前就已经受约束** —— 必须先读原文,不凭印象;
- **A1 那个张力是真冲突,还是我自己制造的跨比较?**
  ★ 用常设第 5 问逐项查:**同一条流水线 / 同一个量 / 同一个 h 区间 / 同一种几何(球 vs 圆柱 vs M6 机翼)/ 同一个范数**。
  **在确认之前不得宣称"两条committed结论矛盾"**;
- **A2 P11 的机理是否**由构造**排除等参 P2?** mapped-P1 的失效是"几何阶 > 场阶"的**错配**;
  等参 P2 把两者拉齐 ⇒ **那条特定机理不适用**。这需要读 `curved_wall.py` 与 P11 的判定原文确认,**而不是从名字推断**;
- **A3 P2 在这个代码库里要动多少东西(只读定量)**:
  ★ 关键结构事实是 **P1 四面体的单元梯度是常数**,而人工密度上风、供体单元图、σ 输运、`element_mach2`、Jacobian 三项**都建立在"每单元一个 q²"之上**。
  用 grep 数出**依赖这一点的模块与内核**;
- **A4 有没有更便宜的干预打的是同一个机理?** 只读比较候选(全尺度加密 / 壁面恢复 / 正交壁面层已测 / …),
  ★ 并且必须报**每一条已经被测过什么**,不许把测过的东西当新点子。

## 3. 事前判据(**建议**的三条带,后续完全不同)

| | 条件 | 建议与后续 |
|---|---|---|
| **R1 值得开** | A2 成立(P11 机理不适用于等参 P2)**且** A1 判定"**在 M6 的 LE 相关 h 区间上,binding 误差确实是壁面表示/几何**"**且** A3 的改动面**有界**(见下)**且** A0 不禁止 | ⇒ 建议**另行预注册一个受限的第一步**(不是整套 P2):明确它先证明什么、在哪个最小算例上、什么条件下判死 |
| **R2 保持关闭** | A1/A2 任一不成立,**或** A3 显示改动会穿透上风/供体/σ 这些核心内核(下面写死了"穿透"的判法) | ⇒ 建议把上表面那 91 % **写进能力边界作为当前离散的固有限制**,并把 P2 记为**已评估-未开**(带理由),★ 而不是"没想到" |
| **R3 只读判不了** | A1 得出"两条读数**不可比**"且没有任何只读论证能分开它们 | ⇒ **不给方向**,只输出**一条最便宜的、能分开它们的测量**及其成本估计 |

**A3 "穿透"的事前判法(避免我事后拍)**:
若 P2 场要求修改下列**任意一处**的内核契约,即记为**穿透** ——
① 上风密度对"每单元一个 q²/ρ"的依赖;② `upstream_elements` 的面邻居/质心位移供体规则;
③ σ 输运的单元级 s 与指针倍增;④ Newton Jacobian 的三项装配。
★ 这四条是**事前列的**,因为它们都是本项目已经被咬过的地方(mixed-plain 四次、σ 环一次)。

## 4. 守卫

- **G-P(溯源)**:每一个引用的数字必须给出**它所在的已提交产物路径**;
  ★ 拿不到产物的数字**只能记为"prose-only,不作证据"**(纪律 #3);
- **G-5(第 5 问)**:每一次把两个数放在一起比较之前,写出它们的**流水线/级别/量/几何**;
  ★ 这一轮的整个价值取决于它,因为 A1 就是一次跨比较审计;
- **G-N(不许估**再**用)**:任何我算出来的派生数(份额、比值)必须标为**派生**,
  ★ 且**不许**用一个我自己估的数去支撑建议 —— 上一轮 §2 我就把一个手算派生数写错过。

## 5. ★★ KILL CRITERION

1. **需要一次求解才能回答** ⇒ **停**,把那次求解**另行预注册**;★ 不许"顺手跑一个";
2. **需要改库才能回答** ⇒ 停(本轮零库改动);
3. ★ **发现某个关键数字只存在于 .md 散文里** ⇒ 记为**未测**,**不得**据它下建议;
4. ★ **A0 显示存在明文禁止项** ⇒ 直接进 **R2**,并把技术论证记为"**受约束未评**",
   **不许**用技术论证去越过流程规则。

**明确不算 kill**:**R2**(路保持关闭)—— 它是一个诚实且有价值的结论,
它把"还有一条没试的路"变成"**已评估、有理由地不走**";**M3 仍不达标**(沿用前十一轮的豁免)。

## 6. 本轮不做

- **不改任何库代码、不跑任何求解**;
- ★ **不重跑 P11 的扫描**(它的产物已提交:`cases/demo/p11_curved_walls/results/`);
- ★ **不清那两条未清的注册义务**(GS2.1 §7 样本外验证 4 011 s、Q-B 直解臂)——
  它们与本轮无关,**继续挂账**;
- ★ **不因为"只剩这一条路"就倾向 R1** —— 判据把 R2 写成了同等命名结果,
  而**"唯一剩下的路"不等于"这条路可行"**。
