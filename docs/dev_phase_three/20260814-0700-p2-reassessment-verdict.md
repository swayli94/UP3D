# 20260814-0700 — 判定:**R2 —— 等参 P2 保持关闭**;而这一轮真正的发现是
# ★★★ **我四轮以来当作"未清且昂贵"的那条义务,早就在同一张已提交的 CSV 里付过了 —— 而它的答案是正的**

> **建议 = R2**(P2 不开),但**不是**按我上一轮给使用者的那个理由("接受为固有限制")。
> ★★★ 打开 `phases/p2/bench/gate_results/tip_allscales.csv` 之后:那里有**四条**全尺度加密腿,
> **LE 上 RMS 0.3899 → 0.2704 → 0.2358 → 0.1758,四级单调下降,共 −54.9 %**。
> GS2.1 §7.2 那条**事前可证伪的样本外预测**("coarse→T0→T1 必须单调下降,且 coarse 明显差于 0.2358")
> **被满足**(coarse = 1.65× 更差)—— 而它一直被我记为"未做,且要 4 011 s"。
> ★★ 所以上一轮"候选只剩 G1.6 家族"那句话**有一个洞**:我把"**LE 分辨率**"测死了,
> 却从没把"**按比例的全尺度加密**"单独列进候选表 —— 而它是唯一被测到**能降低对实验误差**的杠杆。
> ★ 而**不能**把它说成"体网格分级":§5c 里 M6 自己的单旋钮腿**只动误差 −1.4 %…+3.4 %**,
> 所以起作用的是**尺度按比例一起动**,不是"把体网格调细"。**这两句差别很大,而我差点合成一句。**
> ★ 代价必须同时说:那 −54.9 % 要 **25.2× 单元 / 549× 墙钟(7.2 s → 3 950 s)**,
> 而产品指标是**含网格 2 CPU·min** ⇒ **"全都加密"解释了误差,但它不是产品答案。**

预注册 [20260814-0500](20260814-0500-p2-reassessment-prereg.md)(动任何代码/求解之前提交,`7143e2b`)。
**零求解、零库改动**(kill 条 1/2 未触发)。

## 1. A0 —— 禁止项:**存在**,而且已经有一条同源的使用者裁决

`docs/dev_phase_two/roadmap.md` §"明确的禁止项"逐字:
> **不得再新增第二条并行技术路线**(第一阶段两套尾迹模型 × Picard/Newton 孪生 = 4 个驱动器,
> Track B 有 17 个阶段花在其中一条的病理上)。

而 **D6(2026-08-11,使用者裁决)**已经用**同一条动机**停掉了六面体路线,其原文包含:
> …⇒ 壁面误差由 `O(h)` 刻面几何决定,而平面刻面的六面体壁面一模一样 ⇒ 这条动机对 (A) 与 (B) 都被否;
> **只有曲面几何能动它,而 P11 已把 mapped-P1 测负。**

★ 并且 `docs/design.md` §5.1.3 自己写着,**可压/跨声速装配路径 + Kutta/尾迹 ↔ 曲面元耦合**
是"**its own phase** after the sphere leg lands" —— 也就是说,连 P11 那条只做 Laplace 的球腿,
都把跨声速应用**显式推给另一个阶段**。

⇒ 按注册 kill 条 4:**存在明文禁止项 ⇒ 直接进 R2**。★ 但我**不**用流程规则代替技术论证 ——
下面 A1–A4 照做,因为使用者有权知道技术上到底站不站得住。

## 2. A1 —— 那个"张力"是真的,但**不是我以为的那个**

我进场时的假设:圆柱的 `O(h)` 与球的 ~2 阶之差是**max vs mean 范数**造成的。**被产物否掉:**

| 圆柱(`bench/gate_results/hex_q2_wall_accuracy.csv`,非结构族) | 收敛阶 |
|---|---|
| `cp_err_max` | **1.019 / 1.015** |
| `cp_err_mean` | ★ **1.006 / 1.011** |
| `normal_err_max_deg` | **1.001 / 1.000**(逐级精确减半) |

⇒ **mean 也是 `O(h)`** ⇒ 范数不是出口。**我的假设是错的,而产物直接说了。**

★★ **真正的跨比较在别处**:球那条被反复引用的 **"阶 1.67/1.98"** 是
**`phi_err_wall_max`(壁面 φ)**;圆柱的 1.0 是 **Cp**。**两者差一次导数。**
把同一个量放在一起(`cases/demo/p11_curved_walls/results/e6_ico_control.csv`,DERIVED):

| 球 icosphere(全尺度,平面刻面) | 阶 |
|---|---|
| `phi_err_wall_max` | 1.668 / 1.977 ←（被引用的那个） |
| `cp_max` | **1.345 / 1.462** |
| `cp_mean` | **1.639 / 1.813** |

⇒ 同量比较后差距**缩小但不消失**(球 Cp 1.35–1.81 vs 圆柱 Cp 1.01),而两者**几何不同**
(球 vs 圆柱)、**流态不同**(都是 Laplace,但不是 M6 的跨声速升力场)。
★ **结论:"M6 的 LE binding 误差就是壁面几何"这个前提,在证据上并未成立** ——
它是从两个都不是 M6 的算例外推来的,而那两个算例**彼此还不一致**。

## 3. A2 —— 对 P2 **有利**的那条硬事实(必须原样说)

`pyfp3d/solve/curved_wall.py` 与 `docs/design.md` 的机理是**构造性**的:
mapped-P1 = **几何二阶 / 场一阶**的**错配**,曲面元上"物理线性函数不再被精确再现"(coarse 球实测偏差 **0.138**),
误差 `O(h)` 与它移除的刻面法向 `O(h)` **同阶** ⇒ 净收益被吃掉。

★ **等参 P2(几何二阶 + 场二阶)按构造没有这条缺陷** —— 一致性恢复。
★★ 而且更强的一条:P11 的**再归因**把余下 ≈11 pp 归给"**h=0.08 处 P1 场的固有能力**",
**那正好是等参 P2 的另一半(场阶)所攻击的东西** ⇒
**P2 不是"那个已经失败的几何修正",它同时抬场阶。** 这一点我不能因为结论是 R2 就淡化。

## 4. A3 —— 但**穿透 4/4**,而这是注册里**事前**写死的判 R2 条件

| 事前列的内核契约 | 位置 | 是否穿透 |
|---|---|---|
| ① 上风密度依赖"每单元一个 q²/ρ" | `kernels/upwind.py`(`per element` 明写于 §3.1'/3.2) | ★ **是** —— P2 场的单元内梯度**不是常数**,"每单元一个 q²" 需要重新定义 |
| ② `upstream_elements` 供体规则(面邻居 + 质心位移) | `kernels/upwind.py` + `kernels/entropy.py` | **是** —— 输入 `V_e` 不再是常向量 |
| ③ σ 输运(单元级 s + 指针倍增) | `kernels/entropy.py` | **是** —— 同上 |
| ④ Newton Jacobian 三项装配 | `kernels/jacobian.py` | **是** —— P1 四面体 4 DOF → P2 十节点 10 DOF,单元块 4×4→10×10,**CSR 图变** |

规模(只读实测):**38 个 `@njit` 内核**分布在 `entropy / gradient / upwind / jacobian / residual` 五个文件,
其中 **22 处**硬编码"每四面体 4 个节点"的模式(`gradient.py` / `upwind.py` / `residual.py` / `jacobian.py`)。
★ 对照 P11 的实际改动面:`curved_wall.py` 是**纯 numpy、opt-in、只挂在 `picard.py` 的 Laplace 路径上**
(`stiffness_delta`)—— 它之所以便宜,正因为它**没有**碰这四条契约。

⇒ **按注册的事前判法,四条中任一穿透即 R2。这里是 4/4。**

## 5. ★★★ A4 —— 唯一被测到能降误差的杠杆**不是 P2**,而它的 M6 样本外验证**已经付过了**

### 5a. 球上:同一量、同一几何、同一流水线的杠杆对比

| 干预(球,`cp_mean`) | 效果 | 产物 |
|---|---|---|
| **曲面壁面**(mapped-P1,medium,同一张网格 A/B) | **−8.0 %** | `results/g11_1_ab.csv` |
| **只加密体网格**(h_min 0.03 不变,`h_max` 3.0→1.0) | ★ **−50.4 %**(`phi_err_wall_max` −68.5 %) | `results/e8_bulk_floor.csv` |

⇒ **体网格杠杆是壁面曲面杠杆的 6.3×**(DERIVED;同量/同几何/同流水线/**同一壁面离散**,这是一次合法的同族比较)。
★ 而 M3 的度量是**带内 RMS**(均方),所以 `cp_mean` 这一列才是对应的那一列,不是 `cp_max`。

★★ **但这条不能外推到 M6,而且是 M6 自己的数据挡住的** —— 见 §5c。**先写在这里,免得读者只读到 6.3×。**

### 5b. ★★★ M6 上:那条"未清义务"其实已经付了 —— 我只读了 .md 的表,没打开 CSV

GS2.1 §7.2 事前写的样本外预测:
> 最便宜的样本外预测:**coarse + taper**(55 k tets,约 **20 s**)—— 若"全尺度加密"是真杠杆,
> 则 coarse → T0 → T1 的 LE 上 RMS 必须**单调下降**,且 coarse 明显差于 0.2358。
> 这是一条**可被证伪的事前预测**,而且几乎免费。

`phases/p2/bench/gate_results/tip_allscales.csv` 里**已经有那条腿,还多一条**(全部 `converged`、0 lim/0 flr):

| 腿 | h_wall | h_le | tets | **LE 上 RMS** | 池化 | cl_p | M_max | 求解 s | g1 |
|---|---|---|---|---|---|---|---|---|---|
| `Tm1_coarse_taper` | 0.030 | 0.01500 | 46 067 | **0.3899** | 0.1992 | 0.2506 | 1.365 | **7.2** | PASS |
| `Tp5_h020_taper` | 0.020 | 0.01000 | 145 303 | **0.2704** | 0.1454 | 0.2645 | 1.592 | 60.9 | PASS |
| `T0_baseline_taper` | 0.015 | 0.00750 | 350 718 | **0.2358** | 0.1249 | 0.2735 | 1.672 | 248.1 | FAIL |
| `T1_allscales_taper` | 0.010 | 0.00375 | 1 159 200 | **0.1758** | 0.1008 | 0.2811 | 1.956 | 3 950.4 | FAIL |

- **四级单调下降**,**LE 上 −54.9 %**、池化 **−49.4 %**;
- 预测的两条都满足:**单调** ✓,**coarse = 0.3899 = 0.2358 的 1.65×,明显更差** ✓
  ⇒ ★ **那条事前可证伪的预测被满足,而证据一直在 HEAD 里躺着。**

★ **诚实的限定,三条,一条都不能省:**
1. **阶是乱的且低于一阶**(DERIVED,按 h_wall:**0.902 / 0.476 / 0.725**)⇒
   这是**单调下降**,**不是**干净的网格收敛 —— 不许说成"二阶收敛已验证";
2. ★★ **解本身在动**:M_max **+43.2 %**(1.365→1.956)、m1_max +35.3 %、σ_min −14.5 %、cl_p **+12.1 %**
   ⇒ 那 −54.9 % **不能全部**归给"同一个解被解析得更准",里面混着"**解变了**";
3. ★ **不许把这四条腿的数字接到 HEAD 上**:它们是 2026-08-01 的同一次会话(同代码、同配方,内部自洽),
   而 M6 分诊那轮已经测出 LE 数字在两次生产变更后**移动过**(第 5 问,跨时间)。

### 5c. ★★★ 而球的"体网格杠杆"在 M6 上**没有复现** —— 这一条必须和 6.3× 一起说

`docs/dev_phase_two/PHASE_TWO_CAPABILITY_BOUNDARY.md` 同一节里,phase two 的 LE 因子实验记着**两句相反的话**:

- **依据 2 / 判据 P(已达成)**:LE 亏空由**体网格(含近体分级)**主导 —— 只动"远场"那一腿把 LE 带 Cp
  **移动 0.09811 = 全尺度加密 0.12067 的 81 %**(判据要 ≥ 60 %),且是其自身 LE 污染上界的 **10.4 倍**;
- ★★ **同一节紧接着的"同时被否掉的推论"**:**同一批腿对实验的 LE 误差只动**
  **0.28214 → 0.29171(+3.4 %,更差)/ 0.28017(−0.7 %)/ 0.27810(−1.4 %)** ——
  **位移 0.098–0.143,而误差只动 −1.4 %…+3.4 %**。

★★★ **所以"体网格主导"说的是它主导 Cp 的位移,不是它降低对实验的误差。这两件事在 M6 上被明确地分开了,而我在 §5a 差点把球上的 −50.4 %(那是**误差**)读成同一件事。**
⇒ 两个几何**不一致**:球上"固定壁面、只加密体网格"把**误差**降 50.4 %;M6 上同类的单旋钮腿**几乎不动误差**。
★ 三家一致的只有一条:**只动单个尺度不行(壁面-only 在球上不行、LE-only 在 M6 上 −8.3 % 且非单调、单旋钮腿在 M6 上 −1.4 %…+3.4 %),而把所有尺度按比例一起动**才降误差**(球 icosphere 阶 1.64–1.81;M6 四级阶梯 −54.9 %)。**
★★ 这对本轮的判定是**加强**而不是削弱:P2 攻击的是**壁面表示**,而**壁面-only** 恰恰是在两个几何上都被测过不起作用的那一类;唯一被测到起作用的杠杆(**按比例的全尺度**)不是 P2。

### 5d. 那条被扣住的读数,现在为什么可以放出来(而且**不是**靠放宽判据)

GS2.1 的处置是:尖端治愈闸门 **G1 FAIL ⇒ 不得把 T1 的 RMS 当作 P11 杠杆的答案**。照办是对的。
★ 而 **G1 的用途**是"先确保尖端不污染 LE 带的读数"。**本会话的尖端轮(W2,2026-08-13)**
用**无代理**的剂量响应直接测了这件事:`tip_taper` 半径 3 档,**LE-RMS 展向剖面几乎不动**
(外/内比 ΔR = **0.0027**、各站 ≤ 0.0004),而 **cl_p 动了 −3.13 %**(旋钮确实生效)。
⇒ **尖端处理对 LE 带的影响被独立定量地限住了**,所以扣住 T1 读数的那个理由不再成立。
★ 限定:W2 变的是**半径**(不是 taper 开/关),且只在 medium 上 ⇒ 它**界定**了该剂量范围内的影响,不是全域证明。
★★ 而 G1 本身 `M_max < 1.6` 这条**随加密必然失效**(表里 1.365→1.956,`g1` 从 PASS 翻 FAIL)——
它正是它自己作者标注过的"手拍阈值",也正是 roadmap 禁止项"**不得用 M_max 作为能力指标**"管的那种用法。

★ **我不越权**:GS2.1 的 **D2 带**写作"T1 相对 T0 **≤ 0.15**",实测比值 **0.7452 = −25.5 %**。
"比值 ≤0.15"(减 85 %)与"变化 ≥15 %"两种读法都讲得通 ⇒ **两种都报,不宣称该带被满足**。
可以宣称的只有 §7.2 那条**措辞无歧义**的样本外预测。

## 6. 判定与建议

**R2 —— 等参 P2 保持关闭**,理由按重要性排序:

1. **前提未成立**(A1):"M6 的 LE binding 误差是壁面几何"是从两个都不是 M6、且**彼此不一致**的算例外推的;
2. ★★ **唯一被测到能降低误差的杠杆不是 P2**(A4/§5c):**按比例的全尺度加密** ——
   M6 四级阶梯 **−54.9 %(单调)**,球 icosphere Cp 阶 **1.64–1.81**;
   而 **壁面-only** 这一类在**两个几何上都被测过不起作用**(球 h_min-only 阶塌陷、M6 LE-only −8.3 % 非单调)。
   ★ **自我更正**:我不能把球的"体网格 6.3×"当作支持 M6 的第二处测量 —— §5c 里 M6 自己的单旋钮腿
   **只动误差 −1.4 %…+3.4 %**,把它算进来会是一次跨几何的越权外推(第 5 问)。
   **先花在已测能降误差的那条上**,这也是 roadmap 禁止项"不得在根因未修时做精细化"的同一逻辑;
3. **成本与流程**(A3/A0):穿透 4/4 条内核契约(事前判法)、38 个 njit 内核 / 22 处 4 节点硬编码、
   design.md 自己把跨声速 + Kutta 耦合列为"另一个阶段"、roadmap 明文禁止新增并行技术路线、
   D6 已用同源动机停过一条路线。

★ **同时记下 P2 唯一站得住的那半边**(A2):它**按构造**免疫 P11 的失效机理,并且**抬场阶** ——
所以它**不是被否证,而是尚未被证明必要**。而现在它的立项路径**具体化**了:
**先测"在固定单元预算下,靠分级/自适应能拿回 T1 那 −54.9 % 的多少"**;
若答案是"拿不回来,只能靠 25× 单元",那**提高阶数**就第一次有了**被测量支撑的**立项理由。
这比"它是唯一剩下的路"强得多 —— 后者从来不是证据。

## 7. ★★ 三条勘误(纪律 #11:动过的数字要在同一次提交里逐处更正)

**勘误 1 —— 我把那条义务的成本记错了,而且据此推迟了四轮。**
我在 `20260813-2100` §0 写"那条样本外验证至今未做,而 T1 单腿 **4 011 s** ⇒ 本轮不做它",
并在 `20260813-2300` / `20260814-0300` / `20260814-0500` 沿用。
**4 011 s 是 T1/T2 的成本,不是那条义务的成本** —— §7.2 登记的样本外腿是 **coarse + taper,约 20 s**
(实测 `Tm1_coarse_taper` **7.2 s**)。★ **而它已经跑过了。**
⇒ 我用一个错的成本把一条**几乎免费、且已经付清**的义务推迟了四轮。
**教训**:"这条义务很贵"是一个**可以查证的事实断言**,不是背景假设 —— 查 CSV,别查 .md 的表。
★ 同族:M6 分诊那轮的"LE 70 % 是陈的"也是**只读了脚本的默认值没读证据**。

**勘误 2 —— 上一轮"候选只剩 G1.6 家族"那张表有一个洞。**
表里"**LE 分辨率**"被正确地测死(GS2.1 第一步:只动 h_le,−8.3 % 且非单调),
但**"全尺度 / 体网格分级"从未作为一条独立候选出现过** —— 而三处独立测量都指着它:
球的 E8(体网格 −50.4 %)、M6 的四级阶梯(−54.9 %)、以及 phase two 已记的
"`h_far` 其实是体网格分级旋钮,**把近体调粗会让 LE 误差变差 +3.4 %**"。
⇒ "只剩 G1.6"应改写为:**"LE 带误差由体网格/全尺度分级主导(已测),其中不可由分级消除的余量归 G1.6 家族(未分离)"**。
★ 这不是措辞问题:它把下一步从"接受固有限制 / 开 P2"改成"**先量分级能拿回多少**"。

**勘误 3 —— 能力边界把大网格成本少写了 4.2×。**
`PHASE_TWO_CAPABILITY_BOUNDARY.md` 的 **M3b 表**与**依据 5** 都写 T1(1.16 M tets)**950 s**,
而已提交产物 `tip_allscales.csv` 的 `solve_s` = **3 950.4 s**(GS2.1 结果 .md 写 4 011 s,含生成)。
★ 看形状像掉了一个 "3"。**这处不是无害的**:那张表正是用它论证"大网格未达标"对 **< 60 s** 目标的距离 ——
少写 4.2× 会把差距从 **66×** 说成 **16×**。已在两处就地更正并标注。

## 8. kill criterion 与守卫

| 条 | 是否成立 |
|---|---|
| 1. 需要求解 | 不成立(零求解) |
| 2. 需要改库 | 不成立(零库改动) |
| 3. 关键数字只存在于散文 | ★ **反向成立**:我以为要新测的数字**存在于已提交 CSV**,而 .md 的表**漏报**了两条腿 ⇒ 记为勘误 1 |
| 4. A0 有明文禁止项 ⇒ 进 R2 | ★ **成立** —— 已按注册进 R2,且技术论证照做未省 |

| 守卫 | 结果 |
|---|---|
| **G-P 溯源** | 每个引用数字都给了产物路径;无 prose-only 数字进入论证 |
| **G-5 第 5 问** | ★ **抓到两次**:范数(max/mean,被否)与**量(φ vs Cp,差一次导数)**;并禁止把四级阶梯接到 HEAD |
| **G-N 派生标注** | 阶、比值、6.3× 均标 DERIVED |

## 9. 下一步(**另行预注册**,使用者裁决)

1. ★★ **首选(问题已按 §5d 收窄)**:M6 的单旋钮/分级腿**已经测过,而且不降误差** ——
   所以下一轮**不是**"再试一个分级旋钮",而是:**在固定单元预算下,把网格按"全尺度比例"重分配,看能拿回多少 T1 的 −54.9 %**。
   ★ 这条与前面那批腿的区别是**事前就说清的**:那批变的是**单个尺度**,这条变的是**尺度之间的比例**(而总量固定)—— 若连按比例重分配都拿不回来,那 T1 的收益就是**纯单元数**,那才轮到"提高阶数"。
   ~~在固定单元预算下量"分级"能拿回多少 T1 的收益。~~
   这是把 §5b 的发现变成产品答案的唯一路径,而且它**直接对着 2 CPU·min 指标**:
   T1 的 −54.9 % 要 25.2× 单元 / 549× 墙钟,产品上不可接受;问题因此从
   "误差来自哪"变成"**同样的精度能不能用同样的单元数买到**"。
   ★ 而 phase 3 任务 ② 留下的 `pyfp3d/meshgen/structured.py` + **G0 逐位单变量**
   正是为这种实验造的(D6 明写它作为**实验工具**保留)—— 这条路线**不需要**新技术路线;
2. **在 HEAD 上重建那条全尺度阶梯的同代码基线**(§5b 限定 3):四条腿是 2026-08-01 的,
   而 LE 数字在那之后移动过;★ 最便宜的腿只有 **7.2 s**,所以这条重建**几乎免费**;
3. **P2 记为"已评估-未开"**,连同 A2 那半边有利事实与 §6 的立项条件一起记入能力边界 ——
   ★ 这样它是"**有理由地不走**",不是"没想到";
4. 仍悬着:**Q-B 直解臂**、**coarse 上两条流水线差 2.0 %**。
