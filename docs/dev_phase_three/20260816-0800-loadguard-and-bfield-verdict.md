# 20260816-0800 — 判定:**① G-L2 PASS ⇒ 趋势可判 = T-HOLD(我的预测被推翻)**;
# **② B-PHYS(我的解释也被推翻)—— 横流是后缘的真实结构,不是粗网格噪声**

> ★★ **① G-L2 通过**:三明治两端 **S2 136.8 s → S2r 134.9 s = +1.3 %**(≤10 %)⇒ **趋势有资格被判**。
> **T-HOLD**:P1 = **25.82 / 10.21 / 6.07**(S0/S2/S4),**单调下降**但 **S4 的 6.07 仍在带 [4, 20] 内**
> ⇒ ★★ **我事前登记的预测(T-DROP,~3×)被推翻** ——**S4 的成本动机在生产级成立。**
> ★★★ **② B-PHYS**:top-1 % |B| 的空间格重叠 **J = 0.600**,零假设 **0.060** ⇒ **10.0×**(B-PHYS 需 ≥4×)
> ⇒ **我"粗网格 B 是离散噪声"的解释被推翻**。
> ★ 而空间图像干净得没有歧义:**两级的 top-1 % |B| 都在 x/c 0.97–1.00(后缘)**,
> S2 覆盖 z/b 0.00–0.55、S4 覆盖 0.00–0.97 ⇒ **同一个后缘横流结构,细网格只是在更大展向范围上解析它。**
> ⇒ **P2 的"塌陷"是一个真实结构的幅度收敛,不是伪影消失。**
> ★★ **本轮两次被自己的报告层缺陷挡住**(遗留的门常量、按 tag 查无粘表),各烧掉 ~17 min ——
> 项目自己的教训"**守卫报告代码要像守卫求解代码一样**"第 N 次应验;第二次之后我改成**先干跑校验再花机时**。

预注册 [20260816-0600](20260816-0600-loadguard-and-bfield-prereg.md)(动代码前提交,`0e38496`)。
脚本 `bench/run_task3_s4_premise_check.py` → `gate_results/task3_s4_premise_check.csv`;U 落盘 `U_S{2,4,2r}.npz`。

## 1. ① 守卫替换成功,趋势判定 = **T-HOLD**

★★ **旧 G-L 的问题不是阈值,是它量的东西**:`load average` 含本进程 ⇒ 量自己的效应。
**新 G-L2 直接量它本来要量的性质**(可重复性),而且**它只读自己两条腿的墙钟、不读任何全局状态**
⇒ **不自指**(事前声明的 G-B)。

| | S2(前) | S4 | S2r(后) | G-L2 |
|---|---|---|---|---|
| 一次 IBL | **136.8 s** | **918 s 级** | **134.9 s** | ★ **+1.3 % ⇒ PASS** |

★ 顺带:S2 的 IBL 现已有**四次独立测量**:133.5 / 133.7 / 135.1 / 136.8(与同进程的 134.9)
⇒ **散布 ≤ 2.5 %** ⇒ 这台机器上单次 IBL 的墙钟**是可重复的**。

**趋势(现在有资格判)**

| 级 | 体单元 | BL 未知量 | 一次 IBL | 无粘 | **P1** |
|---|---|---|---|---|---|
| S0(跨运行) | 9 303 | 5 676 | 23.2 s | 0.9 s | **25.82×** |
| S2 | 59 359 | 21 342 | 136.8 s | 13.4 s | **10.21×** |
| ★ **S4(生产)** | **448 197** | **82 584** | ~918 s | 150.6 s | ★ **6.07×** |

**单调下降 ✓**,而 **6.07 ∈ [4, 20]** ⇒ **T-HOLD**。
⇒ ★★ **我的预测被推翻**(我写的是"落到 ~3× ⇒ T-DROP")。**方向对、幅度错**,
而上一轮的标度指数(IBL 1.41 / 无粘 1.20 ⇒ P1 ~ 体积^−0.26)仍然成立,
它只是**预测下一级才穿带**,不是这一级。
⇒ **S4 的成本动机在生产级成立** —— 这是本轮对 GS4.1 立项最实际的一句。

## 2. ★★★ ② B-PHYS:横流是**后缘的真实结构**

**binding 量(事前登记、未看过):top-1 % |B| 节点的空间格持久性**(10×10 格,局部 x/c × z/b)。

| 级 | n_live | top-1 % 节点 | **x/c 范围** | z/b 范围 | 占格 |
|---|---|---|---|---|---|
| S2 | 2 575 | 26 | ★ **0.9817–1.0000** | 0.000–0.554 | x 格 **9**,z 格 0–5(6 格) |
| **S4** | 9 814 | 98 | ★ **0.9712–1.0000** | 0.000–0.966 | x 格 **9**,z 格 0–9(10 格) |

**J = |bins(S2) ∩ bins(S4)| / |bins(S4)| = 6/10 = 0.600**;
**零假设(随机)= |bins(S2)|/100 = 0.060** ⇒ **J / 零假设 = 10.00** ⇒ **B-PHYS**(需 ≥4×)。

★★★ **所以我的解释错了**,而正确的图像**更简单**:
**横流集中在最后 3 % 弦长(后缘)**,两级都是;**细网格把它在几乎整个展向上解析出来**
(z/b 0.55 → 0.97),而**幅度**从 max|B| 41.2 降到 3.73。
⇒ **这是一个真实的后掠翼后缘横流结构,其幅度在粗网格上被高估**,
**不是**"粗网格噪声在加密后消失"。

★★ **对上一轮的勘误(理由错,结论不变)**:上一轮我写
"**逐点横流 >1 那条削弱撤回(粗网格伪影)**"。
**撤回本身仍然成立**(生产级逐点 max 只有 **0.0315**),
**但理由"粗网格伪影"被本轮否证** —— 正确的理由是:
**那是一个真实结构,其幅度在粗网格上被高估,而在生产分辨率上小到 3.2 %。**

★★ **对 S4/GS4.1 的实际含义(必须这么说,不能说成"它是噪声")**:
二维条带 IBL **丢掉的是一个真实的后缘横流**;在**生产分辨率**上它的比值是 **0.0186**、逐点最大 **3.2 %**
⇒ **"丢掉它是可接受的"是一个有测量支撑的取舍**,而**不是**"那里本来什么都没有"。

## 3. ★★ 本轮被自己的报告层挡了两次(过程记录)

1. **遗留的门常量**:预注册写 **2 400 s**,而脚本仍是上一轮的 **900 s**
   ⇒ **重复腿被我自己的旧门跳过**,G-L2 无法评估(烧掉 ~17 min);
2. **按 tag 查无粘表**:重复腿 `S2r` 在 CSV 里没有行 ⇒ **kill 条 3 停机**
   (φ 路径我做了 `rstrip('r')`,**查表那处漏了**)(又 ~17 min)。

⇒ 两处都在**报告/仪器层**,而项目已记过 "**Guard REPORT code the way solver code is guarded**"。
★ **第二次之后我改了做法**:**先干跑校验三条查找**(inviscid / φ / LEVELS / 门常量)**再花机时** ——
校验用了几秒,而它本可以省掉前面 34 min。**这条应当进常设做法。**

## 4. kill criterion 与守卫

| 条 | 是否成立 |
|---|---|
| 1. 缓存缺失 | 不成立 |
| 2. 任一腿 IBL 失败 | 不成立(三腿全跑出) |
| 3. G-R(非计时)不过 | 不成立 —— 3557 / 0.1471 **精确** |
| 4. 总墙钟 > 2 400 s | 不成立(~1 190 s) |
| 5. 三级 top-1 % 格集合完全相同 | 不成立(6 格 vs 10 格)⇒ **不是"我算了三次同一套网格"** |

| 守卫 | 结果 |
|---|---|
| ★★ **G-L2(新)** | **PASS**,+1.3 % ⇒ 趋势可判;★ 且**不自指**(只读自己两条腿的墙钟) |
| **G-R(非计时)** | PASS(精确) |
| **G-φ / G-cache** | φ 路径与形状打印;★ **U 已落盘**(上一轮缺的正是它) |
| **G-U** | U 的列含义(1=A、2=B)沿用从 V5 桥读出的定义 |
| ★ **G-B(自指自检)** | 事前声明并成立 |

## 5. 读数汇总(四句话)

1. ★★ **① T-HOLD**:P1 = 25.82 / 10.21 / **6.07**,单调下降但**生产级仍在带内**
   ⇒ **我的预测(~3×)被推翻**,**S4 的成本动机在生产级成立**;
2. ★★★ **② B-PHYS**:J/零假设 = **10.0** ⇒ **我"粗网格 B 是噪声"的解释被推翻**;
   横流是**后缘(x/c 0.97–1.00)的真实结构**,细网格把它解析到几乎整个展向,**幅度**在粗网格被高估;
3. ★★ **勘误(理由,非结论)**:上一轮"逐点横流 >1 是粗网格伪影"的**撤回仍成立**(生产级 max 3.2 %),
   但**理由改为**"真实结构 + 粗网格高估幅度";
   ⇒ GS4.1 丢掉的是**真实的**后缘横流,**取舍有测量支撑**,不是"那里没东西";
4. ★★ **守卫替换成功**:G-L(量自己)→ G-L2(量可重复性)⇒ **换代理,不是放宽阈值**;
   而本轮两次被自己的**报告层缺陷**挡住,已把"**先干跑校验再花机时**"写进做法。

## 6. 下一步

1. ★★ **GS4.1 现在可以立项**,而文书必须写进三条:**成本动机在生产级成立(6.07×)**、
   **P1 会在下一级穿带**(标度指数外推)、**丢掉的横流是真实的后缘结构(生产级 ≤3.2 %)**;
2. ★ **P2 的工况维度仍未测**:第一阶段的 0.072 在 **M0.5**,本阶段全部在 M0.8395
   ⇒ 若要引用"跨工况"结论,需要 M0.5 的 φ(**没有缓存**)⇒ 单独一轮;
3. 仍悬着:**Q-B 直解臂**、**coarse 两流水线差 2.0 %**、`wing3d.py` docstring/签名矛盾、
   **M4 缺翼身粘性 case 入口**。
