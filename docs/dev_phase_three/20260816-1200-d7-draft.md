# 20260816-1200 — **裁决 D7 的起草**(任务 ③ 的处置)

★★ **这不是一条裁决。** `docs/dev_phase_two/roadmap.md` §7 的表头写着
「**已裁决事项(使用者决定**,带日期;**不得在后续轮次里被静默改掉**)」
⇒ **第 1 块(裁决本身)留空给使用者**;我只把 **依据 / 要锁住的事 / 重开条件** 按已提交证据填好。
★ 本会话已因**漏读 D2** 栽过一次(把一条你已否决的路线写成"已评估-未开",勘误 `d849bf5`),
所以这份起草**不预设**你会选哪条。

---

## 1. 需要你选的那一块(四条互斥,后续完全不同)

| | 选项 | 它承诺了什么 |
|---|---|---|
| **(a)** | **任务 ③ 收口,转产品线** | LE 线以**测量结论**收口;下一步 = **GS4.1**(文书已备)+ 补 **M4 的翼身粘性 case 入口**(实现任务) |
| **(b)** | **只开 GS4.1** | 采纳 [立项文书](20260816-1000-gs41-initiation.md)(含其 5 条风险,首条:"整机翼 <1 s" 对生产级实测 918 s 是 **~900×** 的目标);LE 线暂挂 |
| **(c)** | **继续 LE 线** | ★ 需要**你显式推翻 D2**(等参 P2 已被否决)或新开元技术路线 —— 后者与 §3 禁止项"**不得再新增第二条并行技术路线**"**直接冲突** |
| **(d)** | **先清挂账** | Q-B 直解臂、coarse 两流水线差 **2.0 %**、**M4 case 入口**、P2 的 **M0.5** 工况维度 |

★ 也可以组合(例如 **(a)+(b)**:收口并开 GS4.1)。

---

## 2. 可直接粘贴的表格行(裁决字段留了占位符)

> ★ 用法:把下面**整行**粘到 §7 表的 D6 之后,把 `【裁决:(a)/(b)/(c)/(d) —— 待使用者填】`
> 换成你的决定;其余内容都是**已提交证据**,不需要改。

```
| **D7** | 2026-08-16 | phase 3 任务 ③「其余问题」的处置(34 轮已执行;阶段边界核查已完成) | 【裁决:(a) 收口转产品线 / (b) 只开 GS4.1 / (c) 继续 LE 线(需显式推翻 D2)/ (d) 先清挂账 —— 待使用者填】 **阶段边界核查**:全门控集 **488 passed + 1 skipped + 3 xfailed + 1 XPASSED,0 failed**(1:11:48 @16 线程;四数对平:未门控 479+12+2 ⇒ 解锁 11 个 skip ⇒ 9→passed、1→xfailed、1→XPASSED);★ 那个 xpass 由 mark 自身预言(`test_p4_transonic::test_g41_transonic_medium_gate` 故意非严格,理由原文记着"16 线程收敛 / 8 线程不收敛")⇒ **无回归**,并得出常设一句:**门控计数必须带线程数引用**;快速能力锁层 **5/5 绿**(526 s);未门控套件 **479+12+2,0 failed**(447.77 s @8t)= 记录基线。**实测依据(按发现顺序)**:① **加密使鲁棒性变差的上游是离散解不唯一** —— M0.80/α1.25 上只换种子即使 x_shock 散 **0.168–0.306 c**(= M1 容差的 **31–56×**),而包线内 M0.72 唯一(0.0002 c)⇒ **解选择被钉住之前,M1 的 (b)(c) 在该工况上不可测量**;② **σ 不是选择算子**(σ OFF 两族都唯一,x_shock 散布 0.0000;多解随 σ ON 一起出现),σ 在第 8 步被**永久冻住**且冻在哪个相位由种子决定 ⇒ cl 0.26 vs 0.35;极限环已复现(周期 2–5)并**已交付纯读判的报警**;三条治法(软集合 / 幅度守恒 / 捕获选择)全部实测,**捕获选择被 K3 判死**;σ 线以"**唯一性 vs 修正强度是取舍**"写进能力边界;③ **M6 LE 线**:旧数 **69.6 %/0.2361 已退役**(出处 CSV 早于三次配方变更),HEAD 替代读数 **70.35 %/0.2422**;弦向错位(H2)、尖端(W2 无代理剂量响应 + η0.96/0.99 样本外)、局部弦长、Kutta 形式、σ(比值 1.000)**逐条否证**;LE 误差在上表面是**纯散布**(f_bias 五站全 ≤ 1/n)、下表面有**真实小偏置**;固定预算下**把单元搬进 LE 带是真杠杆但会饱和**(落在族曲线下 ~17 %,`h_edge` ≈ 生产值 1/2–2/3 见底);④ ★★ **自收敛读数:LE 上表面的 `d_self` 随加密增大**(0.2417→0.2928,**R = 1.2245**)而 LE 下/MID 的级差在缩小 ⇒ **"换网格解决不了 M3"的证据基础失效**(它是从一个**没在收敛**的序列做四点外推);分区检验 **P-MIX**:近奇异内区以 **41.7 %** 的点占 **74.9 %** 的 d² 质量、去掉它 d_self 减半,**但外区仍不收敛**(R = 0.91)⇒ 该解释**只覆盖约一半**;⑤ **产品线**:S4「做减法」的两条前提在 HEAD 上重测 —— **P1 = 6.07×**(生产级,T-HOLD,经可重复性守卫 +1.3 % 后裁决;标度指数 IBL^1.41 / 无粘^1.20 ⇒ P1~体积^−0.26 ⇒ **下一级会穿带**)、**P2 = 0.0186**(逐点 max 0.0315);★ **横流是后缘(x/c 0.97–1.00)的真实结构**(空间格重叠 J = 10× 零假设)⇒ **不是粗网格噪声**。**不许被静默改掉的六条**:(i) **M3 的可达性现在是"未知"**,不是"不可达"也不是"可达" —— 旧的"不可达"外推**证据不成立**;(ii) **LE 占 70 % 的旧数已退役**,新旧**不可比**;(iii) **两处 flat-cap 污染**(四级全尺度阶梯、"LE 分辨率已否证")—— 平截帽在加密下发散,LE 带阶 0.37 flat / 0.87 round;(iv) **横流是真实结构**,GS4.1 的二维条带丢掉的是真东西(生产级 ≤3.2 %,**有测量支撑的取舍**);(v) ★ **D2 仍然有效**(等参 P2 已否决;本会话曾把它误写成"已评估-未开+立项条件",已勘误);(vi) **门控计数带线程数**。**过程产出(代价换来的做法)**:本阶段自查出 **7 个判据缺陷**(最新一个是"**定带用的参考数跨工况**")、**3 次自指守卫**(`pgrep -f` / G-Z 匹配自己的禁止词 / G-L 量自己的负载)、**2 次报告层缺陷各烧 ~17 min** ⇒ 新增两条常设做法:**守卫必须自问"我的存在会不会改变我要量的东西"**、**先干跑校验查找/常量/级别清单再花机时**。**重开条件**:(c) 需**显式推翻 D2**;LE 线若重开,须先给出**外区那另一半**的机理候选(本阶段已把 σ/尖端/激波足迹/局部弦长/Γ 全部测负);GS4.1 若开工,须在文书里带上"P1 会在下一级穿带"与"丢掉的横流是真实的"两条。 |
```

---

## 3. 起草时**刻意没做**的三件事

1. ★ **没填裁决** —— 那是 §7 表头明写的"使用者决定";
2. ★ **没把任何数字四舍五入或美化** —— 每个数都能在本阶段的已提交 CSV / 轮次文件里找到;
3. ★ **没把"约一半"写成"主要原因"** —— 分区检验是 **P-MIX**,内区解释约一半,**外区仍不收敛**,
   这条限定必须留在裁决里,否则下一轮会把它当成已解释。

## 4. 我建议的选项(**建议,不是裁决**)

**(a) + (b)**:任务 ③ 以测量结论收口,并开 **GS4.1**。理由:
- LE 线的**候选已被逐条测负**,而剩下的路要么**撞 D2**、要么**再切一次带**(而切带已证明只解释一半);
- **产品指标从未被本阶段碰过**,而 GS4.1 现在**有 HEAD 依据**(P1 6.07×、P2 0.0186)且**风险已写明**;
- ★ 反面也说清:选 (a)+(b) 就意味着**接受 M3 的可达性停在"未知"**,而不是继续追它。
