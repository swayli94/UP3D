# 20260816-1200 — **裁决 D7 的起草**(任务 ③ 的处置)

★★ **这不是一条裁决。** `docs/dev_phase_two/roadmap.md` §7 的表头写着
「**已裁决事项(使用者决定**,带日期;**不得在后续轮次里被静默改掉**)」
⇒ **第 1 块(裁决本身)留空给使用者**;我只把 **依据 / 要锁住的事 / 重开条件** 按已提交证据填好。
★ 本会话已因**漏读 D2** 栽过一次(把一条你已否决的路线写成"已评估-未开",勘误 `d849bf5`),
所以这份起草**不预设**你会选哪条。

---

## 1. 需要你选的那一块(四条互斥,后续完全不同)

### 1.0 先把代号解释清楚(下面和第 2 节的表格行都会用到)

★★ **一个必须先提醒的重名**:`S4` 在本项目里指**两个不同的东西** ——
① **阶段 S4 =「粘性重启」**(phase-two roadmap 的第四个阶段);
② 本阶段自收敛射线的**网格级别 S4**(h_wall = 0.015、448 197 单元、即生产级)。
下文一律写成「**阶段 S4**」或「**级别 S4**」,不单写 S4。

**产品指标(M 系列)** —— 它们是"这个求解器算不算能用"的验收条件,由 phase-two 定义:

| 代号 | 全称 / 工况 | 目标 | 现状 |
|---|---|---|---|
| **M1** | 二维无粘标定(NACA0012,M0.80/α1.25) | (a) 激波位置 0.61 ± 0.02 (b) 两级 cl 差 < 3 % (c) `upwind_c ∈ [1,3]` 内 cl 变化 < 3 % | ✗ **三条里过一条**:(a) 0.6073/0.6006 **✓**;(b) **−16.2 %** ✗;(c) 9.4 %/17.9 % ✗ |
| **M1a** | 包线内的网格收敛性(NACA0012,M0.72/α1.25) | 三级 cl 单调 + 收缩(比率 < 0.7)+ 末步 < 5 % | ✓ **达标**(0.5056 / 4.2 %) |
| **M2** | 二维**粘性**标定(RAE2822 case 9/10) | 激波 ±0.02 c、cl ±3 %、cd ±10 counts | ✗ **未动**;第一阶段实测每个激波偏后 0.06–0.10 c、1/4 腿收敛、无可信 cd。**前置 = M1 + 阶段 S4** |
| **M3a** | 三维**有粘**对实验(ONERA M6,M0.8395/α3.06,七站 Cp) | 七站 Cp RMS **< 0.05** | ✗ 实测 **0.1007**;地板在 **MID(79 %)/TE**;**归属已改为阶段 S4/粘性**;标注"**无粘路线上不可达**" |
| **M3b** | 三维**无粘速度**(同算例) | **< 60 s** | ◐ **medium 达标**(49.9 s)/ **大网格未达标**(1.16 M 单元 3 950 s) |
| **M4** | **翼身组合体**粘性单点(**含生成网格**) | **< 2 CPU·min** | ✗ **未动**;审计实测 **3.5 h 且不收敛**。**前置 = M2/M3a + 阶段 S4** |
| **M5** | 包线健壮性(单配置 20 点) | 全部收敛、无钳制 | ✗ 本身未度量;旁证的能力矩阵 72/78 收敛、0 钳制 |

**阶段(S 系列)** —— phase-two 定的**执行顺序,不可换**:
**S1 激波算子 → S2 壁面精度 → S3 速度 → S4 粘性 → S5 产品化**(另有 **S6** = 壁面精度的后备路线,已被 **裁决 D2 搁置**)。
- **GS4.1** = 阶段 S4 的**第一道门**:把第一阶段那套**三维** IBL 换成**弦向条带的二维 IBL**(沿流向行进),
  验收(逐字):**整机翼边界层 < 1 s**;平板/Blasius/自相似解验证通过。
- **IBL** = integral boundary layer,积分边界层(不解全 N-S,只解沿表面行进的边界层积分方程)。

**已有的裁决(D 系列,§7 那张表)** —— 它们是**你**的决定,后续轮次不得静默绕过:
- **D2**(2026-07-28):**等参 P2 不采用**("生产级全速势求解器都不用它 ⇒ 非必要,复杂度过高");
  半结构贴体网格作为**后备(S6)**,只在非结构路线被**测量证明**达不到 M2/M3 之后才启动。
  ★ 「**等参 P2**」= 几何和场都用二次单元(isoparametric P2),是把壁面精度提上去的唯一已知路线。
- **D5**(2026-08-09):**放弃 level-set 尾迹路线**(phase 3 任务 ① 据此删掉 4 624 行库代码)。
- **D6**(2026-08-11):**停止六面体贴体路线的求解器侧投入**(phase 3 任务 ② 的两道门都 FAIL)。

**phase 3 的三个任务**(你在 2026-08-11 定的顺序):
**① 删掉 level-set → ② 六面体贴体网格 → ③ 其余问题**。①② 已完成(② 以 D6 收口),
**本会话 34 轮全部是任务 ③**。

**本阶段反复出现的技术名词**:
- **LE / MID / TE 带** = 弦向分段 x/c ∈ [0, 0.15) / [0.15, 0.85) / [0.85, 1.01],M3a 的误差按这三带记账;
- **η** = 展向站位 z/b(0 = 翼根、1 = 翼尖);**x/c** = 弦向相对位置;
- **e** = 计算 Cp 与实验 Cp 的 RMS 差(**对实验的误差**);
- **d_self** = **相邻两级网格之间**计算 Cp 的 RMS 差(**解与自己比**,即有没有在收敛);
- **R = d_self / e** ⇒ **R ≥ 1 表示"解还没收敛"**,R ≤ 1/3 表示"基本收敛";
- **σ(熵修正)** = 非等熵全速势里乘在密度上的总压损失因子,只在激波上游马赫 > 1 处起作用;
- **P1 / P2** = 阶段 S4「做减法」的两条**立项前提**:**P1 = 边界层墙钟 ÷ 无粘墙钟**(第一阶段 9×)、
  **P2 = 横流量级 max|B|/max|A|**(第一阶段 0.072,即"横流没用上");
- **h_wall / h_edge / h_te / h_far** = 生成器的四个尺寸旋钮(壁面 / 前后缘 / 后缘单独 / 远场);
- **flat / round tip cap** = 翼尖平截帽 / 圆角帽 —— **平截帽在加密下发散**,所以任何基于加密的论断
  建在平截帽上都**前提为假**(LE 带收敛阶 0.37 flat vs 0.87 round)。

### 1.1 现在是什么处境(任务 ③ 改变了什么、没改变什么)

★ 任务 ③ 的净效果是**把几个"以为知道"的事变成"测过的事"**,而**产品指标一个都没有前进**:

| 指标 | 任务 ③ 有没有动它 | 具体 |
|---|---|---|
| **M1** | ★ **解释了它为什么卡**,没修 | 测出 **M0.80/α1.25 上离散解不唯一** —— 只换 Picard 种子,x_shock 就散 **0.168–0.306 c**(= M1 容差的 **31–56×**)⇒ **(b)(c) 这两条在该工况上根本不可测量**,而包线内 M0.72 唯一(0.0002 c) |
| **M3a** | ★★ **动了它的"可达性"陈述** | 旧结论"换网格解决不了"是从**一个没在收敛的序列**外推的 ⇒ **证据不成立** ⇒ **可达性现在是"未知"**。★ 但**归属仍在阶段 S4/粘性**(那基于 h→0 的地板,与本阶段的"当下预算"是两件事) |
| **M3b** | 顺带 | 固定单元预算下"把单元搬进 LE 带"是**真杠杆(~17 %)但会饱和**;不改 M3b 的达标状态 |
| **M4** | ★ **发现它卡在机器上** | HEAD 上**没有翼身粘性的 case 入口**(只有翼型 / 闭合体 / 机翼三个)⇒ 它现在是**实现任务**,不是测量任务 |
| **M2 / M5** | 没动 | 前置未满足 |
| **阶段 S4** | ★★ **把它的两条立项前提在 HEAD 上重测了** | **P1 = 6.07×**(生产级,仍是成本大头)、**P2 = 0.0186**(横流确实很小);并写出 **GS4.1 立项文书** |

**而 LE 那条线(任务 ③ 的主线)现在的状态是:候选被逐条测负,只剩两条都不好走的路。**
已否证:Kutta 形式、**LE 分辨率(★ 但那是平截帽腿,已降级)**、几何、弦向错位、
**尖端(两次:剂量响应 + 样本外)**、局部弦长、实验点陡度、**σ(比值 1.000)**、
**激波足迹(声速线在 0.371–0.649,不在 LE 带内)**、Γ / Γ/c。
**已定位到的**:非收敛集中在**前缘、x ≈ 0.004–0.008 c**、且**整条 LE 带都在超声速泡内**(泡从 x ≈ 0.005 起);
分区检验 **P-MIX**:近奇异内区以 **41.7 %** 的点占 **74.9 %** 的 d² 质量、去掉它 d_self **减半**,
**但外区仍不收敛(R = 0.91)** ⇒ ★ **这个解释只覆盖约一半。**

### 1.2 为什么现在需要一条裁决(而不是再跑一轮)

三条,都是**规则层面**的,不是我能自己决定的:

1. ★★ **再往 LE 线走会撞已有裁决**:剩下的技术出路是**提高阶数**(= 等参 P2),而 **D2 已经否决它**;
   另一条是新开元技术路线,而 §3 禁止项明写"**不得再新增第二条并行技术路线**"
   ⇒ **要走就必须由你显式推翻其中一条**;
2. ★ **任务 ③ 的范围本来就是开放的**(「其余问题」)⇒ 它**没有自带终止条件**,
   而本会话已经 **34 轮**;一个开放任务什么时候算完,是排期决定;
3. **阶段边界的核查已经做完**(全门控 0 failed / 锁层 5/5 / 套件 = 基线)
   ⇒ **技术上随时可以收**,剩下的只有方向。

### 1.3 四个选项,逐条讲清

#### (a) 任务 ③ 收口,转产品线

**做什么**:把 LE 线的结果记成**测量结论**(而不是"待续"),任务 ③ 关闭;
接下来做**产品指标**:开 **GS4.1**,并补 **M4 的翼身粘性 case 入口**(纯实现)。
**为什么可行**:候选已逐条测负;阶段边界核查已过;而 **M4 从未被本阶段碰过**,它的目标(**含网格 2 CPU·min**)
才是你最初定的产品标尺。
★ **代价**:**接受 M3a 的可达性停在"未知"** —— 既不宣称可达也不宣称不可达。
★ **风险**:若将来发现 LE 那"另一半"其实很关键,需要重开一轮(重开条件已写进 D7 第 4 块)。

#### (b) 只开 GS4.1(阶段 S4 的第一道门)

**做什么**:采纳立项文书,实现**弦向条带二维 IBL**,按门的原文验收(整机翼 BL **< 1 s** + 平板/Blasius 验证)。
**为什么现在有据**:**P1 = 6.07×**(生产级边界层仍是成本大头,而这条动机**一度被我判为可能不成立、随后被数据推翻**);
**P2 = 0.0186**(横流很小)。
★★ **必须一起接受的三条**(文书 §2/§4 已写):
① "**< 1 s**"对着生产级实测 **918 s** 是个 **~900×** 的目标 —— **数字照门原文保留**,第一版落在 10–100 s 只能按 RECORDED 报;
② **P1 随加密下降**(标度指数 ⇒ P1 ~ 体积^−0.26)⇒ **再细一级就掉出带** ⇒ 收益**在生产级最大**;
③ **丢掉的横流是真实的后缘结构**(不是噪声),生产级 ≤ **3.2 %** ⇒ 这是**取舍**,要在文书里写明。
★ **与 (a) 的关系**:(b) 是 (a) 的一部分;单选 (b) 意味着**任务 ③ 不正式关闭**,LE 线只是暂挂。

#### (c) 继续 LE 线

**做什么**:去治那个**已定位**的机理 —— 前缘 x ≈ 0.005 c 处**声速界面的亚单元表示**。
★★ **它必须先解决一个规则问题**:技术上只有两条路,
**提高阶数(等参 P2)被 D2 否决**、**新开元技术路线被 §3 禁止项挡住**
⇒ **选 (c) 等于要求你显式推翻 D2(或那条禁止项),并把推翻的理由写进 §7**。
★ **可以支持推翻的新证据**(本阶段测到的、D2 当时没有的):
"换网格解决不了 M3"的**证据基础已失效**、非收敛已被**定位到前缘 x<0.01 c**、
而 **σ / 尖端 / 激波足迹 / 分辨率**都已被逐条排除。
★ **反面**:分区检验说这条机理**只解释约一半**(外区 R = 0.91 仍不收敛),
⇒ 即使治好近奇异区,**也不能保证 M3a 达标**;而 D2 当年的理由("生产级全速势求解器都不用等参 P2")**并没有被本阶段否证**。

#### (d) 先清挂账

**做什么**:把四笔明确的欠账做掉,再定方向:
1. **Q-B 直解臂** —— M6 分诊那轮登记的:**不用马赫爬坡、直接在 M0.8395 求解**,看**系统本身**有没有多解
   (爬坡本身可能就是钉住解的东西;当时因成本推迟);
2. **coarse 上两条流水线差 2.0 %** —— 同一网格、两条脚本路径给出的 cl 差 2.0 %,**至今未解释**;
3. **M4 的翼身粘性 case 入口** —— 纯实现,做完 M4 才**可测**;
4. **P2 的工况维度** —— 第一阶段的 0.072 在 **M0.5**,本阶段全部在 **M0.8395** ⇒ 跨工况**未比过**
   (需要 M0.5 的 φ,没有缓存)。
★ **为什么它可能值得先做**:①②④ 都是**便宜的**,而 ①④ 会影响别的结论的解读(①动 M1 的可测性、④动阶段 S4 的前提)。
★ **代价**:它**不推进任何产品指标**,只是把地基补齐。

### 1.4 不选每一条,会失去什么(让选择两面都清楚)

| 不选 | 失去 |
|---|---|
| (a) | 任务 ③ 继续开着,而它**没有自带终止条件** ⇒ 下一轮仍会从"再切一次带"开始 |
| (b) | **唯一一条现在有 HEAD 依据的产品动作**被推迟,而 P1 的优势**随加密衰减**(越晚做,依据越弱) |
| (c) | LE 那条线**停在"只解释一半"** ⇒ M3a 的可达性会长期停在"未知" |
| (d) | 四笔欠账继续影响别处的解读(尤其 ① 会让"M6 有没有多解"一直是开放问题) |

★ 也可以组合,例如 **(a) + (b)**(收口并开 GS4.1)、或 **(d) 里挑 ①④ + (b)**(先补两笔便宜的地基再开门)。

---

## 2. 可直接粘贴的表格行(裁决字段留了占位符)

> ★ 用法:把下面**整行**粘到 §7 表的 D6 之后,把 `【裁决:(a)/(b)/(c)/(d) —— 待使用者填】`
> 换成你的决定;其余内容都是**已提交证据**,不需要改。

```
| **D7** | 2026-08-16 | phase 3 任务 ③「其余问题」的处置(34 轮已执行;阶段边界核查已完成) | 【裁决:(a) 收口转产品线 / (b) 只开 GS4.1 / (c) 继续 LE 线(需显式推翻 D2)/ (d) 先清挂账 —— 待使用者填】 **阶段边界核查**:全门控集 **488 passed + 1 skipped + 3 xfailed + 1 XPASSED,0 failed**(1:11:48 @16 线程;四数对平:未门控 479+12+2 ⇒ 解锁 11 个 skip ⇒ 9→passed、1→xfailed、1→XPASSED);★ 那个 xpass 由 mark 自身预言(`test_p4_transonic::test_g41_transonic_medium_gate` 故意非严格,理由原文记着"16 线程收敛 / 8 线程不收敛")⇒ **无回归**,并得出常设一句:**门控计数必须带线程数引用**;快速能力锁层 **5/5 绿**(526 s);未门控套件 **479+12+2,0 failed**(447.77 s @8t)= 记录基线。**实测依据(按发现顺序)**:① **加密使鲁棒性变差的上游是离散解不唯一** —— M0.80/α1.25 上只换种子即使 x_shock 散 **0.168–0.306 c**(= M1 容差的 **31–56×**),而包线内 M0.72 唯一(0.0002 c)⇒ **解选择被钉住之前,M1 的 (b)(c) 在该工况上不可测量**;② **σ 不是选择算子**(σ OFF 两族都唯一,x_shock 散布 0.0000;多解随 σ ON 一起出现),σ 在第 8 步被**永久冻住**且冻在哪个相位由种子决定 ⇒ cl 0.26 vs 0.35;极限环已复现(周期 2–5)并**已交付纯读判的报警**;三条治法(软集合 / 幅度守恒 / 捕获选择)全部实测,**捕获选择被 K3 判死**;σ 线以"**唯一性 vs 修正强度是取舍**"写进能力边界;③ **M6 LE 线**:旧数 **69.6 %/0.2361 已退役**(出处 CSV 早于三次配方变更),HEAD 替代读数 **70.35 %/0.2422**;弦向错位(H2)、尖端(W2 无代理剂量响应 + η0.96/0.99 样本外)、局部弦长、Kutta 形式、σ(比值 1.000)**逐条否证**;LE 误差在上表面是**纯散布**(f_bias 五站全 ≤ 1/n)、下表面有**真实小偏置**;固定预算下**把单元搬进 LE 带是真杠杆但会饱和**(落在族曲线下 ~17 %,`h_edge` ≈ 生产值 1/2–2/3 见底);④ ★★ **自收敛读数:LE 上表面的 `d_self` 随加密增大**(0.2417→0.2928,**R = 1.2245**)而 LE 下/MID 的级差在缩小 ⇒ **"换网格解决不了 M3"的证据基础失效**(它是从一个**没在收敛**的序列做四点外推);分区检验 **P-MIX**:近奇异内区以 **41.7 %** 的点占 **74.9 %** 的 d² 质量、去掉它 d_self 减半,**但外区仍不收敛**(R = 0.91)⇒ 该解释**只覆盖约一半**;⑤ **产品线**:S4「做减法」的两条前提在 HEAD 上重测 —— **P1 = 6.07×**(生产级,T-HOLD,经可重复性守卫 +1.3 % 后裁决;标度指数 IBL^1.41 / 无粘^1.20 ⇒ P1~体积^−0.26 ⇒ **下一级会穿带**)、**P2 = 0.0186**(逐点 max 0.0315);★ **横流是后缘(x/c 0.97–1.00)的真实结构**(空间格重叠 J = 10× 零假设)⇒ **不是粗网格噪声**。**不许被静默改掉的六条**:(i) **M3 的可达性现在是"未知"**,不是"不可达"也不是"可达" —— 旧的"不可达"外推**证据不成立**;(ii) **LE 占 70 % 的旧数已退役**,新旧**不可比**;(iii) **两处 flat-cap 污染**(四级全尺度阶梯、"LE 分辨率已否证")—— 平截帽在加密下发散,LE 带阶 0.37 flat / 0.87 round;(iv) **横流是真实结构**,GS4.1 的二维条带丢掉的是真东西(生产级 ≤3.2 %,**有测量支撑的取舍**);(v) ★ **D2 仍然有效**(等参 P2 已否决;本会话曾把它误写成"已评估-未开+立项条件",已勘误);(vi) **门控计数带线程数**。**过程产出(代价换来的做法)**:本阶段自查出 **7 个判据缺陷**(最新一个是"**定带用的参考数跨工况**")、**3 次自指守卫**(`pgrep -f` / G-Z 匹配自己的禁止词 / G-L 量自己的负载)、**2 次报告层缺陷各烧 ~17 min** ⇒ 新增两条常设做法:**守卫必须自问"我的存在会不会改变我要量的东西"**、**先干跑校验查找/常量/级别清单再花机时**。**重开条件**:(c) 需**显式推翻 D2**;LE 线若重开,须先给出**外区那另一半**的机理候选(本阶段已把 σ/尖端/激波足迹/局部弦长/Γ 全部测负);GS4.1 若开工,须在文书里带上"P1 会在下一级穿带"与"丢掉的横流是真实的"两条。 |
```

---

## 3. 起草时**刻意没做**的三件事

1. ★ **没填裁决** —— 那是 §7 表头明写的"使用者决定";
2. ★ **没把任何数字四舍五入或美化** —— 每个数都能在本阶段的已提交 CSV / 轮次文件里找到;
3. ★ **没把"约一半"写成"主要原因"** —— 分区检验是 **P-MIX**,内区解释约一半,**外区仍不收敛**,
   这条限定必须留在裁决里,否则下一轮会把它当成已解释。

## 4. 我建议的选项(**建议,不是裁决**)

**(a) + (b)**:任务 ③ 以测量结论收口,并开 **GS4.1**。理由:
- LE 线的**候选已被逐条测负**,而剩下的路要么**撞 D2**、要么**再切一次带**(而切带已证明只解释一半);
- **产品指标从未被本阶段碰过**,而 GS4.1 现在**有 HEAD 依据**(P1 6.07×、P2 0.0186)且**风险已写明**;
- ★ 反面也说清:选 (a)+(b) 就意味着**接受 M3 的可达性停在"未知"**,而不是继续追它。
