# 20260811-2100 — 判定:任务 ③ 第一项。**问题被答成了另一个问题**:M0.80/α1.25 上离散解**不唯一**

> ## ★★★ 勘误(2026-08-12,由下一轮发现并修好)—— 本文的两个非结构族数字是**错的**
>
> `run_task3_seed_nonuniqueness.py` 当时算散布**没有过滤 `converged`**,于是把**钳制垃圾态**
> 也算进了散布。修好后:
>
> | block | 本文发表的 | **正确值** |
> |---|---|---|
> | 非结构 coarse x_shock | 0.2745 c | **0.0122 c**(小 **22 倍**) |
> | 非结构 coarse cl | 23.1 % | **0.21 %** |
> | 非结构 medium | 0.3060 c / 705 % | **无定义(0/3 收敛)** |
> | 混合 R0 | 0.1683 c / 32.11 % | **不变**(3/3 都收敛) |
>
> **仍然成立**:核心结论(四条腿的加密对照不可读)—— 它靠的是**混合族**,而那四条腿就在混合族上,
> 三个种子全收敛、x_shock 散 **0.1683 c**、cl 散 **32.11 %**;范围结论(M0.72 唯一 0.0002 c、
> α=0 近乎唯一 0.0088 c)也都是 3/3 收敛;C1 的分类、仪器验证、以及 §5b 的自我更正,全部不受影响。
>
> **被推翻**:本文 §4 的"**与网格族无关,非结构族更糟**"。在**收敛的**状态之间,
> 非结构族反而**更紧**(0.0122 c / 0.21 %);它糟的是**收敛性**(medium 0/3)。
> 于是 §8 里"M1 的 (b)(c) 因多解而不可测量"在 **M1 自己的族上不成立** ——
> 那里的跨种子 cl 散布 0.21 %,**在 3 % 判据之内**;M1 的 medium 卡在**根本没有解**(8 线程 0/3),
> 这是**收敛障碍,不是多解障碍**。大幅多解目前只在**混合族**上被证实。
>
> 详见 [20260812-0100 判定](20260812-0100-sigma-selection-verdict.md) §9。
> ★ **教训**:"散布"这个词默认了被比较的东西是**解**。
> 一个不带 `converged` 过滤的 max−min,量的是**状态**的散布,那不是同一件事。

> 一句话:**加密并没有破坏收敛 —— 是那个工况上根本没有唯一的解可收敛到。**
> 网格固定、只换 Picard 种子,x_shock 就撑开 **0.1683 c**、cl 撑开 **32.11 %**(混合族,三种子全收敛),
> 三个解各自都是 `|R| ≤ 3e-11、0/0 clamps` 的**真根**。
> ★ 需要 **强激波 + 升力耦合** 同时在场:包线内 M0.72 **完全唯一**(0.0002 c),α=0 **近乎唯一**(0.0088 c)。
> ★★ **本文件在提交前自我更正过一条头条**:见 §5 —— "这个发现在 M1 已提交的 CSV 里早就有"**是过度主张**,
> M1 门只采两个种子,而那两个种子在 coarse 上是吻合的。

预注册 [20260811-1500](20260811-1500-task3-refinement-paradox-prereg.md)(动代码前提交,判据一字未改)。

| 脚本 | 产物 |
|---|---|
| `bench/run_task3_local_window_guard.py` | `gate_results/task3_local_window_guard.csv`(§5.3 守卫 **PASS**) |
| `bench/run_task3_refinement_paradox.py` | `task3_refinement_paradox.csv` + `task3_alpha0_control.csv` |
| `bench/run_task3_seed_nonuniqueness.py` | `task3_seed_nonuniqueness.csv` ★ **决定性对照** |
| `tests/test_meshgen_structured.py`(新) | 把 `structured.py` 的逐位单变量性从 bench 搬进常开测试层 |

---

## 1. 先纠正我自己两处错,因为它们改变了"下一步做什么"

★★ **(1) 预注册 §1 的事实错误。** 我写"那个 `conv=False` **至今没有被分类**"。
**这是错的** —— phase two `progress.md` 第 6 行(GS1.2b,2026-07-28)已经分类并定位了根因:

> α=0 **激波位置收敛**(0.464/0.502/0.513,比值 3.3),α=1.25 **升力发散**(0.373/0.523/0.715)
> ⇒ 根因锁定在**升力耦合**,GS1.3b 升为 S1 关键项

后果不是"白做一轮",而是**我注册的仪器错了**:A1/A2/A3 三个归因分支**都预设"有腿不收敛"**,
而记录里 α=1.25 的病是**升力发散**。这是项目已记的"不要把上一轮的归因当结果带进新测量"的**反面**:
我**没有把上一轮已经做出的测量带进来**。

★ **(2) 本会话中我说过的一句撤回。** R0 的 `x_shock = 0.6051` 我记为"落在 M1 参考带 0.61 ± 0.02 内"。
**撤回**:同一张网格 seed 0 给 **0.7141**、seed 12 给 **0.5458**。那是**抽样运气**,不是读数。

## 2. 四条腿(全部 `converged`、`0/0 clamps`、两次运行逐位一致)

| 腿 | 变量 | tets | M_max | x_shock | cl |
|---|---|---|---|---|---|
| R0 | — | 36426 | 1.3329 | 0.6051 | 0.2582 |
| RG | 站数 ×2 + 壁面法向 ÷2 | 65670 | **2.0410** | **0.7739** | **0.3471** |
| RS | 只加密激波带 [0.45,0.75] ×3 | 36546 | 1.3577 | **0.4783** | 0.2995 |
| RN | 只加密壁面法向 | 52674 | 1.3364 | 0.6244 | 0.2866 |

| 判据 | 判定 |
|---|---|
| **C1** 不收敛的腿都给出模式 | ★★ 在这四条腿上 **VACUOUS**(不收敛的腿 **0 条**)。与我在 Q1 里抓到并加了守卫的**空洞满足**同族 —— 只不过这次空洞的是**我自己的判据**。**已在 §3b 用真有失效的腿补答:5/5 分类,其中 `limit_cycle` 那条就是注册要找的。** |
| **C2** 同一腿两次运行同一模式 | 收敛性一半是真读数(两次逐位一致);模式一半 VACUOUS。★ 且见 §3:**C2 在原理上看不到要紧的东西**。 |
| **C3** `budget_limited` 严格口径 | **满足**(§3b:尾部非单调 ⇒ 不判 `budget_limited`,且未用 descent10 比率定案) |
| **A1 / A2 / A3** | ★★ **NOT APPLICABLE** —— 三个分支都预设有腿失效 |

## 3. ★★★ 决定性对照:**网格固定,只换种子**

C2 要求"两次独立运行同一模式"。但**同一种子+同一线程数的两次运行是构造性逐位相同的** ——
C2 认证的是**可复现性**,对**解的唯一性一个字都没说**。phase two 早已登记 **soft shift**
(收敛、0 限幅,却落到另一个解上,**不报任何错**);RG 的 M_max 2.04 / cl 0.347 正是那个形状。

`bench/run_task3_seed_nonuniqueness.py`,**网格逐位相同,只变 `n_picard_seed ∈ {0, 5, 12}`**:

| block | 收敛 | **x_shock 散布** | cl 散布 | vs M1 容差 ±0.0055 c |
|---|---|---|---|---|
| 混合 R0 M0.80/α1.25 | 3/3 | **0.1683 c** | **32.1 %** | **31×** |
| 混合 RG(全局加密) | 2/3 | ~~0.2577 c~~ → **0.2503 c** | ~~144.9 %~~ → **33.04 %** | 45× |
| **非结构 coarse**(M1 的族) | 2/3 | ~~0.2745 c~~ → **0.0122 c** | ~~23.1 %~~ → **0.21 %** | ~~50×~~ → **2.2×** |
| **非结构 medium**(M1 的网格) | **0/3** | ~~0.3060 c~~ → **无定义** | ~~705 %~~ → **无定义** | — |
| 混合 M0.72/α1.25(包线内) | 3/3 | **0.0002 c** | **0.0 %**(cl 一致到 **6 位**) | **0.04×** ✓ |
| 混合 M0.80/α=0 | 3/3 | **0.0088 c** | 0.8 % | 1.6× |

⇒ **种子一个人就复现了我准备归因给"加密"的全部散布**(0.1683 vs 四条腿的 0.2956)。
**§2 那张表不可读**:每条腿按注册固定 `seed = 5`(固定它本身是对的),
但**固定种子不固定解**,它只固定了从多解集里的**一次任意抽样**;
拿"R0@seed5"和"RG@seed5"相比是拿两次任意抽样相比,**差值不能归因给网格**。

**三个解都是真解,不是停机假象**:`|R|` 1.28e-13 / 9.46e-14 / 3.26e-11,`0/0 clamps` ——
到机器精度各自都是离散系统的**根**。这是**离散问题的多解**,不是收敛不彻底。

### 3b. ★★ C1 补答 —— **注册要问的那个分类,现在有答案了**

四条腿全收敛,所以 C1 在 §2 只能被空洞满足。种子对照里**有 5 条不收敛的腿**,全部分类
(`task3_seed_nonuniqueness.csv` 的 `failure_mode` 列):

| 腿 | 模式 | 钳制 | \|R\| |
|---|---|---|---|
| 混合 RG / seed 12 | `clamping` | 8221/547 | 3.45e-01 |
| 非结构 coarse / seed 12 | `clamping` | 2600/580 | 2.25e-02 |
| 非结构 medium / seed 0 | `clamping` | 4388/563 | 1.84e-02 |
| ★★ **非结构 medium / seed 5** | **`limit_cycle`** | **0/0** | **9.91e-07** |
| 非结构 medium / seed 12 | `clamping` | 9096/543 | 2.08e-01 |

★★ **最后一条(不是钳制的那条)就是注册要分类的东西**:GS1.2 的 fine 失效明写
`n_floored = 0`,而这条腿正是那个形状 —— 且它在 **M1 自己的族、自己的网格、自己的配方**上。
⇒ 分类结果 **`limit_cycle`**,按分类表其修法在**冻结选择 churn**,
**不在**预条件子 / 全局化 / 熵修正。

**C3 按注册的严格口径独立验过,没有信那个比率**(项目记录过 period-3 极限环给出
descent10 = 2.0021 落在手挑阈值另一侧、并据此发表过结论):
残差尾部在 ~1e-6 附近振荡并有到 1.14e-05 的跳出,**30 步里 5 个值在 ≥3 步之后被重访到 1e-3 内**;
`descent10 = 0.9817`(残差其实略**升**);**尾部非单调 ⇒ 不是 `budget_limited`**。⇒ `limit_cycle` 成立。

★ **一条如实的限定**:这 5 条腿的 `accept_reason` 全是 `None`,而设定明写
**`accept_reason` 优于任何推断的签名** —— 这里没有 solver 自己的说法可用,
所以这些分类是**推断出来的签名**(其中最承重的那条已按上面的独立口径验过)。

★ 但**不得声称我复现了 GS1.2 的那个失效**:钳制的四条与它明写的 `n_floored = 0` 不符;
真正同形的只有 medium/seed 5 那一条。

## 4. 范围:多解需要**强激波 和 升力耦合 同时在场**,且**与网格族无关**

| | 有激波? | 有升力? | x_shock 种子散布 |
|---|---|---|---|
| M0.72 / α1.25 | 是(M_max 1.11) | 是 | **0.0002 c** |
| M0.80 / α=0 | 是(M_max 1.20) | 无 | **0.0088 c** |
| M0.80 / α1.25 | 是 | 是 | **0.1683 c**(混合,3/3);非结构 coarse **0.0122 c**(2/3);非结构 medium **无定义**(0/3) |

**只有激波近乎唯一;只有升力完全唯一;两者同时才炸开。** 这既**证实**又**补全**了 GS1.2b 的
"根因在升力耦合" —— 缺的那一半是**激波强度**,GS1.2b 当年只有 α 一个轴,分不出来。
★ **族对照是必须做的,不是加分项**:不做它,这条读数就只覆盖本轮新建的网格族,
而它要影响的结论(能力边界 §3.6)是在**非结构族**上测的 —— 那就是 phase two
"守卫查表面、结论关于体网格"的同一个错。
★★ **但族对照的结论在 2026-08-12 被更正**(见顶部勘误):加上 `converged` 过滤后,
**非结构族在收敛状态之间更紧**(0.0122 c / 0.21 %),而不是更糟;
它糟在**收敛性**(medium 0/3)。⇒ **大幅多解目前只在混合族上被证实,不是族无关的。**

★ **顺带清掉一个我怀疑过的东西**:α=0 上 NACA0012 按对称性 cl 应为 0,实测 **−0.0036**,
**三个种子一致到 5 位** ⇒ 它是**网格不对称**(混合族外域是非结构的),**不是**解支效应。
量级是 α=1.25 升力的 1.4 %,如实登记。

## 5. ★★ 仪器验证,以及**我在提交前自我更正的一条头条主张**

我这一轮的读数跑在 **8 线程**,而能力边界 §3.6 的读数在 **16 线程**。逐位对照已提交的 CSV:

| medium / C=1.5 | seed 0 | seed 5 |
|---|---|---|
| `m1_gate_default_8t.csv`(8 线程,已提交) | False, x_shock 0.8798, **4388/563** | False, **0.5737947433**, 0/0 |
| **我这一轮**(8 线程) | False, 0.8798, **4388/563** ✓ | False, **0.5738**, 0/0 ✓ |
| `m1_gate_default.csv`(16 线程,已提交) | **True, 0.6005923629** | **True, 0.6005923629** |

⇒ **我的仪器逐位复现了已提交的 8 线程 M1 数**(所以 §3.6 与我不矛盾:差的是线程)。
并且**缺陷 #3(线程依赖)现在有了量级**:同代码/网格/种子/C,
**0.6006(16t) vs 0.5738(8t,且不是解)= 0.027 c**。

### 5b. ★★★ 自我更正:**"这个发现在已提交的 CSV 里早就有"是过度主张**

本文件的初稿写着"这个发现从 2026-08-10 起就躺在提交的 CSV 里"。**核对后不成立,已改掉。**
**M1 门只采两个种子(0 和 5)**,而这两个种子在已提交数据上、在两条腿都收敛处是**吻合**的:

| 已提交 M1 数据(两腿都收敛处) | cl_p seed 0 | cl_p seed 5 | 跨种子差 | vs (b)(c) 的 3 % |
|---|---|---|---|---|
| coarse C=1.0 | 0.424730 | 0.413008 | **2.76 %** | 用掉 **92 %** 的预算,但**未超** |
| coarse C=1.5 | 0.407978 | 0.407108 | 0.21 % | 远内 |
| coarse C=3.0 | 0.377355 | 0.377442 | 0.02 % | 远内 |
| medium C=1.5(16t) | 0.341340 | 0.341340 | **0.00 %** | ★ 逐位相同 —— 因为**回退把 seed 0 送到了 seed 5** |

★★ 我那个 **32 %** 的散布来自 **seed 12,而 M1 门从不采它**。
⇒ **诚实的版本**:已提交数据里能看到的是 coarse C=1.0 上 **2.76 %** 的跨种子 cl_p 差,
**几乎吃掉 (b)(c) 判据的全部 3 % 预算** —— 那是一个**警讯**,不是"发现本身早已在册"。
把它写成后者,等于用一个门没采过的种子去指控那个门。

★ **顺带一个真收获(独立验证,不是我的假设)**:同一条腿的跨种子 x_shock 差是 **0.0136 c**,
按 GS1.1 的 `dcl/dx_shock ≈ 2` 应给 **2.7 %** 的 cl 差 —— 实测 **2.76 %**。
⇒ **GS1.1 那条敏感度链在这里被独立验证了**(它是"±0.0055 c"这个派生要求的来源)。

★ **过程结论仍然成立,但要按这个尺度陈述**:`run_m1_gate.py:193-195` 对 **每个**种子分别评判据
(`overall &= _criteria(per-seed subset)`,所以 seed 5 并没有被忽略),
**但从不问两个种子是否落在同一个答案上** —— 于是那 2.76 % 从来不会被当成一个数打印出来,
而 (b)(c) 的整个预算就是 3 %。⇒ **缺的是一条会跨种子看的判据**,
这与项目已记的"**非 strict 的 xfail 检测不到回归**"同族。

## 6. 按注册的 kill criterion 判(不弯条文)

| 条 | 是否成立 |
|---|---|
| 1. A3 且两次运行不稳定 | **不成立**(四条腿全收敛、两次逐位一致) |
| 2. 分类出的模式属已排除路线 | **不成立**(那四条腿没有模式;§3b 补答的 `clamping` 与 `limit_cycle` 都不属已排除路线 —— `limit_cycle` 指向**冻结选择 churn**,是一条**新的、可做的**方向) |
| 3. 局部加密旋钮过不了守卫 | **不成立** —— 守卫 **PASS**(§7) |

⇒ **本项没有按自己的判据判死。** 它是**被答成了另一个问题**:
注册的问题("加密为什么让鲁棒性变差")预设了一个**下游**障碍,
实测在它**上游**找到一个更大的:**那个工况上没有唯一解**。
注册里"**明确不算 kill:M1 仍不达标**"这一条仍适用。

## 7. 新交付的生成器能力(§5.3 守卫 **PASS**)+ 一条补上的测试债

`airfoil_surface_distribution(..., local_window, local_factor)`:弦向窗口内加密、**站数不变**。
守卫:站数 160→160;窗口内间距 0.020370 → 0.010426(**0.512×**),点数 30→60。

★ **它的守卫不是逐位的,代价已量化**:固定站数意味着点是**从窗口外挪进来**的,窗口外变疏 **12.4 %**;
对比窗口内 48.8 % 的加密 = **3.9× 比值** —— 这是**有界的污染**(口径:"**bound 不等于 clean**")。
刻意选"固定站数"的理由:加站会同时改全局 DOF,那样 RS 与 RG **又分不开** —— 正是 phase two 逃不出的混淆。
两个默认路径**逐位不变**已锁(`local_window=None` 与 `local_factor=1.0` 都逐位等于旧调用)。

★★ **同时补上一笔测试债**:`pyfp3d/meshgen/structured.py`(任务 ② 新建的整个模块)在 `tests/` 里
**此前零覆盖** —— G0 的逐位单变量性只被 `bench/run_hex_g0_single_var.py` 断言,而 bench 不在任何节奏上跑。
这正是 row 2b 刚刚为 conforming 翼身跨声速能力补锁时说的那句话:**能力边界在陈述它,却没有东西会因它坏掉而变红**。
新增 `tests/test_meshgen_structured.py`(常开层,纯网格算术):壁面锚定分级的
**追加式逐位前缀**、`h_wall_normal` 的精确首层、`radii` 与 `distances` **故意不合并**的理由锁、
站数精确、LE 比 TE 细(TE 去聚点)、局部窗口的**两个默认逐位不变 + 窗口外代价上界**、
以及层块质量漂移锁(AR max < 8 / median < 3,实测 5.899 / 2.012;`min_area` > 1e-6,实测 5.08e-06;
正交性中位 < 1°,实测 0.251;**TE 行进方向锁成精确 ±y**,实测 [0, +1] 与 [0, −1] —— 实测是精确的,
就不该松成"y 主导")。

## 8. ★★ 这对 M1 意味着什么(用 M1 判据的原文,不用派生数)

**M1 的三条判据原文**:(a) 激波位置 **0.61 ± 0.02**、(b) 两级 cl 差 **< 3 %**、
(c) `upwind_c ∈ [1,3]` 内 cl 变化 **< 3 %**。
★ **±0.0055 c 不是判据本身**,它是 GS1.1 由 (b)(c) **派生**的激波位置控制精度
(`dcl/dx_shock ≈ 2`,cl ≈ 0.36 ⇒ ±0.011 ⇒ ±0.0055 c)—— §5b 刚好独立验证了这条链。

**直接对上判据的量(★ 2026-08-12 按 `converged` 过滤更正后)**:

| 族 | 收敛 | 跨种子 cl 散布 | vs (b)(c) 的 3 % | 跨种子 x_shock | vs (a) 的 ±0.02 |
|---|---|---|---|---|---|
| **混合 R0** | **3/3** | **32.11 %** | **10.7×** | **0.1683 c** | **8.4×** |
| **非结构 coarse**(M1 的族) | 2/3 | **0.21 %** | **0.07×(判据之内)** | **0.0122 c** | 0.6× |
| **非结构 medium**(M1 的网格) | **0/3** | **无定义** | — | **无定义** | — |

⇒ **必须按族分开说,这是勘误改掉的那一条**:

1. **在混合族上**,种子这一个与分辨率无关的自由度就是 (b)(c) 判据的 **10.7 倍**
   ⇒ 在那里,**加密激波带无从谈起**;
2. **在 M1 自己的族上,多解不是障碍** —— coarse 的跨种子 cl 只差 **0.21 %**(三种子诊断口径 2.80 %),
   在 3 % 之内;
3. **M1 的 medium 卡在别处:8 线程下 0/3 种子收敛**(σ ON 与 σ OFF 都是 0/3,见下一轮判定)
   ⇒ (b)(c) 在那里**因为没有解可比**而无法评估,**不是因为解不唯一**。
   ★ 16 线程下已提交记录是**收敛**的(0.6005923629)⇒ 这个障碍**带线程限定**。

⇒ 修正后的净陈述:**"M1 的 (b)(c) 在 M0.80/α1.25 上不可测量"在 medium 上成立,
但理由是收敛而不是多解;多解这个障碍目前只在混合族上被证实。**

★ **附带指出、不作提案**(注册 §8 明写"不动 M1 的三条判据"):
M1 的工况正落在项目自己记录的**折叠区**(NACA medium M≈0.79,dcl/dM ≈ 6–10),
而纪律 #4 明写折叠区**只做单网格回归锁,不做网格收敛性断言**。
⇒ M1 的 (b)(c) 是**在一个按本项目标准禁止做跨网格断言的工况上**定的。这是**规格层面的输入**,
交使用者裁决,我不改判据。

★ **文献侧定位是推断,不是我的测量**:跨声速全位势方程在**升力 + 强激波**下存在多解,
是有文献记载的性质(Steinhoff & Jameson 1982 一类),与 §4 的"两者同时才炸开"吻合。
**我测到的是"多个收敛根"**;"它就是那个已知现象"是**推断**,按此标注。
—— 若成立,机理含义很具体:**等熵位势模型缺少挑出物理解的熵条件**,
而本项目的**熵修正(σ,默认 ON)本来就是为这件事存在的**。

## 9. 下一项候选(**另行预注册**,本项不顺手做)

按注册 §8"不趁手修分类出来的那个模式"。推荐排序:

1. ★★ **`limit_cycle` 指向的冻结选择 churn**(§3b 现在给了定向)**或** **熵修正是否在挑解支** —— σ 默认 ON,立项理由(S1b)是"把 M0.80 激波移进 Euler 锚带"。
   直接问:σ ON/OFF 下这个种子散布怎么变?收窄 ⇒ 机理确认、路线明确;不变 ⇒ σ 不是选择算子。
   **成本极低**(4–35 s/解),**判据可事前写死**。★ 且已有 `m1_gate_entropy.csv` 可作入口对照。
2. **给 M1 门加第三个种子 + 一条跨种子/跨线程的一致性诊断**(先只 RECORDED,不改 (a)(b)(c))
   —— §5b 表明门的两种子采样看不到这件事,且 coarse C=1.0 已经用掉 92 % 的 3 % 预算。
   **便宜,且能防止同类失明再发生**,与 1 不冲突。
3. **把"解选择"做成显式可复现的规则**(动求解器)—— 应在 1 有读数之后。
4. 能力边界的诚实登记:**M0.72 唯一 / M0.80+α 多解 / 线程贡献 4.9× 容差**
   —— 这一条**无论上面走哪条都要做**。

## 10. 本轮我的错(逐条,全部由断言、对照或读记录/读代码抓到)

1. ★★ **预注册误称"从未分类"**(§1),导致 A1/A2/A3 与要测的病对不上;
2. ★ **把 R0 的"在参考带内"说出口**(§1(2))—— 抽样运气,已撤回;
3. ★★★ **初稿把"发现早已在提交的 CSV 里"当成头条**,核对后发现 M1 门只采 {0, 5}
   而那两个种子在 coarse 上吻合到 0.02–2.76 % —— 我那 32 % 来自门从不采的 seed 12。
   **提交前自我更正**(§5b),并把 §8 改成用 M1 判据原文陈述。
   教训与本项开头那条同源:**先去核对记录/代码,再下"早就有"或"从未有"这类断言**;
4. ★★ **我的 C2 判据在原理上看不到解的非唯一性**(§3):"两次独立运行"在固定种子+固定线程下
   构造性逐位相同。与"**非 strict 的 xfail 检测不到回归**"同族 ——
   **一个被 pass 和 fail 同时满足的判据不是判据**;
5. **C1 被空洞满足**(§2),而空洞守卫是我自己在 Q1 里加的;
6. ★ **差点拿"M1 走马赫爬坡"这个记忆去推翻 §3.6** —— 去读了 `run_m1_gate.py:116` 才发现
   它是**目标马赫直解、参数与我逐字相同**,真正的差别是线程数。
   项目规则"**读签名,不要凭记忆**"这次是**在两个方向上**都救了场。
