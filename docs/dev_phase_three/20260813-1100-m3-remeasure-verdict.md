# 20260813-1100 — 判定:**HEAD 上的 M3 预算测出来了**,而 **LE 主导的结论在配置变更之后依然成立**

> **加上生产 `tip_taper` 之后,medium 爬坡就收敛了** —— `converged=True`、`|R| = 7.33e-15`、`n_floored = 0`。
> 这独立印证了入口检查的诊断:**圆角帽 + 无 taper** 正是被实测判死的那个组合,
> 而这个脚本在 2026-08-05 生产采纳 taper 之后**从未更新**(`grep -c tip_taper` = 0)。
> **新占比:LE 上表面 70.35 %、MID 上表面 21.05 %。**
> ★★ 而旧的 69.6 % / 20.7 % 已**退役** —— 两者**不可比**(几何与配方都变了)。
> **新数落在几乎同一位置,是一条 RECORDED 的稳健性观察,不是"验证了旧数"。**
> ⇒ **实用后果:LE 主导的结论没有建在沙上,LE 那条线可以继续。**

预注册 [20260813-0900](20260813-0900-m3-remeasure-prereg.md)(动代码前提交)。
产物 `bench/gate_results/m3_budget_head_medium.csv`(**新文件名**;2026-07-31 的历史 CSV 一字节未动)。

## 1. 判据

| | 要求 | 实测 |
|---|---|---|
| **R1 收敛(硬前提)** | `converged=True`、0 clamp | ★ **PASS**:`converged=True`、`res_final` **7.327557e-15**、`n_floored = 0`。★ 限定:该 CSV **没有 `n_limited` 列**,所以"0 lim"我**只能报 `n_floored`**,不声称更多 |
| **R2 配方一致** | taper 构造与 `_m6_case` 逐字相同,由 assert 钉住 | **PASS** —— 断言直接检查 `test_p8_newton::_m6_case` 的源码里那一行仍在;将来那边改了,这里会红而不是悄悄分叉 |
| **R3 内部一致** | 三带 SS 复现全点 RMS | ★ **PASS 且是精确的**:sqrt(ΣSS/Σn) = **0.132319** = 全点 pooled **0.132319** |
| **R4 新占比** | 报各带占比与绝对 RMS | 见 §2 |
| **R5 taper 偏差** | 任何升力都要带那 −1.3 % | cl_p **0.272994** / cl_KJ **0.277167**,**两者都带生产 taper 的 −1.3 % cl_p 模型偏差** |

## 2. R4 —— HEAD 上的误差预算(圆角帽 + 生产 taper,medium,M0.8395/α3.06,生产腿)

| 带 | RMS | 点数 | **占误差平方和** |
|---|---|---|---|
| **LE 上表面** | **0.242221** | 38 | ★ **70.35 %** |
| **MID 上表面** | 0.098318 | 69 | **21.05 %** |
| LE 下表面 | 0.094043 | 18 | 5.02 % |
| TE 上表面 | 0.071563 | 15 | 2.42 % |
| MID 下表面 | 0.026208 | 36 | 0.78 % |
| TE 下表面 | 0.048574 | 5 | 0.37 % |
| **全点 pooled** | **0.132319** | 181 | 100 % |

## 3. ★★ 旧数退役,而新数落在同一位置 —— 这两句要分开说

**退役**:2026-07-31 的 **69.6 % / 20.7 % / LE 上 RMS 0.2361** 出自**平角帽网格 + 无 taper**,
与本轮的**圆角帽 + 生产 taper** **不是同一个配置** ⇒ 按注册 §0,**旧数不得再当在用的数引用**,
也**不得**写成"70 % 变成了 70.35 %"(那正是本项目已犯过四次的跨时间比较)。

**RECORDED 的稳健性观察**:新的独立测量给出 **70.35 % / 21.05 %**,与退役的 69.6 % / 20.7 %
**几乎同一位置**;而绝对 RMS 动了(LE 上 0.2361 → **0.2422**,+2.6 %)、升力也动了。
⇒ **占比结构对这次配置变更是稳健的,而绝对值不是。** 这是一条**观察**,
**不是**"旧数被验证了" —— 它们的可比性在 §0 就已被否掉。

★★ **实用后果(这是本轮对使用者问题的真正回答)**:
**LE 主导没有建在沙上。** 那条线可以继续 —— 而它现在有了一个 **HEAD 上的、收敛的、内部自洽的**数字。

## 4. RECORDED(跨流水线 + 跨时间,只作参考,不作验证)

cl_p **0.272994** vs `run_m3_budget` 里记的 P14 medium 锚点 **0.277628** = **−1.67 %**。
而生产 taper 自身的偏差是 **−1.3 %** ⇒ 扣掉它之后余 **−0.37 %**。
★ **但这只是算术上的相合**:P14 的那个锚点**也早于圆角帽切换**,所以这是
**跨流水线 + 跨时间**的比较(第 5 问),**只作参考**。

## 5. 本轮修掉的**脚本陈旧**(不只是数字陈旧)

`bench/run_m3_budget.py` 现在:

1. **生产腿默认加 `tip_taper`**(构造与 `_m6_case` 逐字,并由 assert 钉住);
2. `taper=False` 保留,**仅用于复现历史配置**;
3. ★ **taper 运行写自己的文件名**(`m3_budget_head*.csv`),
   **2026-07-31 的 CSV 一字节未动** —— 它是被能力边界引用的历史产物,
   而覆盖它就是本项目记过的那个 G3 类危险;
4. `main` 可选**腿**与**级别** ⇒ 本轮只跑生产腿(另三条腿是另一个问题,理由已在注册记录)。

## 6. 成本

单腿 **~10 min**(注册把闸从 600 s 上调到 900 s,**理由已登记**:
600 s 是从 coarse 的 ~35 s 直觉外推来的,对 medium 本来就太紧)。**未触闸。**

## 7. 下一步

1. **勘误收口**:把 §2 的新数写进能力边界,并把那 5 处"过期"标注升级为
   "**已退役,替代读数见此**" —— ★ 这是**本轮必须做完**的,否则文档里留着一个只说"过期"却没有替代的洞;
2. 使用者裁决里的**第 3 步**:把 LE 的研究搬到 **α = 0**(唯一区)——
   ★ 而现在它的入口条件变好了:**LE 主导在 HEAD 上成立**,所以那次迁移是在一个**已确认的**亏空上做,
   而不是在一个过期的数上做;
3. ★ 仍然悬着的:**coarse 上两条流水线差 2.0 %**(上一轮留档)——
   与本轮无关,但它是一条未解释的不一致,应当在某一轮里被处理。
