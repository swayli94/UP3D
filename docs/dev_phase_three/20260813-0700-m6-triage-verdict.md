# 20260813-0700 — 判定:**T4 / UNDEFINED + 成本闸开火**;而本轮真正的产出是:**那 70 % 已过期**

> 注册的问题("M6 medium 在 M0.8395/α3.06 上是否落在多解区")**没有被回答** ——
> 三条腿全不收敛(平衡面板为空 ⇒ **T4**),16 线程臂在第一条腿就被**成本闸**拦下(622 s > 600 s ⇒ **kill 条 3**)。
> ★★★ 但守卫 **G-R** 在过程中揪出一件更重要的事:
> **"LE 占 69.6 % / MID 20.7 %" 这个数出自 2026-07-31,而生产配方此后改过三次** ——
> 它是**陈旧产物**,而能力边界一直把它当**在用的数**引用。
> ⇒ **使用者那个问题("多解时怎样研究 LE 那 70 %")的答案是:现在还谈不到多解 —— 先重测。**

预注册 [20260813-0300](20260813-0300-m6-triage-prereg.md) + **四份附录**(全部在对应动作之前提交)。
脚本 `bench/run_task3_m6_triage.py` → `gate_results/task3_m6_triage_8t.csv` / `_16t.csv`。

## 1. 实测

| 臂 / 线程 | seed | 收敛 | \|R\| | cl_p | cl_KJ | rms_LE(上/下) | 墙钟 |
|---|---|---|---|---|---|---|---|
| A / **8t** | 0 | ✗ | 8.80e-07 | 0.249336 | 0.256491 | 0.295325 / 0.106043 | 482 s |
| A / 8t | 5 | ✗ | 1.87e-06 | 0.237561 | 0.246222 | 0.333385 / 0.113395 | 224 s |
| A / 8t | 12 | ✗ | 8.80e-07 | **0.249336**(与 seed 0 **逐位相同**) | 0.256491 | 0.295325 / 0.106043 | 522 s |
| A / **16t** | 0 | ✗ | 8.80e-07 | **0.249336** | **0.256491** | **0.295325 / 0.106043** | **622 s** |

**已提交的参照**(`m3_budget.csv` 的 `ON/pressure`,medium):cl_p **0.276527** / cl_KJ **0.281179** /
LE 上 **0.235615** / LE 下 **0.092155**、`converged = True`。

**三条判读,分开说:**

1. ★ **16 线程与 8 线程逐位相同** ⇒ **这条 M6 medium 爬坡是线程无关的**。
   我原以为它会像 NACA M0.80 medium 那样有线程依赖 —— **它没有**(附录 #3 的诊断因此被撤回);
2. ★ **seed 0 与 seed 12 也逐位相同**,只有 seed 5 不同 ⇒ **入口处的"抽样"只有两个不同结果,不是三个**
   (M0.70 的 probe 解在 seed 0 与 12 上落到同一状态);
3. **全部不收敛**(|R| ~1e-6,`m_final = None`)⇒ 平衡面板为空 ⇒ **T4 / UNDEFINED**,
   注册的 T1–T3 **不可判**。按前提 P1,这些数**不是读数**,只作 RECORDED。

## 2. G-R:**不可满足(参照过期)** —— 不是"我的配方错"

| 时间 | 事件 |
|---|---|
| **2026-07-31 16:09** | `gate_results/m3_budget.csv` 提交 —— **那 70 % 的唯一出处** |
| **2026-08-02** `12b9e3d` | **GS3.1:生产 M6 配方翻成 `amg`** |
| **2026-08-05** `3631f6f` | **G8.2 重锚到圆角帽 + 生产 taper** |
| 2026-08-10 `1a41cec` | 快层线程钉定 |

⇒ **参照早于三次已记录的变更**,因此**与 HEAD 的运行不可比**;G-R 在任何正确运行上都不可能过。
★ 最可能的主因是 **08-05 的圆角帽/taper 重锚**(cl_p 0.2765 → 0.2493 = **−9.8 %**,量级与几何变更相称),
但 `.msh` 是 gitignored,**我无法核实 07-31 当天的 `medium.msh` 是圆角还是平角帽**
⇒ 标为**最可能**,**不作断言**。

★★ **这两句的区别是全部**:"我的配方错" ⇒ 去改脚本;"**参照过期**" ⇒ 去**重测并勘误**。
G-R 的价值就是把它们分开了。

## 3. kill criterion

| 条 | 是否成立 |
|---|---|
| 1. G-R 不过 | ★ **成立,但性质是"参照过期"**(见 §2)⇒ 按条文停下对齐,已对齐到"参照不可用" |
| 2. T4 | ★ **成立** —— 臂 A 收敛腿 0 条 |
| 3. **单腿 > 10 min** | ★★ **成立** —— 16 线程 **622 s**,**停,不加预算** |
| 4. 三腿逐位相同 | 部分成立(seed 0 与 12 逐位相同,seed 5 不同)⇒ **仪器有效但有效样本只有 2 个** |

⇒ **注册的问题未获回答,本轮如实记为未答。** ★ 而 kill 条 3 的开火是它该有的样子:
附录 #2 已预警"515 s 贴近 600 s",16 线程实测 622 s(**比 8 线程的 482 s 还慢** ——
又一次"墙钟是机器标定,不是求解器读数")。

## 4. ★★★ 本轮真正的产出:那 70 % 已过期,而顺序被它颠倒

**"LE 占 69.6 % / MID 20.7 %"是 2026-07-31 的产物**,而能力边界一直把它当在用的数引用。
这正是项目自己的**再定基勘误清单**要防的事:一次变更取代了旧数字,旧数字留在文档里没人改。

⇒ **对使用者那个问题的答案**:
**"在有多解时怎样研究 LE 那 70 %"这个提问本身已经过时了 —— 那 70 % 必须先在 HEAD 上重测。**
重测之后才轮到给它做种子分诊(而分诊要按 §5 的成本重新设计)。

★ **本轮不重测**:那是另一轮的事,它要动 `run_m3_budget` 的重跑与整条勘误链。
把"发现 + 重测 + 勘误"混在一轮里,就分不清哪一步的结论属于哪一次测量。

## 5. 下一轮必须先解决的**成本**问题

M6 medium 单腿 **482–622 s**,而三种子 × 两臂根本装不进任何合理预算。可选(交裁决):

1. **在 coarse 上做分诊**,并**同时**在 coarse 上重测那 70 %
   —— ★ 但要先造出 coarse 的参照(本脚本此前**没有** coarse 行);
   ★ 而本轮已顺带留下 coarse 的首次读数:cl_p 0.268115 / cl_kj 0.273582,
   **与 P14 coarse 锚点差 2.0 %** —— 两条流水线在 coarse 上不一致,这一条也需要解释;
2. **只重测那 70 %(单种子,HEAD)**,先把陈旧数换掉,分诊延后;
3. **把 LE 项整体移到 α = 0**(唯一区,协议 §2)—— 这样既绕开多解、又便宜,
   ★ 但**它换了工况**,所以那 70 % 的含义也随之变化,必须重新定义"占比"相对什么。

**我推荐 2 → 3**:先把在用的数变成 HEAD 的数(否则后面每一步都建在过期数上),再把 LE 的研究搬到唯一区。

## 6. 本轮我的错(四份附录,全部在动作之前提交)

1. ★ **附录 #1**:种子要变在**哪一处** —— 只改爬坡第 0 级会因 `phi_init` 而**惰性**,
   产出三条逐位相同的腿并**因错的理由**触发 kill 条 4。**冒烟抓到,执行前修**;
2. ★★ **附录 #2 的两个错**:把 `main(levels=("coarse",))` 的**函数默认值**当成证据(实际是 medium);
   G-R 拿了**另一条流水线**(P14)的锚点,而本脚本**没有** coarse 行;
3. ★★ **附录 #3 的诊断错**:把 G-R 失败归因于**线程**,而 16t 与 8t **逐位相同**证否 ⇒ 已撤回;
4. ★★★ **同一族错误在本轮出现四次** —— **跨流水线 / 跨级别 / 跨溯源 / 跨时间**,
   四次都是"**我要比的两个数不是同一个东西**"。已升为 CLAUDE.md 常设检查的**第 5 问**。
   ★ 而四次**全部由守卫先开火**,没有一次把不可比的数读成了结论 —— 这就是 G-R"不过就停"的全部价值。
