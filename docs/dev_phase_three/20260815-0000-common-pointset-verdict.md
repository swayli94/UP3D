# 20260815-0000 — 判定:**N-DISC —— LE 上表面的解根本没收敛**(`d_self` 随加密**增大**);
# ★★★ 而全带表交出一条更有用的结构:**三个带处在三种不同的状态**

> **零求解**(G-Z 断言)。**G-R PASS**(S4 全点集 e = **0.242221 逐位**)、
> **G-I PASS**(点集恒等式违背 **0.00e+00**)、**G-N**:LE 上公共点 **36/38**(只丢 2 点,x = 0.00042 / 0.0021)。
> ★★ **公共点集上**:e = **0.541744 / 0.356840 / 0.239153**(S0/S2/S4),
> 而 **d_self = 0.241693(S0→S2)→ 0.292833(S2→S4)** ⇒ **越细,相邻两级之差越大(+21.2 %)**。
> **R = 0.292833 / 0.239153 = 1.2245** ⇒ **N-DISC 的两个条件都成立**(R ≥ 1 **且** d_self 不缩小)。
> ★ **我事前写下的预测(N-DISC)被证实** —— 而它可以被推翻的那条路(前缘夹紧点承载了差异)
> 也被**直接测掉了**:P2 显示去掉那些点只改变 **−9.7 %**,**上一轮的读数不是伪影**。
> ★★★ **而全带表是本轮真正的收获**:**LE 上是唯一 `d_self` 增大的带**;MID 与 LE 下**正常收敛**;
> 而 **TE 两面 `d_self` 缩小但 e 单调变差** ⇒ **它们在收敛,只是收敛到离实验更远的地方**。

预注册 [20260814-2200](20260814-2200-common-pointset-prereg.md) + 其 §0 的**事前预测**(动代码前提交,`59719f0`)。
脚本 `bench/run_task3_common_pointset.py` → `gate_results/task3_common_pointset.csv`。

## 1. 守卫(先过,再读)

| 守卫 | 结果 |
|---|---|
| **G-Z 零求解** | **PASS**(断言脚本体内不含求解入口)★ 见 §5:这条守卫的**第一版误报了自己** |
| **G-R 保管链** | **PASS** —— S4 全点集 e(LE 上) **0.242221 逐位** ⇒ 读的是同一批缓存曲线 |
| ★★ **G-I 点集恒等式** | **PASS,违背 0.00e+00** —— `e_full²·n_full = e_common²·n_common + Σ(丢点残差²)` |
| **G-N 点数** | LE 上 **36/38**(丢 x = 0.00042 / 0.0021)、LE 下 **17/18**(丢 x = 0.0)、**MID/TE 一点没丢** |

★ 只丢了 **3 个点**(全在 x < 0.003 c),与上一轮的诊断完全一致。

## 2. 主判读:**N-DISC**

**公共点集,LE 上表面**(binding):

| | S0(h 0.060) | S2(h 0.030) | S4(h 0.015) |
|---|---|---|---|
| tets | 9 303 | 59 359 | 448 197 |
| **e**(对实验) | 0.541744 | 0.356840 | **0.239153** |
| **d_self** | — 0.241693 → | — **0.292833** → | |

- **d_self 随加密增大 +21.2 %**(0.241693 → 0.292833)⇒ **不缩小**;
- **R = d_self(S2→S4) / e(S4) = 1.2245 ≥ 1**;
- ⇒ **N-DISC 的两条件同时成立。**

★★ **这句话要说准**:N-DISC **不是**"再加密就能达标"。它说的是
**这个序列没在收敛,所以从它做任何外推(向好或向坏)都不成立**。
⇒ 而现有结论"**换网格解决不了 M3**"正是**从这个序列外推出来的**(GS2.1 的四点外推 + 地板判定)——
**它的推理基础因此失效**。★ 不是"结论被证反",是"**证据不成立**":靶子可达与否**现在是未知**,
而**先要修的是"为什么 LE 上表面不收敛"**,不是网格预算。

★ **一条对结论有利的限定**:S0 的网格质量本来就差(AR 23.73)⇒ `d_self(S0→S2)` 若含 S0 的质量成分,
它会**偏大**;而实测**更细的那一对反而更大** ⇒ 这个限定**加强**而不是削弱结论。

## 3. ★★★ 全带表 —— 三个带处在**三种不同的状态**

| 带/面 | n | e(S0) | e(S2) | e(S4) | d(S0→S2) | d(S2→S4) | **状态** |
|---|---|---|---|---|---|---|---|
| **LE / 上** | 36 | 0.54174 | 0.35684 | **0.23915** | 0.24169 | ★ **0.29283** | ★★ **没收敛**(d 增大) |
| LE / 下 | 17 | 0.31309 | 0.22102 | 0.09431 | 0.20577 | 0.16416 | 正常收敛(d 缩小,e 缩小) |
| MID / 上 | 69 | 0.17839 | 0.13087 | 0.09832 | 0.07703 | 0.06270 | 正常收敛 |
| MID / 下 | 36 | 0.06994 | 0.04153 | 0.02621 | 0.03517 | 0.02163 | 正常收敛 |
| **TE / 上** | 15 | 0.02941 | 0.03919 | ★ **0.07156** | 0.04532 | 0.04122 | ★★ **收敛,但朝离实验更远处** |
| **TE / 下** | 5 | 0.01799 | 0.04615 | 0.04857 | 0.04693 | 0.02071 | ★ 同上(n = 5,样本很薄) |

★★★ **三种状态,三种含义,而它们要分开处理:**

1. **LE 上(预算大头)**:`d_self` **增大** ⇒ **离散/求解器侧的非收敛** —— 这是 N-DISC 的来源,也是靶子;
2. **LE 下 + MID**:`d_self` **缩小**且 `e` **缩小** ⇒ 这些带**是在正常收敛的** ——
   ★ 所以"整个解都不收敛"是**错的**说法,**非收敛被定位到 LE 上表面**;
3. **TE 两面**:`d_self` **缩小**而 `e` **单调变差**(TE 上 0.0294 → 0.0392 → 0.0716,**+143 %**)
   ⇒ **解在收敛,只是收敛到离实验更远的地方** = **模型误差的干净签名**。
   ★ 而这正是本轮 N-MODEL 那一支所描述的状态 —— **它出现在 TE,不在 LE**。

★ 限定:TE 上只有 **15 点**、TE 下只有 **5 点**(而 TE 下 n=5 是本会话"n 小到判据空洞"那一族的老熟人)
⇒ TE 的两条只作 **RECORDED**,不进判据。

## 4. P2(RECORDED,数字已被看过):**上一轮的读数不是伪影**

| √2 对 S3→S4 的 d_self | 值 |
|---|---|
| 全部点(上一轮报的) | **0.250115** |
| 只用公共点 | **0.225735** |
| 只用被丢的点(**n = 1**) | ★ **0.701279** |

⇒ 去掉被夹紧的点只改变 **−9.7 %** ⇒ **上一轮那 0.214/0.250 不是前缘夹紧伪影。**
★ 值得注意的细节:**那一个被丢的点本身差 0.70**(很大),但它只占 1/38 权重,所以 RMS 只动 9.7 %。
⇒ **"个别点很脏"与"读数被污染"是两件事**,而这里是前者。

★★ 所以我事前写下的**那条推翻路径被测掉了** —— 这就是把预测和它的反驳方式**都**写在前面的用处。

## 5. ★★ 一个守卫误报了自己(第三次同族)

**G-Z 的第一版**扫描自己的源码找"求解入口",而它**匹配到了自己的禁止词清单**,
于是在一个**一次求解都没有**的文件上触发。
⇒ 与本项目已记两次的 **`pgrep -f <pattern>` / `ps | grep` 匹配到自己的命令行**是**同一族:自指守卫**。
修法:禁止词在运行时**拼接**,字面量不出现在被扫描的源码里。
★ 教训一句话:**守卫也要考虑"它自己算不算被检查的对象"。**

## 6. kill criterion

| 条 | 是否成立 |
|---|---|
| 1. G-R 不过 | 不成立(逐位) |
| 2. G-I 不过 | 不成立(**0.00e+00**) |
| 3. LE 上公共点 < 20 | 不成立(**36**) |
| 4. 缓存曲线缺失 | 不成立(S0/S2/S4 全在) |
| 5. d_self 恰为 0 | 不成立 |

★ **N-DISC 不算 kill**(注册明写三支都不算)。**M3 仍不达标**(沿用前十五轮的豁免)。

## 7. 读数汇总(四句话)

1. ★★ **LE 上表面的解没有收敛**:`d_self` 随加密**增大 21.2 %**,且已达 e 自身的 **1.22 倍**;
2. ★★★ **非收敛是局部的**:LE 下与 MID **正常收敛** ⇒ 不能说"解不收敛",要说"**LE 上表面不收敛**";
3. ★★ **TE 是另一种病**:`d_self` 缩小而 `e` 变差 ⇒ **收敛到离实验更远处 = 模型误差签名**(RECORDED,样本薄);
4. ★★★ **"换网格解决不了 M3"这个结论的证据基础失效** —— 它是从**一个没在收敛的序列**外推出来的。
   **不是**结论被证反,是**外推不成立**;靶子可达与否**现在未知**,而**下一个问题变成了
   「为什么 LE 上表面不收敛」**,那与"预算/分配/阶数"都不是同一类问题。

## 8. 下一步(**另行预注册**)

1. ★★★ **「LE 上表面为什么不收敛」的机理**。事前可想到的候选(**注册时要写成互斥的、可分辨的**):
   (a) **前缘吸力峰的位置/幅值随网格跳变**(本会话已见:LE 越细,全局 M_max 搬到内侧,
       cl_p 在 0.2588–0.2659 间非单调);
   (b) **激波/声速线的离散位置在跳**(项目已把"亚单元激波定位"列为能力边界 §3 的首位亏空);
   (c) **σ 冻结的相位**(本会话已测:σ 在第 8 步被永久冻住,冻在哪个相位由种子/网格决定);
   (d) **P13 翼尖自由边奇点**的残余影响(taper 已经压住它,但 W2 只测了半径剂量)。
   ★ 这四条**指向完全不同的修法**,所以第一轮应当是**判别式**而不是修补;
2. ★ **S1 那个中间级的非收敛**(0 clamps 的停滞)可以用项目自己的 `classify_failure` 分型,**便宜**;
3. 仍悬着:**Q-B 直解臂**、**coarse 两流水线差 2.0 %**、`wing3d.py` 的 docstring/签名帽型矛盾。
