# 第三阶段开发历史(每轮一行)

规则同 phase two:每轮一行,叙事写在轮次文件里,判定只有 PASS / FAIL / RECORDED。
轮次文件格式见 [../dev_phase_two/_TEMPLATE.md](../dev_phase_two/_TEMPLATE.md)。

**接手材料**:先读
[../dev_phase_two/PHASE_TWO_CAPABILITY_BOUNDARY.md](../dev_phase_two/PHASE_TWO_CAPABILITY_BOUNDARY.md)。
计划与裁决(D1–D5)仍在 [../dev_phase_two/roadmap.md](../dev_phase_two/roadmap.md) ——
何时可以把那 6 个文件移进 `phases/p2/docs/` 有一份逐文件条件检查单,见
[../dev_phase_two/README.md](../dev_phase_two/README.md)。

**使用者定的顺序**:① 删 level-set → ② 六面体贴体网格 → ③ 其余(按能力边界 §3 亏空 / §4 缺陷)。

| # | 日期 | 轮次文件 | 内容 | 一句话结论 | 判定 | 测试状态 |
|---|---|---|---|---|---|---|
| 1 | 2026-08-10 | [20260810-1900-ls-deletion-plan.md](20260810-1900-ls-deletion-plan.md) | 任务 ① 的**计划**(动代码前提交) | 固定范围/顺序/每步验证;★ 核实出**两条不能照抄清单的前提**:范围是"删活的、不删归档"(清单写于目录整理之前),且清单第 4 步"先补 V2 的 conforming 等价测试"**已经满足**(那两条性质在同一文件里各已有一条 conforming 对应)—— 照字面做会去写一个已存在的测试 | RECORDED(计划) | 未动代码 |
| 2 | 2026-08-11 | [20260811-0100-ls-deletion-verdict.md](20260811-0100-ls-deletion-verdict.md) | 任务 ① 执行:**删除 level-set** | ★★ **9 库文件 / 4624 deletions**(与 AST 实测一致 ⇒ roadmap 的"约 2500"低 **1.85 倍**,已更正);`pyfp3d/wake/` 整包移除;`post/unified.py` **塌缩**成 conforming 一半(`mvop=`/`phi_ext=` 关键字删掉而非留成惰性参数)。**"只减不改"两层账都算平**:删库那一步 passed **457→457 一个没动**;门控 720/2/8 → 466/1/4,差额 **208+47+5−1 = 259** 精确吻合(那 47 项用 `git worktree` 回到删前提交数出来)。★ 归档 vs 摘腿**按目的判不按名字判**:清单的"18 整删/8 摘腿"实为 **23 整删/3 摘腿**(跟随 helper 后 5 个"混合"文件是 100% LS);第一版分类器低报,改传递闭包并在最易错的 b4 上验证。★★ 途中**先补了一条能力锁**(见下一行),所以本轮不是纯只减。★ 三处自己的错都记了:p13 误按摘腿改了一半(读到 G13.1 判据才发现,用 `git checkout HEAD --` 撤回)、`PROJECT_STRUCTURE` 目录树按行删留下孤儿续行(撤回改按条目块删)、以及一条 `python -c` 忘了激活 `up3d` 撞上记录在案的环境陷阱 | **PASS(只减不改)** | 常开 **457 / 12 / 2**;门控 **466 / 1 / 4**(2:08:50);快层 **5/5 green, 891 s** |
| 2b | 2026-08-11 | 同上 §2 | ★ **补 conforming 翼身跨声速能力锁**(`tests/test_b32_wingbody_conforming_transonic.py`) | 使用者问"既然放弃 LS 了跨模型还有什么用"—— 这一问戳出**真缺口**:b18 的 demo 除跨模型外还记着 **conforming 翼身 M0.84 / cl_p 0.2738 / 0 clamps**,而那条能力**没有任何活的锁**(b9 的测试只到 M0.5;b31 锁 blend 代数;b18 自己的测试 4/4 全 LS 已归档)⇒ 能力边界在陈述它、却没有东西会因它坏掉而变红,而下一步换网格范式正是最可能弄坏它的工作。⇒ **先补锁再归档**;锚点全取 b18 已提交值、配方取其 `conf_ramp` 逐字;docstring 明写它是**漂移锁不是正确性声明**(翼身升力无外部真值),clamp 按 GS1.4 单独断言。★ 并加入**快层**——不加就等于建了一条比它所属层级少跑 6 倍的锁 | **PASS** | 单测 1 passed / 652 s;快层内 628 s |

| 3 | 2026-08-11 | [20260811-0300-hex-mesh-prereg.md](20260811-0300-hex-mesh-prereg.md) | 任务 ② 预注册(**含 kill criterion**,动代码前提交) | 使用者裁决:**先做 (A) 六面体→四面体剖分**,关切两条 —— **熵修正的上游单元是否更容易确定**、**壁面曲面的解析是否更好**;若都解决不了再做 (B)。首靶 2.5-D NACA0012 + M1。★★ 两条风险事前写下:① **剖分引入对角面 ⇒ 供体收益可能死在剖分上**(所以 Q1 是被测量的对象,不是假设);② **六面体不消掉刻面法向误差**(O 型壁面仍是平面刻面)⇒ Q2 的量是壁面 Cp 误差与阶数,不是法向误差 | RECORDED(预注册) | 未动代码 |
| 4 | 2026-08-11 | 同上 + `pyfp3d/meshgen/structured.py` | **G0 单变量性** | ★★ **G0 PASS,4/4 臂逐位不变** —— 而且单变量性是**构造的性质**不是挑对旋钮:分级**锚在壁面**+固定增长率+**层数派生**(扩域只向外追加层)、切向只依赖 `n_theta`、**不走 gmsh 尺寸场**。判据是**逐位相同**而非"变化很小":`r_far` 20→60 层数 37→45 而节点前缀逐位相同;`h_wall_normal` ÷2 而壁面节点/边/`h_t` 逐位相同;`n_theta` ×2 而径向序列逐位相同;`growth` 变而第一层间距逐位相同。★ 剖分**复用已被测试锁住的 `extrude_single_layer`**(固定对角线切四边形 ⇒ 每 hex 6 tet),不新写剖分器 —— 固定对角线也是为了**连通性不随分辨率变**,否则逐位判据无从谈起。★ 风险 ② 的数已到手:刻面法向误差 **1.2516 → 0.6252 → 0.3125 度**,干净 O(h) | **PASS** | 纯网格统计,无求解 |
| 5 | 2026-08-11 | [20260811-0500-hex-q2-verdict.md](20260811-0500-hex-q2-verdict.md) | **Q2 壁面精度**(圆柱对解析解) | ★★ **两条判据都 FAIL,而机理查清了**:壁面自由度**精确匹配**(126/252/504,靠 G0 的 `n_theta` 单变量性)且**壁面刻面几何逐位相同**的前提下,结构化正交分级只改善 max \|ΔCp\| **3.4–9.6%**、**阶数零改善**(两族都 ≈1.0)⇒ **壁面误差由 `O(h)` 刻面几何决定,不由网格结构/重构模板决定**。★ W3 两族逐位相同(0.953083/…)**先查后解释**:两族壁面各 63 个均匀角度、排序后差 **7.99e-15**,而网格明显不同(tets 6894 vs 11718)⇒ 构造性事实,不是量错。★★ **这对 (B) 同样成立** —— 平面刻面的六面体壁面几何一模一样 ⇒ **依据 3(壁面精度)对 (A)(B) 都被否**,只有曲面几何能动它,而 P11 已测负。★ **范围不得外推**:圆柱光滑、曲率均匀、壁面分布均匀,恰是非结构本就接近理想的情形;LE 要由 L2(NACA0012)回答。★ `assert nh == nu` 抓到**因子 2**(挤出使壁面节点翻倍)—— 没有它,这一轮会在壁面自由度差 2 倍的前提下跑完且不报错 | **FAIL(两条)+ 机理 RECORDED** | 圆柱三级,每级秒级 |

| 6 | 2026-08-11 | [20260811-0300-hex-mesh-prereg.md](20260811-0300-hex-mesh-prereg.md) → [0500 Q2](20260811-0500-hex-q2-verdict.md) → [1200 Q1 v2](20260811-1200-hex-q1-verdict-v2.md)(0800 版**已作废**)| 任务 ② / 路线 (A) | ★★ **kill criterion 第 1 条成立:(A) 对使用者关切的两件事都无效。** **Q2(壁面精度,圆柱对解析解)两条判据 FAIL** —— 壁面自由度精确匹配(126/252/504,靠 G0 的单变量性)、且**壁面刻面几何逐位相同**(两族各 63 个均匀角度,排序后差 7.99e-15,而网格明显不同)时,结构化正交分级只改善 max \|ΔCp\| **3.4–9.6%**、**阶数零改善**(两族 ≈1.0)⇒ **误差由 `O(h)` 刻面几何决定** ⇒ ★ **这对 (B) 同样成立(平面刻面六面体壁面一样),依据 3 被解析解否掉**。**Q1(供体确定性,NACA0012 M0.72 跨声速)FAIL** —— D1 1.12×(需 ≥2×)、D2/D3 **空洞**(基线本就 0 环 0 自指)⇒ ★★ **那个问题本来就不存在**,风险 1(剖分引入对角面、结构性摊平在面邻居图)被证实。⇒ **(A) 真正交付的是"实验可控性"(G0 逐位单变量),不是求解器改善**。★ **使用者两次看图纠正把这一轮从错的读数上拉回来**:v1 O 型网格 AR **1305**、含 1.1e-07 微小单元(我的守卫只查定向,"无翻转 ≠ 可用"),Q1 因此**撤回重测**;按使用者架构重建(有限层块 + 截断 + gmsh 外域、**TE 锁 ±y**(尖尾缘处平均法向是 +x,marching 会沿尾迹线退化)、**TE 去聚点**(余弦在两端都密,TE/LE 比 0.81 → **7.05**,同站数下 LE 反而更细))⇒ AR **1305 → 47 → 5.9**;重测后 D1 0.96× → **1.12×**、D2 差 9.3× → 差 2.5%,**方向对了但判据仍差得远**。★ 修网格中抓到 4 个真 bug(悬挂 wake 端点、TE 表面节点未焊、TE 立柱给 gmsh 一条 Line 而层块按层分段=22 条多余边界边、合并后未压实)+ **我自己 2 个仪器错**(交叉检测器误报 800、焊接后探针索引失效)| **FAIL(Q1+Q2)⇒ kill criterion 成立** | 两腿 converged / 0-0 clamps / 超临界;网格 AR max 5.9、自交 0、缝合 224=160+64 |

| 7 | 2026-08-11 | [20260811-1500 预注册](20260811-1500-task3-refinement-paradox-prereg.md) → [20260811-2100 判定](20260811-2100-task3-nonuniqueness-verdict.md) | 任务 ③ 第一项:**加密为什么让鲁棒性变差** | ★★★ **问题被答成了另一个问题:M0.80/α1.25 上离散解不唯一。** 四条腿(基线 / 全局加密 / **只加密激波带** / 只加密壁面法向)**全部收敛、0/0 clamps、两次运行逐位一致**,所以注册的 A1/A2/A3 **NOT APPLICABLE**、C1 **空洞**(不收敛的腿 0 条 —— 这次被空洞满足的是**我自己的判据**)。★★ 决定性对照:**网格逐位固定、只换 `n_picard_seed`**,x_shock 就撑开 **0.1683 c、cl 撑开 32.11%**(混合族,3/3 收敛)= (a) 的 ±0.02 的 **8.4×** / (b)(c) 的 3% 的 **10.7×**(★ 用 M1 判据原文,不用派生的 ±0.0055 c)。★★★ **2026-08-12 勘误(下一轮发现)**:非结构族那两个数**发表错了** —— 脚本算散布时**没过滤 `converged`**,把钳制垃圾态也算进去:coarse **0.2745 → 0.0122 c**(小 22 倍)、cl **23.1% → 0.21%**,medium **0.3060 → 无定义(0/3 收敛)**;混合族不变。⇒ **"族无关、非结构更糟"被推翻**:收敛状态之间非结构族更**紧**,它糟的是**收敛性**;M1 的 medium 卡在**没有解**(8 线程 0/3,σ ON/OFF 都是),**不是**卡在多解。核心结论(四腿对照不可读)不受影响 —— 那四条腿就在混合族上。而三个解 `|R| ≤ 3e-11、0/0 clamps` **各自都是真根** ⇒ **§2 那张加密对照表不可读**(固定种子**不固定解**,只固定了多解集里的一次任意抽样)。**范围**:需要**强激波和升力耦合同时在场** —— 包线内 M0.72 **完全唯一(0.0002 c,cl 一致到 6 位)**、M0.80/α=0 **近乎唯一(0.0088 c)**;**与网格族无关,非结构族更糟**。★★ **仪器验证**:我的两条腿**逐位复现**已提交的 `m1_gate_default_8t.csv`(0.8798/4388-563、0.5737947433/0-0)⇒ 与能力边界 §3.6 不矛盾,差的是**线程**,而线程本身值 **0.6006(16t,收敛)vs 0.5738(8t,不是解)= 0.027 c**。★★★ **提交前我自我更正掉了一条头条**:初稿写「这个发现从 2026-08-10 起就躺在提交的 CSV 里」—— **核对后不成立**,M1 门**只采种子 {0, 5}**,而这两个在已提交 coarse 上只差 **0.02/0.21/2.76 %**(cl_p),我那 32 % 来自**门从不采的 seed 12**。诚实版本:已提交数据里的警讯是 **coarse C=1.0 的 2.76 %,用掉 (b)(c) 全部 3 % 预算的 92 %**,而门 `overall &= _criteria(per-seed)` **对每个种子分别评、从不问两者是否落在同一答案上** ⇒ **缺的是一条会跨种子看的判据**(与「非 strict 的 xfail 检测不到回归」同族)。★ 顺带**独立验证**:该腿跨种子 x_shock 差 0.0136 c × GS1.1 的 `dcl/dx_shock ≈ 2` = 2.7 % 预测 vs **2.76 % 实测** ⇒ 那条敏感度链成立。★ **C1 已补答**:种子对照里 5 条失效腿 **5/5 分类** —— 4 条 `clamping`,而**非结构 medium/seed 5 是 `limit_cycle`**(0/0 clamps、|R| 9.91e-07)= **GS1.2 原报那个「不是钳制」失效的同形腿**,⇒ 修法指向**冻结选择 churn**;C3 按严格口径**独立验过**(30 步里 5 个值被重访、尾部非单调 ⇒ 不判 budget_limited,**没用 descent10 比率定案**)。★ **我四个错**:预注册误称「至今没被分类」(GS1.2b 早已定位到升力耦合 ⇒ 我的仪器从一开始就对不上)、把 R0「落在参考带内」说出口(抽样运气,已撤回)、**C2 在原理上看不到解的非唯一性**(固定种子+固定线程的两次运行构造性逐位相同)、以及上面那条**提交前自我更正的头条过度主张**。★ 差点拿「M1 走马赫爬坡」的记忆去推翻 §3.6,读 `run_m1_gate.py:116` 才发现是目标马赫直解、参数与我逐字相同 | **A1/A2/A3 N/A;C1 空洞→§3b 补答 5/5;C3 满足;kill criterion 三条都不成立** | 常开 **468 / 12 / 2, 0 failed**(494.47 s @8 线程,机器空闲;457 + 11 账平);新增 `tests/test_meshgen_structured.py` **11 passed**(其中 2 条首跑就红 —— 我按 `len(surface)` 索引点阵而 TE 被拆分 ⇒ `n_stations = len+1`,与本轮那个 LE 探针错同族) |

| 8 | 2026-08-12 | [20260811-2300 预注册](20260811-2300-sigma-selection-prereg.md)(+ 附录 #1/#2)→ [20260812-0100 判定](20260812-0100-sigma-selection-verdict.md) | 任务 ③ 第二项:**熵修正是不是在挑解支** + **给 M1 门补跨种子诊断** | ★★★ **字面判定 S3(不宣称方向),而 RECORDED 读数方向与假设相反**:σ **OFF**(等熵)在两个网格族上都 **3/3 收敛且三种子答案逐位相同**(x_shock 散布 **0.0000**);σ **ON**(生产默认)在 coarse 丢一个种子、在混合族散 **32.11 % / 0.1683 c** ⇒ **多解是随熵修正一起出现的**。★ 三个 case 都落 S3 但**三个不同理由**:coarse 被**我自己的空洞守卫**判掉(σ ON 自身散布仅 0.21 % ≤ 5 %)、medium **两腿都 0/3 收敛**(前提 P2:散布**无定义**,不许当「散布小」)、混合族比值 0.00 落在**我单边的带**之间。★★ **附录 #1 在看到 binding 腿之前就登记了「我的带是单边的」并明确不补第四条带**(事后补带 = 事后放宽);★★ **附录 #2 登记了我注册内部的自相矛盾**(§3.1 加种子 vs G3 不覆盖被引用的 CSV),解法是把第三种子做成 `--third-seed` 开关并如实标为对字面的偏离。★ 取舍已量化:σ OFF **唯一但在 M1 参考带外**(coarse 0.6581 高侧 / 混合 0.5605 低侧),σ ON **在带内但不唯一** —— 与 S1b 的立项理由一致,只是揭示了代价。★ **一个我查过之后否掉的猜想**:2 个负升力状态**不是「镜像环量支」**(比值 4.16/4.31/0.19/0.18,且都非收敛 + 重度钳制)。★★★ **本轮发现并修好上一轮的真缺陷**:`run_task3_seed_nonuniqueness.py` 算散布**没过滤 `converged`**,把钳制垃圾态算成了解 ⇒ 非结构 coarse **0.2745 → 0.0122 c**、medium **0.3060 → 无定义**;**「族无关/非结构更糟」被推翻**,**M1 的 medium 卡在没有解而不是多解**;上一轮核心结论(四腿对照不可读)不受影响。勘误已推到 4 个面 | **S3(binding)+ RECORDED 反向读数;G1/G2/G3 全 PASS** | G1 **12 行 × 14 字段差异 0**(逐位复现已提交 8 线程记录);G2 `M1: FAIL` 未被移动;G3 `m1_gate*.csv` 一字节未动 |

| 9 | 2026-08-12 | [20260812-0300 预注册](20260812-0300-sigma-strength-prereg.md) → [20260812-0500 判定](20260812-0500-sigma-strength-verdict.md) | 任务 ③ 第一项(使用者定的 1→3 里的 **1**):**σ 的强度是不是多解的剂量?** | ★ **入口检查零求解就问出关键前提**:σ **没有强度旋钮** —— 它是物理量(RH 总压比),`entropy_correction` 是 bool;`knee_frac` 是**探测**参数且 docstring 明写已被实测校准(扫它会混淆),改 M∞ 也混淆 ⇒ **必须新增临时仪器**。注册**在动代码之前**定死了刻度形式(`sigma_eff = 1 − θ(1 − σ_累积)`,施加在 transport **之后**;★ 另一个位置(transport 前缩放每单元 s)被**事前否掉**,因为 σ 是沿链**乘积**,缩放后的乘积不是乘积的缩放 ⇒ 那个 θ 不是线性刻度,选它会让单调性判据失去意义),**并把旋钮的删除条件也写在开工那一轮**。★★ **T0a 逐位性由构造保证**:`1 − 1.0·(1 − σ)` 浮点上**不等于** σ(0.9812 → 0.9811999999999999),内核在 θ==1 上**显式短路** —— 无分支写法会在**默认设置**下静默移动每个已提交数字;实测改库后 M1 门默认路径与已提交 8 线程记录 **12 行 × 14 字段差异 0**,并补 4 条单元测试(与旋钮同生共死)。**字面判定:两族都 T2(阈值型,不是剂量型)**。★★★ **但我自己戳穿了 T2 的触发条件**:θ=0.75 上两族都只有 2/3 收敛,而**少一个点的 max−min 定义上 ≤ 含它的三点** —— 死掉的种子恰是前一 θ 的**离群者** ⇒ 我的判据**跨 θ 比散布却不要求收敛种子集相同**(上一轮是「单边带」,这一轮是「未平衡面板」,同一毛病两个面)。**RECORDED 的未注册再分析(不用来改判)**:平衡面板下**混合族单调 0.01 % → 30.59 %**(单看它是 T1),**非结构族平衡后仍非单调**(θ=0.5 峰 6.42 %,θ=1 回落 0.21 %)⇒ **两族剂量形状不同**。★★ 机理侧的统一说法:**σ 变强让不同起点走向不同的支,并让部分起点直接失收敛** —— 「散布」同时被这两件相反方向的事影响,这正是非单调的根源 ⇒ 下一轮该量「分叉 + 失收敛」两件事,而不是一个把它们混在一起的数。★ 教训:**当自变量同时影响样本的存在性时,跨自变量的统计必须先固定样本集**。★ 回移检查:`picard.py` 默认走短路逐位不变,**决定不接进 Picard**(待删仪器,接进去只会让将来要拆的面翻倍) | **T2(两族);T0a/T0b/P3 全 PASS;kill 三条都不成立**(T2 明写不算 kill) | 30 点 ~3 min;`tests/test_s1b_entropy.py` +4;常开全套见下 |

| 10 | 2026-08-12 | [20260812-0700 预注册](20260812-0700-sigma-charge-count-prereg.md) → [20260812-0900 判定](20260812-0900-sigma-charge-count-verdict.md) | 任务 ③:**一条流线被收了几次激波的费?**(使用者按"收费次数"这个新方向裁决开启) | **字面判定 D2:不是次数,是每次收费的幅度** —— 实测 `N_at_min` 是 **1–2**,不是我预告的 7–12。★★★ **而我必须撤回自己的一句话**:上一轮我写「『被收多次』不是假设,是算式逼出来的 N ≥ 7–12」,推理用 `sqrt(mach2_max)`(最终收敛场峰值 1.40)当 M₁ 的上界 —— 而实际的 `m1_max` 是 **1.95**,两者**测在迭代的不同时刻**,我做了**跨量比较并据此把假设升级成定理**。(与项目已记的「引用你流水线真正跑过的那一个」、「M_max 2.4818 vs 1.995 是跨 family 比较」同族。讽刺的是**注册里的 N2 断言用的是正确的 `m1_max`** —— 这次是判据比我的散文严谨。)★★★ **机理被读出来了,不是推断**:`_SIGMA_REFRESH_MAX = 8` —— σ 只刷新 8 次然后**永久冻住**(注释的理由是实测且正确的:后激波**集合**是离散选择、会极限环,冻住才能让尾部变成固定光滑系统)。实测五条腿**全部把 8 次预算用光**,之后冻住 **13–40 步**;而 m1_max 轨迹是**振荡**的,**seed 0/5 的最后一次刷新正好落在瞬态尖峰 1.95/1.86 上并被永久冻住**,seed 12 落在已安定的 1.25 上 ⇒ **冻在振荡的哪个相位由种子决定**,冻住的幅度不同 ⇒ 尾部要解的**固定系统本身不同** ⇒ cl 0.26 vs 0.35。★★ **最尖的一条(从代码自己的理由得到)**:那段注释论证的是**离散集合**不可被 Newton 解决,但**实现把集合和幅度一起冻了** —— 幅度是连续量、本不需要冻,把它冻在任意相位正是路径依赖的注入点 ⇒ 候选修法有了明确靶子(**只冻集合、幅度继续更新**)。★ 守卫:**G1 预言机 PASS 且最大相对差 0.00e+00**(逐跳走链器与 `transport_sigma` 零共享代码;rtol 1e-12 事前定死、未放宽),G2 惰性 PASS,五条收敛腿**环数全 0**(反证了「收敛态无环」那个论证)。★ 那条非收敛腿(非结构 seed 12:m1_max **2.9690** ≈ m_cap、**1510 条链撞环**、σ_min 0.337)是记录在案的**塌缩路径**且**被正确拒绝**,按前提 P4 单独标注、排除出判定。★ 库改动只有一处**只记录、无开关、无数值影响**的 `upstream_sigma`(当次用的供体图无法事后重算——冻结步用 freeze 的图) | **D2;G1/G2/N2 全过;kill 三条都不成立**(D2 明写不算 kill) | 5 次求解 1–12 s;常开 41 passed(v0 + entropy);G2 见下 |

| 11 | 2026-08-12 | [20260812-1100 预注册](20260812-1100-sigma-freeze-prereg.md) → [20260812-1300 判定](20260812-1300-sigma-freeze-verdict.md) | 任务 ③:**σ 冻结** —— 使用者要求「分析为什么有极限环 / 怎么消除 / 至少要能报警」(Part 1+2 完成,**Part 3 未做**) | ★★ **入口检查先把这一轮的形状改了**:这里是**两种病、方向相反** —— **P-A 冻得太早**(σ 在大幅瞬态中被永久冻住,σ_min 摆 0.02–0.99,我已量化,想要**继续刷新**)与 **P-B 尾部集合翻动**(n_shock 73↔75、σ_min 动第 7 位、残差停滞,**我从未复现过**,想要**停止刷新**)⇒ `_SIGMA_REFRESH_MAX = 8` 是两边都不保证的折中。**使用者问「为什么有极限环」,而我还没复现过它 —— 对散文做机理分析就是在分析散文** ⇒ Part 1 必须是复现。★ 三个候选先按记录筛:**松弛/平均同族可用**;**加耗散排除并留下理由**(它已存在为 `upwind_c`,而 M1 判据 (c) 量的正是它的代价 9.4/17.9 %)。**Part 1 = L1:P-B 复现**(`_SIGMA_REFRESH_MAX` 在**进程内 monkeypatch**,**未编辑库**):9 腿 **5 条命中 L1**,周期 **2/3/3/4/5**,Δσ **钉成常数**;**注释记录的那条工况**给出 `74↔75 / Δσ 1.55e-02 / |R| 2.15e-06` 对上注释的 `73↔74 / 2.9e-2 / ~5e-6`。★★★ **9 条腿全部不收敛** ⇒ **冻结是承重结构**,这次是我自己测的而非抄注释。★★★ **机理定量钉住**:集合判定是**硬开关**(`m2u<=1` / `m2e>=1`),一个坐在声速线上的单元进出集合时 σ 从 1.0 **跳** `1−σ_RH(M1)`;把钉住的 Δσ 反解得 M₁ **1.268 / 1.228** —— **就是一个单元一次开/关的跳变量** ⇒ **离散集合 × 连续场、无阻尼 ⇒ 极限环;周期 = 场推回去要几步**。★★ **而同一内核家族已经解决过一次**:人工密度用的是**连续斜坡** `nu_e = C·max(0, 1 − mc²/max(m²,mc²))`(软),**只有熵的集合判定是硬的** ⇒ 一处内部不一致(与 B20「一个方程两个速度场」同族),并长出一个**比松弛/平均更对症的新候选:把集合判定做软**(注册明写 Part 3 方向由 Part 1 定,故在设计内,但如实标注为新增)。**Part 2 交付**:`_sigma_freeze_report()` 纯读判 + 返回字典 + 触发打印警告,**两个签名分开报不合成一个数**;**G-P2 逐位 PASS(12×14 差异 0)**。★★ **触发率先测再谈升级**:M1 门 14 次告警**全部是 frozen_in_transient、零次 selection_churn**(⇒ 生产配置下看不到集合翻动,正是冻结在起作用)⇒ **τ=1e-3 上 100% 触发,现在升级成拒绝会让 M1 门全红**;阈值敏感性 **1e-1 → 36% / 1e-2 → 86% / 1e-3 → 100%**,所以可行的中间路是把**拒绝**定在更严的一档(如 1e-1 只挑出最坏的 36%),τ=1e-3 留在**警告**级 —— 升不升级 = 使用者裁决。★ 两处库改动**都是只记录、无数值影响**(`upstream_sigma`、`shock_set_history` 上限 128)。★ 我的错:核对阈值时脚本末尾印出了**与它自己表格矛盾**的总结句(表格 36% 而句子写 100%),本文用表格的数 | **L1(Part 1);Part 2 交付 + G-P2 PASS;Part 3 未做** | 9 腿各 2–13 s;常开 **474 / 12 / 2, 0 failed**(468 实测 @469.74 s + 6 条独立实测的报警判读断言);`tests/test_s1b_entropy.py` **+6**(锁住四条件 AND、period 2 与 period 3、以及「短历史说不知道而不是说干净」)|

| 12 | 2026-08-12 | [20260812-1100 预注册 + 附录 #1](20260812-1100-sigma-freeze-prereg.md) → [20260812-1500 判定](20260812-1500-soft-membership-verdict.md) | 任务 ③ / **Part 3:软化后激波集合判定**(附录 #1 在动代码前定死斜坡形式 `s = 1 − w_u·w_e·(1−σ_RH)` 与 eps 的**标定地位**;候选重排的理由:软化从**源头**去跳变,而松弛/平均只是阻尼后果,且同一内核里 `m_crit` 已有先例)| **F3 PASS** —— eps=0 走原分支、**逐位复现 5 个 legacy 答案**(靠内核里的**显式短路**:`1−1.0·(1−s)` 浮点上不等于 `s`,无分支写法会连带外单元一起动 —— θ 那次的同一教训)。**F1+F2 在预注册的 eps=0.05 上两族同时 PASS**:混合族跨种子 cl 散布 **32.11 % → 4.28 %(降 7.5 倍)**、3/3 收敛、无 churn。★★★ **但我自己戳穿了它的归因**:软化把 σ_min 从 0.743/0.786 推到 **0.964–0.980**(修正整体**变弱**),而 σ **完全关掉**时该族 x_shock 是 **0.5605**、软化后三种子收拢到 **0.52–0.54 就在它旁边**;与上一轮已删除的 θ 刻度对照,软化落在 **θ≈0.25** 那一档(σ_min 0.98914,散布 1.39 %)⇒ **「散布收拢」分不清是「跳变消失」还是「没多少修正可敏感」,与 θ 那轮同源的混淆** ⇒ **F1 的读数成立、归因不成立**。**F5 FAIL(混合族)**:答案收拢到 **M1 参考带之外**(在带内 **1/3 → 0/3**,0.52–0.54 vs [0.59,0.63]);非结构族 2/2 未变坏 ⇒ ★ 附录 #1 加 F5 的理由被证实:**只看散布会把一个把激波推出参考带的改动判成成功**。★★ **eps 敏感性剧烈且非单调**(非结构族 0.21 → 10.27 → 1.84 → **41.08 %**)⇒ eps=0.05 是**窄甜点不是稳健设置**(同一形状已咬过四次:EW forcing / taper r_c / descent10 / τ)。★ **我判据的第三个缺陷**:F1 是**绝对阈值** ≤5 %,而两族基线差 150 倍 ⇒ 非结构族上散布 0.21 %→1.84 %(**实际变差 8.8 倍**)也被判 PASS;连续三轮三个同族缺陷(**单边带 → 未平衡面板 → 绝对阈值**),按规矩**不事后改判**,登记为下一轮必须先修。kill 条 3 不成立(F1/F2 可同时满足)⇒ **这条路不判死**;净结论:**机理靶子是对的,但这一版参数化不可采纳** | **F3 PASS;F1+F2 PASS@eps0.05(归因被混淆);F5 FAIL(混合族);kill 四条都不成立** | 24 点 0–10 s;常开 **479 / 12 / 2, 0 failed**(474 实测 @478.40 s + 5 条独立实测);`tests/test_s1b_entropy.py` **+5**(两端点必须与硬开关一致、带内部分收费且随 eps 单调、负 eps 报错)|

| 13 | 2026-08-12 | [20260812-1700 预注册](20260812-1700-magnitude-preserving-prereg.md) → [20260812-1900 判定](20260812-1900-magnitude-preserving-verdict.md) | 任务 ③:**幅度守恒的软化** —— 把「跳变是源头」与「只是修正变弱了」分开(★ 这是**新的注册而非附录**,因为它要**改判据 F1**,而在 F1 的结果已被读过之后用附录改它,形式上就是事后放宽)| 仪器:给权重加指数 `s = 1 − w^q(1−σ_RH)`,**q=1 就是上一轮**(所以上一轮成了本轮的一条腿)、**q<1 把带内权重推回 1 恢复幅度**,而**两端点仍精确**(0→0、1→1)⇒ F3 与端点测试不受影响。★ **F1′(相对基线)当场就起作用**:非结构族 q=1 的 1.84 % 在旧的绝对判据下是 PASS,在 F1′ 下是 **FAIL**(比该族基线差 8.8 倍)—— 上一轮的缺陷已修。**F3 PASS**(逐位)。**binding(混合族)= J3**:**没有任何 q 能把 σ_min 拉回 legacy ±0.03**(最近 **+0.0751**)⇒ J1/J2 读不出来,**kill 条 3 不成立、这条路不判死**;第二族字面 **J2**(匹配处 2.89 % vs 基线 0.21 %)。★★★ **而失败的原因是我的判据本身**:σ_min 是**收敛态的输出**不是输入侧幅度 —— legacy 的 σ_min 0.743 来自 **m1_max ≈ 1.95 的瞬态尖峰**,软化后尖峰不再被捕获(m1_max 1.37/1.32/1.35,**三种子几乎一致**),所以 σ_min 变高**既因权重变小也因解变了** ⇒ **这个目标在设计上就不可能稳定命中**。★★ **连续四轮第四个判据缺陷**(单边带 → 未平衡面板 → 绝对阈值 → **把输出当输入**),已升为 CLAUDE.md 的**常设四问**。★★ 数据交出一个**更好的、且两轮前机理就命名过的**归因变量:**冻结时刻捕获到的 m1_max 在种子间差多少** —— 与 cl 散布**单调对应**(0.705→32.11 %、0.054→4.28 %、0.077→3.99 %、0.344→24.19 %)⇒ **软化有效是因为它让「被冻住的那个状态」不再依赖种子**(即 P-A 机理),而不是「跳变」或「削弱」单独起作用;★ 但**仍不能宣布归因** —— q 同时改幅度与捕获均匀性(q 1→0.1 修正**变强**而散布**变差**),现有每个旋钮下两者同向。★ F5 在所有软化腿上仍是 **0/3 在带内**,采纳门槛远未达到 | **F3 PASS;binding J3(判据设计不可命中);第二族 J2;kill 四条都不成立** | 30 点 0–10 s;回归 52 passed(entropy 46 + v0 6);无新增测试(纯 q 轴,能力锁已在上一轮) |

| 14 | 2026-08-12 | [20260812-2100 预注册 + 附录 #1](20260812-2100-capture-selection-prereg.md) → [20260812-2300 判定](20260812-2300-capture-selection-verdict.md) | 任务 ③:**改「捕获」而不改「幅度」** —— 在已算出的 σ 场里**挑一个**,而不是混合 | ★★ **入口检查直接判掉了使用者原列的两个候选**:**平均**与**松弛**都**不是幅度中性**的(0.743 与 0.987 的均值是 0.865,比 0.743 弱;松弛就是指数加权平均)⇒ 它们会把上一轮刚排除的「削弱」混淆带回来;而**「选择」构造性中性**(采纳的是某次刷新的原样)⇒ 本轮做选择不做平滑,**且不引入任何新旋钮**。★ 判据先过了 CLAUDE.md 刚立的**常设四问**(不单边=K3;自变量会改变样本存在性 ⇒ **平衡面板**;阈值改**相对基线** F1′;**不做任何「匹配回基线」**,幅度中性由**输入侧守卫 G-M** 验证)。**G-M PASS 12/12(逐位)+ F3 PASS 5/5** ⇒ 问题被问干净了。★ **附录 #1**:冒烟抓到初版规则「|Δσ| 最小」有**系统性偏置** —— 它被「σ 几乎没在做事」的早期刷新最小化(采纳第 2 次:σ_min 0.98975,x_shock 0.5601 ≈ 等熵 0.5605)⇒ 改成**相对 Δσ**(按 1−σ_min 归一,无新旋钮),采纳第 6 次(σ_min 0.93238),**修正发生在读任何 K 判据之前**,有偏那版保留为 RECORDED 对照臂。**binding 族主臂 = K3(有害)**:平衡面板 [0,5] 上 cl 散布 **1.21 % → 7.47 %** 且**丢一个种子**(收敛 3→2);非结构族两臂也 K3(0.21 %→0.24 %)⇒ **kill 条 3 成立,选择规则这条路判死**。★★ **平衡面板当场救了一次误读**:主臂丢了 seed 12 后面板只剩 [0,5],**该面板上 off 臂的基线是 1.21 % 而不是含 seed 12 的 32.11 %** —— 不用平衡面板我会把「7.47 % vs 32.11 %」读成巨大改善,真相是**变差 6 倍**。★★★ **本轮最有价值的一条**:**每个臂的 m1_max 均匀性都改善了**(0.705→0.193、0.094→0.070、0.052→0.031)**而 cl 散布不跟** ⇒ 上一轮我记下的「捕获到的 m1_max 散布」这个归因变量**是相关不是杠杆**,那句「软化有效是因为捕获变均匀」**被否掉**;唯一真正压下 cl 散布的是**有偏的 abs 臂**(32.11 %→11.31 %),而它偏向弱修正 ⇒ 仍然指向「**削弱才是买到均匀性的东西**」。⇒ 按 kill 条 3 的原文,已把**七轮收口的取舍陈述写进能力边界**:「唯一性与修正强度是一对取舍,不是可单独修的缺陷」,并保留边界(M0.72 完全唯一;只在强激波+升力耦合同时在场时出问题)| **G-M/F3 PASS;binding K3 ⇒ kill 条 3 成立、路线判死** | 18 点;常开 **479 / 12 / 2, 0 failed**(494.75 s,**直接实测**,与上一轮 474+5 的账吻合);库改动:`capture_select` + 对照臂旗标,默认 off、legacy 逐位 |

| 15 | 2026-08-13 | [20260813-0100 协议](20260813-0100-post-sigma-research-protocol.md) → [20260813-0300 预注册 + 附录 #1–#4](20260813-0300-m6-triage-prereg.md) → [20260813-0700 判定](20260813-0700-m6-triage-verdict.md) | 分诊 ①:**M6 medium 在 M0.8395/α3.06 上是否落在多解区?**(σ 线收口后的第一步;选项 3「换离散化」已按使用者裁决登记为**未来方向、未开启**)| **注册的问题未获回答:T4 / UNDEFINED**(臂 A 三条腿**全不收敛**,平衡面板为空)+ **kill 条 3 开火**(16 线程单腿 **622 s > 600 s**,停、不加预算;附录 #2 已预警 515 s 贴近上限,而 16t 比 8t 的 482 s **还慢** —— 又一次「墙钟是机器标定」)。★★★ **但守卫 G-R 揪出一件更重要的事:那 70 % 已过期。** `m3_budget.csv` 提交于 **2026-07-31 16:09**,而生产 M6 配方此后**改过三次**(**08-02 `12b9e3d`** GS3.1 翻 `amg`、**08-05 `3631f6f`** G8.2 重锚到**圆角帽 + 生产 taper**、08-10 `1a41cec`)⇒ **参照与 HEAD 不可比,G-R 在任何正确运行上都不可能过**;HEAD 上重跑同一条腿给 **cl_p 0.2493 而非 0.2765(−9.8 %)、LE 上 RMS 0.2953 而非 0.2361、且不收敛**。⇒ ★ **「在有多解时怎样研究 LE 那 70 %」这个提问本身过时了 —— 那 70 % 必须先在 HEAD 上重测**,顺序被这条发现颠倒。勘误已按纪律 #11 推到**全部 5 处引用**(能力边界 ×1、progress ×2、roadmap ×2)。★ 另两条独立读数:**16t 与 8t 逐位相同 ⇒ 这条 M6 medium 爬坡线程无关**(我原以为会像 NACA M0.80 medium 那样有线程依赖,**没有**);**seed 0 与 seed 12 也逐位相同**,只 seed 5 不同 ⇒ 入口抽样**只有 2 个不同结果不是 3 个**。★ coarse 首次读数留档:cl_p 0.268115 / cl_kj 0.273582,**与 P14 coarse 锚点差 2.0 %**(两条流水线在 coarse 上不一致,待解释)。★★★ **同一族错误本轮出现四次**:**跨流水线 / 跨级别(读函数默认值而非证据)/ 跨溯源(8t vs 16t)/ 跨时间(HEAD vs 三次变更前)** —— 四次**全部由守卫先开火**,没有一次把不可比的数读成结论;已升为 CLAUDE.md 常设检查的**第 5 问**。四份附录**全部在对应动作之前提交**(#1 冒烟后修种子注入点、#2 改级别与参照、#3 归因线程、#4 **撤回 #3**)| **T4 / UNDEFINED;G-R 不可满足(参照过期);kill 条 3 开火** | 8t 三腿 + 16t 一腿(622 s 触闸);`task3_m6_triage_8t.csv` / `_16t.csv`;库零改动 |

| 16 | 2026-08-13 | [20260813-0900 预注册](20260813-0900-m3-remeasure-prereg.md) → [20260813-1100 判定](20260813-1100-m3-remeasure-verdict.md) | 在 HEAD 上**重测 M3 误差预算**(那 70 % 的替代品;使用者裁决的 2 → 3 里的 **2**)| ★★ **入口检查把这一轮的性质改了**:上一轮只知道「参照过期」,查下去是**两个独立过期源** —— ① `medium.msh` 文件时间 **2026-08-04 16:43**(正是圆角帽切换日),而 07-31 的 CSV 出自**平角帽**网格,**而 `.msh` 是 gitignored,这次几何变更在 git 里完全看不见**;② `run_m3_budget.py` 里 `tip_taper` 出现 **0 次**(grep 实测),而 B32/G8.2 于 08-05 采纳它。而 CLAUDE.md 明写**圆角帽在高 M 需要 taper**(去掉它 M0.88 medium 6/6 钳制),`test_p8_newton` 自己的注释也写着「round, no taper, CONVERGED 这条腿**不存在**」⇒ **HEAD 的失败被完全解释:不是代码回归,是脚本在两次生产变更后从未更新**。⇒ 本轮遂**不是刷新一个数,而是一次新测量**,旧数**退役**而非更新(注册 §0 事前写死,免得判定里出现「70 % 变成了 X %」这种本项目已犯四次的跨时间比较)。**补上生产 taper 后 medium 爬坡就收敛了**:`converged=True`、**|R| 7.327557e-15**、`n_floored 0`。**R1/R2/R3/R5 全过**(R3 **精确**:sqrt(ΣSS/Σn) = 0.132319 = 全点 pooled 0.132319;R2 用 assert 直接钉 `_m6_case` 的源码那一行,将来那边改了这里会红)。**R4 新预算(HEAD:圆角帽+生产 taper,medium)**:**LE 上 70.35 %(RMS 0.2422)**、**MID 上 21.05 %(0.0983)**、LE 下 5.02 %、TE 上 2.42 %、MID 下 0.78 %、TE 下 0.37 %,全点 pooled **0.1323**;cl_p **0.272994** / cl_KJ **0.277167**(★ R5:两者都带生产 taper 的 **−1.3 % cl_p 模型偏差**)。★★ **旧数退役与「新数落在同一位置」是两句话,分开说**:69.6 %/20.7 % 出自平角帽+无 taper,与本轮**不可比**;新的独立测量给出 70.35 %/21.05 %,**几乎同一位置**,而绝对 RMS 动了(0.2361 → 0.2422,+2.6 %)⇒ **占比结构对这次配置变更稳健、绝对值不稳健**,这是**观察**不是「旧数被验证」。★★★ **实用后果:LE 主导没有建在沙上,那条线可以继续**,而且现在它有一个 HEAD 上的、收敛的、内部自洽的数。★ 脚本的陈旧也修了:生产腿默认加 taper、`taper=False` 仅留作复现历史、**taper 运行写自己的文件名**(07-31 的 CSV **一字节未动** —— 覆盖它就是本项目记过的 G3 类危险)、`main` 可选腿与级别。★ 勘误收口:5 处标注从「过期」升级为「**已退役 + 替代读数**」。★ 成本闸从 600 s **有记录地**上调到 900 s(600 s 是从 coarse 的 ~35 s 外推来的,对 medium 本来就太紧),实测单腿 ~10 min,未触闸 | **R1–R5 全过;新预算已产出;旧数退役** | 1 腿 ~600 s;`m3_budget_head_medium.csv`(新文件);库零改动 |

| 17 | 2026-08-13 | [20260813-1300 预注册](20260813-1300-m6-triage-redo-prereg.md) → [20260813-1500 判定](20260813-1500-m6-triage-redo-verdict.md) | 分诊 ① **重做**(上一轮的 taper 修复解除了阻塞);**α=0 迁移改为条件性** | ★★ **入口检查发现使用者定的第 3 步前提未测**:搬到 α=0 的**唯一理由**是躲多解,而**多解只在 2.5-D 上测过**,分诊 ① 在 M6 上是 UNDEFINED;而 taper 修复让同一腿**收敛且只用 162 s** ⇒ 先测前提(α=0 会**去掉升力**,而 M3 目标是升力算例,代价是真的)。**结果 T1:三个种子全部收敛到机器精度**(|R| 5.5e-15 / 6.3e-15 / 7.3e-15),**cl_p 散布 0.0011 %**、cl_KJ 0.0007 %、**LE 上 RMS 散布 0.0025 %**、全点 pooled 0.0030 % —— 远在 0.5 % / 2.5 % 容差内 ⇒ ★★★ **α=0 迁移不必做**,既省成本又保住工况的物理相关性。★ 守卫:**G-R 是本会话第一次五问全过的比较**(参照 = 上一轮刚产出的同溯源行,同脚本/腿/级别/代码/线程);**G-D**:seed 5 与 seed 0 **不同 ⇒ 仪器已连通**,seed 12 与 seed 0 相同 ⇒ 按 G-D **如实记为「该种子可能未进入求解」**,故**有效样本是 2 个不同抽样不是 3 个**(与 2.5-D 同一观察一致),**T1 建立在最小样本上,如实登记**;**G-C**:每种子写自己的文件,seed 0 证据一字节未动;**线程由脚本打印实测 16/16/16**,不再靠「Bash 不保留 shell 状态」这类推理。★★★ **遵守注册的禁令**:这**不叫**「M6 没有多解」—— **Q-A(生产路径可复现)已答**,**Q-B(系统本身是否多解)仍开**,它需要绕开马赫爬坡的直解臂(上一轮按成本推迟、至今未跑);而 2.5-D 的多解为何没传到 3-D,有**三个未分离候选**(爬坡钉住选择 / taper / 三维本身)⇒ **不作归因**。★ 本会话已有守卫**第三次**在我的仪器产出错数之前挡住它:我第一版用 monkeypatch 改 `M6_NEWTON_KW` 注入种子,**配方漂移守卫立刻开火且它是对的**(那 dict 就是守卫的比对目标);脚本注释里已写着正确做法 —— **故意的偏离放在守卫之后作为显式参数**(`n_newton_max` 的既有模式,注释还记着 2026-08-01 因顺序错误「cost a four-leg sweep」)⇒ 照它加了 `probe_seed` | **T1;G-R/G-D/G-C 全过;kill 三条都不成立** | 2 腿 199 s / 215 s(闸 900 s);库零改动 |

| 18 | 2026-08-13 | [20260813-1700 预注册](20260813-1700-le-registration-prereg.md) → [20260813-1900 判定](20260813-1900-le-registration-verdict.md) | **LE 那 70 % 里有多少只是弦向错位?**(一个近零成本判别器)| ★ **入口检查先读 GS2.1**,确认**三条已否证**(**Kutta 形式** —— 作者先写解读规则再看数字、自己的规则否掉自己的假设;**LE 分辨率** —— 只加密 LE 最好 −8.3 % **且非单调**,而分辨率误差不会非单调;**几何 `h_te` 分裂场** —— L0p 与 L0 **逐位相同**),**一条测坏了**(尖端:G1 按字面 FAIL 但作者**自陈闸门设计错**)⇒ 本轮**不重测任何一条**。**要问的是从未被问过的那件事**:LE 带在 x/c∈[0,0.15] 上表面,而实验 Cp 极陡 —— 实测 **\|dCp/dx\| 中位数 19.05/弦 ⇒ 0.001 c 错位 ≈ 0.019 Cp**,**前提成立**;但**答案是否**:**最优 δ = 0.000**、LE 上 RMS 停在 **0.242221**,**可测范围内没有任何平移能改善**,逐站最优也全是 0(仅 η=0.90 为 +0.001)⇒ **H2:位置解释不了亏空** ⇒ 亏空被钉在**真实振幅误差**上,下一个嫌疑是 **G1.6 家族**(刻面几何 + P1 壁面恢复),而 **P11 已把曲面元测负** ⇒ 这是一条**很硬**的结论。★★ **我的设计缺陷,如实登记**:**41 个 δ 只有 21 个有效,无效的恰是全部负 δ** —— LE 带从 x=0 起,负平移**无处取值**(`np.interp` 端点截断而非外推,而注册要求的显式有效性检查**正是它抓出来的**)⇒ **H2 只在 δ∈[0,+0.02] 这一半上成立**;且 kill 条 2 要求有效 δ ≥ 21,实测**恰好 21,差一个就该停**,不当成通过得稳。★★★ **数据交出一条新线索**:LE 上 RMS **沿展向向翼尖增大并在 η=0.90 跳升 43 %**(0.1817/0.2043/0.2122/0.2067 → **0.2964**),而 η=0.90 是最外侧未遮蔽站 ⇒ **正指向唯一没被干净否掉的那条假设(尖端自由边奇点)**;★ 但**是线索不是结论** —— 展向梯度也可能来自后掠+锥度下 LE 曲率与局部弦长的变化、或外侧站的实验不确定度,**本轮不作归因**,它给出的是「下一轮该重新设计那个闸门」。★ 反向禁令也写明:**H2 不等于「位置一定没问题」** | **H2;kill 四条都不成立(第 2 条恰好擦边)** | 1 腿 385 s(闸 900 s);库零改动 |

| 19 | 2026-08-13 | [20260813-2100 预注册](20260813-2100-tip-gate-redesign-prereg.md) → [20260813-2300 判定](20260813-2300-tip-gate-verdict.md) | **重新设计尖端闸门**:用 LE-RMS 的**展向剖面**,不用任何代理 | ★ **入口检查读原文**:上一次闸门(`M_max < 1.6` 或峰值落在 η>0.95 尖端带)之所以坏,是作者**用峰值位置当奇点的代理**,而峰值**搬家到外翼前缘**(另一个东西)却仍被算进"尖端带";`M_max < 1.6` 也是拍的(2.1005 → 1.6719 = −20 %,只差一点没过任意线)⇒ 新闸门**不用代理**(直接用要解释的量)且**不用手拍阈值**(用**无量纲的外/内比**)。干预用**已有旋钮 `tip_taper` 的半径** r_c ∈ {0.025, 0.05(生产), 0.10};**不扫 r_c=0**(已知不收敛,非读数)。**结果 W2:三腿全收敛**(0/0 clamps、|R| ~7e-15),外/内比 **1.8185 → 1.8194 → 1.8213**,**ΔR = +0.0027 = 基线的 0.15 %**(W2 需 ≤0.05)⇒ **尖端不是 LE 亏空的驱动者**;★ 而**旋钮确实生效**:同一扫描 **cl_p −3.13 %** ⇒ kill 条 4 不触发,即 **taper 半径改变了展向载荷却几乎不改变 LE 误差的展向分布** —— 这正是闸门要分开的东西。★★ **我在注册里把基线比写成 1.631,错了**:上一轮那张表是**逐站最优 δ**(η0.90 取在 δ=+0.001),我当成了 δ=0 的剖面;**G-R 抓住了它**(池化 LE 0.242221 **逐位相同** ⇒ 仪器无问题,错的是我手算的派生数),而**更正让上一轮的观察更强**:η0.90 对 η0.80 是 **+60 %** 不是 +43 %。★★★ **η0.90 跳升的两条自然解释被零成本各否一条**:① **局部弦长/相对分辨率** —— corr(1/c,RMS)=+0.77 但 **RMS×c 不变平**(散布 40.4 % vs 65.6 %)且**变非单调** ⇒ 不是机理;② **实验点陡度/分布** —— **η0.90 的 \|dCp/dx\| 中位 2.18,比内侧(15–35)平一个数量级**、点数占比居中(32.3 %)⇒ **被否且方向相反**:最差那站在曲线**最平**处 ⇒ **那里是 Cp 量级本身错**,**位置在那里原则上就解释不了**,独立加强上一轮的 H2,并把亏空定性为**量级/水平误差**而非形状或位置误差。★★ **候选清单现在只剩一条**:Kutta / LE 分辨率 / `h_te` 几何 / 弦向错位 / **尖端** / 局部弦长 / 实验点分布 **全部已否证** ⇒ 只剩 **G1.6 家族(刻面几何 + P1 壁面恢复)**,而 **P11 已把曲面元测负** ⇒ **很硬的结论**;★ 但明写「**排除法从不排除还没想到的东西**」 | **W2;G-R 通过;kill 四条都不成立** | 3 腿 272/184/194 s(闸 900 s);`taper_rc` 按 `probe_seed` 模式加在漂移守卫之后(默认 0.05 = 生产,既有调用不变) |

| 20 | 2026-08-14 | [20260814-0100 预注册](20260814-0100-le-offset-scatter-prereg.md) → [20260814-0300 判定](20260814-0300-le-offset-scatter-verdict.md) | **LE 误差是「常数偏置」还是「散布」?**(把量级误差再分成两种不同的病)| 量是**恒等式不是拟合**:`RMS² = bias² + var` ⇒ `f_bias = bias²/RMS²` **无量纲、有界 [0,1]、自归一**,判据不需要任何物理尺度的手拍阈值(正好避开我前几轮栽过的绝对阈值缺陷)。**G-I PASS**(恒等式最坏相对违背 **3.16e-16**,rtol 1e-12)、**G-R PASS**(池化 LE 上 **0.242221 逐位相同** ⇒ 同一条腿)。**binding(η0.90 LE 上)f_bias = 0.037 ⇒ O2:散布主导**。★★ 而把 f_bias 与**纯噪声期望 1/n** 相比(无偏置时 bias 方差 σ²/n、rms²≈σ² ⇒ E[f_bias]≈1/n),结论**比 O2 更强**:**LE 上表面 5 站全部 f_bias ≤ 1/n** ⇒ **检测不到任何水平偏置** —— 不是「偏置较小」,是**在这个样本量下测不出来**;**G-S** 各站 bias 符号**翻转**(η0.20 为 −0.026 其余为正)⇒ **不是一个全局水平误差**,与 O2 一致。★★★ **同一个 1/n 比较抓到一处空洞**:**TE 下表面每站 n=1 ⇒ f_bias ≡ 1.000 是构造性的**(n=1 时 var≡0),我差点把「TE 下是纯偏置」当发现报出去 —— 与项目记过的「`d2h == 0` 在基线本就为 0 时被判 PASS」同族;**已把 1/n 列与空洞标注固化进脚本**,免得下次又只看 f_bias。★★ **真结构:两个表面是两种病** —— **上表面(LE 上 70.35 % + MID 上 21.05 % = 预算 91 %)是散布、检测不到偏置** ⇒ 指向**壁面恢复/刻面几何(G1.6 家族)**,与「只剩一条候选」一致并**收紧**它;**下表面(合计预算 6.2 %)有真实的系统性小正偏置**(MID/下 **5/5 站显著高于 1/n**,bias +0.018…+0.025;LE/下 4/5)⇒ 那是**载荷/环量**签名。★ 两条都要说:**91 % 是散布 ⇒ 投入在 G1.6 那侧**,而下表面那个偏置**小且不在预算大头上** ⇒ 归因是「**散布主导 + 一个不在大头上的小载荷偏置**」,不是二者之一。★ 限定:**n = 1–17**,每站 LE 带仅 6–10 点 ⇒ 全部结论都是**弱样本**结论,而 1/n 比较是处理它的正确方式而非掩盖 | **O2(收紧为「检测不到偏置」);G-I/G-R/G-S 全过;kill 四条都不成立** | 1 腿 202 s(闸 900 s);库零改动 |

| 21 | 2026-08-14 | [20260814-0500 预注册](20260814-0500-p2-reassessment-prereg.md) → [20260814-0700 判定](20260814-0700-p2-reassessment-verdict.md) | **只读重评:等参 P2 壁面层该不该开**(使用者裁决:先做只读评估)| **建议 = R2(P2 保持关闭)**,但**不是**上一轮我给的那个理由。★★★ **本轮真正的发现**:我四轮以来记作"未清且昂贵(4 011 s)"的那条注册义务,**早就在同一张已提交 CSV 里付过了** —— `phases/p2/bench/gate_results/tip_allscales.csv` 有**四条**全尺度腿(h_wall 0.030→0.010,46 k→1.16 M tets,全部收敛 0 lim/0 flr),**LE 上 RMS 0.3899 → 0.2704 → 0.2358 → 0.1758,四级单调,共 −54.9 % ★★★ **勘误 2026-08-14 addendum #1:这四条腿是 `tip_cap="flat"`**(`run_tip_allscales.py:49` 导入的 `run_le_response.build` 里 `BASE` 写死 flat),而生产族是 **round** ⇒ 按 CLAUDE.md 的标准规则"**平截帽上任何基于加密的论断前提为假**"(P13/G13.3:平截帽在加密下发散,LE 带阶 **0.37 flat / 0.87 round**),**此读数降级为 flat-cap 专属,生产行为 UNMEASURED**。见 [addendum #1](20260814-1000-fixed-budget-addendum1.md)。**(池化 −49.4 %)⇒ GS2.1 §7.2 那条**事前可证伪**的样本外预测(单调 + coarse 明显差于 0.2358;实测 1.65× 更差)**被满足**。**4 011 s 是 T1/T2 的成本,不是那条义务的成本**(登记的腿是 coarse+taper ≈20 s,实测 **7.2 s**)⇒ 我用一个**错的成本**把一条**几乎免费且已付清**的义务推迟了四轮(勘误 1;同族 = M6 分诊那轮"只读脚本默认值不读证据")。★★ **A1 抓到我自己的假设错了两次**:先假设圆柱 `O(h)` 与球 ~2 阶之差是 **max vs mean**,产物直接否掉(圆柱 `cp_err_mean` 阶 **1.006/1.011**);真正的跨比较是**量**——球那条被反复引用的 **1.67/1.98 是 `phi_err_wall_max`**,圆柱的 1.0 是 **Cp**,**差一次导数**(同量后球 Cp 1.35–1.81 vs 圆柱 1.01,仍跨几何)⇒ **"M6 的 LE binding 误差就是壁面几何"这个前提并未成立**。★★★ **A4 里又抓到我第二个越权外推**:球上"固定壁面只加密体网格"降**误差** 50.4 %(6.3× 于壁面曲面),但能力边界同一节记着 M6 **同类单旋钮腿对实验误差只动 −1.4 %…+3.4 %**(它们主导的是 Cp 的**位移** 81 %)⇒ **起作用的是尺度按比例一起动,不是把体网格单独调细**,§6 理由 2 已就地自我更正。★ **A2 对 P2 有利的那半边照原样说**:等参 P2 **按构造**免疫 P11 的失效机理(mapped-P1 = 几何二阶/场一阶错配,`O(h)` 抵消 `O(h)`),且**抬场阶** = P11 再归因所指的那 ≈11 pp ⇒ **P2 未被否证,只是尚未被证明必要**。★★ 判 R2 的是**事前**写死的三条:**A3 穿透 4/4** 条内核契约(上风"每单元一个 q²"/供体/σ 输运/Jacobian 4×4→10×10 改 CSR 图;38 个 njit 内核、22 处 4 节点硬编码)、**A0 明文禁止项**("不得再新增第二条并行技术路线";D6 已用同源动机停过六面体路线;design.md 自己把跨声速+Kutta 耦合列为"另一个阶段")、**前提未成立**(A1)。★ 勘误 2:上一轮"候选只剩 G1.6"那张表**漏了"按比例全尺度"这一条**,而能力边界的**依据 2 早已记着**相关测量 —— 又一次"没读原文"。★ 勘误 3:能力边界把 T1 成本写作 **950 s**,产物是 **3 950.4 s**(像掉了个"3")⇒ 它论证的"距 <60 s 的差距"应是 **66× 而不是 16×**,两处已就地更正 | **R2(★★★ 勘误 2026-08-14:P2 其实已被**裁决 D2(2026-07-28)**否决 —— 我那轮的 A0 漏读了 §2 的表与 D2,结论同向但「已评估-未开 + 立项条件」的框架是错的;重开 = 推翻 D2 = 使用者裁决);A0 kill 条 4 成立;G-P/G-5/G-N 全过,G-5 抓到两次跨比较** | **零求解、零库改动**;3 条勘误 propagate 至 6 处 |

| 22 | 2026-08-14 | [20260814-0900 预注册](20260814-0900-fixed-budget-allocation-prereg.md) + [addendum #1](20260814-1000-fixed-budget-addendum1.md) → [20260814-1200 判定](20260814-1200-fixed-budget-verdict.md) | **固定单元预算下重分配尺度**:全尺度那 −54.9 % 是"分配"买的还是"纯单元数"买的?| ★★★ **addendum #1(动代码前)**:那四条产生 −54.9 % 的腿是 **`tip_cap="flat"`**(`run_tip_allscales.py:49` 导入的 `run_le_response.build` 里 `BASE` 写死 flat),生产族是 **round** ⇒ 按 CLAUDE.md 标准规则"**平截帽上任何基于加密的论断前提为假**"(P13/G13.3;LE 带阶 **0.37 flat / 0.87 round**),**上一轮头条降级为 flat-cap 专属**;★ 我上一轮写的限定是"跨时间",而真正要紧的是**跨网格族** —— 第 5 问我问了四项、漏了"同一种几何"。新守卫 **G-C**(断言帽型)因此诞生。**判定按字面 = B3**:参照曲线在生产族上**HEAD 重测**(C_coarse **0.372531**/68 624 tets → C_medium **0.242221**/448 197 tets,**G = 0.130310 = −35.0 %**,局部指数 n^−0.2294 —— **两点是斜率不是曲线**);**A3(`h_edge` 减半 + 抬 `h_wall` 补偿)LE 上 RMS 0.312736 = 恢复 r = +0.459、落在族自身曲线下 16.9 %**,而 **A1(外场重)+11.5 % 落在曲线上**。`r ≥ 1/3` 那半满足、**剂量双侧那半没有**(A2 被 G-Q 排除:AR **32.39** = 控制的 3.4×)⇒ ★ **我不把判据里点名的 A2 换成 A3** —— 本季已栽四次判据缺陷,不能第五次靠改判据拿 PASS。★★★ **B2 明确不成立** ⇒ **等参 P2 的立项条件没被满足**,相反**分配被实测证明有空间** ⇒ 提高阶数继续推迟。★★★ **本轮最有价值的一条是 G-M 抓出来的**:我登记 A1 为"体网格重",逐壳**计数**测出它把 6 766 个单元搬到 r>2、**近体反而少 2 472 个** ⇒ 它测的是"**外场重 + 近体抽瘦**",与 phase two 已记的"把近体调粗使 LE 误差 +3.4 %"同向 —— **守卫没让我把一条名不副实的臂当成"体网格无用"的证据**。⇒ 真轴 = **近体单元占比**(r<1 m),**四个固定预算点在其上单调**(27.84 %→0.4096、31.50 %→0.3725、32.71 %→0.3493、39.80 %→0.3127);★ 而 A3 买到的正是 **C_medium 的 LE 带间距**(h_t 0.004988 vs 0.004979,差 0.2 %)= "用 coarse 的总量买到 medium 的 LE 带"是测出来的。★★★ **一个被判死的候选回来了**:"LE 误差不受 LE 分辨率控制"(GS2.1 第一步)**也是 flat-cap 腿**,而平截帽正是 LE 加密看起来没用的地方 ⇒ **"LE 分辨率已否证"撤到 RECORDED,候选表两轮内第二次更正**;★ 限定:A3 与那条臂差**两件事**(帽型 + 预算补偿),不能全归帽型 ⇒ 留一条 ~20 s 的可证伪检验。★ 计划外仪器检查 **PASS**:C_medium 复现已提交 HEAD medium 的 **LE RMS 0.242221 与 cl_p 0.272994 到 6 位**。★ 成本:**A3 用 C_medium 的 1/8.3 墙钟拿 46 % 增益**,但仍比其误差高 **29.1 %** ⇒ 重分配**没达到**那一级的精度。★ 限定:MID 上 **+8.2 % 变差**(A3 把 MID 换成 LE)、逐站不均(η0.65 −34.8 % 最好,**η0.90 只 −4.7 %** 仍最顽固)、解在动(A3 峰值移到**内侧**) | **B3(按字面);B2 明确不成立 ⇒ P2 立项条件未满足;G-C/G-B/G-M/G-S 全过,G-Q 排除 A2;kill 五条都不成立** | 5 腿全收敛 0/0 clamps;总 **~211 s**(闸 3 600);库零改动 |

| 23 | 2026-08-14 | [20260814-1400 预注册](20260814-1400-nearbody-axis-prereg.md) → [20260814-1600 判定](20260814-1600-nearbody-axis-verdict.md) | **N1** 事前确认近体占比这条轴 + **N2** flat-cap 假设 + **N3** 给「MID 换 LE」定价(三个问题分开登记、各自带判据)| ★ **G-R PASS**(D2 逐位复现上一轮控制腿 **0.372531 / 68 624 tets**)、**G-C PASS**、**G-Q 5/5 通过**(★ 上一轮预告的"D5 大概率被质量排除"**没有发生**,如实报);★ 又白捡一个仪器检查:池化组装出 **C_medium 0.132319** = M3 重测轮记录的 HEAD medium 全点 pooled 0.1323。★★ **N1c(按字面,非单调)**,而**非单调的形状本身是结论**:LE 上 RMS 逐段 **−15.1 / −15.1 / −1.1 / +1.5 %**,去掉单元数影响(vs 族曲线 n^−0.229)为 **+18.7 / 0.0 / −16.1 / −16.9 / −13.2 %** ⇒ **分配的优势在 `h_edge` ≈ 0.010–0.0075 处饱和,再细反而退**;跨度 33.8 %(判据要 ≥10 %)⇒ 是**非单调**那一支不是"跨度太小"。★ **D5 的回升不是预算假象**:它是唯一预算偏正的腿(+8.7 %),更多单元本该更好却更差 ⇒ 校正后回升只会更大。★★ ⇒ **上一轮的单调只在下降段成立**(那四点全在 D1→D4 一侧)—— 读数不算错但**外推不出去**,这正是"事后认轴必须事前确认"的价值。★ 下界 **0.312736 仍比 C_medium 高 29.1 %** ⇒ **分配把精度/成本曲线下移 ~17 %,但不替代单元数**。**N2c 不宣称**:round cap 上无预算约束的 LE-only 降幅 **+14.0 %** = flat 的 **1.7×**,但**低于我事前画的 15 % 线一个点** ⇒ ★★ **我不把线挪到 14 %** —— 事前阈值是**标定**不是保证(项目已在 EW forcing / taper r_c / descent10 / 墙钟 assert 上记过四次),挪一点就能把 RECORDED 变成 PASS。★★★ 但 N2 独立给出一条更有用的:它落在族曲线下 **10.2 %** ⇒ "**把单元搬向 LE 跑赢单元数**"在**第二个预算级别**(medium 448 k→542 k)复现;★ 限定:**D4 带预算补偿、N2 不带 ⇒ 不是同一个干预**,所以 −16.9 % 与 −10.2 % **不能**拿来比"优势随预算衰减"(第 5 问)。★ N2 是本阶段最好的 LE 数字(**0.208353**,此前 0.242221)。**N3**:**池化改善 −14.7 %** ⇒ LE 的收益**不是 band 之间搬家、是真精度**;★ **但 MID 上随近体占比系统性变差**(0.1225→0.1379,D2→D4 **+8.2 %**)、LE 下 **−47.0 %** ⇒ **真取舍,已定价**。★ G-S:**LE 越细峰值越往内侧搬**(D3/D4/D5 的全局 M_max 落在 η≤0.92 内),cl_p 在 0.2588–0.2659 间**非单调** ⇒ 不能把 RMS 变化全归给"同一个解更准" | **N1c(非单调=饱和);N2c(不宣称,不挪线);N3 RECORDED(真取舍);G-C/G-R/G-B/G-Q/G-M/G-S 全过;kill 五条都不成立** | 6 腿全收敛 0/0 clamps;最长 N2 224 s;库零改动 |

| 24 | 2026-08-14 | [20260814-1800 预注册](20260814-1800-selfconvergence-prereg.md) → [20260814-2000 判定](20260814-2000-selfconvergence-verdict.md) | **LE 带的残余误差是「离散误差」还是「模型误差」?**(自收敛/Cauchy 读数,零新参照)| 起因:能力边界写着**「换网格解决 M3」= 负**、S2 的靶 **LE 上 <0.08 = 测量为不可达**,而按我自己的 round-cap 曲线从最好的 0.208353 到 0.08 需 **64.9× 单元 = 35.2 M tets** ⇒ 继续打磨分配就是禁止项里的"根因未修时做精细化"。⇒ 改问 phase two 登记却没人答的那个:**为什么加密不收敛**。★★ 量选得干净:`e(h)=RMS[Cp_h−Cp_exp]` 与 `d_self=RMS[Cp_h−Cp_finer]` **同单位同横坐标**,判据是**比值** ⇒ 不需要物理阈值(避开本季三次绝对阈值缺陷);且**零新参照** —— 已查 `reference_data/` 里 M6 只有实验、**没有无粘参照**。★ 射线先修干净:`h_far` 显式 = 120·h_wall,因为默认 `min(2.5,120h)` 的**上限破坏自相似性**(P13/G13.3 已记:coarse 不在同一射线上、三点 Richardson **无效**)。**判定 = UNDEFINED,撞上自己的 kill 条 2/3**:**G-X 判 S0 与 S3 无效、S1 根本不收敛 ⇒ 射线相邻的有效对 = 0**。★★★ **我不放宽那条已触发的守卫** —— 诊断出越界只有 **0.0001–0.001 c**(每站 1–2 点,机理是**截面切割的首点必然 x>0** 而实验点可取到 x=0),但"越界很小"是**事实、不是重读判据的许可**(本季已两次拒绝同类动作)。★ 修的是**真缺陷**:配对用了收敛级的相邻项 ⇒ S1 死时会把 **S0→S2 当成相邻对**(因子 2 不是 √2),一条链混两种步长会让单调性与阶双双失真;修完**零求解重读,结论不变**。★★ 三条不依赖被判死读数的 RECORDED:**(1) `h_far` clamp 对 LE 带的效应首次量化** —— 同 h_wall 0.030,截断版 **+15.6 % 单元**却只读好 **−1.0 %** ⇒ 对 Richardson 致命(不同族)但对 LE 绝对值只 ~1 %,两句必须一起说;**(2) ★★ 可解性沿干净射线是非单调的** —— S1(h_wall 0.0424)|R| 1.26e-03 **0 lim/0 flr(停滞非钳制)**而上下两级都收敛 ⇒ 是"解在游走"的**求解器侧同伴**;**(3)** 粗端质量迅速恶化(AR 23.73/24.82/16.18/8.27/6.35)。★★★ **下一轮零求解**:**S0/S2/S4 本身就是干净的因子-2 射线且三级全收敛**、曲线已落盘(那层 cache 正为此加的),缺的只是**正确构造的公共点集**(三级都有效点的**交集**,算 d_self 前固定 —— 逐点丢会让不同级丢不同点,两个向量就不是同一个对象了,第 5 问)。★ **诚实约束写进下一轮**:√2 链的 d_self(0.214/0.250)**我已经看过** ⇒ 判据只能建在**尚未算过的因子-2 链**上,√2 那两个数只能作 RECORDED。★ 值得做的**理由而非结论**:那两个无效值 0.214/0.250 与 e 的 0.242–0.376 **量级相当**,若在公共点集上仍如此会落到 **N-DISC**(解没收敛)并**推翻**"换网格解决不了 M3";但**现在不能读** —— 被夹紧的点恰在被测的前缘带里,**污染方向未知**。★ 注册的风险清单**漏了"中间级死"**这一种,而它恰好发生 | **UNDEFINED(kill 条 2/3 成立);G-R 逐位 PASS、G-C PASS;G-X 按字面触发且不放宽** | 5 级、总求解 ~206 s;库零改动;曲线落盘使下一轮零求解 |

| 25 | 2026-08-15 | [20260814-2200 预注册](20260814-2200-common-pointset-prereg.md)(★ §0 含**事前写下的预测**)→ [20260815-0000 判定](20260815-0000-common-pointset-verdict.md) | **公共点集上的自收敛重读**(因子-2 链 S0/S2/S4,**零求解**)| ★★★ 注册先处理**诚实问题**:我已看过 √2 链的数 ⇒ **不能声称盲测**,所以**把预测(N-DISC)与它可以怎样被推翻(前缘夹紧点承载了差异)都写在前面**。**守卫全过**:**G-Z 零求解**、**G-R** S4 全点集 e **0.242221 逐位**、★★ **G-I 点集恒等式违背 0.00e+00**、**G-N** LE 上公共点 **36/38**(只丢 3 点,全在 x<0.003 c,与上一轮诊断一致)。★★ **判定 = N-DISC,两条件同时成立**:公共点集上 e = 0.541744/0.356840/**0.239153**(S0/S2/S4),而 **d_self 0.241693 → 0.292833(随加密增大 +21.2 %)**、**R = 1.2245 ≥ 1** ⇒ **LE 上表面的解根本没收敛**。★ **预测被证实,而那条推翻路径被直接测掉**(P2:去掉被夹点只改 **−9.7 %** ⇒ 上一轮读数**不是伪影**;细节:被丢的**那一个点自身差 0.70** 却只占 1/38 权重 ⇒ "个别点脏"与"读数被污染"是两件事)。★ 有利限定:S0 质量本就差(AR 23.73)会让 d(S0→S2) **偏大**,而实测**更细那一对反而更大** ⇒ 限定**加强**结论。★★★ **全带表是真收获 —— 三个带三种状态**:**LE 上是唯一 d_self 增大的带**;**LE 下 + MID 正常收敛**(d 缩小、e 缩小)⇒ **不能说"解不收敛",要说"LE 上表面不收敛"**(非收敛被**定位**);而 **TE 两面 d_self 缩小却 e 单调变差**(TE 上 0.0294→0.0392→**0.0716**,+143 %)⇒ **收敛到离实验更远处 = 模型误差的干净签名**,而它出现在 **TE 不在 LE**(★ n=15/5,只作 RECORDED —— TE 下 n=5 又是"样本薄到判据空洞"那一族)。★★★ **后果**:"**换网格解决不了 M3**"这个结论的**证据基础失效** —— 它是从**一个没在收敛的序列**做的四点外推;★ **不是**结论被证反,是**外推不成立**,靶子可达与否**现在未知**,下一个问题变成**「为什么 LE 上表面不收敛」**(与预算/分配/阶数都不同类)。★★ 另记:**G-Z 的第一版误报了自己** —— 它扫源码找求解入口却**匹配到自己的禁止词清单**,与项目已记两次的 `pgrep -f` / `ps\|grep` 匹配自己命令行**同族(自指守卫)**,第三次;修法 = 禁止词运行时拼接 | **N-DISC(预测被证实);G-Z/G-R/G-I/G-N 全过;kill 五条都不成立** | **零求解**(读落盘曲线);库零改动 |

| 26 | 2026-08-15 | [20260815-0200 预注册](20260815-0200-nonconvergence-discriminator-prereg.md) → [20260815-0400 判定](20260815-0400-nonconvergence-discriminator-verdict.md) | **判别式:LE 上表面为什么不收敛?**(四条候选 (a) 前缘峰 / (b) 亚单元激波 / (c) σ 冻结 / (d) 尖端残余,互斥可辨)| 原理:`d_self` 一直只被当成 RMS,但它是**逐点**量 ⇒ **它的平方质量在弦向/展向的分布就是四条候选的指纹**;每区打 **集中度 =(质量占比)/(点占比)**,零假设 **1.0**(与"偏置 vs 散布"那轮的 1/n 比较同一手法)。**G-R PASS**(S4 全点集 e **0.242221 逐位**)、**G-I PASS**(弦向分区质量 **1.000000000000**)。★★★ **(c) σ 干净否证**:d_self σ ON **0.292833** vs σ OFF **0.292897**、**比值 1.000**(判据要 ≤0.5),σ OFF 三级**全收敛**(S4 \|R\| 5.50e-15)⇒ **非收敛不经过 σ**;★ 这是**强**否证,因为 σ 并非惰性(各级 σ_min 在 0.98–0.74 间移动)却完全不改 d_self。★★★ **(b) 足迹检验在带内不适用**:声速线穿越在 **x/c 0.371–0.649**(随展向前扫),带止于 0.15 ⇒ 声速邻域 **0 点**;★ **但激波自己还在动**(η0.90 **0.438→0.371**,移 0.067 c)⇒ **不得**读成"(b) 与 LE 亏空无关",它只是**没留下带内指纹**(该限定是**事前登记**的,现在触发)。★★ **仪器更正自报**:第一版声速定位窗口设在 x_hi=0.30,五站**全返回 ~0.29 = 窗口边缘**,长得像"整齐的常数";已修(x_hi=1.0),上述为修后值 —— 与"**旋钮的名字不是它的作用域**"同族:**窗口的边不是测量值**。★★★ **新事实改写候选 (a)**:超声速区**从 x/c 0.004–0.011 起** ⇒ **LE 带几乎整段在超声速泡内**,而 d² 的 **92.6 %** 就在 **x<0.05**(紧贴泡的前脚)⇒ (a) 必须从"前缘吸力峰"**改称「超声速泡前段不稳定」** —— 不是措辞:亚声速极值的修法是分辨率/恢复,而**泡前脚是一条声速线**,求解器在那里切换人工密度并做上风供体选择,**修法完全不同**。★★ **判据缺陷 #5(自查,与本季第 4 号同族)**:`CONC_max = 1/点占比`,而 LE 前端区占 **66.7 %** 的点 ⇒ **CONC_max = 1.50 < 我事前要求的 2.0** ⇒ **按构造不可达**(实测 1.39 = **可达上限的 92.6 %**);⇒ **C-MIX 这个字面判定里有一部分是我自己阈值造成的**,★ 我**不**因此重读成 C-A,而是披露并写下一般规则:**份额型集中度判据必须先看该区自己的点占比**。★ RECORDED 交叉表(**事后**,非预注册通过)把位置缩到**一格**:**x<0.05 且 η≥0.80 —— 27.8 % 点、60.8 % 质量、CONC 2.19**(其余三格 0.82/0.34/0.14)⇒ **边缘分布被稀释**正因每个边缘都混入亏空格 | **C-MIX(按字面);(c) 否证;(b) 足迹 N/A 且不得读成否证;(a) 改写为「超声速泡前段」;判据缺陷 #5 披露** | 臂 1 零求解 + 臂 2 三级 σ OFF ~154 s;库零改动 |

| 27 | 2026-08-15 | [20260815-0600 预注册](20260815-0600-tipstations-mechanism-prereg.md) → [20260815-0800 判定](20260815-0800-tipstations-mechanism-verdict.md) | **Q1** 用没看过的 **η0.96/0.99** 两站做样本外确认 + **Q2** 分辨「泡前脚在动」vs「峰值幅度在动」(同一批重解) | ★★★ **G-R 先触发,而它抓到的是我自己的点集不一致**:求解**逐位相同**(\|R\| 1.40e-14/1.76e-11/7.33e-15,且三站逐位一致),差的两站**正是 S0 有越界点的 η0.65/η0.80** —— 基线在**三级公共点集**上算,我这轮用了全点(**第 5 问又一次**);★★ 这是**仪器缺陷不是判据**,修点集后**五站全部逐位 PASS**(比任何容差都强),并改为**报出每站被丢点数**而非藏在布尔里。★★ **Q1 = O-REFUTE**:d_self 在 **η0.90 见顶后下降**(0.373187 → **0.355123**(0.96)→ **0.268246**(0.99))⇒ **向外递增不延续** ⇒ **我上一轮"x<0.05 且 η≥0.80 那一格"的读数被削弱** —— 那一格之所以质量最高,是因为 **η0.90 幅度最大且点最多(10 点)**,不是"外侧"本身。★★★ **但指纹在七站全部复现**:前端(x<0.05)质量占比 **87.9 %–96.6 %**、CONC 1.27–1.46(占各自可达上限 **87–97 %**)⇒ **前缘集中是全展向的** ⇒ 结论改写为「**整条展向的前缘现象,幅度在 η0.90 见顶**」,而"见顶"本身是新信息(它与 taper 作用带起点 η>0.95 **相邻**)。**Q2 = M-PEAK 按字面成立**(峰值 3/5、前脚 2/5,判据 ≥3),★★ 但**两个估计都单边,必须一起说**:前脚在外侧**高估 4.70/6.12/6.91×**(斜率 127–164 处一维估计根本不适用 —— 不是"前脚无关",是**估计太粗**),峰值**处处低估**(0.24–0.88,**全 ≤1**)⇒ **峰值深度是可辨认的主要成分但解释不了全部**(内侧解释得多、外侧少)。★★ RECORDED 硬事实:**超声速泡前脚随加密一律向前推进**(0.0133→0.0112、0.0136→0.0060、0.0251→0.0053、0.0231→0.0060、0.0198→0.0041,最大 **0.020 c**)、**前脚斜率 52→164** ⇒ **被测带的上游边界本身在移动、并且在变陡**。★ 本轮把 **φ 落盘**(`phi_S*.npz`),下一轮不必再付重解 | **Q1 = O-REFUTE(削弱我自己上一轮);Q2 = M-PEAK(擦线,两估计单边);G-R 触发→抓到自身缺陷→修后逐位 PASS;G-X/G-C/G-φ 全过** | 三级重解 ~168 s;库零改动 |

| 28 | 2026-08-15 | [20260815-1000 预注册](20260815-1000-shape-and-taper-prereg.md) → [20260815-1200 判定](20260815-1200-shape-and-taper-verdict.md) | **A** 峰值深度漏掉的那 24–88 % 是什么(零求解)+ **B**「幅度在 η0.90 见顶」是不是 taper 造成的 | **G-R(A) 七站逐位 PASS**、**G-I PASS(3.86e-16)**。★★ **A = A-SHAPE**:`f_bias ≤0.3 且 >1/n` 在 **3/5** 站、`≥0.7` 只 **1/5** ⇒ **RMS 由「点间形状变化」主导**;而 **`|bias|/|ΔCp_min|` 在 3/5 站落在 [0.5,2]** ⇒ **水平位移分量的大小正是峰值深度量到的那个** ⇒ ★★ **那 24–88 % 的缺口不是新机制,而是「峰值深度按定义只能量到一半」**。★ **bias 七站全为正** ⇒ 加密**一致地加深 LE 吸力**(方向明确);★ 但**各站配比不同**:η0.44 的 f_bias **0.016 ≤ 1/n = 0.125**(连水平位移都测不出、纯形状),而 η0.80/η0.99 反而**水平主导**(0.786/0.789)。★★ **B = B-GEOM**:`R_d = d_self(η0.90)/d_self(η0.20)` across r_c 0.025→0.10 只动 **+0.1 %**(判据 ≤3 %)、**argmax 三档都钉在 η0.90**,而**同一扫描 cl_p(S4) 动 −3.1 %**(0.275261→0.272994→0.266656)⇒ **旋钮活、剖面不动** ⇒ **η0.90 见顶是机翼自身的展向结构,不是 taper 的印记**;★ 那三个 cl_p 与 **W2 表逐位相同** = 一次**计划外的跨轮仪器核对,通过**;★ R_d 非单调但幅度只 0.1 %,记为数值抖动。★★★ **三轮第一次拼成自洽的图(解释,非新测量)**:被测带位于**超声速泡前段**,而**泡前脚随加密前移 ≤0.020 c 并变陡(斜率 52→164)** —— 一条**移动中的声速界面会让曲线变形而不是平移**,这同时解释了**刚性平移估计高估 4.7–6.9×**与**水平位移估计低估到 0.24–0.88** ⇒ 靶子是「**前几个百分点弦长内、声速界面的亚单元表示**」:**不是** LE 分辨率(分配已测饱和)、**不是**激波(不在带内)、**不是** σ(已否证)、**不是**尖端(两次否证);★ 它与能力边界 §3 首位的「**亚单元激波定位**」**同族**(声速界面的亚单元表示),只是位置在**泡的前脚** —— ★ 这是**解释性合流,不是判据通过**,立项仍需一轮事前登记的确认 | **A-SHAPE + B-GEOM;G-R/G-I/G-N/G-C/G-X 全过 + 一次计划外跨轮核对;kill 五条都不成立** | A 零求解;B 四条新腿 ~421 s(6/6 腿全收敛);库零改动 |

| 29 | 2026-08-15 | [20260815-1400 预注册](20260815-1400-alignment-and-spanwise-prereg.md) → [20260815-1600 判定](20260815-1600-alignment-and-spanwise-verdict.md) | **F** 变化对齐的是「前脚位移」还是「峰值位置」+ **G** η0.90 见顶的展向来源(**两部分零求解**)| ★ 注册时 Bash 分类器**瞬时不可用**,于是我**不等它**,把**可构造性检查写进守卫与 kill 条**(设计同时覆盖两种采样情形)—— 比先偷看元数据更干净。★★ 采样问题**事前**解决:前脚扫过区间只 0.002–0.020 c 宽而每站实验点仅 5–10 个 ⇒ **主形式改用计算曲线的密网格(301 点)**、实验点降为一致性检查,并明写**两者是不同点集、只比位置不比数值**;实测**辅形式果然不可用**(窗口 A 内实验点 **0/1/2/2/2**)。**G-R 七站逐位 PASS**;★★ **G-W(两窗口强制等宽)PASS 且重叠 ≤0.02** ⇒ 后面的"都不中"**不是分不开造成的**。★★ **F = F-NEITHER**:in A **2/5**、in B **1/5**(判据要 ≥3)⇒ **两个候选都不成立**;而实测位置很集中:`x_argmax` = **0.0040/0.0045/0.0347/0.0084/0.0042**,**四站在 x ≈ 0.004–0.008 c**,`x_cen` 0.023–0.047 ⇒ **一个尖锐前端极大 + 向后长尾**。★★★ **RECORDED(非判据通过)**:3/5 站的极大落在 **`x_foot(S4)`(最细一级声速前脚)± 0.0024 c** 内,η0.90 **几乎逐点重合**(0.00418 vs 0.00405)—— ★ **我不事后换成"x_foot(S4) 的邻域"窗口**去凑 3/5(那正是本会话已拒绝三次的动作:A2→A3、15 %→14 %、放宽 G-X),留给一轮事前登记的确认。★ 如实说:这**部分削弱**我上一轮的合成 —— 方向(声速界面)仍指同处,但**"扫过的区间"这个具体形式是错的**。★★ **G = G-MULTI 按字面**(`|ΔCp_min|` argmax 0.90、`|dΓ/dz|` argmax 0.96 都在 ±0.10 容差内并回落),★★★ **但实质更接近 G-NONE**:`|ΔCp_min|` 与 d_self **是同一族的量**(都是同一对网格的 Cp 逐级差)⇒ 用它"解释" d_self **近乎循环**;而**唯一独立**的 `|dΓ/dz|` **形状完全不像**(0.01/0.03/0.09/0.15/0.24/**1.00**/0.34 vs d_self 的 0.35…1.00…0.72),只靠 argmax 落进容差才"匹配" ⇒ **Γ、Γ/c、局部弦长、x_foot、Cp_min 全部测负** ⇒ **B-GEOM 说的"机翼自身的展向结构"目前没有任何具体几何量支撑**,它成了一个**待解释的展向结构**。★ 判定归判定、实质归实质,两者都写 | **F-NEITHER;G-MULTI(按字面)而实质近 G-NONE;G-Z/G-R/G-GRID/G-W/G-N 全过;kill 五条都不成立** | **零求解**(读落盘曲线 + γ 170 站);库零改动 |

| 30 | 2026-08-15 | [20260815-1800 预注册](20260815-1800-nearsingular-partition-prereg.md) → [20260815-2000 判定](20260815-2000-nearsingular-partition-verdict.md) | **判决性检验:非收敛是否只局限在前缘近奇异区?**(零求解;判据**复用**自收敛轮已登记的 `R ≤1/3 / ≥1`,不引入新阈值)| 被检验的解释来自已提交数字:最细一级 η0.90 处**一个壁面单元 ≈ 0.0102 c**、**声速前脚 0.00405 c(在第一个单元内)**、**最上游被打分实验点 0.00057 c(距前缘 1/18 个单元)** ⇒ P1 场在一个单元里表示不了"滞止→强超声速",加密只把坏区**搬得更前**。★★ 构造问题事前解决:分割点取 **`x_foot(S2)`(较粗级前脚)** ⇒ **两级共用同一分割点 ⇒ 同一点集**(与"公共点集"同一逻辑);采样可行性事前算清 ⇒ **判定建立在 5 站池化**(n_inner 15 / n_outer 21,门槛 10)。**G-R 七站逐位 PASS**、★★ **G-I 分区质量守恒 1.44e-16 PASS**。★★ **判定 = P-MIX**:`R_inner = 1.4327`(未收敛 ✓)但 `R_outer = **0.9112**`(要 ≤1/3 ✗)⇒ **不宣称方向**。★ **方向确实对且要说清有多强**:去掉内区使 **d_self 从 0.3927 减半到 0.1919**、收敛率 e4/e2 从 0.743 改善到 **0.606**、**内区以 41.7 % 的点占 74.9 % 的 d² 质量**(CONC 1.80 = 可达上限的 75 %);★★ **但外区没进收敛带** ⇒ **「非收敛 = 前缘近奇异」不完整,大约只覆盖一半**,而未解释的那一半**偏外侧**(外区 R 逐站 0.347/0.770/**1.509**/0.813/0.975)。★ 敏感性(2 单元宽)只报不判:内 1.470 / 外 **0.601** ⇒ 与"坏区比声速前脚更宽"一致,但**也没进带**。★★★ **kill 条 4 触发,而查下去抓到的是我自己**:对照组 **LE 下外区 R = 1.72** 也未收敛 ⇒ 按注册先查实现 —— **G-I 精确 1.44e-16**,且 **LE 下的全带 R 本来就是 1.74**(两个数都取自已提交表)⇒ **分区实现干净,是我把 LE 下/MID 当成「R≤1/3 的对照组」选错了**。★★★ **根因是判据层面的第 5 问**:两轮前"LE 下 + MID **正常收敛**"依据的是"**d_self 在两对之间缩小**",而本轮注册把同一句话当成了"**R ≤ 1/3**" —— **两个不同判据共用一句中文**;我一直在数字上问"这两个数是同一个东西吗",**这次该问的是"这两个判据是同一个东西吗"**。★ 由此测出一条之前没被说出来的事实:**整个数据集里没有任何 band 的 R ≤ 1/3**(最好是 MID 上 0.638)⇒ 前几轮"正常收敛"的措辞须读作"**相邻级差在缩小**",不可读作"已收敛"(勘误已登记,逐处标注随本轮提交) | **P-MIX;kill 条 4 成立并已执行(工具干净、对照组选错);G-Z/G-R/G-I/G-N 全过** | **零求解**;库零改动 |

| 31 | 2026-08-16 | [20260815-2200 预注册](20260815-2200-s4-premise-check-prereg.md) → [20260816-0000 判定](20260816-0000-s4-premise-check-verdict.md) | **按 ③ 转向产品指标**:在 HEAD 上核实 **S4「做减法」的两条立项前提**(P1 BL 贵 9×、P2 横流没用上)| ★★ **入口检查改了靶子**:① **M3a 已被分类为"无粘不可达"**(靶 <0.05、实测 0.1007、**地板在 MID 79%/TE**,而"LE 带收敛到零 阶 0.709")⇒ 归属 2026-08-02 已改 **S4/粘性**;★★★ 我**差点报"这与我 LE 十轮矛盾"** —— **不矛盾**:那是 **h→0 的地板**,我量的是**当下预算**(LE 现在占 70 % 但外推趋零、MID 现在小但有地板)⇒ **第 5 问,这次在宣称前抓住了**,并给 LE 线一个更准的收口句(**它不改变 M3a 的归属**);② **S4 是已登记路线**(GS4.1–GS4.5);③ ★★ **HEAD 上没有翼身粘性 case 入口** ⇒ **M4 卡在不存在的机器上**,"M4 可达性探查"会主要测到我写代码的速度 ⇒ 改为核实 S4 前提。★ 近乎免费:**φ 落盘 ⇒ 无粘零求解**,`n_outer_max=1` ⇒ **只一次 IBL**(两条前提都是单次求解量)。**守卫**:G-φ 打印缓存路径/形状、G-P 无粘墙钟取**本阶段自己的 CSV**(不许用第一阶段 115 s)、★ **G-U 横流定义从 V5 桥源码读出并打印**。**实测**:S2(59 359 tets / 表面 3 557 节点 / BL 未知量 **21 342**)**一次 IBL 133.5 s** vs 无粘 **13.4 s** ⇒ **P1 = 9.96×**(带 [4,20] ✓);**P2 = max\|B\|/max\|A\| = 0.1471**(带 ≤0.15 ✓)⇒ **字面 Q-BOTH**。★ `converged False` 是构造性的(n_outer_max=1,松循环一轮不可能收敛),非 IBL 失败。★★★ **但三条实质削弱**:**(1) 判据缺陷 #7(新形状)** —— P2 只以 **2 %** 余量过线,而它的带来自**第一阶段 0.072,那是 M0.5 上测的**,我这轮在 **M0.8395** ⇒ **定带用的参考数没过第 5 问**(前六个缺陷都在"被比较的量"上,这个在"**带的出处**"上)⇒ **不挪带**,但**"P2 前提复现"这句话不能说**,只能说"M3 工况上 0.147";**(2) P1 是级别的性质不是代码的性质** —— S2 **9.96×** 而 S0 **25.82×**(带外),机理:无粘 ~ 体单元(超线性)、IBL ~ 表面节点 ⇒ 加密时比值下降,向生产级(S4,无粘 153 s)外推**很可能掉到 ~3× = 带的下沿之外** ⇒ **S4 的成本动机在生产级未被确立**(★ 推断非测量,且**不在本轮补测**——看到趋势后加腿是范围蔓延);**(3) "横流没用上"只对两个最大值之比成立** —— 逐点中位仅 **0.0084**,但**逐点 max 1.5843**(S0 更是 17.97)⇒ 个别节点横流比流向分量还大,而 `max\|B\|/max\|A\|` 把两个不同节点的最大值相除、**天然看不到**这点 ⇒ **GS4.1 的二维条带必须交代那些节点** | **Q-BOTH(按字面);三条削弱已披露;判据缺陷 #7;G-φ/G-P/G-U 全过;kill 五条都不成立** | 无粘零求解;两级 IBL 各一次共 ~157 s;库零改动 |

| 32 | 2026-08-16 | [20260816-0200 预注册](20260816-0200-s4-cost-trend-prereg.md) → [20260816-0400 判定](20260816-0400-s4-cost-trend-verdict.md) | **S4 成本动机在生产级是否成立**:把 P1 从单点值改成**级别趋势**(S2+S4 **同进程**,S0 取上一轮 CSV)| ★ 事前写下预测(**不假装盲测**):"P1 会落到 ~3× ⇒ **T-DROP**",并同时写下它可以怎样被推翻。★★ 关键设计:**墙钟不能用作复现守卫**(项目已记 5.4× 同物理差)⇒ **G-R 改建在非计时量上**:S2 表面节点 **3557**、P2 **0.1471** —— 两者**精确复现,PASS**。**实测**:S4(生产,448 197 tets / 表面 **13 764** 节点 / BL 未知量 **82 584**)**一次 IBL 895.6 s** vs 无粘 150.6 s ⇒ **P1 = 5.95×**;三级 P1 = **25.82 / 9.98 / 5.95** 单调下降。★★ **我的预测被推翻**:5.95 **仍在带 [4,20] 内** ⇒ 若判定则是 **T-HOLD**(方向对、幅度错)。★★ **派生标度指数证实了我上一轮的机理**:IBL vs BL 未知量 **≈1.41**、无粘 vs 体单元 **≈1.20**,而表面 ~ 体积^⅔ ⇒ IBL~体积^0.94、无粘~体积^1.20 ⇒ **P1 ~ 体积^(−0.26)**,必然随加密下降;外推再加一级给 **P1 ≈ 3.5** ⇒ **下一级会穿过带的下沿**(推断)。★★★ **本轮最强的一条:P2 在生产级塌了** —— max\|B\|/max\|A\| **0.1546 / 0.1471 / 0.0186**(8× 下降),**逐点 max 17.97 / 1.58 / 0.0315**(**570× 下降**)⇒ **上一轮"逐点横流 >1、GS4.1 必须交代那些节点"那条削弱撤回(粗网格伪影)**;且生产级 0.0186 比第一阶段声称的(M0.5 上 0.072)**小 3.9 倍** ⇒ "为横流付的复杂度没用上"**在生产工况+生产分辨率上更强**(★ 限定:单次 IBL、跨级别比较)。★★ **而 G-L 触发**:load average **0.22→3.48(15×)** ⇒ 按 kill 条 4 **趋势按字面不判**;★★★ 查下去是**守卫缺陷 #8**:`load average` **包含本进程** ⇒ **任何计算密集轮次都必然触发它,它量的是自己的效应** = **自指族第三次**(`pgrep -f` 匹配自己 → G-Z 匹配自己的禁止词清单 → G-L 量自己的负载)⇒ 常设检查新增一条:**守卫必须问"我的存在会不会改变我要量的东西"**;★ 但我**不据此绕过 kill 条**(本会话已四次拒绝同类动作),趋势仍不判、数字作 RECORDED。★ 经验支撑:**S2 的 IBL 在两次独立运行里 133.5 vs 133.7 s(差 0.15 %)** | **趋势按字面不判(G-L);RECORDED 推翻我的预测(T-HOLD 而非 T-DROP);P2 生产级塌陷 ⇒ 撤回上一轮第三条削弱;守卫缺陷 #8;G-R 非计时 PASS** | S2+S4 同进程 IBL 共 ~17 min;无粘零求解;库零改动 |

## 阶段进度

| 任务 | 内容 | 状态 |
|---|---|---|
| ① | **删除 level-set** | **✓ 完成 2026-08-11**。删完只剩一条尾迹路线:`mesh/wake_cut.py` + `constraints/te_pressure.py` + `solve/newton.py` / `picard.py`,配 `tip_taper`(带 **−1.3% cl** 模型偏差) |
| ② | **六面体贴体网格** | ★★ **已收口 2026-08-11 —— 裁决 D6:停止求解器侧投入,不开 (B)**。路线 (A) 的 kill criterion 第 1 条成立 —— Q1(供体确定性)与 Q2(壁面精度)都不 PASS。**交付物**:`pyfp3d/meshgen/structured.py`(圆柱 O 型 + 翼型混合层块,**G0 逐位单变量**)、两个网格族、四项质量自检。**净结论**:买到的是**实验可控性**,不是求解器改善。**(B) 现无经测量的立项理由**(壁面精度对 (B) 同样被否;供体确定性经测不是瓶颈;只剩未测的 DOF 效率)。待使用者裁决 (a)/(b)/(c)。原文:未开始。★ **开工那一轮先写 kill criterion**(原则 8,不许事后补);依据在能力边界 §7,逐条标了测量 vs 推断;⚠ 其中 S6 触发条件**按字面没被满足**(M3a 达不到的原因归到无粘模型而非网格),真正支撑"转"的是另外两条 |
| ③ | 其余问题 | ◐ **进行中**。第一项(**加密为什么让鲁棒性变差**)✓ 2026-08-11:★★★ 答案在上游 —— **M0.80/α1.25 上离散解不唯一**,只换种子即 混合族上 cl 散 **10.7× (b)(c) 的 3% 判据**;★ 而 M1 自己的族上是**收敛障碍**(medium 8 线程 0/3),不是多解(见勘误),⇒ **解选择被钉住之前 M1 的 (b)(c) 在该工况上不可测量**;包线内 M0.72 唯一(0.0002 c)。交付:局部加密窗口(守卫 PASS,窗口外代价 12.4% 已量化登记)+ `tests/test_meshgen_structured.py`(补掉 `structured.py` 在 `tests/` 零覆盖的债)。第二项(**σ 是不是选择算子** + M1 门诊断)✓ 2026-08-12:字面 **S3**,而 RECORDED 读数**方向相反** —— **σ OFF 在两族上都唯一(x_shock 散布 0.0000),多解随 σ ON 一起出现**;取舍已量化(σ OFF 唯一但在参考带外)。M1 门加了 `--third-seed` 诊断(只记录、不投票,G1 逐位 PASS)。★ 并**发现并修好上一轮的散布未过滤 `converged`** 缺陷。第三项(**σ 强度扫描**,使用者定的 1→3 里的 1)✓ 2026-08-12:字面 **T2(阈值型)**,而 ★★ 我又抓到自己判据的**第二个缺陷** —— 跨 θ 比散布却不固定收敛种子集;平衡面板下**混合族单调 0.01 %→30.59 %、非结构族仍是峰** ⇒ **两族剂量形状不同**(RECORDED,未注册口径,不改判)。机理:**σ 变强 = 让起点分叉 + 让部分起点失收敛**,两件相反方向的事被一个散布数混在一起。临时仪器 `sigma_scale` 按注册的到期条件**已删除**(使用者顺序里没有 1b)。第四项(**收费次数**,使用者新定方向)✓ 2026-08-12:**D2 —— 不是次数(实测 1–2)而是幅度**;★★★ 机理读出:**σ 在第 8 步被永久冻住,而 m1_max 轨迹在振荡,冻在哪个相位由种子决定**(seed 0/5 冻在尖峰 1.95/1.86,seed 12 冻在安定的 1.25)⇒ 尾部解的固定系统不同 ⇒ cl 0.26 vs 0.35。★★ 靶子来自代码自己的理由:注释论证的是**离散集合**不可解,而实现把**集合和幅度一起冻**了。★ 我撤回了上一轮末尾「N≥7 是算式逼出来的」那句(跨量比较)。★★ **原定的第 3 项(冻结选择 churn)与本项汇流** —— 同一个对象,不必当两项。第五项(**σ 冻结**:为什么有极限环 / 报警)Part 1+2 ✓ 2026-08-12,**Part 3 未做**:★★ P-B **复现**(周期 2/3/4/5,Δσ 钉成常数,**9 腿全不收敛 ⇒ 冻结是承重结构**);★★★ 机理定量钉住 —— **集合判定是硬开关**,一个声速线上的单元进出时 σ 跳 `1−σ_RH(M1)`,反解得 M₁ 1.268/1.228 **正是钉住的 Δσ**;而**同一内核里人工密度的开关早就是软斜坡**(`m_crit`)⇒ 一处内部不一致 + 一个**新候选:把集合判定做软**。报警已交付(纯读判、G-P2 逐位 PASS、触发率先测:τ=1e-3 上 100% ⇒ 现在升级成拒绝会全红,1e-1 上只挑最坏的 36%)。**Part 3 ✓ 2026-08-12**(软化集合判定):F3 逐位 PASS、F1+F2 在 eps=0.05 上两族 PASS(混合族散布 **32.11 %→4.28 %**),★★★ **但归因被混淆**(σ_min 0.74→0.97,软化≈削弱,答案落在等熵答案旁边)、**F5 失败**(激波被推出 M1 带)、eps 敏感性**非单调**(最坏 41 %)⇒ **靶子对、这一版参数化不可采纳**。幅度守恒的软化 ✓ 2026-08-12:**binding J3** —— 而原因是**我的判据把输出当输入**(σ_min 是收敛态的输出,目标不可能稳定命中);★★ 但数据交出更好的归因变量:**捕获到的 m1_max 的跨种子散布**与 cl 散布**单调对应**(0.705→32 %、0.054→4.3 %、0.344→24 %)⇒ 软化有效是因为**让被冻住的状态不再依赖种子**。捕获选择 ✓ 2026-08-12:**K3 —— 有害,kill 条 3 成立、路线判死**(binding 族 cl 散布 1.21 %→7.47 % 且丢一个种子);★★★ 并**否掉了上一轮那个「更好的归因变量」**—— m1_max 均匀性每个臂都改善而 cl 散布不跟 ⇒ **相关不是杠杆**。⇒ **σ 这条线收口**:七轮的取舍陈述已写进能力边界(**唯一性 vs 修正强度是取舍,不是可单独修的缺陷**;包线内 M0.72 完全唯一)。σ 线**已收口**(取舍陈述进能力边界);使用者裁决:**选项 3(换离散化)登记为未来方向、未开启**,**先做选项 1** —— 回到 LE 那 70 % 与外推未测。★ 而**分诊 ① 发现那 70 % 已过期**(出处 CSV 早于三次配方变更;HEAD 重跑给 LE RMS 0.2953 而非 0.2361)⇒ **先重测再谈分诊**,勘误已推到 5 处。★ **重测已完成 2026-08-13**:补上生产 taper 后 medium **收敛**(|R| 7.33e-15),**新预算 LE 上 70.35 % / MID 上 21.05 %**(旧的 69.6 %/20.7 % **退役**,两者不可比);**LE 主导没有建在沙上** ⇒ 那条线可以继续。★★★ **分诊 ① 重做 = T1**(三种子 cl 散布 **0.0011 %**、LE 上 RMS **0.0025 %**,全部机器精度收敛)⇒ **α=0 迁移不必做** —— 迁移的唯一理由是躲多解,而这个配置上没有多解要躲,于是保住了工况的物理相关性。★ 禁令遵守:这**不叫**「M6 没有多解」(**Q-B 仍开**)。★ **LE 机理已查一轮 2026-08-13**:**H2 —— 位置(弦向错位)解释不了亏空**(最优 δ=0,RMS 停在 0.2422;而陡度前提成立:0.001 c ≈ 0.019 Cp)⇒ 亏空是**真实振幅误差**,下一嫌疑是 **G1.6 家族**(而 P11 已把曲面元测负 ⇒ 很硬)。★★ 并交出新线索:**LE RMS 向翼尖增大、在 η=0.90 跳升 43 %** ⇒ 指向**唯一没被干净否掉的尖端假设**。★★ **尖端闸门已重设计并执行 2026-08-13 = W2:尖端不是驱动者**(ΔR = 0.0027 而同扫描 cl_p 动 3.13 % ⇒ 旋钮生效);并零成本否掉**局部弦长**与**实验点陡度**两条(★ 后者方向相反:η0.90 在曲线**最平**处却最差 ⇒ **Cp 量级本身错**,位置在那里原则上解释不了)。⇒ **候选只剩 G1.6 家族**(而 P11 已把曲面元测负)= 很硬的结论,但明写排除法不排除未想到的东西。★★ **分解已做 2026-08-14 = O2 并收紧**:**LE 上表面 5 站 f_bias 全部 ≤ 1/n ⇒ 检测不到任何偏置**(纯散布)⇒ 指向 **G1.6 家族**;而**下表面另有真实的系统性小正偏置**(MID/下 5/5 站,+0.018…+0.025)= 载荷/环量签名,但**只占预算 6.2 %**。★ 并抓到一处空洞(**TE/下 n=1 ⇒ f_bias≡1**),已把 1/n 判读固化进脚本。★★★ **(b) 已执行 2026-08-14 = R2(等参 P2 保持关闭,记为「已评估-未开」并带立项条件)** —— 而那一轮的真发现不是 P2:**一条被我记错成本(4 011 s)、推迟了四轮的注册义务其实早已付清**,`tip_allscales.csv` 里有**四条**全尺度腿,**LE 上 RMS 0.3899→0.2704→0.2358→0.1758 四级单调、共 −54.9 % ★★★ **勘误 2026-08-14 addendum #1:这四条腿是 `tip_cap="flat"`**(`run_tip_allscales.py:49` 导入的 `run_le_response.build` 里 `BASE` 写死 flat),而生产族是 **round** ⇒ 按 CLAUDE.md 的标准规则"**平截帽上任何基于加密的论断前提为假**"(P13/G13.3:平截帽在加密下发散,LE 带阶 **0.37 flat / 0.87 round**),**此读数降级为 flat-cap 专属,生产行为 UNMEASURED**。见 [addendum #1](20260814-1000-fixed-budget-addendum1.md)。**,GS2.1 §7.2 那条事前可证伪预测**被满足**(登记的腿只需 ~20 s,实测 **7.2 s**)。⇒ ★★ **上一行「候选只剩 G1.6」那句有洞**:漏了**「按比例的全尺度加密」**,它是唯一被测到**能降低对实验误差**的杠杆;★ 而**不能**说成「体网格调细」—— M6 自己的单旋钮腿对误差只动 −1.4 %…+3.4 %(它们主导的是 Cp 的**位移**)。★ 代价:−54.9 % 要 **25.2× 单元 / 549× 墙钟** ⇒ 对 2 CPU·min **不是产品答案**。★★★ **已执行 2026-08-14 = B3(按字面),而方向很强**:固定预算下**把单元搬进近体/LE 恢复 r = 0.459 的级差增益**(落在族自身误差-单元数曲线下 **16.9 %**、墙钟只需 C_medium 的 **1/8.3**),搬向外场则 **+11.5 %** 更差;**四个点在近体单元占比这个轴上单调**。⇒ **B2 明确不成立 ⇒ 等参 P2 的立项条件未满足,提高阶数继续推迟。**★★ 而 **addendum #1** 查出那四条 −54.9 % 的腿是 **flat cap** ⇒ 上一轮头条降级;**同一个 flat-cap 问题也污染了「LE 误差不受 LE 分辨率控制」** ⇒ **该候选撤到 RECORDED**(两轮内第二次更正)。★★ **三条已执行 2026-08-14**:(1) **N1c —— 轴非单调、分配会饱和**(优势在 `h_edge` ≈ 0.010–0.0075 处见底,再细退回;vs 曲线 +18.7/0/−16.1/−16.9/−13.2 %)⇒ **上一轮的单调只在下降段成立**,而下界 **0.3127 仍比 medium 高 29.1 %** ⇒ **分配下移曲线 ~17 %,不替代单元数**;(2) **N2c 不宣称**(+14.0 % vs 事前 15 % 线,★ 不挪线),但它独立地落在族曲线下 **10.2 %** ⇒ 分配效应在**第二个预算级别**复现;(3) **N3:池化改善 14.7 % ⇒ 真精度**,★ **代价是 MID 上 +8…+13 %**。★★★ **方向已改(使用者裁决 2026-08-14)**:不再打磨分配 —— 能力边界写着**「换网格解决 M3」= 负**、靶子 **<0.08 measured 不可达**,而从最好的 0.208353 到 0.08 需 **64.9× 单元**。改问 phase two 登记未答的**「为什么加密不收敛」**:自收敛(Cauchy)读数 **= UNDEFINED,撞自己的 kill 条**(G-X 判两级无效 + **S1 中间级不收敛** ⇒ 0 个有效相邻对),★ **不放宽已触发的守卫**。交出三条 RECORDED:**h_far clamp 对 LE 带首次量化(+15.6 % 单元、只好 1.0 %)**、**可解性沿干净射线非单调**(S1 停滞 0 clamps 而上下两级都收敛)、粗端质量恶化。★★★ **零求解重读已做 2026-08-15 = N-DISC**(两条件同时成立:**d_self 随加密增大 +21.2 %**、**R = 1.2245**)⇒ **LE 上表面的解根本没收敛**;事前预测被证实,而那条推翻路径被测掉(去掉夹紧点只改 −9.7 %)。★★★ 全带表把非收敛**定位**到 LE 上表面(LE 下 + MID 正常收敛),而 **TE 两面是另一种病**(d 缩小、e 变差 = **收敛到离实验更远处** = 模型误差签名)。★★ **后果:「换网格解决不了 M3」的证据基础失效** —— 它是从一个**没在收敛的序列**外推的;**不是结论被证反,是外推不成立**。★★★ **判别式已做 2026-08-15**:按字面 **C-MIX**,但 **(c) σ 干净否证**(d_self 比值 **1.000**)、**(b) 足迹在带内测不到**(声速线 0.371–0.649,带止于 0.15;★ 但激波自身仍移 0.067 c ⇒ 不得读成否证)、★★★ **新事实**:超声速区从 x≈0.005 起 ⇒ **LE 带整段在超声速泡内** ⇒ 候选 (a) 改写为**「超声速泡前段不稳定」**(修法与"分辨吸力峰"完全不同);★★ 并自查出**判据缺陷 #5**(份额型集中度的 `CONC_max = 1/点占比`,前端区 66.7 % 的点 ⇒ 上限 1.50,我事前要求 2.0 = **按构造不可达**);★ RECORDED 交叉表把位置缩到**一格**:**x<0.05 且 η≥0.80(27.8 % 点、60.8 % 质量、CONC 2.19)**。★★ **(1)+(2) 已一轮做完 2026-08-15**:**Q1 = O-REFUTE** —— 样本外两站证明**向外递增在 η0.90 之后反转**(0.3732→0.3551→0.2682)⇒ **上一轮"外侧那一格"被削弱**(它其实是 5 站集边界效应);★★★ **但指纹七站全部复现**(前端质量 87.9–96.6 %)⇒ 改写为「**整条展向的前缘现象,幅度在 η0.90 见顶**」。**Q2 = M-PEAK 按字面**(3/5 vs 2/5),★ 但峰值**处处低估**(≤1)、前脚在外侧**高估 4.7–6.9×** ⇒ 峰值深度是**主要但非全部**。★★ 并测到:**泡前脚随加密一律前移(最多 0.020 c)、斜率 52→164** ⇒ **被测带的上游边界在移动且变陡**。★ φ 已落盘。★★ **(3)+(1) 已一轮做完 2026-08-15**:**A-SHAPE** —— 缺口 = **曲线变形**,而水平分量的大小**正是**峰值深度量到的(`|bias|/|ΔCp_min|` 3/5 站在 2 倍内)⇒ **不是新机制**;**B-GEOM** —— R_d 只动 **+0.1 %** 而 cl_p 动 **−3.1 %** ⇒ **见顶是机翼自身展向结构,不是 taper**(★ 三个 cl_p 与 W2 表逐位相同 = 计划外跨轮核对通过)。★★★ **三轮拼成自洽图**:靶子 = 「**前几个百分点弦长内、声速界面的亚单元表示**」,与能力边界 §3 首位的「亚单元激波定位」**同族**(位置在**泡的前脚**);它同时解释了刚性平移估计高估 4.7–6.9× 与水平估计低估 0.24–0.88。★★ **(i)+(iii) 已一轮做完 2026-08-15(零求解)**:**F-NEITHER** —— 前脚扫过窗口与等宽峰值窗口**都不中**(2/5 与 1/5,而重叠 ≤0.02 ⇒ 不是分不开);实测极大**集中在 x ≈ 0.004–0.008 c** 且 3/5 站落在**最细一级声速前脚 ±0.0024 c** 内(η0.90 几乎重合)⇒ ★ **合成的方向仍指声速界面,但"扫过的区间"这个形式是错的**;★ 我**不事后换窗口**去凑命中。**G-MULTI 按字面而实质近 G-NONE**:`|ΔCp_min|` 与 d_self **同族(循环)**、唯一独立的 `|dΓ/dz|` **形状完全不像** ⇒ **Γ / Γ/c / 弦长 / x_foot / Cp_min 全部测负** ⇒ **"机翼自身的展向结构"目前没有任何具体几何量支撑**。★★★ **判决性检验已做 2026-08-15 = P-MIX**:内区 **R 1.43 未收敛**、外区 **R 0.91 混合**(要 ≤1/3)⇒ **「非收敛 = 前缘近奇异」方向对但只覆盖约一半**(去掉内区 d_self 减半、内区以 41.7 % 的点占 **74.9 %** 的 d² 质量,但**外区仍不收敛**,且未解释的那一半**偏外侧**)。★★★ **kill 条 4 抓到我自己**:分区实现精确(G-I 1.44e-16),而"LE 下/MID 是 R≤1/3 的对照组"这个前提从来不成立 —— 两轮前那句"正常收敛"用的是**另一个判据**(d_self 缩小)⇒ **判据层面的第 5 问**;并测出**整个数据集没有任何 band 的 R ≤ 1/3**(最好 MID 上 0.638)。⇒ ★★ **「①收口」的前提被削弱,不能按原样执行**;LE 这条线已交出能交的(位置、机制族、约一半的定量归因),**继续下去要么撞裁决 D2、要么只是再切一次带**。★★ **已按 ③ 开一轮(2026-08-16)**:入口检查把靶子从 **M4 移到 S4 前提核实**(M4 在 HEAD 上**没有翼身粘性 case 入口**),结果**字面 Q-BOTH** 但**三条实质削弱**(P2 的带**跨工况** = 判据缺陷 #7、P1 **是级别的性质**且生产级可能掉出带、"横流没用上"**只对两个最大值之比**成立);★★★ 并在入口检查里发现 **M3a 已被分类"无粘不可达"且地板在 MID**,而我**差点报成与 LE 十轮矛盾** —— 不矛盾(**地板 vs 当下预算**),这给了 LE 线更准的收口句。原建议:**走 ③(转向产品指标 M4 / 速度侧),并把本轮这段作为 LE 线的收口记录** —— 使用者裁决。见 [判定 §8](20260815-2000-nearsingular-partition-verdict.md) |

## ★ D5 的代价现在生效了

level-set 删除之后,下面这些结论**只剩已提交的 CSV/PNG,不再可重跑**:P14 跨模型
**0.17% / 0.36%**、B9 翼身两路径 **0.4% / 0.6%**、B27 跨模型 **2.4%**、A1 四驱动器成本、
**B5 远场域稳健性 0.45% / 1.09%**、B18 两路径跨声速对照。
⇒ **此后唯一的外部裁判是 `cases/reference_data/`**,而三维**升力**在那里没有任何锚点。
