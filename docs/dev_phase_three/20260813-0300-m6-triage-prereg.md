# 20260813-0300 预注册 — **分诊 ①:M6 coarse 在 M0.8395/α3.06 上是否落在多解区?**

**在任何代码之前提交**(roadmap 原则 8)。使用者裁决:按
[20260813-0100 协议](20260813-0100-post-sigma-research-protocol.md) §5 的顺序执行 ①。

## 0. ★★ 入口检查(零求解)—— 它改变了本轮要问的问题

| 事实(读代码得到) | 后果 |
|---|---|
| `run_m3_budget.py` 默认 `levels=("coarse",)` | ★ 那 70 % 是 **M6 coarse** 的数 ⇒ 分诊很便宜(coarse 爬坡实测 ~35 s) |
| `M6_NEWTON_KW` 里 `n_picard_seed = 0` | 已提交锚点是**单一种子** |
| ★★ `solve_newton_transonic` 只**冷启第 0 级**,后续级**热启动**(脚本注释原文:*the transonic driver only cold-seeds level 0*) | **种子只影响爬坡的第一级** |
| 压力 Kutta 腿的第 0 级是**由一次 M0.70 的 probe Newton 解播种**的 | 种子的影响还要再经过这一层 |
| CLAUDE.md:*ramp 与 seed 是同一功能的两种实现* | ⇒ 爬坡本身**可能就是**把解钉住的东西 |

★★★ **所以本轮必须把两个不同的问题分开,不许混成一个:**

1. **Q-A(实用问题)**:**已提交的 M6 锚点是不是一次任意抽样?**
   ⇒ 用**生产配方(爬坡)**跑三个种子。这是决定那些数可信度的那一问。
2. **Q-B(机理问题)**:**M6 这个离散系统本身在该工况上是否多解?**
   ⇒ 需要**绕开爬坡**(直解),因为爬坡会掩盖它。

★ **若 Q-A 稳定而 Q-B 散** ⇒ 结论是"**爬坡把选择钉住了**",**不是**"M6 没有多解"。
这两句话不同,而**只有前者是数据支持的**。本轮**明确禁止**把 Q-A 的稳定报成后者。

## 1. 常设四问(CLAUDE.md)

| 问 | 回答 |
|---|---|
| **1 单边?** | 不单边:**T3(散布大)**与 **T1(散布小)**都是命名带,且两者**结论不同而非好坏之分** |
| **2 自变量会改变样本存在性吗?** | 会(某个种子可能不收敛)⇒ **平衡面板**:只在**收敛**的腿之间算散布,少于 2 条 ⇒ **UNDEFINED** |
| **3 阈值绝对还是相对?** | ★ 这里**绝对阈值是对的**,因为要判的是"能不能当锚点用",而**锚点的容差是外部给定的**:cl 用 **P14 锚点的 ±0.5 %**(committed coarse cl_p 0.262778 / cl_KJ 0.268813),LE 带误差用 **±2.5 %**(committed 4 变体的实测散布上界)。**两个数事前写死,并注明它们是从已提交证据取的,不是我挑的** |
| **4 匹配的量是输入还是输出?** | 本轮**不做任何"匹配回基线"** —— 只报**集合的散布**,那是纯输出侧的描述性统计 |

## 2. 设计

**条件**:M6 / **M0.8395 / α3.06**(TEST 2308 逐字)、**coarse**、**8 线程**。
**配方**:`run_m3_budget.py::solve` 的 **`(entropy=True, kutta="pressure")` 生产腿逐字**
—— 它就是产出 P14 锚点与 M3 预算的那一条。
**种子集合事前声明**:`{0, 5, 12}`(与 2.5-D 各轮同一集合,便于对照)。

| 臂 | 做什么 | 回答 |
|---|---|---|
| **A(爬坡,生产)** | 三个种子各跑一次生产爬坡 | **Q-A** |
| **B(直解,机理)** | 三个种子各在 M0.8395 上**直接** `solve_newton_lifting`(无爬坡),其余逐字相同 | **Q-B** |

⇒ **6 次求解**。★ 臂 B 很可能不收敛(GS3.3a 实测:无爬坡 222.8 s 不收敛 vs 爬坡 70.8 s 收敛)——
**那是一个被允许的结果**,按前提 P1 记为 UNDEFINED,**不得**当成"臂 B 没有多解"。

**每条腿记**:cl_p、cl_KJ、x_shock(各 η)、**LE / MID / TE 三个带的 RMS**、m_final、
clamps、σ_min、m1_max、`sigma_freeze_report`、墙钟。

## 3. 事前判据

**主量**:臂 A 平衡面板上的 **cl_p 与 cl_KJ 相对散布**,以及 **LE 带 RMS 的相对散布**。

| | 条件 | 判定与后续 |
|---|---|---|
| **T1** | 臂 A:cl 散布 **≤ 0.5 %** **且** LE 带 RMS 散布 **≤ 2.5 %** | ⇒ ★ **已提交的 M6 coarse 锚点在生产配方下是可复现的**,不是任意抽样 ⇒ **LE 那 70 % 站得住**,分诊 ② 可以照常推进 |
| **T2** | 臂 A:cl 散布超 0.5 % **但** LE 带 RMS 散布 ≤ 2.5 % | ⇒ ★★ **协议 §3 那个假设被证实**:**升力是多解的载体,LE 亏空与它可分离** ⇒ LE 工作可推进,而**每一个 3-D 升力数都要改报集合** |
| **T3** | 臂 A:**LE 带 RMS 散布也超 2.5 %** | ⇒ **LE 那 70 % 需要重新定基**,"LE 占 70 %"在该工况上无意义 ⇒ LE 项必须改到 α=0 / 冻结 Γ 上做 |
| **T4** | 臂 A 平衡面板 < 2 条 | ⇒ **UNDEFINED**,RECORDED,不宣称方向 |

**臂 B 一律 RECORDED**(它回答 Q-B,而 Q-B 不决定锚点可信度):
若 B 散而 A 稳 ⇒ 记为"**爬坡钉住了选择**";若 B 不收敛 ⇒ 记为"**Q-B 在本配方下不可测**"。

**守卫 G-R(仪器)**:臂 A 的 **seed 0** 腿必须复现已提交的 P14 coarse 锚点
(cl_p **0.262778** / cl_KJ **0.268813**)到 **0.5 %** 之内。★ 不过就停 —— 那说明我没在复现生产路径,
下面所有散布都无从解释。

## 4. ★★ KILL CRITERION

1. **G-R 不过** ⇒ 我跑的不是生产路径,停下对齐;
2. **T4**(臂 A 收敛腿 < 2)⇒ 分诊读不出来,停并如实上报;
3. **单腿 > 10 min** ⇒ 停(coarse 爬坡实测 ~35 s,预算 ≤ 20 min;臂 B 可能更慢,单独计时);
4. ★ **发现臂 A 的三条腿逐位相同** ⇒ **不是通过,是可疑**:说明种子根本没进到求解里
   (`n_picard_seed` 被配方覆盖等)⇒ 先查仪器再读数。

**明确不算 kill**:**T3**(LE 也散)—— 那是本轮最有用的负结果,它直接改写 LE 项的做法;
臂 B 不收敛;**M1 仍不达标**。

## 5. 本轮不做

- **不改任何库代码** —— 纯 bench 侧的种子扫描;
- **不改 M3 的口径**(带定义 LE/MID/TE 与 5 个未遮蔽站点逐字沿用);
- **不重新定基任何已提交数字** —— 本轮只回答"要不要重新定基";
- ★ **不把臂 A 的稳定报成"M6 没有多解"**(§0 已写死这条禁令)。

---

## 附录 #1(**动代码之前登记**)—— 种子要变在**哪一处**,决定实验是否有效

生产腿(`entropy=True, kutta="pressure"`)里种子出现**两次**:

1. **M0.70 的 probe Newton 解**:`solve_newton_lifting(..., **M6_NEWTON_KW)`,其中 `n_picard_seed=0`;
2. 爬坡第 0 级:`kw["newton_kw"].update(..., phi_init=r0["phi"], gamma_init=..., n_picard_seed=0)`。

★★ **第 2 处是惰性的**:那里已经给了 `phi_init`,而 Picard 播种只在**没有热启动**时才起作用。
⇒ **只改第 2 处会得到三条逐位相同的腿**,并因此触发 kill 条 4 ——
而那会是**因为错的理由**触发(仪器没接上,不是"种子无影响")。

**因此(现在定)**:种子变在 **第 1 处(M0.70 的 probe 解)** —— 它是整条链的**入口**,
改它才真正改变了那一次"抽样"。第 2 处保持 `n_picard_seed=0` 逐字不动。

★ **顺带,这条附录本身就是 kill 条 4 的价值示范**:那条"逐位相同 = 可疑而非通过"的条款,
在**执行之前**就已经把一个会产出假读数的接法挡掉了。

---

## 附录 #2(**G-R 触发 kill 条 1 之后、重跑之前登记**)—— 两个错都在我这边

**G-R FAIL**:我的 seed 0 **coarse** 爬坡腿给 cl_p 0.268115 / cl_kj 0.273582,
对 P14 coarse 锚点(0.262778 / 0.268813)差 **2.03 % / 1.77 %**,远超 0.5 %。
按 kill 条 1 停下对齐 —— 而对齐之后发现**两个错都在注册里**:

### 错 1:我把级别读错了

我读 `def main(levels=("coarse",))` 就断定"那 70 % 是 coarse 的数"。
**已提交的 `gate_results/m3_budget.csv` 自己写着 `level = medium`**(全部 4 行)。
⇒ **那 70 % 是 M6 medium 的数。** 我读的是**函数默认值**,不是**证据**。

### 错 2:G-R 拿了**另一条流水线**的锚点

我把守卫设成"复现 P14 的 coarse 锚点"。但 P14 是**另一条流水线**,
而 `run_m3_budget` 自己的已提交输出里**没有 coarse 行** —— 我根本没有可对齐的 coarse 参照。
⇒ 这正是项目记过的**"引用你的流水线真正跑过的那一个"**(P14 的 TE gap 两种口径栽过一次)。

### 修正(现在定,不看任何散布)

- **分诊移到 `medium`**,因为**这条腿自己的已提交参照在那里**:
  `ON/pressure` 行 = **cl_p 0.276527 / cl_kj 0.281179 / band_LE_upper 0.235615 / band_LE_lower 0.092155**;
- **G-R 改为**:seed 0 的 medium 腿必须复现**上面这一行**(同一脚本、同一腿、同一级别)到 **0.5 %**;
- 判据 T1–T4、平衡面板、kill 条 2/4、LE 的 2.5 % 阈值 **一字不改**;
- ★ **coarse 的那三条腿不作废,记为 RECORDED**:它们是这条腿在 coarse 上的**首次读数**,
  并且它与 P14 coarse 锚点差 2.0 % 这件事本身值得留档(**两条流水线在 coarse 上不一致**)。

### 成本闸修订(必须一并登记)

M6 **medium** 爬坡实测 **~515 s**(CLAUDE.md)⇒ 臂 A 三条腿 ≈ **26 min**,
**单腿 515 s 已贴近注册的 600 s 上限**。⇒
- **先只跑臂 A**;
- **臂 B(直解 medium)推迟**,是否跑由臂 A 的结果决定 ——
  它很可能烧满 80 步而不收敛(GS3.3a 在 coarse 上已如此),在 medium 上代价更高而回报是 RECORDED。
  **这不是缩小范围,是把一次可能纯烧预算的运行放到有信息之后再决定。**

---

## 附录 #3(**16 线程重跑之前登记**)—— 同一族的第三次:**跨溯源比较**

臂 A 的 seed 0 在 **8 线程**下给:**`converged = False`**、|R| 8.80e-07、`m_final = None`、
cl_p 0.249336 / cl_KJ 0.256491、rms_LE 0.295325/0.106043(482 s)。
对已提交的 medium `ON/pressure` 行(0.276527 / 0.281179)差约 **9.8 %** ⇒ G-R 又 FAIL。

**原因不是配方,是线程**:`run_m3_budget.py:21-23` 自己 `setdefault` 的是 **16**,
而我在注册 §2 写了 **8 线程**,并在启动时用 `env NUMBA_NUM_THREADS=8` **显式覆盖**,
使那个 `setdefault` 失效。⇒ 我拿 **8 线程的运行**去对 **16 线程的已提交行**。

★★ **这是本轮同一族错误的第三次**:
① 跨流水线(P14 锚点 vs 本脚本)、② 跨级别(函数默认 coarse vs 证据 medium)、
③ **跨溯源(8 线程 vs 16 线程)**。三次都是"**比较的两边不是同一个东西**"。
⇒ 建议把这一条并入 CLAUDE.md 的常设四问,作为**第 5 问**:
**"我要比的两个数,是同一条流水线、同一级别、同一线程数、同一时刻测的吗?"**

### 修正(现在定)

- **线程改为 16**(与参照的溯源一致);当前机器 `load average 8.25 / 24 核`,符合纪律 1;
- **8 线程那一臂保留为 RECORDED,而且它本身有价值**:
  **生产腿在 8 线程的 M6 medium 上不收敛** —— 这与项目已记的
  "NACA M0.80 medium 的线程依赖"是同一族现象,是一条独立于本轮问题的读数;
- ★ **判据 T1–T4 与 LE 的 2.5 % 阈值一字不改**;G-R 的参照行也一字不改。

### ★ 一条必须先说清的限定

**8 线程臂的"散布"仍然是可读的**(臂内可比),但**它回答的是 8 线程下的问题**。
而已提交的 70 % 是 16 线程的数 ⇒ **要判"那 70 % 是否任意抽样",必须在 16 线程上判。**
两者不可互替。

---

## 附录 #4(**撤回附录 #3 的诊断**)—— 真原因是**跨时间**,而它揭示了一笔别人的债

**16 线程的 seed 0 与 8 线程的结果逐位相同**(conv=False、|R| 8.80e-07、cl_p 0.249336、
cl_KJ 0.256491、rms_LE 0.295325/0.106043)。
⇒ ★ **附录 #3 把 G-R 失败归因于线程,是错的,撤回。** 这条 M6 medium 爬坡是**线程无关**的
(而这本身是一条 RECORDED 读数:我原以为它会像 NACA M0.80 medium 那样有线程依赖,**它没有**)。

### 真原因:参照本身是**被取代的代码**产出的

| 时间 | 事件 |
|---|---|
| **2026-07-31 16:09** | `gate_results/m3_budget.csv` 提交(**那 70 % 的出处**) |
| **2026-08-02** `12b9e3d` | **GS3.1:生产 M6 配方翻成 `amg`** |
| **2026-08-05** `3631f6f` | **G8.2 重锚到圆角帽 + 生产 taper** |
| 2026-08-10 `1a41cec` | 快层线程钉定 |

⇒ **参照比现行代码早三次变更。** 所以 **G-R 在任何正确的 HEAD 运行上都不可能被满足** ——
不是我的配方错了,是**参照过期了**。

★ 最可能的主因是 **2026-08-05 的圆角帽/taper 重锚**(cl_p 0.2765 → 0.2493 = −9.8 %,
量级与几何变更相称);但 `.msh` 是 gitignored,**我无法核实 07-31 当天的 `medium.msh` 是不是平角帽**
⇒ 这一条标为**最可能**,不作断言。**可断言的是**:参照早于三次已记录的变更,因此**与 HEAD 运行不可比**。

### ★★★ 它揭示的东西比本轮注册的问题更重要

**"LE 占 69.6 % / MID 20.7 %" 这个数是 2026-07-31 的产物**,而能力边界一直把它当**在用的数**引用。
⇒ **这正是项目自己的"再定基勘误清单"要防的事**:一次变更取代了旧数字,而旧数字留在文档里没人改。

⇒ **对使用者那个问题("在有多解时怎样研究 LE 那 70 %")的真正答案是:**
**现在还谈不到多解 —— 那 70 % 必须先在 HEAD 上重测**,然后才轮到给它做种子分诊。
顺序被这条发现**颠倒**了,而这是本轮最有价值的产出。

### 本轮判定的形式(现在定)

- **T4 / UNDEFINED**:三条腿全不收敛,平衡面板为空 ⇒ 注册的 T1–T3 **不可判**;
- **G-R 记为"不可满足(参照过期)"**,而不是"我的配方错" —— 两者的后续完全不同;
- **不重测那 70 %**:那是**另一轮**的事(它要动 `run_m3_budget` 的重跑与勘误链),
  本轮只**发现并登记**它过期。★ 不趁手做,是因为"发现 + 重测 + 勘误"混在一轮里,
  就分不清哪一步的结论属于哪一次测量。
