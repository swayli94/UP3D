# 20260816-0000 — 判定:**Q-BOTH 按字面成立**,但**三条实质性削弱**,其中一条是**我自己的判据缺陷(第七个)**

> **零无粘求解**(φ 落盘)。**binding = S2**(最细可用级):
> **P1 = 9.96×**(带 [4, 20] ✓,第一阶段参照 9.0×)、**P2 = 0.1471**(带 ≤0.15 ✓)⇒ **Q-BOTH。**
> ★★★ **但削弱一:P2 只以 2 % 的余量过线,而它的带来自一个跨工况的数** ——
> 第一阶段的 **0.072 是在 M0.5** 上测的(GV5.0 桥),我这轮在 **M0.8395**。
> ⇒ **我把带定成"第一阶段 0.072 的两倍"时,继承了一个跨工况前提** = **本季第七个判据缺陷**,同族第 5 问。
> ★★ **削弱二:P1 强烈依赖级别** —— S2 **9.96×** 而 S0 **25.82×**(带外)。
> 无粘成本随**体单元**超线性增长,而 IBL 随**表面节点**增长 ⇒ **比值随加密下降**
> ⇒ 那个"9×"**是级别的性质,不是代码的性质**,并且**在生产级很可能掉出带的下沿**(推断,已标注)。
> ★★ **削弱三:逐点 |B|/|A| 在个别节点 > 1**(S2 max **1.5843**,S0 **17.97**),
> 而中位只有 0.0084 ⇒ **"横流没用上"是关于「两个最大值之比」的陈述**,逐点并非如此。

预注册 [20260815-2200](20260815-2200-s4-premise-check-prereg.md)(动代码前提交,`e0420a0`)。
脚本 `bench/run_task3_s4_premise_check.py` → `gate_results/task3_s4_premise_check.csv`。
16 线程(脚本打印)。**无粘侧零求解**(φ 落盘);两级 IBL 各一次,共 ~157 s(闸 900 s)。

## 1. 实测

| 级 | 体单元 | 表面节点 | BL 未知量 | **一次 IBL** | 无粘(同溯源 CSV) | **P1** | **P2 = max\|B\|/max\|A\|** | 逐点中位 / p90 / max |
|---|---|---|---|---|---|---|---|---|
| **S2**(binding) | 59 359 | 3 557 | **21 342** | **133.5 s** | **13.4 s** | ★ **9.96×** | ★ **0.1471** | 0.0084 / 0.0881 / **1.5843** |
| S0 | 9 303 | 946 | 5 676 | 23.2 s | 0.9 s | **25.82×** | 0.1546 | 0.0176 / 0.1415 / **17.97** |

**守卫**:**G-φ** 打印了缓存路径与形状(`phi_S2.npz` φ 13985 / γ 85)⇒ **没有重解无粘**;
**G-P** 无粘墙钟取自**本阶段自己的** `task3_selfconvergence.csv`(打印了 leg / tets / |R|);
**G-U** 横流定义**从 V5 桥的源码读出并打印**(`live = ~tip`、`a = max|U[live,1]|`、`b = max|U[live,2]|`、
tip 为 `z > z_tip·(1−0.05)`)。

★ **IBL 报 `converged False` 是构造性的**:`n_outer_max = 1` ⇒ 松循环**一轮不可能收敛**。
**不是 IBL 失败**,而是本轮只需要**一次求解**(P1 比单次、P2 是单次诊断)。

## 2. ★★★ 削弱一:P2 的**带**继承了一个跨工况的数(判据缺陷 #7)

| | 值 | 工况 |
|---|---|---|
| 第一阶段(S4 立项依据) | **0.072** | ★ **M0.5**(GV5.0 桥) |
| 本轮 S2 | **0.1471** | ★ **M0.8395**(M3 工况) |

⇒ 两者**不可比**(跨工况),而我把 P2 的带定成"第一阶段 0.072 的两倍余量 = 0.15" ——
**于是那条带本身继承了跨工况前提**,而实测值以 **2 %** 的余量压线通过。

★★★ **这是本季第七个判据缺陷**,而它的形状是新的:
前六个都在**被比较的量**上(单边带 / 不平衡面板 / 绝对阈值 / 输出当输入 / 两个数不同源 / 上限按构造不可达),
**这一个在「带本身的出处」上** —— 我检查了"我要比的两个数是不是同一个东西",
**却没检查"我拿来定带的那个数是不是同一个工况"**。
⇒ **常设检查要加一条:定带用的参考数,也要过第 5 问。**

★ 处置:**不挪带**(挪了就是事后改判据)⇒ **Q-BOTH 的字面判定保留**,
但**"P2 前提在 HEAD 上复现"这句话不能说** —— 能说的只有:
**在 M3 工况上,max|B|/max|A| = 0.147**,而**第一阶段那个 0.072 是另一个工况的数**。

## 3. ★★ 削弱二:P1 是**级别的性质**,不是代码的性质

**S2 9.96× / S0 25.82×** ⇒ 比值随加密**下降**,机理清楚:
**无粘成本 ~ 体单元数(超线性)**,**IBL 成本 ~ 表面节点数** ⇒ 加密时**分母涨得更快**。

★ 定量:S2→S0 体单元 ×6.4、表面节点 ×3.8;而无粘墙钟 ×14.9、IBL 墙钟 ×5.8。
⇒ **向生产级(S4:448 197 tets、无粘 153 s)外推,P1 很可能落到 ~3× 量级** —— **带的下沿是 4**
⇒ ★★ **在生产级上,那条"BL 是成本大头"的前提可能不成立。**
★ 这是**推断,不是测量**(我**没有**测 S4 的 IBL),而它**不在**注册的级别清单里
⇒ **不在本轮补测**(看到趋势之后加腿就是范围蔓延),**登记为下一步的第一条**。

## 4. ★★ 削弱三:"横流没用上"只是**两个最大值之比**

| 级 | max\|B\|/max\|A\| | 逐点中位 | 逐点 p90 | ★ 逐点 **max** |
|---|---|---|---|---|
| S2 | 0.1471 | **0.0084** | 0.0881 | ★ **1.5843** |
| S0 | 0.1546 | 0.0176 | 0.1415 | ★ **17.97** |

⇒ **中位只有 0.008**(横流确实在绝大多数节点上可忽略),
**但逐点比值在个别节点 > 1** ⇒ **那里横流比流向分量还大**。
★ 而 `max|B|/max|A|` 这个定义**把两个不同节点的最大值相除**,天然**看不到**这一点
(我按 G-U 读了定义并沿用它,所以这不是实现偏差,是**那个量本身的盲区**)。
⇒ **对 S4 的"做减法"的实际含义**:把横流**全域**丢掉在中位上无害,
但在**那些节点**上是错的 ⇒ **GS4.1 的二维条带需要说明它在那些节点上怎么办**(哪怕是"接受并记录")。

## 5. kill criterion

| 条 | 是否成立 |
|---|---|
| 1. φ 缓存缺失 | 不成立(两级都在) |
| 2. IBL 两级都不可用 | 不成立(**两级都跑出来了**) |
| 3. 无粘墙钟无同溯源出处 | 不成立(取自本阶段 CSV,已打印) |
| 4. 总墙钟 > 900 s | 不成立(~157 s) |
| 5. P1 > 100 或 < 1 | 不成立(9.96 / 25.82) |

★ **Q-BOTH 不算 kill**(四支都不算)。**M3/M4 仍不达标**(沿用前二十一轮的豁免)。

## 6. 读数汇总(四句话)

1. **字面判定 Q-BOTH**:S2 上 **P1 = 9.96×**、**P2 = 0.1471**,两条都进带;
2. ★★★ **但 P2 的带出处跨工况**(第一阶段 0.072 在 **M0.5**,本轮在 **M0.8395**)
   ⇒ **判据缺陷 #7**,新形状:**定带用的参考数没过第 5 问**;
   ⇒ 能说的只是"**M3 工况上 0.147**",**不能**说"前提复现";
3. ★★ **P1 是级别的性质**(9.96× vs 25.82×),向生产级外推**很可能掉到带外**(推断,已标注)
   ⇒ **S4 的成本动机在生产级上未被确立**;
4. ★★ **"横流没用上"只对两个最大值之比成立**;逐点在个别节点 **> 1**
   ⇒ 二维条带必须交代那些节点。

## 7. 下一步(**另行预注册**)

1. ★★★ **在生产级(S4:448 197 tets)量一次 IBL**(约 500 s 量级,**在闸内**)——
   它直接决定 **S4 的成本动机在生产级是否成立**,而这是本轮唯一没测到的关键点;
   ★ 事前把带写成"P1 的**级别趋势**",而不是一个单点值;
2. ★★ **把 P2 换成一个没有盲区的量** —— 例如**逐点 |B|/|A| 的 p95/p99 与 >1 的节点数**,
   并在 **M0.5 与 M0.8395 两个工况**上各测一次(★ 这才是"第一阶段那个 0.072 有没有漂移"的**同工况**问法);
3. ★ **GS4.1 若开工,需先说明**它在"逐点横流 > 1"的节点上怎么办;
4. 仍悬着:**Q-B 直解臂**、**coarse 两流水线差 2.0 %**、`wing3d.py` 的 docstring/签名帽型矛盾、
   **M4 缺翼身粘性 case 入口**(实现任务,非测量轮)。
