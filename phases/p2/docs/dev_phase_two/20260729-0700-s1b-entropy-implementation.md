# GS1b.3：实现熵修正（非等熵 FP）

> ★★★ **ERRATUM 2026-07-29 —— §5.6 / §5.8 的量值结论已被 GS1b.3b 推翻。**
> 读之前先读 [`20260729-1500-s1b-preshock-mach-fix.md`](20260729-1500-s1b-preshock-mach-fix.md)。
>
> 本轮的 M₁ 取"上游超声速行程的**峰值**"，而翼型气泡的马赫数**不是单调的**
> （M0.80/α1.25：峰值 1.408 在 x/c 0.33，激波在 x/c 0.63 处只有 ≈1.15–1.29），
> 所以 M₁ 被读成 1.3897 而不是 ≈1.27 ⇒ **修正强了约 2 倍**。
>
> 受影响：**§5.6 判据 F 的 +5.73% / +11.46% 作废**（正确量值 **+13.79%**，coarse→medium，
> 且 fine 未到目标马赫）；**§5.8 判据 H 的 1−σ 三级数**（3.187/3.322/4.383%）是偏强量值下的，
> 需重测。**不受影响**：判据 A（0 维恒等式）、C（Jacobian FD 精确）、D（默认关闭逐位不变）、
> B（喷管仍 PASS —— 而它**在结构上照不到这个错误**，因为喷管单调加速，峰值恰等于波前值）、
> 以及 E 的 churn 机制结论。
>
> 修正后的读数：翼型 M0.80/α1.25 coarse 的激波 **0.6186**，与 Euler 锚点中点 0.615
> 差 0.004c（等熵是 0.6581，带外）。

轮次文件名：`20260729-0700-s1b-entropy-implementation.md`
阶段 / 门：`S1b` / `GS1b.3`
分支：`claude/phase-two-s0-s1`
裁决依据：使用者 2026-07-29（roadmap §7 **D4**：先测量，然后走 B）

> **§1–§3 在动任何库代码之前写下并单独提交**（commit a2cb9c3，roadmap 原则 8）。§4 起执行后填。
> 这是 GS1.6 与 GS1b.1 两次的教训：前者的判据 0 在我投入一整个阶段之前把路走死了；
> 后者我没有预注册"怎么区分 fold 与延拓失败"，结果把步长极限当成了极限点。

---

## 1. 目的与要实现的东西

GS1b.2 的前提检验给出四条支撑（roadmap S1b 表），其中**结构性的那一条是决定性的**：
代码里现有的唯一削波机制（人工密度）∝ h，**随加密自己消失**（Q5 实测：fine 上
C=1.5 与 C=3.0 几乎重合），所以治不了一个不随 h 消失的病。熵因子只依赖**当地激波前
马赫数**，是 h 无关的。

**要实现的关系式（GS1b.2 §4.1 已用数值验证，不是推导出来就用）**：

> **ρ_s = σ · ρ_isen(q²)，σ = p₀₂/p₀₁（激波前马赫数 M₁ 的总压比）**

完全气体、总焓沿流线守恒 ⇒ `ρ₀ = p₀/(R T₀)` 且 T₀ 过激波不变 ⇒
`ρ₀₂/ρ₀₁ = p₀₂/p₀₁` **恰好**。已验证：加上它之后 FP 的质量守恒跳跃**精确复现
Rankine–Hugoniot**（0.0000%，M₁ = 1.15–1.60 全部）。
★ **不是** `σ^{1/(γ−1)}`（那会把指数用两次，我在 GS1b.2 里犯过一次并已订正）。

### 1.1 实现方案（三件事，每件都写清为什么这么选）

**(1) 激波探测 —— 复用现有的上游指派，不新建一套。**
`UpwindOperator` 在 `weighted=False` 模式下已经为每个单元算出唯一的上游供体
（`upstream_elements` → `self._upstream`）。激波 = 沿流线由超声速转亚声速，
所以判据是：单元 `e` 的供体 `u` 超声速而 `e` 亚声速 ⇒ `e` 是**激波后**单元，
`M₁ = M(u)`，局部因子 `s_e = p₀₂/p₀₁(M₁)`。**不新增几何或拓扑机器。**

**(2) 向下游输运 —— 沿供体链做乘积。**
熵沿流线守恒，所以 `σ_e = σ_{u(e)} · s_e`。供体映射是每单元唯一，构成一个森林，
所以用**固定次数的扫掠**迭代这个关系式即可（信息每扫掠传播一跳），扫掠数上限
兼作防环保护。σ = 1 表示从未过激波。

**(3) σ 冻结（lagged），不进 Jacobian —— 并且如实说明这是什么。**
σ 通过 `p₀₂/p₀₁(M₁)` **连续**依赖 φ，所以漏掉 ∂σ/∂φ 会让 Jacobian 真的不精确
（这与现有"上游指派不可导"不同 —— 那个是分片常数，几乎处处可导）。
因此：**σ 在每个 Newton 步开始时冻结，整步（残差 + Jacobian + 线搜索）共用同一个
σ，步间刷新**，与现有的 freeze/refresh 机器同一节奏。这样
**Jacobian 对"被求解的那个（冻结 σ）系统"是精确的**，FD 检验也对它做；
外层 σ 刷新是一个不动点迭代，它的收敛性是本轮**必须记录**的量。
若外层刷新不收敛 ⇒ 记 FAIL 并升级到"把 ∂σ/∂φ 也求导"（不在本轮范围）。

**(4) 插入点 —— 库改动尽可能小。**
`UpwindOperator.rho_tilde(grad, q2, rho, ...)` 把等熵密度 `rho` 当**输入**，
所以熵因子可以在**内核之外**乘上去：`rho ← σ * rho`，人工密度混合与其灵敏度
一行不用改。新旗标 `entropy_correction`（默认 **False**），
删除条件写在 §2.3。

### 1.2 接线范围与 backport 决定（第一阶段纪律 #9）

| 路径 | 本轮是否接线 | 理由 |
|---|---|---|
| `solve_newton_lifting`（`newton.py`） | **接** | M1 / M1a / 所有 S1 判据都走它 |
| `solve_subsonic_lifting`（`picard.py`） | **接** | 它是所有路径的种子；种子与主解用不同的密度关系会引入一个假的不一致 |
| `newton_ls.py` / `picard_ls.py`（level-set 孪生） | **不接** | roadmap S5 计划删掉 level-set 路径；本轮不新增第二条并行路线（禁止项）。**明确记录为 N/A，不是沉默** |
| `continuation.py`（马赫爬坡） | **不接** | GS1.2b 已测出爬坡可删、目标马赫直接解同解且更快 |

## 2. 失败判据（预先写下，逐条可判）

### 2.1 正确性（必须全过，否则不看效果）

| # | 判据 | 判定 |
|---|---|---|
| **A** | **0 维恒等式**：给定 M₁ ∈ [1.15, 1.60]，修正后的质量守恒跳跃复现 R–H 到 **< 1e-6 相对** | 不过 ⇒ 关系式或实现错，停 |
| **B** | **1-D 喷管命中预注册的预测**（§2.2）：修正 ON 时激波落在 **x_s = 11.62**（不是 12.0），且随加密收敛到它；修正 OFF 时仍落在 12.0（GS1.1 的结果一位不变） | 落在 12.0 附近 ⇒ 修正没起作用；落在别处 ⇒ 输运或探测错 |
| **C** | **Jacobian 对冻结 σ 系统 FD 精确**：`‖Jv − FD‖/‖FD‖ < 1e-6`，且随 ε 变化（1/ε 走向 = FD 舍入，不是缺项）—— 这个 ε 判别式是第一阶段 B19 的教训 | ε-无关 ⇒ 缺项，停 |
| **D** | **默认关闭逐位不变**：`bench/bitcheck.py` **10/10**；M1a 锁不动；`bench/run_bench.py --compare` 全绿；全套测试 0 失败 | 不满足 ⇒ 停 |
| **E** | **外层 σ 刷新收敛**：记录刷新次数与 `max|Δσ|` 的走向 | 不收敛 ⇒ 记 FAIL，升级到求导 ∂σ/∂φ（下一轮） |

### 2.2 ★ 预注册的定量预测（在写任何求解器代码之前算出来的）

用 `bench/s1_duct/nozzle.py` 的几何与同一组 Dirichlet 数据
（Δφ = 19.31862767，即等熵模型下激波正好在 x_s = 12.0、M₁ = 1.29998 的那一组）：

| 模型 | 激波位置 x_s | M₁ | σ |
|---|---|---|---|
| 等熵（现状） | **12.0000** | 1.30000 | — |
| **熵修正（预测）** | **11.6200** | 1.28022 | 0.982657 |

**位移 = −0.3800 长度单位 = −1.90% L ≈ −2.3 个 nx=120 单元，方向 UPSTREAM。**

判据 B 就是命中这个数：`|x_s(ON) − 11.62|` 必须随加密下降，且在最细一级
（nx = 400，h = 0.05）**小于 2 个单元**。这个数字与求解器无关（纯 quasi-1-D 反解），
所以它是一个真正的外部锚点，不是自我一致性检查。

### 2.3 效果（这才是 B 值不值得的判据）

| # | 判据 | 判定 |
|---|---|---|
| **F**（主判据） | 固定 C = 1.5，M0.7875/α1.25 的**三级 cl 跳幅收缩**：现状 coarse→medium **+40.5%**。要求跳幅**至少减半**（≤ +20%） | 收缩 < 5 个百分点 ⇒ 等熵过预测**不是**翼型发散的驱动因素，**B 关闭为负结果**，转 §6 的升级路线 |
| **G** | **x_shock 随加密转向上游**（朝 M1 锚点 0.61）；现状 C=3.0 下 0.567 / 0.631 / 0.708 一路下游 | 仍单调下游 ⇒ 与 1-D 的方向证据矛盾，必须解释（可能是升力反馈盖过了修正） |
| **H** | **修正项不随 h 衰减**：记录每级的 `1 − σ_min` 与"σ < 1 的单元占比"；三级应大致相同（h 无关） | 随加密衰减 ⇒ 又造了一个 C，B 无意义 |
| **I** | **同时记录 T₀ 与 (1−b) 的三级走向**（GS1.7 的 0/0 闭合结构风险） | 只记录，不设阈值；用于判断"cl 收缩了但闭合仍退化"这种情形 |

**kill criterion（明确写下）**：A–E 全过但 **F 不过** ⇒ **B 是测量出来的负结果**：
等熵过预测可以被精确修掉（1-D 命中预测），但它**不是**翼型三级发散的驱动因素。
那时的下一步不是继续调熵修正，而是转向 GS1.7 测出的**闭合方程 0/0 结构**
（`F = T₀ + (b−1)Γ`，两者同时 ~h^0.6 趋零），或者触发 roadmap §7 D2 搁置的 S6。

### 2.4 不允许的做法（自我约束）

* 不允许为了让 F 通过而调 `upwind_c`、`m_crit`、`freeze_tol` 或工况 —— 全部沿用
  GS1b.2 Q5 的设置（C = 1.5，M0.7875/α1.25，自适应延拓）；
* 不允许给 σ 加限幅、松弛因子或"经验系数"来改善收敛 —— 若需要，那是一个新发现，
  必须单独记录并说明它把模型改成了什么；
* 不允许把 1-D 的成功（判据 B）说成"B 成功了"—— F 才是产品判据。

## 3. 怎么测（命令）

```bash
PYTHONPATH=. python bench/s1_duct/run_entropy_premise.py          # 判据 A（已有）
PYTHONPATH=. python bench/s1_duct/run_entropy_nozzle.py           # 判据 B（新增）
PYTHONPATH=. python -m pytest tests/test_s1b_entropy.py -q        # A/C/D 的常开锁
python bench/bitcheck.py --diff bench/results/bit_a.npz bench/results/bit_s1b.npz
PYTHONPATH=. python bench/run_bench.py --compare bench/baseline_2026-07-28.csv
PYTHONPATH=. python bench/s1_duct/run_h_convergence_vs_C.py --entropy  # 判据 F/G/H
PYTHONPATH=. python -m pytest tests/ -q                           # 影响面
```

## 4. 改了什么

### 4.1 库代码（新增一个内核 + 两个驱动器接线，默认全部关闭）

| 文件 | 改动 |
|---|---|
| **`pyfp3d/kernels/entropy.py`（新）** | `total_pressure_ratio(M₁)` = R–H 总压比；`shock_factor_sweep` = 激波探测（供体超声速 + 自己亚声速 ⇒ 激波后单元，M₁ = **上游超声速行程的峰值马赫**）；`transport_sigma` = 沿供体链求积（**指针倍增**，O(n·log 深度)，带环路检测）；`EntropyOperator` 工作区 |
| `pyfp3d/kernels/upwind.py` | 两个密度灵敏度 sweep 增加 `(sigma, use_sigma)`：`ρ_s = σ_e·ρ_isen(q²_e)` ⇒ 解析的 `dρ/dq²` 必须同乘 σ（调用方传入的修正 `rho` 只修好了跳跃项，修不了这个导数）。**藏在布尔早退后面**而不是写成"乘一个全 1 数组"，这样默认路径执行的算术**一行不变** |
| `pyfp3d/solve/newton.py` | `entropy_correction`（默认 False）+ `entropy_refresh_max=8`；`NewtonWorkspace.ent` / `sigma_frozen` / `refresh_sigma()`；σ 进 state 并由 `assemble_coupled` 传给灵敏度；**收敛判定挂上 σ 输运标志**（环路 ⇒ 拒绝上报收敛，GS1.4 契约）；复用工作区时清空 σ；诊断 `sigma_history` / `sigma_min` / `n_shock_cells` / `m1_max` / `n_sigma_refresh` |
| `pyfp3d/solve/picard.py` | 同一个旗标；Picard 无 Jacobian，所以只需修正密度；σ 输运失败并入 `clamped` |

**backport 决定（纪律 #9）**：`newton_ls.py` / `picard_ls.py` **不接线** —— roadmap S5 计划删掉 level-set 路径，本轮不新增第二条并行路线（禁止项）。**明确记录为 N/A。**

### 4.2 判据台与测试

新增 `bench/s1_duct/run_entropy_nozzle.py`（判据 B）、`run_entropy_closure.py`（判据 I）、
`run_h_convergence_vs_C.py --entropy`（F/G/H）、`run_m1_gate.py --entropy`、
`bench/s1_duct/duct.py` 的 entropy 钩子（与库同样的冻结语义），
以及 `tests/test_s1b_entropy.py`（**16 个常开测试**）。

### 4.3 ★ 顺手修掉两个"会导致错误归因"的基线缺陷

两个都是我自己这次差点踩进去的：

1. **`bench/bitcheck.py` 不记录线程数**。8 线程对 16 线程基线做 diff，会报
   `gamma_wing`（79 个值、16 ULP）和 `phi_wing`（6470 个值、2 ULP）"动了"——**代码一行没改**，
   那两个 probe 走并行内核、求和顺序随线程数变。现在线程数进 npz，`--diff` 在表格前先警告。
2. **`bench/run_bench.py` 同病**（时间类指标 8t vs 16t 漂 13–85%），同样修法。
   并且顺带查清了 `wing/coarse` 的 `n_newton: 17 → 18`：**在改动前的 commit a2cb9c3 上重跑也是 18**
   （cl_p / gamma_mean / m_max / n_gmres 全部一致）⇒ 它来自 **GS1.4**（那一轮让
   `solve_subsonic_lifting` 在钳制时拒绝上报收敛，于是 Picard 种子变了），是**上一轮的基线欠账**，
   与熵修正无关。已就地写进 `compare()` 的注释。

## 5. 结果

### 5.1 判据 A = PASS（0 维恒等式）+ 负对照

7 个 M₁（1.15–1.60）上修正后的跳跃复现 R–H 到 **< 1e-6 相对**（实测 0.0000%）。
★ 并加了**负对照**：两个"看起来合理"的指数 `σ^{1/(γ−1)}`、`σ^{1/γ}` 偏差 **> 1%**，
所以判据 A 真的有鉴别力（这就是让 GS1b.2 那个指数错误不可能再犯的锁）。

### 5.2 判据 B = PASS：命中预注册的喷管位置

预注册值 **11.6200** 在测量脚本里独立复现为 **11.6210**。

| nx | h | OFF x_sh（参考 12.0） | ON x_sh | 实测位移 | 占预测 −0.379 的比例 |
|---|---|---|---|---|---|
| 100 | 0.20 | 11.8698 | 11.7010 | −0.1689 | 45% |
| 200 | 0.10 | 11.9315 | 11.6807 | −0.2508 | **66%** |
| 400 | 0.05 | 11.9616 | 11.7776 | −0.1840 | 49%（★ 两条腿都未收敛） |

**方向每一级都是 UPSTREAM ✓。** 位移只到 45–66%，原因**测得清楚**：求解器检测到的 M₁ 被
抹开的激波压低（1.24435 / 1.25904 / 1.27633 vs 参考 1.28024），所以施加的 σ 偏弱
（0.98780 / 0.98581 / 0.98326 vs 0.982645）—— 而这两个量都**单调收敛到真值**。

把这两件事分开（用"求解器自己那个 σ"反解出同口径参考）后，**纯位置误差是亚单元的**：

| nx | 同口径参考 x_s | 位置误差 |
|---|---|---|
| 100 | 11.7470 | **−0.0461（−0.23 单元）** |
| 200 | 11.7000 | **−0.0193（−0.19 单元）** |

⇒ **机制本身准确到亚单元**，阈值是 2 单元。
★ nx=400 **两条腿都未收敛**：这是判据台自己的既有限制（GS1.1 的committed sweep 里
nx=400 就是 `converged: False`、res 1.885416e-08、x_shock 11.96161），我的 OFF 腿
**逐位复现**了那两个数 —— 所以 B 判在收敛的那两级上，nx=400 如实记为未收敛。

### 5.3 判据 C = PASS：Jacobian 对冻结-σ 系统 FD 精确

| ε | ‖Jv−FD‖/‖FD‖ |
|---|---|
| 1e-6 | **2.570e-08** |
| 1e-7 | 2.922e-07 |
| 1e-8 | 3.046e-06 |

**spread(1e-8/1e-6) = 118.5 ≈ 1/ε ⇒ FD 舍入，不是缺项**（B19 判别式；缺项会给出与 ε
**无关**的相对误差，B19 当年实测 spread = 1.00）。
★ 我第一版把 `< 1e-6` 套到了每个 ε，于是 ε=1e-8 处"失败"——那里光舍入就有 3e-6。
按预注册的两条（sweet spot 处 < 1e-6 **且** 随 ε 变化）重写断言后过。

### 5.4 判据 D = PASS：默认关闭逐位不变

| 项 | 结果 |
|---|---|
| `bitcheck` | **10/10 逐位相同** —— 与改动前 commit a2cb9c3、**同 8 线程、同网格**的有效 A/B |
| M1a 锁 | 3 passed + 1 skipped（门控 fine），数字未动 |
| `bench --compare` | 数值类指标与 a2cb9c3 **完全一致**；`n_newton 17→18` 与时间漂移已归因（§4.3） |
| 全套测试 | **673 passed / 28 skipped / 2 xfailed，0 失败**（910 s @8t；+16 = 新增的 16 个测试） |

### 5.5 判据 E = **FAIL（如实记录）**：σ 的不动点是极限循环

σ 刷新**不收敛**。coarse M0.7875 上一个单元在"激波后"集合里反复进出：
`n_shock` 73 ↔ 74、`max|Δσ|` 钉在 **2.9e-2** 不降、`σ_min` 在第 7 位交替，
残差停在 **~5e-6**，80 步用完。**这是项目已知的"选择churn极限循环"类**
（第一阶段 B15/B21）：离散选择不是 Newton 能解开的东西。

处置 = 项目对同类问题的既有做法：**刷新到第 8 次就停，尾巴在一个固定的光滑系统上收**
——于是 **12 步收到 5.25e-12**（比 OFF 的 23 步还少）。这不是松弛因子也不是限幅
（§2.4 禁止的那两样），它是把"冻结选择"这个既有纪律用到同一类对象上；而且预注册本来
就写了"σ 冻结整步"。**但极限循环本身是一个发现，按 E 记 FAIL**，升级路线登记：
把 ∂σ/∂φ 也求导，或把激波后集合光滑化。

★ 连带一个诚实的说明：`m1_max` 报的是**最后一次刷新时**的值，不是最终状态的
（fine 上 1.4080 > 最终 M_max 1.37459 就是这个原因）—— 冻结带来的固有小不一致。

### 5.6 判据 F = **PASS**：三级 cl 跳幅从 +40.5% 掉到 +5.73% / +11.46%

M0.7875 / α1.25、C = 1.5、同一套自适应延拓：

| | coarse | medium | fine | 逐级跳幅 |
|---|---|---|---|---|
| 等熵（OFF） | 0.37252 | 0.52343 | (0.55829 @M0.7825，差 0.005 未到) | **+40.5%** |
| **熵修正（ON）** | 0.35298 | 0.37320 | **0.41596** | **+5.73%，再 +11.46%**（总 **+17.84%**） |

预注册要求"至少减半（≤ +20%）"⇒ **PASS**。
★ 附带：ON 的 fine **走到了目标马赫**，OFF 那条腿耗尽 4 次步长细化后仍差 0.005 —— 修正
同时改善了延拓的健壮性。

### 5.7 判据 G = **FAIL（按字面）**：激波仍随加密往下游走

| | coarse | medium | fine |
|---|---|---|---|
| OFF x_shock | 0.5635 | 0.6738 | — |
| ON x_shock | **0.5278** | **0.5750** | **0.6180** |

**每一级的激波都被修正推向上游**（−0.036 / −0.099）✓，**逐级漂移也减半以上**
（+0.110 → +0.047）——但**随加密的方向仍然是下游**，所以按字面判 FAIL，如实记。

### 5.8 判据 H = **PASS**：修正项不随 h 衰减，反而增强

| | coarse | medium | fine |
|---|---|---|---|
| 1 − σ_min | 3.187% | 3.322% | **4.383%** |
| 检测到的 M₁ | 1.3575 | 1.3636 | **1.4080** |
| 激波后单元数 | 73 | 148 | 319 |

⇒ 与人工耗散**正好相反**（Q5 实测那个随 h 消失）。这条正是 §5 支撑 ③ 说的"结构"要求。
★ 同时它给出了残余 +11.46% 的**机制**：**波前峰值马赫本身仍随加密增长**
（1.358 → 1.364 → 1.408），激波越来越强，所以 cl 还在涨。修正拿掉了反馈的大部分，
但没有改变峰值增长本身。

### 5.9 判据 I（只记录）：0/0 闭合结构被**减缓**，未消除

`b = −K·J_ff⁻¹·B`（GS1.3b 的矩阵路线，一次线性解），M0.7875：

| | coarse 1−b | medium 1−b | 相邻比值 | coarse T₀ | medium T₀ |
|---|---|---|---|---|---|
| OFF | 0.031157 | 0.011728 | **0.376** | 0.005893 | 0.003099 |
| ON | 0.033454 | 0.018058 | **0.540** | 0.006001 | 0.003412 |

三级全貌（OFF 的 fine 腿只到 M0.7825，不同马赫、不可比，故不列入比值）：

| | 1−b 三级 | 相邻比值 | T₀ 相邻比值 | Γ* 漂移 |
|---|---|---|---|---|
| OFF | 0.03116 / 0.01173 / — | **0.376** | 0.526 | **+39.7%** |
| ON | 0.03345 / 0.01806 / 0.00972 | **0.540 / 0.538** | 0.569 / 0.599 | **+5.6% / +11%** |

★★ **这是判据 F 用闭合语言表述出来的机制**：0/0 结构**依然存在**（(1−b) 仍每级缩小
约一半），但熵修正让 **T₀ 与 (1−b) 的塌陷速率变得匹配**（0.54 vs 0.57–0.60），
而不修正时是**错配**的（0.376 vs 0.526）—— 根 `Γ* = T₀/(1−b)` 是两者之比，
速率匹配 ⇒ 根收敛；速率错配 ⇒ 根跑掉。所以 F 的收缩不是巧合，它就是这件事。

⇒ GS1.7 的 0/0 风险**被减缓、未消除**：如果以后要把跳幅从 ~11% 再压下去，
这个结构仍然是一个独立的靶子。

### 5.10 附加记录（**不在预注册判据内**）：产品门 M1 第一次有判据通过

`bench/run_m1_gate.py --entropy`（三条判据、工况、C 扫掠、配方一律不动，只换密度关系）：

| 判据 | 等熵（2026-07-29 记录的 FAIL） | 熵修正 |
|---|---|---|
| (a) coarse 激波 vs 0.61±0.02 | 0.6581 **带外** | **0.5992 → PASS** ★ 第一次 |
| (a) medium | 0.8819，未收敛 | 0.7236，未收敛（仍 FAIL） |
| (b) 两级 cl 差 < 3% | **+101.4%** | **+36.3%**（仍 FAIL） |
| (c) coarse C∈[1,3] 内 cl 变化 < 3% | **36.2%** | **6.8%**（仍 FAIL） |
| (c) medium | 0 条腿收敛 | 1 条腿收敛（仍 FAIL） |

**M1 仍然 FAIL（退出码 1），如实记录。** 但差距缩小了 3–5 倍，且 (a) coarse 首次进带。
★ medium 的两条腿要单独说明，不能当结果引用：C=1.0 钳到 M_max = 3.0（未收敛），
**C=3.0 收敛到 cl 0.0708 / M_max 1.24 —— 一个低升力分支**。M1 门是**冷启动**
（按设计不做延拓），而 GS1b.2 的 Q4 已经测出 medium M0.80 需要小步延拓才走得过去；
GS1.1 也早已证明这个问题有多解。所以这是**门的协议**问题，不是修正的结论，如实记录、不追。

## 6. 判定与下一步

### 6.1 判定：**熵修正成立**，A/B/C/D/F/H PASS，E/G 如实 FAIL，I 记录

| 判据 | 结果 | 一句话 |
|---|---|---|
| **A** 0 维恒等式 | **PASS** | 修正后的跳跃 = R–H，0.0000%（M₁ 1.15–1.60），带负对照 |
| **B** 1-D 喷管命中预注册值 | **PASS** | 预注册 11.6200 独立复现 11.6210；位移每级 UPSTREAM；同口径位置误差 **−0.23 / −0.19 单元**；nx=400 两腿皆未收敛（既有台限，OFF 腿逐位复现 GS1.1） |
| **C** Jacobian FD 精确 | **PASS** | 2.570e-08 @ε=1e-6，spread **118.5 ≈ 1/ε** ⇒ 舍入不是缺项 |
| **D** 默认关闭逐位不变 | **PASS** | bitcheck **10/10**（有效同环境 A/B）；M1a 不动；bench 数值指标与改动前一致；全套 **673+28+2，0 失败** |
| **E** σ 刷新收敛 | **FAIL** | 不动点是**极限循环**（一个单元反复进出激波后集合，Δσ 钉在 2.9e-2，残差停 5e-6）；按既有"冻结选择"纪律封顶到 8 次后 **12 步收到 5.25e-12**；升级路线登记 |
| **F** 三级 cl 跳幅至少减半 | **PASS** | **+40.5% → +5.73% / +11.46%**（总 +17.84%）；ON 的 fine 还多走到了目标马赫 |
| **G** x_shock 随加密转上游 | **FAIL（按字面）** | 每级都被推上游（−0.036 / −0.099）、逐级漂移减半以上（+0.110 → +0.047），但**方向仍是下游** |
| **H** 修正项不随 h 衰减 | **PASS** | 1−σ **3.187 → 3.322 → 4.383%**，随加密**增强** —— 与人工耗散正好相反 |
| **I** 0/0 闭合结构 | 记录 | 结构仍在，但 T₀ 与 (1−b) 的塌陷速率**变匹配**（0.54 vs 0.57–0.60，原来 0.376 vs 0.526）⇒ 这就是 F 的机制 |

**预注册的 kill criterion 没有触发**（它要求 A–E 全过而 F 不过）：F 过得很干脆。
所以 **B 路线不是负结果 —— 它成立**，并且留下两个**已命名**的残余：

1. **σ 的离散选择churn**（判据 E）—— 靶子明确：把 ∂σ/∂φ 求导，或把激波后集合光滑化；
2. **残余 +11.46%**，机制已测明：**波前峰值马赫本身仍随加密增长**（1.358 → 1.364 → 1.408），
   修正拿掉了反馈的大部分但没改变峰值增长；闭合侧 (1−b) 仍每级减半（判据 I）。

### 6.2 旗标的处置 —— **需要你裁决**（roadmap 原则 4 不允许留永久 default-off 旋钮）

`entropy_correction` 现在默认 **False**、逐位不变。原则 4 说它"要么成为默认，要么在验收后删掉"。
我的建议是**成为默认**（它是物理上更对的密度关系，1-D 上命中外部锚点，翼型上把发散压掉 7 倍，
且不随 h 衰减）。但这会**移动第一阶段每一个已提交的跨声速门禁数字**，属于 B20 那一类的
采纳裁决，所以我**没有自作主张**：旗标保持 default-off，等你点头。

### 6.3 下一步（按我的建议排序，你可以改）

1. **GS1b.4 = 修 σ 的 churn**（判据 E 的升级）：把 ∂σ/∂φ 纳入 Jacobian，或把激波后集合
   光滑化（例如用 ν 开关那样的连续过渡而不是硬的 M>1 判据）。这条能直接换来：
   medium/fine 上不再需要 refresh 封顶、残余跳幅可能再降；
2. **峰值马赫增长**（残余 +11.46% 的机制）——这条要先测清它是物理的还是离散的
   （S2 壁面精度直接相关：峰值在壁面附近，而球面纯 Laplace 就有 11.6% 的 Cp 误差）；
3. **S2（壁面精度）** 仍在 roadmap 顺序上，并且现在有了新的理由（上一条）。

★ 我建议先做 1（便宜、靶子明确、能让 E 从 FAIL 变 PASS），再决定 2/3 的顺序。
