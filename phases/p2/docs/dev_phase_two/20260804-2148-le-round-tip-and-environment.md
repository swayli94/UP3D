# 20260804-2148 — LE-5…LE-15、round-tip 采纳、以及环境自洽化

**这一轮的笔记严重落后**(用户 2026-08-04 指出):从 LE-5 到 LE-15、round-tip 采纳、
以及整个环境工作,十几项发现与用户的六个裁决都没进 note。本文件补齐,并把其中**耐久的规则
写进 CLAUDE.md**(用户要求"写在你的设定里")。

## 0. 用户在本轮做出的六个裁决(全部已执行)

1. **taper 保持 `vanish_smooth`/0.05**,把 **−1.3% cl 偏差写进报告口径**
2. **熵修正保持开启**,并**停止把它描述为稳定性技巧**
3. **不做**固定-Γ 因果检验
4. **所有 gate 用 round tip**(先 LS,后 conforming 一并)
5. **后续所有测试与 demo 不考虑 fine 网格** ⇒ 阶梯 = **(xcoarse, coarse, medium)**
6. **建 `up3d` conda 环境**并配置 VSCode,保证每次运行都在其中

## 1. taper 移除流程:走完,结论是**不能移除**

| 选项 | 裁决 | 依据 |
|---|---|---|
| 移除 taper | **拒绝** | **M0.88 medium 真失败**(6/6 clamps,\|R\| 8.00e-05),而 taper 在同点 \|R\| 1.78e-14 / 0/0 clamps / 墙钟 1/3 |
| 改 r_c=0.025(偏差砍 2.9×) | **拒绝(但理由后来被削弱,见 §4)** | 当时认为它紧邻 [0.030, 0.0375] 死带 |
| 维持 0.05 | **采纳** | |

**分区图景**:M ≤ 0.86 时 taper 不必要(LE-4 的单旋钮 A/B 确证熵修正接管了防发散);
**M0.88 必要**。B32 的归因在 M0.84 上确实已被熵修正取代,**但 taper 在更高马赫仍有真实作用** ——
我先前只测到 M0.84 就准备移除,是覆盖不足。

## 2. 熵修正的机理:我的和用户的假设**都被否证**

LE-9(M0.84 / 翼身 medium,单旋钮):

| 腿 | 熵 | `upwind_c` | 收敛 | nu_max | n_active | M²max | cl_p |
|---|---|---|---|---|---|---|---|
| ref | ON | 1.5 | ✓ | 1.2893 | 106348 | 6.426 | 0.27678 |
| ref | OFF | 1.5 | ✗ | 1.2586 | 48690 | 5.607 | 0.25751 |
| (M2) | OFF | 3.0 | ✗ | **1.9468** | **2498** | 2.571 | 0.22837 |
| taper | OFF | 1.5 | ✓ | 1.2053 | 105409 | 4.594 | 0.27378 |

- ❌ **用户的"额外人工耗散"假设**:`upwind_c` 1.5→3.0 让 nu_max **+55%**,**仍不收敛**,而且把超声速
  单元从 48690 抹到 2498、cl_p −11% ⇒ 它在**消灭激波**,不是稳定它
- ❌ **我的"熵修正削弱激波"假设**:熵 ON 的 M²max **6.426 > OFF 的 5.607**,**峰值更强**

## 3. 定位与配对:两个有产出的轮次

- **LE-11**(残差密度,按体积归一化 + 近场掩码):**83% 的最大残差落在翼尖 10% 内**,argmax 在
  η≈1.001、超声速。**位置不区分成败,幅度区分**:收敛腿 7.5e-04/8.2e-04 vs 失败腿
  3.9e-03/3.9e-03/**1.12e-02**(加耗散最糟)⇒ taper 与熵修正把**同一处**的峰压低 5–15 倍。
  ★ 第一版未归一化,五条腿 argmax 全在远场同一单元(η=8.23)= **体积权重伪影**,已记。
- **LE-12**(配对,同一基线):**翼尖外 corr 0.9978 / k 0.968 / 相对残差 0.065** —— 两个旋钮在
  34.5 万单元上做**同一件事**;**翼尖内 corr 0.429** —— 做**不同**的事。

## 4. ★★ 失效模式分类(用户的方法论纠正)——**它撤回了两个结论**

用户指出:我一直把失效当黑箱(`conv=False, |R|=…, n/m clamps`)去和旋钮做相关,
**从未刻画失效本身**。五种模式签名不同、处置不同(削波 / 极限环 / 矩阵畸形 /
线搜索崩塌 / σ 输运)。**把五种病混成一个标签,任何相关性分析都可能在用一个量分开一堆不同的病** ——
这本身就解释了为什么我今天**五个**假设全灭。

LE-14 加入分类器后(M0.88 / 翼身 medium,r_c 一个旋钮,六个**已知**结果):

| r_c | 结果 | **模式** | 证据 |
|---|---|---|---|
| 0 | ✗ | **clamping** | |
| 0.025 | ✓ | — | |
| 0.030 | ✗ | **clamping** | |
| **0.0375** | ✗ | **budget_limited** | **descent10 = 2.002**(收敛腿是 1e9)|
| 0.045 | ✓ | — | accept_reason=tol |
| 0.05 | ✓ | — | accept_reason=tol |

★★ **撤回 1:"死带 [0.030, 0.0375]" 是两种不同的病拼出来的** —— 0.030 削波、0.0375 只是
**迭代预算不够**(残差仍在下降)。**死带作为我描述的那个东西,很可能不存在**,而"生产值在
安全侧"的论证建立在它之上 ⇒ **该论证撤回**,待 0.0375 的加预算重测。

★★ **撤回 2:"tip 载荷自由度 vs 高马赫收敛"这个统一叙事不成立** —— 三次观察是**不同的模式**。
四个候选量(A |dΓ/dz| 最外 1/2/3、B Γ_tip 对照、C 翼尖残差峰、D 翼尖 M²)**全部重叠、无一分开**,
与分类器的结论独立一致。

## 5. round tip:采纳完成,但是一个**真实的取舍**

| | flat tip | round tip |
|---|---|---|
| **LE 带收敛阶** | 0.37 **(前提失效:加密下发散 p=+0.321)** | **0.87**(与 2.5-D NACA 的 0.85 一致) |
| **LS medium 跨声速包线** | M0.84 | **M0.70** |
| B7 coarse clamps | 0/0 | 0/2 |

⇒ **"三维 LE 带远差于二维"是平面翼尖造成的,不是三维性。** 而代价是跨声速包线。
**LE-13 判定它不是标定问题**(freeze_tol 1e-4/1e-5/1e-6 与 dm 减半四条腿全部到不了 M0.84,
且三条给出**逐位相同**的 m_final/clamps/|R| ⇒ 旋钮没有咬合)。
⚠ 但按 §4 的教训,**这条"几何代价"结论同样是在未分类的证据上下的**,LE-15 本要分类它,
被 pyamg 故障阻断 ⇒ **仍待分类,不得当作定论**。

**实现方式**(三个设计决定,第二个反转了我自己的第一版):
1. `wing3d` 的 `tip_cap` 默认 **flat → round** —— 挡住"没注意默认"的调用者(**我就是**)
2. **标准族名 = round,flat 退为显式 `_flat` 层名**(第一版把 `onera_m6` 钉死为 flat,会让
   conforming 全部 gate 留在 flat 上并需要改 6–7 个测试文件)
3. **P13 / M5 显式指向 `_flat`** —— **在翻转前查到**:否则它们会变成 round-vs-round 测
   "flat 发散",**且不报错**

**阶梯 re-spec**:`(xcoarse_ss, coarse_ss, medium)`,三处常量已改(`onera_m6` /
`onera_m6_roundtip` 的 `RICHARDSON_LADDER`、P13 的 `LADDER`)。它**顺带解掉**了 fine 退出后
P13 需要 `fine_flat` 的难题。

## 6. 环境:`base` 自相矛盾,`up3d` 建立

- ❌ **我先前两个诊断都错了**:(a) 不是"pip 遮蔽 conda"(sys.path 里 anaconda 在 user-site
  **之前**);(b) **不是**"钉住 scipy 1.11.4 保数值不动" —— 代码 `spla.cg(rtol=…)` /
  `gmres(rtol=…)` **无版本分支**,而 `tol→rtol` 是 scipy **1.12** 引入的 ⇒
  **scipy ≥ 1.12 是代码要求**,证据不可能产在 1.11.4 上。第一版 up3d 钉 1.11.4 → **81 failed /
  50 errors,全是 `TypeError: cg() got an unexpected keyword argument 'rtol'`**。
- **base 的真实状态**:anaconda scipy 1.11.4(2024-02-28,`cg` 无 `rtol`)+ user-site pyamg
  5.3.0(需 scipy≥1.12)+ user-site 一个**残缺的 scipy 目录**(只剩 `stats`,时间戳 2026-08-04
  19:57)⇒ **自相矛盾**。conda 修订历史只有一次(初装)⇒ **base 不是被 conda 改坏的**。
  ★ 残缺 scipy 的成因**未完全解释**,如实记为已知未解。
- **`up3d` 采纳判据 = 全套**:**676 passed / 8 failed / 28 skipped / 2 xfailed @14:33**
  (numpy 2.4.6 / scipy 1.17.1 / numba 0.66.0 / pyamg 5.3.0),**8 个失败逐条归因,没有一个是
  数值漂移**:5 个是 round-tip 切换的预期后果(m5 断言"默认是 flat"、p13 翼尖局部性、
  v5 翼尖 mask ×2)、2 个是 up3d **缺 pandas**、1 个(`test_a1_instrumentation::
  test_conforming_picard`)待查。
- **隔离必须靠 `PYTHONNOUSERSITE=1`**:实测不屏蔽时 up3d 里 `import pyamg` **仍然失败**
  (user-site 5.3.0 遮蔽 env 的版本),屏蔽后九个 import 全部落在 up3d。**建环境本身不够。**

## 7. 未完成(按优先级)

1. `environment.yml` 补 **pandas**;修 m5/p13/v5 的 round-tip 断言;查 a1 那条
   > ★ **a1 那条已根因 2026-08-05**:`test_a1_instrumentation::test_conforming_picard` 的
   > `other < 20% * wall` 断言在**numba 缓存冷**时必然失败(编译时间落进 `other`)——
   > 删掉 `pyfp3d/kernels/__pycache__` 即**按需复现**(35.4% 未计入),热缓存立刻通过;
   > 用**修复前**的熵内核冷跑同样失败(32.2%)⇒ **与任何一次改动无关,是这条断言本身的脆弱性**:
   > 它断言的其实是**编译**,不是它要检查的账目完整性。**这也解释了为什么新建的 up3d 环境
   > 会在这里报失败而热仓库不会** —— 新环境的缓存按定义全是冷的。修法:模块级 autouse
   > fixture 先跑一次丢弃的求解把内核编译掉(本模块四个驱动共享 assembly/upwind/entropy/
   > linear-solve 内核,一次即可);冷缓存下 `tests/test_a1_instrumentation.py` 现 **7/7 通过**。
2. **LE-15**:用分类器分类 round-tip 的包线退步 —— §5 的"几何代价"结论**悬而未决**
3. r_c = 0.0375 的**加预算重测** —— 决定"死带"是否存在
4. round-tip 切换的 erratum 清单 + 五面收口(discipline #11)

## 8. 本轮我的错误(共同模式)

**凭记忆写签名/名字,而不是读**(五次):`pyfp3d.wake`(应 `pyfp3d.constraints.wake`)、
`phi_init` 放顶层(应在 `newton_kw` 内,害得 LE-4 首跑 5 条腿全 TypeError)、
`cap.ls_wing_ramp`(不存在)、`density_ratio`(应 `density_field`)、
`rho_tilde` 传九个位置参数(只收八个,把 3.0 塞进 gamma)。

**pgrep/pkill 自匹配**(两次):`pgrep -f "pytest tests/"` 匹配到自己的等待命令 ⇒ 反复误读
"仍在跑";`pkill -f run_le5_taper_coverage` **杀掉了自己的 shell**(exit 144)并让脚本没写成。
⇒ 现在**按 PID 杀**。

**缓存不全导致重复重解**(三次):LE-10 只存 φ ⇒ LE-11 无法重建完整残差;LE-14 首launch 又漏
诊断历史 ⇒ 跑 5 分钟即停掉重启。⇒ 现在 **φ + γ + 诊断历史一起缓存**。
