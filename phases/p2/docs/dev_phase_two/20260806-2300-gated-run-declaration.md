# 20260806-2300 — 门控全套运行声明(**写在启动之前**)

⚠ 时序如实说明:本文件写在**重启那条命令内、启动之前**(与 `kill` 同一条命令),
但**提交**是启动之后几分钟。所以它满足「事前写下」,不满足「事前提交」——
如实记,不含糊。

2026-08-06 22:5x。目的:测量 round-tip 切换(2026-08-04)在**门控集**里留下多少 erratum 账单
—— 今天只跑了 3 个文件就发现 2 条能力锁全红,其余 14 个带门控标记的文件未测。

## 本次运行的已知污染,以及它如何影响判读

启动时 `uptime` 报 **load average 14.37**,启动后升到 **31.32**(16C/32T 机器)⇒ **有他人负载约 15**。
按纪律 #1,重负载在场时应降到 8 线程 —— 我第一次启动用了 16,**看负载看晚了**,已按 PID 杀掉重启。

**执行前声明(不得事后放宽)**:

| 失败类型 | 本轮如何判读 |
|---|---|
| 断言里提到 **wall / budget / 秒数** | **作废,不算发现** —— 负载污染;记为"待在空闲机器上复测" |
| **锚点值**(cl / γ / M_max / x_shock / 残差 / clamp 数) | **算发现** —— 与负载无关 |
| 收敛 / 不收敛 | **算发现**,但若同一测试同时有墙钟断言,须先确认失败的是哪一条 |

⇒ 本轮的**目标量是锚点值**,而锚点值对负载不敏感,所以这次运行仍然有效;
只有墙钟那一类要另跑。

## 线程

8 线程(`NUMBA_NUM_THREADS=OMP_NUM_THREADS=OPENBLAS_NUM_THREADS=8`)。
⚠ 因此**所有墙钟数与 16 线程的既有记录不可比** —— 这与 GV5.x 那批 8 线程会话同样的口径。
