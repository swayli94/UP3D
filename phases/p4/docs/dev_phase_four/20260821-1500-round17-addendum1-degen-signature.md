# 20260821-1500 — **第十七轮 附录 #1**：G-DEGEN 找错了特征；比较站也随之搬（**比较尚未做**）

**性质**：预注册附录。**在做 Z-SEED 的比较之前提交**。第一次执行的输出**保留不改写**。

---

## 1. 第一次执行报了假 MISMATCH

**G-XTON 通过**：请求 `XTR 0.05370`，XFOIL 的 `Top_Xtr = Bot_Xtr = 0.05370` —— **正是请求值**。
**而 G-DEGEN 报 MISMATCH**：它找到的"第一个湍流站"是 `x/c 0.05862`，不是 0.05370。

★★ **G-DEGEN 找的是 `H` 塌陷。而按本轮自己 §0 的机制，那恰恰是错的特征** ——
`XT = X2` 且湍流子区间**长度为零** ⇒ **该站的 `θ`/`H` 仍是层流值**，
**只有 `CTAU` 变成 `ST`**。⇒ **`H` 在那一站按构造不会塌陷。**

**实测证实机制成立**（`XTR = 0.05370` 的 dump）：

| `x/c` | side_b `H` | side_b `CT` |
|---|---|---|
| 0.04916 | 2.4000 | **0.000000** |
| ★ **0.05370（= XTR）** | ★ **2.3980（仍是层流值）** | ★ **1.203100e-02** |
| 0.05862 | 2.1560 | 3.869700e-02 |
| 0.06391 | 1.7990 | 4.950700e-02 |

⇒ **`x/c 0.05370` 的 `CT` 就是 `ST = CTR·CQ`，而它的 `(θ, H)` 是层流值 —— 正是 §0 预言的。**

## 2. 修法（两处，**都只改仪器不改判据**）

| | |
|---|---|
| **G-DEGEN** | 改为断言**两条**：① 请求站的 `H` **仍在层流平台**（与上一站差 < 0.05）；② **下一站** `H` 开始塌陷（> 0.1） ⇒ **`CT` 的语义正是在请求站翻转** |
| ★ **比较站** | Z-SEED 在**请求站**（`x/c 0.05370`）上比，**不是**在第一个 `H` 塌陷的站上 |

★ **判据 Z-SEED 的措辞一字不改**（"我们的 `S_tr` 对 XFOIL 的 `CT`_站，2 % 带"）——
改的是**"站"指哪一站**，而那由 §0 的机制**唯一确定**，不是我挑的。

★ **G-FRAC 同样重算**：`XT` 与请求站的距离占区间的比例 —— `Top_Xtr` 与请求值相同到打印精度
⇒ 预期 `f ≈ 0`。

## 3. 诚实交代

★★ **XFOIL 侧的数我已经看到了**（上表的 `1.203100e-02`），**但我们侧的值还没算，比较尚未做**。
本附录在**做比较之前**提交。

## 4. 追加事前预测

4. ★★ **Z-SEED 进带** —— 与第十六轮预测 2 同一条依据（那 10 % 只需 `H` 偏 2 %，本轮用 XFOIL 自己的 `H`）。
   ★ 现在有一条**新的、更强的**依据支持它：`CT` 在请求站 = **1.2031e-02**，
   而 `√CτEQ` 在那个状态量级 **~0.065**（层流 `H ≈ 2.4`）⇒ `CTR ≈ 0.185`，
   与 `CTRCON·exp(−CTRCEX/(H_k−1))` 在 `H_k = 2.398` 处的 `1.8·exp(−3.3/1.398) = 0.171` **同量级**
   ⇒ ★ **量级已经对上，剩下的是它是否在 2 % 内。**
5. ★ **side_a 的 `CT` 在 `XTR` 之前非零**（0.0264 / 0.0848 = 放大因子 `ñ`），
   而 side_b 为零 ⇒ **两侧的自由转捩进程不同**，但**不影响 Z-SEED**（它只用请求站）。
