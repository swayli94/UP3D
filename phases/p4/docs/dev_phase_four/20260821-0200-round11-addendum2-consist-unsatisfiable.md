> ★★★ **勘误（同日，重跑之后）：本附录 §1 的诊断是错的。**
> 解析 `u_e` 换上去之后 X-CONSIST **仍然失败**，而且**更差**（地板 5.5e-3 对 2.0e-3）。
> 我那次诊断**一次改了两件事** —— 换插值**同时**换了种子（`H` 1.823 而不是转捩站的 2.303）。
> 2×2 实测：四条腿**全部**有地板，解析腿反而更高 ⇒ **插值不是原因**。
> 真正的原因见[第十一轮判定](20260821-0300-two-point-verdict.md)：**动能方程不一致**。
> **本文其余内容保留不改写**；§3（接受条件读残差不读 `sol.success`）与 §4 仍然成立。

# 20260821-0200 — **第十一轮 附录 #2**：X-CONSIST 按附录 #1 的写法**原理上不可满足**；两处修正

**性质**：预注册附录。**在重跑之前提交**。第一次执行的输出（含 kill 条开火）**保留不改写**。

---

## 1. kill 条开火了，而诊断说**转录是对的**

第一次执行：X-CONSIST 在真实 `u_e` 上 **2.651e-02 → 3.789e-03 → 1.951e-03 → 2.010e-03**
⇒ **x4→x8 不再下降** ⇒ **kill 1 开火，整轮停在解释任何东西之前**。★ 守卫按设计生效。

**诊断（不猜，实测）：**

| 位置 | 实测 |
|---|---|
| **下游站** | `dH[-1]` **8.25e-5 → 6.68e-6 → 5.58e-6 → 7.56e-6** ⇒ **两套格式确实收敛到同一连续解** |
| **前 1–4 站** | 残差集中在这里，且**不随加密消失**（`x/c 0.0586–0.0820`） |
| ★★★ **换成解析 `u_e`（`m = −0.08`，非 ZPG）** | **1.010e-04 → 2.785e-05 → 7.312e-06 → 1.872e-06**，比值 **3.63 / 3.81 / 3.91** ⇒ **阶 = 2.0**，正是预测 7 |

⇒ **`BLDIF` 的转录没有问题。地板在 `u_e` 的插值里。**

★★ **机制**：两套格式**消费 `u_e` 的方式不同** —— 两点格式用站点端值的 `ln(u2/u1)`，
RK4 积分同一条 PCHIP 的 `du_e/dx`。**PCHIP 只有 C1，二阶导在节点上跳**，
而**加密站点并不会让插值本身变光滑** ⇒ 两种消费方式的差**不随站点加密消失**。
**那是共享输入的性质，不是任何一套格式的性质。**

## 2. ★★★ 判据缺陷 #7：**我注册了一个正确转录也无法满足的判据**

附录 #1 把 X-CONSIST 从合成算例**搬到**真实 `u_e`，理由是覆盖（ZPG 下 `ULOG` 项全熄火）。
**理由成立，做法错了** —— 它把一个**不随加密消失的共享输入误差**放进了一个**要求随加密消失**的判据。

★ 家族仍然是那一条：**判据没有覆盖它所比较的量的定义域**。
这次的新面孔：**比较两套离散时，经由一个自身误差不随加密消失的共享输入**。

**修法**：X-CONSIST 回到**解析 `u_e`，但取 `m ≠ 0`** ——
★ 覆盖问题**照样解决**（`m = −0.08` ⇒ `ULOG ≠ 0`，三条残差里每一项都点着），
**而插值被整个去掉**。★ 这**不是**事后放宽：判据（随加密单调下降 + 报阶）**一字不改**，
换掉的是**输入**。

## 3. 第二处：**接受条件读了 `sol.success` 而不是残差**

`x16` 那条腿抛了 `two-point step failed at X = 0.065606, |F| = 9.637e-16` ——
**残差已在机器零，`scipy` 的 `hybr` 仍报 `success = False`**（`xtol` 在那个尺度上不可达）。
⇒ **接受条件改成残差判据**（`|F| <= 1e-12`），`sol.success` 只作打印。

★ 这是本项目已记过的那一族：**报告层要像求解器一样加守卫** ——
一次已经跑完的求解被一个**报告层**的判断丢掉。

## 4. 对 X-ANSWER 的附带说明（**写在重跑前，免得事后被当成解释**）

★★ X-ANSWER 的两点臂跑在 **XFOIL 自己的站点**上，**只用站点上的 `u_e` 值,完全不插值**
⇒ **它消费的正是 XFOIL 消费过的东西**。而 RK4 臂**必须**插值（它要 `du_e/dx`）
⇒ **RK4 臂天生带着 §1 那个插值地板，两点臂没有**。
**这一条在判定里必须写明，且不得用来解释掉 X-ANSWER 的任何失败。**

## 5. 事前预测（重跑前）

8. **X-CONSIST 通过，阶 ≈ 2.0**（已在诊断里见到，故这条**不算独立预测**，如实标注）。
9. ★★ **X-ANSWER 的两点臂明显优于 RK4 臂** —— 两个理由叠加：它用 XFOIL 的格式，**且**不插值。
   ★ **但仍不进带**（预测 2 不改）。
10. ★ **X-C3 的读数会被 §4 那条污染** —— 它量的是 `|两点 − RK4|`，其中含**插值**这一项，
    **不是纯离散** ⇒ 判定里必须把 X-C3 标成**「离散 + 插值」的合并读数**，
    并把 §1 测到的插值地板（**~2e-3**）作为它的**下限**一并给出。
