# 20260818-2100 GS4.1 第一轮 — **执行附录 #3**（第一次执行挂死；参照臂的机械缺陷，重执行前提交）

**性质**：执行附录，**在重执行之前**提交（入口文书 §7.2）。**不动任何判据、守卫、kill criterion。**
改的是**参照臂**（解析预言机）里的一个机械缺陷，与被测对象（条带核）无关。

## 1. 发生了什么

第一次执行**挂死**（> 6 min 无输出，被我主动杀掉，**没有产出任何行**）。
定位：**不是条带核**，是本脚本自己的 Falkner–Skan 打靶。
`f''' + f f'' + β(1 − f'²) = 0` 在 **β = 1** 上，试射值偏离根时 `f'` 会在 `eta_max` 之前就发散
（`f'` 冲高或塌负），而 `DOP853 @ rtol 1e-12` 去积分那条发散解会把步长磨到无穷小。
实测：`β=0` 的两个端点各 0.003 s / 0.006 s，**`β=1, s=0.1` 60 s 未返回**。

## 2. 改了什么

给打靶加**终止事件**（`f' > 1.5` 或 `f' < −0.2` 即停），残差在**实际到达的端点**上读
⇒ 符号仍然正确（冲高 → 正，塌负 → 负），brentq 的括号不变。

**修复后的自检**（★ 这是 G-ANALYTIC 的要点：数是**在脚本里积出来的**，不是我记的）：

| m | β | f''(0) | H | c_f√Re_x |
|---|---|---|---|---|
| 0 | 0 | **0.469600** | **2.591100** | **0.664115** |
| 1/23 | 1/12 | 0.568934 | 2.495664 | 0.821899 |
| 1 | 1 | **1.232588** | **2.216229** | 2.465175 |

⇒ 与教科书值（Blasius `0.4696 / 2.591 / 0.664`、Hartree `β=1: 1.23259 / 2.216`）**逐位吻合**
⇒ 预言机可信。**这些教科书值是打印出来的自检，不是判据**（预注册 §3 G-ANALYTIC 原文）。

## 3. 记账

- 第一次执行**未产生任何读数**（挂在 F-SIMILAR 的 m=1 参照上，此前的 V-BLASIUS / V-ORDER 输出被
  `| tail` 缓冲吞掉，**未落盘**）⇒ **没有"看了数字再改协议"的可能**；重执行用 `python -u` 直接落盘。
- ★ 机时记账：这次挂死约 **6.5 min**，**计入** kill 条 4 的 20 min 预算（它就是本轮的机时），
  收口时连同重执行一起报。
- ★ 教训归档（与本项目已记的同族）：**参照臂也要干跑**。我对被测对象（条带核）做了返回数/参数类型的干跑，
  却把**预言机**当成"标准库调用不会出错"直接下场 —— 挂死的正是它。
