# 20260820-1700 — **第九轮 B 腿 附录 #3**：L-TURB 注册的是**逐站**，我实现成了**中位数**（重跑前）

**性质**：预注册附录。**在重跑之前提交**。第一次执行的输出保留在下文，**不改写**。

---

## 1. ★★★ 这是第七轮那条缺陷**原样重演**

预注册 §4 的 L-TURB 写的是：

> 湍流段 `θ/H/c_f` 对 XFOIL **逐站**在推导带内

我实现成了 **`median(|dev|) <= band`**。第一次执行因此报 **L-TURB PASS**，而同一行的实测里写着
`cf max 101.34 %`、`ctau max 270.17 %`。

★★ **弱实现会通过** —— 这正是第七轮 R-STATIONS 的失败模式
（**注册「站点逐一对应」、实现「行数相等」**），当时已写成常设条：
**「实现每条判据时逐字回读它注册时的措辞」**。★ **本轮我又没做。**
⇒ 常设条不够，**得有机器检查**：判据函数里**打印注册措辞**并断言实现形式（见 §4）。

## 2. 同时修：G-LEGACY 的**带写错了**（检查是对的）

第一次执行打印 `band=2.590433 / 1.2932778384340817  measured=2.590433 / 2.590433`。
★ 检查跑的是**层流**行进（`x_tr=None`），正确答案就是 `2.590433`；
是我把**湍流平板**的端点 `1.29328` 抄进了带的文字里。
★ 而且判定逻辑**只比了第一个数**，第二个数打印出来但**没进断言** ⇒ **半个检查**。
⇒ 带的文字改对，**两个数都进断言**，并**另加**湍流平板端点 `1.2932778384340817` 作第三条。

## 3. 修法（L-TURB）

**逐站**：每个量、每一站都必须在带内；报**出界站数**与**最差站的位置**。
带不变：`max(10 × L-FLOOR, 2 %)`（两侧同式 ⇒ 地板主导）。

★ **反向结果落在哪**：出界 ⇒ 我们的湍流行进与 XFOIL 不同 —— 而这**几乎必然发生**，
因为第一次执行的最大偏差已经摆在那里。**本附录不是为了让它通过。**

## 4. 新守卫 G-ASREGISTERED

判据函数**打印它注册时的原文**，并断言实现的**形式**（逐站 = 对每站求值并计数，
而不是先聚合）。★ 具体做法：逐站判据必须返回 `(n_inside, n_total, worst_station)`，
**没有这三元组就不是逐站实现**。

## 5. 事前预测（重跑前）

9. ★★ **L-TURB 逐站会 FAIL**，出界站**集中在转捩后一小段**（第一次执行的最差 5 站
   全在 `x/c 0.049–0.082`，而转捩在 0.045/0.054）。
10. ★ **下游（`x/c > 0.1`）全部在带内** ⇒ 失败是**局部的**，不是整段偏移。
11. **L-LAM / L-LAG 的判定不变**（本附录不碰它们的实现）。
