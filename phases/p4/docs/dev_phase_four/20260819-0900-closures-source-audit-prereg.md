# 20260819-0900 — **GS4.1 第四轮**预注册：`closures.py` 对其装订出处的**源码审计**

> ★★★ **勘误指针（第五阶段第二轮 F1，2026-08-22）**：本文引用的**型线族不动点**是
> **pre-F1** 的值。`closures.py` 的层流求积由 **8 点改为 24 点**后（F1，
> [判定](../../../p5/docs/dev_phase_five/20260822-0400-f1b-verdict.md)），它们移动为：
> `A` **8.02881134 → 8.028406562**（−5.0e-05）、`H` **2.708292 → 2.707931**（−1.3e-04）、
> `c_f√Re_x` **0.710235 → 0.710301160**（+9.3e-05）、
> 楔 `m=0.0435` **2.469411 → 2.469240**、`m=1` **2.087933 → 2.087926**、
> 对 Blasius 的差 **0.04523 → 0.0450893**。
> ★ **本文的数一个都不改写** —— 它们是 pre-F1 的记录。
> ★★ **结论未变**：全部移动 **≤ 1.3e-04**，而它们上面压着的模型误差是 **4.5 %** 的
> Blasius 地板 ⇒ **"型线族留不住 Blasius"这条结论原样成立**。

**性质**：预注册。**在任何代码之前提交**（roadmap 原则 8 / 入口文书 §7.2）。
**使用者裁决 2026-08-19**：选 **C** —— **先 B（本轮审计）后 A（湍流相关式）**。
A 的材料已到位（`reference/drela-giles-2012-*.pdf` = AIAA-J 相关式原文、`reference/XFOIL6.99.zip` = 闭包子程序），
**A 不再是堵点，但它排在本轮之后**。

**装订出处**：`reference/drela-2013-three-dimensional-integral-boundary-layer-formulation-for-general-configurations.pdf`
（AIAA 2013-2437，24 页；★ 目录 `reference/` **已 gitignore**，版权材料不进仓库）。

---

## 0. 为什么这一轮值得做，以及它可能推翻什么

`closures.py` 是 Track V 的**地基**：V1→V6、GS4.1 前三轮的每一个数都经过它。
而它的装订出处**在此之前一直不在工作树里**（gitignored）⇒ **这份库从来没有被对着源头核过**。

★★★ **本轮完全可能推翻一条立了三周多的结论**：第一轮测得型线族的 ZPG 不动点
`H = 2.708292`（+4.52 %）、`c_f√Re_x = 0.710235`（+6.94 %），并把它归因为**家族的性质**。
**若审计发现 `closures.py` 的型线转录有误，那条结论就变成实现缺陷** ——
它会同时影响 GV1.1 (a) 的 FAIL（2026-07-22 起）与 GS4.1 第一轮的整个判定。
**这是本轮的反向结果，而它是真实可能的**（第 1 问）。

## 1. ★★ 已经做过的抽查（**执行前披露**，不假装盲测）

在写本预注册之前，为判断这一轮是否可做，我已抽查三处（纯阅读，零机时）：

| 论文 | 库 | 抽查结果 |
|---|---|---|
| eq (46) `f0..f3`（含 `(1−η)²` 因子）+ 四条 η-导数 | `closures.py:145-158` | **吻合** |
| eq (42) `U = A(1−0.6(A−3)η³)f1 + f0` | `:183-187` | **吻合** |
| eq (51) Spalding `y⁺(u⁺)` | `:220-235` | **吻合** |

⇒ **Tier 1 的层流侧很可能 MATCH**，本轮的价值因此**更可能落在湍流侧、积分定义与常数上**。
★ 照第一轮附录 #1、第三轮 §1 的做法在执行前写下，**不改判据**。

## 2. ★★★ 本轮最容易犯的判据缺陷，事前堵上

**审计的样本集必须在检查之前定死。** 否则我会自然地"查到累了就停"，
而停下来的位置**恰好是我查过的都通过的位置** ⇒ 结论不可证伪。
这是入口文书 §5 **第 2 问**（自变量是否改变样本存在性）在审计场景下的形态。

⇒ **§3 的清单是完整的、封闭的、在看任何结果之前写下的。**
执行时**逐条都要出一个判定码**，包括我查不动的那些（`UNCHECKABLE`）。
**不得增删条目**；若执行中发现遗漏了某条关系，**记为附录并说明**，不静默补进去。

★ 第二个坑（**第 7 问：守卫会不会量到自己**）：
**论文侧的实现必须与 `closures.py` 零共享代码** —— 若我用库自己的 helper 去算"论文侧"，
那就是拿库验证库。⇒ **论文侧从 PDF 正文重新独立实现**，数值比对。
（同第一轮"用 brute-force oracle 证明内核性质、与实现不共享代码"的纪律。）

★ 第三个坑：**PDF 文本抽取会把数学搞坏**（实测已见 `¯¯K`、断裂的分式）。
⇒ **`UNCHECKABLE` 是一个正当判定码**。**看不清的公式就报看不清，不猜** ——
上一轮我拒绝去凑湍流常数，同一条纪律在这里同样适用。

## 3. 审计清单（**封闭，执行前固定**）

### Tier 1 — 型线族（地基）

| # | 论文 | 库 |
|---|---|---|
| A1 | eq (46) `f0..f3` + η-导数 | `_lam_f0123` |
| A2 | eq (42) `U(η;A)` | `_lam_UW` |
| A3 | eq (43) `W(η;B,Ψ)` | `_lam_UW` |
| A4 | eq (44)(45) 层流 `S, T` | `_lam_UW` / `closure_node` |
| A5 | eq (51) Spalding `y⁺(u⁺)` | `_spalding_yplus` |
| A6 | eq (52) Coles `g_o = 3η²−2η³` | `_turb_UW` |
| A7 | eq (47)(48) 湍流 `U, W` | `_turb_UW` |
| A8 | eq (49)(50) 湍流 `S, T` | `_turb_UW` |
| A9 | eq (53)(54) `K, Υ` | `_turb_scales` |
| A10 | eq (55)(56)(57) `y⁺, δ⁺, q_τ, U_τ, W_τ` | `_turb_scales` |

### Tier 2 — 积分厚度与系数

| # | 论文 | 库 |
|---|---|---|
| A11 | eq (60) 全部积分厚度（`δ_ρ, δ*₁, δ*₂, φ₁₁, φ₁₂, φ₂₂, φ*₁, φ*₂, δ'₁, δ'₂, δ_q, δ°_q, θ°₁, θ°₂, δ°₁, δ°₂`） | `closure_node` 的 30 输出包 |
| A12 | eq (61) `C_f1, C_f2, C_D, C_D^×, C_D°` | `OUT_CF1/CF2/CD/CDX/CDC` |
| A13 | `θ₁₁ = φ₁₁ − δ*₁`、`θ*₁ = φ*₁ − δ*₁` | `OUT_TH11 / OUT_THS1` |

### Tier 3 — 密度 / 粘性

| # | 论文 | 库 |
|---|---|---|
| A14 | eq (58) Crocco–Busemann `R` | `_density_R` |
| A15 | eq (59) Sutherland `M` | `closure_node` |

### Tier 4 — 常数（**inventory，RECORDED**）

| # | 论文 | 库 |
|---|---|---|
| A16 | `a1 = 0.15`（p.8） | `A1_BRADSHAW` |
| A17 | Spalding `κ, B` | `KAPPA=0.41, B_SPALDING=5.5` |
| A18 | `r ≃ Pr^{1/2}`（p.13） | `RECOVERY_R=0.85` |
| A19 | `C_τcrit ≃ 1e-4`（p.9 eq 35） | `CTAU_LAM=1e-8` |
| A20 | ★ 耗散长度 `L`：p.9 明写**按 Clauser G-beta locus 标定** | `C_L_DEFAULT=0.09`，注释写的是 **Bradshaw 值** + 项目自己的二维标定 |

### Tier 5 — 机制（**inventory，RECORDED**）

| # | 论文 | 库 |
|---|---|---|
| A21 | 转捩：eq (34)(35) e^N + TS 包络 `f_N`，按 `\|C_τ\|` vs `C_τcrit` 切换（p.9 §7） | 强制转捩（D-TR） |

## 4. 每条的判定码（**五选一，事前定死**）

| 码 | 含义 |
|---|---|
| **MATCH** | 与论文**数学等价**（数值比对通过；★ 不要求文本相同 —— 库可用 Horner 等重排形式，第 5 问） |
| **DIVERGENT-DOCUMENTED** | 有差异，且差异**已记录在项目设计文档里**（`D-*` 决策等） |
| ★★ **DIVERGENT-UNDOCUMENTED** | 有差异且**无记录** —— **这是本轮的靶子** |
| **NOT-IN-SOURCE** | 论文**未规定**，库的选择是项目自己的（**不是缺陷**，但必须知道） |
| **UNCHECKABLE** | PDF 抽取的数学不可靠 ⇒ **报看不清，不猜** |

## 5. 判据

| 判定码 | 条件 |
|---|---|
| **AUD-FAITHFUL** | **Tier 1–3**（数值）全部为 MATCH 或 DIVERGENT-DOCUMENTED |
| ★★ **AUD-DEFECT** | Tier 1–3 中 **≥1 条 DIVERGENT-UNDOCUMENTED** |
| **AUD-PARTIAL** | Tier 1–3 中 **≥3 条 UNCHECKABLE** ⇒ 覆盖率不足以下 FAITHFUL 的结论，**不报方向** |
| Tier 4–5 | **inventory，RECORDED，不进 PASS/FAIL** —— 常数与机制上 NOT-IN-SOURCE 是常态 |

★ **数值比对的容差**：`MATCH` 要求相对差 **≤ 1e-12**（两侧都是双精度、同一公式的不同排列
⇒ 只应差在舍入）。★ **这个数是从"同公式重排的舍入量级"推出来的，不是挑的整数**
（第三轮那两条同族缺陷的修法）；若某条因量级悬殊而不适用，**在判定里单独说明该条的容差与理由**。

## 6. 第八问（极端样本）

| 情形 | 处置（事前） |
|---|---|
| 论文公式在某个 η 上奇异（如 `(1−η)²` 在 η=1） | 比对区间取 **η ∈ (0, 1)** 的高斯点，**不取端点**；打印所用点集 |
| 库把某个量**根本没有实现**（如 `C_D°`） | 记 **NOT-IN-SOURCE 的镜像** —— 新码 **`ABSENT`**，★ 事前加进第 4 节 |
| 某条 Tier-1 关系 UNCHECKABLE | 它**计入** AUD-PARTIAL 的计数；**不得**用"抽查过类似的所以应该没问题"替代 |
| 全部 MATCH | 照报，**但不得**写成"`closures.py` 是正确的" —— 审计只覆盖**清单上的关系**，不覆盖装配、离散化、求解器 |

## 7. Kill criterion（写在开工之前）

1. ★★★ **Tier 1 出现 DIVERGENT-UNDOCUMENTED ⇒ 停，只报不修。**
   修它会移动**每一个已提交的 Track V 数字**（V1→V6 + GS4.1 三轮）
   ⇒ 那是它自己的一轮，且需要一份 re-baseline 勘误清单（纪律 11）。
2. **论文侧实现与 `closures.py` 共享了任何代码 ⇒ 停**（守卫量到自己）。
3. **需要改 `closures.py` / `ibl3.py` ⇒ 停**（冻结令延续）。
4. **总机时 > 20 min ⇒ 停**（本轮应是秒级；口径同前三轮，**不含**收口锁层）。
5. ★ **发现清单遗漏了某条关系 ⇒ 记附录，不静默补进本轮计分。**

## 8. 事前预测（不假装盲测；本季已落空 17 次）

1. **AUD-FAITHFUL**，Tier 1 层流侧（A1–A4）全 MATCH —— ★ 依据是 §1 已抽查 A1/A2/A5。
2. ★ **Tier 1 湍流侧（A6–A10）至少 1 条 UNCHECKABLE** —— eq (47)–(57) 在抽取文本里
   已经明显破碎（`u+S (δ+)`、上下标丢失）。
3. ★★ **A20（`C_L`）= DIVERGENT-DOCUMENTED，而 G-beta 核对 = 从未做过**。
   这是我最有把握的一条"有东西"，也是本轮最可能的**可交付发现**。
4. **A19（`C_τcrit` 1e-4 vs `CTAU_LAM` 1e-8）= DIVERGENT-DOCUMENTED**
   （库注释写着 `<< Ctaucrit`，是有意的）。
5. **A21 转捩 = DIVERGENT-DOCUMENTED**（D-TR），且与 GV3.1 记录的
   "首个转捩后站 `c_f` +44 %"同源。
6. **A17（`κ, B`）= NOT-IN-SOURCE**（论文只说"Spalding law"，未给常数值）。
7. 总机时 < 5 min，零求解。

★ **禁止句三条**：
① 全 MATCH **不得**写成"`closures.py` 是正确的"（只覆盖清单上的关系，§6）；
② **不得**用本轮结果去重新解释第一轮的判定 —— 若 Tier 1 全 MATCH，第一轮的归因**被确认**，
   仅此而已；若不 MATCH，按 kill 条 1 **只报不修**；
③ **不得**把论文的 e^N 转捩与库的强制转捩说成"等价的两种实现"。
