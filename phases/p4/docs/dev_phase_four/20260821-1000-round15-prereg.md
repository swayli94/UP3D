# 20260821-1000 — **GS4.1 第十五轮**预注册：① E-CF 降为 RECORDED（使用者裁决）+ ② **G18**

**性质**：预注册。**在任何代码之前提交**。
**使用者裁决 2026-08-21**：**「把 E-CF 降为 RECORDED；做 G18」**。

---

## ① E-CF 降为 RECORDED —— **裁决的依据是已测的,不是这一轮要测的**

**依据（全部已在册,本轮不重测）**：

1. [第九轮 A 腿](20260820-1400-five-terms-verdict.md) §4：把闭包改得**更忠实于源码**
   （五处缺项照 `xblsys.f` 补齐），与那两条相关式的一致**变差**（62/69 → 43/70）；
2. [第十四轮](20260821-0900-g13-verdict.md) §4：平板出带的 `Re_θ 576–2997` 那**一大段，
   在翼型上（**同族**参照 XFOIL）完全没有对应** —— 翼型侧 **5/214**，且 `θ` 中位 **0.11 %**；
3. 两条相关式的**适用范围本身没有可引出处**在仓库里（`reference/` 只有 Drela 的文献与 XFOIL 源码）。

⇒ **E-CF 量的是"族差异"，不是缺陷；用它判 PASS/FAIL 不成立。**

★★ **降级的三条纪律，事前写死**：
1. **不改窗口、不改带、不改阈值** —— 只把**判定码**从 `PASS/FAIL` 改成 `RECORDED`。
   ★ 改窗口或阈值就是"挑一个能过的下限"，那是被禁的；
2. **数一个都不改写**（43/70、`Re_θ 576–2997`、最差 13.2 % 全部原样留在记录里）；
3. ★★★ **必须同时写下降级**不**意味着什么**：
   **不**意味着"平板侧没问题"，而是"**平板的红不能归给闭包,因为它的参照不合格**"。

★ **重开条件（写下来才算降级而不是删除）**：
若将来补进一条**有可引出处的适用范围**的 ZPG 参照（或带 BL 剖面的实验），
**E-CF 可以按那个出处的范围重新成为门**。

## ② G18：`side_b` 首站 `Cτ` 低 3.8 % —— 种子公式,还是区间分段的残留？

**已知**（第十四轮）：转捩后首站 `Cτ` 比 —— side_a **1.0070**（过）、side_b **0.9618**（不过，band 2 %）。
**两种读法**：

| | 读法 |
|---|---|
| **(1)** | 种子公式 `S_tr = CTRCON·exp(−CTRCEX/(H_k−1))·√CτEQ` 在 `H_k = 2.267` 上不准 |
| **(2)** | 第十轮那件事的残留：XFOIL 的转捩站状态由 **`TRDIF` 区间分段**给出，我们在**整站**上算种子 |

★★ **判别子（第十轮已验证可用的干净读法）**：
> 从**最后一个纯层流站**起步、`x_tr` 放在**我们自己设的强制转捩点**（`x/c = 0.05`），
> 让转捩站与其后首站**由预测得到**，再看 `Cτ` 比。

| 结果 | 判定 |
|---|---|
| 比进 2 % 带 | ★ **(2) 成立** —— 是喂进种子的 `(H, Re_θ)` 不同，**种子公式无罪** |
| 仍不进带 | ★ **(1) 成立** —— **种子公式是库里的问题**，登记给下一轮（**本轮不改库**） |
| 两侧一侧过一侧不过 | **UNDEFINED**，不报方向 |

★ **带不重新挑**：沿用 `max(10×L-FLOOR, 2 %)`。
★ **`x_tr` 不扫**（第十轮的教训：取最好点有自由度）—— **只用我们设的那一个点**。

## 判据与守卫

| 判定码 | 条件 |
|---|---|
| **E-DEMOTE** | 三处 bench 脚本里 E-CF 的判定码改为 RECORDED；**窗口/带/数一个不动**（逐字对照旧输出） |
| ★ **W-SEED** | G18 的判别子结果，按上表三分 |
| **G-FROZEN-LIB** | ★ 本轮**必须** `pyfp3d/` 零改动（若 W-SEED 落在 (1)，改库是**下一轮**的事） |
| **G-ONECHANGE** / **G-XTR** / **G-COLSEM** / **G-PROV** | 沿用 |
| ★ **G-NOREWRITE**（新） | 降级前后 `window_*.csv` 与 `summary.csv` 里的**数值列逐位相同**，只有 verdict 列变 |

## Kill criterion

1. ★ 想借降级顺手改窗口/带 ⇒ 停。
2. 想改 `pyfp3d/` 让 W-SEED 过 ⇒ 停。
3. 一侧过一侧不过就写方向 ⇒ 停（判 UNDEFINED）。
4. > 10 min ⇒ 停。

## 事前预测

1. ★★ **W-SEED 落在 (2)** —— 依据：side_a 用**同一个公式**通过（1.0070），若公式本身不准没有理由一侧过一侧不过。
   ★ **但这正是第十四轮说过"是推理不是测量"的那条推理** —— 本轮把它变成测量，**它可以落空**。
2. ★ **降级后 E-CF 的数一位不变**（G-NOREWRITE 会证实）。
3. 零 FP 求解，< 5 min。
