# 20260820-0100 GS4.1 第七轮 — **执行附录 #1**：R-STATIONS 拦下的是**我的解析器**

**性质**：执行附录，**重执行前**提交。
★★ **判据、容差、kill criterion 一个字没动。** 改的是本轮脚本里两处**纯解析 bug**。

## 1. 发生了什么

第一次执行在 **R-STATIONS 停住**（kill 条 1），报 `140 upper + 177 lower` vs 已提交的 `140 + 140`。
★ **守卫按设计工作了** —— 它在"把两组不同的点比成一组"之前就把整轮拦下来了。

## 2. 两处 bug，都是我的

**(a) 表面/尾迹没分开。** 我用"LE 之后 `x` 的最大值"当表面终点，
但**尾迹在 `x > 1` 继续延伸**，所以 argmax 落在尾迹里，整段尾迹被当成了表面。

实测边界毫不含糊：

| i | x | y | Ue/Vinf | Cf |
|---|---|---|---|---|
| 279 | 1.00000 | −0.00126 | −0.88651 | 1.407e-03 |
| **280** | **1.00010** | **0.00000** | 0.88651 | **0.000e+00** |

⇒ **280 表面 + 36 尾迹**（`y = 0`、`Cf = 0`、`x > 1` 三条同时成立）。
**修法**：表面止于 LE 之后**第一个 `x > 1` 的行**。

**(b) LE 行被两侧重复计入。** 我写 `lower = surf[le:]`，与 `upper = surf[:le+1]` 共用 LE 行
⇒ 280 行给出 140 + 141。
**修法**：`lower = surf[le+1:]` —— ★ 这**不是我的约定选择**，而是**已提交生成器自己的**
（`generate_xfoil_reference.py`：`upper = x[:k+1][::-1]`、lower 取 `rest`）⇒ 140 + 140。

## 3. 记账

- 第一次执行**没有产生任何比较读数**（停在先决条件上）⇒ 不存在"看了数字再改"；
- ★ **事前预测 1 的实质内容被证实**（"316 = 280 表面 + 36 尾迹"）——
  **预测对了，我的解析器错了**，两者是分开的两件事，如实分开记；
- 机时：第一次执行约 3 s。
