# 20260812-1100 预注册 — **σ 冻结**:先复现极限环、再报警、最后才谈治法(含 kill criterion)

**在任何代码之前提交**(roadmap 原则 8)。使用者指示:同意"只冻集合"的 A/B,
**并要求**(i) 分析**为什么**有极限环、(ii) 有什么办法消除(松弛?耗散?平均?)、
(iii) **至少要能提示极限环存在并报错**。

## 0. ★★ 入口检查(零求解)—— 它已经改变了本轮的形状

上一轮判定的勘误(commit `0ca6263`)测出:**这里有两种病,不是一种。**

| | P-A **冻得太早** | P-B **尾部集合翻动** |
|---|---|---|
| 症状 | σ 在场还处于**大幅瞬态**时被永久冻住 | `n_shock 73↔74` 交替、σ_min 只动**第 7 位**、max\|Δσ\| 钉在 2.9e-2、残差停在 ~5e-6 |
| σ_min 摆幅 | **0.02–0.99**(实测,本人) | **~1e-7**(注释记录) |
| 何时 | 前 8 步(刷新预算内) | **尾部**(场已近收敛) |
| 状态 | ★ **我已复现并量化** | ★★ **我从未复现过** —— 只在代码注释里见过 |
| 想要的处置方向 | **继续刷新**直到安定 | **停止刷新** |

★★★ **两者方向相反**,而 `_SIGMA_REFRESH_MAX = 8` 是一个两边都不保证的折中。

⇒ **本轮的顺序因此是被迫的,不是偏好**:使用者问"为什么有极限环",而
**我还没有复现过那个极限环**。对着一段散文做机理分析,就是在分析散文。
所以 **Part 1 必须是复现**;不复现就没有"为什么"的主语。

★ 另外**先把使用者列的三个候选筛一遍**(读记录,不做实验):

| 候选 | 评估 |
|---|---|
| **松弛因子** | ✓ 合理 —— σ 现在是 `sigma_frozen = sig.copy()`,**没有任何松弛**;加 ω 就是在迭代空间里低通,直接压掉"冻在尖峰上" |
| **平均** | ✓ 合理,且与松弛**同族**(都是迭代空间低通);差别是有限窗 vs 指数窗 |
| **加耗散** | ★ **要小心,它已经存在**:人工密度上游化的强度就是 `upwind_c`,而 **M1 的判据 (c) 量的正是 cl 对它的敏感度,实测 9.4 %(coarse)/ 17.9 %(medium)** ⇒ 加大它**不是免费的**,而且抹得更开会让激波位置误差更大(GS1.1)。⇒ **本轮不把它当候选**,理由在此记录 |

⇒ 三个候选里**只有两条同族可用**:**松弛**与**平均**,都是"在迭代空间里低通",它们进 Part 3;
**耗散**被上表的理由排除,并把排除理由留在案。

## 1. Part 1 — **复现 P-B**(必须最先,且**不动库**)

`_SIGMA_REFRESH_MAX` 是**模块常量**,在驱动循环里**运行时读取**(`newton.py:1265`)。
⇒ 诊断脚本**在进程内 monkeypatch 它**(`newton._SIGMA_REFRESH_MAX = 10**9`),
**不编辑库文件** —— 这是本轮 Part 1 唯一的"改动",而且它只活在 bench 进程里。

**腿**:注释记录 P-B 的那个工况 **coarse M0.7875 / α1.25** 逐字复现,
外加本轮已在手的 **M0.80 / α1.25**(混合 R0 + 非结构 coarse)、种子 {0, 5, 12}。
每步都刷新,记录 `n_shock`、`sigma_min`、`max|Δσ|`、残差、以及**哪些单元在集合里进出**。

**事前判据**

| | 条件 | 判定 |
|---|---|---|
| **L1** | 存在一条腿,其 `n_shock` 在尾部(最后 10 次刷新)**取值 ≤ 3 个且反复出现**,且 `max|Δσ|` **不趋零**,且残差**不再下降** | ⇒ **P-B 复现**,机理分析有主语 → Part 2/3 按它定向 |
| **L2** | 没有任何腿出现 L1(σ 与残差都继续收敛到底) | ⇒ ★ **P-B 在这些工况上不存在** ⇒ 那么 `_SIGMA_REFRESH_MAX` 的**立项理由本身需要重新测量**,而"为什么有极限环"这个问题**在本处无对象** —— 如实上报,**不去别处找一个来证明它** |
| **L3** | 出现别的形态(如缓慢漂移、多周期、间歇) | ⇒ **RECORDED,按形态命名**,不套用"极限环"这个词 |

★ **L1 的口径事前定死**,因为"看起来在振荡"不是判据:必须是**取值集合小 + 反复 + Δσ 不趋零 + 残差不降**四条同时成立。
★ 并且**周期要报出来**(2 / 3 / 更长),因为项目已记过一次栽在这上面:
**period-3 的极限环给出 descent10 = 2.0021,落在手挑阈值"还在下降"的那一侧,并据此发表过结论。**

## 2. Part 2 — **报警机制**(使用者的最低要求,**本轮必交**)

驱动器**已经**收集了需要的数据:`sigma_history` 每次刷新存
`(sigma_min, n_shock, m1_max, sigma_delta, converged)`。所以检测**不需要新的测量**,只需要判读。

**两个签名分开报,不合成一个数**(否则又是把两种病混成一个指标):

| 标志 | 触发 | 含义 |
|---|---|---|
| `sigma_frozen_in_transient` | **最后一次**刷新的 `sigma_delta` > 阈值 τ_t | σ 在**还在动**的时候被冻住了(P-A) |
| `sigma_selection_churn` | 尾部若干次刷新里 `n_shock` 取值集合 ≤ 3 且反复,且 `sigma_delta` 不趋零(P-B) | 集合在翻动 |

**必交的形式**:两个布尔 + 支撑数字进**返回字典**,并在触发时打印**警告**。

★★ **"报错"的严格程度是一个被测量的决定,不是本轮就拍的**:
把它升级成**拒绝**(像 `sigma_transport_not_converged` 那样让 `converged=False`)是一次**契约变更**,
它可能让**已提交的能力锚点集体变红**。⇒ 本轮**先测**:在已提交的 M1 门 12 点 + 快层的 5 组上
**有多少会触发**;数出来之后,升级与否写进判定,交使用者裁决。
**先测再升级**,而不是"先严起来再看谁红" —— 后者会把一次契约变更伪装成一次 bug 修复。

★ **阈值 τ_t 必须事前定,且必须承认它是标定**:取 **τ_t = 1e-3**,理由是注释记录的 P-B 把
`max|Δσ|` 钉在 **2.9e-2**、而收敛尾部的 Δσ 应当趋零,1e-3 在两者之间约一个数量级。
★★ 项目已记的规矩:**任何固定阈值都是标定不是保证**(EW forcing / taper r_c / descent10 都栽过)
⇒ 判定里必须报**触发率对 τ_t 的敏感性**(τ_t ∈ {1e-2, 1e-3, 1e-4}),而不是只报一个数。

**守卫 G-P2**:报警是**纯读判**,不得移动任何数字 ⇒ M1 门默认路径与已提交 8 线程记录
**12 行 × 14 字段逐位相同**。不过就停。

## 3. Part 3 — **治法 A/B**(方向由 Part 1 决定,**默认关闭**)

候选(全部默认 OFF、legacy 逐位):

1. ★★ **只冻集合、不冻幅度**(使用者已同意的那条,靶子来自代码自己的理由:
   注释论证的是**离散集合**不可被 Newton 解决,而实现把**集合和幅度一起冻**了);
2. **松弛**:`sigma ← (1−ω)·sigma_old + ω·sigma_fresh`,ω < 1;
3. **平均**:σ 取最近 k 次刷新的均值(或**最小值** —— 后者更保守,不会低估熵增)。

**事前判据(binding = 路径依赖,因为那才是这条线要治的病)**

| | 要求 |
|---|---|
| **F1** | 跨种子 {0, 5, 12} 的 **cl_p 散布**(★ **只在收敛腿之间**算,少于 2 条收敛则 **UNDEFINED**)从基线的 **32.11 %** 降到 **≤ 5 %** |
| **F2** | 不得把 P-B 放回来:开启后**不出现 L1 签名**,且残差仍收敛到与基线同量级 |
| **F3** | legacy 路径**逐位相同**(flag 默认 OFF) |
| **F4** | 收敛代价 RECORDED:Newton 步数与墙钟(不设判据,如实记) |

★ **F1 与 F2 是一对不能只满足一个的要求** —— 只满足 F1 等于用"永不冻结"换来一致性,
而那正是 `_SIGMA_REFRESH_MAX` 当初要解决的问题;只满足 F2 等于没治病。

## 4. ★★ KILL CRITERION

1. **G-P2 不过**(报警机制移动了数字)⇒ 立刻回退,停;
2. **Part 1 得 L2**(P-B 复现不出来)⇒ **Part 3 的候选 1 失去靶子**
   (它的立项理由就是"集合会极限环"),此时 **Part 3 只做候选 2/3**,并把
   "`_SIGMA_REFRESH_MAX` 的立项理由需重新测量"作为结论上报;
3. **F1 与 F2 无法同时满足**,且两轮内改不出来 ⇒ 这条路判死,把
   "σ 冻结的路径依赖是当前离散化的固有代价"写进能力边界;
4. **单腿 > 10 min** ⇒ 停(预算 ≤ 40 min,Part 1 的无上限刷新腿可能变慢,单独计时)。

**明确不算 kill**:
- **L2/L3**(极限环不是我以为的形态)—— 有用的负结果;
- **Part 3 只有一个候选成功** —— 一个够了;
- **M1 仍不达标**(沿用前四轮的豁免)。

## 5. 本轮不做

- **不改任何默认行为**(治法全部默认 OFF;报警只增字段与警告);
- ★ **不把报警直接升级成拒绝** —— 先测已提交锚点的触发率(§2);
- **不加耗散**(§0 已记录理由:它已存在为 `upwind_c`,且 M1 的 (c) 正在量它的代价);
- **不改 M1 的 (a)(b)(c)**;
- ★ **不在同一轮里既改冻结又扫 σ 强度** —— θ 已按上一轮条款删除,本轮不重开。

---

## 附录 #1(**Part 3 动代码之前登记**)—— 候选重排 + 软斜坡的**精确定义**与它的标定

Part 1 的机理(§判定 2b)长出一个**不在原候选清单里**的候选。注册 §3 明写"方向由 Part 1 决定",
所以它在设计之内,但**它是新增的,且必须在动代码前把形式定死** —— 否则我会在看过曲线之后
挑一个"效果好"的变体,那就是事后放宽。

**执行顺序(改)**:候选 **4(软化集合判定)** 先做;原候选 1(只冻集合)、2/3(松弛/平均)作对照。
理由:4 从**源头**去掉跳变,而 1/2/3 都是处理它的后果;且 4 在同一内核里**有既有先例**(`m_crit`)。

### 候选 4 的精确定义(事前定死)

现在的判定是两个**硬**测试;把每一个换成一个**连续斜坡**,与 `nu_e` 同形:

    w_u = clamp((m2u - 1) / eps, 0, 1)        # 供体"有多超声速"
    w_e = clamp((1 - m2e) / eps, 0, 1)        # 自己"有多亚声速"
    s_e = 1 - w_u * w_e * (1 - sigma_RH(M1))  # 两端点与现在**完全一致**

- `w_u = w_e = 1`(带外、深处)⇒ `s_e = sigma_RH(M1)`,**与今天相同**;
- 任一 `w = 0` ⇒ `s_e = 1`,**与今天相同**(等价于现在的 `continue`);
- **只有带内的单元改变**。⇒ 因此**整体不是逐位相同的**,这是它的设计;
  ★ 所以旗标**默认 OFF**,而 **OFF 路径必须逐位相同**(F3),并且带外单元要靠**显式短路**保证
  (θ 那次的教训:`1 − 1.0·(1−σ)` 在浮点上不等于 σ)。

### ★★ 宽度 `eps` 是一个标定,按标定对待

- **事前取 `eps = 0.05`(M² 单位)** ⇒ 带宽约 M ∈ [0.975, 1.025];
  理由:翻动发生在**单元自身的 M 跨过 1** 处,所以带要窄且对称地夹住 M=1;
- ★ **判定必须报 `eps ∈ {0.02, 0.05, 0.10}` 的敏感性**,不许只报一个数
  (EW forcing / taper r_c / descent10 / τ 都栽在"一个手挑的数决定了结论"上)。

### 追加的判据(在 F1–F4 之外,因为候选 4 是**模型改动**)

| | 要求 |
|---|---|
| **F5** | ★ 软化会给**尚未过激波**的单元收一部分费、也会给已过激波的单元少收 ⇒ **必须报激波位置的变化**(M1 判据 (a) 的口径 0.61 ± 0.02),并说明它是否把 (a) 推出带外。**这不是一个可以只看散布就交差的改动。** |

★ **F1 与 F2 仍是不能只满足一个的一对**(注册 §3);
★ 并且 Part 1 测到的"**去掉上限 9/9 不收敛**"意味着 **F2 会很紧** —— 若软化之后仍需保留冻结,
那就要**如实报**"软化减轻了多少、但没有取代冻结"。
