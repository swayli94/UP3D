# 20260816-1800 预注册 — **挂账 ②:coarse 上两条流水线差 2.0 %**(phase 3 的最后一笔)

**在任何代码之前提交**(roadmap 原则 8)。裁决 **D7** 把它留在 phase 3;使用者 2026-08-16 确认做 ①②。

## 0. ★★★ 入口检查已经把这笔债**重新定位**(读原文,零求解)

**旧的记法找不到产物**:分诊判定写的是"**与 P14 coarse 锚点差 2.0 %**",但**没给锚点的值与出处**;
而我先查到的 `g82_anchor_check.csv`(cl 0.268691)**是 medium**(`run_g82_anchor_check.py:51`
写着 `_m6_case("medium")`)⇒ **它不是那个参照**。

**真正的参照找到了**,而且就在 `bench/run_m3_budget.py` 里:

```
#: P14 pressure-Kutta anchors (cl_p, cl_KJ), GV5.3's P14_ANCHOR verbatim
P14_ANCHOR = {"coarse": (0.262778, 0.268813), "medium": (0.277628, 0.282263)}
```

⇒ **coarse 参照 cl_p = 0.262778**;分诊脚本 HEAD 上的 coarse 首读 **cl_p = 0.268115**
⇒ **+2.03 %** —— **这正是那个「2.0 %」**。★ 债现在**可引**了。

### ★★ 而两条"流水线"其实是**同一条配方**,差的是**网格族**

读原文:
- **GV5.3 的 P14_ANCHOR**(phase 1,2026-07-25)= "P14 transonic FP recipe **verbatim**:
  M0.70 探针种子 → `NEWTON_M6_RECIPE` 爬坡、**pressure** Kutta、**NO tip_taper**";
- **分诊脚本的 `production_solve`** = 同样的 M0.70 探针 + `NEWTON_M6_RECIPE` + pressure、
  **也没有 taper**(这一点是上一轮 Q-B 的入口检查查出来的)。

⇒ **配方相同**。而**不同的是**:`cases/meshes/onera_m6/coarse.msh` 在 **2026-08-04** 随
"级别名 flat → round 翻转"**被重新生成**(`_m6_case` 的注释逐字记着这件事)
⇒ **GV5.3 的锚点量在平截帽网格上,分诊的 HEAD 读数量在圆角帽网格上。**

★ 而 `_m6_case` 自己的注释给了同族的量级:medium 上 flat→round **+ taper** 合起来 **cl_p +1.82 %**,
而**taper 单独 −1.3 %** ⇒ **纯 flat→round 约 +3.1 %**(medium)。coarse 上两侧都无 taper,
实测 **+2.03 %** ⇒ **符号与量级都对得上**。

### ★★★ 因此我有预测,而且必须写在前面

> **预测:F-MESH** —— 那 2.03 % 是 **coarse.msh 的 flat→round 切换**,不是两条流水线不一致。
> **它可以怎样被推翻**:在 **`coarse_flat.msh`** 上跑同一条腿,若它**不**落在 0.262778 附近,
> 那帽型就不是解释,**存在真正的跨流水线不一致**要追。

★ 这是本会话第三次"我先有预测" ⇒ 照前两次的办法:**预测与反驳方式都写在前面**。

## 1. 设计(两条腿,都很便宜)

★ **`coarse.msh`(圆角)与 `coarse_flat.msh`(平截)都在仓库里**(已 `ls` 确认)⇒ 检验可构造。

| 腿 | 网格 | 作用 |
|---|---|---|
| **R(round)** | `coarse.msh` | **G-R**:必须复现分诊的 **cl_p 0.268115**(同脚本/同配方/同代码) |
| **F(flat)** | `coarse_flat.msh` | **binding**:它落在 **0.262778** 附近吗? |

**配方**:分诊脚本的 `production_solve(seed=0, ramp=True)` **逐字**
(M0.70 探针 → `NEWTON_M6_RECIPE` 爬坡 → pressure Kutta、**无 taper**、entropy ON、M0.8395/α3.06)。
★ **不加 taper** —— 加了就不是这条腿了(而且参照那侧也没有)。

## 2. 五问检查

| 问 | 回答 |
|---|---|
| **1 单边?** | **三条命名**:**F-MESH**(帽型解释了它)/ **F-OTHER**(★ 帽型**不**解释 ⇒ 存在真正的跨流水线不一致,要追)/ **F-UNDEF**(腿不收敛) |
| **2 自变量改变样本存在性?** | ★ **会** —— **圆角帽 + 无 taper 是已知脆弱的配置**(`_m6_case` 注释:round 无 taper 在 medium 上不收敛)⇒ 某腿可能不收敛 ⇒ **F-UNDEF**,并**分型**(用 `classify_failure`,★ 按新加的做法**先干跑校验返回数与类型**) |
| **3 阈值绝对还是相对?** | **相对**:F 腿与参照 0.262778 的**相对差**,带 **±0.5 %**(= 分诊自己给 G-R 用的同一条带,不新造数字) |
| **4 匹配的量是输入还是输出?** | cl_p 是输出;输入只有**网格文件** |
| **5 两个要比的数是同一个东西吗?** | ★★★ 这一整轮就是这一问:同脚本 / 同配方 / 同工况 / 同代码 / 同线程,**唯一变量是帽型**;而 G-R 腿证明"同脚本能复现分诊那个数" |

## 3. 事前判据(binding = **F 腿的 cl_p 对 0.262778 的相对差**)

| | 条件 | 判定与后续 |
|---|---|---|
| **F-MESH** | **G-R 通过**(R 腿复现 0.268115,±0.5 %)**且** F 腿在 **0.262778 ±0.5 %** | ⇒ ★★ **那 2.03 % 是 flat→round 的网格族切换** ⇒ 这笔债是**又一次跨族比较**,不是流水线不一致 ⇒ **债结清**,并把"P14_ANCHOR 是 flat 帽时代的数"写进 `run_m3_budget.py` 的注释与能力边界 |
| **F-OTHER** | G-R 通过,但 F 腿**不在**带内 | ⇒ ★★★ **我的预测被推翻**:帽型不解释 ⇒ **存在真正的跨流水线不一致**,必须继续追(下一轮:逐项 diff 配方与后处理) |
| **F-UNDEF** | 任一腿不收敛 | ⇒ **UNDEFINED**,报**分型**;★ 而**不得**把"不收敛"读成任何一侧的证据 |

**必须一并报**:两腿的 `converged / |R| / n_limited / n_floored / 分型 / 墙钟 / cl_p / cl_kj / M_max`;
★ 与 **P14_ANCHOR 的 cl_KJ 0.268813** 的对照(两个量都报,免得只挑对自己有利的那个);
★ 两张网格的**单元数与 tip_cap**(证明它们真的只差帽型)。

**守卫**
- **G-R**:R 腿复现分诊的 0.268115(±0.5 %)—— 不过就**停**(不是同一条腿);
- **G-M(网格身份)**:打印两张 `.msh` 的单元/节点数;★ 若单元数差异过大(> 20 %),
  说明它们不只差帽型 ⇒ **如实记为混淆**,判定降级为 RECORDED;
- **G-C**:写**自己的** `task3_coarse_pipeline_gap.csv`,不碰任何已有证据;
- ★ **G-DRY(新做法)**:动机时前先**干跑校验** —— 查找、常量、`classify_failure` 的**返回数与参数类型**
  (本会话刚补进 CLAUDE.md 的那条)。

## 4. ★★ KILL CRITERION

1. **G-R 不过** ⇒ 停下对齐;
2. **两张网格的单元数差 > 20 %** ⇒ 判定降级 RECORDED(它们不只差帽型);
3. **单腿 > 900 s** ⇒ 停该腿并如实报;
4. **总墙钟 > 1 800 s** ⇒ 停;
5. ★ **两腿 cl_p 逐位相同** ⇒ 不是 F-OTHER,是**可疑**(网格没真的换)⇒ 先查仪器。

**明确不算 kill**:三支判定中的**任何**一条 —— 尤其 **F-OTHER**(它推翻我的预测,并会开一条新线索);
**M3a 仍不达标**(沿用前二十五轮的豁免)。

## 5. 本轮不做

- **不改库**;**不加 taper**(那会换掉这条腿);**不去修 `P14_ANCHOR`**(先测,再决定怎么标注);
- ★ **不因为 F-MESH 是我的预测、又能收掉最后一笔债就往它靠** ——
  F-OTHER 被写成"我错了 + 开新线索",而 phase 3 的收口**不应当以一个想要的结论为条件**。
