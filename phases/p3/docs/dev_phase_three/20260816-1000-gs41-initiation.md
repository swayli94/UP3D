# 20260816-1000 — **GS4.1 立项文书**(弦向条带二维 IBL)= ★★★ **PHASE 4 的入口文件**

**性质**:立项**提案**,不是判定。
★★★ **裁决 D7(2026-08-16)已定:阶段 S4 / GS4 视为 phase 4 的部分 ⇒ 本文书是 phase 4 的输入,phase 3 不开工。** 因此下面的一切都是**给 phase 4 的准备**,而**不是**一个已开的门 ——任何后续轮次若把它当成已开工,即与 D7 冲突。
依据来自 2026-08-16 的两轮 HEAD 测量:
[前提核实](20260816-0000-s4-premise-check-verdict.md) 与 [成本趋势 + B 场](20260816-0800-loadguard-and-bfield-verdict.md)。

---

## 0. ★★★ 这份文件是 phase 4 的**唯一入口**:开工前必须先读完 §0

**使用者 2026-08-16 定**:phase 4 **只依赖本文件**作为入口 —— 开工的人不必先去翻 phase 3 的 68 份轮次
文件、progress、roadmap 才能知道自己站在哪里。因此 §0 把**必须随身带的东西**内联在这里,而不是只给链接。

★ **维护约定(它自己就是一个 rot 风险)**:本文件**留在原地不搬**,phase 4 的 `phases/p4/docs/dev_phase_four/progress.md`
**链接**它而不是复制它 —— 复制出来的入口会和原件分叉,这正是本项目记过多次的那类失效。
若将来必须移动,**移动 + 改指针必须同一个提交**。

### 0.1 ★★ 开工必须带的两条已测结论(**不得省略,不得改写**)

> **① P1(BL 成本占比)在下一级会穿过带的下沿。**
> 生产级 **P1 = 6.07×**(判据带 [4, 20] ⇒ T-HOLD,动机**成立**),但标度指数给
> **IBL ~ 体积^0.94 / 无粘 ~ 体积^1.20 ⇒ P1 ~ 体积^(−0.26)**;再加一级(×7.5 单元)⇒ **P1 ≈ 3.5 < 4**。
> ⇒ **GS4.1 的收益在生产级最大,并随进一步加密衰减** —— 若产品最终用更细的网格,这条动机会失效。
> **收口时必须同时报 S2 与 S4 两级**,让衰减可见(§2.2 / R4)。
>
> **② 二维条带丢掉的横流是真实的后缘结构,不是粗网格噪声。**
> 空间格重叠 **J = 0.600 vs 随机零假设 0.060 = 10.0×** ⇒ 横流集中在**最后 3 % 弦长**,两级都是。
> 生产级 **max|B|/max|A| = 0.0186、逐点最大 0.0315(3.2 %)**。
> ⇒ 立项**不得**写成"横流是噪声、可以丢";正确写法是**丢掉它是一个有测量支撑的取舍**,
> 并在后缘那 3 % 弦长上**记录被丢掉的量**,而不是默认它为零(§2.3 / §3.4)。

★ 这两条都是**推翻了我自己事前预测**的读数(① 我登记的是 T-DROP;② 我上一轮曾写成"粗网格伪影",已勘误)
—— 所以它们不是顺手带的注脚,是**这个门的立项前提本身**。

### 0.2 交接时的世界状态(**HEAD = `main`**)

phase 3 三项任务全部合入 `main`:任务 ①(删 level-set)经 **PR #26**,任务 ②③ 经 **PR #27**(合并提交 `d5efe53`)。

| 口径 | 数字 | 条件 |
|---|---|---|
| **未门控套件** | ★ **500 passed + 12 skipped + 2 xfailed,0 failed** | 465.24 s **@8 线程**(2026-08-17 实测,GS4.0 后) |
| ↳ *交接时的原值* | ~~479 + 12 + 2~~ | 462.92 s @8 线程(2026-08-16);**+21 = GS4.0 的仪器锁 `tests/test_gs40_provenance.py`**,skipped/xfailed 未动 |
| **全门控集** | ★ **509 passed + 1 skipped + 3 xfailed + 1 XPASSED,0 failed** | 1:12:38 **@16 线程**(2026-08-18 实测,GS4.0 后) |
| ↳ *交接时的原值* | ~~488 + 1 + 3 + 1~~ | 1:11:48 @16 线程(2026-08-16);**488 + 21 = 509**,增量恰好是 GS4.0 的仪器锁 |
| **快速能力锁层** | **5/5 绿** | 532 s @8 线程 |

★ **门控计数必须带线程数引用** —— 那个 XPASSED 由 mark 自身预言(该腿 16 线程收敛、8 线程不收敛),
**不是回归**。★ 环境:conda env **`up3d`**、**`PYTHONNOUSERSITE=1` 是必需的**(否则 `import pyamg` 失败)、
**scipy ≥ 1.12 是代码要求**、**不要在 anaconda `base` 里工作**。

### 0.3 phase 4 还继承了哪些项(D7 分派)

| 项 | 状态 |
|---|---|
| **阶段 S4 / GS4** | 本文件 = 入口(**未开工**) |
| **M4 翼身粘性 case 入口** | 归 phase 4;★ **HEAD 上还没有这个入口**,是独立的实现任务(§6) |
| **P2 的 M0.5 工况维度** | 归 phase 4;★ 跨工况比较**未做**(phase 3 全在 M0.8395,第一阶段的 0.072 在 M0.5,无缓存 φ) |
| **Q-B(系统本身是否多解)** | ★ **仍然开着**。phase 3 只测出"**该配置上直解答不了它**"(直解臂 0/3 收敛,三腿一致分型为 `clamping`)。**禁止句**:**不收敛不是唯一性的证据** |

### 0.4 ★★ 六条不许被静默改掉的事项(裁决 D7 原文,**phase 4 同样受约束**)

1. **M3a 的可达性现在是「未知」**,既不是"不可达"也不是"可达" —— 旧的"不可达"是对一个**没在收敛**的序列
   做四点外推,**证据基础已失效**;
2. **LE 占 70 % 的旧数(69.6 %/0.2361)已退役**,HEAD 替代读数 **70.35 %/0.2422**,**新旧不可比**;
3. **两处 flat-cap 污染**(四级全尺度阶梯、"LE 分辨率已否证")—— **平截帽在加密下发散**
   (LE 带阶 **0.37 flat / 0.87 round**);★ 同族:`run_m3_budget.P14_ANCHOR` 是**平截帽时代的数**
   (coarse 上 flat→round 值 **+2.03 %**),拿 HEAD 读数去对它是**跨网格族**比较;
4. **横流是真实结构**(= §0.1 的第 ②);
5. ★ **裁决 D2 仍然有效** —— 等参 P2 已被否决,**"提高阶数"这条路要重开必须显式推翻 D2**;
6. **门控计数必须带线程数**。

### 0.5 ★ 重开 LE 线的前置条件(若 phase 4 想碰它)

必须**先给出外区那另一半的机理候选**:phase 3 已把 **σ / 尖端 / 激波足迹 / 局部弦长 / Γ 与 Γ/c** 逐条测负,
而近奇异内区只解释约一半(41.7 % 的点占 74.9 % 的 d² 质量,**去掉它外区仍不收敛,R = 0.91**)。

---

## 1. 门的定义(**逐字引自** `phases/p2/docs/dev_phase_two/roadmap.md` §S4,不改写)

> | GS4.1 | 弦向条带二维 IBL(Drela 二方程 + lag,或 Drela–Giles 三方程),沿流向行进 |
> | 整机翼边界层 **< 1 s**;平板/Blasius/自相似解验证通过 |

★ **不动这个数字**(本项目禁止"把解不出来的工况改成解得出来的"),但见 §4 的风险 R1。

## 2. HEAD 上的立项依据(三条,**每条都必须写进立项**)

### 2.1 ★★ 成本动机**在生产级成立**(而它一度被我判为可能不成立)

| 级 | 体单元 | BL 未知量 | 一次 IBL | 无粘 | **P1 = IBL / 无粘** |
|---|---|---|---|---|---|
| S0 | 9 303 | 5 676 | 23.2 s | 0.9 s | **25.82×** |
| S2 | 59 359 | 21 342 | 136.8 s | 13.4 s | **10.21×** |
| ★ **S4(生产)** | **448 197** | **82 584** | **~918 s** | 150.6 s | ★ **6.07×** |

**T-HOLD**(判据带 [4, 20],**趋势经 G-L2 可重复性守卫 +1.3 % 通过后裁决**)
⇒ **在生产级,边界层仍是成本大头(6.07×)**。
★ **诚实记录**:我事前登记的预测是 **T-DROP(~3×,动机不成立)**,**被这组数推翻**。

### 2.2 ★★ 但 **P1 随加密下降,下一级会穿过带的下沿**

派生标度指数:**IBL ~ (BL 未知量)^1.41**、**无粘 ~ (体单元)^1.20**;而**表面 ~ 体积^⅔**
⇒ **IBL ~ 体积^0.94、无粘 ~ 体积^1.20 ⇒ P1 ~ 体积^(−0.26)**。
⇒ 再加一级(×7.5 单元)给 **P1 ≈ 3.5 < 4** ⇒ ★ **"BL 是成本大头"这条动机在更细的网格上会失效**。
**立项文书必须带这一条**:GS4.1 的收益**在生产级最大,并随进一步加密衰减**。

### 2.3 ★★★ 二维条带丢掉的横流是**真实的后缘结构**,不是噪声

| 级 | top-1 % \|B\| 的 x/c | z/b | max\|B\|/max\|A\| | 逐点 max |
|---|---|---|---|---|---|
| S2 | **0.982–1.000** | 0.000–0.554 | 0.1471 | 1.5843 |
| ★ **S4(生产)** | **0.971–1.000** | 0.000–0.966 | ★ **0.0186** | ★ **0.0315** |

空间格重叠 **J = 0.600** vs 随机零假设 **0.060** ⇒ **10.0×** ⇒ **B-PHYS**:
**横流集中在最后 3 % 弦长(后缘),两级都是**;细网格把它解析到几乎整个展向,**幅度**在粗网格被**高估**。

★★★ **所以立项文书不能写"横流是噪声、可以丢"** ——
必须写:**它是真实的后掠翼后缘横流,而在生产分辨率上它的比值是 0.0186、逐点最大 3.2 %**
⇒ **丢掉它是一个有测量支撑的取舍**。
★ 而这条**同时修正了我自己上一轮的措辞**(我曾把它写成"粗网格伪影",已勘误)。

## 3. 立项范围建议(**最小可判**,不求一步做完)

1. **一维条带核**:沿流向行进的二维 IBL(Drela 二方程 + lag),**独立于现有 `ibl3.py`**
   (roadmap 明写 `ibl3.py` + `closures.py` **冻结不删**,标记研究分支);
2. **验证按门的原文**:平板 / Blasius / 自相似解;
3. **成本按 §2.1 的同一口径量**:**一次 BL 求解 vs 一次无粘求解**,在 **S2 与 S4 两级**上各测
   ⇒ 与本轮的 10.21× / 6.07× **可比**;
4. ★ **横流的处置必须显式**:在后缘那 3 % 弦长上**记录被丢掉的量**(≤3.2 % 逐点),
   而不是默认它为零。

★ **不建议**在 GS4.1 里同时做耦合(那是 GS4.2 的 Veldman 准同时/交互律)——
本项目已记过"一次改两件事就分不清是哪件起作用"。

## 4. ★★ 这个门自己的风险(事前写下,免得开工后才发现)

| | 风险 | 依据 / 处置 |
|---|---|---|
| **R1** | ★★★ **"< 1 s"是一个 ~900× 的目标** —— 生产级 3-D IBL 实测 **918 s** | 门的数字**逐字保留**,但**立项时必须承认这个倍数**;若第一版条带核落在 10–100 s,那是**部分达成**,应按 RECORDED 报,**不得**改门 |
| **R2** | 条带化在**后缘汇流处**(两侧条带相遇)需要一个处置 | 现有 `build_wing_case` 已有"**两条 TE 线都是自然出流边界**"的语义,可复用;★ 但要**测**,不能假设 |
| **R3** | 二维条带在**尖端带**(η>0.95)不适用 | 现有机制已有 tip 掩蔽(生产 taper 半径 r_c = 0.05·b_semi),**沿用并声明** |
| **R4** | ★ **成本动机随加密衰减**(§2.2)⇒ 若产品最终用更细的网格,GS4.1 的收益会缩小 | 立项文书带上这条;并在 GS4.1 收口时**同时报 S2/S4 两级**,让衰减可见 |
| **R5** | 我在本季**七次**判据缺陷、**三次**自指守卫 ⇒ 新门的判据要过**常设六问**(含新增的两条) | 见 §5 |

## 5. ★ 给 GS4.1 的判据加一条清单(本季代价换来的)

开工前,GS4.1 的验收判据要逐条过:
1. 单边吗?(反向结果落在哪)
2. 自变量改变样本存在性吗?
3. 阈值绝对还是相对?
4. 匹配的量是输入还是输出?
5. 两个要比的数是同一个东西吗?(★ 含**同工况**、**同级别**、**同点集**、**同判据措辞**)
6. ★ **定带用的参考数也过第 5 问了吗?**(缺陷 #7)
7. ★ **这条守卫会不会量到自己?**(缺陷 #8,自指族第三次)
★ 另加做法:**先干跑校验查找/常量/级别清单,再花机时**(本季两次各烧 ~17 min 换来的)。

## 6. 明确不在本文书里

- **不开工**(这是提案);**不改 roadmap 的门文字与数字**;
- **不承诺 M4**(翼身粘性在 HEAD 上**还没有 case 入口**,那是独立的实现任务);
- ★ **不把 P2 的工况维度当已测**:第一阶段的 0.072 在 **M0.5**,本阶段全部在 **M0.8395**
  ⇒ 跨工况比较**未做**(需要 M0.5 的 φ,没有缓存)。

## 7. ★ phase 4 开工的第一步(操作层,内联在此以免再去翻别处)

1. **建 `phases/p4/docs/dev_phase_four/progress.md`**,第一行**链接本文件**(不复制,见 §0 的维护约定);
   轮次文件命名沿用 `yyyymmdd-hhmm-<name>.md`,**一轮一份预注册 + 一份判定**。
2. ★★ **预注册在任何代码之前提交**(roadmap 原则 8)。判据与 **kill criterion 必须写在执行之前**,
   事后不得放宽;每次(重)执行前的改动写成**附录**并**先提交**。判定用
   **PASS / FAIL / RECORDED / UNDEFINED** 四种,**收敛腿 < 2 时报 UNDEFINED,不报方向**。
3. **判据先过 §5 的七问**,再花机时;★ 并**先干跑校验**:查找、常量、级别清单,
   以及被调函数的**返回数与参数类型**(不只是签名 —— 本季两次报告层缺陷各烧掉一次昂贵求解)。
4. **每次收口跑快速能力锁层**:`PYFP3D_TRANSONIC_GATES=1 python bench/run_capability_locks.py`
   (5 组,约 9 min @8 线程;它自己钉线程并打印解析值与 load average);**全门控集在阶段边界跑**。
5. **线程上限连 BLAS 一起钉**:`NUMBA_NUM_THREADS / OMP_NUM_THREADS / OPENBLAS_NUM_THREADS`;
   ★ **墙钟是机器标定,不是求解器性质** —— 同一答案在本机测到过 1.6×、1.9×、5.4× 的墙钟散布,
   所以任何墙钟都要带线程数与负载一起引用。
6. **证据必须有提交的产物**:只存在于 .md 散文里的数字不算证据;
   ★ 而"**一笔债在被转述第二次之前,先花零机时把参照是谁、出处在哪定下来**" ——
   phase 3 最后一笔债挂了五天、在 8 份文件里被转述为"未解释",实际只值 **14 秒**机时。
7. **别重算贵的已提交产物**(P4 重型 demo ~40 min、G4.1 medium ~17 min、P5 medium 45–75 min);
   ★ 而 `cases/reference_data/` 是**唯一的外部裁判**,且**三维升力在那里没有锚点**(D5 的代价)。
