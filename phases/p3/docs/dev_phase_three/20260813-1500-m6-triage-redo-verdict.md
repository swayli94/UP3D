# 20260813-1500 — 判定:**T1 —— 生产配置下的 M6 medium 可复现**;⇒ **α=0 迁移不必做**

> 三个种子、生产腿、medium、带 taper、16 线程(**由脚本打印实测**,不靠推理):
> **全部收敛到机器精度**(|R| 5.5e-15 / 6.3e-15 / 7.3e-15),
> **cl_p 散布 0.0011 %**、**LE 上 RMS 散布 0.0025 %** —— 远在 0.5 % / 2.5 % 的容差之内 ⇒ **T1**。
> ★★★ **后果:使用者原定的第 3 步(把 LE 研究搬到 α=0)不必做** ——
> 迁移的**唯一理由**是躲开多解,而**在这个配置上没有多解要躲**;
> 于是既省下迁移成本,又**保住了工况的物理相关性**(α=0 会去掉升力,而 M3 的目标是升力算例)。

预注册 [20260813-1300](20260813-1300-m6-triage-redo-prereg.md)(动代码前提交)。
产物 `m3_budget_head_medium{,_seed5,_seed12}.csv`(**每个种子写自己的文件**,守卫 G-C)。

## 1. 实测

| 量 | seed 0 | seed 5 | seed 12 |
|---|---|---|---|
| converged | True | True | True |
| \|R\| | 7.327557e-15 | 6.290436e-15 | 5.478272e-15 |
| **cl_p** | 0.272994 | **0.272997** | 0.272994 |
| cl_KJ | 0.277167 | 0.277169 | 0.277167 |
| **LE 上 RMS** | 0.242221 | **0.242215** | 0.242221 |
| LE 下 / MID 上 | 0.094043 / 0.098318 | 0.094042 / 0.098309 | 0.094043 / 0.098318 |
| 全点 pooled | 0.132319 | 0.132315 | 0.132319 |
| 墙钟 | 162 s | 199 s | 215 s |

**平衡面板 3/3**(全部收敛)。散布:

| 量 | 绝对 | 相对 | 容差 | 判定 |
|---|---|---|---|---|
| cl_p | 3.0e-06 | **0.0011 %** | 0.5 % | **PASS** |
| cl_KJ | 2.0e-06 | 0.0007 % | 0.5 % | **PASS** |
| **LE 上 RMS** | 6.0e-06 | **0.0025 %** | 2.5 % | **PASS** |
| 全点 pooled | 4.0e-06 | 0.0030 % | 2.5 % | **PASS** |

⇒ **T1。**

## 2. 守卫

| | 结果 |
|---|---|
| **G-R**(同溯源活参照) | ★ **满足** —— seed 0 就是上一轮刚产出的那一行;同脚本/腿/级别/代码/线程。**本会话第一次五问全过的比较** |
| **G-D**(确定性) | seed 5 与 seed 0 **不同** ⇒ **种子确实进入了求解**(仪器已连通);seed 12 与 seed 0 **在 CSV 精度上相同** ⇒ 按 G-D **如实记为"该种子可能未进入求解"** |
| **G-C**(不覆盖证据) | **满足** —— 每个种子写自己的文件名,seed 0 的证据文件一字节未动 |
| 线程 | ★ **脚本打印实测 16/16/16**,load average 0.28 —— 不再靠"Bash 工具不保留 shell 状态"这类**推理** |

★★ **有效样本是 2 个不同抽样,不是 3 个**(seed 0 与 12 落在同一状态,与 2.5-D 上的同一观察一致)。
⇒ **T1 建立在 2 个不同抽样之上**,那是散布的**最小样本**。
两个抽样一致到 **0.001 %** 是很强的证据,但**样本很薄**,如实登记。

## 3. ★★★ 必须遵守的那条禁令:这不叫"M6 没有多解"

注册 §0 明写:**Q-A 的稳定不得报成 Q-B 的答案。**

- **Q-A(已答)**:**生产路径可复现** —— 已提交的 M6 medium 锚点**不是**一次任意抽样;
- **Q-B(仍开)**:**这个离散系统本身在该工况上是否多解?** —— **未答**。
  它需要**绕开马赫爬坡**的直解臂,而那一臂按上一轮的成本决定**被推迟**且**至今未跑**。

★ 而为什么 2.5-D 的多解**没有**传到 3-D 生产配置上,有三个未分离的候选:
**马赫爬坡把选择钉住了**(本会话早先的假设)、**taper**、**三维本身**。
⇒ **不作归因**;要分开就得跑 Q-B 那一臂。

## 4. kill criterion

| 条 | 是否成立 |
|---|---|
| 1. T4(收敛腿 < 2) | **不成立** —— 3/3 收敛 |
| 2. G-C 不过 | **不成立** |
| 3. 单腿 > 900 s | **不成立** —— 162 / 199 / 215 s |

⇒ **本项不死,而且给出了它能给的最好结果**:**T1 省掉了整个 α=0 迁移**。

## 5. 这一轮里守卫第三次挡住我的仪器

我第一版用 monkeypatch 改 `M6_NEWTON_KW` 来注入种子,而**配方漂移守卫立刻开火**:

    AssertionError: the P14 recipe's newton_kw[n_picard_seed] = 0 no longer matches
    GV5.3's recorded 5 -- the comparison basis moved

**它是对的** —— 那个 dict 就是守卫的比对目标,被我改过之后与"意外漂移"无从区分。
而脚本自己的注释里已经写了正确做法:**故意的、有记录的偏离要放在守卫之后,作为显式参数**
(`n_newton_max` 就是这么做的,而且注释记着 2026-08-01 曾因顺序错误"cost a four-leg sweep")。
⇒ 按那个既有模式加了 `probe_seed`。

★ 本会话已有守卫**三次**在我的仪器产出错数之前把它挡住(G-R ×2、配方漂移守卫 ×1)。

## 6. 下一步

1. ★ **LE 那 70.35 % 的机理**(GS2.1 已否证:分辨率、几何、Kutta)——
   现在它有一个 **HEAD 上收敛、可复现、内部自洽**的数,**而且不必搬到 α=0** ⇒
   这是使用者原定第 3 步的**替代入口**,另行预注册;
2. **Q-B(直解臂)** 仍开 —— 它回答"系统本身是否多解",与锚点可信度无关,**优先级低于 1**;
3. ★ 仍悬着的 **coarse 上两条流水线差 2.0 %**(分诊 ① 留档),与本轮无关但未解释。
   ★ **勘误 2026-08-16:已结清 —— 不是流水线不一致,是网格族**(`coarse.msh` 在 2026-08-04 随 flat→round 重生成;`coarse_flat.msh` 上同一条腿给 cl_p 0.262123 = 对 P14 锚点 −0.249 %),见 [20260816-2000](20260816-2000-coarse-pipeline-gap-verdict.md)。
