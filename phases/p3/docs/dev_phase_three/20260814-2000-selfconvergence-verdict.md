# 20260814-2000 — 判定:**UNDEFINED —— 本轮撞上了自己的 kill 条**(0 个有效相邻对);
# ★★ 而它交出三条不依赖那个被判死读数的 RECORDED 结果,并把下一轮变成**零求解**

> **G-R PASS**(S4 = **0.242221 逐位**、448 197 tets = C_medium ⇒ 仪器对)、**G-C PASS**。
> ★★★ **但 G-X 判 S0 与 S3 无效,而 S1 根本不收敛** ⇒ **射线相邻的有效对 = 0** ⇒
> **UNDEFINED,不宣称任何方向**(kill 条 2/3)。
> ★★ **我不放宽这条已经触发的守卫** —— 越界只有 **0.0001–0.001 c**(每站 1–2 个点),
> 放宽它就能把 UNDEFINED 变成读数,而那正是本季我已拒绝两次的动作。
> ★ 修的是一个**真缺陷**(配对没按射线相邻),修完**结论不变**。
> ★★★ 而下一轮**不需要任何新求解**:**S0/S2/S4 本身就是一条干净的因子-2 射线且三级全收敛**,
> 缺的只是一个**正确构造**的公共点集。

预注册 [20260814-1800](20260814-1800-selfconvergence-prereg.md)(动代码前提交,`d7779c6`)。
脚本 `bench/run_task3_selfconvergence.py` → `gate_results/task3_selfconvergence.csv`。
5 级,16 线程(脚本打印),总求解 ~206 s(闸 3 600 s)。

## 1. 实测(因子-√2 射线,`h_far` 显式 = 120·h_wall)

| 级 | h_wall | h_far | tets | 收敛 | \|R\| | **e(LE 上)** | AR | 求解 s |
|---|---|---|---|---|---|---|---|---|
| S0 | 0.0600 | 7.20 | 9 303 | ✓ | 1.40e-14 | ★ **G-X 无效** | 23.73 | 1 |
| **S1** | 0.0424 | 5.09 | 24 626 | ★ **✗** | **1.26e-03** | — | 24.82 | 1 |
| S2 | 0.0300 | 3.60 | 59 359 | ✓ | 1.76e-11 | **0.376394** | 16.18 | 13 |
| S3 | 0.0212 | 2.55 | 159 533 | ✓ | 6.64e-15 | ★ **G-X 无效** | 8.27 | 40 |
| **S4** | 0.0150 | 1.80 | 448 197 | ✓ | 7.33e-15 | **0.242221** | 6.35 | 151 |

**射线相邻对**:S0→S1 ✗(S1 不收敛)、S1→S2 ✗(同)、S2→S3 **G-X 无效**、S3→S4 **G-X 无效**
⇒ **0 个有效对 ⇒ UNDEFINED。**

## 2. G-X 为什么触发(诊断,零求解 —— 曲线已落盘)

**全部是前缘处的越界,每站 1–2 个实验点**:

| 级 | 站/面 | 实验 x 起点 | 计算曲线 x 起点 | **越界** |
|---|---|---|---|---|
| S0 | η0.65 上 | 0.00042 | 0.00115 | **0.000727 c** |
| S0 | η0.80 上 | 0.00210 | 0.00276 | 0.000662 c |
| S3 | η0.80 下 | 0.00000 | 0.00049 | 0.000494 c |
| S3 | η0.90 上 | 0.00057 | 0.00159 | **0.001020 c** |
| S4 | η0.80 下 | 0.00000 | 0.00010 | 0.000101 c |

★ 机理清楚:**截面切割的第一个点必然落在 x > 0**(它是网格节点,不是几何前缘点),
而实验点最靠前的可以取到 **x = 0.00000**。⇒ 这是**后处理的构造性事实**,不是物理。
★ 五级站点**全部存在**(不是截面失败),所以无效**只**来自这条越界。

★★★ **但我不放宽 G-X。** 它写的是"任何实验横坐标落在范围外 ⇒ 该 (级,带,面) 无效",它按字面触发了。
越界只有 0.0001–0.001 c 这件事**是 RECORDED 的事实,不是重读判据的许可** ——
本季我已两次拒绝这种动作(A2→A3 的替换、14 % vs 15 % 的线),第三次也不做。

★★ 而**正确的修法不是放宽阈值,是改构造**(下一轮,§5):
`np.interp` 在范围外是**夹紧**,所以被夹的点是**假读数**;而如果我"逐点丢掉越界点",
**不同级会丢掉不同的点** ⇒ 两个 d_self 向量就**不是同一个对象**了(第 5 问)。
⇒ 必须用**所有参与级的交集**做一个**公共点集**,在算任何 d_self **之前**固定下来。

## 3. 一个真缺陷,已修(不是判据改动)

我的配对用 `seq`(只含收敛级)的**相邻项** ⇒ **S1 不收敛时,S0→S2 会被当成"相邻对"**,
而那是**因子 2 不是 √2** ⇒ 一条 d_self 链里混两种步长,单调性与观测阶**双双失真**。
已改为**按射线相邻**配对。★ 修完重读(用落盘曲线,零求解):**仍是 0 个有效对,结论不变。**

## 4. ★★ 三条 RECORDED 结果,都不依赖那个被判死的读数

**(1) `h_far` 上限对 LE 带的效应 —— 首次量化。**
同 `h_wall` 0.030,只差 `h_far`:

| | h_far | tets | LE 上 RMS |
|---|---|---|---|
| C_coarse(**默认,被截断**) | 2.50 | 68 624 | 0.372531 |
| **S2(在射线上)** | 3.60 | 59 359 | 0.376394 |

⇒ 截断给出 **+15.6 % 单元**,而 LE 带只读好 **−1.0 %**。
★ 也就是说:那条 **2026-07-13 记录的 clamp 缺陷,对 Richardson 是致命的(不同族),
但它对 LE 带绝对值的影响只有 ~1 %** —— 这两句必须一起说,否则会被读成"缺陷无关紧要"。

**(2) ★★ 可解性沿一条干净射线是非单调的。**
**S1(h_wall 0.0424)不收敛**(|R| 1.26e-03,**0 lim / 0 flr** ⇒ 停滞而非钳制),
而它**上下两级都收敛**(S0 1.4e-14、S2 1.8e-11)。
⇒ 这是 phase two 那句"解在游走"的**求解器侧同伴**:不是"越细越难",是**某些级就是解不动**。
★ 与本项目已记的"加密把峰值推进 `m_cap`"不同 —— 这里 **0 clamps**。

**(3) 网格质量在粗端迅速恶化**:AR **23.73 / 24.82 / 16.18 / 8.27 / 6.35**(S0→S4)。
⇒ 因子-√2 射线的**粗端两级本身质量就差**,而它们正是不收敛/G-X 失效发生的地方 ——
下一轮若还要用粗端,必须先看这一列。

★ 顺带:σ 冻结报警在每条腿上都报 `frozen_in_transient`(与本会话早先的触发率一致),RECORDED。

## 5. ★★★ 下一轮:**零求解**,而且它必须诚实处理"我已经看过一部分数字"

**好消息**:**S0 / S2 / S4 本身就是一条干净的因子-2 射线**(h_wall 0.060/0.030/0.015、
h_far 7.20/3.60/1.80 = 120·h_wall),**三级全部收敛**,且曲线**已落盘**
(`curves_S*.npz`,那层缓存正是为这种情况加的)⇒ **两个射线相邻对,零新求解。**

**必须在预注册里写死的两件事**:

1. **公共点集**:取 S0/S2/S4 **三级都有效**的实验点的**交集**,在算任何 d_self 之前固定;
   **报出被丢掉的点数与它们的位置**(它们全在 x < 0.003 c,来自 §2);
   ★ 这**不是**放宽 G-X,是把它从"整带作废"改成"**同一个对象上的逐点有效性**";
2. ★★★ **诚实约束**:我**已经看过** √2 链的 d_self(0.214298 / 0.250115)。
   ⇒ 下一轮的判据只能建在**因子-2 链**上(那两个数**尚未算过、我没看过**),
   而 √2 链的两个数在下一轮只能作 **RECORDED、不参与判定**。
   ★ 若我把已看过的数拿去当判据,那和事后改阈值是同一件事。

★★ **值得做的理由(而不是结论)**:那两个**无效**的 d_self 是 **0.214 / 0.250**,
而 e 是 **0.242–0.376** —— **量级相当**。若它们在公共点集上仍然如此,读数会落到 **N-DISC**
(解没收敛,根因在离散侧),那会**推翻**"换网格解决不了 M3"这个现有结论。
★ 但它们**现在不能读**:被夹紧的点**恰好落在被测量的那条带里**(前缘),
而那正是误差最大的地方 ⇒ **污染方向未知,不可外推。**

## 6. kill criterion

| 条 | 是否成立 |
|---|---|
| 1. G-R 不过 | 不成立 —— **逐位 PASS** |
| 2. 有效相邻对 < 2 | ★ **成立(0 对)⇒ UNDEFINED,停** |
| 3. G-X 在 binding 带失效 | ★ **成立**(S0/S3)|
| 4. 总墙钟 > 3 600 s / 单级 > 900 s | 不成立(总 ~206 s) |
| 5. 各级逐位相同 | 不成立(e 0.376 vs 0.242) |

★ **按注册,S0 太粗而死不算 kill;但这次死的是 S1(中间级),而注册没预见"中间级死"** ——
如实记下:**注册的风险清单漏了这一种**,而它恰好发生了。
**M3 仍不达标**(沿用前十四轮的豁免)。

## 7. 下一步

1. ★★ **§5 的零求解重读**(公共点集 + 因子-2 链),**另行预注册**,并把"√2 链已被看过"写进注册;
2. ★ **S1 的非收敛值得单独看一眼**:0 clamps 的停滞在一条干净射线的中间级 ——
   它可能是 `descent10`/限制循环那一族(本项目已有分类器),**便宜**;
3. 仍悬着:**Q-B 直解臂**、**coarse 两流水线差 2.0 %**、`wing3d.py` 的 docstring/签名帽型矛盾。
