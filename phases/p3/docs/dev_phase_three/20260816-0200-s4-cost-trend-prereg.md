# 20260816-0200 预注册 — **S4 成本动机在生产级是否成立**:把 P1 从单点值改成**级别趋势**

**在任何代码之前提交**(roadmap 原则 8)。上游 = [20260816-0000 判定](20260816-0000-s4-premise-check-verdict.md) §7 第 1 条;
使用者裁决 2026-08-16:**做**。

## 0. ★★★ 先写下我的预测(我**已经**推断过,不能假装盲测)

上一轮我在判定里写过:"向生产级外推,**P1 很可能落到 ~3× 量级**,而带的下沿是 4"。
⇒ **本轮不是盲测**,处置照 N-DISC 那轮的办法:**把预测和它的反驳方式都写在前面**。

> ★★ **预测:T-DROP** —— P1 随加密**单调下降**,且 **P1(S4) < 4**(带的下沿之外)
> ⇒ **S4 的成本动机在生产级不成立**。
> ★★★ **它可以怎样被推翻**:IBL 的成本若随表面节点**超线性**增长(例如线性代数或行进步数随规模变差),
> 那 P1 可能**维持甚至上升** ⇒ **T-HOLD / T-RISE**,而那会**推翻我上一轮的机理陈述**
> (无粘 ~ 体单元超线性、IBL ~ 表面节点)。**结果是真不确定的。**

## 1. ★★ 关键设计:**墙钟不能用作复现守卫**

本项目已记过 **同一物理、同一机器、5.4× 墙钟差**(G8.2)与 **1.6×/1.9× 的套件差**。
⇒ **G-R 不能建在"S2 的 IBL 还是 133.5 s"上** —— 那条件本身不可复现。
**G-R 改建在非计时量上**:S2 的**表面节点数 3557**、**P2 = 0.1471**(两者都应**精确**复现)。

★ 并且:**两条腿必须在同一次运行、同一进程里跑**,这样两个墙钟**同负载同线程**可比;
运行前后各打印一次 **load average**,★ 若两次相差 > 2× ⇒ **计时判为污染,拒绝判趋势**(kill 条 4)。

## 2. 设计

**腿**:同一次运行里依次 **S2**(h_wall 0.030,59 359 tets)与 **S4**(0.015,**448 197 tets**),
各**一次 IBL 求解**(`n_outer_max = 1`,φ 落盘 ⇒ 无粘零求解),**配方与上一轮逐字相同**。
★ S0 的两个数(P1 25.82×、IBL 23.2 s)取上一轮**已提交 CSV**,作趋势的第三点,**标注为跨运行**。

**无粘侧墙钟**:仍取**本阶段自己的** `task3_selfconvergence.csv`(S2 13.4 s / S4 **153 s**),打印出处。
★★ 注意这本身是**跨运行**的(那些数来自另一次运行)⇒ **P1 的绝对值带这个限定**;
而**趋势**(S2 与 S4 的 P1 之比)受它影响较小,因为两边**同一个 CSV、同一次运行**。

## 3. 五问检查

| 问 | 回答 |
|---|---|
| **1 单边?** | **四条命名**:**T-DROP**(动机在生产级不成立)/ **T-HOLD**(成立)/ **T-RISE**(★ **推翻我的机理陈述**)/ T-MIX |
| **2 自变量改变样本存在性?** | ★ S4 的 IBL 可能超时或失败 ⇒ **趋势 UNDEFINED**,如实报已完成部分(S2 仍可读) |
| **3 阈值绝对还是相对?** | ★ 判据用**已登记的带 [4, 20]** 的**下沿 4**(不新增数字),外加**单调性**(无量纲) |
| **4 匹配的量是输入还是输出?** | 墙钟、节点数、P1、P2 **全是输出**;输入只有级别 |
| **5 两个要比的数是同一个东西吗?** | ★★★ 本轮的核心:**两条 IBL 腿同进程同负载**(可比);**无粘侧两个数同 CSV 同运行**(可比);★ **S0 那点跨运行**,标注;★ 而 **G-R 不用墙钟**(§1) |

## 4. 事前判据(binding = **S4**)

| | 条件 | 判定与后续 |
|---|---|---|
| **T-DROP** | P1(S4) **< 4** 且 P1 随加密**单调下降**(S0 > S2 > S4) | ⇒ ★★ **预测被证实:S4 的成本动机在生产级不成立** ⇒ "做减法"买到的速度远小于第一阶段那个 9× 所暗示的 ⇒ **GS4.1 的收益需要重估**(它可能仍值得做,但**理由要换**:不是省成本,而是**去掉没用上的复杂度/提高稳健性**) |
| **T-HOLD** | P1(S4) ∈ **[4, 20]** | ⇒ **动机在生产级成立** ⇒ GS4.1 按原理由立项 |
| **T-RISE** | P1(S4) **> P1(S2)** | ⇒ ★★★ **我上一轮的机理陈述被推翻**(IBL 是超线性的)⇒ 必须在判定里**明写我错了**,并重新解释 |
| **T-MIX** | 其余(例如 P1(S4) < 4 但非单调) | ⇒ RECORDED,不宣称 |

**必须一并报**
- 三级的 **表面节点数 / BL 未知量 / 体单元数 / IBL 墙钟 / 无粘墙钟 / P1**;
- ★ **派生的标度指数**:IBL 墙钟 vs BL 未知量、无粘墙钟 vs 体单元 —— **报出,不作判据**
  (派生量已两次误导本会话);
- **P2 三级**(顺带,零成本);★ 以及**逐点 |B|/|A| 的 p95/p99 与 >1 的节点数**
  —— 上一轮指出 `max/max` 有盲区,这两个数**顺手补上**(RECORDED,不进判据);
- **运行前后的 load average 与线程数**。

**守卫**:**G-R(非计时)** S2 的表面节点 **3557** 与 **P2 = 0.1471** 精确复现;
**G-φ** 打印缓存 φ 路径与形状;**G-P** 打印无粘墙钟出处;
★ **G-L(负载)** 前后 load average 打印,相差 > 2× ⇒ 计时污染。

## 5. ★★ KILL CRITERION

1. **φ 或网格缓存缺失** ⇒ 停(不重解);
2. **S4 的 IBL 失败或 > 1 800 s** ⇒ **趋势 UNDEFINED**,报已完成部分并停;
3. **G-R(非计时)不过** ⇒ 停下对齐(不是同一条腿);
4. ★ **G-L:前后 load average 相差 > 2×** ⇒ **拒绝判趋势**,只作 RECORDED
   —— 墙钟是标定,本项目已记过 5.4× 的同物理差;
5. ★ **P1(S4) > 100 或 < 0.5** ⇒ 可疑,先查两侧各自量的是什么(沿用上一轮的条 5)。

**明确不算 kill**:四支判定中的**任何**一条;★ 尤其 **T-RISE**(它会推翻我上一轮的机理);
**M3/M4 仍不达标**(沿用前二十二轮的豁免)。

## 6. 本轮不做

- **不改库**、**不跑耦合回路**、**不实现翼身粘性 case**;
- ★ **不在两个工况上重测 P2**(那是上一轮 §7 第 2 条,单独一轮 —— 它需要 M0.5 的 φ,而那**没有缓存**);
- ★★ **不因为 T-DROP 是我的预测就往它靠** —— T-RISE 被写成"我错了并要明写",这条约束为此而设。
