# 20260815-2000 — 判定:**P-MIX —— 我的解释方向对,但只解释了大约一半**;
# ★★★ 而 kill 条 4 触发后查下去,**错在我自己选的对照组**:**整个数据集里没有任何带满足 R ≤ 1/3**

> **零求解**。**G-R 七站逐位 PASS**、**G-I 分区质量守恒 1.44e-16 PASS**。
> ★★ **binding(LE 上,池化)**:`R_inner = 1.4327`(**未收敛**,判据要 ≥1 ✓)、
> `R_outer = **0.9112**`(判据要 ≤1/3 ✗)⇒ **P-MIX,不宣称方向。**
> ★ **方向确实对**:去掉近奇异区后 **d_self 从 0.3927 掉到 0.1919(近一半)**,
> 收敛率 e4/e2 也从 0.743 改善到 0.606;**内区拿着 41.7 % 的点却占 74.9 % 的 d² 质量**(CONC 1.80)。
> ★★ **但外区没有进入收敛带** ⇒ **"非收敛只是前缘近奇异"这个解释是不完整的**,大约只覆盖一半。
> ★★★ **而 kill 条 4 触发了**:对照组 **LE 下** 的外区 R = **1.72**(也未收敛)。按注册我先查实现 ——
> **G-I 精确到 1.44e-16**,而 LE 下的**全带** R 本来就是 **1.74**(0.16416 / 0.09431,取自已提交表)。
> ⇒ **分区实现是干净的;错的是我把 LE 下 / MID 当成"R ≤ 1/3 的对照组"。**
> ★★★ 根因:两轮前我写"LE 下 + MID **正常收敛**",依据是"**d_self 在两对之间缩小**";
> 本轮注册却把这句话当成了"**R ≤ 1/3**"。**两个不同判据,同一句话** —— 第 5 问,这次栽在**判据**上而不是数字上。
> ★ 实测结果:**整个数据集里没有任何 band 的 R ≤ 1/3**(MID 上 0.638 是最好的)。

预注册 [20260815-1800](20260815-1800-nearsingular-partition-prereg.md)(动代码前提交,`ebd0dcb`)。
脚本 `bench/run_task3_nearsingular_partition.py` → `gate_results/task3_nearsingular_partition.csv`。

## 1. 守卫

| 守卫 | 结果 |
|---|---|
| **G-Z** | PASS(零求解) |
| **G-R** | 七站 d_self **逐位** PASS |
| ★★ **G-I** | **PASS,rel 1.44e-16** —— `Σd²(内) + Σd²(外) = Σd²(全带)` 精确成立 ⇒ 分区**互斥且穷尽** |
| **G-N** | 分割点与两区点数逐站报出(见 §2) |
| ★ **kill 条 4(工具自检)** | **触发** —— 处置见 §4(查完的结论是**实现干净、对照组选错**) |

## 2. 分区(binding:`x_split = x_foot(S2)`,较粗一级的声速前脚)

| η | x_split | n_inner | n_outer |
|---|---|---|---|
| 0.20 | 0.01326 | 3 | 4 |
| 0.44 | 0.01358 | 3 | 5 |
| 0.65 | 0.02514 | 3 | 3 |
| 0.80 | 0.02311 | 2 | 3 |
| 0.90 | 0.01978 | 4 | 6 |
| **池化** | — | **15** | **21** |

两侧都 ≥ 10 ⇒ kill 条 3 不触发,判定可读。

## 3. binding 读数:**方向对,覆盖不全**

| 区 | n | e(S2) | e(S4) | **e4/e2** | **d_self** | **R** | 判读 |
|---|---|---|---|---|---|---|---|
| **内(近奇异)** | 15 | 0.369078 | 0.274089 | 0.743 | **0.392700** | ★ **1.4327** | **未收敛** |
| **外** | 21 | 0.347835 | 0.210681 | **0.606** | **0.191962** | **0.9112** | 混合 |
| 全带 | 36 | 0.356840 | 0.239153 | 0.670 | 0.292833 | 1.2245 | 未收敛 |

⇒ **P-MIX**(`R_inner ≥ 1` ✓,但 `R_outer = 0.9112` 不 ≤ 1/3)。

★★ **方向被证实的部分,要如实说清有多强**:
- 去掉内区后 **d_self 减半**(0.3927 → 0.1919);
- **收敛率**从 0.743 改善到 **0.606**;
- **内区以 41.7 % 的点占 74.9 % 的 d² 质量**(CONC **1.80**,CONC_max 2.40 ⇒ 达到可达上限的 75 %)。

★★ **而没被证实的部分同样要说清**:**外区的 R 仍是 0.91,离收敛带(≤1/3)很远**
⇒ **"非收敛 = 前缘近奇异"是不完整的**:近奇异区是**主要贡献者**(约 3/4 的 d² 质量),
但把它整段去掉之后,**剩下的部分依然不收敛**。

★ 逐站(**只报不判**,每区 2–6 点):外区 R = **0.347 / 0.770 / 1.509 / 0.813 / 0.975**(η 0.20→0.90)
⇒ **内侧站的外区确实接近收敛(η0.20 的 0.347 就在带内),而 η0.65–0.90 的外区仍不收敛。**
⇒ 未解释的那一半**偏外侧**。

★ **敏感性版本(2 个单元宽,只报)**:内 R **1.4700** / 外 R **0.6009**(x_split 0.0286–0.0430)。
⇒ **把内区放宽,外区的 R 从 0.91 降到 0.60** —— 与"坏区比声速前脚更宽"一致。
★ 但它**不改判定**(注册明写),而且它本身也**没有进入收敛带**。

## 4. ★★★ kill 条 4 触发的处置:实现干净,**是我选错了对照组**

| 对照(应当"正常收敛") | 区 | n | e(S4) | d_self | **R** |
|---|---|---|---|---|---|
| **LE 下** | 内 | 7 | 0.104318 | 0.183589 | **1.7599** |
| **LE 下** | 外 | 10 | 0.086616 | 0.149061 | ★ **1.7209** |
| MID 上 | 外 | 69 | 0.098318 | 0.062699 | **0.6377** |

按注册,对照组外区 R ≥ 1 ⇒ **先查实现,不先读判定**。查的结果:

1. **G-I 精确成立**(1.44e-16)⇒ 分区**没有丢失或重复任何质量**;
2. ★★★ **LE 下的全带 R 本来就是 1.74**(d_self 0.16416 / e(S4) 0.09431,**两个数都取自已提交的表**)
   ⇒ 它在**分区之前**就不满足 R ≤ 1/3。
⇒ **实现是干净的;错的是"对照组"这个选择。**

★★★ **根因是一次判据层面的第 5 问**:两轮前我写下"**LE 下 + MID 正常收敛**",
依据是"**d_self 在 S0→S2 与 S2→S4 两对之间缩小,且 e 也缩小**" —— 按那个判据它成立。
而本轮注册把同一句话当成了"**R ≤ 1/3**"。**两个不同的收敛判据,共用了同一句中文。**
⇒ 我一直在数字上问"这两个数是同一个东西吗",这次**该问的是"这两个判据是同一个东西吗"**。

★★ **一条实测的、之前没被说出来的事实**:**整个数据集里没有任何 band 的 R ≤ 1/3** ——
最好的是 MID 上的 **0.638**。⇒ **"某些带正常收敛"这个说法,按 R 判据从来没有成立过**;
成立的只是较弱的那句:**它们的 d_self 在缩小**。
★ 这不改变前面几轮的**判定**(它们用的是各自登记的判据),但**改变它们的措辞**,
所以进 §6 的勘误。

## 5. kill criterion

| 条 | 是否成立 |
|---|---|
| 1. G-R 不过 | 不成立(逐位) |
| 2. G-I 不过 | 不成立(**1.44e-16**) |
| 3. 池化 n < 10 | 不成立(15 / 21) |
| 4. 对照组外区 R ≥ 1 ⇒ 先查工具 | ★ **成立并已执行**:工具干净,**对照组选错**(§4) |
| 5. 两区 d_self 相同 | 不成立(0.393 vs 0.192) |

★ **P-MIX 不算 kill**(注册明写三支都不算)。**M3 仍不达标**(沿用前二十轮的豁免)。

## 6. ★ 勘误(措辞,不是判定)

前几轮多处写过"**LE 下 / MID 正常收敛**"。**按当时登记的判据(d_self 缩小 + e 缩小)那是对的**,
但它**不等于** `R ≤ 1/3`,而本轮实测 **LE 下的 R = 1.72–1.76、MID 上 0.638**。
⇒ 那些句子应读作"**它们的相邻级差在缩小**",**不可**读作"它们已收敛"。
**受影响处**:`20260815-0000` 判定 §3 与 §7、`20260815-0400` §0、
本阶段 progress 第 25/26 行、能力边界 2026-08-15 的注记。
★ 已在本文件登记;**逐处标注随本轮提交一并做**。

## 7. 读数汇总(四句话)

1. **P-MIX**:内区 **R 1.43(未收敛)**、外区 **R 0.91(混合)** ⇒ **不宣称方向**;
2. ★★ **解释的方向对但只覆盖一半**:去掉内区使 **d_self 减半**、内区以 41.7 % 的点占 **74.9 %** 的 d² 质量
   ⇒ 近奇异区是**主要贡献者**;**但外区仍不收敛** ⇒ **"只是近奇异"不成立**;
3. ★ 未解释的那一半**偏外侧**(外区 R 逐站 0.347 / 0.770 / 1.509 / 0.813 / 0.975);
4. ★★★ **kill 条 4 抓到的是我自己**:分区实现精确(G-I 1.44e-16),而"LE 下 / MID 是 R≤1/3 的对照组"
   这个前提**从来不成立** —— 两轮前那句"正常收敛"用的是**另一个判据**。
   ⇒ **整个数据集里没有任何 band 的 R ≤ 1/3。**

## 8. 对"下一步"的直接影响(**这是本轮最实际的产出**)

★★ 我上一轮给使用者的三条路里,**① 收口**的前提是"外区正常收敛,所以可以把亏空归给近奇异区并写成固有限制"。
**本轮把这个前提削弱了**:外区 R = 0.91,**没有**正常收敛。
⇒ **① 不能按原样执行**。它现在只能写成一句**更弱但仍有价值**的话:

> **LE 带误差里约 3/4 的逐级变化质量集中在前缘近奇异区(41.7 % 的点),
> 去掉它 d_self 减半;但剩余部分依然不收敛,且偏外侧。**

⇒ 而 **② 治近奇异**(受 D2 阻)与 **③ 转向产品指标(M4 / 速度侧)**不受本轮影响。
★ 我的建议因此**改**为:**不要再往这条线加轮次**了 —— 它已经交出了能交的东西
(位置、机制族、约一半的定量归因),而**继续下去要么撞 D2,要么就是再切一次带**。
**建议走 ③,并把本轮这段话作为 LE 线的收口记录。**
