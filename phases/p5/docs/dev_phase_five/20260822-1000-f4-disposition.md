# 20260822-1000 — **F4 处置的订正**：可以不修，但**我原来的理由是循环的**

**性质**：处置订正（规范性 —— 改变一条登记项的**理由与重开条件**，不改任何测量数）。
**触发**：使用者 2026-08-22：「**F4 不解决可以吗**」。

---

## 1. ★★★ 先撤掉我原来的理由

第二轮与第四轮我都写过：**"F4 的效应 2.5e-07 低于在用的每一条带 ⇒ 只报不修"**。

★★ **第四轮自己把这条推翻了**：那一轮实测 `ETA_TURB` **24 → 96**（闭包输出移动 **5.2e-03**）
**全套件零红** ⇒ **在用的带看不见 5.2e-03**。
⇒ **"低于在用的每一条带"几乎什么都没说** —— **用一把刚被证明看不见东西的尺子，
去论证"它看不见所以没关系"，是循环。**

★ **F5 的新锚也不改变这一点**：它是 `rel = 1e-6`，而 **2.5e-07 仍在它之下**。

## 2. 站得住的理由（三条，**都不依赖测试带**）

| | |
|---|---|
| ★ **① 量级对模型误差** | **2.5e-07** 对型线族对 Blasius 的 **4.5 %** 地板、GV1.1 的 **2 %** 带 ⇒ **低五个数量级**。★ 这条**不引用任何测试带** |
| ★★ **② 它不是任一张表的瓶颈** | 层流侧 binding 确实是 `OUT_SD`，**但只值 2.5e-07**（`n=24`）；★ 湍流侧 binding 是 **`OUT_CDX`**（**1.7e-03**）—— **F4 在那边根本不是问题**（第三轮实测） |
| ★★★ **③ 改它是不可验证的改动** | 会**再动一遍全部层流数** + 又一份勘误清单，**为一个没有任何测试能看见的 2.5e-07** ⇒ 与第四轮对 F6 的结论**同构** |

## 3. 重开条件（**两条，具体**）

| | 条件 |
|---|---|
| ★ **(a)** | 需要层流表**优于 ~1e-7** —— 那时**代数收敛就是硬上限**，**加点数无用**（`n=40` 也才 1.2e-08，且要 40 点） |
| ★★★ **(b)** | **F6 动了那张循环** —— **两张表共用同一个 `n_g` 循环**（第二轮已核），届时做换元**只花一份勘误清单而不是两份** ⇒ **F4 应与 F6 捆绑，不单独做** |

## 4. 修法记下来（免得将来重新推导）

`∫ f(η)·w(η)^{3/2} dη`，`w = 4η(1−η)`。取

> **η = (1 − cos θ)/2** ⇒ `w = sin²θ`，`w^{3/2} = sin³θ`，`dη = ½ sin θ dθ`

⇒ 被积函数变光滑，**端点的平方根型奇异被吸收** ⇒ **收敛从代数回到指数**。

★ **写下来不等于批准去做** —— 它属 §3 的两个条件之一成立时。

## 5. ★ 这条订正**不改任何测量数**

第二/三/四轮的实测（2.5e-07、1.7e-03、5.2e-03、9/30 输出、n-扫全表）**一个都不改写**。
**变的只是 F4 那条登记项的理由与重开条件。**
