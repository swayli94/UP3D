# 20260824-0300 — **第五阶段第二十三轮判定（R22）**：medium 上 `[1,3]` 里**只有一个可信的 `C`** —— 而"收敛 + 0 钳制"这个可用性判据**不足**

**性质**：判定。判据、守卫、kill criterion 逐字来自 [预注册](20260824-0200-r22-prereg.md)（**代码之前提交**）。
4 条新 medium 解，**157.5 s @16 线程**；`pyfp3d/` 零改动。证据 `bench/studies/r22_c_interval/results/`。

---

## 1. 扫掠（medium，α ≡ 1.25，seed ≡ 0；只动 `upwind_c`）

| `C` | conv | 钳制 | 模式 | \|R\| | **Γ** | cl_p | x_shock |
|---|---|---|---|---|---|---|---|
| 1.00 | ✗ | 1135/0 | clamping | 1.63e-02 | +0.798 | (0.858) | (0.979) |
| ★ **1.10** | **✓** | **0/0** | **converged** | ★ **2.28e-13** | ★ **+0.0227** | ★ **0.0403** | 0.657 |
| **1.25** | ✗ | ★ **5217/1543** | clamping | 4.81e+00 | +3.274 | (2.503) | (0.892) |
| **1.50** | ✓ | 0/0 | converged | 2.86e-13 | **+0.1738** | **0.3413** | **0.6006** |
| 2.00 | ✗ | 0/0 | limit_cycle | 4.64e-07 | +0.240 | (0.474) | (0.692) |
| 2.50 | ✗ | 0/0 | limit_cycle | 1.28e-07 | +0.282 | (0.555) | (0.723) |

（括号 = 非收敛态的数，**不得引用**，GS1.4。）

## 2. ★★★ 头条：`C = 1.10` 是一个**收敛的、无钳制的、错的根**

它通过了本项目**所有**现成的可用性检查：`converged` ✓、**0 钳制** ✓、**\|R\| = 2.28e-13** ✓。
**而 Γ = +0.0227 对参照 `C=1.5` 的 +0.1738 —— 差 7.6 倍**，cl_p **0.0403 对 0.3413（8.5 倍）**。
⇒ **它是离散系统的一个真根，只是几乎没有升力。**

★★ **这正是能力边界异常 #1 事前写下的东西**：
> **它不报任何错**；种子回退治不了它；★ **任何"两次独立运行"式的判据在原理上看不到它** ⇒ 需要**答案锚点**。

**本轮是它第一次出现在 `(c)` 这条轴上、在 medium 上、并且被一个我自己写的"可用性"判据放了进来。**
⇒ ★★★ **"收敛 + 0 钳制"不是充分的可用性判据。GS1.4 的钳制契约看不见这一类。**

## 3. 于是 `E-VAR` **作废**，而结论比它原本要给的更强

| 判据 | 如注册 | 审过之后 |
|---|---|---|
| **E-EDGE** | 可用 = **{1.1, 1.5}**，下边缘 ∈ (1.0, 1.1]、上边缘 ∈ [1.5, 2.0) | ★ **1.1 是伪根** ⇒ **真正可信的只有 `{1.5}`** |
| **E-VAR** | 747.69 %（/min）、88.20 %（/max）、157.79 %（/mean） | ★★★ **作废** —— 跨越一个伪根算出来的；**只剩一个可信点 ⇒ 散布 UNDEFINED**（R13 的"少于两条腿的散布叫 UNDEFINED"） |

⇒ ★★★ **`(c)` 在 medium 上不可评估的真正理由，比"两端失败"更硬：
`[1, 3]` 里只有一个 `C` 给出可信解，而一次扫掠至少需要两个。**

## 4. ★★ 两条顺带的硬读数

1. ★★★ **`C` 上非单调**：**`C=1.25` 比 `C=1.0` 差得多**（5217/1543 vs 1135/0，\|R\| 4.81 vs 1.63e-02，
   6 步就崩，Γ = 3.27 = 参照的 19 倍）⇒ **"更多上风量更稳"在这条轴上不成立**。
2. **`C=2.0 / 2.5` 是 limit_cycle 且 Γ（0.240 / 0.282）离参照不远** ——
   与 `C=1.1` 相反：**那两条的答案方向对而收敛不了；`C=1.1` 收敛了而答案错。**
   ⇒ **"收敛"与"正确"在这条轴上是两个独立的失效维度。**

★ **E-XS**：可用集合（含伪根）上 `x_shock` 0.6006–0.6574，**并非全在被提交的参照带 `0.62 ± 0.03` 内**
（★ 参照是 `shock_reference.csv` 的 0.62 ± 0.03，**不是流传于九份文档的 0.61 ± 0.02**）。
★ 而 `C=1.5` 单点 **0.6006 在带内**。

## 5. 事前预测的兑现（**不改预测**）

| # | 预测 | 实测 | |
|---|---|---|---|
| 1 | ★★ 可用区间两端都比 `[1,3]` 窄 | **下边缘 ∈ (1.0,1.1]、上边缘 ∈ [1.5,2.0)** | ✓ |
| 2 | ★★★ `E-VAR` 超过 3 % | ★ **计算值 747.69 % 超过，而判据作废** ⇒ **不可判** | 不可判 |
| 3 | ★ 4 条腿 20–60 min | **157.5 s** | ✓（远低于预期） |

⇒ 本季累计落空 **68.5 次**（本轮 0 落空，但**预测 2 不可判，不记为命中**）。

## 6. 禁止句
① **不得**引用 `E-VAR` 的 747.69 %（跨越伪根）；
② **不得**把 `C=1.1` 当成可用点 —— Γ 差 7.6 倍；
③ **不得**引用非收敛腿的 cl_p / x_shock（表中带括号者）；
④ **不得**说"更多上风量更稳" —— `C=1.25` 比 `C=1.0` 差（§4.1）；
⑤ **不得**据本轮重判 `(c)` —— 规格属于使用者。

## 7. ★★★ 本轮真正的产出：一条**可用性判据**的缺口，以及它的修法已被登记过

**缺口**：`converged ∧ 0 clamps ∧ |R| ~1e-13` **仍可能是错的答案**。
**修法**（能力边界异常 #1 已登记，**本轮第一次在生产轴上证明它必要**）：
**答案锚点** —— 例如把 **Γ 对同工况参照的相对差**加入可用性判据。
★ 具体地：本轮若把可用性加上「**\|Γ − Γ_ref\| / Γ_ref ≤ 0.5**」，`C=1.1` 会被正确地排除，
而 `C=1.5` 保留 ⇒ **一行判据就能修掉它**，而它**十几轮以来一直不在任何脚本里**。

---

# ★★★ 勘误（同日，使用者问「你的分析对吗」后自查）：**§3 的结论是错的 —— 它是 seed 0 的话，而门采两个种子**

## E1. 反证在我这轮之前读过两次的那份已提交 CSV 里

`bench/gate_results/m1_gate_default.csv`（medium）：

| `C` | seed | conv | 钳制 | `cl_p` | |
|---|---|---|---|---|---|
| 1.5 | 0 | ✓ | 0/0 | 0.341340 | ★ 可用 |
| 1.5 | 5 | ✓ | 0/0 | 0.341340 | ★ 可用 |
| ★ **3.0** | ★ **5** | ★ **✓** | ★ **0/0** | ★ **0.289614** | ★ **可用** |
| 1.0 / 3.0 | 0 | ✗ | — | — | |
| 1.0 | 5 | ✗ | 2587/391 | — | |

⇒ ★★★ **seed 5 上有两个可用点** ⇒ **`(c)` 在 medium 上能凑齐，而我写的是"凑不齐"。**
★ 而 `C=3.0/seed5` 收敛这件事**我自己在 R15 里就把它当对照臂测过并写进判定**。

## E2. ★★★ 而凑齐之后，`(c)` 是一个**可测的 FAIL，差约 5–6 倍**

| | `C=1.5` | `C=3.0` | 跨度 |
|---|---|---|---|
| `cl_p`（seed 5） | 0.341340 | 0.289614 | ★ **/min 17.86 % · /max 15.15 % · /mean 16.40 %** |

**门要求 < 3 %。** ★★ 而这是**部分扫掠**（`[1, 1.5)` 无可用点）⇒
**17.86 % 是全区间变化的下界，补点只会更大** ⇒ **部分覆盖不削弱这个 FAIL，只加强它。**

★★★ **而这一切来自已提交数据，零新求解 —— 在 R22 跑之前就可以算出来。**

## E3. 顺带：能力边界那一行也带同一个缺陷

它记「(c) coarse 12.6 % FAIL / **medium 只 1 条收敛腿、无法评估 FAIL**」——
**那是 seed 0 的读数，而没有写明种子**（与"引用门控计数必须带线程数"同族）。
**seed 5 上是两条腿，且可评估。** ⇒ 该行需要就地补上种子限定与本节的数。

## E4. R22 里**仍然成立**的部分

| | |
|---|---|
| ★★★ `C = 1.10` 是**收敛的、0 钳制的、\|R\| 2.3e-13 的错根**（Γ 差 7.6 倍、cl_p 差 8.5 倍、`x_shock` 0.657 出参照带） | **成立，且更重要了** |
| **`C` 上非单调**（`C=1.25` 比 `C=1.0` 差得多） | 成立 |
| ★★ **可用性判据的缺口**：`converged ∧ 0 clamps ∧ \|R\|~1e-13` 仍可能是错答案 ⇒ 需**答案锚点** | 成立，**而 E1 说明它还要再加一条：判据必须跨门自己采样的每一个轴（此处是种子）** |
| `E-VAR` 的 747.69 % | **仍作废**（跨伪根） |

## E5. 我这次错的形式
★★★ **我把一条只在 seed 0 上成立的话，写成了关于 medium 的话** ——
而**种子是门自己采样的轴**，就在同一份 CSV 的同一列里，**我本轮开工前还读过它两次**
（一次查 `C` 值有哪些、一次取 coarse 的 (c) 读数）。
⇒ **常设新增：任何"在 X 上不可评估/只有一个点"的断言，必须先枚举门自己采样的每一个轴** ——
不是"查过那个文件"，是**查过那个文件的每一列**。
