# 20260824-0100 — **自审记录**：R21-d 的分解**作废**，而更严重的是 —— **那团东西与它出发的那个失败已被实测解离**

**性质**：自审（零机时、零求解）。触发：使用者「你再检查一下，看看你的结构是不是正确的，得到了什么结论」。
**不是判定** —— 本文不产生新测量，只审已提交的数据与我据此写下的结论。

---

## 1. 审 #1：R21-d 的"几何 vs 解"分解**是一个尺寸恒等式**

`S_e = ‖φ_e − φ̄‖₂`（势的量纲）、`G_eff = |grad|/S`（1/长度）。在梯度网格上，
对光滑场 `S ~ h·|∇φ|`、`G ~ 1/h` ⇒ **两项按构造反相关，且主要内容是 `h`**。实测（`h = V^{1/3}`）：

| 腿 | corr(log G, log h) | corr(log S, log h) | corr(log G, log S) |
|---|---|---|---|
| a0 / M0.84 | ★ **−0.9909** | ★ **+0.9387** | ★ **−0.9468** |
| C1.0 / α1.25 | **−0.9781** | +0.5327 | −0.5424 |

⇒ **腿 1 上 `log G ≈ −log h`（0.99）、`log S ≈ +log h`（0.94）** ⇒
**那个"分解"实际上是 `log q = (−log h) + log(h·q)`** —— **一个代数恒等式被我当成了物理分解。**
⇒ ★★★ **R21-d 的结论「超额来自解自身的 φ 变化，不是几何放大」不被那个分解支持，作废。**

★ **什么还活着**：`D-GEOM` 的**直接比较**不依赖分解 ——
腿 1 的 ‖B‖ 比 **1.001**、体积比 **0.993** ⇒ **在腿 1 上超声速单元与基率同尺寸、同 ‖B‖，几何确实被排除**。
★★ 而**腿 2 的单元大 1.93 倍 ⇒ 腿 2 被尺寸混淆，它的"一致"是假的一致。**
⇒ **能说的只剩一句：在腿 1 上，几何不是原因。**

## 2. ★★★ 审 #2：**那团东西既不必要也不充分** —— 九轮所追的对象与出发点的失败**解离**

这条线的出发点是 R14：**`(c)` 的下端在 medium 上失败**，而失败的钳制足迹在后缘。
从我**自己已提交的数据**里查：

| | 实测 |
|---|---|
| **有团 + 收敛且零钳制** | ★★★ **3 个格**（R19 网格）：`Γ=0.0/M0.86` **TE sup 325**、`Γ=0.1/M0.84` 10、`Γ=0.1/M0.86` 69 —— 全部 `conv=1, clamps=0/0` ⇒ **团不足以致败** |
| **不收敛 + 无团** | ★★★ **R15 的 `C=3.0/seed0`**：`limit_cycle`、**TE sup 0** ⇒ **团不必要** |

⇒ **两个方向的解离都存在** ⇒ **那团超声速区既不是收敛失败的必要条件，也不是充分条件。**
⇒ ★★★ **R20 / R21-a / R21-d 三轮刻画的对象，与它们要解释的那个失败没有被建立的联系。**

★★ **而这在开 R20 之前就已经可知**：`Γ=0.0/M0.86`（有团、收敛、零钳制）出自**我自己跑的 R19 网格**，
`C=3.0/seed0`（失败、无团）出自 **R15**。**两者都在我的已提交数据里，早于 R20。**
⇒ **与第八至十一轮那次同型（在一个已被作废的前提上继续建），只是这次作废的数据是我自己两轮前产生的。**
★ 常设已有那一条（"开工第一个动作是 `ls` 产物 + 读脚本选项"）**不够** ——
**还要加一条：开一条新线之前，先在自己已有的数据里找它的反例。**

## 3. 所以，实际得到了什么结论（**审过之后**）

**活着的：**
1. **`(c)` 在 medium 上被两个不同的病堵在扫掠两端**：`C=1.0` 两个种子都 `clamping`（1135/0、2587/391），
   `C=3.0/seed0` 是 `limit_cycle`（0 钳制），`C=1.5` 两个种子都干净 ⇒ **两个种子都凑不齐三个 `C`**（R15）。
2. **线程数改变"能不能收敛"，不改变"落在哪个根上"** —— 两边都收敛的 7 个格**逐位相同**（R13）。
3. ★ **`gamma_target` 已在 Newton 路径上**（R19）：默认逐位不变、6 条锁、**套件 580/12/2 恰好闭合**。
   **这是这一段唯一留下的可用工具。**
4. **那个超声速区的刻画本身成立**（作为对象的测量）：**0 / 2146 贴割缝**（R20）、
   **一整团**（P/V 比零假设低 4 倍，R21-a）、**腿 1 上几何被排除**（§1）。
5. **M1 的处置不变**：当前成本下没有开着的路（R12 + 能力边界的处置补记）。

**作废/未建立的：**
| | |
|---|---|
| R21-d 的"几何 vs 解"分解 | **作废**（§1，尺寸恒等式） |
| "那团东西造成 `(c)` 下端失败" | ★★★ **未建立，且已被解离否证**（§2） |
| R18 的 `upM` 与 \|Γ\| 两条分离 | 早已由 R19 撤回 |

## 4. ★ 诚实的总结

**九轮（R14→R21-d）把一个对象测得很清楚，而它不是那个挡路的东西。**
唯一对产品指标有净贡献的是 **R19 的库工具**，以及 **R13/R15 关于 `(c)` 为什么凑不齐格的两条事实**。
★★ **本季累计落空 68.5 次，而这一条不是"落空"，是一条整线的前提失效** —— 单独记在此。
