# 20260822-0600 — **第五阶段第三轮判定（F3）**：湍流侧注释的**两个断言都不成立** —— 只报不修

**性质**：判定。判据、守卫、kill criterion 逐字来自
[预注册](20260822-0500-f3-prereg.md)（**测量代码之前提交**）。**`pyfp3d/` 零改动。**

---

## 0. 一句话

`closures.py:128-130` 的注释有两个可检验的断言，**两个都不成立**：
**② "(unit-tested)" 是假的**（开工前干跑证实：`tests/` 里**没有任何断言**触及 `ETA_TURB`）；
**① "24 点够用"在本轮测到的每一个状态上都不成立** —— 残差 **1.7e-03 ~ 5.2e-03**，
比**同样 24 点的层流表**（2.5e-07）**高三到四个数量级**。
★★ **只报不修**（与第四轮、与 F4 同一处置）。

## 1. 实测（G-FRESH：每腿清 `pyfp3d/viscous/__pycache__`；G-LAMFIX：`ETA_LAM` 全程 24）

对 `n = 96` 参照：

| `n` | `re_δ` 1e4 | 1e5 | 1e6 | 1e7 | binding |
|---|---|---|---|---|---|
| **24** | **1.69e-03** | 1.63e-03 | **5.16e-03** | 2.20e-03 | `OUT_CDX` |
| 40 | 1.44e-05 | 2.38e-04 | 9.68e-04 | 2.02e-03 | `OUT_CDX` |
| 64 | 5.20e-08 | 4.06e-06 | 4.14e-05 | 5.45e-04 | `OUT_CDX` |

⇒ **24 点在这条路径上的每一个状态都留着 1.7e-03 以上**；★ 而**同一个计数在层流表上是 2.5e-07**
（第二轮实测）⇒ **两张表用同一个 24，精度差三到四个数量级。**

## 2. ★★★ 两处仪器缺陷，**都在写判定之前自己抓住**

| # | 缺陷 | 后果 |
|---|---|---|
| ★★ **(i)** | 第一次扫描的**标签不是它量的东西**：腿名写 `re_1e4…re_1e7`，而实际 `re_δ` 是 **10…1e4**，`δ⁺` 只到 **283** —— **比注释声称的 `δ⁺ ~ 1e5` 差近三个数量级**；且最低两腿 `c_f = 1.6`（**不物理**） | 重做，并把这段写进脚本注释 |
| ★★★ **(ii)** | 重做后 `δ⁺` 到 **8.9e3**，**仍未达到 1e5**；且**固定 `A` 只增大 `δ` 不是物理的 Re 扫描** —— `c_f` 按构造 ∝ `1/re_δ`（1.6e-3 → 1.6e-6），而真实湍流边界层的 `c_f` 只随 `log Re` 缓降 | ★ **本轮不声称任何关于 `δ⁺ = 1e5` 的事** |

★★★ **而结论不依赖路径**：**在本轮测到的每一个状态上**，`n=24` 都留着 **≥ 1.7e-03**。
⇒ **"24 点够用"这条断言，在远低于它自己声称的极限处就已经不成立** —— 这比原本能得出的结论**更强**，
**而它不需要我到达 `δ⁺ = 1e5`。**

## 3. T-WHICH：**binding 是 `OUT_CDX`，不是 `OUT_SD`** —— 预测 3 落空

```python
cDc_g = S * ddpsiU_deta + T * ddpsiW_deta     # 相邻的 OUT_CDC
```
`OUT_CDX` 与 `OUT_CDC` 都是**耗散扇区**里带**剖面导数**的量。
★ 我事前预测 binding 是 `OUT_SD`（层流侧那个 `(R·w)^{1.5}`）—— **落空**：
湍流侧被**近壁剖面导数**主导，`w^{3/2}` 的端点奇异**不是这里的瓶颈**。
★★ **这是新面孔，指名如上**（预注册 T-WHICH 的"若 binding 是别的输出 ⇒ 新面孔，指名"生效）。

## 4. 事前预测的兑现（**不改预测**）

| # | 预测 | 实测 | |
|---|---|---|---|
| 1 | ★★★ 误差随 `δ⁺` 增大，`δ⁺ = 1e5` 处 **> 1e-4** | ★ **量偏 / 部分不可判**：误差**在每个状态都 > 1e-3**，**但我没到达 `δ⁺ = 1e5`** ⇒ **不声称那一点** | 部分命中 |
| 2 | ★★ T-N 代数收敛（与层流同因） | ★ **落空** —— `n` 24→40→64 在低 `re_δ` 端下降 **117×/277×**（近指数），且 §3 表明**根本不同因** | **落空** |
| 3 | ★ binding 是 `OUT_SD` | ★ **落空** —— 是 `OUT_CDX` | **落空** |
| 4 | 零 FP 求解，<10 min | ✓ | ✓ |

⇒ 本季累计落空 **46 次**，一次都没有事后改预测。
★★ **预测 2、3 一起落空,而它们错在同一个地方**：我把第二轮的层流结论
（`OUT_SD` 的端点奇异）**按构造迁移**到湍流侧（预注册 §1.1 就是这么写的），
**而迁移是错的** —— 湍流侧的瓶颈是**近壁剖面导数**，不是端点权重。
★★★ **教训：共享一个循环 ≠ 共享同一个瓶颈。** 预注册把它写成"按构造迁移"，
**那句"按构造"就是没有验证的意思**。

## 5. 登记给后续

| | |
|---|---|
| ★★ **F6（新）** | 湍流表 24 点在**每个测到的状态**上留 ≥1.7e-03，binding `OUT_CDX`；★ 与 F4 一样**只报不修**，因为改它会移动**全部湍流侧**的已提交数（本轮**未量**那个半径） |
| ★ **F7（新）** | **注释的两个断言都要改**：删掉"(unit-tested)"（假），并把"够用到 `δ⁺ ~ 1e5`"改成实测能支持的说法 —— ★ 但改注释也要带半径量测（同 F1 的教训）⇒ 与 F6 一并做 |
| **F2** | 承下一轮 |
| **M1** | 承本阶段 |

## 6. 实测口径

- 零 FP 求解；**`pyfp3d/` 逐字节未改**（G-SWAP 验证）；`ETA_LAM` 全程 **24**（G-LAMFIX）。
- 套件**未重跑**（本轮零库改动）⇒ 承 **572 / 12 / 2**。
