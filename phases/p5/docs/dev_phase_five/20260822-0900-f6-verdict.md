# 20260822-0900 — **第五阶段第四轮判定（F6/F5/F7）**：**没有任何已提交的数能约束这个选择** ⇒ 先修覆盖，不改点数

**性质**：判定。判据、守卫、kill criterion 逐字来自
[预注册](20260822-0700-f6-prereg.md) + [附录 #1](20260822-0800-f6-addendum1.md)（**均在对应执行之前提交**）。

---

## 0. 一句话

`ETA_TURB` **24 → 96**（闭包输出移动最大 **5.2e-03**）之后，
**全套件 572 passed / 12 skipped / 2 xfailed，零失败** ⇒
★★★ **`n*` 判 W-UNDEF —— 不是"没有够用的 n"，而是"没有任何已提交的数能约束这个选择"。**
⇒ **本轮不改点数**；★★ **真正可做的是修覆盖（F5），而它已经做完并验证会开火。**

## 1. W-RADIUS / W-UNDEF

| | |
|---|---|
| 改动 | `ETA_TURB` **24 → 96**（`ETA_LAM` 全程 24，G-LAMFIX） |
| 闭包输出的移动 | 最大 **5.2e-03**（第三轮实测） |
| **全套件** | **572 / 12 / 2，0 failed**（1003.09 s @8 线程） |
| ⇒ | ★★★ **移动的断言数 = 0** |

★ **附录 #1 的判据正是为这个结果写的**：`n*` 定义为"使得再提高到 96 时**没有任何已提交断言移动**
的最小 `n`" ⇒ **24 就已经满足** —— **而那说明的不是 24 够好，是没有东西在看。**

★★ **本轮不改点数的理由（不是偷懒）**：
① kill 条 1 禁止"挑一个 `n*` 使套件变绿"——**它已经是绿的**；
② ★★★ **在没有任何测试能看见的情况下改一个数，那个改动是不可验证的** ——
**先让它可见，再谈改。**

## 2. ★★ F5 是本轮真正该做的，而它做完了

**F5 的证据现在有两条，第二条更强**：

| | 测到什么 | 谁没看见 |
|---|---|---|
| 第一轮（F1，层流表） | 闭包输出动 **6.5e-04** | Track V 全部锁 |
| ★★ **本轮（湍流表）** | 闭包输出动 **5.2e-03** | ★ **全套件 572 条，零红** |

**修法照 F5 自己注册的**：**窄锚**，不是收紧所有带（收紧所有带会让无关改动无理由变红）。

新增 `tests/test_gs41_closures_audit.py::TestQuadratureIsVisible`（+2）——
在**一个固定状态**上把 `OUT_CD / OUT_CDX / OUT_CDC / OUT_SD` 以 **`rel = 1e-6`** 钉住，
**比本轮测到的 5.2e-03 紧 5000 倍**。

★★★ **G-TEETH 已验证它真的会开火**：把 `ETA_TURB` 临时改成 40，
该锁 **FIRES**（并已恢复，`closures.py` 逐字节相同）。
★ 这条检查是第四轮审计的教训（"修完加 G-TEETH，注入已知扰动看是否被抓到"）—— **一个锁必须证明它会响。**

★ 锁的 docstring 明写：**它就是要在有人改点数时变红** —— 那时改动会带着**实测半径 + 勘误清单**到来，
**正如 F1 所做**。

## 3. F7：**已修**（在使用者提问后立即）

那两句被实测为假的注释已订正，**逐条**说明为假，并写明"点数**故意没改**，因为半径未测"。
★ **改注释不动任何数** ⇒ **不需要半径**；已用 `git diff` 证明**改动行全是注释行**。

## 4. ★ 一处我在本轮承认并改掉的处置错误

第三轮我把 F6 判成"只报不修"，理由是"没量半径"。★★ **那正是 F1 开场时的同一句话，
而 F1 第一轮的结论恰恰是：去量半径，不是推迟。** ⇒ 本轮量了。
★ 量出来的答案（"没有东西在看"）**比我原以为的更值得知道**，
而它**只有量了才能知道** —— 这正是那条标准存在的理由。

★ **F4 的"只报不修"仍然成立**（成因未除，但效应 2.5e-07 低于在用的每条带）；
**F6 的处置从"只报不修"改为"先修覆盖，改点数待决"**。

## 5. 事前预测的兑现（**不改预测**）

| # | 预测 | 实测 | |
|---|---|---|---|
| 1 | ★★★ W-UNDEF：没有 `n ≤ 96` 够用 | ★ **判定码命中、理由完全相反** —— 是 **W-UNDEF**，但因为**没有约束**，不是因为**不够用** | 命中（理由相反） |
| 2 | ★★ 移动的断言 > 0，且**包含 Track V** | ★ **落空** —— **零红** | **落空** |
| 4（附录） | ★★★ `n=96` 全套件零红 | ★ **命中** | ✓ |
| 5（附录） | 若 4 命中 ⇒ 结论是"约束不了"，且**必须明说没有测试支持** | ★ **照此执行**（§1） | ✓ |
| 3 | 零 FP 求解 | ✓ | ✓ |

⇒ 本季累计落空 **47 次**，一次都没有事后改预测。
★★ **预测 1 的"命中"要打折扣着说**：判定码对了，**而我给的理由是错的**
（我预测"64 仍留 5.5e-04 ⇒ 不够用"，实际是"根本没有带在管它"）。
**判定码相同不等于我说对了** —— 记成"命中（理由相反）"，不记成命中。

## 6. 登记给后续

| | |
|---|---|
| ★ **F6（改状态）** | 从"只报不修"改为 **"覆盖已修，点数待决"** —— 现在改点数**会被锁看见**，届时按 F1 的流程（半径 → 重钉 → 勘误） |
| **F5** | ★ **已修并验证会开火**；★ 但**只锚了 4 个输出、1 个状态** ⇒ 覆盖仍是**局部的**，如实记 |
| **F4 / F2 / M1** | F4 只报不修（理由 §4）；F2、M1 承下一轮 |

## 7. 实测口径

- 零 FP 求解；`closures.py` 本轮**仅注释订正（F7）**，点数**未改**，量测用 `git stash` 式换出并已恢复（逐字节相同）。
- 套件在 `ETA_TURB=96` 下 **572 / 12 / 2, 0 failed**；恢复 24 后 **+2 锁** ⇒ 下一次全套件应为 **574**（本轮**未重跑**，如实标注）。
