# 20260822-1200 — **第五阶段第五轮判定（F2）**：落在 **(B)** —— 而**四条出处主张全部无支撑**

**性质**：判定。判据、守卫、kill criterion 逐字来自
[预注册](20260822-1100-f2-prereg.md)（**测量之前提交**）。**`pyfp3d/` 仅注释订正，值未改。**

---

## 0. 一句话

**分支 (B)**：论文**只描述标定、不给值**（`0.09` 在论文里出现 **0 次**）。
★★★ **而查下来比预期更彻底：注释的四条出处主张，一条都不成立** ⇒
**`C_L = 0.09` 是项目自己的数，没有任何已记录的推导。**

## 1. C-SOURCE：★ **逐字引用 + 页码**（G-QUOTE）

**p.9**（Drela, AIAA 2013-2437）：

> "The outer-layer dissipation length **L** is calibrated so that the dissipation
> integral **D** matches the dissipation implied by **Clauser's G-beta locus**.^{17,18}"

**p.12**：

> "…the dissipation length **L** in the dissipation integral **D_τ**, which is
> **suitably calibrated** to obtain the correct results for the **G-beta locus flows
> of Clauser**.^{17,18}"

★★ **两处都只说"标定过了"，都不给程序、也不给值。** `"0.09"` 全文 **0 次命中**；
参考文献 17/18 是 **Clauser 1954/1956** —— **数据来源，不是那个常数**。

⇒ **落在 (B)。**

## 2. ★★★ 四条主张，逐条查

| # | 注释原来的说法 | 实测 |
|---|---|---|
| 1 | 论文按 Clauser G-beta locus 标定 | ★ **描述属实，但它是 Drela 做的、不是我们做的**；论文**不给值** |
| 2 | "Bradshaw outer-layer value" | ★ **`reference/` 里没有 0.09 的可引出处**（`A1_BRADSHAW = 0.15` 是 Bradshaw 的**应力比**，另一回事） |
| 3 | "2-D-reduction calibration **recorded in the GV1.1 VERDICT**" | ★★ **假** —— 那份 VERDICT 把 `c_l = 0.09` 列在 **"Numerical settings (as run)"** 之下；★ 它提到的"addendum 里的标定表"属于 **`eps_diff_s`**，**不是 `c_l`** |
| 4 | "(D-CT-2)" | ★★★ **该 ID 没有任何设计记录** —— 全仓库只在**这个值旁边**出现（GV1.1 的预注册/判定 + 两个库文件），**没有一处定义它** |

⇒ **不是"标定没做"，而是"这个数没有任何已记录的来历"** —— 比第四轮 A20 的说法**更强**。

## 3. C-COMMENT：**已订正**（★ 只动注释）

新注释把上表四条**逐条**写明，并采用**与第四轮对 `κ, B, r` 相同的处置**：
**论文点了名却没给数 ⇒ 那个数是我们的**。
★ **`git diff` 已证明**：`pyfp3d/` 里除注释外**零改动**（唯一的非注释差异是那一行注释被截短）；
`C_L_DEFAULT` 的**值逐位不变**（G-SENTINEL：0.09）。

## 4. C-CAL：**未做,而这是事前定的**

预注册 §2 事前写死：**"不论差多少都只 RECORDED —— 本轮不改 `C_L` 的值"**。
★ 而落在 (B) 之后**连"差"都不存在** —— **论文没给可比的数**。
★★ 若将来要做真正的 G-beta 标定，那是**新工作**（需 Clauser 的 locus 数据），
且**改 `C_L` 的值要走 F1/F6 的半径流程**（会移动全部 Track V 数）。

## 5. 事前预测的兑现（**不改预测**）

| # | 预测 | 实测 | |
|---|---|---|---|
| 1 | ★★ 落在 (B) | ★ **命中** | ✓ |
| 2 | 若落在 (A)，差 < 50 %（★ 事前标注"我没有把握"） | **前提不成立** ⇒ **不可判** | 不适用 |
| 3 | 零 FP 求解，< 30 min | ✓ | ✓ |

⇒ 本季累计落空 **47 次**（本轮 0 落空），一次都没有事后改预测。
★ **预测 1 的依据也兑现了**：我给的理由是"第四轮已发现论文对 `κ, B, r` 只给符号不给值，
`C_L` 很可能同样" —— **实测正是同一形状**。

## 6. 登记给后续

| | |
|---|---|
| ★ **F2** | **关闭** —— 注释已与实际来源一致；★ **重开条件**：若要把 `C_L` 变成**有出处的**数，需 (i) Clauser locus 数据 + (ii) F1/F6 的半径流程 |
| ★★ **F8（新）** | **`D-CT-2` 这个 ID 没有设计记录** —— ★ 而 `D-CT-1` 同样只在 `closures.py` 里出现 ⇒ **"D-*" 这套 ID 里可能还有别的悬空**，值得一次 grep 普查（零求解） |
| **M1** | 承本阶段（入口 = **亚单元位置仪器**） |

## 7. 实测口径

- 零 FP 求解；`pyfp3d/` **仅注释订正**，`C_L_DEFAULT` **值未改**；
  相关三个测试文件 **30 passed**。★ 套件**未重跑**（无数值改动）⇒ 承 **574 / 12 / 2**。
