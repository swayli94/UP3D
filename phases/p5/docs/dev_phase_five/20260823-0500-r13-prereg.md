# 20260823-0500 — **第五阶段第十三轮预注册（R13）**：把已经测过的三份 CSV **并起来比** —— 零机时

**性质**：预注册。**在任何代码之前提交**。
**执行前置**：[R12 判定 + 勘误 2](20260823-0400-r12-verdict.md) + 能力边界 §3.6。
**零求解、零 `pyfp3d` 改动。**

---

## 0. ★★★ 开工前核事实，范围**再一次缩小了** —— 而这是本轮最该记的一条

我原本要"跑一轮跨种子 × 跨线程诊断"。**去核指称物，发现三件东西都已经存在：**

| 已提交产物 | 是什么 |
|---|---|
| `bench/run_m1_gate.py` 的 **`--third-seed`** | 第三阶段 2026-08-11 就已实现，**含"少于两条收敛腿 ⇒ 散布 UNDEFINED"的样本集守卫**（`:237`）与 worst-spread 打印 |
| `bench/gate_results/m1_gate_default_3seed.csv` | **跨种子 {0,5,12} 已跑过并提交**（18 行，7 收敛） |
| `bench/gate_results/m1_gate_default_8t.csv` | **8 线程已跑过并提交**（12 行，7 收敛），对 `m1_gate_default.csv` 的 16 线程 12 行 / 9 收敛 |

⇒ **两条轴都已经量过。从未做过的是那个"比"** —— 没有人把 16 线程那份与 8 线程那份**并排逐格算差**。
**数据在盘上，join 不在。**

★★★ **而这是今天第三次同一个形状**：
① 第八至十一轮重做了第四阶段第 19 轮；② R12 的 `h` 答案就在网格统计里；③ 本轮的两条轴已在 `gate_results/`。
⇒ **常设新增（本轮的主要产出，先于任何数字）：一轮开工的第一个动作是 `ls` 那个产物目录 + `grep` 那个脚本的 `--help`，
在写任何判据之前。** 我这一天里三次把"读已有的东西"当成了"去测新的东西"。

## 1. 判据（全部由三份已提交 CSV 计算）

### ★★★ R-THREAD（binding，**从未做过的那个比较**）
按 (level, C, seed) 逐格对齐 16 线程与 8 线程，报 `x_shock` 与 `cl_p` 的差。
★★ **样本集先固定**：只比**两边都 `converged=True` 且 0/0 钳制**的格；
一边收敛一边不收敛的格**单独列为"收敛性本身线程依赖"**，**不得**混进散布。

| 结果 | 读法 |
|---|---|
| 任一格 \|Δx_shock\| > **0.0055 c** 或 \|Δcl_p\| > **3 %** | ★ **答案线程依赖，在门自己的族和工况上被确认** |
| 全部格都在容差内 | 线程依赖只体现为**收敛与否**，不体现为**落在哪个根上** |

### R-SEED（binding，复算一个已提交的数）
从 `_3seed.csv` 逐格算跨种子散布（**沿用脚本自己的 UNDEFINED 规则：< 2 条收敛腿不报**）。
| 结果 | 读法 |
|---|---|
| coarse C=1.0 复现 ≈ **2.76 %** | 能力边界那句"用掉 3 % 预算的 92 %"**在门自己的产物上成立** |
| 出现 > 3 % 的格 | **仅换种子就超掉 (b)(c) 的全部预算** |

### ★★ R-BLIND（判据是"门打不打印"，不是"数大不大"）
读 `run_m1_gate.py` 的输出路径：它**是否**打印跨种子散布、**是否**打印跨线程比较。
★ 我上一条消息对使用者说过"门从不跨种子比" —— **本判据要核这句话**，
因为脚本 `:242-261` 看起来**是**打印跨种子 worst spread 的。

| 结果 | 读法 |
|---|---|
| 跨种子**打印**、跨线程**不打印** | 能力边界"门从不跨种子比"这句**已过时**（诊断是第三阶段补的、opt-in）；**真正的缺口是跨线程** |
| 两者都不打印 | 我上一条消息说对了 |

### R-2076（RECORDED）
把 `m1_gate_default.csv` 里 coarse 各 C 的 `x_shock` / `cl_p` 与被提交的 M1 门数并列，
确认本轮读的是同一份产物（**跨来源检查**）。

## 2. 守卫
**G-NOSOLVE**（本轮不 import 任何求解器入口）、**G-FROZEN-LIB**、
★ **G-SCOPE**（上一轮勘误的产物）：**断言用到的每个 level 都在正典阶梯 (xcoarse, coarse, medium) 内** ——
`fine` 行若存在则**排除并打印排除了几行**；
★ **G-PROV**：打印三份 CSV 的路径 + 行数 + 收敛数，**并打印它们各自的线程数来源**（文件名/脚本记录），
不得把两份不同线程数的文件当成一份。

## 3. Kill criterion
1. 任何求解 ⇒ 停。2. 改 `pyfp3d/` ⇒ 停。3. 用到 `fine` 行 ⇒ 停。
4. ★★ **不得据本轮任何数字重判 M1 的门** —— 本轮是**诊断**，M1 的规格属于使用者。
5. ★ 三份 CSV 若在 (level, C, seed) 上无法对齐 ⇒ **报"对不齐"并停**，不得用近似匹配凑格。

## 4. 事前预测
1. ★★ **R-THREAD：至少一格超容差** —— 依据：能力边界记 medium/C=1.5 的 16 vs 8 线程是
   **0.6006 收敛 vs 0.5738 不收敛**；那一格会落进"收敛性线程依赖"而非散布，
   **所以我预测的是另有一格在"两边都收敛"的情况下也超**。★ 可以落空，而且落空很有信息量。
2. ★★★ **R-BLIND：跨种子打印、跨线程不打印** ⇒ **我上一条消息对使用者说的"门从不跨种子比"是错的**，
   而能力边界那句话是**写在诊断被补上之前**的。★ 事前写下来，因为这会是我今天第四次自我订正。
3. ★ **R-SEED：coarse C=1.0 复现 2.76 %**，且我**不预测**是否有格 > 3 %（`_3seed.csv` 只 7/18 收敛，
   多数格可能 UNDEFINED —— 那本身是读数）。
4. 零求解，< 5 min。
