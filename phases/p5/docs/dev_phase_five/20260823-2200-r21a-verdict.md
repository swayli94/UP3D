# 20260823-2200 — **第五阶段第二十一轮判定（R21-a）**：**是一整团**（P/V 比零假设低 4 倍）—— 而我把最强的统计量事前判成了弱的

**性质**：判定。判据、守卫、kill criterion 逐字来自 [预注册](20260823-2100-r21a-prereg.md)（**代码之前提交**）。
**零求解、`pyfp3d/` 零改动**，秒级。证据 `bench/studies/r21a_coherence/results/coherence.csv`。

---

## 1. 测到的（medium，同一带内同尺寸随机零假设 200 次，`seed = 20260823`）

| 腿 | 占比 | **P/V 实测** | **零假设 [p5, p95]** | **分量数** | 零假设 | **最大分量份额** | 零假设 |
|---|---|---|---|---|---|---|---|
| a0 / M0.84（2146） | 0.3226 | ★ **0.7456** | [2.9086, 2.9833] | ★ **2** | 1000–1073 | ★ **0.9995** | 0.0070–0.0135 |
| C1.0 / α1.25（787） | 0.1183 | ★ **1.3215** | [3.5704, 3.6621] | **34** | 618–654 | **0.6544** | 0.0051–0.0102 |

**C-PV：两条腿都远低于零假设 5 % 分位（约 4 倍）⇒ 场结构。两条腿一致。**

## 2. ★★★ 而 `C-COMP` —— 我事前声明"没有判别力"的那条 —— 判别力差了**两个数量级**

**2 个分量 vs 零假设 1000–1073；最大分量份额 0.9995 vs 零假设 0.007–0.0135。**

★★★ **我的理由是错的，而且错在一个可核对的地方**：我写"三维位点渗流阈值约 **0.31**，
而占比 32.3 % 正在阈值上 ⇒ 随机集合本来也会连成一团"。
**0.31 是立方格（配位数 z = 6）的阈值。四面体网格按面相邻，配位数 z = 4**
（钻石格 `p_c ≈ 0.43`）⇒ **32 % 占比在 z = 4 上远未渗流**，所以随机集合是 1000 多个碎块。
⇒ **我用了错格子的阈值**，于是把**最强的统计量降级成了次要的**。
★ 这个错**方向是保守的**（我挑了看起来更弱的主统计量，而它照样成立），**但推理是错的**，如实记。

## 3. 这个结构现在被刻画清楚了

| | a0 / M0.84 | C1.0 / α1.25 |
|---|---|---|
| `x/c` 中位 | **0.904** | 0.914 |
| \|y\| 中位 / 90 % / 最大 | **0.089** / 0.216 / 1.318 | 0.135 / 0.524 / 1.955 |
| \|y\| ≤ 0.06 的份额 | 30.3 % | 13.0 % |

⇒ **一个单连通的紧凑超声速区，坐在后缘略前方（x/c ≈ 0.90）、贴着但不接触割缝，
主体在 \|y\| ~0.1 量级，带一条伸到 \|y\| 1.3–2.0 的尾巴。**
★ 与 R20 合起来：**它避开贴缝层、在带内部按带自身的分布铺开、而且是一整团。**

★★ **顺带一处命名缺陷（R14 起就有）**：我一直叫它 "TE 带"，而它的定义是
**`x/c ∈ [0.80, 1.20]` 的整高薄片**（带内 \|y\| 最大 **14.70**），**不是后缘邻域**。
★ 本轮实测该结构**确实靠近物面**（\|y\| 中位 0.089），**所以没有结论因此改变** ——
但那个名字**七轮以来一直在暗示一件它没有定义的事**。

## 4. ★ `C-FIX` 我又一次"注册了没实现"

预注册 §2 的 `C-FIX`（修 R20 的 `TE-NODE` 分类缺陷）**本轮没有实现**。
★★★ **这是本季第三次同型**：R14 的墙钟 kill 条、R20 的 `M-GEOM`、本轮的 `C-FIX`。
⇒ **常设加强（R14 已记一次，显然不够）：预注册里每一条判据，在脚本里必须有一行对应代码；
写完脚本后逐条对照预注册点名核销，缺的当场记为"未实现"而不是留到判定里才发现。**

## 5. 事前预测的兑现（**不改预测**）

| # | 预测 | 实测 | |
|---|---|---|---|
| 1 | ★★ C-PV 显著低于零假设 ⇒ 场结构 | **两条腿都低约 4 倍** | ✓ |
| 2 | ★★★ C-COMP 判别不出来（渗流阈值） | ★ **判别力差两个数量级，理由用错了格子** | **落空** |
| 3 | ★ C-FIX：`TE-NODE` 仍 0 富集 | ★ **未实现，不可判**（§4） | 不可判 |
| 4 | 零求解 < 3 min | 秒级 | ✓ |

⇒ 本季累计落空 **67.5 次**。

## 6. 禁止句与下一步
① **不得**说"这是椒盐噪声" —— P/V 与分量数都决定性否掉了；
② **不得**再引"三维渗流阈值 0.31"来论证这个网格上的任何事（§2，错格子）；
③ **不得**继续把 `x/c ∈ [0.80, 1.20]` 的整高薄片叫"后缘邻域"（§3）；
④ **不得**宣布因果 —— 本轮仍是刻画。

★ **下一步（登记）**：结构已知是**一整团、在体内部、避开割缝、坐在 x/c ≈ 0.90 / \|y\| ~0.1**。
剩下的因果问题因此变得具体：**是什么让那一团的 `q²` 跨过声速** ——
候选是**人工密度的 donor map 在该区的取向**（`upstream[e]` 指向哪里）与**该区的单元几何**。
两者都可在**已缓存的腿上零求解**测，且**都是可干预的**（donor 深度、`m_crit`）⇒ **R21-d**。
